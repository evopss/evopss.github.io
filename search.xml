<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title></title>
      <link href="/2021/12/24/traefik/traefik-huan-jing-pei-zhi/"/>
      <url>/2021/12/24/traefik/traefik-huan-jing-pei-zhi/</url>
      
        <content type="html"><![CDATA[<h1 id="traefik环境配置"><a href="#traefik环境配置" class="headerlink" title="traefik环境配置"></a>traefik环境配置</h1><h2 id="使用-Docker-Provider-启动-Traefik"><a href="#使用-Docker-Provider-启动-Traefik" class="headerlink" title="使用 Docker Provider 启动 Traefik"></a>使用 Docker Provider 启动 Traefik</h2><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">version</span><span class="token punctuation">:</span> <span class="token string">"3"</span><span class="token key atrule">services</span><span class="token punctuation">:</span>  <span class="token key atrule">reverse-proxy</span><span class="token punctuation">:</span>    <span class="token comment"># The official v2.0 Traefik docker image</span>    <span class="token key atrule">image</span><span class="token punctuation">:</span> traefik<span class="token punctuation">:</span>v2.0    <span class="token comment"># Enables the web UI and tells Traefik to listen to docker</span>    <span class="token key atrule">command</span><span class="token punctuation">:</span> <span class="token punctuation">-</span><span class="token punctuation">-</span>api <span class="token punctuation">-</span><span class="token punctuation">-</span>providers.docker    <span class="token key atrule">ports</span><span class="token punctuation">:</span>      <span class="token comment"># The HTTP port</span>      <span class="token punctuation">-</span> <span class="token string">"80:80"</span>      <span class="token comment"># The Web UI (enabled by --api)</span>      <span class="token punctuation">-</span> <span class="token string">"8080:8080"</span>    <span class="token key atrule">volumes</span><span class="token punctuation">:</span>      <span class="token comment"># So that Traefik can listen to the Docker events</span>      <span class="token punctuation">-</span> /var/run/docker.sock<span class="token punctuation">:</span>/var/run/docker.sock  <span class="token key atrule">whoami</span><span class="token punctuation">:</span>  <span class="token comment"># A container that exposes an API to show its IP address</span>  <span class="token key atrule">image</span><span class="token punctuation">:</span> containous/whoami  <span class="token key atrule">labels</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> <span class="token string">"traefik.http.routers.whoami.rule=Host(`whoami.docker.localhost`)"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/openstack/neutron/neutron/"/>
      <url>/2021/12/24/openstack/neutron/neutron/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/openstack/nova/nova/"/>
      <url>/2021/12/24/openstack/nova/nova/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/openstack/swift/swift/"/>
      <url>/2021/12/24/openstack/swift/swift/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/traefik/traefik/"/>
      <url>/2021/12/24/traefik/traefik/</url>
      
        <content type="html"><![CDATA[<h1 id="traefik"><a href="#traefik" class="headerlink" title="traefik"></a>traefik</h1><blockquote><p>官网：<a href="https://traefik.io/">https://traefik.io/</a><br>github地址：<a href="https://github.com/traefik">https://github.com/traefik</a><br>文档：<a href="https://doc.traefik.io/traefik/%EF%BD%9Chttps://traefik.tech/">https://doc.traefik.io/traefik/｜https://traefik.tech/</a></p></blockquote><h2 id="traefik简介"><a href="#traefik简介" class="headerlink" title="traefik简介"></a>traefik简介</h2><p>Traefik 是一款 <em>边缘路由器</em>，可让您轻松地发布服务. 它接收来自您的系统请求，并找出负责处理它们的后端服务组件。</p><p>traefik 与众不同在于它能够自动发现适合您服务的配置。 当 Traefik 检查您的基础设施时，它会发现相关信息，并发现哪个服务服务于哪个请求。</p><p>Traefik 支持多种集群技术，如 Kubernetes，Kubernetes, Docker, Docker Swarm, AWS, Mesos, Marathon; 并且可以同时处理多个 providers。</p><p>使用 Traefik，无需维护和同步配置文件：所有操作都会自动实时完成（无重启，不用中断服务）。 使用 Traefik，您只需花时间于系统开发和部署新功能，而不是配置和维护其工作状态。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/s2i/s2i-ming-ling-xiang-jie/"/>
      <url>/2021/12/24/s2i/s2i-ming-ling-xiang-jie/</url>
      
        <content type="html"><![CDATA[<h1 id="s2i命令详解"><a href="#s2i命令详解" class="headerlink" title="s2i命令详解"></a>s2i命令详解</h1><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage:  s2i [flags]  s2i [command]Available Commands:  build       构建新镜像  completion  s2i命令行补全(bash or zsh)  create      Bootstrap a new S2I image repository  generate    Generate a Dockerfile using an existing S2I builder       image that can be used to produce an image by any application supporting the format.  rebuild     重新构建存在的镜像  usage       Print usage of the assemble script associated with the image  version     展示版本Flags:      --ca string        Set the path of the docker TLS ca file (default "/Users/andrew.wan/.docker/ca.pem")      --cert string      Set the path of the docker TLS certificate file (default "/Users/andrew.wan/.docker/cert.pem")      --key string       Set the path of the docker TLS key file (default "/Users/andrew.wan/.docker/key.pem")      --loglevel int32   Set the level of log output (0-5)      --tls              Use TLS to connect to docker; implied by --tlsverify      --tlsverify        Use TLS to connect to docker and verify the remote  -U, --url string       Set the url of the docker socket to use (default "unix:///var/run/docker.sock")<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="命令使用"><a href="#命令使用" class="headerlink" title="命令使用"></a>命令使用</h2><ul><li><p><code>s2i build</code>:从源仓库和基础镜像构建名为<tag>的新docker镜像</tag></p><ul><li>source :构建源码。可以是本地仓库，也可以是远程仓库</li><li>image:基础镜像</li><li>tag:生成的镜像</li><li>flags:构建参数</li></ul>  <pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage:  s2i build &lt;source&gt; &lt;image&gt; [&lt;tag&gt;] [flags]Flags:      --add-host stringArray             Specify additional entries to add to the /etc/hosts in the assemble container, multiple --add-host can be used to add multiple entries  -u, --allowed-uids user.RangeList      Specify a range of allowed user ids for the builder and runtime images  -n, --application-name string          Specify the display name for the application (default: output image name)      --as-dockerfile string             EXPERIMENTAL: Output a Dockerfile to this path instead of building a new image      --assemble-runtime-user string     Specify the user to run assemble-runtime with      --assemble-user string             Specify the user to run assemble with      --callback-url string              Specify a URL to invoke via HTTP POST upon build completion      --cap-drop strings                 Specify a comma-separated list of capabilities to drop when running Docker containers      --context-dir string               Specify the sub-directory inside the repository with the application sources  -c, --copy                             Use local file system copy instead of git cloning the source url      --description string               Specify the description of the application  -d, --destination string               Specify a destination location for untar operation      --dockercfg-path string            Specify the path to the Docker configuration file (default "/Users/andrew.wan/.docker/config.json")  -e, --env string                       Specify an single environment variable in NAME=VALUE format  -E, --environment-file string          Specify the path to the file with environment      --exclude string                   Regular expression for selecting files from the source tree to exclude from the build, where the default excludes the '.git' directory (see https://golang.org/pkg/regexp for syntax, but note that "" will be interpreted as allow all files and exclude no files) (default "(^|/)\\.git(/|$)")      --ignore-submodules                Ignore all git submodules when cloning application repository      --image-scripts-url string         Specify a URL containing the default assemble and run scripts for the builder image (default "image:///usr/libexec/s2i")      --incremental                      Perform an incremental build      --incremental-pull-policy string   Specify when to pull the previous image for incremental builds (always, never or if-not-present) (default "if-not-present")  -i, --inject string                    Specify a directory to inject into the assemble container      --keep-symlinks                    When using '--copy', copy symlinks as symlinks. Default behavior is to follow symlinks and copy files by content  -l, --location string                  DEPRECATED: Specify a destination location for untar operation      --network string                   Specify the default Docker Network name to be used in build process  -p, --pull-policy string               Specify when to pull the builder image (always, never or if-not-present) (default "if-not-present")  -q, --quiet                            Operate quietly. Suppress all non-error output.  -r, --ref string                       Specify a ref to check-out      --rm                               Remove the previous image during incremental builds      --run                              Run resulting image as part of invocation of this command  -a, --runtime-artifact string          Specify a file or directory to be copied from the builder to the runtime image      --runtime-image string             Image that will be used as the base for the runtime image      --runtime-pull-policy string       Specify when to pull the runtime image (always, never or if-not-present) (default "if-not-present")      --save-temp-dir                    Save the temporary directory used by S2I instead of deleting it      --scripts string                   DEPRECATED: Specify a URL for the assemble and run scripts  -s, --scripts-url string               Specify a URL for the assemble, assemble-runtime and run scripts      --use-config                       Store command line options to .s2ifile  -v, --volume stringArray               Specify a volume to mount into the assemble container<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>2i create</code>:</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage:  s2i create &lt;imageName&gt; &lt;destination&gt; [flags]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><code>s2i generate</code></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage:  s2i generate &lt;builder image&gt; &lt;output file&gt; [flags]Examples:# Generate a Dockerfile for the centos/nodejs-10-centos7 builder image:$ s2i generate docker.io/centos/nodejs-10-centos7 Dockerfile.genFlags:      --assemble-runtime-user string   Specify the user to run assemble-runtime with      --assemble-user string           Specify the user to run assemble with  -e, --env string                     Specify an single environment variable in NAME=VALUE format  -h, --help                           help for generate  -q, --quiet                          Operate quietly. Suppress all non-error output<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>s2i rebuild</code></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage:  s2i rebuild &lt;image&gt; [&lt;new-tag&gt;] [flags]Flags:      --callback-url string              Specify a URL to invoke via HTTP POST upon build completion  -d, --destination string               Specify a destination location for untar operation      --dockercfg-path string            Specify the path to the Docker configuration file (default "/Users/andrew.wan/.docker/config.json")      --incremental                      Perform an incremental build      --incremental-pull-policy string   Specify when to pull the previous image for incremental builds (always, never or if-not-present) (default "if-not-present")  -p, --pull-policy string               Specify when to pull the builder image (always, never or if-not-present) (default "if-not-present")  -q, --quiet                            Operate quietly. Suppress all non-error output.      --rm                               Remove the previous image during incremental builds      --runtime-pull-policy string       Specify when to pull the runtime image (always, never or if-not-present) (default "if-not-present")      --save-temp-dir                    Save the temporary directory used by S2I instead of deleting it<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>s2i usage</code></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage:  s2i usage &lt;image&gt; [flags]Flags:      --callback-url string              Specify a URL to invoke via HTTP POST upon build completion  -d, --destination string               Specify a destination location for untar operation      --dockercfg-path string            Specify the path to the Docker configuration file (default "/Users/andrew.wan/.docker/config.json")      --incremental                      Perform an incremental build      --incremental-pull-policy string   Specify when to pull the previous image for incremental builds (always, never or if-not-present) (default "if-not-present")  -l, --location string                  Specify a destination location for untar operation  -p, --pull-policy string               Specify when to pull the builder image (always, never or if-not-present) (default "if-not-present")  -q, --quiet                            Operate quietly. Suppress all non-error output.      --rm                               Remove the previous image during incremental builds      --runtime-pull-policy string       Specify when to pull the runtime image (always, never or if-not-present) (default "if-not-present")      --save-temp-dir                    Save the temporary directory used by S2I instead of deleting it<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/openstack/keystone/keystone/"/>
      <url>/2021/12/24/openstack/keystone/keystone/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/openstack/glance/glance/"/>
      <url>/2021/12/24/openstack/glance/glance/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/notebook/obsidian/obsidian/"/>
      <url>/2021/12/24/notebook/obsidian/obsidian/</url>
      
        <content type="html"><![CDATA[<h1 id="obsidian"><a href="#obsidian" class="headerlink" title="obsidian"></a>obsidian</h1><blockquote><p>官网：<a href="https://obsidian.md/">https://obsidian.md/</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/openstack/horizon/horizon/"/>
      <url>/2021/12/24/openstack/horizon/horizon/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/openstack/cinder/cinder/"/>
      <url>/2021/12/24/openstack/cinder/cinder/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/openstack/kolla/kolla/"/>
      <url>/2021/12/24/openstack/kolla/kolla/</url>
      
        <content type="html"><![CDATA[<h1 id="colla"><a href="#colla" class="headerlink" title="colla"></a>colla</h1><blockquote><p>github 地址：<a href="https://github.com/openstack/kolla">https://github.com/openstack/kolla</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/kustomize/kustomize-ming-ling-xing/"/>
      <url>/2021/12/24/kustomize/kustomize-ming-ling-xing/</url>
      
        <content type="html"><![CDATA[<h1 id="Kustomize命令行"><a href="#Kustomize命令行" class="headerlink" title="Kustomize命令行"></a>Kustomize命令行</h1><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage:  kustomize [command]Available Commands:  build                     Build a kustomization target from a directory or URL.  cfg                       Commands for reading and writing configuration.  completion                Generate shell completion script  create                    Create a new kustomization in the current directory  edit                      Edits a kustomization file  fn                        Commands for running functions against configuration.  help                      Help about any command  version                   Prints the kustomize versionFlags:  -h, --help          help for kustomize      --stack-trace   print a stack-trace on errorAdditional help topics:  kustomize docs-fn                   [Alpha] Documentation for developing and invoking Configuration Functions.  kustomize docs-fn-spec              [Alpha] Documentation for Configuration Functions Specification.  kustomize docs-io-annotations       [Alpha] Documentation for annotations used by io.  kustomize docs-merge                [Alpha] Documentation for merging Resources (2-way merge).  kustomize docs-merge3               [Alpha] Documentation for merging Resources (3-way merge).  kustomize tutorials-command-basics  [Alpha] Tutorials for using basic config commands.  kustomize tutorials-function-basics [Alpha] Tutorials for using functions.<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/kustomize/kustomize-api/"/>
      <url>/2021/12/24/kustomize/kustomize-api/</url>
      
        <content type="html"><![CDATA[<h1 id="Kustomize-API"><a href="#Kustomize-API" class="headerlink" title="Kustomize API"></a>Kustomize API</h1><h2 id="commonAnnotations"><a href="#commonAnnotations" class="headerlink" title="commonAnnotations"></a><strong>commonAnnotations</strong></h2><p>为所有资源添加注释，如果资源上已经存在注解键，该值将被覆盖。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">apiVersion</span><span class="token punctuation">:</span> kustomize.config.k8s.io/v1beta1<span class="token key atrule">kind</span><span class="token punctuation">:</span> Kustomization<span class="token key atrule">commonAnnotations</span><span class="token punctuation">:</span>  <span class="token key atrule">oncallPager</span><span class="token punctuation">:</span> 800<span class="token punctuation">-</span>555<span class="token punctuation">-</span><span class="token number">1212</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h2 id="commonLabels"><a href="#commonLabels" class="headerlink" title="commonLabels"></a><strong>commonLabels</strong></h2><p>为所有资源和 selectors 增加标签。如果资源上已经存在注解键，该值将被覆盖。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">apiVersion</span><span class="token punctuation">:</span> kustomize.config.k8s.io/v1beta1<span class="token key atrule">kind</span><span class="token punctuation">:</span> Kustomization<span class="token key atrule">commonLabels</span><span class="token punctuation">:</span>  <span class="token key atrule">someName</span><span class="token punctuation">:</span> someValue  <span class="token key atrule">owner</span><span class="token punctuation">:</span> alice  <span class="token key atrule">app</span><span class="token punctuation">:</span> bingo<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="configMapGenerator"><a href="#configMapGenerator" class="headerlink" title="configMapGenerator"></a><strong>configMapGenerator</strong></h2><h2 id="crds"><a href="#crds" class="headerlink" title="crds"></a><strong>crds</strong></h2><h2 id="generatoroptions"><a href="#generatoroptions" class="headerlink" title="generatoroptions"></a><strong>generatoroptions</strong></h2><h2 id="images"><a href="#images" class="headerlink" title="images"></a><strong>images</strong></h2><h2 id="namePrefix"><a href="#namePrefix" class="headerlink" title="namePrefix"></a><strong>namePrefix</strong></h2><ul><li><p><strong>namespace</strong></p></li><li><p><strong>nameSuffux</strong></p></li><li><p><strong>patches</strong></p></li><li><p><strong>replicas</strong></p></li><li><p><strong>secretGenerator</strong></p></li><li><p><strong>vars</strong></p></li><li></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/gitlab/gitlab-cicd/gitlabci/"/>
      <url>/2021/12/24/gitlab/gitlab-cicd/gitlabci/</url>
      
        <content type="html"><![CDATA[<h1 id="gitlabci"><a href="#gitlabci" class="headerlink" title="gitlabci"></a>gitlabci</h1><h2 id="gitlab-ci简介"><a href="#gitlab-ci简介" class="headerlink" title="gitlab ci简介"></a>gitlab ci简介</h2><ul><li>GitLab提交持续集成服务，当你在项目根目录中添加 <code>.gitlab-ci.yml</code> 文件，并配置项目的运行器( <code>GitLab Runner</code> )，那么后续的每次提交都会触发CI流水线( <code>pipeline</code> )的执行。</li><li>Job作业是 <code>.gitlab-ci.yml</code> 文件的基本元素，每个作业至少有 <code>script</code> 子句，在流水线中可以定义任意多个作业。</li><li>每个作业必须具有唯一的名称，但有一些保留的关键字不能用作作业名称</li><li>在流水线脚本中可以使用预定义的全局变量</li></ul><h2 id="gitlabci语法"><a href="#gitlabci语法" class="headerlink" title="gitlabci语法"></a>gitlabci语法</h2><h3 id="gitlab保留字"><a href="#gitlab保留字" class="headerlink" title="gitlab保留字"></a>gitlab保留字</h3><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">imageservicesstagestypesbefore_scriptafter_scriptvariablescache<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="配置参数"><a href="#配置参数" class="headerlink" title="配置参数"></a>配置参数</h3><p>定义 job 行为的参数列表</p><table><thead><tr><th>Keyword</th><th>描述</th></tr></thead><tbody><tr><td>script</td><td>由运行程序执行的 shell 脚本。</td></tr><tr><td>image</td><td>使用 Docker镜像。还提供：image:name 和 image:entrypoint。</td></tr><tr><td>services</td><td>使用 Docker 服务镜像。还提供：services:name、services:alias、services:entrypoint 和 services:command。</td></tr><tr><td>before_script</td><td>重写在 job 之前执行的一组命令。</td></tr><tr><td>after_script</td><td>重写在 job 后执行的一组命令。</td></tr><tr><td>stages</td><td>定义管道中的阶段。</td></tr><tr><td>stage</td><td>定义 job 阶段（default: test）。</td></tr><tr><td>only</td><td>限制创建 when jobs。还可用：only：refs，only：kubernetes，only：variables，only：changes。</td></tr><tr><td>except</td><td>限制未创建 when jobs。还可用：except:refs，except:kubernetes，except:variables 和 except:changes。</td></tr><tr><td>tags</td><td>用于选择运行程序的标记列表。</td></tr><tr><td>allow_failure</td><td>允许 job 失败。失败的 job 不影响提交状态。</td></tr><tr><td>when</td><td>何时运行 job。还可用：when:manual and when:delayed。</td></tr><tr><td>environment</td><td>job 部署到的环境的名称。还可用：environment:name, environment:url, environment:on_stop, and environment:action.</td></tr><tr><td>cache</td><td>在后续运行之间应缓存的文件列表。还可用：cache:paths, cache:key, cache:untracked, and cache:policy.。</td></tr><tr><td>artifacts</td><td>成功时要附加到 job 的文件和目录列表。还可用：artifacts:paths, artifacts:name, artifacts:untracked, artifacts:when, artifacts:expire_in, artifacts:reports, and artifacts:reports:junit。在 Gitlab 企业版中，这些可用：artifacts:reports:codequality, artifacts:reports:sast, artifacts:reports:dependency_scanning, artifacts:reports:container_scanning, artifacts:reports:dast, artifacts:reports:license_management, artifacts:reports:performance and artifacts:reports:metrics。</td></tr><tr><td>dependencies</td><td>job 所依赖的其他 job，以便在它们之间传递 artifacts。</td></tr><tr><td>coverage</td><td>给定 job 的代码覆盖率设置。</td></tr><tr><td>retry</td><td>如果出现故障，一个 job 可以自动重试的时间和次数。</td></tr><tr><td>parallel</td><td>一个 job 应并行运行多少个实例。</td></tr><tr><td>trigger</td><td>定义下游管道触发器。</td></tr><tr><td>include</td><td>允许此 job 包含外部 yaml 文件。还提供：include:local、include:file、include:template 和 include:remote。</td></tr><tr><td>extends</td><td>此 job 将从中继承的配置项。</td></tr><tr><td>pages</td><td>上载 job 的结果以用于 Gitlab 页面。</td></tr><tr><td>variables</td><td>在 job 级别定义 job 变量。</td></tr></tbody></table><h3 id="默认参数"><a href="#默认参数" class="headerlink" title="默认参数"></a>默认参数</h3><p>某些参数可以全局设置为所有 job 的默认值，使用默认值：关键字。然后，默认参数可以被特定于 job 的参数覆盖配置。</p><p>可以在默认块内定义以下 job 参数：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">imageservicesbefore_scriptafter_scriptcache<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 默认块</span><span class="token key atrule">default</span><span class="token punctuation">:</span>  <span class="token key atrule">image</span><span class="token punctuation">:</span> ruby<span class="token punctuation">:</span><span class="token number">2.5</span><span class="token key atrule">rspec</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> bundle exec rspec<span class="token key atrule">rspec 2.6</span><span class="token punctuation">:</span>  <span class="token key atrule">image</span><span class="token punctuation">:</span> ruby<span class="token punctuation">:</span><span class="token number">2.6</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> bundle exec rspec<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="参数详解"><a href="#参数详解" class="headerlink" title="参数详解"></a>参数详解</h3><h4 id="script"><a href="#script" class="headerlink" title="script"></a><strong>script</strong></h4><p><code>script</code> 是作业中唯一必须的关键字参数，是运行器需要执行的脚本</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> echo "hello world"<span class="token key atrule">build</span><span class="token punctuation">:</span><span class="token key atrule">script</span><span class="token punctuation">:</span> <span class="token comment"># 使用数组的多个命令</span><span class="token punctuation">-</span> echo "hello world"<span class="token punctuation">-</span> uname <span class="token punctuation">-</span>a<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>注意：使用冒号时应使用引号包裹起来(yaml 解析器知道将整个事件解释为字符串而不是 “key:value” 对。)，使用特殊字符时需要特别注意！</p><p>注意如果要输出冒号字符，冒号后面不能紧接空格！</p></blockquote><h4 id="image"><a href="#image" class="headerlink" title="image"></a><strong>image</strong></h4><p><code>image</code> 指定使用Docker镜像</p><ul><li>name 名称，如果只有一个名字，可以省略</li><li>entrypoint docker拉取后执行的命令</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">image</span><span class="token punctuation">:</span>  <span class="token key atrule">name</span><span class="token punctuation">:</span> <span class="token string">"registry.example.com/my/image:latest"</span>  <span class="token key atrule">image</span><span class="token punctuation">:</span>  <span class="token key atrule">name</span><span class="token punctuation">:</span> super/sql<span class="token punctuation">:</span>experimental  <span class="token key atrule">entrypoint</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"/bin/sh"</span><span class="token punctuation">,</span> <span class="token string">"-c"</span><span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="services"><a href="#services" class="headerlink" title="services"></a><strong>services</strong></h4><p><code>services</code> 指定使用Docker镜像服务</p><ul><li>alias 服务别名</li><li>name 名称，如果只有一个名字，可以省略</li><li>entrypoint 拉取镜像启动容器后，要执行的命令</li><li>command 要执行的命令</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">services</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> my<span class="token punctuation">-</span>postgres<span class="token punctuation">:</span><span class="token number">11.7</span>    <span class="token key atrule">alias</span><span class="token punctuation">:</span> db<span class="token punctuation">-</span>postgres    <span class="token key atrule">entrypoint</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"/usr/local/bin/db-postgres"</span><span class="token punctuation">]</span>    <span class="token key atrule">command</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"start"</span><span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="before-script"><a href="#before-script" class="headerlink" title="before_script"></a><strong>before_script</strong></h4><p><code>before_script</code> 用于定义在所有作业之前需要执行的命令</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">before_script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> echo "Before script section"<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h4 id="after-script"><a href="#after-script" class="headerlink" title="after_script"></a><strong>after_script</strong></h4><p><code>after_script</code> 用于定义在所有作业(即使失败)之后需要执行的命令</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">after_script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> echo "After script section"<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p><strong>特点：</strong></p><ul><li>before_script和script在一个上下文中是串行执行的，after_script是独立执行的，即after_script与before_script/script的上下文环境不同。</li><li>after_script会将当前工作目录设置为默认值。</li><li>在after_script中无法看到在before_script和script中所做的修改：<ul><li>在before_script和script中的命名别名、导出变量，对after_script不可见</li><li>before_script和script在工作树之外安装的软件，对after_script不可见</li></ul></li><li>你可以在作业中定义before_script，after_script，也可以将其定义为顶级元素，定义为顶级元素将为每一个任务都执行相应阶段的脚本或命令。<strong>作业级会覆盖全局级的定义。</strong></li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">before_script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> echo "Before script section"<span class="token key atrule">after_script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> echo "After script section"<span class="token key atrule">build1</span><span class="token punctuation">:</span><span class="token key atrule">stage</span><span class="token punctuation">:</span> build<span class="token key atrule">before_script</span><span class="token punctuation">:</span> <span class="token punctuation">-</span> yum install cloc <span class="token punctuation">-</span>y<span class="token key atrule">after_script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> cloc <span class="token punctuation">-</span><span class="token punctuation">-</span>version<span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> cloc <span class="token punctuation">-</span><span class="token punctuation">-</span>version<span class="token key atrule">tags</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> bluelog<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="stages"><a href="#stages" class="headerlink" title="stages"></a><strong>stages</strong></h4><p><code>stages</code> 定义流水线全局可使用的阶段，阶段允许有灵活的多级管道，<strong>阶段元素的排序定义了作业执行的顺序</strong></p><ul><li>相同 <code>stage</code> 阶段的作业并行运行</li><li>默认情况下，上一阶段的作业全部运行成功后才执行下一阶段的作业</li><li>默认有三个阶段， <code>build</code> 、<code>test</code> 、<code>deploy</code> 三个阶段</li><li>如果一个作业未定义 <code>stage</code> 阶段，则作业使用 <code>test</code> 测试阶段</li><li>默认情况下，任何一个前置的作业失败了，commit提交会标记为failed并且下一个stages的作业都不会执行</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">stages</span><span class="token punctuation">:</span><span class="token punctuation">-</span> build<span class="token punctuation">-</span> test<span class="token punctuation">-</span> build_image<span class="token punctuation">-</span> deploy<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="stage"><a href="#stage" class="headerlink" title="stage"></a><strong>stage</strong></h4><p><code>stage</code> 定义流水线中每个作业所处的阶段，处于相同阶段的作业并行执行</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">Build Image</span><span class="token punctuation">:</span><span class="token key atrule">stage</span><span class="token punctuation">:</span> build_image<span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>默认情况下，GitLab Runner运行器每次只执行一个作业，只有当满足以下条件之一时，才会真正的并行执行:</p><ul><li>作业运行在不同的运行器上；</li><li>你修改了运行器的 <code>concurrent</code> 设置，默认情况下 <code>concurrent = 1</code> 。</li></ul></blockquote><h4 id="only-except"><a href="#only-except" class="headerlink" title="only/except"></a><strong>only/except</strong></h4><p><code>only</code> 和 <code>except</code> 用于在创建作业时对作业的限制策略。</p><ul><li><code>only</code> 定义了哪些分支或标签(branches and tags)的作业会运行,其默认为 <code>only: ['branches', 'tags']</code></li><li><code>except</code> 定义了哪些分支或标签(branches and tags)的作业不会运行,默认 <code>except</code> 规则为空</li></ul><p><strong>策略规则：</strong></p><ul><li><code>only</code> 和 <code>except</code> 可同时使用，如果在一个作业中同时定义了 <code>only</code> 和 <code>except</code> ，则同时 <code>only</code> <code>except</code> 进行过滤(注意，不是忽略 <code>except</code> 条件) 。</li><li><code>only</code> 和 <code>except</code> 可以使用正则表达式。</li><li><code>only</code> 和 <code>except</code> 允许指定用于过滤forks作业的存储库路径。</li><li><code>only</code> 和 <code>except</code> 中可以使用特殊的关键字</li></ul><p><strong><code>only</code> 和 <code>except</code> 中可以使用特殊的关键字</strong>：</p><table><thead><tr><th>关键字</th><th>描述释义</th></tr></thead><tbody><tr><td>branches</td><td>当一个分支被push上来</td></tr><tr><td>tags</td><td>当一个打了tag标记的Release被提交时</td></tr><tr><td>api</td><td>当一个pipline被第二个piplines api所触发调起(不是触发器API)</td></tr><tr><td>external</td><td>当使用了GitLab以外的外部CI服务，如Jenkins</td></tr><tr><td>pipelines</td><td>针对多项目触发器而言，当使用CI_JOB_TOKEN， 并使用gitlab所提供的api创建多个pipelines的时候</td></tr><tr><td>pushes</td><td>当pipeline被用户的git push操作所触发的时候</td></tr><tr><td>schedules</td><td>针对预定好的pipline计划而言（每日构建一类）</td></tr><tr><td>triggers</td><td>用触发器token创建piplines的时候</td></tr><tr><td>web</td><td>在GitLab WEB页面上Pipelines标签页下，按下run pipline的时候</td></tr><tr><td>merge_requests</td><td>当合并请求创建或更新的时候</td></tr><tr><td>chats</td><td>当使用GitLab ChatOps 创建作业的时候</td></tr></tbody></table><p><strong>使用示例：</strong></p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># job将只会运行以 issue- 开始的refs(分支)，然而except中指定分支不能执行，</span><span class="token comment"># 所以这个job将不会执行</span><span class="token key atrule">job</span><span class="token punctuation">:</span><span class="token key atrule">only</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> /^issue<span class="token punctuation">-</span>.<span class="token important">*$/</span><span class="token key atrule">except</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> branches<span class="token comment"># 匹配模式默认是大小写敏感的(case-sensitive)，使用 i 标志，</span><span class="token comment"># 如 /pattern/i 可以使匹配模式大小写不敏感</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> /^issue<span class="token punctuation">-</span>.<span class="token important">*$/i</span>  <span class="token key atrule">except</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> branches<span class="token comment"># 仅当指定标记的tags的refs引用，或者通过API触发器的构建、或者流水线计划调度的构建才会运行</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> tags    <span class="token punctuation">-</span> triggers    <span class="token punctuation">-</span> schedules<span class="token comment"># 仓库的路径(repository path)只能用于父级仓库执行作业，不能用于forks</span><span class="token comment"># 将会在所有分支执行，但 不会在 master主干以及以release/开头的分支上执行</span><span class="token key atrule">job</span><span class="token punctuation">:</span><span class="token key atrule">only</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> branches@gitlab<span class="token punctuation">-</span>org/gitlab<span class="token punctuation">-</span>ce<span class="token key atrule">except</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> master@gitlab<span class="token punctuation">-</span>org/gitlab<span class="token punctuation">-</span>ce  <span class="token punctuation">-</span> /^release/.<span class="token important">*$/@gitlab-org/gitlab-ce</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>正则表达式使用的说明:</strong></p><ul><li>因为 <code>@</code> 用于表示ref的存储库路径的开头，所以在正则表达式中匹配包含 <code>@</code> 字符的ref名称需要使用十六进制字符代码 <code>\x40</code> 。</li><li>仅标签和分支名称才能使用正则表达式匹配，仓库路径按字面意义匹配</li><li>如果使用正则表达式匹配标签或分支名称，则匹配模式的整个引用部分都是正则表达式</li><li>正则表达式必须以 <code>/</code> 开头和结尾，即 <code>/regular expressions/</code> ，因此， <code>issue-/.*/</code> 不会匹配以 <code>issue-</code> 开头的标签或分支。</li><li>可以在正则表达式中使用锚点 <code>^$</code> ，用来匹配开头或结尾，如 <code>/^issue-.*$/</code> 与 <code>/^issue-/</code> 等价， 但 <code>/issue/</code> 却可以匹配名称为 <code>severe-issues</code> 的分支，所以正则表达式的使用要谨慎！</li></ul><p><strong><code>only</code> 和 <code>except</code> 高级用法</strong></p><ul><li><p><code>only</code> 和 <code>except</code> 支持高级策略，<code>refs</code> 、 <code>variables</code> 、 <code>changes</code> 、 <code>kubernetes</code> 四个关键字可以使用。</p></li><li><p>如果同时使用多个关键字，中间的逻辑是 <code>逻辑与AND</code> 。</p></li><li><p>refs 策略可以使用 only 和 except 基本用法中的关键字</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># only:refs/except:refs</span><span class="token key atrule">deploy</span><span class="token punctuation">:</span> <span class="token comment"># deploy作业仅当流水线是计划作业或者在master主干运行</span>  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">refs</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> master      <span class="token punctuation">-</span> schedules<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>kubernetes</code> 策略仅支持 <code>active</code> 关键字</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># only:kubernetes/except:kubernetes</span><span class="token key atrule">deploy</span><span class="token punctuation">:</span> <span class="token comment"># deploy作业仅当kubernetes服务启动后才会运行</span>  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">kubernetes</span><span class="token punctuation">:</span> active<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>variables</code> 关键字用来定义变量表达式，你可以使用预定义变量、项目、组、环境变量来评估一个作业是否需要创建或运行。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># pnly:varaibles/except:varaibles</span><span class="token key atrule">deploy</span><span class="token punctuation">:</span> <span class="token comment"># 使用了变量表达式</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> cap staging deploy  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">refs</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> branches    <span class="token key atrule">variables</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> $RELEASE == "staging"      <span class="token punctuation">-</span> $STAGING<span class="token key atrule">end-to-end</span><span class="token punctuation">:</span> <span class="token comment"># 根据提交日志信息来排除某些作业</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> rake test<span class="token punctuation">:</span>end<span class="token punctuation">-</span>to<span class="token punctuation">-</span>end  <span class="token key atrule">except</span><span class="token punctuation">:</span>    <span class="token key atrule">variables</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> $CI_COMMIT_MESSAGE =~ /skip<span class="token punctuation">-</span>end<span class="token punctuation">-</span>to<span class="token punctuation">-</span>end<span class="token punctuation">-</span>tests/<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>changes</code> 策略表明一个作业只有在使用 <code>git push</code> 事件使文件发生变化时执行。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># only:changes/except:changes</span><span class="token key atrule">docker build</span><span class="token punctuation">:</span> <span class="token comment"># deploy作业仅当流水线是计划作业或者在master主干运行</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> docker build <span class="token punctuation">-</span>t my<span class="token punctuation">-</span>image<span class="token punctuation">:</span>$CI_COMMIT_REF_SLUG .  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">changes</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> Dockerfile      <span class="token punctuation">-</span> docker/scripts/*      <span class="token punctuation">-</span> dockerfiles/<span class="token important">**/*</span>      <span class="token punctuation">-</span> more_scripts/<span class="token important">*.</span><span class="token punctuation">{</span>rb<span class="token punctuation">,</span>py<span class="token punctuation">,</span>sh<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>使用 <code>glob模式匹配</code> 来匹配根目录下的文件，或者任何目录下的文件。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">test</span><span class="token punctuation">:</span>   <span class="token key atrule">script</span><span class="token punctuation">:</span> npm run test  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">changes</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> <span class="token string">"*.json"</span>      <span class="token punctuation">-</span> <span class="token string">"**/*.sql"</span><span class="token comment"># 当md文件发生变化时，会忽略CI作业</span><span class="token key atrule">build</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> npm run build  <span class="token key atrule">except</span><span class="token punctuation">:</span>    <span class="token key atrule">changes</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> <span class="token string">"*.md"</span><span class="token comment"># 合并请求中修改了 Dockerfile 文件或者修改了 service 目录下的文件，都会触发Docker构建。      </span><span class="token key atrule">docker build service one</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> docker build <span class="token punctuation">-</span>t my<span class="token punctuation">-</span>service<span class="token punctuation">-</span>one<span class="token punctuation">-</span>image<span class="token punctuation">:</span>$CI_COMMIT_REF_SLUG .  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">refs</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> merge_requests    <span class="token key atrule">changes</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> Dockerfile      <span class="token punctuation">-</span> service<span class="token punctuation">-</span>one/<span class="token important">**/*</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p><code>glob模式匹配</code> 的字符串需要使用双引号包裹起来，否则会导致 <code>.gitlab-ci.yml</code> 解析错误。</p></blockquote></li></ul><h4 id="tags"><a href="#tags" class="headerlink" title="tags"></a><strong>tags</strong></h4><p><code>tags</code> 关键字用于指定 <code>GitLab Runner</code> 运行器使用哪一个运行器来执行作业。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 只有运行器注册时定义了 ruby 和 postgres 两个标签的运行器才能执行作业</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">tags</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> ruby    <span class="token punctuation">-</span> postgres<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="allow-failure"><a href="#allow-failure" class="headerlink" title="allow_failure"></a><strong>allow_failure</strong></h4><ul><li><code>allow_failure</code> 可以用于当你想设置一个作业失败的之后并不影响后续的CI组件的时候。失败的作业不会影响到commit提交状态。</li><li>如果允许失败的作业失败了，则相应的作业会显示一个黄色的警告，但对流水线成功与否不产生影响。</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">job1</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> execute_script_that_will_fail  <span class="token key atrule">allow_failure</span><span class="token punctuation">:</span> <span class="token boolean important">true</span><span class="token key atrule">job2</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> execute_script_that_will_succeed<span class="token key atrule">job3</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> deploy_to_staging<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="when"><a href="#when" class="headerlink" title="when"></a><strong>when</strong></h4><p><code>when</code> 关键字用于实现在作业失败时或发生故障时运行的作业</p><p><strong><code>when</code>可以设置的值：</strong></p><ul><li><code>on_success</code> ：只有前面的阶段的所有作业都成功时才执行，这是默认值。</li><li><code>on_failure</code> ：当前面阶段的作业至少有一个失败时才执行。</li><li><code>always</code> : 无论前面的作业是否成功，一直执行本作业。</li><li><code>manual</code> ：手动执行作业，作业不会自动执行，需要人工手动点击启动作业。</li><li><code>delayed</code> : 延迟执行作业，配合 <code>start_in</code> 关键字一起作用， <code>start_in</code> 设置的值必须小于或等于1小时，<code>start_in</code> 设置的值示例： <code>10 seconds</code> 、 <code>30 minutes</code> 、 <code>1 hour</code> ，前面的作业结束时计时器马上开始计时。</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 延时处理</span><span class="token key atrule">timed rollout 10%</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span> echo 'Rolling out 10% <span class="token punctuation">...</span>'  <span class="token key atrule">when</span><span class="token punctuation">:</span> delayed  <span class="token key atrule">start_in</span><span class="token punctuation">:</span> 30 minutes<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="environment"><a href="#environment" class="headerlink" title="environment"></a><strong>environment</strong></h4><p><code>environment</code> 用于定义作业部署到特殊的环境中。如果指定了 <code>environment</code> ，并且在 <code>运维</code> –&gt; <code>环境</code> 界面的环境列表中没有该名称下的环境，则会自动创建新环境。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">deploy to production</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span> git push production HEAD<span class="token punctuation">:</span>master  <span class="token key atrule">environment</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> production<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><p><code>environment:name</code></p><ul><li>推荐使用 <code>name</code> 关键字来定义环境的名称</li><li><code>name</code> 关键字的参数可以使用任何定义的CI变量，包括预定义的变量、安全变量、以及 <code>.gitlab-ci.yml</code> 配置文件中定义的变量，但不能使用 <code>script</code> 中定义的变量(因为这里面的变量是局部变量)。</li><li><code>environment</code> 环境的名称可以包含：英文字母(letters)、数字(digits)、空格(space)、_、/、$、{、}等。</li></ul></li><li><p><code>environment:url</code></p><ul><li><code>environment:url</code> 是可选的，用于设置环境的URL地址的按钮，通过点击按钮可以访问环境相应的URL地址。</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 如果作业都成功完成，那么会在 评审请求 和 环境部署 页面创建一个Button按钮，</span><span class="token comment"># 你点击 打开运行中的环境 按钮就可以访问环境对应的URL地址 https://prod.example.com 。</span><span class="token key atrule">deploy to production</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span> git push production HEAD<span class="token punctuation">:</span>master  <span class="token key atrule">environment</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> production    <span class="token key atrule">url</span><span class="token punctuation">:</span> https<span class="token punctuation">:</span>//prod.example.com<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>environment:on_stop和environment:action</code></p><ul><li><code>environment:on_stop</code> 与 <code>environment:action</code> 配合使用。</li><li>可以通过 <code>environment:on_stop</code> 关键字定义一个关闭(停止)环境的作业。</li><li><code>action</code> 关键字在关闭环境的作业中定义。</li></ul></li><li><p>动态环境</p><p>在环境的名称中使用变量，在 <code>environment:name</code> 和 <code>environment:url</code> 中使用变量，则可以达到动态环境的目的，动态环境需要底层应用的支持。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">deploy as review app</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span> make deploy  <span class="token key atrule">environment</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> review/$<span class="token punctuation">{</span>CI_COMMIT_REF_NAME<span class="token punctuation">}</span>    <span class="token key atrule">url</span><span class="token punctuation">:</span> https<span class="token punctuation">:</span>//$<span class="token punctuation">{</span>CI_ENVIRONMENT_SLUG<span class="token punctuation">}</span>.example.com/<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h4 id="cache"><a href="#cache" class="headerlink" title="cache"></a><strong>cache</strong></h4><ul><li>可以在全局设置或者每个作业中设置。</li><li>可以在不同的的流水线或作业之间共享数据。</li><li>在 <code>artifacts</code> 工件之前恢复缓存。</li><li><code>cache</code> 缓存机制用于指定一系列的文件或文件夹在不同的流水线或作业之间共享数据，仅能使用项目工作空间( <code>project workspace</code> )中的路径作为缓存的路径。</li><li>如果 <code>cache</code> 配置的路径是作业工作空间外部，则说明配置是全局的缓存，所有作业共享。</li></ul><h4 id="artifacts"><a href="#artifacts" class="headerlink" title="artifacts"></a><strong>artifacts</strong></h4><ul><li><p><code>artifacts</code> 用于指定在作业成功、失败、或者一直等状态下时，一系列的文件或文件夹附加到作业中。</p></li><li><p>作业完成后，工件被发送到GitLab，可以在GitLab Web界面下载。</p></li><li><p>默认情况下，只有成功的作业才会生成工件。</p></li><li><p>并不是所有的 <code>executor</code> 执行器都支持工件。</p></li><li><p><code>artifacts:paths</code> 用于指定哪些文件或文件夹会被打包成工件，仅仅项目工作空间( <code>project workspace</code> )的路径可以使用。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 将目录 binaries/ 和文件 .config 打包成工件</span><span class="token key atrule">artifacts</span><span class="token punctuation">:</span>  <span class="token key atrule">paths</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> binaries/    <span class="token punctuation">-</span> .config<span class="token comment"># 要禁用工件传递，请使用空依赖关系定义作业</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> build  <span class="token key atrule">script</span><span class="token punctuation">:</span> make build  <span class="token key atrule">dependencies</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token comment"># 仅为打标记的release发布版本创建工作，这样可以避免临时构建产生大量的存储需求</span><span class="token key atrule">default-job</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> mvn test <span class="token punctuation">-</span>U  <span class="token key atrule">except</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> tags<span class="token key atrule">release-job</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> mvn package <span class="token punctuation">-</span>U  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> target/<span class="token important">*.war</span>  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> tags<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>工件的默认名称是 <code>artifacts</code> ，当下载时名称是 <code>artifacts.zip</code></p></li><li><p>通过 <code>artifacts:name</code> 关键字可以自定义工件的归档名称，这样你可以为每个工件设置独一无二的名称，归档名称可以使用预定义的变量。</p></li><li><p>如果分支名称中包含斜杠(比如 <code>feature/my-feature</code> )，推荐使用 <code>$CI_COMMIT_REF_SLUG</code> 代替 <code>$CI_COMMIT_REF_NAME</code> 作为工件名称。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 使用作业名称使用工件名称</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> <span class="token string">"$CI_JOB_NAME"</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span class="token comment"># 使用当前分支或tag版本标签名作为工件名称</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> <span class="token string">"$CI_COMMIT_REF_NAME"</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span class="token comment"># 使用当前作业名称以及当前分支或tag版本标签名作为工件名称</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> <span class="token string">"$CI_JOB_NAME-$CI_COMMIT_REF_NAME"</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span class="token comment"># 同时使用当前作业阶段名称以及当前分支名称作为工件名称</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> <span class="token string">"$CI_JOB_STAGE-$CI_COMMIT_REF_NAME"</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>artifacts:untracked</code> 用于将git未加入版本库的文件作为工件文件。</p></li><li><p><code>artifacts:untracked</code> 将会忽略配置文件 <code>.gitignore</code>。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 将所有的未跟踪文件打包成工件</span><span class="token key atrule">artifacts</span><span class="token punctuation">:</span>  <span class="token key atrule">untracked</span><span class="token punctuation">:</span> <span class="token boolean important">true</span><span class="token comment"># 将所有的未跟踪文件以及目录 binaries 中文件打包成工件</span><span class="token key atrule">artifacts</span><span class="token punctuation">:</span>  <span class="token key atrule">untracked</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token key atrule">paths</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> binaries/<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>artifacts:when</code> 用于在作业失败时或者忽略失败时上传工件。</p><p><strong>可用值：</strong></p><ul><li><code>on_success</code> ，默认值，当作业成功上传工件。</li><li><code>on_failure</code> ，当作业失败上传工件。</li><li><code>always</code> ，无论作业是否成功一直上传工件。</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">when</span><span class="token punctuation">:</span> on_failure<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p><code>artifacts:expire_in</code> 用于设置工件的过期时间。</p></li><li><p>你可以点击界面上的 <code>Keep</code> 保持按钮，永久保存工件。</p></li><li><p>工件到期后，默认情况下每小时删除一次工件(通过cron作业)，并且后续不能再访问该工件。</p></li><li><p>工件默认有效期是30天，可以通过 <code>Admin area</code> –&gt; <code>Settings</code> –&gt; <code>Continuous Integration and Deployment</code> 设置默认的有效性时间。</p></li><li><p>如果你不提供时间单位的话，工作有效性的时间是以秒为单位的时间</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 时间示例</span><span class="token comment"># ‘42’</span><span class="token comment"># ‘3 mins 4 sec’</span><span class="token comment"># ‘2 hrs 20 min’</span><span class="token comment"># ‘2h20min’</span><span class="token comment"># ‘6 mos 1 day’</span><span class="token comment"># ‘47 yrs 6 mos and 4d’</span><span class="token comment"># ‘3 weeks and 2 days’</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">expire_in</span><span class="token punctuation">:</span> 1 week<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>artifacts:reports</code> 用于收集测试报告(report)，并在GitLab UI界面中显示出来。</p></li><li><p>无论作业是否成功，都会收集测试报告。</p></li><li><p>可以通过设置工件的打包路径 <code>artifacts:paths</code> 添加测试的报告输出文件。</p></li><li><p><code>artifacts:reports:junit</code> 可以用来收集单元测试的报告</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">rspec</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> bundle install  <span class="token punctuation">-</span> rspec <span class="token punctuation">-</span><span class="token punctuation">-</span>format RspecJunitFormatter <span class="token punctuation">-</span><span class="token punctuation">-</span>out rspec.xml  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">reports</span><span class="token punctuation">:</span>      <span class="token key atrule">junit</span><span class="token punctuation">:</span> rspec.xml<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>如果你的测试报告是多个XML文件，你可以在一个作业中指定多个单元测试报告，GitLab会自动将他们转换成一个文件，可以像下面这样表示报告的路径:</p><ul><li>文件匹配模式: <code>junit: rspec-*.xml</code></li><li>文件列表: <code>junit: [rspec-1.xml, rspec-2.xml, rspec-3.xml]</code></li><li>混合模式：<code>junit: [rspec.xml, test-results/TEST-*.xml]</code></li></ul></blockquote></li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">golang</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> go get <span class="token punctuation">-</span>u github.com/jstemmer/go<span class="token punctuation">-</span>junit<span class="token punctuation">-</span>report  <span class="token punctuation">-</span> go test <span class="token punctuation">-</span>v 2<span class="token punctuation">&gt;</span><span class="token important">&amp;1</span> <span class="token punctuation">|</span> go<span class="token punctuation">-</span>junit<span class="token punctuation">-</span>report <span class="token punctuation">&gt;</span> report.xml  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">reports</span><span class="token punctuation">:</span>      <span class="token key atrule">junit</span><span class="token punctuation">:</span> report.xml<span class="token comment"># 如果GoogleTest需要运行在多个平台(如 x86 、 x64 、arm )，需要为每种平台设置唯一的报告名称，最后将结果汇总起来。</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="dependencies"><a href="#dependencies" class="headerlink" title="dependencies"></a><strong>dependencies</strong></h4><ul><li><p><code>dependencies</code> 依赖关键字应该与 <code>artifacts</code> 工件关键字联合使用，允许你在不同作业间传递工件。</p></li><li><p>默认情况下，会传递所有本作业之前阶段的所有工件。</p></li><li><p>需要在作业上下文中定义 <code>dependencies</code> 依赖关键字，并指出所有需要使用的前序工件的作业名称列表。 <strong>作业列表中不能使用该作业后的作业名称</strong> 。</p></li><li><p>定义空的依赖项，将下不会下载任何工件。</p></li><li><p>使用依赖项不会考虑前面作业的运行状态。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">build:osx</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> build  <span class="token key atrule">script</span><span class="token punctuation">:</span> make build<span class="token punctuation">:</span>osx  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span class="token key atrule">build:linux</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> build  <span class="token key atrule">script</span><span class="token punctuation">:</span> make build<span class="token punctuation">:</span>linux  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span class="token key atrule">test:osx</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span> make test<span class="token punctuation">:</span>osx  <span class="token key atrule">dependencies</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> build<span class="token punctuation">:</span>osx<span class="token key atrule">test:linux</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span> make test<span class="token punctuation">:</span>linux  <span class="token key atrule">dependencies</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> build<span class="token punctuation">:</span>linux<span class="token key atrule">deploy</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span> make deploy<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h4 id="coverage"><a href="#coverage" class="headerlink" title="coverage"></a><strong>coverage</strong></h4><ul><li><p><code>coverage</code> 可以从作业的输出log中提取代码覆盖率。</p></li><li><p>仅支持正则表达式方式获取覆盖率。</p></li><li><p>字符串的前后必须使用/包含来表明一个正确的正则表达式规则。特殊字符串需要转义。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">job1</span><span class="token punctuation">:</span>  <span class="token key atrule">coverage</span><span class="token punctuation">:</span> <span class="token string">'/Code coverage:\d+\.\d+%/'</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li></ul><h4 id="retry"><a href="#retry" class="headerlink" title="retry"></a><strong>retry</strong></h4><ul><li><p><code>retry</code> 重试关键字用于配置当作业失败时可以重新执行的次数。</p></li><li><p>当作业失败时，如果配置了 <code>retry</code> ，那么该作业就会重试，直到允许的最大次数。</p></li><li><p>如果 <code>retry</code> 设置值为2，如果第一次重试运行成功了，那么就不会进行第二次重试。</p></li><li><p><code>retry</code> 设置值只能是0、1、2三个整数。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">test</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> rspec  <span class="token key atrule">retry</span><span class="token punctuation">:</span> <span class="token number">2</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p>为了更好的控制重试次数，<code>retry</code> 可以设置以下两个关键字：</p><ul><li><code>max</code> : 最大重试次数</li><li><code>when</code> : 何时重试</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 只有当运行器系统出现故障时才能最多重试两次</span><span class="token key atrule">test</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> rspec  <span class="token key atrule">retry</span><span class="token punctuation">:</span>    <span class="token key atrule">max</span><span class="token punctuation">:</span> <span class="token number">2</span>    <span class="token key atrule">when</span><span class="token punctuation">:</span> runner_system_failure<span class="token comment"># 为了针对多种重试情形，我们可以使用矩阵形式罗列出错误情形</span><span class="token key atrule">test</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> rspec  <span class="token key atrule">retry</span><span class="token punctuation">:</span>    <span class="token key atrule">max</span><span class="token punctuation">:</span> <span class="token number">2</span>    <span class="token key atrule">when</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> runner_system_failure      <span class="token punctuation">-</span> stuck_or_timeout_failure<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>when的值：</strong></p><ul><li><code>always</code> : 一直重试，默认值。</li><li><code>unknown_failure</code> ：当错误未知时重试。</li><li><code>script_failure</code> ： 脚本错误时重试。</li><li><code>api_failure</code> ： API调用错误时重试。</li><li><code>stuck_or_timeout_failure</code> ： 作业卡信或超时错误时重试。</li><li><code>runner_system_failure</code> ： 运行器系统错误(如设置工作失败)时重试。</li><li><code>missing_dependency_failure</code> ： 依赖工件丢失错误时重试。</li><li><code>runner_unsupported</code> ： 运行器不支持错误时重试。</li></ul></li></ul><h4 id="parallel"><a href="#parallel" class="headerlink" title="parallel"></a><strong>parallel</strong></h4><h4 id="include"><a href="#include" class="headerlink" title="include"></a>include</h4><p><code>include</code> 包含关键字可以将其他yaml文件载入到当前的 <code>.gitlab-ci.yml</code> 配置文件中</p><h4 id="trigger"><a href="#trigger" class="headerlink" title="trigger"></a><strong>trigger</strong></h4><p><code>trigger</code> 关键字用于多项目流水线时，定义下游的流水线工程，由于社区版本不支持此功能，不详细介绍。</p><h4 id="extends"><a href="#extends" class="headerlink" title="extends"></a><strong>extends</strong></h4><ul><li><code>extends</code> 扩展用于定义当前作业从哪里继承。</li><li>它是使用YAML锚点的替代方案，更加灵活、可读性强。</li></ul><h4 id="pages"><a href="#pages" class="headerlink" title="pages"></a>pages</h4><p><code>pages</code> 是一项特殊工作，用于将静态内容上传到GitLab，可用于为您的网站提供服务。</p><h4 id="variables"><a href="#variables" class="headerlink" title="variables"></a><strong>variables</strong></h4><ul><li><p>在 <code>.gitlab-ci.yml</code> 配置文件中可以通过 <code>variables</code> 关键字配置全局变量或者作业级的局部变量。</p></li><li><p>当 <code>variables</code> 关键字使用在作业层级时，它会覆盖全局变量或预定义变量。</p></li><li><p>可以在 <code>variables</code> 关键字中定义非敏感性配置。</p></li><li><p>全局变量可以在各个作业中作业，而作业级别的局部变量只能在该作业中使用。</p></li><li><p>可以在GitLab WEB界面定义一些敏感性配置变量，或者可能变动的变量。</p></li><li><p>在 <code>script</code> 中使用 <code>export</code> 可以导出当前可用的变量信息。</p></li><li><p>作业内部修改全局变量只对当前作用生效，不会影响其他作业。</p></li><li><p>可以使用赋值语句对全局变量或局部变量进行重新赋值。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">variables</span><span class="token punctuation">:</span>  <span class="token key atrule">DATABASE_URL</span><span class="token punctuation">:</span> <span class="token string">"postgres://postgres@postgres/my_database"</span><span class="token key atrule">test1</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> echo <span class="token punctuation">-</span>e "$<span class="token punctuation">{</span>DATABASE_URL<span class="token punctuation">}</span>"<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><blockquote><p>变量的优先顺序是（从最高到最低）：</p><p>触发变量或预定的流水线变量。<br>项目级别变量或受保护变量。<br>组级别变量或受保护变量。<br>YAML定义的作业级变量。<br>YAML定义的全局变量。<br>部署环境变量。<br>预定义的环境变量。</p><ul><li>不要将敏感信息，如用户密码、Token等信息放在 <code>.gitlab-ci.yml</code> 配置文件中定义变量。</li><li>敏感信息可以WEB配置界面添加变量，并将变量设置为 <code>Protected受保护</code> 或者 <code>Masked</code> ，设置为 <code>Masked</code> 的变量不会直接显示在作业日志信息中。</li></ul></blockquote><p><strong>关闭全局层级定义的变量</strong></p><p>如果你要在作业层级关闭全局层级定义的变量，可以给 <code>variables</code> 关键字定义一个空的 <code>hash</code></p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">job_name</span><span class="token punctuation">:</span>  <span class="token key atrule">variables</span><span class="token punctuation">:</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p><strong>变量定义时使用其他变量</strong></p><p>可以在变量定义时，使用其他的变量，需要使用 <code>$$</code> 进行转义。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">variables</span><span class="token punctuation">:</span>  <span class="token key atrule">LS_CMD</span><span class="token punctuation">:</span> <span class="token string">'ls $FLAGS $$TMP_DIR'</span>  <span class="token key atrule">FLAGS</span><span class="token punctuation">:</span> <span class="token string">'-al'</span><span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span class="token string">'eval $LS_CMD'</span>  <span class="token comment"># will execute 'ls -al $TMP_DIR'</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>克隆策略</strong></p><ul><li>你可以通过设置 <code>GIT_STRATEGY</code> 变量来指定GitLab Runner运行作业时使用什么样的方式来获取最新的代码，可以在全局级或作业级进行设置。</li><li><code>GIT_STRATEGY</code> 变量可以设置为 <code>fetch</code> 、<code>clone</code> 、<code>none</code> 。</li><li><code>clone</code> 是最慢的方式，这种方式会从头开始克隆仓库。</li><li><code>fetch</code> 相对来说更快一点，如果本地项目空间中存在远程仓库的克隆，只用从远程获取最新到本地，而不用从头开始克隆整个仓库(如果本地项目空间不存在远程仓库的克隆的话，则此时等同于clone，从头开始克隆远程仓库)。</li><li><code>none</code> 也是利用本地项目空间中的文件，但不会从远程获取到最新的修改数据，本地数据不是最新的。</li></ul><p><strong>子模块策略</strong></p><ul><li><code>GIT_SUBMODULE_STRATEGY</code> 类似于 <code>GIT_STRATEGY</code> ，当你的项目需要包含别的项目代码时，可以将别的项目作为你的项目的子模块，这个时候就可以使用 <code>GIT_SUBMODULE_STRATEGY</code> 。</li><li><code>GIT_SUBMODULE_STRATEGY</code> 默认取值 <code>none</code> ，即拉取代码时，子模块不会被引入。</li><li><code>GIT_SUBMODULE_STRATEGY</code> 可取值 <code>normal</code> ，意味着在只有顶级子模块会被引入。</li><li><code>GIT_SUBMODULE_STRATEGY</code> 可取值 <code>recursive</code> ，递归的意思，意味着所有级子模块会被引入。</li></ul><p><strong>检出分支策略</strong></p><ul><li><p>当 <code>GIT_STRATEGY</code> 设置为 <code>fetch</code> 或者 <code>clone</code> 时，可以通过 <code>GIT_CHECKOUT</code> 变量设置是否需要做 <code>git checkout</code> 操作，如果未指定该参数，默认值为 <code>true</code> ，即需要做 <code>git checkout</code> 操作。</p></li><li><p>可以在全局级或作业级进行设置。</p></li><li><p>如果 <code>GIT_CHECKOUT</code> 变量设置为 <code>true</code> ，GitLab Runner都会将本地工作副本检出并切换到当前流水线相关的修订版本分支上。</p></li><li><p>如果 <code>GIT_CHECKOUT</code> 变量设置为 <code>false</code> ，那么运行器操作如下：</p><ul><li><p><code>fetch</code> 操作时，更新仓库并在当前版本上保留工作副本。</p></li><li><p><code>clone</code> 操作时，克隆仓库并在默认分支中保留工作副本。</p></li></ul></li></ul><p><strong>清理工作</strong></p><ul><li>可以使用 <code>GIT_CLEAN_FLAGS</code> 变量来控制在检出源码后 <code>git clean</code> 的默认行为，可以在全局级或作业级进行设置。</li><li><code>GIT_CLEAN_FLAGS</code> 变量接受 <code>git clean</code> 命令的所有参数。</li><li>如果指定了 <code>GIT_CHECKOUT: "false"</code> ，那么 <code>git clean</code> 将不可用。</li><li><code>git-clean</code> : Remove untracked files from the working tree，即删除未跟踪的文件。</li><li><code>git clean</code> 命令用来从你的工作目录中删除所有没有tracked过的文件， <code>git reset --hard</code> 用于删除跟踪文件的修改记录。</li><li><code>git clean -n</code> 命令列出将被删除的文件。</li><li><code>git clean -f</code> 命令删除当前目录下所有没有跟踪过的文件。</li><li><code>git clean -d</code> 命令删除当前目录下所有没有跟踪过的目录。</li><li><code>git clean -e &lt;pattern&gt;</code> 命令排除( <code>--exclude=&lt;pattern&gt;</code> ) 某文件或目录，即不删除模式匹配的文件。</li><li><code>git clean -ffxd</code> 命令删除当前目录(包括由其他git仓库管理的子目录)下所有没有跟踪过的目录和文件。</li><li><code>GIT_CLEAN_FLAGS</code> 变量未指定时， <code>git clean</code> 命令的参数是 <code>-ffxd</code> 。</li><li><code>GIT_CLEAN_FLAGS</code> 变量指定为 <code>none</code> 时， <code>git clean</code> 命令不会执行。</li></ul><p><strong>作业重试次数</strong></p><ul><li>您可以设置正在运行的作业尝试执行以下每个阶段的尝试次数。可以在全局级或作业级进行设置。</li><li>涉及三个变量 <code>GET_SOURCES_ATTEMPTS</code> 、 <code>ARTIFACT_DOWNLOAD_ATTEMPTS</code> 、 <code>RESTORE_CACHE_ATTEMPTS</code> 。</li><li><code>GET_SOURCES_ATTEMPTS</code> 变量设置获取源码的尝试次数。</li><li><code>ARTIFACT_DOWNLOAD_ATTEMPTS</code> 变量设置下载归档文件的尝试次数。</li><li><code>RESTORE_CACHE_ATTEMPTS</code> 变量设置重建缓存的尝试次数。</li><li>默认是1次尝试。</li></ul><p><strong>处理并发</strong></p><ul><li><p>当执行器配置并发数 <code>concurrent</code> 大于1时，有可能导致作业运行失败，因为有可能多个作业都运行在相同的目录上，GitLab Runner运行器并不会去阻止这种情形，管理员和开发人员必须遵守Runner配置的要求。</p></li><li><p>要避免这种情况，您可以在 <code>$CI_BUILDS_DIR</code> 中使用唯一路径，因为Runner公开了另外两个提供唯一并发ID的变量：</p><ul><li><code>$CI_CONCURRENT_ID</code> ：给定执行程序中运行的所有作业的唯一ID。</li><li><code>$CI_CONCURRENT_PROJECT_ID</code> ：在给定执行程序和项目中运行的所有作业的唯一ID。</li></ul></li><li><p>在任何场景和任何执行器中都应该运行良好的最稳定的配置是在 <code>GIT_CLONE_PATH</code> 中使用 <code>$CI_CONCURRENT_ID</code> 。</p></li></ul><p><strong>特殊yaml功能</strong></p><p>可以使用特殊的YAML功能，如锚点( <code>＆</code> )，别名( <code>*</code> )和合并( <code>&lt;&lt;</code> )，这将使您大大降低 <code>.gitlab-ci.yml</code> 的复杂性。</p><ul><li><p>隐藏关键字和作业</p><p>如果我们想暂时禁用某个作业，我们可以将该作业的所有行都注释掉，更好的方法是，我们在作业名称前面增加一个点号( <code>.</code> )， 这样GitLab CI流水线就会自动处理忽略掉 <code>.hidden_job</code> 作业</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">hidden_job</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> run test<span class="token key atrule">.hidden_job</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> run test<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>锚点</p><p>锚点可以让你容易复制文档内容，锚点可以用来复制或继承某些属性，锚点与隐藏作业一起使用可提供作业模板。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">.job_template</span><span class="token punctuation">:</span> <span class="token important">&amp;job_definition</span>  <span class="token comment"># Hidden key that defines an anchor named 'job_definition'</span>  <span class="token key atrule">image</span><span class="token punctuation">:</span> ruby<span class="token punctuation">:</span><span class="token number">2.1</span>  <span class="token key atrule">services</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> postgres    <span class="token punctuation">-</span> redis<span class="token key atrule">test1</span><span class="token punctuation">:</span>  <span class="token key atrule">&lt;&lt;</span><span class="token punctuation">:</span> <span class="token important">*job_definition</span>           <span class="token comment"># Merge the contents of the 'job_definition' alias</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> test1 project<span class="token key atrule">test2</span><span class="token punctuation">:</span>  <span class="token key atrule">&lt;&lt;</span><span class="token punctuation">:</span> <span class="token important">*job_definition</span>           <span class="token comment"># Merge the contents of the 'job_definition' alias</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> test2 project<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><code>&amp;</code> 用于设置锚点名称为 <code>job_definition</code> ，也就是给隐藏作业设置一个锚点 <code>job_definition</code> 。</li><li><code>&lt;&lt;</code> 合并，将锚点定义的模板内容复制到当前作业的当前位置来。</li><li><code>*</code> 包含锚点的名称 <code>job_definition</code>。</li></ul></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/frontend/vue/vue/"/>
      <url>/2021/12/24/frontend/vue/vue/</url>
      
        <content type="html"><![CDATA[<h1 id="vue"><a href="#vue" class="headerlink" title="vue"></a>vue</h1><blockquote><p>官网：<a href="https://vuejs.org/">https://vuejs.org/</a><br>github 地址：<a href="https://github.com/vuejs/vue">https://github.com/vuejs/vue</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/hugo/themes/zoo/"/>
      <url>/2021/12/24/hugo/themes/zoo/</url>
      
        <content type="html"><![CDATA[<h1 id="zoo"><a href="#zoo" class="headerlink" title="zoo"></a>zoo</h1><blockquote><p>官网：<a href="https://zzo-docs.vercel.app/zzo">https://zzo-docs.vercel.app/zzo</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/ibora/ibora/"/>
      <url>/2021/12/24/ibora/ibora/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/hugo/hugo/"/>
      <url>/2021/12/24/hugo/hugo/</url>
      
        <content type="html"><![CDATA[<h1 id="hugo"><a href="#hugo" class="headerlink" title="hugo"></a>hugo</h1><blockquote><p>官网： <a href="https://gohugo.io/">https://gohugo.io/</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/c-c/tool/makefile/makefile-han-shu/"/>
      <url>/2021/12/24/c-c/tool/makefile/makefile-han-shu/</url>
      
        <content type="html"><![CDATA[<h1 id="makefile函数"><a href="#makefile函数" class="headerlink" title="makefile函数"></a>makefile函数</h1><p>在Makefile中可以使用函数来处理变量，从而让我们的命令或是规则更为的灵活和具有智能。函数调用后，函数的返回值可以当做变量来使用。</p><h2 id="函数的调用语法"><a href="#函数的调用语法" class="headerlink" title="函数的调用语法"></a>函数的调用语法</h2><p>函数调用，很像变量的使用，也是以 <code>$</code> 来标识的，其语法如下：</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile"><span class="token variable">$</span><span class="token punctuation">(</span>&lt;function&gt; &lt;arguments&gt;<span class="token punctuation">)</span><span class="token comment"># 或是</span><span class="token variable">$</span><span class="token punctuation">{</span>&lt;function&gt; &lt;arguments&gt;<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p> <code>&lt;function&gt;</code> 就是函数名， <code>&lt;arguments&gt;</code> 为函数的参数，参数间以逗号 <code>,</code> 分隔，而函数名和参数之间以“空格”分隔。函数调用以 <code>$</code> 开头，以圆括号或花括号把函数名和参数括起。函数中的参数可以使用变量，为了风格的统一，函数和变量的括号最好一样</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile">comma<span class="token operator">:=</span> ,empty<span class="token operator">:=</span>space<span class="token operator">:=</span> <span class="token variable">$</span><span class="token punctuation">(</span>empty<span class="token punctuation">)</span> <span class="token variable">$</span><span class="token punctuation">(</span>empty<span class="token punctuation">)</span>foo<span class="token operator">:=</span> a b cbar<span class="token operator">:=</span> <span class="token variable">$</span><span class="token punctuation">(</span><span class="token keyword">subst</span> <span class="token variable">$</span><span class="token punctuation">(</span>space<span class="token punctuation">)</span>,<span class="token variable">$</span><span class="token punctuation">(</span>comma<span class="token punctuation">)</span>,<span class="token variable">$</span><span class="token punctuation">(</span>foo<span class="token punctuation">)</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在这个示例中， <code>$(comma)</code> 的值是一个逗号。 <code>$(space)</code> 使用了 <code>$(empty)</code> 定义了一个空格， <code>$(foo)</code> 的值是 <code>a b c</code> ， <code>$(bar)</code> 的定义用，调用了函数 <code>subst</code> ，这是一个替换函数，这个函数有三个参数，第一个参数是被替换字串，第二个参数是替换字串，第三个参数是替换操作作用的字串。这个函数也就是把 <code>$(foo)</code> 中的空格替换成逗号，所以 <code>$(bar)</code> 的值是 <code>a,b,c</code> 。</p><h2 id="字符串处理函数"><a href="#字符串处理函数" class="headerlink" title="字符串处理函数"></a>字符串处理函数</h2><h3 id="subst"><a href="#subst" class="headerlink" title="subst"></a>subst</h3><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile"><span class="token variable">$</span><span class="token punctuation">(</span><span class="token keyword">subst</span> &lt;from&gt;,&lt;to&gt;,&lt;text&gt;<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><p>名称：字符串替换函数</p></li><li><p>功能：把字串 <code>&lt;text&gt;</code> 中的 <code>&lt;from&gt;</code> 字符串替换成 <code>&lt;to&gt;</code> 。</p></li><li><p>返回：函数返回被替换过后的字符串。</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile"><span class="token variable">$</span><span class="token punctuation">(</span><span class="token keyword">subst</span> ee,EE,feet on the street<span class="token punctuation">)</span><span class="token comment"># 把 `feet on the street` 中的 `ee` 替换成 `EE` ，返回结果是 `fEEt on the strEEt`</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li></ul><h3 id="patsubst"><a href="#patsubst" class="headerlink" title="patsubst"></a>patsubst</h3><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile"><span class="token variable">$</span><span class="token punctuation">(</span><span class="token keyword">patsubst</span> &lt;pattern&gt;,&lt;replacement&gt;,&lt;text&gt;<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><p>名称：模式字符串替换函数。</p></li><li><p>功能：查找 <code>&lt;text&gt;</code> 中的单词（单词以“空格”、“Tab”或“回车”“换行”分隔）是否符合模式 <code>&lt;pattern&gt;</code> ，如果匹配的话，则以 <code>&lt;replacement&gt;</code> 替换。这里， <code>&lt;pattern&gt;</code> 可以包括通配符 <code>%</code> ，表示任意长度的字串。如果 <code>&lt;replacement&gt;</code> 中也包含 <code>%</code> ，那么， <code>&lt;replacement&gt;</code> 中的这个 <code>%</code> 将是 <code>&lt;pattern&gt;</code> 中的那个 <code>%</code> 所代表的字串。（可以用 <code>\</code> 来转义，以 <code>\%</code> 来表示真实含义的 <code>%</code> 字符）</p></li><li><p>返回：函数返回被替换过后的字符串。</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile"><span class="token variable">$</span><span class="token punctuation">(</span><span class="token keyword">patsubst</span> %.c,%.o,x.c.c bar.c<span class="token punctuation">)</span><span class="token comment"># 把字串 `x.c.c bar.c` 符合模式 `%.c` 的单词替换成 `%.o` ，返回结果是 `x.c.o bar.o`</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p>备注：这和我们前面“变量章节”说过的相关知识有点相似。如 <code>$(var:&lt;pattern&gt;=&lt;replacement&gt;;)</code> 相当于 <code>$(patsubst &lt;pattern&gt;,&lt;replacement&gt;,$(var))</code> ，而 <code>$(var: &lt;suffix&gt;=&lt;replacement&gt;)</code> 则相当于 <code>$(patsubst %&lt;suffix&gt;,%&lt;replacement&gt;,$(var))</code> 。</p><p>例如有:</p><pre class="line-numbers language-none"><code class="language-none">objects = foo.o bar.o baz.o，<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>那么， <code>$(objects:.o=.c)</code> 和 <code>$(patsubst %.o,%.c,$(objects))</code> 是一样的。</p></li></ul><h3 id="strip"><a href="#strip" class="headerlink" title="strip"></a>strip</h3><pre class="line-numbers language-none"><code class="language-none">$(strip &lt;string&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><p>名称：去空格函数。</p></li><li><p>功能：去掉 <code>&lt;string&gt;</code> 字串中开头和结尾的空字符。</p></li><li><p>返回：返回被去掉空格的字符串值。</p><pre class="line-numbers language-none"><code class="language-none">$(strip a b c )# 把字串 `a b c `去到开头和结尾的空格，结果是`a b c`<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li></ul><h3 id="findstring"><a href="#findstring" class="headerlink" title="findstring"></a>findstring</h3><pre class="line-numbers language-none"><code class="language-none">$(findstring &lt;find&gt;,&lt;in&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><p>名称：查找字符串函数</p></li><li><p>功能：在字串 <code>&lt;in&gt;</code> 中查找 <code>&lt;find&gt;</code> 字串。</p></li><li><p>返回：如果找到，那么返回 <code>&lt;find&gt;</code> ，否则返回空字符串。</p></li><li><p>示例：</p><blockquote><pre class="line-numbers language-none"><code class="language-none">$(findstring a,a b c)$(findstring a,b c)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></blockquote></li></ul><p>第一个函数返回 <code>a</code> 字符串，第二个返回空字符串</p><h3 id="filter"><a href="#filter" class="headerlink" title="filter"></a>filter</h3><pre class="line-numbers language-none"><code class="language-none">$(filter &lt;pattern...&gt;,&lt;text&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><p>名称：过滤函数</p></li><li><p>功能：以 <code>&lt;pattern&gt;</code> 模式过滤 <code>&lt;text&gt;</code> 字符串中的单词，保留符合模式 <code>&lt;pattern&gt;</code> 的单词。可以有多个模式。</p></li><li><p>返回：返回符合模式 <code>&lt;pattern&gt;</code> 的字串。</p></li><li><p>示例：</p><blockquote><pre class="line-numbers language-none"><code class="language-none">sources := foo.c bar.c baz.s ugh.hfoo: $(sources)    cc $(filter %.c %.s,$(sources)) -o foo<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></blockquote><p><code>$(filter %.c %.s,$(sources))</code> 返回的值是 <code>foo.c bar.c baz.s</code> 。</p></li></ul><h3 id="filter-out"><a href="#filter-out" class="headerlink" title="filter-out"></a>filter-out</h3><pre class="line-numbers language-none"><code class="language-none">$(filter-out &lt;pattern...&gt;,&lt;text&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><p>名称：反过滤函数</p></li><li><p>功能：以 <code>&lt;pattern&gt;</code> 模式过滤 <code>&lt;text&gt;</code> 字符串中的单词，去除符合模式 <code>&lt;pattern&gt;</code> 的单词。可以有多个模式。</p></li><li><p>返回：返回不符合模式 <code>&lt;pattern&gt;</code> 的字串。</p><pre class="line-numbers language-none"><code class="language-none">objects=main1.o foo.o main2.o bar.omains=main1.o main2.o# `$(filter-out $(mains),$(objects))` 返回值是 `foo.o bar.o`<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li></ul><h3 id="sort"><a href="#sort" class="headerlink" title="sort"></a>sort</h3><pre class="line-numbers language-none"><code class="language-none">$(sort &lt;list&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：排序函数</li><li>功能：给字符串 <code>&lt;list&gt;</code> 中的单词排序（升序）。</li><li>返回：返回排序后的字符串。</li><li>示例： <code>$(sort foo bar lose)</code> 返回 <code>bar foo lose</code> 。</li><li>备注： <code>sort</code> 函数会去掉 <code>&lt;list&gt;</code> 中相同的单词。</li></ul><h3 id="word"><a href="#word" class="headerlink" title="word"></a>word</h3><pre class="line-numbers language-none"><code class="language-none">$(word &lt;n&gt;,&lt;text&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：取单词函数</li><li>功能：取字符串 <code>&lt;text&gt;</code> 中第 <code>&lt;n&gt;</code> 个单词。（从一开始）</li><li>返回：返回字符串 <code>&lt;text&gt;</code> 中第 <code>&lt;n&gt;</code> 个单词。如果 <code>&lt;n&gt;</code> 比 <code>&lt;text&gt;</code> 中的单词数要大，那么返回空字符串。</li><li>示例： <code>$(word 2, foo bar baz)</code> 返回值是 <code>bar</code> 。</li></ul><h3 id="wordlist"><a href="#wordlist" class="headerlink" title="wordlist"></a>wordlist</h3><pre class="line-numbers language-none"><code class="language-none">$(wordlist &lt;ss&gt;,&lt;e&gt;,&lt;text&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：取单词串函数</li><li>功能：从字符串 <code>&lt;text&gt;</code> 中取从 <code>&lt;ss&gt;</code> 开始到 <code>&lt;e&gt;</code> 的单词串。 <code>&lt;ss&gt;</code> 和 <code>&lt;e&gt;</code> 是一个数字。</li><li>返回：返回字符串 <code>&lt;text&gt;</code> 中从 <code>&lt;ss&gt;</code> 到 <code>&lt;e&gt;</code> 的单词字串。如果 <code>&lt;ss&gt;</code> 比 <code>&lt;text&gt;</code> 中的单词数要大，那么返回空字符串。如果 <code>&lt;e&gt;</code> 大于 <code>&lt;text&gt;</code> 的单词数，那么返回从 <code>&lt;ss&gt;</code> 开始，到 <code>&lt;text&gt;</code> 结束的单词串。</li><li>示例： <code>$(wordlist 2, 3, foo bar baz)</code> 返回值是 <code>bar baz</code> 。</li></ul><h3 id="words"><a href="#words" class="headerlink" title="words"></a>words</h3><pre class="line-numbers language-none"><code class="language-none">$(words &lt;text&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：单词个数统计函数</li><li>功能：统计 <code>&lt;text&gt;</code> 中字符串中的单词个数。</li><li>返回：返回 <code>&lt;text&gt;</code> 中的单词数。</li><li>示例： <code>$(words, foo bar baz)</code> 返回值是 <code>3</code> 。</li><li>备注：如果我们要取 <code>&lt;text&gt;</code> 中最后的一个单词，我们可以这样： <code>$(word $(words &lt;text&gt;),&lt;text&gt;)</code> 。</li></ul><h3 id="firstword"><a href="#firstword" class="headerlink" title="firstword"></a>firstword</h3><pre class="line-numbers language-none"><code class="language-none">$(firstword &lt;text&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：首单词函数——firstword。</li><li>功能：取字符串 <code>&lt;text&gt;</code> 中的第一个单词。</li><li>返回：返回字符串 <code>&lt;text&gt;</code> 的第一个单词。</li><li>示例： <code>$(firstword foo bar)</code> 返回值是 <code>foo</code>。</li><li>备注：这个函数可以用 <code>word</code> 函数来实现： <code>$(word 1,&lt;text&gt;)</code> 。</li></ul><p>以上，是所有的字符串操作函数，如果搭配混合使用，可以完成比较复杂的功能。这里，举一个现实中应用的例子。我们知道，make使用 <code>VPATH</code> 变量来指定“依赖文件”的搜索路径。于是，我们可以利用这个搜索路径来指定编译器对头文件的搜索路径参数 <code>CFLAGS</code> ，如：</p><pre class="line-numbers language-none"><code class="language-none">override CFLAGS += $(patsubst %,-I%,$(subst :, ,$(VPATH)))<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>如果我们的 <code>$(VPATH)</code> 值是 <code>src:../headers</code> ，那么 <code>$(patsubst %,-I%,$(subst :, ,$(VPATH)))</code> 将返回 <code>-Isrc -I../headers</code> ，这正是cc或gcc搜索头文件路径的参数。</p><h2 id="文件名操作函数"><a href="#文件名操作函数" class="headerlink" title="文件名操作函数"></a>文件名操作函数</h2><p>下面我们要介绍的函数主要是处理文件名的。每个函数的参数字符串都会被当做一个或是一系列的文件名来对待。</p><h3 id="dir"><a href="#dir" class="headerlink" title="dir"></a>dir</h3><pre class="line-numbers language-none"><code class="language-none">$(dir &lt;names...&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：取目录函数——dir。</li><li>功能：从文件名序列 <code>&lt;names&gt;</code> 中取出目录部分。目录部分是指最后一个反斜杠（ <code>/</code> ）之前的部分。如果没有反斜杠，那么返回 <code>./</code> 。</li><li>返回：返回文件名序列 <code>&lt;names&gt;</code> 的目录部分。</li><li>示例： <code>$(dir src/foo.c hacks)</code> 返回值是 <code>src/ ./</code> 。</li></ul><h3 id="notdir"><a href="#notdir" class="headerlink" title="notdir"></a>notdir</h3><pre class="line-numbers language-none"><code class="language-none">$(notdir &lt;names...&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：取文件函数——notdir。</li><li>功能：从文件名序列 <code>&lt;names&gt;</code> 中取出非目录部分。非目录部分是指最後一个反斜杠（ <code>/</code> ）之后的部分。</li><li>返回：返回文件名序列 <code>&lt;names&gt;</code> 的非目录部分。</li><li>示例: <code>$(notdir src/foo.c hacks)</code> 返回值是 <code>foo.c hacks</code> 。</li></ul><h3 id="suffix"><a href="#suffix" class="headerlink" title="suffix"></a>suffix</h3><pre class="line-numbers language-none"><code class="language-none">$(suffix &lt;names...&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：取後缀函数——suffix。</li><li>功能：从文件名序列 <code>&lt;names&gt;</code> 中取出各个文件名的后缀。</li><li>返回：返回文件名序列 <code>&lt;names&gt;</code> 的后缀序列，如果文件没有后缀，则返回空字串。</li><li>示例： <code>$(suffix src/foo.c src-1.0/bar.c hacks)</code> 返回值是 <code>.c .c</code>。</li></ul><h3 id="basename"><a href="#basename" class="headerlink" title="basename"></a>basename</h3><pre class="line-numbers language-none"><code class="language-none">$(basename &lt;names...&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：取前缀函数——basename。</li><li>功能：从文件名序列 <code>&lt;names&gt;</code> 中取出各个文件名的前缀部分。</li><li>返回：返回文件名序列 <code>&lt;names&gt;</code> 的前缀序列，如果文件没有前缀，则返回空字串。</li><li>示例： <code>$(basename src/foo.c src-1.0/bar.c hacks)</code> 返回值是 <code>src/foo src-1.0/bar hacks</code> 。</li></ul><h3 id="addsuffix"><a href="#addsuffix" class="headerlink" title="addsuffix"></a>addsuffix</h3><pre class="line-numbers language-none"><code class="language-none">$(addsuffix &lt;suffix&gt;,&lt;names...&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：加后缀函数——addsuffix。</li><li>功能：把后缀 <code>&lt;suffix&gt;</code> 加到 <code>&lt;names&gt;</code> 中的每个单词后面。</li><li>返回：返回加过后缀的文件名序列。</li><li>示例： <code>$(addsuffix .c,foo bar)</code> 返回值是 <code>foo.c bar.c</code> 。</li></ul><h3 id="addprefix"><a href="#addprefix" class="headerlink" title="addprefix"></a>addprefix</h3><pre class="line-numbers language-none"><code class="language-none">$(addprefix &lt;prefix&gt;,&lt;names...&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：加前缀函数——addprefix。</li><li>功能：把前缀 <code>&lt;prefix&gt;</code> 加到 <code>&lt;names&gt;</code> 中的每个单词后面。</li><li>返回：返回加过前缀的文件名序列。</li><li>示例： <code>$(addprefix src/,foo bar)</code> 返回值是 <code>src/foo src/bar</code> 。</li></ul><h3 id="join"><a href="#join" class="headerlink" title="join"></a>join</h3><pre class="line-numbers language-none"><code class="language-none">$(join &lt;list1&gt;,&lt;list2&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>名称：连接函数——join。</li><li>功能：把 <code>&lt;list2&gt;</code> 中的单词对应地加到 <code>&lt;list1&gt;</code> 的单词后面。如果 <code>&lt;list1&gt;</code> 的单词个数要比 <code>&lt;list2&gt;</code> 的多，那么， <code>&lt;list1&gt;</code> 中的多出来的单词将保持原样。如果 <code>&lt;list2&gt;</code> 的单词个数要比 <code>&lt;list1&gt;</code> 多，那么， <code>&lt;list2&gt;</code> 多出来的单词将被复制到 <code>&lt;list1&gt;</code> 中。</li><li>返回：返回连接过后的字符串。</li><li>示例： <code>$(join aaa bbb , 111 222 333)</code> 返回值是 <code>aaa111 bbb222 333</code> 。</li></ul><h2 id="foreach-函数"><a href="#foreach-函数" class="headerlink" title="foreach 函数"></a>foreach 函数</h2><p>foreach函数和别的函数非常的不一样。因为这个函数是用来做循环用的，Makefile中的foreach函数几乎是仿照于Unix标准Shell（/bin/sh）中的for语句，或是C-Shell（/bin/csh）中的foreach语句而构建的。它的语法是：</p><pre class="line-numbers language-none"><code class="language-none">$(foreach &lt;var&gt;,&lt;list&gt;,&lt;text&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这个函数的意思是，把参数 <code>&lt;list&gt;</code> 中的单词逐一取出放到参数 <code>&lt;var&gt;</code> 所指定的变量中，然后再执行 <code>&lt;text&gt;</code> 所包含的表达式。每一次 <code>&lt;text&gt;</code> 会返回一个字符串，循环过程中， <code>&lt;text&gt;</code> 的所返回的每个字符串会以空格分隔，最后当整个循环结束时， <code>&lt;text&gt;</code> 所返回的每个字符串所组成的整个字符串（以空格分隔）将会是foreach函数的返回值。</p><p>所以， <code>&lt;var&gt;</code> 最好是一个变量名， <code>&lt;list&gt;</code> 可以是一个表达式，而 <code>&lt;text&gt;</code> 中一般会使用 <code>&lt;var&gt;</code> 这个参数来依次枚举 <code>&lt;list&gt;</code> 中的单词。举个例子：</p><pre class="line-numbers language-none"><code class="language-none">names := a b c dfiles := $(foreach n,$(names),$(n).o)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>上面的例子中， <code>$(name)</code> 中的单词会被挨个取出，并存到变量 <code>n</code> 中， <code>$(n).o</code> 每次根据 <code>$(n)</code> 计算出一个值，这些值以空格分隔，最后作为foreach函数的返回，所以， <code>$(files)</code> 的值是 <code>a.o b.o c.o d.o</code> 。</p><p>注意，foreach中的 <code>&lt;var&gt;</code> 参数是一个临时的局部变量，foreach函数执行完后，参数 <code>&lt;var&gt;</code> 的变量将不在作用，其作用域只在foreach函数当中。</p><h2 id="if-函数"><a href="#if-函数" class="headerlink" title="if 函数"></a>if 函数</h2><p>if函数很像GNU的make所支持的条件语句——ifeq（参见前面所述的章节），if函数的语法是：</p><pre class="line-numbers language-none"><code class="language-none">$(if &lt;condition&gt;,&lt;then-part&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>或是</p><pre class="line-numbers language-none"><code class="language-none">$(if &lt;condition&gt;,&lt;then-part&gt;,&lt;else-part&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>可见，if函数可以包含“else”部分，或是不含。即if函数的参数可以是两个，也可以是三个。 <code>&lt;condition&gt;</code> 参数是if的表达式，如果其返回的为非空字符串，那么这个表达式就相当于返回真，于是， <code>&lt;then-part&gt;</code> 会被计算，否则 <code>&lt;else-part&gt;</code> 会被计算。</p><p>而if函数的返回值是，如果 <code>&lt;condition&gt;</code> 为真（非空字符串），那个 <code>&lt;then-part&gt;</code> 会是整个函数的返回值，如果 <code>&lt;condition&gt;</code> 为假（空字符串），那么 <code>&lt;else-part&gt;</code> 会是整个函数的返回值，此时如果 <code>&lt;else-part&gt;</code> 没有被定义，那么，整个函数返回空字串。</p><p>所以， <code>&lt;then-part&gt;</code> 和 <code>&lt;else-part&gt;</code> 只会有一个被计算。</p><h2 id="call函数"><a href="#call函数" class="headerlink" title="call函数"></a>call函数</h2><p>call函数是唯一一个可以用来创建新的参数化的函数。你可以写一个非常复杂的表达式，这个表达式中，你可以定义许多参数，然后你可以call函数来向这个表达式传递参数。其语法是：</p><pre class="line-numbers language-none"><code class="language-none">$(call &lt;expression&gt;,&lt;parm1&gt;,&lt;parm2&gt;,...,&lt;parmn&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>当make执行这个函数时， <code>&lt;expression&gt;</code> 参数中的变量，如 <code>$(1)</code> 、 <code>$(2)</code> 等，会被参数 <code>&lt;parm1&gt;</code> 、 <code>&lt;parm2&gt;</code> 、 <code>&lt;parm3&gt;</code> 依次取代。而 <code>&lt;expression&gt;</code> 的返回值就是 call 函数的返回值。例如：</p><pre class="line-numbers language-none"><code class="language-none">reverse =  $(1) $(2)foo = $(call reverse,a,b)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>那么， <code>foo</code> 的值就是 <code>a b</code> 。当然，参数的次序是可以自定义的，不一定是顺序的，如：</p><pre class="line-numbers language-none"><code class="language-none">reverse =  $(2) $(1)foo = $(call reverse,a,b)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>此时的 <code>foo</code> 的值就是 <code>b a</code> 。</p><p>需要注意：在向 call 函数传递参数时要尤其注意空格的使用。call 函数在处理参数时，第2个及其之后的参数中的空格会被保留，因而可能造成一些奇怪的效果。因而在向call函数提供参数时，最安全的做法是去除所有多余的空格。</p><h2 id="origin函数"><a href="#origin函数" class="headerlink" title="origin函数"></a>origin函数</h2><p>origin函数不像其它的函数，他并不操作变量的值，他只是告诉你你的这个变量是哪里来的？其语法是：</p><pre class="line-numbers language-none"><code class="language-none">$(origin &lt;variable&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><p>注意， <code>&lt;variable&gt;</code> 是变量的名字，不应该是引用。所以你最好不要在 <code>&lt;variable&gt;</code> 中使用</p><p><code>$</code> 字符。Origin函数会以其返回值来告诉你这个变量的“出生情况”，下面，是origin函数的返回值:</p></li><li><p><code>undefined</code></p><p>如果 <code>&lt;variable&gt;</code> 从来没有定义过，origin函数返回这个值 <code>undefined</code></p></li><li><p><code>default</code></p><p>如果 <code>&lt;variable&gt;</code> 是一个默认的定义，比如“CC”这个变量，这种变量我们将在后面讲述。</p></li><li><p><code>environment</code></p><p>如果 <code>&lt;variable&gt;</code> 是一个环境变量，并且当Makefile被执行时， <code>-e</code> 参数没有被打开。</p></li><li><p><code>file</code></p><p>如果 <code>&lt;variable&gt;</code> 这个变量被定义在Makefile中。</p></li><li><p><code>command line</code></p><p>如果 <code>&lt;variable&gt;</code> 这个变量是被命令行定义的。</p></li><li><p><code>override</code></p><p>如果 <code>&lt;variable&gt;</code> 是被override指示符重新定义的。</p></li><li><p><code>automatic</code></p><p>如果 <code>&lt;variable&gt;</code> 是一个命令运行中的自动化变量。关于自动化变量将在后面讲述。</p></li></ul><p>这些信息对于我们编写Makefile是非常有用的，例如，假设我们有一个Makefile其包了一个定义文件 Make.def，在 Make.def中定义了一个变量“bletch”，而我们的环境中也有一个环境变量“bletch”，此时，我们想判断一下，如果变量来源于环境，那么我们就把之重定义了，如果来源于Make.def或是命令行等非环境的，那么我们就不重新定义它。于是，在我们的Makefile中，我们可以这样写：</p><pre class="line-numbers language-none"><code class="language-none">ifdef bletch    ifeq "$(origin bletch)" "environment"        bletch = barf, gag, etc.    endifendif<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当然，你也许会说，使用 <code>override</code> 关键字不就可以重新定义环境中的变量了吗？为什么需要使用这样的步骤？是的，我们用 <code>override</code> 是可以达到这样的效果，可是 <code>override</code> 过于粗暴，它同时会把从命令行定义的变量也覆盖了，而我们只想重新定义环境传来的，而不想重新定义命令行传来的。</p><h2 id="shell函数"><a href="#shell函数" class="headerlink" title="shell函数"></a>shell函数</h2><p>shell函数也不像其它的函数。顾名思义，它的参数应该就是操作系统Shell的命令。它和反引号“`”是相同的功能。这就是说，shell函数把执行操作系统命令后的输出作为函数返回。于是，我们可以用操作系统命令以及字符串处理命令awk，sed等等命令来生成一个变量，如：</p><pre class="line-numbers language-none"><code class="language-none">contents := $(shell cat foo)files := $(shell echo *.c)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>注意，这个函数会新生成一个Shell程序来执行命令，所以你要注意其运行性能，如果你的Makefile中有一些比较复杂的规则，并大量使用了这个函数，那么对于你的系统性能是有害的。特别是Makefile的隐晦的规则可能会让你的shell函数执行的次数比你想像的多得多。</p><h2 id="控制make的函数"><a href="#控制make的函数" class="headerlink" title="控制make的函数"></a>控制make的函数</h2><p>make提供了一些函数来控制make的运行。通常，你需要检测一些运行Makefile时的运行时信息，并且根据这些信息来决定，你是让make继续执行，还是停止。</p><pre class="line-numbers language-none"><code class="language-none">$(error &lt;text ...&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>产生一个致命的错误， <code>&lt;text ...&gt;</code> 是错误信息。注意，error函数不会在一被使用就会产生错误信息，所以如果你把其定义在某个变量中，并在后续的脚本中使用这个变量，那么也是可以的。例如：</p><p>示例一：</p><pre class="line-numbers language-none"><code class="language-none">ifdef ERROR_001    $(error error is $(ERROR_001))endif<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>示例二：</p><pre class="line-numbers language-none"><code class="language-none">ERR = $(error found an error!).PHONY: errerr: $(ERR)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>示例一会在变量ERROR_001定义了后执行时产生error调用，而示例二则在目录err被执行时才发生error调用。</p><pre class="line-numbers language-none"><code class="language-none">$(warning &lt;text ...&gt;)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这个函数很像error函数，只是它并不会让make退出，只是输出一段警告信息，而make继续执行。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/c-c/tool/makefile/makefile-bian-liang/"/>
      <url>/2021/12/24/c-c/tool/makefile/makefile-bian-liang/</url>
      
        <content type="html"><![CDATA[<h1 id="makefile变量"><a href="#makefile变量" class="headerlink" title="makefile变量"></a>makefile变量</h1><p>在Makefile中的定义的变量，就像是C/C++语言中的宏一样，他代表了一个<strong>文本字串</strong>。在Makefile中，变量可以使用在“目标”，“依赖目标”， “命令”或是Makefile的其它部分中。</p><p>变量的命名字可以包含字符、数字，下划线（可以是数字开头），但不应该含有 <code>:</code> 、 <code>#</code> 、 <code>=</code> 或是空字符（空格、回车等）。变量是大小写敏感。</p><p>传统的Makefile的变量名是全大写的命名方式，但推荐使用大小写搭配的变量名，如：MakeFlags。这样可以避免和系统的变量冲突。</p><h2 id="变量的基础"><a href="#变量的基础" class="headerlink" title="变量的基础"></a>变量的基础</h2><p>变量在声明时需要给予初值，而在使用时，需要给在变量名前加上 <code>$</code> 符号，但最好用小括号 <code>()</code> 或是大括号 <code>{}</code> 把变量给包括起来。如果你要使用真实的 <code>$</code> 字符，那么你需要用 <code>$$</code> 来表示。</p><p>变量可以使用在许多地方，如规则中的“目标”、“依赖”、“命令”以及新的变量中。</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile">objects <span class="token operator">=</span> program.o foo.o utils.o<span class="token symbol">program</span> <span class="token punctuation">:</span> <span class="token variable">$</span><span class="token punctuation">(</span>objects<span class="token punctuation">)</span>    cc -o program <span class="token variable">$</span><span class="token punctuation">(</span>objects<span class="token punctuation">)</span><span class="token symbol"><span class="token variable">$</span>(objects)</span> <span class="token punctuation">:</span> defs.h<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>变量会在使用它的地方精确地展开，就像C/C++中的宏一样</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile">foo <span class="token operator">=</span> c<span class="token symbol">prog.o</span> <span class="token punctuation">:</span> prog.<span class="token variable">$</span><span class="token punctuation">(</span>foo<span class="token punctuation">)</span>    <span class="token variable">$</span><span class="token punctuation">(</span>foo<span class="token punctuation">)</span><span class="token variable">$</span><span class="token punctuation">(</span>foo<span class="token punctuation">)</span> -<span class="token variable">$</span><span class="token punctuation">(</span>foo<span class="token punctuation">)</span> prog.<span class="token variable">$</span><span class="token punctuation">(</span>foo<span class="token punctuation">)</span><span class="token comment"># 展开后得到</span><span class="token symbol">prog.o</span> <span class="token punctuation">:</span> prog.c    cc -c prog.c<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>不要在你的Makefile中这样干</p></blockquote><h2 id="变量中的变量"><a href="#变量中的变量" class="headerlink" title="变量中的变量"></a>变量中的变量</h2><p>在定义变量的值时，我们可以使用其它变量来构造变量的值，在Makefile中有两种方式来在用变量定义变量的值。</p><p>先看第一种方式，也就是简单的使用 <code>=</code> 号，在 <code>=</code> 左侧是变量，右侧是变量的值，右侧变量的值可以定义在文件的任何一处.</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile">foo <span class="token operator">=</span> <span class="token variable">$</span><span class="token punctuation">(</span>bar<span class="token punctuation">)</span>bar <span class="token operator">=</span> <span class="token variable">$</span><span class="token punctuation">(</span>ugh<span class="token punctuation">)</span>ugh <span class="token operator">=</span> Huh?<span class="token symbol">all</span><span class="token punctuation">:</span>    echo <span class="token variable">$</span><span class="token punctuation">(</span>foo<span class="token punctuation">)</span><span class="token comment"># 变量是可以使用后面的变量来定义的</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这个功能有好的地方，也有不好的地方，好的地方是，我们可以把变量的真实值推到后面来定义，如：</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile"><span class="token comment"># 好处：可以把变量的真实值推到后面来定义</span>CFLAGS <span class="token operator">=</span> <span class="token variable">$</span><span class="token punctuation">(</span>include_dirs<span class="token punctuation">)</span> -Oinclude_dirs <span class="token operator">=</span> -Ifoo -Ibar<span class="token comment"># 缺点：递归定义</span>CFLAGS <span class="token operator">=</span> <span class="token variable">$</span><span class="token punctuation">(</span>CFLAGS<span class="token punctuation">)</span> -OA <span class="token operator">=</span> <span class="token variable">$</span><span class="token punctuation">(</span>B<span class="token punctuation">)</span>B <span class="token operator">=</span> <span class="token variable">$</span><span class="token punctuation">(</span>A<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这会让make陷入无限的变量展开过程中去，当然，我们的make是有能力检测这样的定义，并会报错。还有就是如果在变量中使用函数，那么，这种方式会让我们的make运行时非常慢，更糟糕的是，他会使用得两个make的函数“wildcard”和“shell”发生不可预知的错误。因为你不会知道这两个函数会被调用多少次。</p><p>为了避免上面的这种方法，我们可以使用make中的另一种用变量来定义变量的方法。这种方法使用的是 <code>:=</code> 操作符，如：</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile">x <span class="token operator">:=</span> fooy <span class="token operator">:=</span> <span class="token variable">$</span><span class="token punctuation">(</span>x<span class="token punctuation">)</span> barx <span class="token operator">:=</span> later<span class="token comment"># 等价于</span>y <span class="token operator">:=</span> foo barx <span class="token operator">:=</span> later<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>前面的变量不能使用后面的变量，只能使用前面已定义好了的变量。如果是这样：</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile">y <span class="token operator">:=</span> <span class="token variable">$</span><span class="token punctuation">(</span>x<span class="token punctuation">)</span> barx <span class="token operator">:=</span> foo<span class="token comment"># y的值是“bar”，而不是“foo bar”</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>一个复杂的例子，其中包括了make的函数、条件表达式和一个系统变量“MAKELEVEL”的使用：</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile"><span class="token keyword">ifeq</span> <span class="token punctuation">(</span>0,<span class="token variable">$</span><span class="token punctuation">{</span>MAKELEVEL<span class="token punctuation">}</span><span class="token punctuation">)</span>cur-dir   <span class="token operator">:=</span> <span class="token variable">$</span><span class="token punctuation">(</span><span class="token keyword">shell</span> pwd<span class="token punctuation">)</span>whoami    <span class="token operator">:=</span> <span class="token variable">$</span><span class="token punctuation">(</span><span class="token keyword">shell</span> whoami<span class="token punctuation">)</span>host-type <span class="token operator">:=</span> <span class="token variable">$</span><span class="token punctuation">(</span><span class="token keyword">shell</span> arch<span class="token punctuation">)</span>MAKE <span class="token operator">:=</span> <span class="token variable">$</span><span class="token punctuation">{</span>MAKE<span class="token punctuation">}</span> host-type<span class="token operator">=</span><span class="token variable">$</span><span class="token punctuation">{</span>host-type<span class="token punctuation">}</span> whoami<span class="token operator">=</span><span class="token variable">$</span><span class="token punctuation">{</span>whoami<span class="token punctuation">}</span><span class="token keyword">endif</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>对于系统变量“MAKELEVEL”，其意思是，如果我们的make有一个嵌套执行的动作，那么，这个变量会记录了我们的当前Makefile的调用层数。</p><p>下面再介绍两个定义变量时我们需要知道的，请先看一个例子，如果我们要定义一个变量，其值是一个空格，那么我们可以这样来：</p><pre class="line-numbers language-none"><code class="language-none">nullstring :=space := $(nullstring) # end of the line<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>nullstring是一个Empty变量，其中什么也没有，而我们的space的值是一个空格。因为在操作符的右边是很难描述一个空格的，这里采用的技术很管用，先用一个Empty变量来标明变量的值开始了，而后面采用“#”注释符来表示变量定义的终止，这样，我们可以定义出其值是一个空格的变量。请注意这里关于“#”的使用，注释符“#”的这种特性值得我们注意，如果我们这样定义一个变量：</p><pre class="line-numbers language-none"><code class="language-none">dir := /foo/bar    # directory to put the frobs in<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>dir这个变量的值是“/foo/bar”，后面还跟了4个空格，如果我们这样使用这样变量来指定别的目录——“$(dir)/file”那么就完蛋了。</p><p>还有一个比较有用的操作符是 <code>?=</code> ，先看示例：</p><pre class="line-numbers language-none"><code class="language-none">FOO ?= bar<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>其含义是，如果FOO没有被定义过，那么变量FOO的值就是“bar”，如果FOO先前被定义过，那么这条语将什么也不做，其等价于：</p><pre class="line-numbers language-none"><code class="language-none">ifeq ($(origin FOO), undefined)    FOO = barendif<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h2 id="变量高级用法"><a href="#变量高级用法" class="headerlink" title="变量高级用法"></a>变量高级用法</h2><ul><li>变量值的替换</li></ul><p>替换变量中的共有的部分，其格式是 <code>$(var:a=b)</code> 或是 <code>${var:a=b}</code> ，其意思是，把变量“var”中所有以“a”字串“结尾”的“a”替换成“b”字串。这里的“结尾”意思是“空格”或是“结束符”。</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile">foo <span class="token operator">:=</span> a.o b.o c.obar <span class="token operator">:=</span> <span class="token variable">$</span><span class="token punctuation">(</span>foo<span class="token punctuation">:</span>.o<span class="token operator">=</span>.c<span class="token punctuation">)</span><span class="token comment"># 定义了一个 `$(foo)` 变量，把$(foo)中所有以.o字串“结尾”全部替换成.c</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><ul><li>变量替换的技术是以“静态模式”</li></ul><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile">foo <span class="token operator">:=</span> a.o b.o c.obar <span class="token operator">:=</span> <span class="token variable">$</span><span class="token punctuation">(</span>foo<span class="token punctuation">:</span>%.o<span class="token operator">=</span>%.c<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>这依赖于被替换字串中的有相同的模式，模式中必须包含一个 <code>%</code> 字符，这个例子同样让 <code>$(bar)</code> 变量的值为“a.c b.c c.c”。</p><ul><li>把变量的值再当成变量</li></ul><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile">x <span class="token operator">=</span> yy <span class="token operator">=</span> za <span class="token operator">:=</span> <span class="token variable">$</span><span class="token punctuation">(</span><span class="token variable">$</span><span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token comment"># $(x)的值是“y”，所以$($(x))就是$(y)，于是$(a)的值就是“z”。</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>让我们再复杂一点，使用上“在变量定义中使用变量”的第一个方式，来看一个例子：</p><pre class="line-numbers language-none"><code class="language-none">x = $(y)y = zz = Helloa := $($(x))<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>这里的 <code>$($(x))</code> 被替换成了 <code>$($(y))</code> ，因为 <code>$(y)</code> 值是“z”，所以，最终结果是： <code>a:=$(z)</code> ，也就是“Hello”。</p><p>再复杂一点，我们再加上函数：</p><pre class="line-numbers language-none"><code class="language-none">x = variable1variable2 := Helloy = $(subst 1,2,$(x))z = ya := $($($(z)))<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这个例子中， <code>$($($(z)))</code> 扩展为 <code>$($(y))</code> ，而其再次被扩展为 <code>$($(subst 1,2,$(x)))</code> 。 <code>$(x)</code> 的值是“variable1”，subst函数把“variable1”中的所有“1”字串替换成“2”字串，于是，“variable1”变成 “variable2”，再取其值，所以，最终， <code>$(a)</code> 的值就是 <code>$(variable2)</code> 的值——“Hello”。（喔，好不容易）</p><p>在这种方式中，或要可以使用多个变量来组成一个变量的名字，然后再取其值：</p><pre class="line-numbers language-none"><code class="language-none">first_second = Helloa = firstb = secondall = $($a_$b)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>这里的 <code>$a_$b</code> 组成了“first_second”，于是， <code>$(all)</code> 的值就是“Hello”。</p><p>再来看看结合第一种技术的例子：</p><pre class="line-numbers language-none"><code class="language-none">a_objects := a.o b.o c.o1_objects := 1.o 2.o 3.osources := $($(a1)_objects:.o=.c)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>这个例子中，如果 <code>$(a1)</code> 的值是“a”的话，那么， <code>$(sources)</code> 的值就是“a.c b.c c.c”；如果 <code>$(a1)</code> 的值是“1”，那么 <code>$(sources)</code> 的值是“1.c 2.c 3.c”。</p><p>再来看一个这种技术和“函数”与“条件语句”一同使用的例子：</p><pre class="line-numbers language-none"><code class="language-none">ifdef do_sort    func := sortelse    func := stripendifbar := a d b g q cfoo := $($(func) $(bar))<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这个示例中，如果定义了“do_sort”，那么： <code>foo := $(sort a d b g q c)</code> ，于是 <code>$(foo)</code> 的值就是 “a b c d g q”，而如果没有定义“do_sort”，那么： <code>foo := $(strip a d b g q c)</code> ，调用的就是strip函数。</p><p>当然，“把变量的值再当成变量”这种技术，同样可以用在操作符的左边:</p><pre class="line-numbers language-none"><code class="language-none">dir = foo$(dir)_sources := $(wildcard $(dir)/*.c)define $(dir)_printlpr $($(dir)_sources)endef<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这个例子中定义了三个变量：“dir”，“foo_sources”和“foo_print”。</p><h2 id="追加变量值"><a href="#追加变量值" class="headerlink" title="追加变量值"></a>追加变量值</h2><p>我们可以使用 <code>+=</code> 操作符给变量追加值，如：</p><pre class="line-numbers language-none"><code class="language-none">objects = main.o foo.o bar.o utils.oobjects += another.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>于是，我们的 <code>$(objects)</code> 值变成：“main.o foo.o bar.o utils.o another.o”（another.o被追加进去了）</p><p>使用 <code>+=</code> 操作符，可以模拟为下面的这种例子：</p><pre class="line-numbers language-none"><code class="language-none">objects = main.o foo.o bar.o utils.oobjects := $(objects) another.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>所不同的是，用 <code>+=</code> 更为简洁。</p><p>如果变量之前没有定义过，那么， <code>+=</code> 会自动变成 <code>=</code> ，如果前面有变量定义，那么 <code>+=</code> 会继承于前次操作的赋值符。如果前一次的是 <code>:=</code> ，那么 <code>+=</code> 会以 <code>:=</code> 作为其赋值符，如：</p><pre class="line-numbers language-none"><code class="language-none">variable := valuevariable += more<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>等价于：</p><pre class="line-numbers language-none"><code class="language-none">variable := valuevariable := $(variable) more<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>但如果是这种情况：</p><pre class="line-numbers language-none"><code class="language-none">variable = valuevariable += more<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>由于前次的赋值符是 <code>=</code> ，所以 <code>+=</code> 也会以 <code>=</code> 来做为赋值，那么岂不会发生变量的递补归定义，这是很不好的，所以make会自动为我们解决这个问题，我们不必担心这个问题。</p><h2 id="override-指示符"><a href="#override-指示符" class="headerlink" title="override 指示符"></a>override 指示符</h2><p>如果有变量是通常make的命令行参数设置的，那么Makefile中对这个变量的赋值会被忽略。如果你想在Makefile中设置这类参数的值，那么，你可以使用“override”指示符。其语法是:</p><pre class="line-numbers language-none"><code class="language-none">override &lt;variable&gt;; = &lt;value&gt;;override &lt;variable&gt;; := &lt;value&gt;;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>当然，你还可以追加:</p><pre class="line-numbers language-none"><code class="language-none">override &lt;variable&gt;; += &lt;more text&gt;;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>对于多行的变量定义，我们用define指示符，在define指示符前，也同样可以使用override指示符，如:</p><pre class="line-numbers language-none"><code class="language-none">override define foobarendef<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h2 id="多行变量"><a href="#多行变量" class="headerlink" title="多行变量"></a>多行变量</h2><p>还有一种设置变量值的方法是使用define关键字。使用define关键字设置变量的值可以有换行，这有利于定义一系列的命令</p><p>define指示符后面跟的是变量的名字，而重起一行定义变量的值，定义是以endef 关键字结束。其工作方式和“=”操作符一样。变量的值可以包含函数、命令、文字，或是其它变量。因为命令需要以[Tab]键开头，所以如果你用define定义的命令变量中没有以 <code>Tab</code> 键开头，那么make 就不会把其认为是命令。</p><p>下面的这个示例展示了define的用法:</p><pre class="line-numbers language-none"><code class="language-none">define two-linesecho fooecho $(bar)endef<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h2 id="环境变量"><a href="#环境变量" class="headerlink" title="环境变量"></a>环境变量</h2><p>make运行时的系统环境变量可以在make开始运行时被载入到Makefile文件中，但是如果Makefile中已定义了这个变量，或是这个变量由make命令行带入，那么系统的环境变量的值将被覆盖。（如果make指定了“-e”参数，那么，系统环境变量将覆盖Makefile中定义的变量）</p><p>因此，如果我们在环境变量中设置了 <code>CFLAGS</code> 环境变量，那么我们就可以在所有的Makefile中使用这个变量了。这对于我们使用统一的编译参数有比较大的好处。如果Makefile中定义了CFLAGS，那么则会使用Makefile中的这个变量，如果没有定义则使用系统环境变量的值，一个共性和个性的统一，很像“全局变量”和“局部变量”的特性。</p><p>当make嵌套调用时（参见前面的“嵌套调用”章节），上层Makefile中定义的变量会以系统环境变量的方式传递到下层的Makefile 中。当然，默认情况下，只有通过命令行设置的变量会被传递。而定义在文件中的变量，如果要向下层Makefile传递，则需要使用exprot关键字来声明。（参见前面章节）</p><p>当然，我并不推荐把许多的变量都定义在系统环境中，这样，在我们执行不用的Makefile时，拥有的是同一套系统变量，这可能会带来更多的麻烦。</p><h2 id="目标变量"><a href="#目标变量" class="headerlink" title="目标变量"></a>目标变量</h2><p>前面我们所讲的在Makefile中定义的变量都是“全局变量”，在整个文件，我们都可以访问这些变量。当然，“自动化变量”除外，如 <code>$&lt;</code> 等这种类量的自动化变量就属于“规则型变量”，这种变量的值依赖于规则的目标和依赖目标的定义。</p><p>当然，我也同样可以为某个目标设置局部变量，这种变量被称为“Target-specific Variable”，它可以和“全局变量”同名，因为它的作用范围只在这条规则以及连带规则中，所以其值也只在作用范围内有效。而不会影响规则链以外的全局变量的值。</p><p>其语法是：</p><pre class="line-numbers language-none"><code class="language-none">&lt;target ...&gt; : &lt;variable-assignment&gt;;&lt;target ...&gt; : overide &lt;variable-assignment&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><variable-assignment>;可以是前面讲过的各种赋值表达式，如 <code>=</code> 、 <code>:=</code> 、 <code>+= `` 或是 ``?=</code> 。第二个语法是针对于make命令行带入的变量，或是系统环境变量。</variable-assignment></p><p>这个特性非常的有用，当我们设置了这样一个变量，这个变量会作用到由这个目标所引发的所有的规则中去。如：</p><pre class="line-numbers language-none"><code class="language-none">prog : CFLAGS = -gprog : prog.o foo.o bar.o    $(CC) $(CFLAGS) prog.o foo.o bar.oprog.o : prog.c    $(CC) $(CFLAGS) prog.cfoo.o : foo.c    $(CC) $(CFLAGS) foo.cbar.o : bar.c    $(CC) $(CFLAGS) bar.c<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在这个示例中，不管全局的 <code>$(CFLAGS)</code> 的值是什么，在prog目标，以及其所引发的所有规则中（prog.o foo.o bar.o的规则）， <code>$(CFLAGS)</code> 的值都是 <code>-g</code></p><h2 id="模式变量"><a href="#模式变量" class="headerlink" title="模式变量"></a>模式变量</h2><p>在GNU的make中，还支持模式变量（Pattern-specific Variable），通过上面的目标变量中，我们知道，变量可以定义在某个目标上。模式变量的好处就是，我们可以给定一种“模式”，可以把变量定义在符合这种模式的所有目标上。</p><p>我们知道，make的“模式”一般是至少含有一个 <code>%</code> 的，所以，我们可以以如下方式给所有以 <code>.o</code> 结尾的目标定义目标变量：</p><pre class="line-numbers language-none"><code class="language-none">%.o : CFLAGS = -O<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>同样，模式变量的语法和“目标变量”一样：</p><pre class="line-numbers language-none"><code class="language-none">&lt;pattern ...&gt;; : &lt;variable-assignment&gt;;&lt;pattern ...&gt;; : override &lt;variable-assignment&gt;;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>override同样是针对于系统环境传入的变量，或是make命令行指定的变量。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/database/cassandra/cassandra/"/>
      <url>/2021/12/24/database/cassandra/cassandra/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/database/mongodb/mongo-huan-jing-pei-zhi/"/>
      <url>/2021/12/24/database/mongodb/mongo-huan-jing-pei-zhi/</url>
      
        <content type="html"><![CDATA[<h1 id="mongo环境配置"><a href="#mongo环境配置" class="headerlink" title="mongo环境配置"></a>mongo环境配置</h1><h2 id="docker部署"><a href="#docker部署" class="headerlink" title="docker部署"></a>docker部署</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">docker pull mongo<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">docker run -p <span class="token number">27017</span>:27017 -v <span class="token operator">&lt;</span>LocalDirectoryPath<span class="token operator">&gt;</span>:/data/db --name docker_mongodb -d mongo<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h2 id="docker-compose部署"><a href="#docker-compose部署" class="headerlink" title="docker-compose部署"></a>docker-compose部署</h2><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">version</span><span class="token punctuation">:</span> <span class="token string">'3.1'</span><span class="token key atrule">services</span><span class="token punctuation">:</span>  <span class="token key atrule">mongo</span><span class="token punctuation">:</span>    <span class="token key atrule">image</span><span class="token punctuation">:</span> mongo    <span class="token key atrule">restart</span><span class="token punctuation">:</span> always    <span class="token key atrule">environment</span><span class="token punctuation">:</span>      <span class="token key atrule">MONGO_INITDB_ROOT_USERNAME</span><span class="token punctuation">:</span> root      <span class="token key atrule">MONGO_INITDB_ROOT_PASSWORD</span><span class="token punctuation">:</span> root    <span class="token key atrule">command</span><span class="token punctuation">:</span> mongod <span class="token punctuation">-</span><span class="token punctuation">-</span>auth    <span class="token key atrule">ports</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> <span class="token string">"27017:27017"</span>    <span class="token key atrule">volumes</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> <span class="token string">"./data/configdb:/data/configdb"</span>      <span class="token punctuation">-</span> <span class="token string">"./data/db:/data/db"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="helm-部署"><a href="#helm-部署" class="headerlink" title="helm 部署"></a>helm 部署</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="kustomize部署"><a href="#kustomize部署" class="headerlink" title="kustomize部署"></a>kustomize部署</h2><h2 id="ansible部署"><a href="#ansible部署" class="headerlink" title="ansible部署"></a>ansible部署</h2><h2 id="shell部署"><a href="#shell部署" class="headerlink" title="shell部署"></a>shell部署</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/frontend/angularjs/angularjs/"/>
      <url>/2021/12/24/frontend/angularjs/angularjs/</url>
      
        <content type="html"><![CDATA[<h1 id="angularjs"><a href="#angularjs" class="headerlink" title="angularjs"></a>angularjs</h1><blockquote><p>官网：<a href="https://angular.io/">https://angular.io/</a></p><p>github地址：<a href="https://github.com/angular/angular">https://github.com/angular/angular</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/frontend/ant-design/ant-design/"/>
      <url>/2021/12/24/frontend/ant-design/ant-design/</url>
      
        <content type="html"><![CDATA[<h1 id="ant-design"><a href="#ant-design" class="headerlink" title="ant design"></a>ant design</h1><blockquote><p>官网：<a href="https://ant.design/">https://ant.design/</a><br>github 地址：<a href="https://github.com/ant-design">https://github.com/ant-design</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/c-c/tool/makefile/makefile-yin-han-gui-ze/"/>
      <url>/2021/12/24/c-c/tool/makefile/makefile-yin-han-gui-ze/</url>
      
        <content type="html"><![CDATA[<h1 id="makefile隐含规则"><a href="#makefile隐含规则" class="headerlink" title="makefile隐含规则"></a>makefile隐含规则</h1><p>在我们使用Makefile时，有一些我们会经常使用，而且使用频率非常高的东西，比如，我们编译C/C++的源程序为中间目标文件（Unix下是 <code>.o</code> 文件，Windows下是 <code>.obj</code> 文件）。本章讲述的就是一些在Makefile中的“隐含的”，早先约定了的，不需要我们再写出来的规则。</p><p>“隐含规则”也就是一种惯例，make会按照这种“惯例”心照不喧地来运行，那怕我们的Makefile中没有书写这样的规则。例如，把 <code>.c</code> 文件编译成 <code>.o</code> 文件这一规则，你根本就不用写出来，make会自动推导出这种规则，并生成我们需要的 <code>.o</code> 文件。</p><p>“隐含规则”会使用一些我们系统变量，我们可以改变这些系统变量的值来定制隐含规则的运行时的参数。如系统变量 <code>CFLAGS</code> 可以控制编译时的编译器参数。</p><p>我们还可以通过“模式规则”的方式写下自己的隐含规则。用“后缀规则”来定义隐含规则会有许多的限制。使用“模式规则”会更回得智能和清楚，但“后缀规则”可以用来保证我们Makefile的兼容性。我们了解了“隐含规则”，可以让其为我们更好的服务，也会让我们知道一些“约定俗成”了的东西，而不至于使得我们在运行Makefile时出现一些我们觉得莫名其妙的东西。当然，任何事物都是矛盾的，水能载舟，亦可覆舟，所以，有时候“隐含规则”也会给我们造成不小的麻烦。只有了解了它，我们才能更好地使用它。</p><h2 id="使用隐含规则"><a href="#使用隐含规则" class="headerlink" title="使用隐含规则"></a>使用隐含规则</h2><p>如果要使用隐含规则生成你需要的目标，你所需要做的就是不要写出这个目标的规则。那么，make会试图去自动推导产生这个目标的规则和命令，如果 make可以自动推导生成这个目标的规则和命令，那么这个行为就是隐含规则的自动推导。当然，隐含规则是make事先约定好的一些东西。例如，我们有下面的一个Makefile：</p><pre class="line-numbers language-none"><code class="language-none">foo : foo.o bar.o    cc –o foo foo.o bar.o $(CFLAGS) $(LDFLAGS)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>我们可以注意到，这个Makefile中并没有写下如何生成 <code>foo.o</code> 和 <code>bar.o</code> 这两目标的规则和命令。因为make的“隐含规则”功能会自动为我们自动去推导这两个目标的依赖目标和生成命令。</p><p>make会在自己的“隐含规则”库中寻找可以用的规则，如果找到，那么就会使用。如果找不到，那么就会报错。在上面的那个例子中，make调用的隐含规则是，把 <code>.o</code> 的目标的依赖文件置成 <code>.c</code> ，并使用C的编译命令 <code>cc –c $(CFLAGS) foo.c</code> 来生成 <code>foo.o</code> 的目标。也就是说，我们完全没有必要写下下面的两条规则：</p><pre class="line-numbers language-none"><code class="language-none">foo.o : foo.c    cc –c foo.c $(CFLAGS)bar.o : bar.c    cc –c bar.c $(CFLAGS)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>因为，这已经是“约定”好了的事了，make和我们约定好了用C编译器 <code>cc</code> 生成 <code>.o</code> 文件的规则，这就是隐含规则。</p><p>当然，如果我们为 <code>.o</code> 文件书写了自己的规则，那么make就不会自动推导并调用隐含规则，它会按照我们写好的规则忠实地执行。</p><p>还有，在make的“隐含规则库”中，每一条隐含规则都在库中有其顺序，越靠前的则是越被经常使用的，所以，这会导致我们有些时候即使我们显示地指定了目标依赖，make也不会管。如下面这条规则（没有命令）：</p><pre class="line-numbers language-none"><code class="language-none">foo.o : foo.p<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>依赖文件 <code>foo.p</code> （Pascal程序的源文件）有可能变得没有意义。如果目录下存在了 <code>foo.c</code> 文件，那么我们的隐含规则一样会生效，并会通过 <code>foo.c</code> 调用C的编译器生成 <code>foo.o</code> 文件。因为，在隐含规则中，Pascal的规则出现在C的规则之后，所以，make找到可以生成 <code>foo.o</code> 的C的规则就不再寻找下一条规则了。如果你确实不希望任何隐含规则推导，那么，你就不要只写出“依赖规则”，而不写命令。</p><h2 id="隐含规则一览"><a href="#隐含规则一览" class="headerlink" title="隐含规则一览"></a>隐含规则一览</h2><p>这里我们将讲述所有预先设置（也就是make内建）的隐含规则，如果我们不明确地写下规则，那么，make就会在这些规则中寻找所需要规则和命令。当然，我们也可以使用make的参数 <code>-r</code> 或 <code>--no-builtin-rules</code> 选项来取消所有的预设置的隐含规则。</p><p>当然，即使是我们指定了 <code>-r</code> 参数，某些隐含规则还是会生效，因为有许多的隐含规则都是使用了“后缀规则”来定义的，所以，只要隐含规则中有 “后缀列表”（也就一系统定义在目标 <code>.SUFFIXES</code> 的依赖目标），那么隐含规则就会生效。默认的后缀列表是：.out, .a, .ln, .o, .c, .cc, .C, .p, .f, .F, .r, .y, .l, .s, .S, .mod, .sym, .def, .h, .info, .dvi, .tex, .texinfo, .texi, .txinfo, .w, .ch .web, .sh, .elc, .el。具体的细节，我们会在后面讲述。</p><p>还是先来看一看常用的隐含规则吧。</p><ol><li><p>编译C程序的隐含规则。</p><p><code>&lt;n&gt;.o</code> 的目标的依赖目标会自动推导为 <code>&lt;n&gt;.c</code> ，并且其生成命令是 <code>$(CC) –c $(CPPFLAGS) $(CFLAGS)</code></p></li><li><p>编译C++程序的隐含规则。</p><p><code>&lt;n&gt;.o</code> 的目标的依赖目标会自动推导为 <code>&lt;n&gt;.cc</code> 或是 <code>&lt;n&gt;.C</code> ，并且其生成命令是 <code>$(CXX) –c $(CPPFLAGS) $(CFLAGS)</code> 。（建议使用 <code>.cc</code> 作为C++源文件的后缀，而不是 <code>.C</code> ）</p></li><li><p>编译Pascal程序的隐含规则。</p><p><code>&lt;n&gt;.o</code> 的目标的依赖目标会自动推导为 <code>&lt;n&gt;.p</code> ，并且其生成命令是 <code>$(PC) –c $(PFLAGS)</code> 。</p></li><li><p>编译Fortran/Ratfor程序的隐含规则。</p><p><code>&lt;n&gt;.o</code> 的目标的依赖目标会自动推导为 <code>&lt;n&gt;.r</code> 或 <code>&lt;n&gt;.F</code> 或 <code>&lt;n&gt;.f</code> ，并且其生成命令是:</p><ul><li><code>.f</code> <code>$(FC) –c $(FFLAGS)</code></li><li><code>.F</code> <code>$(FC) –c $(FFLAGS) $(CPPFLAGS)</code></li><li><code>.f</code> <code>$(FC) –c $(FFLAGS) $(RFLAGS)</code></li></ul></li><li><p>预处理Fortran/Ratfor程序的隐含规则。</p><p><code>&lt;n&gt;.f</code> 的目标的依赖目标会自动推导为 <code>&lt;n&gt;.r</code> 或 <code>&lt;n&gt;.F</code> 。这个规则只是转换Ratfor 或有预处理的Fortran程序到一个标准的Fortran程序。其使用的命令是：</p><ul><li><code>.F</code> <code>$(FC) –F $(CPPFLAGS) $(FFLAGS)</code></li><li><code>.r</code> <code>$(FC) –F $(FFLAGS) $(RFLAGS)</code></li></ul></li><li><p>编译Modula-2程序的隐含规则。</p><p><code>&lt;n&gt;.sym</code> 的目标的依赖目标会自动推导为 <code>&lt;n&gt;.def</code> ，并且其生成命令是： <code>$(M2C) $(M2FLAGS) $(DEFFLAGS)</code> 。 <code>&lt;n&gt;.o</code> 的目标的依赖目标会自动推导为 <code>&lt;n&gt;.mod</code> ，并且其生成命令是： <code>$(M2C) $(M2FLAGS) $(MODFLAGS)</code> 。</p></li><li><p>汇编和汇编预处理的隐含规则。</p><p><code>&lt;n&gt;.o</code> 的目标的依赖目标会自动推导为 <code>&lt;n&gt;.s</code> ，默认使用编译器 <code>as</code> ，并且其生成命令是： <code>$ (AS) $(ASFLAGS)</code> 。 <code>&lt;n&gt;.s</code> 的目标的依赖目标会自动推导为 <code>&lt;n&gt;.S</code> ，默认使用C预编译器 <code>cpp</code> ，并且其生成命令是： <code>$(AS) $(ASFLAGS)</code> 。</p></li><li><p>链接Object文件的隐含规则。</p><p><code>&lt;n&gt;</code> 目标依赖于 <code>&lt;n&gt;.o</code> ，通过运行C的编译器来运行链接程序生成（一般是 <code>ld</code> ），其生成命令是： <code>$(CC) $(LDFLAGS) &lt;n&gt;.o $(LOADLIBES) $(LDLIBS)</code> 。这个规则对于只有一个源文件的工程有效，同时也对多个Object文件（由不同的源文件生成）的也有效。例如如下规则:</p><pre class="line-numbers language-none"><code class="language-none">x : y.o z.o<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>并且 <code>x.c</code> 、 <code>y.c</code> 和 <code>z.c</code> 都存在时，隐含规则将执行如下命令:</p><pre class="line-numbers language-none"><code class="language-none">cc -c x.c -o x.occ -c y.c -o y.occ -c z.c -o z.occ x.o y.o z.o -o xrm -f x.orm -f y.orm -f z.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>如果没有一个源文件（如上例中的x.c）和你的目标名字（如上例中的x）相关联，那么，你最好写出自己的生成规则，不然，隐含规则会报错的。</p></li><li><p>Yacc C程序时的隐含规则。</p><p><code>&lt;n&gt;.c</code> 的依赖文件被自动推导为 <code>n.y</code> （Yacc生成的文件），其生成命令是： <code>$(YACC) $(YFALGS)</code> 。（“Yacc”是一个语法分析器，关于其细节请查看相关资料）</p></li><li><p>Lex C程序时的隐含规则。</p><p><code>&lt;n&gt;.c</code> 的依赖文件被自动推导为 <code>n.l</code> （Lex生成的文件），其生成命令是： <code>$(LEX) $(LFALGS)</code> 。（关于“Lex”的细节请查看相关资料）</p></li><li><p>Lex Ratfor程序时的隐含规则。</p><p><code>&lt;n&gt;.r</code> 的依赖文件被自动推导为 <code>n.l</code> （Lex生成的文件），其生成命令是： <code>$(LEX) $(LFALGS)</code> 。</p></li><li><p>从C程序、Yacc文件或Lex文件创建Lint库的隐含规则。</p><p><code>&lt;n&gt;.ln</code> （lint生成的文件）的依赖文件被自动推导为 <code>n.c</code> ，其生成命令是： <code>$(LINT) $(LINTFALGS) $(CPPFLAGS) -i</code> 。对于 <code>&lt;n&gt;.y</code> 和 <code>&lt;n&gt;.l</code> 也是同样的规则。</p></li></ol><h2 id="隐含规则使用的变量"><a href="#隐含规则使用的变量" class="headerlink" title="隐含规则使用的变量"></a>隐含规则使用的变量</h2><p>在隐含规则中的命令中，基本上都是使用了一些预先设置的变量。你可以在你的makefile中改变这些变量的值，或是在make的命令行中传入这些值，或是在你的环境变量中设置这些值，无论怎么样，只要设置了这些特定的变量，那么其就会对隐含规则起作用。当然，你也可以利用make的 <code>-R</code> 或 <code>--no–builtin-variables</code> 参数来取消你所定义的变量对隐含规则的作用。</p><p>例如，第一条隐含规则——编译C程序的隐含规则的命令是 <code>$(CC) –c $(CFLAGS) $(CPPFLAGS)</code> 。Make默认的编译命令是 <code>cc</code> ，如果你把变量 <code>$(CC)</code> 重定义成 <code>gcc</code> ，把变量 <code>$(CFLAGS)</code> 重定义成 <code>-g</code> ，那么，隐含规则中的命令全部会以 <code>gcc –c -g $(CPPFLAGS)</code> 的样子来执行了。</p><p>我们可以把隐含规则中使用的变量分成两种：一种是命令相关的，如 <code>CC</code> ；一种是参数相的关，如 <code>CFLAGS</code> 。下面是所有隐含规则中会用到的变量：</p><h3 id="关于命令的变量。"><a href="#关于命令的变量。" class="headerlink" title="关于命令的变量。"></a>关于命令的变量。</h3><ul><li><code>AR</code> : 函数库打包程序。默认命令是 <code>ar</code></li><li><code>AS</code> : 汇编语言编译程序。默认命令是 <code>as</code></li><li><code>CC</code> : C语言编译程序。默认命令是 <code>cc</code></li><li><code>CXX</code> : C++语言编译程序。默认命令是 <code>g++</code></li><li><code>CO</code> : 从 RCS文件中扩展文件程序。默认命令是 <code>co</code></li><li><code>CPP</code> : C程序的预处理器（输出是标准输出设备）。默认命令是 <code>$(CC) –E</code></li><li><code>FC</code> : Fortran 和 Ratfor 的编译器和预处理程序。默认命令是 <code>f77</code></li><li><code>GET</code> : 从SCCS文件中扩展文件的程序。默认命令是 <code>get</code></li><li><code>LEX</code> : Lex方法分析器程序（针对于C或Ratfor）。默认命令是 <code>lex</code></li><li><code>PC</code> : Pascal语言编译程序。默认命令是 <code>pc</code></li><li><code>YACC</code> : Yacc文法分析器（针对于C程序）。默认命令是 <code>yacc</code></li><li><code>YACCR</code> : Yacc文法分析器（针对于Ratfor程序）。默认命令是 <code>yacc –r</code></li><li><code>MAKEINFO</code> : 转换Texinfo源文件（.texi）到Info文件程序。默认命令是 <code>makeinfo</code></li><li><code>TEX</code> : 从TeX源文件创建TeX DVI文件的程序。默认命令是 <code>tex</code></li><li><code>TEXI2DVI</code> : 从Texinfo源文件创建军TeX DVI 文件的程序。默认命令是 <code>texi2dvi</code></li><li><code>WEAVE</code> : 转换Web到TeX的程序。默认命令是 <code>weave</code></li><li><code>CWEAVE</code> : 转换C Web 到 TeX的程序。默认命令是 <code>cweave</code></li><li><code>TANGLE</code> : 转换Web到Pascal语言的程序。默认命令是 <code>tangle</code></li><li><code>CTANGLE</code> : 转换C Web 到 C。默认命令是 <code>ctangle</code></li><li><code>RM</code> : 删除文件命令。默认命令是 <code>rm –f</code></li></ul><h3 id="关于命令参数的变量"><a href="#关于命令参数的变量" class="headerlink" title="关于命令参数的变量"></a>关于命令参数的变量</h3><p>下面的这些变量都是相关上面的命令的参数。如果没有指明其默认值，那么其默认值都是空。</p><ul><li><code>ARFLAGS</code> : 函数库打包程序AR命令的参数。默认值是 <code>rv</code></li><li><code>ASFLAGS</code> : 汇编语言编译器参数。（当明显地调用 <code>.s</code> 或 <code>.S</code> 文件时）</li><li><code>CFLAGS</code> : C语言编译器参数。</li><li><code>CXXFLAGS</code> : C++语言编译器参数。</li><li><code>COFLAGS</code> : RCS命令参数。</li><li><code>CPPFLAGS</code> : C预处理器参数。（ C 和 Fortran 编译器也会用到）。</li><li><code>FFLAGS</code> : Fortran语言编译器参数。</li><li><code>GFLAGS</code> : SCCS “get”程序参数。</li><li><code>LDFLAGS</code> : 链接器参数。（如： <code>ld</code> ）</li><li><code>LFLAGS</code> : Lex文法分析器参数。</li><li><code>PFLAGS</code> : Pascal语言编译器参数。</li><li><code>RFLAGS</code> : Ratfor 程序的Fortran 编译器参数。</li><li><code>YFLAGS</code> : Yacc文法分析器参数。</li></ul><h2 id="隐含规则链"><a href="#隐含规则链" class="headerlink" title="隐含规则链"></a>隐含规则链</h2><p>有些时候，一个目标可能被一系列的隐含规则所作用。例如，一个 <code>.o</code> 的文件生成，可能会是先被 Yacc的[.y]文件先成 <code>.c</code> ，然后再被C的编译器生成。我们把这一系列的隐含规则叫做“隐含规则链”。</p><p>在上面的例子中，如果文件 <code>.c</code> 存在，那么就直接调用C的编译器的隐含规则，如果没有 <code>.c</code> 文件，但有一个 <code>.y</code> 文件，那么Yacc的隐含规则会被调用，生成 <code>.c</code> 文件，然后，再调用C编译的隐含规则最终由 <code>.c</code> 生成 <code>.o</code> 文件，达到目标。</p><p>我们把这种 <code>.c</code> 的文件（或是目标），叫做中间目标。不管怎么样，make会努力自动推导生成目标的一切方法，不管中间目标有多少，其都会执着地把所有的隐含规则和你书写的规则全部合起来分析，努力达到目标，所以，有些时候，可能会让你觉得奇怪，怎么我的目标会这样生成？怎么我的 makefile发疯了？</p><p>在默认情况下，对于中间目标，它和一般的目标有两个地方所不同：第一个不同是除非中间的目标不存在，才会引发中间规则。第二个不同的是，只要目标成功产生，那么，产生最终目标过程中，所产生的中间目标文件会被以 <code>rm -f</code> 删除。</p><p>通常，一个被makefile指定成目标或是依赖目标的文件不能被当作中介。然而，你可以明显地说明一个文件或是目标是中介目标，你可以使用伪目标 <code>.INTERMEDIATE</code> 来强制声明。（如： <code>.INTERMEDIATE : mid</code> ）</p><p>你也可以阻止make自动删除中间目标，要做到这一点，你可以使用伪目标 <code>.SECONDARY</code> 来强制声明（如： <code>.SECONDARY : sec</code> ）。你还可以把你的目标，以模式的方式来指定（如： <code>%.o</code> ）成伪目标 <code>.PRECIOUS</code> 的依赖目标，以保存被隐含规则所生成的中间文件。</p><p>在“隐含规则链”中，禁止同一个目标出现两次或两次以上，这样一来，就可防止在make自动推导时出现无限递归的情况。</p><p>Make会优化一些特殊的隐含规则，而不生成中间文件。如，从文件 <code>foo.c</code> 生成目标程序 <code>foo</code> ，按道理，make会编译生成中间文件 <code>foo.o</code> ，然后链接成 <code>foo</code> ，但在实际情况下，这一动作可以被一条 <code>cc</code> 的命令完成（ <code>cc –o foo foo.c</code> ），于是优化过的规则就不会生成中间文件。</p><h2 id="定义模式规则"><a href="#定义模式规则" class="headerlink" title="定义模式规则"></a>定义模式规则</h2><p>你可以使用模式规则来定义一个隐含规则。一个模式规则就好像一个一般的规则，只是在规则中，目标的定义需要有 <code>%</code> 字符。 <code>%</code> 的意思是表示一个或多个任意字符。在依赖目标中同样可以使用 <code>%</code> ，只是依赖目标中的 <code>%</code> 的取值，取决于其目标。</p><p>有一点需要注意的是， <code>%</code> 的展开发生在变量和函数的展开之后，变量和函数的展开发生在make载入 Makefile时，而模式规则中的 <code>%</code> 则发生在运行时。</p><h3 id="模式规则介绍"><a href="#模式规则介绍" class="headerlink" title="模式规则介绍"></a>模式规则介绍</h3><p>模式规则中，至少在规则的目标定义中要包含 <code>%</code> ，否则，就是一般的规则。目标中的 <code>%</code> 定义表示对文件名的匹配， <code>%</code> 表示长度任意的非空字符串。例如： <code>%.c</code> 表示以 <code>.c</code> 结尾的文件名（文件名的长度至少为3），而 <code>s.%.c</code> 则表示以 <code>s.</code> 开头， <code>.c</code> 结尾的文件名（文件名的长度至少为5）。</p><p>如果 <code>%</code> 定义在目标中，那么，目标中的 <code>%</code> 的值决定了依赖目标中的 <code>%</code> 的值，也就是说，目标中的模式的 <code>%</code> 决定了依赖目标中 <code>%</code> 的样子。例如有一个模式规则如下：</p><pre class="line-numbers language-none"><code class="language-none">%.o : %.c ; &lt;command ......&gt;;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>其含义是，指出了怎么从所有的 <code>.c</code> 文件生成相应的 <code>.o</code> 文件的规则。如果要生成的目标是 <code>a.o b.o</code> ，那么 <code>%c</code> 就是 <code>a.c b.c</code> 。</p><p>一旦依赖目标中的 <code>%</code> 模式被确定，那么，make会被要求去匹配当前目录下所有的文件名，一旦找到，make就会规则下的命令，所以，在模式规则中，目标可能会是多个的，如果有模式匹配出多个目标，make就会产生所有的模式目标，此时，make关心的是依赖的文件名和生成目标的命令这两件事。</p><h3 id="模式规则示例"><a href="#模式规则示例" class="headerlink" title="模式规则示例"></a>模式规则示例</h3><p>下面这个例子表示了,把所有的 <code>.c</code> 文件都编译成 <code>.o</code> 文件.</p><pre class="line-numbers language-none"><code class="language-none">%.o : %.c    $(CC) -c $(CFLAGS) $(CPPFLAGS) $&lt; -o $@<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>其中， <code>$@</code> 表示所有的目标的挨个值， <code>$&lt;</code> 表示了所有依赖目标的挨个值。这些奇怪的变量我们叫“自动化变量”，后面会详细讲述。</p><p>下面的这个例子中有两个目标是模式的：</p><pre class="line-numbers language-none"><code class="language-none">%.tab.c %.tab.h: %.y    bison -d $&lt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>这条规则告诉make把所有的 <code>.y</code> 文件都以 <code>bison -d &lt;n&gt;.y</code> 执行，然后生成 <code>&lt;n&gt;.tab.c</code> 和 <code>&lt;n&gt;.tab.h</code> 文件。（其中， <code>&lt;n&gt;</code> 表示一个任意字符串）。如果我们的执行程序 <code>foo</code> 依赖于文件 <code>parse.tab.o</code> 和 <code>scan.o</code> ，并且文件 <code>scan.o</code> 依赖于文件 <code>parse.tab.h</code> ，如果 <code>parse.y</code> 文件被更新了，那么根据上述的规则， <code>bison -d parse.y</code> 就会被执行一次，于是， <code>parse.tab.o</code> 和 <code>scan.o</code> 的依赖文件就齐了。（假设， <code>parse.tab.o</code> 由 <code>parse.tab.c</code> 生成，和 <code>scan.o</code> 由 <code>scan.c</code> 生成，而 <code>foo</code> 由 <code>parse.tab.o</code> 和 <code>scan.o</code> 链接生成，而且 <code>foo</code> 和其 <code>.o</code> 文件的依赖关系也写好，那么，所有的目标都会得到满足）</p><h3 id="自动化变量"><a href="#自动化变量" class="headerlink" title="自动化变量"></a>自动化变量</h3><p>在上述的模式规则中，目标和依赖文件都是一系例的文件，那么我们如何书写一个命令来完成从不同的依赖文件生成相应的目标？因为在每一次的对模式规则的解析时，都会是不同的目标和依赖文件。</p><p>自动化变量就是完成这个功能的。在前面，我们已经对自动化变量有所提涉，相信你看到这里已对它有一个感性认识了。所谓自动化变量，就是这种变量会把模式中所定义的一系列的文件自动地挨个取出，直至所有的符合模式的文件都取完了。这种自动化变量只应出现在规则的命令中。</p><p>下面是所有的自动化变量及其说明：</p><ul><li><code>$@</code> : 表示规则中的目标文件集。在模式规则中，如果有多个目标，那么， <code>$@</code> 就是匹配于目标中模式定义的集合。</li><li><code>$%</code> : 仅当目标是函数库文件中，表示规则中的目标成员名。例如，如果一个目标是 <code>foo.a(bar.o)</code> ，那么， <code>$%</code> 就是 <code>bar.o</code> ， <code>$@</code> 就是 <code>foo.a</code> 。如果目标不是函数库文件（Unix下是 <code>.a</code> ，Windows下是 <code>.lib</code> ），那么，其值为空。</li><li><code>$&lt;</code> : 依赖目标中的第一个目标名字。如果依赖目标是以模式（即 <code>%</code> ）定义的，那么 <code>$&lt;</code> 将是符合模式的一系列的文件集。注意，其是一个一个取出来的。</li><li><code>$?</code> : 所有比目标新的依赖目标的集合。以空格分隔。</li><li><code>$^</code> : 所有的依赖目标的集合。以空格分隔。如果在依赖目标中有多个重复的，那么这个变量会去除重复的依赖目标，只保留一份。</li><li><code>$+</code> : 这个变量很像 <code>$^</code> ，也是所有依赖目标的集合。只是它不去除重复的依赖目标。</li><li><code>$*</code> : 这个变量表示目标模式中 <code>%</code> 及其之前的部分。如果目标是 <code>dir/a.foo.b</code> ，并且目标的模式是 <code>a.%.b</code> ，那么， <code>$*</code> 的值就是 <code>dir/a.foo</code> 。这个变量对于构造有关联的文件名是比较有较。如果目标中没有模式的定义，那么 <code>$*</code> 也就不能被推导出，但是，如果目标文件的后缀是make所识别的，那么 <code>$*</code> 就是除了后缀的那一部分。例如：如果目标是 <code>foo.c</code> ，因为 <code>.c</code> 是make所能识别的后缀名，所以， <code>$*</code> 的值就是 <code>foo</code> 。这个特性是GNU make的，很有可能不兼容于其它版本的make，所以，你应该尽量避免使用 <code>$*</code> ，除非是在隐含规则或是静态模式中。如果目标中的后缀是make所不能识别的，那么 <code>$*</code> 就是空值。</li></ul><p>当你希望只对更新过的依赖文件进行操作时， <code>$?</code> 在显式规则中很有用，例如，假设有一个函数库文件叫 <code>lib</code> ，其由其它几个object文件更新。那么把object文件打包的比较有效率的Makefile规则是：</p><pre class="line-numbers language-none"><code class="language-none">lib : foo.o bar.o lose.o win.o    ar r lib $?<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>在上述所列出来的自动量变量中。四个变量（ <code>$@</code> 、 <code>$&lt;</code> 、 <code>$%</code> 、 <code>$*</code> ）在扩展时只会有一个文件，而另三个的值是一个文件列表。这七个自动化变量还可以取得文件的目录名或是在当前目录下的符合模式的文件名，只需要搭配上 <code>D</code> 或 <code>F</code> 字样。这是GNU make中老版本的特性，在新版本中，我们使用函数 <code>dir</code> 或 <code>notdir</code> 就可以做到了。 <code>D</code> 的含义就是Directory，就是目录， <code>F</code> 的含义就是File，就是文件。</p><p>下面是对于上面的七个变量分别加上 <code>D</code> 或是 <code>F</code> 的含义：</p><ul><li><p><code>$(@D)</code></p><p>表示 <code>$@</code> 的目录部分（不以斜杠作为结尾），如果 <code>$@</code> 值是 <code>dir/foo.o</code> ，那么 <code>$(@D)</code> 就是 <code>dir</code> ，而如果 <code>$@</code> 中没有包含斜杠的话，其值就是 <code>.</code> （当前目录）。</p></li><li><p><code>$(@F)</code></p><p>表示 <code>$@</code> 的文件部分，如果 <code>$@</code> 值是 <code>dir/foo.o</code> ，那么 <code>$(@F)</code> 就是 <code>foo.o</code> ， <code>$(@F)</code> 相当于函数 <code>$(notdir $@)</code> 。</p></li><li><p><code>$(*D)</code>, <code>$(*F)</code></p><p>和上面所述的同理，也是取文件的目录部分和文件部分。对于上面的那个例子， <code>$(*D)</code> 返回 <code>dir</code> ，而 <code>$(*F)</code> 返回 <code>foo</code></p></li><li><p><code>$(%D)</code>, <code>$(%F)</code></p><p>分别表示了函数包文件成员的目录部分和文件部分。这对于形同 <code>archive(member)</code> 形式的目标中的 <code>member</code> 中包含了不同的目录很有用。</p></li><li><p><code>$(&lt;D)</code>, <code>$(&lt;F)</code></p><p>分别表示依赖文件的目录部分和文件部分。</p></li><li><p><code>$(^D)</code>, <code>$(^F)</code></p><p>分别表示所有依赖文件的目录部分和文件部分。（无相同的）</p></li><li><p><code>$(+D)</code>, <code>$(+F)</code></p><p>分别表示所有依赖文件的目录部分和文件部分。（可以有相同的）</p></li><li><p><code>$(?D)</code>, <code>$(?F)</code></p><p>分别表示被更新的依赖文件的目录部分和文件部分。</p></li></ul><p>最后想提醒一下的是，对于 <code>$&lt;</code> ，为了避免产生不必要的麻烦，我们最好给 <code>$</code> 后面的那个特定字符都加上圆括号，比如， <code>$(&lt;)</code> 就要比 <code>$&lt;</code> 要好一些。</p><p>还得要注意的是，这些变量只使用在规则的命令中，而且一般都是“显式规则”和“静态模式规则”（参见前面“书写规则”一章）。其在隐含规则中并没有意义。</p><h3 id="模式的匹配"><a href="#模式的匹配" class="headerlink" title="模式的匹配"></a>模式的匹配</h3><p>一般来说，一个目标的模式有一个有前缀或是后缀的 <code>%</code> ，或是没有前后缀，直接就是一个 <code>%</code> 。因为 <code>%</code> 代表一个或多个字符，所以在定义好了的模式中，我们把 <code>%</code> 所匹配的内容叫做“茎”，例如 <code>%.c</code> 所匹配的文件“test.c”中“test”就是“茎”。因为在目标和依赖目标中同时有 <code>%</code> 时，依赖目标的“茎”会传给目标，当做目标中的“茎”。</p><p>当一个模式匹配包含有斜杠（实际也不经常包含）的文件时，那么在进行模式匹配时，目录部分会首先被移开，然后进行匹配，成功后，再把目录加回去。在进行“茎”的传递时，我们需要知道这个步骤。例如有一个模式 <code>e%t</code> ，文件 <code>src/eat</code> 匹配于该模式，于是 <code>src/a</code> 就是其“茎”，如果这个模式定义在依赖目标中，而被依赖于这个模式的目标中又有个模式 <code>c%r</code> ，那么，目标就是 <code>src/car</code> 。（“茎”被传递）</p><h3 id="重载内建隐含规则"><a href="#重载内建隐含规则" class="headerlink" title="重载内建隐含规则"></a>重载内建隐含规则</h3><p>你可以重载内建的隐含规则（或是定义一个全新的），例如你可以重新构造和内建隐含规则不同的命令，如：</p><pre class="line-numbers language-none"><code class="language-none">%.o : %.c    $(CC) -c $(CPPFLAGS) $(CFLAGS) -D$(date)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>你可以取消内建的隐含规则，只要不在后面写命令就行。如：</p><pre class="line-numbers language-none"><code class="language-none">%.o : %.s<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>同样，你也可以重新定义一个全新的隐含规则，其在隐含规则中的位置取决于你在哪里写下这个规则。朝前的位置就靠前。</p><h2 id="老式风格的“后缀规则”"><a href="#老式风格的“后缀规则”" class="headerlink" title="老式风格的“后缀规则”"></a>老式风格的“后缀规则”</h2><p>后缀规则是一个比较老式的定义隐含规则的方法。后缀规则会被模式规则逐步地取代。因为模式规则更强更清晰。为了和老版本的Makefile兼容，GNU make同样兼容于这些东西。后缀规则有两种方式：“双后缀”和“单后缀”。</p><p>双后缀规则定义了一对后缀：目标文件的后缀和依赖目标（源文件）的后缀。如 <code>.c.o</code> 相当于 <code>%o : %c</code> 。单后缀规则只定义一个后缀，也就是源文件的后缀。如 <code>.c</code> 相当于 <code>% : %.c</code> 。</p><p>后缀规则中所定义的后缀应该是make所认识的，如果一个后缀是make所认识的，那么这个规则就是单后缀规则，而如果两个连在一起的后缀都被make所认识，那就是双后缀规则。例如： <code>.c</code> 和 <code>.o</code> 都是make所知道。因而，如果你定义了一个规则是 <code>.c.o</code> 那么其就是双后缀规则，意义就是 <code>.c</code> 是源文件的后缀， <code>.o</code> 是目标文件的后缀。如下示例：</p><pre class="line-numbers language-none"><code class="language-none">.c.o:    $(CC) -c $(CFLAGS) $(CPPFLAGS) -o $@ $&lt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>后缀规则不允许任何的依赖文件，如果有依赖文件的话，那就不是后缀规则，那些后缀统统被认为是文件名，如：</p><pre class="line-numbers language-none"><code class="language-none">.c.o: foo.h    $(CC) -c $(CFLAGS) $(CPPFLAGS) -o $@ $&lt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>这个例子，就是说，文件 <code>.c.o</code> 依赖于文件 <code>foo.h</code> ，而不是我们想要的这样：</p><pre class="line-numbers language-none"><code class="language-none">%.o: %.c foo.h    $(CC) -c $(CFLAGS) $(CPPFLAGS) -o $@ $&lt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>后缀规则中，如果没有命令，那是毫无意义的。因为他也不会移去内建的隐含规则。</p><p>而要让make知道一些特定的后缀，我们可以使用伪目标 <code>.SUFFIXES</code> 来定义或是删除，如：</p><pre class="line-numbers language-none"><code class="language-none">.SUFFIXES: .hack .win<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>把后缀 <code>.hack</code> 和 <code>.win</code> 加入后缀列表中的末尾。</p><pre class="line-numbers language-none"><code class="language-none">.SUFFIXES:              # 删除默认的后缀.SUFFIXES: .c .o .h   # 定义自己的后缀<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>先清除默认后缀，后定义自己的后缀列表。</p><p>make的参数 <code>-r</code> 或 <code>-no-builtin-rules</code> 也会使用得默认的后缀列表为空。而变量 <code>SUFFIXE</code> 被用来定义默认的后缀列表，你可以用 <code>.SUFFIXES</code> 来改变后缀列表，但请不要改变变量 <code>SUFFIXE</code> 的值。</p><h2 id="隐含规则搜索算法"><a href="#隐含规则搜索算法" class="headerlink" title="隐含规则搜索算法"></a>隐含规则搜索算法</h2><p>比如我们有一个目标叫 T。下面是搜索目标T的规则的算法。请注意，在下面，我们没有提到后缀规则，原因是，所有的后缀规则在Makefile被载入内存时，会被转换成模式规则。如果目标是 <code>archive(member)</code> 的函数库文件模式，那么这个算法会被运行两次，第一次是找目标T，如果没有找到的话，那么进入第二次，第二次会把 <code>member</code> 当作T来搜索。</p><ol><li>把T的目录部分分离出来。叫D，而剩余部分叫N。（如：如果T是 <code>src/foo.o</code> ，那么，D就是 <code>src/</code> ，N就是 <code>foo.o</code> ）</li><li>创建所有匹配于T或是N的模式规则列表。</li><li>如果在模式规则列表中有匹配所有文件的模式，如 <code>%</code> ，那么从列表中移除其它的模式。</li><li>移除列表中没有命令的规则。</li><li>对于第一个在列表中的模式规则：<ol><li>推导其“茎”S，S应该是T或是N匹配于模式中 <code>%</code> 非空的部分。</li><li>计算依赖文件。把依赖文件中的 <code>%</code> 都替换成“茎”S。如果目标模式中没有包含斜框字符，而把D加在第一个依赖文件的开头。</li><li>测试是否所有的依赖文件都存在或是理当存在。（如果有一个文件被定义成另外一个规则的目标文件，或者是一个显式规则的依赖文件，那么这个文件就叫“理当存在”）</li><li>如果所有的依赖文件存在或是理当存在，或是就没有依赖文件。那么这条规则将被采用，退出该算法。</li></ol></li><li>如果经过第5步，没有模式规则被找到，那么就做更进一步的搜索。对于存在于列表中的第一个模式规则：<ol><li>如果规则是终止规则，那就忽略它，继续下一条模式规则。</li><li>计算依赖文件。（同第5步）</li><li>测试所有的依赖文件是否存在或是理当存在。</li><li>对于不存在的依赖文件，递归调用这个算法查找他是否可以被隐含规则找到。</li><li>如果所有的依赖文件存在或是理当存在，或是就根本没有依赖文件。那么这条规则被采用，退出该算法。</li><li>如果没有隐含规则可以使用，查看 <code>.DEFAULT</code> 规则，如果有，采用，把 <code>.DEFAULT</code> 的命令给T使用。</li></ol></li></ol><p>一旦规则被找到，就会执行其相当的命令，而此时，我们的自动化变量的值才会生成。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/c-c/tool/makefile/makefile-shu-xie-ming-ling/"/>
      <url>/2021/12/24/c-c/tool/makefile/makefile-shu-xie-ming-ling/</url>
      
        <content type="html"><![CDATA[<h1 id="makefile书写命令"><a href="#makefile书写命令" class="headerlink" title="makefile书写命令"></a>makefile书写命令</h1><p>每条规则中的命令和操作系统Shell的命令行是一致的。make会一按顺序一条一条的执行命令，每条命令的开头必须以 <code>Tab</code> 键开头，除非，命令是紧跟在依赖规则后面的分号后的。在命令行之间中的空格或是空行会被忽略，但是如果该空格或空行是以Tab键开头的，那么make会认为其是一个空命令。</p><p>我们在UNIX下可能会使用不同的Shell，但是make的命令默认是被 <code>/bin/sh</code> ——UNIX的标准Shell 解释执行的。除非你特别指定一个其它的Shell。Makefile中， <code>#</code> 是注释符</p><h2 id="显示命令"><a href="#显示命令" class="headerlink" title="显示命令"></a>显示命令</h2><p>通常，make会把其要执行的命令行在命令执行前输出到屏幕上。当我们用 <code>@</code> 字符在命令行前，那么，这个命令将不被make显示出来，最具代表性的例子是，我们用这个功能来向屏幕显示一些信息。如:</p><pre class="line-numbers language-makefile" data-language="makefile"><code class="language-makefile"><span class="token operator">@</span>echo 正在编译XXX模块......<span class="token comment"># 当make执行时，会输出“正在编译XXX模块……”字串，但不会输出命令，如果没有“@”，那么，make将输出</span>echo 正在编译XXX模块......正在编译XXX模块......<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>如果make执行时，带入make参数 <code>-n</code> 或 <code>--just-print</code> ，那么其只是显示命令，但不会执行命令，这个功能很有利于我们调试我们的Makefile，看看我们书写的命令是执行起来是什么样子的或是什么顺序的。而make参数 <code>-s</code> 或 <code>--silent</code> 或 <code>--quiet</code> 则是全面禁止命令的显示。</p><h2 id="命令执行"><a href="#命令执行" class="headerlink" title="命令执行"></a>命令执行</h2><p>当依赖目标新于目标时，也就是当规则的目标需要被更新时，make会一条一条的执行其后的命令。需要注意的是，如果你要让上一条命令的结果应用在下一条命令时，你应该使用分号分隔这两条命令。比如你的第一条命令是cd命令，你希望第二条命令得在cd之后的基础上运行，那么你就不能把这两条命令写在两行上，而应该把这两条命令写在一行上，用分号分隔。如：</p><ul><li>示例一：</li></ul><pre class="line-numbers language-none"><code class="language-none">exec:    cd /home/hchen    pwd<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><ul><li>示例二：</li></ul><pre class="line-numbers language-none"><code class="language-none">exec:    cd /home/hchen; pwd<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>当我们执行 <code>make exec</code> 时，第一个例子中的cd没有作用，pwd会打印出当前的Makefile目录，而第二个例子中，cd就起作用了，pwd会打印出“/home/hchen”。</p><p>make一般是使用环境变量SHELL中所定义的系统Shell来执行命令，默认情况下使用UNIX的标准Shell——/bin/sh来执行命令。但在MS-DOS下有点特殊，因为MS-DOS下没有SHELL环境变量，当然你也可以指定。如果你指定了UNIX风格的目录形式，首先，make会在SHELL所指定的路径中找寻命令解释器，如果找不到，其会在当前盘符中的当前目录中寻找，如果再找不到，其会在PATH环境变量中所定义的所有路径中寻找。MS-DOS中，如果你定义的命令解释器没有找到，其会给你的命令解释器加上诸如 <code>.exe</code> 、 <code>.com</code> 、 <code>.bat</code> 、 <code>.sh</code> 等后缀。</p><h2 id="命令出错"><a href="#命令出错" class="headerlink" title="命令出错"></a>命令出错</h2><p>每当命令运行完后，make会检测每个命令的返回码，如果命令返回成功，那么make会执行下一条命令，当规则中所有的命令成功返回后，这个规则就算是成功完成了。如果一个规则中的某个命令出错了（命令退出码非零），那么make就会终止执行当前规则，这将有可能终止所有规则的执行。</p><p>有些时候，命令的出错并不表示就是错误的。例如mkdir命令，我们一定需要建立一个目录，如果目录不存在，那么mkdir就成功执行，万事大吉，如果目录存在，那么就出错了。我们之所以使用mkdir的意思就是一定要有这样的一个目录，于是我们就不希望mkdir出错而终止规则的运行。</p><p>为了做到这一点，忽略命令的出错，我们可以在Makefile的命令行前加一个减号 <code>-</code> （在Tab键之后），标记为不管命令出不出错都认为是成功的。如：</p><pre class="line-numbers language-none"><code class="language-none">clean:    -rm -f *.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>还有一个全局的办法是，给make加上 <code>-i</code> 或是 <code>--ignore-errors</code> 参数，那么，Makefile中所有命令都会忽略错误。而如果一个规则是以 <code>.IGNORE</code> 作为目标的，那么这个规则中的所有命令将会忽略错误。这些是不同级别的防止命令出错的方法，你可以根据你的不同喜欢设置。</p><p>还有一个要提一下的make的参数的是 <code>-k</code> 或是 <code>--keep-going</code> ，这个参数的意思是，如果某规则中的命令出错了，那么就终止该规则的执行，但继续执行其它规则。</p><h2 id="嵌套执行make"><a href="#嵌套执行make" class="headerlink" title="嵌套执行make"></a>嵌套执行make</h2><p>在一些大的工程中，我们会把我们不同模块或是不同功能的源文件放在不同的目录中，我们可以在每个目录中都书写一个该目录的Makefile，这有利于让我们的Makefile变得更加地简洁，而不至于把所有的东西全部写在一个Makefile中，这样会很难维护我们的Makefile，这个技术对于我们模块编译和分段编译有着非常大的好处。</p><p>例如，我们有一个子目录叫subdir，这个目录下有个Makefile文件，来指明了这个目录下文件的编译规则。那么我们总控的Makefile可以这样书写：</p><pre class="line-numbers language-none"><code class="language-none">subsystem:    cd subdir &amp;&amp; $(MAKE)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>其等价于：</p><pre class="line-numbers language-none"><code class="language-none">subsystem:    $(MAKE) -C subdir<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>定义$(MAKE)宏变量的意思是，也许我们的make需要一些参数，所以定义成一个变量比较利于维护。这两个例子的意思都是先进入“subdir”目录，然后执行make命令。</p><p>我们把这个Makefile叫做“总控Makefile”，总控Makefile的变量可以传递到下级的Makefile中（如果你显示的声明），但是不会覆盖下层的Makefile中所定义的变量，除非指定了 <code>-e</code> 参数。</p><p>如果你要传递变量到下级Makefile中，那么你可以使用这样的声明:</p><pre class="line-numbers language-none"><code class="language-none">export &lt;variable ...&gt;;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>如果你不想让某些变量传递到下级Makefile中，那么你可以这样声明:</p><pre class="line-numbers language-none"><code class="language-none">unexport &lt;variable ...&gt;;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>如：</p><p>示例一：</p><pre class="line-numbers language-none"><code class="language-none">export variable = value<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>其等价于：</p><pre class="line-numbers language-none"><code class="language-none">variable = valueexport variable<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>其等价于：</p><pre class="line-numbers language-none"><code class="language-none">export variable := value<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>其等价于：</p><pre class="line-numbers language-none"><code class="language-none">variable := valueexport variable<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>示例二：</p><pre class="line-numbers language-none"><code class="language-none">export variable += value<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>其等价于：</p><pre class="line-numbers language-none"><code class="language-none">variable += valueexport variable<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>如果你要传递所有的变量，那么，只要一个export就行了。后面什么也不用跟，表示传递所有的变量。</p><p>需要注意的是，有两个变量，一个是 <code>SHELL</code> ，一个是 <code>MAKEFLAGS</code> ，这两个变量不管你是否export，其总是要传递到下层 Makefile中，特别是 <code>MAKEFLAGS</code> 变量，其中包含了make的参数信息，如果我们执行“总控Makefile”时有make参数或是在上层 Makefile中定义了这个变量，那么 <code>MAKEFLAGS</code> 变量将会是这些参数，并会传递到下层Makefile中，这是一个系统级的环境变量。</p><p>但是make命令中的有几个参数并不往下传递，它们是 <code>-C</code> , <code>-f</code> , <code>-h</code>, <code>-o</code> 和 <code>-W</code> （有关Makefile参数的细节将在后面说明），如果你不想往下层传递参数，那么，你可以这样来：</p><pre class="line-numbers language-none"><code class="language-none">subsystem:    cd subdir &amp;&amp; $(MAKE) MAKEFLAGS=<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>如果你定义了环境变量 <code>MAKEFLAGS</code> ，那么你得确信其中的选项是大家都会用到的，如果其中有 <code>-t</code> , <code>-n</code> 和 <code>-q</code> 参数，那么将会有让你意想不到的结果，或许会让你异常地恐慌。</p><p>还有一个在“嵌套执行”中比较有用的参数， <code>-w</code> 或是 <code>--print-directory</code> 会在make的过程中输出一些信息，让你看到目前的工作目录。比如，如果我们的下级make目录是“/home/hchen/gnu/make”，如果我们使用 <code>make -w</code> 来执行，那么当进入该目录时，我们会看到:</p><pre class="line-numbers language-none"><code class="language-none">make: Entering directory `/home/hchen/gnu/make'.<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>而在完成下层make后离开目录时，我们会看到:</p><pre class="line-numbers language-none"><code class="language-none">make: Leaving directory `/home/hchen/gnu/make'<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>当你使用 <code>-C</code> 参数来指定make下层Makefile时， <code>-w</code> 会被自动打开的。如果参数中有 <code>-s</code> （ <code>--slient</code> ）或是 <code>--no-print-directory</code> ，那么， <code>-w</code> 总是失效的。</p><h2 id="定义命令包"><a href="#定义命令包" class="headerlink" title="定义命令包"></a>定义命令包</h2><p>如果Makefile中出现一些相同命令序列，那么我们可以为这些相同的命令序列定义一个变量。定义这种命令序列的语法以 <code>define</code> 开始，以 <code>endef</code> 结束，如:</p><pre class="line-numbers language-none"><code class="language-none">define run-yaccyacc $(firstword $^)mv y.tab.c $@endef<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>这里，“run-yacc”是这个命令包的名字，其不要和Makefile中的变量重名。在 <code>define</code> 和 <code>endef</code> 中的两行就是命令序列。这个命令包中的第一个命令是运行Yacc程序，因为Yacc程序总是生成“y.tab.c”的文件，所以第二行的命令就是把这个文件改改名字。还是把这个命令包放到一个示例中来看看吧。</p><pre class="line-numbers language-none"><code class="language-none">foo.c : foo.y    $(run-yacc)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>我们可以看见，要使用这个命令包，我们就好像使用变量一样。在这个命令包的使用中，命令包“run-yacc”中的 <code>$^</code> 就是 <code>foo.y</code> ， <code>$@</code> 就是 <code>foo.c</code> （有关这种以 <code>$</code> 开头的特殊变量，我们会在后面介绍），make在执行命令包时，命令包中的每个命令会被依次独立执行。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/c-c/tool/makefile/makefile-shu-xie-gui-ze/"/>
      <url>/2021/12/24/c-c/tool/makefile/makefile-shu-xie-gui-ze/</url>
      
        <content type="html"><![CDATA[<h1 id="makefile书写规则"><a href="#makefile书写规则" class="headerlink" title="makefile书写规则"></a>makefile书写规则</h1><p>规则包含两个部分，一个是依赖关系，一个是生成目标的方法。</p><p>在Makefile中，规则的顺序是很重要的，因为，Makefile中只应该有一个最终目标，其它的目标都是被这个目标所连带出来的，所以一定要让make知道你的最终目标是什么。一般来说，定义在Makefile中的目标可能会有很多，但是第一条规则中的目标将被确立为最终的目标。如果第一条规则中的目标有很多个，那么，第一个目标会成为最终的目标。make所完成的也就是这个目标。</p><p>好了，还是让我们来看一看如何书写规则。</p><h2 id="规则举例"><a href="#规则举例" class="headerlink" title="规则举例"></a>规则举例</h2><pre class="line-numbers language-none"><code class="language-none">foo.o: foo.c defs.h       # foo模块    cc -c -g foo.c<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>看到这个例子，各位应该不是很陌生了，前面也已说过， <code>foo.o</code> 是我们的目标， <code>foo.c</code> 和 <code>defs.h</code> 是目标所依赖的源文件，而只有一个命令 <code>cc -c -g foo.c</code> （以Tab键开头）。这个规则告诉我们两件事：</p><ol><li>文件的依赖关系， <code>foo.o</code> 依赖于 <code>foo.c</code> 和 <code>defs.h</code> 的文件，如果 <code>foo.c</code> 和 <code>defs.h</code> 的文件日期要比 <code>foo.o</code> 文件日期要新，或是 <code>foo.o</code> 不存在，那么依赖关系发生。</li><li>生成或更新 <code>foo.o</code> 文件，就是那个cc命令。它说明了如何生成 <code>foo.o</code> 这个文件。（当然，foo.c文件include了defs.h文件）</li></ol><h2 id="规则的语法"><a href="#规则的语法" class="headerlink" title="规则的语法"></a>规则的语法</h2><pre class="line-numbers language-none"><code class="language-none">targets : prerequisites    command    ...<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>或是这样：</p><pre class="line-numbers language-none"><code class="language-none">targets : prerequisites ; command    command    ...<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>targets是文件名，以空格分开，可以使用通配符。一般来说，我们的目标基本上是一个文件，但也有可能是多个文件。</p><p>command是命令行，如果其不与“target:prerequisites”在一行，那么，必须以 <code>Tab</code> 键开头，如果和prerequisites在一行，那么可以用分号做为分隔。（见上）</p><p>prerequisites也就是目标所依赖的文件（或依赖目标）。如果其中的某个文件要比目标文件要新，那么，目标就被认为是“过时的”，被认为是需要重生成的。这个在前面已经讲过了。</p><p>如果命令太长，你可以使用反斜杠（ <code>\</code> ）作为换行符。make对一行上有多少个字符没有限制。规则告诉make两件事，文件的依赖关系和如何生成目标文件。</p><p>一般来说，make会以UNIX的标准Shell，也就是 <code>/bin/sh</code> 来执行命令。</p><h2 id="在规则中使用通配符"><a href="#在规则中使用通配符" class="headerlink" title="在规则中使用通配符"></a>在规则中使用通配符</h2><p>如果我们想定义一系列比较类似的文件，我们很自然地就想起使用通配符。make支持三个通配符： <code>*</code> ， <code>?</code> 和 <code>~</code> 。这是和Unix的B-Shell是相同的。</p><p>波浪号（ <code>~</code> ）字符在文件名中也有比较特殊的用途。如果是 <code>~/test</code> ，这就表示当前用户的 <code>$HOME</code> 目录下的test目录。而 <code>~hchen/test</code> 则表示用户hchen的宿主目录下的test 目录。（这些都是Unix下的小知识了，make也支持）而在Windows或是 MS-DOS下，用户没有宿主目录，那么波浪号所指的目录则根据环境变量“HOME”而定。</p><p>通配符代替了你一系列的文件，如 <code>*.c</code> 表示所有后缀为c的文件。一个需要我们注意的是，如果我们的文件名中有通配符，如： <code>*</code> ，那么可以用转义字符 <code>\</code> ，如 <code>\*</code> 来表示真实的 <code>*</code> 字符，而不是任意长度的字符串。</p><p>好吧，还是先来看几个例子吧：</p><pre class="line-numbers language-none"><code class="language-none">clean:    rm -f *.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>其实在这个clean:后面可以加上你想做的一些事情，如果你想看到在编译完后看看main.c的源代码，你可以在加上cat这个命令，例子如下：</p><pre class="line-numbers language-none"><code class="language-none">clean:    cat main.c    rm -f *.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>其结果你试一下就知道的。 上面这个例子我不不多说了，这是操作系统Shell所支持的通配符。这是在命令中的通配符。</p><pre class="line-numbers language-none"><code class="language-none">print: *.c    lpr -p $?    touch print<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>上面这个例子说明了通配符也可以在我们的规则中，目标print依赖于所有的 <code>.c</code> 文件。其中的 <code>$?</code> 是一个自动化变量，我会在后面给你讲述。</p><pre class="line-numbers language-none"><code class="language-none">objects = *.o<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>上面这个例子，表示了通配符同样可以用在变量中。并不是说 <code>*.o</code> 会展开，不！objects的值就是 <code>*.o</code> 。Makefile中的变量其实就是C/C++中的宏。如果你要让通配符在变量中展开，也就是让objects的值是所有 <code>.o</code> 的文件名的集合，那么，你可以这样：</p><pre class="line-numbers language-none"><code class="language-none">objects := $(wildcard *.o)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>另给一个变量使用通配符的例子：</p><ol><li><p>列出一确定文件夹中的所有 <code>.c</code> 文件。</p><pre class="line-numbers language-none"><code class="language-none">objects := $(wildcard *.c)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>列出(1)中所有文件对应的 <code>.o</code> 文件，在（3）中我们可以看到它是由make自动编译出的:</p><pre class="line-numbers language-none"><code class="language-none">$(patsubst %.c,%.o,$(wildcard *.c))<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>由(1)(2)两步，可写出编译并链接所有 <code>.c</code> 和 <code>.o</code> 文件</p><pre class="line-numbers language-none"><code class="language-none">objects := $(patsubst %.c,%.o,$(wildcard *.c))foo : $(objects)    cc -o foo $(objects)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li></ol><p>这种用法由关键字“wildcard”，“patsubst”指出，关于Makefile的关键字，我们将在后面讨论。</p><h2 id="文件搜寻"><a href="#文件搜寻" class="headerlink" title="文件搜寻"></a>文件搜寻</h2><p>在一些大的工程中，有大量的源文件，我们通常的做法是把这许多的源文件分类，并存放在不同的目录中。所以，当make需要去找寻文件的依赖关系时，你可以在文件前加上路径，但最好的方法是把一个路径告诉make，让make在自动去找。</p><p>Makefile文件中的特殊变量 <code>VPATH</code> 就是完成这个功能的，如果没有指明这个变量，make只会在当前的目录中去找寻依赖文件和目标文件。如果定义了这个变量，那么，make就会在当前目录找不到的情况下，到所指定的目录中去找寻文件了。</p><pre class="line-numbers language-none"><code class="language-none">VPATH = src:../headers<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>上面的定义指定两个目录，“src”和“../headers”，make会按照这个顺序进行搜索。目录由“冒号”分隔。（当然，当前目录永远是最高优先搜索的地方）</p><p>另一个设置文件搜索路径的方法是使用make的“vpath”关键字（注意，它是全小写的），这不是变量，这是一个make的关键字，这和上面提到的那个VPATH变量很类似，但是它更为灵活。它可以指定不同的文件在不同的搜索目录中。这是一个很灵活的功能。它的使用方法有三种：</p><ul><li><p><code>vpath &lt;pattern&gt; &lt;directories&gt;</code></p><p>为符合模式<pattern>的文件指定搜索目录<directories>。</directories></pattern></p></li><li><p><code>vpath &lt;pattern&gt;</code></p><p>清除符合模式<pattern>的文件的搜索目录。</pattern></p></li><li><p><code>vpath</code></p><p>清除所有已被设置好了的文件搜索目录。</p></li></ul><p>vapth使用方法中的<pattern>需要包含 <code>%</code> 字符。 <code>%</code> 的意思是匹配零或若干字符，（需引用 <code>%</code> ，使用 <code>\</code> ）例如， <code>%.h</code> 表示所有以 <code>.h</code> 结尾的文件。<pattern>指定了要搜索的文件集，而<directories>则指定了&lt; pattern&gt;的文件集的搜索的目录。例如：</directories></pattern></pattern></p><pre class="line-numbers language-none"><code class="language-none">vpath %.h ../headers<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>该语句表示，要求make在“../headers”目录下搜索所有以 <code>.h</code> 结尾的文件。（如果某文件在当前目录没有找到的话）</p><p>我们可以连续地使用vpath语句，以指定不同搜索策略。如果连续的vpath语句中出现了相同的<pattern> ，或是被重复了的<pattern>，那么，make会按照vpath语句的先后顺序来执行搜索。如：</pattern></pattern></p><pre class="line-numbers language-none"><code class="language-none">vpath %.c foovpath %   blishvpath %.c bar<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>其表示 <code>.c</code> 结尾的文件，先在“foo”目录，然后是“blish”，最后是“bar”目录。</p><pre class="line-numbers language-none"><code class="language-none">vpath %.c foo:barvpath %   blish<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>而上面的语句则表示 <code>.c</code> 结尾的文件，先在“foo”目录，然后是“bar”目录，最后才是“blish”目录。</p><h2 id="伪目标"><a href="#伪目标" class="headerlink" title="伪目标"></a>伪目标</h2><p>最早先的一个例子中，我们提到过一个“clean”的目标，这是一个“伪目标”，</p><pre class="line-numbers language-none"><code class="language-none">clean:    rm *.o temp<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>正像我们前面例子中的“clean”一样，既然我们生成了许多文件编译文件，我们也应该提供一个清除它们的“目标”以备完整地重编译而用。 （以“make clean”来使用该目标）</p><p>因为，我们并不生成“clean”这个文件。“伪目标”并不是一个文件，只是一个标签，由于“伪目标”不是文件，所以make无法生成它的依赖关系和决定它是否要执行。我们只有通过显式地指明这个“目标”才能让其生效。当然，“伪目标”的取名不能和文件名重名，不然其就失去了“伪目标”的意义了。</p><p>当然，为了避免和文件重名的这种情况，我们可以使用一个特殊的标记“.PHONY”来显式地指明一个目标是“伪目标”，向make说明，不管是否有这个文件，这个目标就是“伪目标”。</p><pre class="line-numbers language-none"><code class="language-none">.PHONY : clean<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>只要有这个声明，不管是否有“clean”文件，要运行“clean”这个目标，只有“make clean”这样。于是整个过程可以这样写：</p><pre class="line-numbers language-none"><code class="language-none">.PHONY : cleanclean :    rm *.o temp<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>伪目标一般没有依赖的文件。但是，我们也可以为伪目标指定所依赖的文件。伪目标同样可以作为“默认目标”，只要将其放在第一个。一个示例就是，如果你的Makefile需要一口气生成若干个可执行文件，但你只想简单地敲一个make完事，并且，所有的目标文件都写在一个Makefile中，那么你可以使用“伪目标”这个特性：</p><pre class="line-numbers language-none"><code class="language-none">all : prog1 prog2 prog3.PHONY : allprog1 : prog1.o utils.o    cc -o prog1 prog1.o utils.oprog2 : prog2.o    cc -o prog2 prog2.oprog3 : prog3.o sort.o utils.o    cc -o prog3 prog3.o sort.o utils.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>我们知道，Makefile中的第一个目标会被作为其默认目标。我们声明了一个“all”的伪目标，其依赖于其它三个目标。由于默认目标的特性是，总是被执行的，但由于“all”又是一个伪目标，伪目标只是一个标签不会生成文件，所以不会有“all”文件产生。于是，其它三个目标的规则总是会被决议。也就达到了我们一口气生成多个目标的目的。 <code>.PHONY : all</code> 声明了“all”这个目标为“伪目标”。（注：这里的显式“.PHONY : all” 不写的话一般情况也可以正确的执行，这样make可通过隐式规则推导出， “all” 是一个伪目标，执行make不会生成“all”文件，而执行后面的多个目标。建议：显式写出是一个好习惯。）</p><p>随便提一句，从上面的例子我们可以看出，目标也可以成为依赖。所以，伪目标同样也可成为依赖。看下面的例子：</p><pre class="line-numbers language-none"><code class="language-none">.PHONY : cleanall cleanobj cleandiffcleanall : cleanobj cleandiff    rm programcleanobj :    rm *.ocleandiff :    rm *.diff<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>“make cleanall”将清除所有要被清除的文件。“cleanobj”和“cleandiff”这两个伪目标有点像“子程序”的意思。我们可以输入“make cleanall”和“make cleanobj”和“make cleandiff”命令来达到清除不同种类文件的目的。</p><h2 id="多目标"><a href="#多目标" class="headerlink" title="多目标"></a>多目标</h2><p>Makefile的规则中的目标可以不止一个，其支持多目标，有可能我们的多个目标同时依赖于一个文件，并且其生成的命令大体类似。于是我们就能把其合并起来。当然，多个目标的生成规则的执行命令不是同一个，这可能会给我们带来麻烦，不过好在我们可以使用一个自动化变量 <code>$@</code> （关于自动化变量，将在后面讲述），这个变量表示着目前规则中所有的目标的集合，这样说可能很抽象，还是看一个例子吧。</p><pre class="line-numbers language-none"><code class="language-none">bigoutput littleoutput : text.g    generate text.g -$(subst output,,$@) &gt; $@<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>上述规则等价于：</p><pre class="line-numbers language-none"><code class="language-none">bigoutput : text.g    generate text.g -big &gt; bigoutputlittleoutput : text.g    generate text.g -little &gt; littleoutput<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>其中， <code>-$(subst output,,$@)</code> 中的 <code>$</code> 表示执行一个Makefile的函数，函数名为subst，后面的为参数。关于函数，将在后面讲述。这里的这个函数是替换字符串的意思， <code>$@</code> 表示目标的集合，就像一个数组， <code>$@</code> 依次取出目标，并执于命令。</p><h2 id="静态模式"><a href="#静态模式" class="headerlink" title="静态模式"></a>静态模式</h2><p>静态模式可以更加容易地定义多目标的规则，可以让我们的规则变得更加的有弹性和灵活。我们还是先来看一下语法：</p><pre class="line-numbers language-none"><code class="language-none">&lt;targets ...&gt; : &lt;target-pattern&gt; : &lt;prereq-patterns ...&gt;    &lt;commands&gt;    ...<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>targets定义了一系列的目标文件，可以有通配符。是目标的一个集合。</p><p>target-pattern是指明了targets的模式，也就是的目标集模式。</p><p>prereq-patterns是目标的依赖模式，它对target-pattern形成的模式再进行一次依赖目标的定义。</p><p>这样描述这三个东西，可能还是没有说清楚，还是举个例子来说明一下吧。如果我们的<target-pattern>定义成 <code>%.o</code> ，意思是我们的<target>;集合中都是以 <code>.o</code> 结尾的，而如果我们的<prereq-patterns>定义成 <code>%.c</code> ，意思是对<target-pattern>所形成的目标集进行二次定义，其计算方法是，取<target-pattern>模式中的 <code>%</code> （也就是去掉了 <code>.o</code> 这个结尾），并为其加上 <code>.c</code> 这个结尾，形成的新集合。</target-pattern></target-pattern></prereq-patterns></target></target-pattern></p><p>所以，我们的“目标模式”或是“依赖模式”中都应该有 <code>%</code> 这个字符，如果你的文件名中有 <code>%</code> 那么你可以使用反斜杠 <code>\</code> 进行转义，来标明真实的 <code>%</code> 字符。</p><p>看一个例子：</p><pre class="line-numbers language-none"><code class="language-none">objects = foo.o bar.oall: $(objects)$(objects): %.o: %.c    $(CC) -c $(CFLAGS) $&lt; -o $@<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>上面的例子中，指明了我们的目标从$object中获取， <code>%.o</code> 表明要所有以 <code>.o</code> 结尾的目标，也就是 <code>foo.o bar.o</code> ，也就是变量 <code>$object</code> 集合的模式，而依赖模式 <code>%.c</code> 则取模式 <code>%.o</code> 的 <code>%</code> ，也就是 <code>foo bar</code> ，并为其加下 <code>.c</code> 的后缀，于是，我们的依赖目标就是 <code>foo.c bar.c</code> 。而命令中的 <code>$&lt;</code> 和 <code>$@</code> 则是自动化变量， <code>$&lt;</code> 表示第一个依赖文件， <code>$@</code> 表示目标集（也就是“foo.o bar.o”）。于是，上面的规则展开后等价于下面的规则：</p><pre class="line-numbers language-none"><code class="language-none">foo.o : foo.c    $(CC) -c $(CFLAGS) foo.c -o foo.obar.o : bar.c    $(CC) -c $(CFLAGS) bar.c -o bar.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>试想，如果我们的 <code>%.o</code> 有几百个，那么我们只要用这种很简单的“静态模式规则”就可以写完一堆规则，实在是太有效率了。“静态模式规则”的用法很灵活，如果用得好，那会是一个很强大的功能。再看一个例子：</p><pre class="line-numbers language-none"><code class="language-none">files = foo.elc bar.o lose.o$(filter %.o,$(files)): %.o: %.c    $(CC) -c $(CFLAGS) $&lt; -o $@$(filter %.elc,$(files)): %.elc: %.el    emacs -f batch-byte-compile $&lt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>$(filter %.o,$(files))表示调用Makefile的filter函数，过滤“$files”集，只要其中模式为“%.o”的内容。其它的内容，我就不用多说了吧。这个例子展示了Makefile中更大的弹性。</p><h2 id="自动生成依赖性"><a href="#自动生成依赖性" class="headerlink" title="自动生成依赖性"></a>自动生成依赖性</h2><p>在Makefile中，我们的依赖关系可能会需要包含一系列的头文件，比如，如果我们的main.c中有一句 <code>#include "defs.h"</code> ，那么我们的依赖关系应该是：</p><pre class="line-numbers language-none"><code class="language-none">main.o : main.c defs.h<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>但是，如果是一个比较大型的工程，你必需清楚哪些C文件包含了哪些头文件，并且，你在加入或删除头文件时，也需要小心地修改Makefile，这是一个很没有维护性的工作。为了避免这种繁重而又容易出错的事情，我们可以使用C/C++编译的一个功能。大多数的C/C++编译器都支持一个“-M”的选项，即自动找寻源文件中包含的头文件，并生成一个依赖关系。例如，如果我们执行下面的命令:</p><pre class="line-numbers language-none"><code class="language-none">cc -M main.c<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>其输出是：</p><pre class="line-numbers language-none"><code class="language-none">main.o : main.c defs.h<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>于是由编译器自动生成的依赖关系，这样一来，你就不必再手动书写若干文件的依赖关系，而由编译器自动生成了。需要提醒一句的是，如果你使用GNU的C/C++编译器，你得用 <code>-MM</code> 参数，不然， <code>-M</code> 参数会把一些标准库的头文件也包含进来。</p><p>gcc -M main.c的输出是:</p><pre class="line-numbers language-none"><code class="language-none">main.o: main.c defs.h /usr/include/stdio.h /usr/include/features.h \    /usr/include/sys/cdefs.h /usr/include/gnu/stubs.h \    /usr/lib/gcc-lib/i486-suse-linux/2.95.3/include/stddef.h \    /usr/include/bits/types.h /usr/include/bits/pthreadtypes.h \    /usr/include/bits/sched.h /usr/include/libio.h \    /usr/include/_G_config.h /usr/include/wchar.h \    /usr/include/bits/wchar.h /usr/include/gconv.h \    /usr/lib/gcc-lib/i486-suse-linux/2.95.3/include/stdarg.h \    /usr/include/bits/stdio_lim.h<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>gcc -MM main.c的输出则是:</p><pre class="line-numbers language-none"><code class="language-none">main.o: main.c defs.h<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>那么，编译器的这个功能如何与我们的Makefile联系在一起呢。因为这样一来，我们的Makefile也要根据这些源文件重新生成，让 Makefile自已依赖于源文件？这个功能并不现实，不过我们可以有其它手段来迂回地实现这一功能。GNU组织建议把编译器为每一个源文件的自动生成的依赖关系放到一个文件中，为每一个 <code>name.c</code> 的文件都生成一个 <code>name.d</code> 的Makefile文件， <code>.d</code> 文件中就存放对应 <code>.c</code> 文件的依赖关系。</p><p>于是，我们可以写出 <code>.c</code> 文件和 <code>.d</code> 文件的依赖关系，并让make自动更新或生成 <code>.d</code> 文件，并把其包含在我们的主Makefile中，这样，我们就可以自动化地生成每个文件的依赖关系了。</p><p>这里，我们给出了一个模式规则来产生 <code>.d</code> 文件：</p><pre class="line-numbers language-none"><code class="language-none">%.d: %.c    @set -e; rm -f $@; \    $(CC) -M $(CPPFLAGS) $&lt; &gt; $@.$$$$; \    sed 's,\($*\)\.o[ :]*,\1.o $@ : ,g' &lt; $@.$$$$ &gt; $@; \    rm -f $@.$$$$<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这个规则的意思是，所有的 <code>.d</code> 文件依赖于 <code>.c</code> 文件， <code>rm -f $@</code> 的意思是删除所有的目标，也就是 <code>.d</code> 文件，第二行的意思是，为每个依赖文件 <code>$&lt;</code> ，也就是 <code>.c</code> 文件生成依赖文件， <code>$@</code> 表示模式 <code>%.d</code> 文件，如果有一个C文件是name.c，那么 <code>%</code> 就是 <code>name</code> ， <code>$$$$</code> 意为一个随机编号，第二行生成的文件有可能是“name.d.12345”，第三行使用sed命令做了一个替换，关于sed命令的用法请参看相关的使用文档。第四行就是删除临时文件。</p><p>总而言之，这个模式要做的事就是在编译器生成的依赖关系中加入 <code>.d</code> 文件的依赖，即把依赖关系：</p><pre class="line-numbers language-none"><code class="language-none">main.o : main.c defs.h<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>转成：</p><pre class="line-numbers language-none"><code class="language-none">main.o main.d : main.c defs.h<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>于是，我们的 <code>.d</code> 文件也会自动更新了，并会自动生成了，当然，你还可以在这个 <code>.d</code> 文件中加入的不只是依赖关系，包括生成的命令也可一并加入，让每个 <code>.d</code> 文件都包含一个完赖的规则。一旦我们完成这个工作，接下来，我们就要把这些自动生成的规则放进我们的主Makefile中。我们可以使用Makefile的“include”命令，来引入别的Makefile文件（前面讲过），例如：</p><pre class="line-numbers language-none"><code class="language-none">sources = foo.c bar.cinclude $(sources:.c=.d)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>上述语句中的 <code>$(sources:.c=.d)</code> 中的 <code>.c=.d</code> 的意思是做一个替换，把变量 <code>$(sources)</code> 所有 <code>.c</code> 的字串都替换成 <code>.d</code> ，关于这个“替换”的内容，在后面我会有更为详细的讲述。当然，你得注意次序，因为include是按次序来载入文件，最先载入的 <code>.d</code> 文件中的目标会成为默认目标。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/c-c/tool/makefile/shi-yong-make-geng-xin-han-shu-ku-wen-jian/"/>
      <url>/2021/12/24/c-c/tool/makefile/shi-yong-make-geng-xin-han-shu-ku-wen-jian/</url>
      
        <content type="html"><![CDATA[<h1 id="make更新函数库文件"><a href="#make更新函数库文件" class="headerlink" title="make更新函数库文件"></a>make更新函数库文件</h1><p>函数库文件也就是对Object文件（程序编译的中间文件）的打包文件。在Unix下，一般是由命令 <code>ar</code> 来完成打包工作。</p><h2 id="函数库文件的成员"><a href="#函数库文件的成员" class="headerlink" title="函数库文件的成员"></a>函数库文件的成员</h2><p>一个函数库文件由多个文件组成。你可以用如下格式指定函数库文件及其组成:</p><pre class="line-numbers language-none"><code class="language-none">archive(member)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这个不是一个命令，而一个目标和依赖的定义。一般来说，这种用法基本上就是为了 <code>ar</code> 命令来服务的。如:</p><pre class="line-numbers language-none"><code class="language-none">foolib(hack.o) : hack.o    ar cr foolib hack.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>如果要指定多个member，那就以空格分开，如:</p><pre class="line-numbers language-none"><code class="language-none">foolib(hack.o kludge.o)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>其等价于:</p><pre class="line-numbers language-none"><code class="language-none">foolib(hack.o) foolib(kludge.o)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>你还可以使用Shell的文件通配符来定义，如:</p><pre class="line-numbers language-none"><code class="language-none">foolib(*.o)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="函数库成员的隐含规则"><a href="#函数库成员的隐含规则" class="headerlink" title="函数库成员的隐含规则"></a>函数库成员的隐含规则</h2><p>当make搜索一个目标的隐含规则时，一个特殊的特性是，如果这个目标是 <code>a(m)</code> 形式的，其会把目标变成 <code>(m)</code> 。于是，如果我们的成员是 <code>%.o</code> 的模式定义，并且如果我们使用 <code>make foo.a(bar.o)</code> 的形式调用Makefile时，隐含规则会去找 <code>bar.o</code> 的规则，如果没有定义 <code>bar.o</code> 的规则，那么内建隐含规则生效，make会去找 <code>bar.c</code> 文件来生成 <code>bar.o</code> ，如果找得到的话，make执行的命令大致如下:</p><pre class="line-numbers language-none"><code class="language-none">cc -c bar.c -o bar.oar r foo.a bar.orm -f bar.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>还有一个变量要注意的是 <code>$%</code> ，这是专属函数库文件的自动化变量，有关其说明请参见“自动化变量”一节。</p><h2 id="函数库文件的后缀规则"><a href="#函数库文件的后缀规则" class="headerlink" title="函数库文件的后缀规则"></a>函数库文件的后缀规则</h2><p>你可以使用“后缀规则”和“隐含规则”来生成函数库打包文件，如：</p><pre class="line-numbers language-none"><code class="language-none">.c.a:    $(CC) $(CFLAGS) $(CPPFLAGS) -c $&lt; -o $*.o    $(AR) r $@ $*.o    $(RM) $*.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>其等效于：</p><pre class="line-numbers language-none"><code class="language-none">(%.o) : %.c    $(CC) $(CFLAGS) $(CPPFLAGS) -c $&lt; -o $*.o    $(AR) r $@ $*.o    $(RM) $*.o<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><p>在进行函数库打包文件生成时，请小心使用make的并行机制（ <code>-j</code> 参数）。如果多个 <code>ar</code> 命令在同一时间运行在同一个函数库打包文件上，就很有可以损坏这个函数库文件。所以，在make未来的版本中，应该提供一种机制来避免并行操作发生在函数打包文件上。</p><p>但就目前而言，你还是应该不要尽量不要使用 <code>-j</code> 参数。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/c-c/tool/makefile/make-de-yun-xing/"/>
      <url>/2021/12/24/c-c/tool/makefile/make-de-yun-xing/</url>
      
        <content type="html"><![CDATA[<h1 id="make的运行"><a href="#make的运行" class="headerlink" title="make的运行"></a>make的运行</h1><h1 id="make-的运行"><a href="#make-的运行" class="headerlink" title="make 的运行"></a>make 的运行</h1><p>一般来说，最简单的就是直接在命令行下输入make命令，make命令会找当前目录的makefile来执行，一切都是自动的。但也有时你也许只想让make重编译某些文件，而不是整个工程，而又有的时候你有几套编译规则，你想在不同的时候使用不同的编译规则，等等。本章节就是讲述如何使用make命令的。</p><h2 id="make的退出码"><a href="#make的退出码" class="headerlink" title="make的退出码"></a>make的退出码</h2><p>make命令执行后有三个退出码：</p><ul><li><p>0</p><p>表示成功执行。</p></li><li><p>1</p><p>如果make运行时出现任何错误，其返回1。</p></li><li><p>2</p><p>如果你使用了make的“-q”选项，并且make使得一些目标不需要更新，那么返回2。</p></li></ul><p>Make的相关参数我们会在后续章节中讲述。</p><h2 id="指定Makefile"><a href="#指定Makefile" class="headerlink" title="指定Makefile"></a>指定Makefile</h2><p>前面我们说过，GNU make找寻默认的Makefile的规则是在当前目录下依次找三个文件——“GNUmakefile”、“makefile”和“Makefile”。其按顺序找这三个文件，一旦找到，就开始读取这个文件并执行。</p><p>当前，我们也可以给make命令指定一个特殊名字的Makefile。要达到这个功能，我们要使用make的 <code>-f</code> 或是 <code>--file</code> 参数（ <code>--makefile</code> 参数也行）。例如，我们有个makefile的名字是“hchen.mk”，那么，我们可以这样来让make来执行这个文件：</p><pre class="line-numbers language-none"><code class="language-none">make –f hchen.mk<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>如果在make的命令行是，你不只一次地使用了 <code>-f</code> 参数，那么，所有指定的makefile将会被连在一起传递给make执行。</p><h2 id="指定目标"><a href="#指定目标" class="headerlink" title="指定目标"></a>指定目标</h2><p>一般来说，make的最终目标是makefile中的第一个目标，而其它目标一般是由这个目标连带出来的。这是make的默认行为。当然，一般来说，你的makefile中的第一个目标是由许多个目标组成，你可以指示make，让其完成你所指定的目标。要达到这一目的很简单，需在make命令后直接跟目标的名字就可以完成（如前面提到的“make clean”形式）</p><p>任何在makefile中的目标都可以被指定成终极目标，但是除了以 <code>-</code> 打头，或是包含了 <code>=</code> 的目标，因为有这些字符的目标，会被解析成命令行参数或是变量。甚至没有被我们明确写出来的目标也可以成为make的终极目标，也就是说，只要make可以找到其隐含规则推导规则，那么这个隐含目标同样可以被指定成终极目标。</p><p>有一个make的环境变量叫 <code>MAKECMDGOALS</code> ，这个变量中会存放你所指定的终极目标的列表，如果在命令行上，你没有指定目标，那么，这个变量是空值。这个变量可以让你使用在一些比较特殊的情形下。比如下面的例子：</p><pre class="line-numbers language-none"><code class="language-none">sources = foo.c bar.cifneq ( $(MAKECMDGOALS),clean)    include $(sources:.c=.d)endif<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>基于上面的这个例子，只要我们输入的命令不是“make clean”，那么makefile会自动包含“foo.d”和“bar.d”这两个makefile。</p><p>使用指定终极目标的方法可以很方便地让我们编译我们的程序，例如下面这个例子：</p><pre class="line-numbers language-none"><code class="language-none">.PHONY: allall: prog1 prog2 prog3 prog4<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>从这个例子中，我们可以看到，这个makefile中有四个需要编译的程序——“prog1”， “prog2”，“prog3”和 “prog4”，我们可以使用“make all”命令来编译所有的目标（如果把all置成第一个目标，那么只需执行“make”），我们也可以使用 “make prog2”来单独编译目标“prog2”。</p><p>即然make可以指定所有makefile中的目标，那么也包括“伪目标”，于是我们可以根据这种性质来让我们的makefile根据指定的不同的目标来完成不同的事。在Unix世界中，软件发布时，特别是GNU这种开源软件的发布时，其makefile都包含了编译、安装、打包等功能。我们可以参照这种规则来书写我们的makefile中的目标。</p><ul><li>all:这个伪目标是所有目标的目标，其功能一般是编译所有的目标。</li><li>clean:这个伪目标功能是删除所有被make创建的文件。</li><li>install:这个伪目标功能是安装已编译好的程序，其实就是把目标执行文件拷贝到指定的目标中去。</li><li>print:这个伪目标的功能是例出改变过的源文件。</li><li>tar:这个伪目标功能是把源程序打包备份。也就是一个tar文件。</li><li>dist:这个伪目标功能是创建一个压缩文件，一般是把tar文件压成Z文件。或是gz文件。</li><li>TAGS:这个伪目标功能是更新所有的目标，以备完整地重编译使用。</li><li>check和test:这两个伪目标一般用来测试makefile的流程。</li></ul><p>当然一个项目的makefile中也不一定要书写这样的目标，这些东西都是GNU的东西，但是我想，GNU搞出这些东西一定有其可取之处（等你的 UNIX下的程序文件一多时你就会发现这些功能很有用了），这里只不过是说明了，如果你要书写这种功能，最好使用这种名字命名你的目标，这样规范一些，规范的好处就是——不用解释，大家都明白。而且如果你的makefile中有这些功能，一是很实用，二是可以显得你的makefile很专业（不是那种初学者的作品）。</p><h2 id="检查规则"><a href="#检查规则" class="headerlink" title="检查规则"></a>检查规则</h2><p>有时候，我们不想让我们的makefile中的规则执行起来，我们只想检查一下我们的命令，或是执行的序列。于是我们可以使用make命令的下述参数：</p><ul><li><p><code>-n</code>, <code>--just-print</code>, <code>--dry-run</code>, <code>--recon</code></p><p>不执行参数，这些参数只是打印命令，不管目标是否更新，把规则和连带规则下的命令打印出来，但不执行，这些参数对于我们调试makefile很有用处。</p></li><li><p><code>-t</code>, <code>--touch</code></p><p>这个参数的意思就是把目标文件的时间更新，但不更改目标文件。也就是说，make假装编译目标，但不是真正的编译目标，只是把目标变成已编译过的状态。</p></li><li><p><code>-q</code>, <code>--question</code></p><p>这个参数的行为是找目标的意思，也就是说，如果目标存在，那么其什么也不会输出，当然也不会执行编译，如果目标不存在，其会打印出一条出错信息。</p></li><li><p><code>-W &lt;file&gt;</code>, <code>--what-if=&lt;file&gt;</code>, <code>--assume-new=&lt;file&gt;</code>, <code>--new-file=&lt;file&gt;</code></p><p>这个参数需要指定一个文件。一般是是源文件（或依赖文件），Make会根据规则推导来运行依赖于这个文件的命令，一般来说，可以和“-n”参数一同使用，来查看这个依赖文件所发生的规则命令。</p></li></ul><p>另外一个很有意思的用法是结合 <code>-p</code> 和 <code>-v</code> 来输出makefile被执行时的信息（这个将在后面讲述）。</p><h2 id="make的参数"><a href="#make的参数" class="headerlink" title="make的参数"></a>make的参数</h2><p>下面列举了所有GNU make 3.80版的参数定义。其它版本和产商的make大同小异，不过其它产商的make的具体参数还是请参考各自的产品文档。</p><ul><li><p><code>-b</code>, <code>-m</code></p><p>这两个参数的作用是忽略和其它版本make的兼容性。</p></li><li><p><code>-B</code>, <code>--always-make</code></p><p>认为所有的目标都需要更新（重编译）。</p></li><li><p><code>-C</code> <em></em></p><dir>, <code>--directory</code>=<em></em><dir><em></em><p></p><p>指定读取makefile的目录。如果有多个“-C”参数，make的解释是后面的路径以前面的作为相对路径，并以最后的目录作为被指定目录。如：“make -C ~hchen/test -C prog”等价于“make -C ~hchen/test/prog”。</p></dir></dir></li><li><p><code>-debug</code>[=<em><options></options></em>]</p><p>输出make的调试信息。它有几种不同的级别可供选择，如果没有参数，那就是输出最简单的调试信息。下面是<options>的取值：a: 也就是all，输出所有的调试信息。（会非常的多）b: 也就是basic，只输出简单的调试信息。即输出不需要重编译的目标。v: 也就是verbose，在b选项的级别之上。输出的信息包括哪个makefile被解析，不需要被重编译的依赖文件（或是依赖目标）等。i: 也就是implicit，输出所以的隐含规则。j: 也就是jobs，输出执行规则中命令的详细信息，如命令的PID、返回码等。m: 也就是makefile，输出make读取makefile，更新makefile，执行makefile的信息。</options></p></li><li><p><code>-d</code></p><p>相当于“–debug=a”。</p></li><li><p><code>-e</code>, <code>--environment-overrides</code></p><p>指明环境变量的值覆盖makefile中定义的变量的值。</p></li><li><p><code>-f</code>=<em><file></file></em>, <code>--file</code>=<em><file></file></em>, <code>--makefile</code>=<em><file></file></em></p><p>指定需要执行的makefile。</p></li><li><p><code>-h</code>, <code>--help</code></p><p>显示帮助信息。</p></li><li><p><code>-i</code> , <code>--ignore-errors</code></p><p>在执行时忽略所有的错误。</p></li><li><p><code>-I</code> <em></em></p><dir>, <code>--include-dir</code>=<em></em><dir><em></em><p></p><p>指定一个被包含makefile的搜索目标。可以使用多个“-I”参数来指定多个目录。</p></dir></dir></li><li><p><code>-j</code> [<em><jobsnum></jobsnum></em>], <code>--jobs</code>[=<em><jobsnum></jobsnum></em>]</p><p>指同时运行命令的个数。如果没有这个参数，make运行命令时能运行多少就运行多少。如果有一个以上的“-j”参数，那么仅最后一个“-j”才是有效的。（注意这个参数在MS-DOS中是无用的）</p></li><li><p><code>-k</code>, <code>--keep-going</code></p><p>出错也不停止运行。如果生成一个目标失败了，那么依赖于其上的目标就不会被执行了。</p></li><li><p><code>-l</code> <em><load></load></em>, <code>--load-average</code>[=<em><load></load></em>], <code>-max-load</code>[=<em><load></load></em>]</p><p>指定make运行命令的负载。</p></li><li><p><code>-n</code>, <code>--just-print</code>, <code>--dry-run</code>, <code>--recon</code></p><p>仅输出执行过程中的命令序列，但并不执行。</p></li><li><p><code>-o</code> <em><file></file></em>, <code>--old-file</code>=<em><file></file></em>, <code>--assume-old</code>=<em><file></file></em></p><p>不重新生成的指定的<file>，即使这个目标的依赖文件新于它。</file></p></li><li><p><code>-p</code>, <code>--print-data-base</code></p><p>输出makefile中的所有数据，包括所有的规则和变量。这个参数会让一个简单的makefile都会输出一堆信息。如果你只是想输出信息而不想执行makefile，你可以使用“make -qp”命令。如果你想查看执行makefile前的预设变量和规则，你可以使用 “make –p –f /dev/null”。这个参数输出的信息会包含着你的makefile文件的文件名和行号，所以，用这个参数来调试你的 makefile会是很有用的，特别是当你的环境变量很复杂的时候。</p></li><li><p><code>-q</code>, <code>--question</code></p><p>不运行命令，也不输出。仅仅是检查所指定的目标是否需要更新。如果是0则说明要更新，如果是2则说明有错误发生。</p></li><li><p><code>-r</code>, <code>--no-builtin-rules</code></p><p>禁止make使用任何隐含规则。</p></li><li><p><code>-R</code>, <code>--no-builtin-variabes</code></p><p>禁止make使用任何作用于变量上的隐含规则。</p></li><li><p><code>-s</code>, <code>--silent</code>, <code>--quiet</code></p><p>在命令运行时不输出命令的输出。</p></li><li><p><code>-S</code>, <code>--no-keep-going</code>, <code>--stop</code></p><p>取消“-k”选项的作用。因为有些时候，make的选项是从环境变量“MAKEFLAGS”中继承下来的。所以你可以在命令行中使用这个参数来让环境变量中的“-k”选项失效。</p></li><li><p><code>-t</code>, <code>--touch</code></p><p>相当于UNIX的touch命令，只是把目标的修改日期变成最新的，也就是阻止生成目标的命令运行。</p></li><li><p><code>-v</code>, <code>--version</code></p><p>输出make程序的版本、版权等关于make的信息。</p></li><li><p><code>-w</code>, <code>--print-directory</code></p><p>输出运行makefile之前和之后的信息。这个参数对于跟踪嵌套式调用make时很有用。</p></li><li><p><code>--no-print-directory</code></p><p>禁止“-w”选项。</p></li><li><p><code>-W</code> <em><file></file></em>, <code>--what-if</code>=<em><file></file></em>, <code>--new-file</code>=<em><file></file></em>, <code>--assume-file</code>=<em><file></file></em></p><p>假定目标<file>;需要更新，如果和“-n”选项使用，那么这个参数会输出该目标更新时的运行动作。如果没有“-n”那么就像运行UNIX的“touch”命令一样，使得<file>;的修改时间为当前时间。</file></file></p></li><li><p><code>--warn-undefined-variables</code></p><p>只要make发现有未定义的变量，那么就输出警告信息。</p></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/c-c/tool/makefile/makefile-tiao-jian-pan-duan/"/>
      <url>/2021/12/24/c-c/tool/makefile/makefile-tiao-jian-pan-duan/</url>
      
        <content type="html"><![CDATA[<h1 id="makefile条件判断"><a href="#makefile条件判断" class="headerlink" title="makefile条件判断"></a>makefile条件判断</h1><p>下面的例子，判断 <code>$(CC)</code> 变量是否 <code>gcc</code> ，如果是的话，则使用GNU函数编译目标。</p><pre class="line-numbers language-none"><code class="language-none">libs_for_gcc = -lgnunormal_libs =foo: $(objects)ifeq ($(CC),gcc)    $(CC) -o foo $(objects) $(libs_for_gcc)else    $(CC) -o foo $(objects) $(normal_libs)endif<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>可见，在上面示例的这个规则中，目标 <code>foo</code> 可以根据变量 <code>$(CC)</code> 值来选取不同的函数库来编译程序。</p><p>我们可以从上面的示例中看到三个关键字： <code>ifeq</code> 、 <code>else</code> 和 <code>endif</code> 。 <code>ifeq</code> 的意思表示条件语句的开始，并指定一个条件表达式，表达式包含两个参数，以逗号分隔，表达式以圆括号括起。 <code>else</code> 表示条件表达式为假的情况。 <code>endif</code> 表示一个条件语句的结束，任何一个条件表达式都应该以 <code>endif</code> 结束。</p><p>当我们的变量 <code>$(CC)</code> 值是 <code>gcc</code> 时，目标 <code>foo</code> 的规则是：</p><pre class="line-numbers language-none"><code class="language-none">foo: $(objects)    $(CC) -o foo $(objects) $(libs_for_gcc)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>而当我们的变量 <code>$(CC)</code> 值不是 <code>gcc</code> 时（比如 <code>cc</code> ），目标 <code>foo</code> 的规则是：</p><pre class="line-numbers language-none"><code class="language-none">foo: $(objects)    $(CC) -o foo $(objects) $(normal_libs)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>当然，我们还可以把上面的那个例子写得更简洁一些：</p><pre class="line-numbers language-none"><code class="language-none">libs_for_gcc = -lgnunormal_libs =ifeq ($(CC),gcc)    libs=$(libs_for_gcc)else    libs=$(normal_libs)endiffoo: $(objects)    $(CC) -o foo $(objects) $(libs)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h2><p>条件表达式的语法为:</p><pre class="line-numbers language-none"><code class="language-none">&lt;conditional-directive&gt;&lt;text-if-true&gt;endif<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>以及:</p><pre class="line-numbers language-none"><code class="language-none">&lt;conditional-directive&gt;&lt;text-if-true&gt;else&lt;text-if-false&gt;endif<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>其中 <code>&lt;conditional-directive&gt;</code> 表示条件关键字，如 <code>ifeq</code> 。这个关键字有四个。</p><p>第一个是我们前面所见过的 <code>ifeq</code></p><pre class="line-numbers language-none"><code class="language-none">ifeq (&lt;arg1&gt;, &lt;arg2&gt;)ifeq '&lt;arg1&gt;' '&lt;arg2&gt;'ifeq "&lt;arg1&gt;" "&lt;arg2&gt;"ifeq "&lt;arg1&gt;" '&lt;arg2&gt;'ifeq '&lt;arg1&gt;' "&lt;arg2&gt;"<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>比较参数 <code>arg1</code> 和 <code>arg2</code> 的值是否相同。当然，参数中我们还可以使用make的函数。如:</p><pre class="line-numbers language-none"><code class="language-none">ifeq ($(strip $(foo)),)&lt;text-if-empty&gt;endif<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>这个示例中使用了 <code>strip</code> 函数，如果这个函数的返回值是空（Empty），那么 <code>&lt;text-if-empty&gt;</code> 就生效。</p><p>第二个条件关键字是 <code>ifneq</code> 。语法是：</p><pre class="line-numbers language-none"><code class="language-none">ifneq (&lt;arg1&gt;, &lt;arg2&gt;)ifneq '&lt;arg1&gt;' '&lt;arg2&gt;'ifneq "&lt;arg1&gt;" "&lt;arg2&gt;"ifneq "&lt;arg1&gt;" '&lt;arg2&gt;'ifneq '&lt;arg1&gt;' "&lt;arg2&gt;"<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>其比较参数 <code>arg1</code> 和 <code>arg2</code> 的值是否相同，如果不同，则为真。和 <code>ifeq</code> 类似。</p><p>第三个条件关键字是 <code>ifdef</code> 。语法是：</p><pre class="line-numbers language-none"><code class="language-none">ifdef &lt;variable-name&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>如果变量 <code>&lt;variable-name&gt;</code> 的值非空，那到表达式为真。否则，表达式为假。当然， <code>&lt;variable-name&gt;</code> 同样可以是一个函数的返回值。注意， <code>ifdef</code> 只是测试一个变量是否有值，其并不会把变量扩展到当前位置。还是来看两个例子：</p><p>示例一：</p><pre class="line-numbers language-none"><code class="language-none">bar =foo = $(bar)ifdef foo    frobozz = yeselse    frobozz = noendif<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>示例二：</p><pre class="line-numbers language-none"><code class="language-none">foo =ifdef foo    frobozz = yeselse    frobozz = noendif<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>第一个例子中， <code>$(frobozz)</code> 值是 <code>yes</code> ，第二个则是 <code>no</code>。</p><p>第四个条件关键字是 <code>ifndef</code> 。其语法是：</p><pre class="line-numbers language-none"><code class="language-none">ifndef &lt;variable-name&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这个我就不多说了，和 <code>ifdef</code> 是相反的意思。</p><p>在 <code>&lt;conditional-directive&gt;</code> 这一行上，多余的空格是被允许的，但是不能以 <code>Tab</code> 键作为开始（不然就被认为是命令）。而注释符 <code>#</code> 同样也是安全的。 <code>else</code> 和 <code>endif</code> 也一样，只要不是以 <code>Tab</code> 键开始就行了。</p><p>特别注意的是，make是在读取Makefile时就计算条件表达式的值，并根据条件表达式的值来选择语句，所以，你最好不要把自动化变量（如 <code>$@</code> 等）放入条件表达式中，因为自动化变量是在运行时才有的。</p><p>而且为了避免混乱，make不允许把整个条件语句分成两部分放在不同的文件中。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/monitor/prometheus/altermanager/receiver/"/>
      <url>/2021/12/24/monitor/prometheus/altermanager/receiver/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/monitor/prometheus/exporter/node-exporter/node-exporter/"/>
      <url>/2021/12/24/monitor/prometheus/exporter/node-exporter/node-exporter/</url>
      
        <content type="html"><![CDATA[<h1 id="node-exporter"><a href="#node-exporter" class="headerlink" title="node exporter"></a>node exporter</h1><blockquote><p>github地址：<a href="https://github.com/prometheus/node_exporter">https://github.com/prometheus/node_exporter</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/monitor/prometheus/promql/promql-han-shu/"/>
      <url>/2021/12/24/monitor/prometheus/promql/promql-han-shu/</url>
      
        <content type="html"><![CDATA[<h1 id="promql函数"><a href="#promql函数" class="headerlink" title="promql函数"></a>promql函数</h1><blockquote><p><a href="https://juejin.cn/post/6844903873153335304">https://juejin.cn/post/6844903873153335304</a></p></blockquote><h3 id="abs"><a href="#abs" class="headerlink" title="abs()"></a>abs()</h3><p><code>abs(v instant-vector)</code>返回输入向量，所有样本值都转换为其绝对值。</p><pre class="line-numbers language-none"><code class="language-none"><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="absent"><a href="#absent" class="headerlink" title="absent()"></a>absent()</h3><p><code>absent(v instant-vector)</code>如果传递给它的向量具有任何元素，则返回空向量;如果传递给它的向量没有元素，则返回值为1的1元素向量。</p><p>这对于在给定度量标准名称和标签组合不存在时间序列时发出警报非常有用。</p><pre class="line-numbers language-none"><code class="language-none">```### ceil()`ceil(v instant-vector)` 将`v`中所有元素的样本值舍入到最接近的整数。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-none"><code class="language-none">### changes()对于每个输入时间系列，`changes(v range-vector)` 将返回其值在所提供的时间范围内更改的次数作为即时向量。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-none"><code class="language-none">### clamp_max()`clamp_max(v instant-vector, max scalar)`钳制`v`中所有元素的样本值，使其上限为`max`。### clamp_min()`clamp_min(v instant-vector, min scalar)`钳制`v`中所有元素的样本值，使其下限为`min`。### day_of_month()`day_of_month(v=vector(time()) instant-vector)`返回UTC中每个给定时间的月中的某天。 返回值为1到31。### day_of_week()`day_of_week(v=vector(time()) instant-vector)`返回UTC中每个给定时间的星期几。 返回值为0到6，其中0表示星期日等。### days_in_month()`days_in_month(v=vector(time()) instant-vector)`返回UTC中每个给定时间的月中天数。 返回值为28到31。### delta()`delta(v range-vector)`计算范围向量`v`中每个时间系列元素的第一个和最后一个值之间的差值，返回具有给定增量和等效标签的即时向量。 `delta`被外推以覆盖范围向量选择器中指定的全时间范围，因此即使样本值都是整数，也可以获得非整数结果。以下示例表达式返回现在和2小时之前CPU温度的差异：<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>delta(cpu_temp_celsius{host=”zeus”}[2h])<br>复制代码</p><pre class="line-numbers language-none"><code class="language-none">`delta`应仅用于仪表。### deriv()`deriv(v range-vector)`函数，计算一个范围向量v中各个时间序列二阶导数，使用[简单线性回归](https://link.juejin.cn/?target=https%3A%2F%2Fen.wikipedia.org%2Fwiki%2FSimple_linear_regression) `deriv`应仅用于仪表。### exp()`exp(v instant-vector)`计算`v`中所有元素的指数函数。特殊情况是：- `Exp(+inf) = +Inf`- `Exp(NaN) = NaN`### floor()`floor(v instant-vector)`将`v`中所有元素的样本值舍入为最接近的整数### histogram_quantile()`histogram_quatile(φ float, b instant-vector)` 计算`b`向量的φ-直方图 (0 ≤ φ ≤ 1) 。（有关φ-分位数的详细解释和直方图度量类型的使用，请参见[直方图和摘要](https://link.juejin.cn?target=https%3A%2F%2Fprometheus.io%2Fdocs%2Fpractices%2Fhistograms%2F)。）`b`中的样本是每个桶中的观察计数。 每个样本必须具有标签`le`，其中标签值表示桶的包含上限。 （没有这种标签的样本会被忽略。）直方图度量标准类型自动提供带有`_bucket`后缀和相应标签的时间序列。使用`rate()`函数指定分位数计算的时间窗口。示例：直方图度量标准称为`http_request_duration_seconds`。 要计算过去10m内请求持续时间的第90个百分位数，请使用以下表达式：<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>histogram_quantile(0.9, rate(http_request_duration_seconds_bucket[10m]))<br>复制代码</p><pre class="line-numbers language-none"><code class="language-none">在`http_request_duration_seconds`中为每个标签组合计算分位数。 要聚合，请在`rate()`函数周围使用`sum()`聚合器。 由于`histogram_quantile()`需要`le`标签，因此必须将其包含在`by`子句中。 以下表达式按作业聚合第90个百分点：<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>histogram_quantile(0.9, sum(rate(http_request_duration_seconds_bucket[10m])) by (job, le))<br>复制代码</p><pre class="line-numbers language-none"><code class="language-none">要聚合所有内容，请仅指定`le`标签：<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>histogram_quantile(0.9, sum(rate(http_request_duration_seconds_bucket[10m])) by (le))<br>复制代码</p><pre><code>`histogram_quantile()`函数通过假设桶内的线性分布来插值分位数值。 最高桶必须具有`+Inf`的上限。 （否则，返回`NaN`。）如果分位数位于最高桶中，则返回第二个最高桶的上限。 如果该桶的上限大于0，则假设最低桶的下限为0.在这种情况下，在该桶内应用通常的线性插值。 否则，对于位于最低桶中的分位数，返回最低桶的上限。如果`b`包含少于两个桶，则返回`NaN`。 对于`φ&lt;0`，返回`-Inf`。 对于`φ&gt; 1`，返回`+Inf`。### holt_winters()`holt_winters(v range-vector, sf scalar, tf scalar)`根据`v`中的范围产生时间序列的平滑值。平滑因子`sf`越低，对旧数据的重要性越高。 趋势因子`tf`越高，则考虑的数据趋势越多。 `sf`和`tf`都必须介于0和1之间。`holt_winters`只能用于仪表### hour()`hour(v=vector(time()) instant-vector)`返回UTC中每个给定时间的一天中的小时。 返回值为0到23### idelta()`idelta(v range-vector)`计算范围向量`v`中最后两个样本之间的差异，返回具有给定增量和等效标签的即时向量。`idelta`只能用于仪表### increase()`increase(v range-vector)`计算范围向量中时间序列的增加。 单调性中断（例如由于目标重启而导致的计数器重置）会自动调整。 增加外推以覆盖范围向量选择器中指定的全时间范围，因此即使计数器仅以整数增量增加，也可以获得非整数结果。以下示例表达式返回范围向量中每个时间系列在过去5分钟内测量的HTTP请求数：&gt; increase(http_requests_total{job="api-server"}[5m])`increase`只应与`counters`一起使用。 它是`rate(v)`的语法糖乘以指定时间范围窗口下的秒数，应该主要用于人类可读性。 在记录规则中使用`rate`，以便每秒一致地跟踪增量作者：飞雪K链接：https://juejin.cn/post/6844903873153335304来源：稀土掘金著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。### irate()`irate(v range-vector)`计算范围向量中时间序列的每秒即时增长率。 这基于最后两个数据点。 单调性中断（例如由于目标重启而导致的计数器重置）会自动调整。以下示例表达式返回范围向量中每个时间序列的两个最新数据点的最多5分钟的HTTP请求的每秒速率：&gt; irate(http_requests_total{job="api-server"}[5m])只应在绘制易失性快速移动计数器时使用`irate`。 警报和缓慢移动计数器的使用率，因为速率的简短更改可以重置`FOR`子句，并且难以阅读完全由稀有峰值组成的图形。注意，当将`irate()`与聚合运算符（例如`sum()`）或随时间聚合的函数（任何以`_over_time`结尾的函数）组合时，请始终首先采用`irate()`，然后进行聚合。 否则，当目标重新启动时，`irate()`无法检测计数器重置### label_join()对于`v`中的每个时间序列，`label_join(v instant-vector, dst_label string, separator string, src_label_1 string, src_label_2 string, ...)`使用`separator`连接所有`src_labels`的所有值，并返回包含连接的标签`dst_label`的时间序列 值。 此函数中可以有任意数量的`src_labels`。此示例将返回一个向量，每个时间序列都有一个`foo`标签，其中添加了值`a,b,c`：&gt; label_join(up{job="api-server",src1="a",src2="b",src3="c"}, "foo", ",", "src1", "src2", "src3")### label_replace()对于`v`中的每个时间序列，`label_replace(v instant-vector, dst_label string, replacement string, src_label string, regex string)` 将正则表达式正则表达式与标签`src_label`相匹配。 如果匹配，则返回时间序列，标签`dst_label`替换为替换扩展。 `$1`替换为第一个匹配的子组，`$2`替换为第二个等。如果正则表达式不匹配，则返回时间序列不变。此示例将返回一个向量，每个时间序列都有一个`foo`标签，其值为`a`：&gt; label_replace(up{job="api-server",service="a:c"}, "foo", "$1", "service", "(.*):.*")### ln()`ln(v instance-vector)`计算`v`中所有元素的自然对数。特殊情况是：&gt; ln(+Inf) = +Inf ln(0) = -Inf ln(x&lt;0) = NaN ln(NaN) = NaN### log2()`log2(v instant-vector)`计算`v`中所有元素的二进制对数。特殊情况等同于`ln`中的特殊情况### log10()`log10(v instant-vector)`计算`v`中所有元素的10进制对数。特殊情况等同于`ln`中的特殊情况### minute()`minute(v=vector(time()) instant-vector)`以UTC为单位返回每个给定时间的分钟。 返回值为0到59### month()`month(v=vector(time()) instant-vector)`返回UTC中每个给定时间的一年中的月份。 返回值为1到12，其中1表示1月等### predict_linear()`predict_linear(v range-vector, t scalar)`根据范围向量`v`使用线性回归预测从现在起t秒的时间序列值。`predict_linear`只应与仪表一起使用### rate()`rate(v range-vector)`计算范围向量中时间序列的每秒平均增长率。 单调性中断（例如由于目标重启而导致的计数器重置）会自动调整。 此外，计算推断到时间范围的末端，允许错过刮擦或刮擦循环与范围的时间段的不完美对齐。以下示例表达式返回范围向量中每个时间系列在过去5分钟内测量的每秒HTTP请求率：&gt; rate(http_requests_total{job="api-server"}[5m])`rate`应仅用于计数器。 它最适用于警报和缓慢移动计数器的图形。注意，当将`rate()`与聚合运算符（例如`sum()`）或随时间聚合的函数（任何以`_over_time`结尾的函数）组合时，始终首先采用`rate()`，然后聚合。 否则，当目标重新启动时，`rate()`无法检测计数器重置### resets()对于每个时序数据，`resets()`在所提供的时间范围内返回计数器重置次数作为即时向量。 两个连续样本之间的值的任何减少都被解释为计数器重置。`resets()`只应与计数器一起使用### round()`round(v instant-vector, to_nearest 1= scalar)`将`v`中所有元素的样本值舍入为最接近的整数。 通过四舍五入解决关系。 可选的`to_nearest`参数允许指定样本值应舍入的最近倍数。 这个倍数也可能是一个分数### scalar()给定单元素输入向量，`scalar(v instant-vector)`将该单个元素的样本值作为标量返回。 如果输入向量不具有恰好一个元素，则`scalar`将返回`NaN`### sort()`sort(v instant-vector)`返回按其样本值排序的向量元素，按升序排列### sort_desc()`sort(v instant-vector`和`sort`相同，但按降序排序### sqrt()`sqrt(v instant-vector)`计算`v`中所有元素的平方根### time()`time()`返回自1970年1月1日UTC以来的秒数。 请注意，这实际上并不返回当前时间，而是返回计算表达式的时间### timestamp()`timestamp(v instant-vector)`返回给定向量的每个样本的时间戳，作为自1970年1月1日UTC以来的秒数### vector()`vector(s scalar)`将标量`s`作为没有标签的向量返回### year()`year(v=vector(time()) instant-vector)`以UTC格式返回每个给定时间的年份### _over_time()以下函数允许聚合给定范围向量的每个系列随时间的变化并返回具有每系列聚合结果的即时向量：- `avg_over_time(range-vector)`: 范围向量内每个度量指标的平均值。- `min_over_time(range-vector)`: 范围向量内每个度量指标的最小值。- `max_over_time(range-vector)`: 范围向量内每个度量指标的最大值。- `sum_over_time(range-vector)`: 范围向量内每个度量指标的求和值。- `count_over_time(range-vector)`: 范围向量内每个度量指标的样本数据个数。- `quantile_over_time(scalar, range-vector)`: 范围向量内每个度量指标的样本数据值分位数，φ-quantile (0 ≤ φ ≤ 1)- `stddev_over_time(range-vector)`: 范围向量内每个度量指标的总体标准偏差。- `stdvar_over_time(range-vector): 范围向量内每个度量指标的总体标准方差。请注意，即使值在整个时间间隔内的间隔不均匀，指定时间间隔内的所有值在聚合中都具有相同的权重。</code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/monitor/prometheus/altermanager/gao-jing-gui-ze/"/>
      <url>/2021/12/24/monitor/prometheus/altermanager/gao-jing-gui-ze/</url>
      
        <content type="html"><![CDATA[<h1 id="自定义告警规则"><a href="#自定义告警规则" class="headerlink" title="自定义告警规则"></a>自定义告警规则</h1><p>Prometheus中的告警规则允许你基于PromQL表达式定义告警触发条件，Prometheus后端对这些触发规则进行周期性计算，当满足触发条件后则会触发告警通知。默认情况下，用户可以通过Prometheus的Web界面查看这些告警规则以及告警的触发状态。当Promthues与Alertmanager关联之后，可以将告警发送到外部服务如Alertmanager中并通过Alertmanager可以对这些告警进行进一步的处理。</p><h2 id="定义告警规则"><a href="#定义告警规则" class="headerlink" title="定义告警规则"></a>定义告警规则</h2><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">groups</span><span class="token punctuation">:</span><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> example  <span class="token key atrule">rules</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span class="token key atrule">alert</span><span class="token punctuation">:</span> HighErrorRate    <span class="token key atrule">expr</span><span class="token punctuation">:</span> job<span class="token punctuation">:</span>request_latency_seconds<span class="token punctuation">:</span>mean5m<span class="token punctuation">{</span>job="myjob"<span class="token punctuation">}</span> <span class="token punctuation">&gt;</span> 0.5    <span class="token key atrule">for</span><span class="token punctuation">:</span> 10m    <span class="token key atrule">labels</span><span class="token punctuation">:</span>      <span class="token key atrule">severity</span><span class="token punctuation">:</span> page    <span class="token key atrule">annotations</span><span class="token punctuation">:</span>      <span class="token key atrule">summary</span><span class="token punctuation">:</span> High request latency      <span class="token key atrule">description</span><span class="token punctuation">:</span> description info<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在告警规则文件中，我们可以将一组相关的规则设置定义在一个group下。在每一个group中我们可以定义多个告警规则(rule)。一条告警规则主要由以下几部分组成：</p><ul><li><p><strong>alert</strong>：告警规则的名称。</p></li><li><p><strong>expr</strong>：基于PromQL表达式告警触发条件，用于计算是否有时间序列满足该条件。</p></li><li><p><strong>for</strong>：评估等待时间，可选参数。用于表示只有当触发条件持续一段时间后才发送告警。在等待期间新产生告警的状态为pending。</p></li><li><p><strong>labels</strong>：自定义标签，允许用户指定要附加到告警上的一组附加标签。</p></li><li><p><strong>annotations</strong>：用于指定一组附加信息，annotations的内容在告警产生时会一同作为参数发送到Alertmanager。</p></li></ul><p>为了能够让Prometheus能够启用定义的告警规则，需要在Prometheus全局配置文件中通过<strong>rule_files</strong>指定一组告警规则文件的访问路径，Prometheus启动后会自动扫描这些路径下规则文件中定义的内容，并且根据这些规则计算是否向外部发送通知：</p><pre class="line-numbers language-none"><code class="language-none">rule_files:  [ - &lt;filepath_glob&gt; ... ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>默认情况下Prometheus会每分钟对这些告警规则进行计算，如果用户想定义自己的告警计算周期，则可以通过<code>evaluation_interval</code>来覆盖默认的计算周期：</p><pre class="line-numbers language-none"><code class="language-none">global:  [ evaluation_interval: &lt;duration&gt; | default = 1m ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h2 id="模版化"><a href="#模版化" class="headerlink" title="模版化"></a>模版化</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/monitor/prometheus/chang-yong-gao-jing-zheng-li/chang-yong-gao-jing-zheng-li/"/>
      <url>/2021/12/24/monitor/prometheus/chang-yong-gao-jing-zheng-li/chang-yong-gao-jing-zheng-li/</url>
      
        <content type="html"><![CDATA[<h1 id="常用告警整理"><a href="#常用告警整理" class="headerlink" title="常用告警整理"></a>常用告警整理</h1><blockquote><p><a href="https://awesome-prometheus-alerts.grep.to/">https://awesome-prometheus-alerts.grep.to/</a></p></blockquote><h2 id="prometheus自身监控"><a href="#prometheus自身监控" class="headerlink" title="prometheus自身监控"></a>prometheus自身监控</h2><ul><li><p>prometheus job丢失</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token punctuation">-</span> <span class="token key atrule">alert</span><span class="token punctuation">:</span> PrometheusJobMissing  <span class="token key atrule">expr</span><span class="token punctuation">:</span> absent(up<span class="token punctuation">{</span>job="prometheus"<span class="token punctuation">}</span>)  <span class="token key atrule">for</span><span class="token punctuation">:</span> 0m  <span class="token key atrule">labels</span><span class="token punctuation">:</span>    <span class="token key atrule">severity</span><span class="token punctuation">:</span> warning  <span class="token key atrule">annotations</span><span class="token punctuation">:</span>    <span class="token key atrule">summary</span><span class="token punctuation">:</span> Prometheus job missing (instance <span class="token punctuation">{</span><span class="token punctuation">{</span> $labels.instance <span class="token punctuation">}</span><span class="token punctuation">}</span>)    <span class="token key atrule">description</span><span class="token punctuation">:</span> <span class="token string">"A Prometheus job has disappeared\n  VALUE = {{ $value }}\n  LABELS = {{ $labels }}"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/monitor/prometheus/altermanager/altermanager-pei-zhi/"/>
      <url>/2021/12/24/monitor/prometheus/altermanager/altermanager-pei-zhi/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/monitor/prometheus/altermanager/jian-jie/"/>
      <url>/2021/12/24/monitor/prometheus/altermanager/jian-jie/</url>
      
        <content type="html"><![CDATA[<h1 id="altermanager详解"><a href="#altermanager详解" class="headerlink" title="altermanager详解"></a>altermanager详解</h1><h2 id="altermanager简介"><a href="#altermanager简介" class="headerlink" title="altermanager简介"></a>altermanager简介</h2><p>告警能力在Prometheus的架构中被划分成两个独立的部分。通过在Prometheus中定义AlertRule（告警规则），Prometheus会周期性的对告警规则进行计算，如果满足告警触发条件就会向Alertmanager发送告警信息。</p><p><strong>告警规则组成</strong></p><ul><li><p>告警名称：用户需要为告警规则命名，当然对于命名而言，需要能够直接表达出该告警的主要内容</p></li><li><p>告警规则：告警规则实际上主要由PromQL进行定义，其实际意义是当表达式（PromQL）查询结果持续多长时间（During）后出发告警</p></li></ul><p>Alertmanager作为一个独立的组件，负责接收并处理来自Prometheus Server的告警信息。Alertmanager可以对这些告警信息进行进一步的处理，比如当接收到大量重复告警时能够消除重复的告警信息，同时对告警信息进行分组并且路由到正确的通知方，Prometheus内置了对邮件，Slack等多种通知方式的支持，同时还支持与Webhook的集成，以支持更多定制化的场景。</p><h2 id="altermanager特性"><a href="#altermanager特性" class="headerlink" title="altermanager特性"></a>altermanager特性</h2><h3 id="分组"><a href="#分组" class="headerlink" title="分组"></a>分组</h3><p>分组机制可以将详细的告警信息合并成一个通知。在某些情况下，比如由于系统宕机导致大量的告警被同时触发，在这种情况下分组机制可以将这些被触发的告警合并为一个告警通知，避免一次性接受大量的告警通知，而无法对问题进行快速定位。</p><p>告警分组，告警时间，以及告警的接受方式可以通过Alertmanager的配置文件进行配置。</p><h3 id="抑制"><a href="#抑制" class="headerlink" title="抑制"></a>抑制</h3><p>抑制是指当某一告警发出后，可以停止重复发送由此告警引发的其它告警的机制。</p><p>抑制机制同样通过Alertmanager的配置文件进行设置。</p><h2 id="静默"><a href="#静默" class="headerlink" title="静默"></a>静默</h2><p>静默提供了一个简单的机制可以快速根据标签对告警进行静默处理。如果接收到的告警符合静默的配置，Alertmanager则不会发送告警通知。</p><p>静默设置需要在Alertmanager的Web页面上进行设置。</p><h2 id="altermanager环境配置"><a href="#altermanager环境配置" class="headerlink" title="altermanager环境配置"></a>altermanager环境配置</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/aws/eksctl/eksctl/"/>
      <url>/2021/12/24/aws/eksctl/eksctl/</url>
      
        <content type="html"><![CDATA[<h1 id="eksctl"><a href="#eksctl" class="headerlink" title="eksctl"></a>eksctl</h1><blockquote><p>&nbsp;官网：<a href="https://eksctl.io/">https://eksctl.io/</a><br>&nbsp;github地址：<a href="https://github.com/weaveworks/eksctl">https://github.com/weaveworks/eksctl</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/monitor/prometheus/altermanager/huan-jing-pei-zhi/"/>
      <url>/2021/12/24/monitor/prometheus/altermanager/huan-jing-pei-zhi/</url>
      
        <content type="html"><![CDATA[<h1 id="altermanager环境配置"><a href="#altermanager环境配置" class="headerlink" title="altermanager环境配置"></a>altermanager环境配置</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/monitor/prometheus/prometheus-operator/prometheus-operator/"/>
      <url>/2021/12/24/monitor/prometheus/prometheus-operator/prometheus-operator/</url>
      
        <content type="html"><![CDATA[<h1 id="prometheus-operator"><a href="#prometheus-operator" class="headerlink" title="prometheus-operator"></a>prometheus-operator</h1><blockquote><p>GitHub 地址：<a href="https://github.com/prometheus-operator/kube-prometheus">https://github.com/prometheus-operator/kube-prometheus</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/architecture/ibora-architecture/ibora-architecture/"/>
      <url>/2021/12/24/architecture/ibora-architecture/ibora-architecture/</url>
      
        <content type="html"><![CDATA[<h1 id="ibora-architecture"><a href="#ibora-architecture" class="headerlink" title="ibora-architecture"></a>ibora-architecture</h1><h2 id="基础选型"><a href="#基础选型" class="headerlink" title="基础选型"></a>基础选型</h2><blockquote></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/monitor/prometheus/chang-yong-gao-jing-zheng-li/bao-jing-xiang/"/>
      <url>/2021/12/24/monitor/prometheus/chang-yong-gao-jing-zheng-li/bao-jing-xiang/</url>
      
        <content type="html"><![CDATA[<ul><li><p><code>NodeFilesystemSpaceFillingUp</code> x</p><ul><li>预计文件系统将在接下来的 24小时内耗尽空间(warning)(触发周期1h)</li><li>预计文件系统将在接下来的 4小时内耗尽空间(critical)(触发周期1h)</li></ul></li><li><p><code>NodeFilesystemAlmostOutOfSpace</code> x</p><ul><li>文件系统剩余空间小于%5(warning)(触发周期1h)</li><li>文件系统剩余空间小于%3(critical)(触发周期1h) 30%</li></ul></li><li><p><code>NodeFilesystemFilesFillingUp</code> x</p><ul><li>预计文件系统将在接下来的 24 小时内耗尽 inode(warning)(触发周期1h)</li><li>预计文件系统将在接下来的 4 小时内耗尽 inode(critical)(触发周期1h)</li></ul></li><li><p><code>NodeFilesystemAlmostOutOfFiles</code>x</p><ul><li>文件系统还剩下不到 5% 的 inode(warning)(触发周期1h)</li><li>文件系统还剩下不到 3% 的 inode(critical)(触发周期1h)</li></ul></li><li><p><code>NodeNetworkReceiveErrs</code> </p><ul><li>网络接口报告许多接收错误&gt;1%(warning)(触发周期1h)</li></ul></li><li><p><code>NodeNetworkTransmitErrs</code></p><ul><li>网络接口报告许多传输错误&gt;1%(warning)(触发周期1h)</li></ul></li><li><p><code>NodeHighNumberConntrackEntriesUsed</code> y</p><ul><li>conntrack的数量已接近极限(触发条件大于75%)</li></ul></li><li><p><code>NodeTextFileCollectorScrapeError</code></p><ul><li>节点导出文本文件收集器抓取失败</li></ul></li><li><p><code>NodeClockSkewDetected</code></p><ul><li>检测到时钟偏差(warning)(触发周期10m)</li></ul></li><li><p><code>NodeClockNotSynchronising</code></p><ul><li>时钟不同步(warning)(触发周期10m)</li></ul></li><li><p><code>NodeRAIDDegraded</code></p><ul><li>RAID 阵列降级(critical)(触发周期15m)</li></ul></li><li><p><code>NodeRAIDDiskFailure</code></p><ul><li>RAID 阵列中的设备故障(warning)(触发周期15m)</li></ul></li><li><p><code>NodeFileDescriptorLimit</code></p><ul><li>预计内核将很快耗尽文件描述符(critical)(触发周期15m)</li></ul></li><li><p><code>HostOutOfMemory</code></p><ul><li>主机内存耗尽(warning)(触发周期15m)</li></ul></li><li><p><code>HostOutOfDiskSpace</code></p><ul><li>主机磁盘耗尽(warning)(触发周期15m)</li></ul></li><li><p><code>HostOutOfInodes</code></p><ul><li>主机inode耗尽(warning)(触发周期15m)</li></ul></li><li><p><code>xdp-service-apps</code></p><ul><li>xdp服务请求错误率高(warning)(触发周期15m)</li></ul></li><li><p><code>NodeNetworkInterfaceFlapping</code></p><ul><li>网络接口经常改变其状态(warning)(触发周期2m)</li></ul></li><li><p><code>KubeAPIErrorBudgetBurn</code></p><ul><li>apiserver错误超出阈值</li></ul></li><li><p><code>KubePodCrashLooping</code></p><ul><li>Pod 崩溃循环(warning)(触发周期15m)</li></ul></li><li><p><code>KubePodNotReady</code></p><ul><li>Pod 处于非就绪状态超过 15 分钟(warning)(触发周期15m)</li></ul></li><li><p><code>KubeContainerWaiting</code></p><ul><li>Pod 容器等待时间超过 1 小时(warning)(触发周期1h)</li></ul></li><li><p><code>KubeDaemonSetNotScheduled</code></p><ul><li>Daemonset pod无法调度(warning)(触发周期10m)</li></ul></li><li><p><code>KubeJobFailed</code></p><ul><li>job为能完成(warning)(触发周期15m)</li></ul></li><li><p><code>KubeCPUOvercommit</code></p><ul><li>集群已过量使用 CPU 资源(warning)(触发周期5m) 10m</li></ul></li><li><p><code>KubeMemoryOvercommit</code></p><ul><li>集群已过量使用Memory资源(warning)(触发周期5m)</li></ul></li><li><p><code>KubeQuotaAlmostFull</code></p><ul><li>namespace配额将被占满(info)(触发周期15m)</li></ul></li><li><p><code>KubePersistentVolumeFillingUp</code></p><ul><li>PersistentVolume即将填满（criticle）(触发周期1m)</li></ul></li><li><p><code>KubePersistentVolumeErrors</code></p><ul><li>PersistentVolume 的配置有问题(criticle)(触发周期5m)</li></ul></li><li><p><code>KubeVersionMismatch</code></p><ul><li>运行的Kubernetes 组件的不同语义版本(warning)(触发周期15m)</li></ul></li><li><p><code>KubeClientErrors</code></p><ul><li>Kubernetes api客户端报错(warning)(触发周期15m)</li></ul></li><li><p><code>KubeClientCertificateExpiration</code></p><ul><li>客户端证书失效(criticle)</li></ul></li><li><p><code>KubeNodeNotReady</code></p><ul><li>节点未就绪(warning)(触发周期15m)</li></ul></li><li><p><code>KubeNodeUnreachable</code></p><ul><li>节点不可达(warning)(触发周期15m)</li></ul></li><li><p><code>KubeletTooManyPods</code></p><ul><li>Kubelet正在满负荷运行(warning)(触发周期15m)</li></ul></li><li><p><code>KubeNodeReadinessFlapping</code></p><ul><li>节点就绪状态不断变化(warning)(触发周期15m)</li></ul></li><li><p><code>KubeletPlegDurationHigh</code></p><ul><li>Kubelet Pod 生命周期事件生成器需要很长时间才能重新工作(warning)(触发周期5m)</li></ul></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/architecture/ibora-architecture/ibora-ji-zhu-xuan-xing/"/>
      <url>/2021/12/24/architecture/ibora-architecture/ibora-ji-zhu-xuan-xing/</url>
      
        <content type="html"><![CDATA[<h1 id="ibora技术选型"><a href="#ibora技术选型" class="headerlink" title="ibora技术选型"></a>ibora技术选型</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/golang/third-party-library/zap/"/>
      <url>/2021/12/24/golang/third-party-library/zap/</url>
      
        <content type="html"><![CDATA[<h1 id="zap"><a href="#zap" class="headerlink" title="zap"></a>zap</h1><blockquote><p>github 地址：<a href="https://github.com/uber-go/zap">https://github.com/uber-go/zap</a></p></blockquote><h2 id="zap简介"><a href="#zap简介" class="headerlink" title="zap简介"></a>zap简介</h2><p>Zap是非常快的、结构化的，分日志级别的Go日志库。</p><ul><li>同时提供了结构化日志记录和printf风格的日志记录</li><li>非常的快</li></ul><h2 id="zap环境配置"><a href="#zap环境配置" class="headerlink" title="zap环境配置"></a>zap环境配置</h2><pre class="line-numbers language-none"><code class="language-none">go get -u go.uber.org/zap<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h1 id="zap-使用"><a href="#zap-使用" class="headerlink" title="zap 使用"></a>zap 使用</h1><h3 id="配置Zap-Logger"><a href="#配置Zap-Logger" class="headerlink" title="配置Zap Logger"></a>配置Zap Logger</h3><p>Zap提供了两种类型的日志记录器—<code>Sugared Logger</code>和<code>Logger</code>。</p><p>在性能很好但不是很关键的上下文中，使用<code>SugaredLogger</code>。它比其他结构化日志记录包快4-10倍，并且支持结构化和printf风格的日志记录。</p><p>在每一微秒和每一次内存分配都很重要的上下文中，使用<code>Logger</code>。它甚至比<code>SugaredLogger</code>更快，内存分配次数也更少，但它只支持强类型的结构化日志记录。</p><h4 id="Logger"><a href="#Logger" class="headerlink" title="Logger"></a>Logger</h4><ul><li>通过调用<code>zap.NewProduction()</code>/<code>zap.NewDevelopment()</code>或者<code>zap.Example()</code>创建一个Logger。</li><li>上面的每一个函数都将创建一个logger。唯一的区别在于它将记录的信息不同。例如production logger默认记录调用函数信息、日期和时间等。</li><li>通过Logger调用Info/Error等。</li><li>默认情况下日志都会打印到应用程序的console界面。</li></ul><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">var</span> logger <span class="token operator">*</span>zap<span class="token punctuation">.</span>Logger<span class="token keyword">func</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span><span class="token function">InitLogger</span><span class="token punctuation">(</span><span class="token punctuation">)</span>  <span class="token keyword">defer</span> logger<span class="token punctuation">.</span><span class="token function">Sync</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token function">simpleHttpGet</span><span class="token punctuation">(</span><span class="token string">"www.google.com"</span><span class="token punctuation">)</span><span class="token function">simpleHttpGet</span><span class="token punctuation">(</span><span class="token string">"http://www.google.com"</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token function">InitLogger</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>logger<span class="token punctuation">,</span> <span class="token boolean">_</span> <span class="token operator">=</span> zap<span class="token punctuation">.</span><span class="token function">NewProduction</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token function">simpleHttpGet</span><span class="token punctuation">(</span>url <span class="token builtin">string</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>resp<span class="token punctuation">,</span> err <span class="token operator">:=</span> http<span class="token punctuation">.</span><span class="token function">Get</span><span class="token punctuation">(</span>url<span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>logger<span class="token punctuation">.</span><span class="token function">Error</span><span class="token punctuation">(</span><span class="token string">"Error fetching url.."</span><span class="token punctuation">,</span>zap<span class="token punctuation">.</span><span class="token function">String</span><span class="token punctuation">(</span><span class="token string">"url"</span><span class="token punctuation">,</span> url<span class="token punctuation">)</span><span class="token punctuation">,</span>zap<span class="token punctuation">.</span><span class="token function">Error</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>logger<span class="token punctuation">.</span><span class="token function">Info</span><span class="token punctuation">(</span><span class="token string">"Success.."</span><span class="token punctuation">,</span>zap<span class="token punctuation">.</span><span class="token function">String</span><span class="token punctuation">(</span><span class="token string">"statusCode"</span><span class="token punctuation">,</span> resp<span class="token punctuation">.</span>Status<span class="token punctuation">)</span><span class="token punctuation">,</span>zap<span class="token punctuation">.</span><span class="token function">String</span><span class="token punctuation">(</span><span class="token string">"url"</span><span class="token punctuation">,</span> url<span class="token punctuation">)</span><span class="token punctuation">)</span>resp<span class="token punctuation">.</span>Body<span class="token punctuation">.</span><span class="token function">Close</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在上面的代码中，我们首先创建了一个Logger，然后使用Info/ Error等Logger方法记录消息。</p><p>日志记录器方法的语法是这样的：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">func</span> <span class="token punctuation">(</span>log <span class="token operator">*</span>Logger<span class="token punctuation">)</span> <span class="token function">MethodXXX</span><span class="token punctuation">(</span>msg <span class="token builtin">string</span><span class="token punctuation">,</span> fields <span class="token operator">...</span>Field<span class="token punctuation">)</span> <span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>其中<code>MethodXXX</code>是一个可变参数函数，可以是Info / Error/ Debug / Panic等。每个方法都接受一个消息字符串和任意数量的<code>zapcore.Field</code>场参数。</p><p>每个<code>zapcore.Field</code>其实就是一组键值对参数。</p><p>我们执行上面的代码会得到如下输出结果：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token punctuation">{</span><span class="token string">"level"</span><span class="token builtin class-name">:</span><span class="token string">"error"</span>,<span class="token string">"ts"</span>:1572159218.912792,<span class="token string">"caller"</span><span class="token builtin class-name">:</span><span class="token string">"zap_demo/temp.go:25"</span>,<span class="token string">"msg"</span><span class="token builtin class-name">:</span><span class="token string">"Error fetching url.."</span>,<span class="token string">"url"</span><span class="token builtin class-name">:</span><span class="token string">"www.sogo.com"</span>,<span class="token string">"error"</span><span class="token builtin class-name">:</span><span class="token string">"Get www.sogo.com: unsupported protocol scheme <span class="token entity" title="\&quot;">\"</span><span class="token entity" title="\&quot;">\"</span>"</span>,<span class="token string">"stacktrace"</span><span class="token builtin class-name">:</span><span class="token string">"main.simpleHttpGet<span class="token entity" title="\n">\n</span><span class="token entity" title="\t">\t</span>/Users/q1mi/zap_demo/temp.go:25<span class="token entity" title="\n">\n</span>main.main<span class="token entity" title="\n">\n</span><span class="token entity" title="\t">\t</span>/Users/q1mi/zap_demo/temp.go:14<span class="token entity" title="\n">\n</span>runtime.main<span class="token entity" title="\n">\n</span><span class="token entity" title="\t">\t</span>/usr/local/go/src/runtime/proc.go:203"</span><span class="token punctuation">}</span><span class="token punctuation">{</span><span class="token string">"level"</span><span class="token builtin class-name">:</span><span class="token string">"info"</span>,<span class="token string">"ts"</span>:1572159219.1227388,<span class="token string">"caller"</span><span class="token builtin class-name">:</span><span class="token string">"zap_demo/temp.go:30"</span>,<span class="token string">"msg"</span><span class="token builtin class-name">:</span><span class="token string">"Success.."</span>,<span class="token string">"statusCode"</span><span class="token builtin class-name">:</span><span class="token string">"200 OK"</span>,<span class="token string">"url"</span><span class="token builtin class-name">:</span><span class="token string">"http://www.sogo.com"</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h4 id="Sugared-Logger"><a href="#Sugared-Logger" class="headerlink" title="Sugared Logger"></a>Sugared Logger</h4><p>现在让我们使用Sugared Logger来实现相同的功能。</p><ul><li>大部分的实现基本都相同。</li><li>惟一的区别是，我们通过调用主logger的<code>. Sugar()</code>方法来获取一个<code>SugaredLogger</code>。</li><li>然后使用<code>SugaredLogger</code>以<code>printf</code>格式记录语句</li></ul><p>下面是修改过后使用<code>SugaredLogger</code>代替<code>Logger</code>的代码：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">var</span> sugarLogger <span class="token operator">*</span>zap<span class="token punctuation">.</span>SugaredLogger<span class="token keyword">func</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span><span class="token function">InitLogger</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">defer</span> sugarLogger<span class="token punctuation">.</span><span class="token function">Sync</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token function">simpleHttpGet</span><span class="token punctuation">(</span><span class="token string">"www.google.com"</span><span class="token punctuation">)</span><span class="token function">simpleHttpGet</span><span class="token punctuation">(</span><span class="token string">"http://www.google.com"</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token function">InitLogger</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>  logger<span class="token punctuation">,</span> <span class="token boolean">_</span> <span class="token operator">:=</span> zap<span class="token punctuation">.</span><span class="token function">NewProduction</span><span class="token punctuation">(</span><span class="token punctuation">)</span>sugarLogger <span class="token operator">=</span> logger<span class="token punctuation">.</span><span class="token function">Sugar</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token function">simpleHttpGet</span><span class="token punctuation">(</span>url <span class="token builtin">string</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>sugarLogger<span class="token punctuation">.</span><span class="token function">Debugf</span><span class="token punctuation">(</span><span class="token string">"Trying to hit GET request for %s"</span><span class="token punctuation">,</span> url<span class="token punctuation">)</span>resp<span class="token punctuation">,</span> err <span class="token operator">:=</span> http<span class="token punctuation">.</span><span class="token function">Get</span><span class="token punctuation">(</span>url<span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>sugarLogger<span class="token punctuation">.</span><span class="token function">Errorf</span><span class="token punctuation">(</span><span class="token string">"Error fetching URL %s : Error = %s"</span><span class="token punctuation">,</span> url<span class="token punctuation">,</span> err<span class="token punctuation">)</span><span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>sugarLogger<span class="token punctuation">.</span><span class="token function">Infof</span><span class="token punctuation">(</span><span class="token string">"Success! statusCode = %s for URL %s"</span><span class="token punctuation">,</span> resp<span class="token punctuation">.</span>Status<span class="token punctuation">,</span> url<span class="token punctuation">)</span>resp<span class="token punctuation">.</span>Body<span class="token punctuation">.</span><span class="token function">Close</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当你执行上面的代码会得到如下输出：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token punctuation">{</span><span class="token string">"level"</span><span class="token builtin class-name">:</span><span class="token string">"error"</span>,<span class="token string">"ts"</span>:1572159149.923002,<span class="token string">"caller"</span><span class="token builtin class-name">:</span><span class="token string">"logic/temp2.go:27"</span>,<span class="token string">"msg"</span><span class="token builtin class-name">:</span><span class="token string">"Error fetching URL www.sogo.com : Error = Get www.sogo.com: unsupported protocol scheme <span class="token entity" title="\&quot;">\"</span><span class="token entity" title="\&quot;">\"</span>"</span>,<span class="token string">"stacktrace"</span><span class="token builtin class-name">:</span><span class="token string">"main.simpleHttpGet<span class="token entity" title="\n">\n</span><span class="token entity" title="\t">\t</span>/Users/q1mi/zap_demo/logic/temp2.go:27<span class="token entity" title="\n">\n</span>main.main<span class="token entity" title="\n">\n</span><span class="token entity" title="\t">\t</span>/Users/q1mi/zap_demo/logic/temp2.go:14<span class="token entity" title="\n">\n</span>runtime.main<span class="token entity" title="\n">\n</span><span class="token entity" title="\t">\t</span>/usr/local/go/src/runtime/proc.go:203"</span><span class="token punctuation">}</span><span class="token punctuation">{</span><span class="token string">"level"</span><span class="token builtin class-name">:</span><span class="token string">"info"</span>,<span class="token string">"ts"</span>:1572159150.192585,<span class="token string">"caller"</span><span class="token builtin class-name">:</span><span class="token string">"logic/temp2.go:29"</span>,<span class="token string">"msg"</span><span class="token builtin class-name">:</span><span class="token string">"Success! statusCode = 200 OK for URL http://www.sogo.com"</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>你应该注意到的了，到目前为止这两个logger都打印输出JSON结构格式。</p><p>在本博客的后面部分，我们将更详细地讨论SugaredLogger，并了解如何进一步配置它。</p><h3 id="定制logger"><a href="#定制logger" class="headerlink" title="定制logger"></a>定制logger</h3><h4 id="将日志写入文件而不是终端"><a href="#将日志写入文件而不是终端" class="headerlink" title="将日志写入文件而不是终端"></a>将日志写入文件而不是终端</h4><p>我们要做的第一个更改是把日志写入文件，而不是打印到应用程序控制台。</p><ul><li>我们将使用<code>zap.New(…)</code>方法来手动传递所有配置，而不是使用像<code>zap.NewProduction()</code>这样的预置方法来创建logger。</li></ul><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">func</span> <span class="token function">New</span><span class="token punctuation">(</span>core zapcore<span class="token punctuation">.</span>Core<span class="token punctuation">,</span> options <span class="token operator">...</span>Option<span class="token punctuation">)</span> <span class="token operator">*</span>Logger<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><code>zapcore.Core</code>需要三个配置——<code>Encoder</code>，<code>WriteSyncer</code>，<code>LogLevel</code>。</p><p>1.<strong>Encoder</strong>:编码器(如何写入日志)。我们将使用开箱即用的<code>NewJSONEncoder()</code>，并使用预先设置的<code>ProductionEncoderConfig()</code>。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">zapcore<span class="token punctuation">.</span><span class="token function">NewJSONEncoder</span><span class="token punctuation">(</span>zap<span class="token punctuation">.</span><span class="token function">NewProductionEncoderConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>2.<strong>WriterSyncer</strong> ：指定日志将写到哪里去。我们使用<code>zapcore.AddSync()</code>函数并且将打开的文件句柄传进去。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">file<span class="token punctuation">,</span> <span class="token boolean">_</span> <span class="token operator">:=</span> os<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token string">"./test.log"</span><span class="token punctuation">)</span>writeSyncer <span class="token operator">:=</span> zapcore<span class="token punctuation">.</span><span class="token function">AddSync</span><span class="token punctuation">(</span>file<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>3.<strong>Log Level</strong>：哪种级别的日志将被写入。</p><p>我们将修改上述部分中的Logger代码，并重写<code>InitLogger()</code>方法。其余方法—<code>main()</code> /<code>SimpleHttpGet()</code>保持不变。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">func</span> <span class="token function">InitLogger</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>writeSyncer <span class="token operator">:=</span> <span class="token function">getLogWriter</span><span class="token punctuation">(</span><span class="token punctuation">)</span>encoder <span class="token operator">:=</span> <span class="token function">getEncoder</span><span class="token punctuation">(</span><span class="token punctuation">)</span>core <span class="token operator">:=</span> zapcore<span class="token punctuation">.</span><span class="token function">NewCore</span><span class="token punctuation">(</span>encoder<span class="token punctuation">,</span> writeSyncer<span class="token punctuation">,</span> zapcore<span class="token punctuation">.</span>DebugLevel<span class="token punctuation">)</span>logger <span class="token operator">:=</span> zap<span class="token punctuation">.</span><span class="token function">New</span><span class="token punctuation">(</span>core<span class="token punctuation">)</span>sugarLogger <span class="token operator">=</span> logger<span class="token punctuation">.</span><span class="token function">Sugar</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token function">getEncoder</span><span class="token punctuation">(</span><span class="token punctuation">)</span> zapcore<span class="token punctuation">.</span>Encoder <span class="token punctuation">{</span><span class="token keyword">return</span> zapcore<span class="token punctuation">.</span><span class="token function">NewJSONEncoder</span><span class="token punctuation">(</span>zap<span class="token punctuation">.</span><span class="token function">NewProductionEncoderConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token function">getLogWriter</span><span class="token punctuation">(</span><span class="token punctuation">)</span> zapcore<span class="token punctuation">.</span>WriteSyncer <span class="token punctuation">{</span>file<span class="token punctuation">,</span> <span class="token boolean">_</span> <span class="token operator">:=</span> os<span class="token punctuation">.</span><span class="token function">Create</span><span class="token punctuation">(</span><span class="token string">"./test.log"</span><span class="token punctuation">)</span><span class="token keyword">return</span> zapcore<span class="token punctuation">.</span><span class="token function">AddSync</span><span class="token punctuation">(</span>file<span class="token punctuation">)</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当使用这些修改过的logger配置调用上述部分的<code>main()</code>函数时，以下输出将打印在文件——<code>test.log</code>中。</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token punctuation">{</span><span class="token string">"level"</span><span class="token builtin class-name">:</span><span class="token string">"debug"</span>,<span class="token string">"ts"</span>:1572160754.994731,<span class="token string">"msg"</span><span class="token builtin class-name">:</span><span class="token string">"Trying to hit GET request for www.sogo.com"</span><span class="token punctuation">}</span><span class="token punctuation">{</span><span class="token string">"level"</span><span class="token builtin class-name">:</span><span class="token string">"error"</span>,<span class="token string">"ts"</span>:1572160754.994982,<span class="token string">"msg"</span><span class="token builtin class-name">:</span><span class="token string">"Error fetching URL www.sogo.com : Error = Get www.sogo.com: unsupported protocol scheme <span class="token entity" title="\&quot;">\"</span><span class="token entity" title="\&quot;">\"</span>"</span><span class="token punctuation">}</span><span class="token punctuation">{</span><span class="token string">"level"</span><span class="token builtin class-name">:</span><span class="token string">"debug"</span>,<span class="token string">"ts"</span>:1572160754.994996,<span class="token string">"msg"</span><span class="token builtin class-name">:</span><span class="token string">"Trying to hit GET request for http://www.sogo.com"</span><span class="token punctuation">}</span><span class="token punctuation">{</span><span class="token string">"level"</span><span class="token builtin class-name">:</span><span class="token string">"info"</span>,<span class="token string">"ts"</span>:1572160757.3755069,<span class="token string">"msg"</span><span class="token builtin class-name">:</span><span class="token string">"Success! statusCode = 200 OK for URL http://www.sogo.com"</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h4 id="将JSON-Encoder更改为普通的Log-Encoder"><a href="#将JSON-Encoder更改为普通的Log-Encoder" class="headerlink" title="将JSON Encoder更改为普通的Log Encoder"></a>将JSON Encoder更改为普通的Log Encoder</h4><p>现在，我们希望将编码器从JSON Encoder更改为普通Encoder。为此，我们需要将<code>NewJSONEncoder()</code>更改为<code>NewConsoleEncoder()</code>。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">return</span> zapcore<span class="token punctuation">.</span><span class="token function">NewConsoleEncoder</span><span class="token punctuation">(</span>zap<span class="token punctuation">.</span><span class="token function">NewProductionEncoderConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>当使用这些修改过的logger配置调用上述部分的<code>main()</code>函数时，以下输出将打印在文件——<code>test.log</code>中。</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token number">1</span>.572161051846623e+09debugTrying to hit GET request <span class="token keyword">for</span> www.sogo.com<span class="token number">1</span>.572161051846828e+09errorError fetching URL www.sogo.com <span class="token builtin class-name">:</span> Error <span class="token operator">=</span> Get www.sogo.com: unsupported protocol scheme <span class="token string">""</span><span class="token number">1</span>.5721610518468401e+09debugTrying to hit GET request <span class="token keyword">for</span> http://www.sogo.com<span class="token number">1</span>.572161052068744e+09infoSuccess<span class="token operator">!</span> statusCode <span class="token operator">=</span> <span class="token number">200</span> OK <span class="token keyword">for</span> URL http://www.sogo.com<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h4 id="更改时间编码并添加调用者详细信息"><a href="#更改时间编码并添加调用者详细信息" class="headerlink" title="更改时间编码并添加调用者详细信息"></a>更改时间编码并添加调用者详细信息</h4><p>鉴于我们对配置所做的更改，有下面两个问题：</p><ul><li>时间是以非人类可读的方式展示，例如1.572161051846623e+09</li><li>调用方函数的详细信息没有显示在日志中</li></ul><p>我们要做的第一件事是覆盖默认的<code>ProductionConfig()</code>，并进行以下更改:</p><ul><li>修改时间编码器</li><li>在日志文件中使用大写字母记录日志级别</li></ul><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">func</span> <span class="token function">getEncoder</span><span class="token punctuation">(</span><span class="token punctuation">)</span> zapcore<span class="token punctuation">.</span>Encoder <span class="token punctuation">{</span>encoderConfig <span class="token operator">:=</span> zap<span class="token punctuation">.</span><span class="token function">NewProductionEncoderConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span>encoderConfig<span class="token punctuation">.</span>EncodeTime <span class="token operator">=</span> zapcore<span class="token punctuation">.</span>ISO8601TimeEncoderencoderConfig<span class="token punctuation">.</span>EncodeLevel <span class="token operator">=</span> zapcore<span class="token punctuation">.</span>CapitalLevelEncoder<span class="token keyword">return</span> zapcore<span class="token punctuation">.</span><span class="token function">NewConsoleEncoder</span><span class="token punctuation">(</span>encoderConfig<span class="token punctuation">)</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>接下来，我们将修改zap logger代码，添加将调用函数信息记录到日志中的功能。为此，我们将在<code>zap.New(..)</code>函数中添加一个<code>Option</code>。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">logger <span class="token operator">:=</span> zap<span class="token punctuation">.</span><span class="token function">New</span><span class="token punctuation">(</span>core<span class="token punctuation">,</span> zap<span class="token punctuation">.</span><span class="token function">AddCaller</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>当使用这些修改过的logger配置调用上述部分的<code>main()</code>函数时，以下输出将打印在文件——<code>test.log</code>中。</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token number">2019</span>-10-27T15:33:29.855+0800DEBUGlogic/temp2.go:47Trying to hit GET request <span class="token keyword">for</span> www.sogo.com<span class="token number">2019</span>-10-27T15:33:29.855+0800ERRORlogic/temp2.go:50Error fetching URL www.sogo.com <span class="token builtin class-name">:</span> Error <span class="token operator">=</span> Get www.sogo.com: unsupported protocol scheme <span class="token string">""</span><span class="token number">2019</span>-10-27T15:33:29.856+0800DEBUGlogic/temp2.go:47Trying to hit GET request <span class="token keyword">for</span> http://www.sogo.com<span class="token number">2019</span>-10-27T15:33:30.125+0800INFOlogic/temp2.go:52Success<span class="token operator">!</span> statusCode <span class="token operator">=</span> <span class="token number">200</span> OK <span class="token keyword">for</span> URL http://www.sogo.com<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h2 id="使用Lumberjack进行日志切割归档"><a href="#使用Lumberjack进行日志切割归档" class="headerlink" title="使用Lumberjack进行日志切割归档"></a>使用Lumberjack进行日志切割归档</h2><p>这个日志程序中唯一缺少的就是日志切割归档功能。</p><blockquote><p><em>Zap本身不支持切割归档日志文件</em></p></blockquote><p>为了添加日志切割归档功能，我们将使用第三方库<a href="https://github.com/natefinch/lumberjack">Lumberjack</a>来实现。</p><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><p>执行下面的命令安装Lumberjack</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">go get -u github.com/natefinch/lumberjack<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="zap-logger中加入Lumberjack"><a href="#zap-logger中加入Lumberjack" class="headerlink" title="zap logger中加入Lumberjack"></a>zap logger中加入Lumberjack</h3><p>要在zap中加入Lumberjack支持，我们需要修改<code>WriteSyncer</code>代码。我们将按照下面的代码修改<code>getLogWriter()</code>函数：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">func</span> <span class="token function">getLogWriter</span><span class="token punctuation">(</span><span class="token punctuation">)</span> zapcore<span class="token punctuation">.</span>WriteSyncer <span class="token punctuation">{</span>lumberJackLogger <span class="token operator">:=</span> <span class="token operator">&amp;</span>lumberjack<span class="token punctuation">.</span>Logger<span class="token punctuation">{</span>Filename<span class="token punctuation">:</span>   <span class="token string">"./test.log"</span><span class="token punctuation">,</span>MaxSize<span class="token punctuation">:</span>    <span class="token number">10</span><span class="token punctuation">,</span>MaxBackups<span class="token punctuation">:</span> <span class="token number">5</span><span class="token punctuation">,</span>MaxAge<span class="token punctuation">:</span>     <span class="token number">30</span><span class="token punctuation">,</span>Compress<span class="token punctuation">:</span>   <span class="token boolean">false</span><span class="token punctuation">,</span><span class="token punctuation">}</span><span class="token keyword">return</span> zapcore<span class="token punctuation">.</span><span class="token function">AddSync</span><span class="token punctuation">(</span>lumberJackLogger<span class="token punctuation">)</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>Lumberjack Logger采用以下属性作为输入:</p><ul><li>Filename: 日志文件的位置</li><li>MaxSize：在进行切割之前，日志文件的最大大小（以MB为单位）</li><li>MaxBackups：保留旧文件的最大个数</li><li>MaxAges：保留旧文件的最大天数</li><li>Compress：是否压缩/归档旧文件</li></ul><h3 id="测试所有功能"><a href="#测试所有功能" class="headerlink" title="测试所有功能"></a>测试所有功能</h3><p>最终，使用Zap/Lumberjack logger的完整示例代码如下：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">package</span> main<span class="token keyword">import</span> <span class="token punctuation">(</span><span class="token string">"net/http"</span><span class="token string">"github.com/natefinch/lumberjack"</span><span class="token string">"go.uber.org/zap"</span><span class="token string">"go.uber.org/zap/zapcore"</span><span class="token punctuation">)</span><span class="token keyword">var</span> sugarLogger <span class="token operator">*</span>zap<span class="token punctuation">.</span>SugaredLogger<span class="token keyword">func</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span><span class="token function">InitLogger</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">defer</span> sugarLogger<span class="token punctuation">.</span><span class="token function">Sync</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token function">simpleHttpGet</span><span class="token punctuation">(</span><span class="token string">"www.sogo.com"</span><span class="token punctuation">)</span><span class="token function">simpleHttpGet</span><span class="token punctuation">(</span><span class="token string">"http://www.sogo.com"</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token function">InitLogger</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>writeSyncer <span class="token operator">:=</span> <span class="token function">getLogWriter</span><span class="token punctuation">(</span><span class="token punctuation">)</span>encoder <span class="token operator">:=</span> <span class="token function">getEncoder</span><span class="token punctuation">(</span><span class="token punctuation">)</span>core <span class="token operator">:=</span> zapcore<span class="token punctuation">.</span><span class="token function">NewCore</span><span class="token punctuation">(</span>encoder<span class="token punctuation">,</span> writeSyncer<span class="token punctuation">,</span> zapcore<span class="token punctuation">.</span>DebugLevel<span class="token punctuation">)</span>logger <span class="token operator">:=</span> zap<span class="token punctuation">.</span><span class="token function">New</span><span class="token punctuation">(</span>core<span class="token punctuation">,</span> zap<span class="token punctuation">.</span><span class="token function">AddCaller</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>sugarLogger <span class="token operator">=</span> logger<span class="token punctuation">.</span><span class="token function">Sugar</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token function">getEncoder</span><span class="token punctuation">(</span><span class="token punctuation">)</span> zapcore<span class="token punctuation">.</span>Encoder <span class="token punctuation">{</span>encoderConfig <span class="token operator">:=</span> zap<span class="token punctuation">.</span><span class="token function">NewProductionEncoderConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span>encoderConfig<span class="token punctuation">.</span>EncodeTime <span class="token operator">=</span> zapcore<span class="token punctuation">.</span>ISO8601TimeEncoderencoderConfig<span class="token punctuation">.</span>EncodeLevel <span class="token operator">=</span> zapcore<span class="token punctuation">.</span>CapitalLevelEncoder<span class="token keyword">return</span> zapcore<span class="token punctuation">.</span><span class="token function">NewConsoleEncoder</span><span class="token punctuation">(</span>encoderConfig<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token function">getLogWriter</span><span class="token punctuation">(</span><span class="token punctuation">)</span> zapcore<span class="token punctuation">.</span>WriteSyncer <span class="token punctuation">{</span>lumberJackLogger <span class="token operator">:=</span> <span class="token operator">&amp;</span>lumberjack<span class="token punctuation">.</span>Logger<span class="token punctuation">{</span>Filename<span class="token punctuation">:</span>   <span class="token string">"./test.log"</span><span class="token punctuation">,</span>MaxSize<span class="token punctuation">:</span>    <span class="token number">1</span><span class="token punctuation">,</span>MaxBackups<span class="token punctuation">:</span> <span class="token number">5</span><span class="token punctuation">,</span>MaxAge<span class="token punctuation">:</span>     <span class="token number">30</span><span class="token punctuation">,</span>Compress<span class="token punctuation">:</span>   <span class="token boolean">false</span><span class="token punctuation">,</span><span class="token punctuation">}</span><span class="token keyword">return</span> zapcore<span class="token punctuation">.</span><span class="token function">AddSync</span><span class="token punctuation">(</span>lumberJackLogger<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token function">simpleHttpGet</span><span class="token punctuation">(</span>url <span class="token builtin">string</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>sugarLogger<span class="token punctuation">.</span><span class="token function">Debugf</span><span class="token punctuation">(</span><span class="token string">"Trying to hit GET request for %s"</span><span class="token punctuation">,</span> url<span class="token punctuation">)</span>resp<span class="token punctuation">,</span> err <span class="token operator">:=</span> http<span class="token punctuation">.</span><span class="token function">Get</span><span class="token punctuation">(</span>url<span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>sugarLogger<span class="token punctuation">.</span><span class="token function">Errorf</span><span class="token punctuation">(</span><span class="token string">"Error fetching URL %s : Error = %s"</span><span class="token punctuation">,</span> url<span class="token punctuation">,</span> err<span class="token punctuation">)</span><span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>sugarLogger<span class="token punctuation">.</span><span class="token function">Infof</span><span class="token punctuation">(</span><span class="token string">"Success! statusCode = %s for URL %s"</span><span class="token punctuation">,</span> resp<span class="token punctuation">.</span>Status<span class="token punctuation">,</span> url<span class="token punctuation">)</span>resp<span class="token punctuation">.</span>Body<span class="token punctuation">.</span><span class="token function">Close</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>执行上述代码，下面的内容会输出到文件——test.log中。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token number">2019</span><span class="token operator">-</span><span class="token number">10</span><span class="token operator">-</span><span class="token number">27</span>T15<span class="token punctuation">:</span><span class="token number">50</span><span class="token punctuation">:</span><span class="token number">32.944</span><span class="token operator">+</span><span class="token number">0800</span>DEBUGlogic<span class="token operator">/</span>temp2<span class="token punctuation">.</span><span class="token keyword">go</span><span class="token punctuation">:</span><span class="token number">48</span>Trying to hit GET request <span class="token keyword">for</span> www<span class="token punctuation">.</span>sogo<span class="token punctuation">.</span>com<span class="token number">2019</span><span class="token operator">-</span><span class="token number">10</span><span class="token operator">-</span><span class="token number">27</span>T15<span class="token punctuation">:</span><span class="token number">50</span><span class="token punctuation">:</span><span class="token number">32.944</span><span class="token operator">+</span><span class="token number">0800</span>ERRORlogic<span class="token operator">/</span>temp2<span class="token punctuation">.</span><span class="token keyword">go</span><span class="token punctuation">:</span><span class="token number">51</span>Error fetching URL www<span class="token punctuation">.</span>sogo<span class="token punctuation">.</span>com <span class="token punctuation">:</span> Error <span class="token operator">=</span> Get www<span class="token punctuation">.</span>sogo<span class="token punctuation">.</span>com<span class="token punctuation">:</span> unsupported protocol scheme <span class="token string">""</span><span class="token number">2019</span><span class="token operator">-</span><span class="token number">10</span><span class="token operator">-</span><span class="token number">27</span>T15<span class="token punctuation">:</span><span class="token number">50</span><span class="token punctuation">:</span><span class="token number">32.944</span><span class="token operator">+</span><span class="token number">0800</span>DEBUGlogic<span class="token operator">/</span>temp2<span class="token punctuation">.</span><span class="token keyword">go</span><span class="token punctuation">:</span><span class="token number">48</span>Trying to hit GET request <span class="token keyword">for</span> http<span class="token punctuation">:</span><span class="token operator">/</span><span class="token operator">/</span>www<span class="token punctuation">.</span>sogo<span class="token punctuation">.</span>com<span class="token number">2019</span><span class="token operator">-</span><span class="token number">10</span><span class="token operator">-</span><span class="token number">27</span>T15<span class="token punctuation">:</span><span class="token number">50</span><span class="token punctuation">:</span><span class="token number">33.165</span><span class="token operator">+</span><span class="token number">0800</span>INFOlogic<span class="token operator">/</span>temp2<span class="token punctuation">.</span><span class="token keyword">go</span><span class="token punctuation">:</span><span class="token number">53</span>Success<span class="token operator">!</span> statusCode <span class="token operator">=</span> <span class="token number">200</span> OK <span class="token keyword">for</span> URL http<span class="token punctuation">:</span><span class="token operator">/</span><span class="token operator">/</span>www<span class="token punctuation">.</span>sogo<span class="token punctuation">.</span>com<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>同时，可以在<code>main</code>函数中循环记录日志，测试日志文件是否会自动切割和归档（日志文件每1MB会切割并且在当前目录下最多保存5个备份）。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/golang/grammer/go-gong-cheng-xiang-mu-jie-gou/"/>
      <url>/2021/12/24/golang/grammer/go-gong-cheng-xiang-mu-jie-gou/</url>
      
        <content type="html"><![CDATA[<h1 id="go工程项目结构"><a href="#go工程项目结构" class="headerlink" title="go工程项目结构"></a>go工程项目结构</h1><p><strong>布局结构</strong></p><pre class="line-numbers language-none"><code class="language-none">├── api├── assets├── build│   ├── ci│   └── package├── cmd│   └── _your_app_├── configs├── deployments├── docs├── examples├── githooks├── init├── internal│   ├── app│   │   └── _your_app_│   └── pkg│       └── _your_private_lib_├── pkg│   └── _your_public_lib_├── scripts├── test├── third_party├── tools├── vendor├── web│   ├── app│   ├── static│   └── template├── website├── .gitignore├── LICENSE.md├── Makefile├── README.md└── go.mod<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="Go目录"><a href="#Go目录" class="headerlink" title="Go目录"></a>Go目录</h2><h3 id="cmd"><a href="#cmd" class="headerlink" title="/cmd"></a><code>/cmd</code></h3><p>项目主要的应用程序。</p><p>对于每个应用程序来说这个目录的名字应该和项目可执行文件的名字匹配（例如，<code>/cmd/myapp</code>）。</p><p>不要在这个目录中放太多的代码。如果目录中的代码可以被其他项目导入并使用，那么应该把他们放在<code>/pkg</code>目录。如果目录中的代码不可重用，或者不希望被他人使用，应该将代码放在<code>/internal</code>目录。显示的表明意图比较好！</p><p>通常来说，项目都应该拥有一个小的<code>main</code>函数，并在<code>main</code>函数中导入或者调用<code>/internal</code>和<code>/pkg</code>目录中的代码。</p><h3 id="internal"><a href="#internal" class="headerlink" title="/internal"></a><code>/internal</code></h3><p>私有的应用程序代码库。这些是不希望被其他人导入的代码。请注意：这种模式是Go编译器强制执行的。再次注意，在项目的目录树中的任意位置都可以有<code>internal</code>目录，而不仅仅是在顶级目录中。</p><p>可以在内部代码包中添加一些额外的结构，来分隔共享和非共享的内部代码。这不是必选项（尤其是在小项目中），但是有一个直观的包用途是很棒的。应用程序实际的代码可以放在<code>/internal/app</code>目录（如，<code>internal/app/myapp</code>），而应用程序的共享代码放在<code>/internal/pkg</code>目录（如，<code>internal/pkg/myprivlib</code>）中。</p><h3 id="pkg"><a href="#pkg" class="headerlink" title="/pkg"></a><code>/pkg</code></h3><p>外部应用程序可以使用的库代码（如，<code>/pkg/mypubliclib</code>）。其他项目将会导入这些库来保证项目可以正常运行，所以在将代码放在这里前，一定要三思而行。请注意，<code>internal</code>目录是一个更好的选择来确保项目私有代码不会被其他人导入，因为这是Go强制执行的。使用<code>/pkg</code>目录来明确表示代码可以被其他人安全的导入仍然是一个好方式。</p><p>当您的根目录包含大量非Go组件和目录时，这也是一种将Go代码分组到一个位置的方法，从而使运行各种Go工具更加容易。</p><h3 id="vendor"><a href="#vendor" class="headerlink" title="/vendor"></a><code>/vendor</code></h3><p>应用程序的依赖关系。如果<code>Go Module</code>满足需要，那么就不需要<code>vendor</code>目录。</p><h2 id="服务端应用程序的目录"><a href="#服务端应用程序的目录" class="headerlink" title="服务端应用程序的目录"></a>服务端应用程序的目录</h2><h3 id="api"><a href="#api" class="headerlink" title="/api"></a><code>/api</code></h3><p>OpenAPI/Swagger规范，JSON模式文件，协议定义文件。</p><h2 id="Web应用程序的目录"><a href="#Web应用程序的目录" class="headerlink" title="Web应用程序的目录"></a>Web应用程序的目录</h2><h3 id="web"><a href="#web" class="headerlink" title="/web"></a><code>/web</code></h3><p>Web应用程序特定的组件：静态Web资源，服务器端模板和单页应用（Single-Page App，SPA）。</p><h2 id="通用应用程序的目录"><a href="#通用应用程序的目录" class="headerlink" title="通用应用程序的目录"></a>通用应用程序的目录</h2><h3 id="configs"><a href="#configs" class="headerlink" title="/configs"></a><code>/configs</code></h3><p>配置文件模板或默认配置。将<code>confd</code>或者<code>consul-template</code>文件放在这里。</p><h3 id="init"><a href="#init" class="headerlink" title="/init"></a><code>/init</code></h3><p>系统初始化（systemd、upstart、sysv）和进程管理（runit、supervisord）配置。</p><h3 id="scripts"><a href="#scripts" class="headerlink" title="/scripts"></a><code>/scripts</code></h3><p>用于执行各种构建，安装，分析等操作的脚本。这些脚本使根级别的Makefile变得更小更简单</p><h3 id="build"><a href="#build" class="headerlink" title="/build"></a><code>/build</code></h3><p>打包和持续集成。</p><p>将云（AMI），容器（Docker），操作系统（deb，rpm，pkg）软件包配置和脚本放在<code>/build/package</code>目录中。</p><p>将CI（travis、circle、drone）配置文件和就脚本放在<code>build/ci</code>目录中。请注意，有一些CI工具（如，travis CI）对于配置文件的位置有严格的要求。尝试将配置文件放在<code>/build/ci</code>目录，然后链接到CI工具想要的位置。</p><h3 id="deployments"><a href="#deployments" class="headerlink" title="/deployments"></a><code>/deployments</code></h3><p>IaaS，PaaS，系统和容器编排部署配置和模板（docker-compose，kubernetes/helm，mesos，terraform，bosh）。请注意，在某些存储库中（尤其是使用kubernetes部署的应用程序），该目录的名字是<code>/deploy</code>。</p><h3 id="test"><a href="#test" class="headerlink" title="/test"></a><code>/test</code></h3><p>外部测试应用程序和测试数据。随时根据需要构建<code>/test</code>目录。对于较大的项目，有一个数据子目录更好一些。例如，如果需要Go忽略目录中的内容，则可以使用<code>/test/data</code>或<code>/test/testdata</code>这样的目录名字。请注意，Go还将忽略以“<code>.</code>”或“<code>_</code>”开头的目录或文件，因此可以更具灵活性的来命名测试数据目录。</p><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><h3 id="docs"><a href="#docs" class="headerlink" title="/docs"></a><code>/docs</code></h3><p>设计和用户文档（除了godoc生成的文档）。</p><p>更多样例查看<a href="https://github.com/golang-standards/project-layout/blob/master/docs/README.md"><code>/docs</code></a>。</p><h3 id="tools"><a href="#tools" class="headerlink" title="/tools"></a><code>/tools</code></h3><p>此项目的支持工具。请注意，这些工具可以从<code>/pkg</code>和<code>/internal</code>目录导入代码。</p><h3 id="examples"><a href="#examples" class="headerlink" title="/ examples"></a><code>/ examples</code></h3><p>应用程序或公共库的示例。</p><h3 id="third-party"><a href="#third-party" class="headerlink" title="/third_party"></a><code>/third_party</code></h3><p>外部辅助工具，fork的代码和其他第三方工具（例如Swagger UI）。</p><h3 id="githooks"><a href="#githooks" class="headerlink" title="/githooks"></a><code>/githooks</code></h3><p>Git的钩子。</p><h3 id="assets"><a href="#assets" class="headerlink" title="/assets"></a><code>/assets</code></h3><p>项目中使用的其他资源（图像，Logo等）。</p><h3 id="website"><a href="#website" class="headerlink" title="/website"></a><code>/website</code></h3><p>如果不使用Github pages，则在这里放置项目的网站数据。</p><h2 id="不应该出现的目录"><a href="#不应该出现的目录" class="headerlink" title="不应该出现的目录"></a>不应该出现的目录</h2><h3 id="src"><a href="#src" class="headerlink" title="/src"></a><code>/src</code></h3><p>有一些Go项目确实包含<code>src</code>文件夹，但通常只有在开发者是从Java（这是Java中一个通用的模式）转过来的情况下才会有。如果可以的话请不要使用这种Java模式。你肯定不希望你的Go代码和项目看起来像Java。</p><p>不要将项目级别的<code>/src</code>目录与Go用于其工作空间的<code>/src</code>目录混淆，就像<a href="https://golang.org/doc/code.html">How to Write Go Code</a>中描述的那样。<code>$GOPATH</code>环境变量指向当前的工作空间（默认情况下指向非Windows系统中的<code>$HOME/go</code>）。此工作空间包括顶级<code>/pkg</code>，<code>/bin</code>和<code>/src</code>目录。实际的项目最终变成<code>/src</code>下的子目录，因此，如果项目中有<code>/src</code>目录，则项目路径将会变成：<code>/some/path/to/workspace/src/your_project/ src/your_code.go</code>。请注意，使用Go 1.11，可以将项目放在GOPATH之外，但这并不意味着使用此布局模式是个好主意。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/12/24/golang/third-party-library/gopsutil/"/>
      <url>/2021/12/24/golang/third-party-library/gopsutil/</url>
      
        <content type="html"><![CDATA[<h1 id="gopsutil"><a href="#gopsutil" class="headerlink" title="gopsutil"></a>gopsutil</h1><h2 id="gopsutil-1"><a href="#gopsutil-1" class="headerlink" title="gopsutil"></a>gopsutil</h2><h2 id="环境配置"><a href="#环境配置" class="headerlink" title="环境配置"></a>环境配置</h2><pre class="line-numbers language-none"><code class="language-none">go get github.com/shirou/gopsutil<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>jenkinsx</title>
      <link href="/2021/12/10/jenkinsx/jenkinsx/"/>
      <url>/2021/12/10/jenkinsx/jenkinsx/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>hue</title>
      <link href="/2021/12/10/bigdata/hue/hue/"/>
      <url>/2021/12/10/bigdata/hue/hue/</url>
      
        <content type="html"><![CDATA[<h1 id="hue"><a href="#hue" class="headerlink" title="hue"></a>hue</h1><blockquote><p>hue官网：<a href="https://gethue.com/">https://gethue.com/</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>catacoda</title>
      <link href="/2021/12/10/catacoda/catacoda/"/>
      <url>/2021/12/10/catacoda/catacoda/</url>
      
        <content type="html"><![CDATA[<h1 id="catacoda"><a href="#catacoda" class="headerlink" title="catacoda"></a>catacoda</h1><blockquote><p>官网：<a href="https://www.katacoda.com/">https://www.katacoda.com/</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>circleci</title>
      <link href="/2021/12/10/circleci/circleci/"/>
      <url>/2021/12/10/circleci/circleci/</url>
      
        <content type="html"><![CDATA[<h1 id="circleci"><a href="#circleci" class="headerlink" title="circleci"></a>circleci</h1><blockquote><p>官网：<a href="https://circleci.com/">https://circleci.com/</a></p><p>github 地址：<a href="https://github.com/circleci">https://github.com/circleci</a></p><p>ords: <a href="https://circleci.com/developer/orbs">https://circleci.com/developer/orbs</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>rook</title>
      <link href="/2021/12/10/rook/rook/"/>
      <url>/2021/12/10/rook/rook/</url>
      
        <content type="html"><![CDATA[<h1 id="rook"><a href="#rook" class="headerlink" title="rook"></a>rook</h1><blockquote><p>rook官网：<a href="https://rook.io/">https://rook.io/</a></p><p>github地址：<a href="https://github.com/rook/rook">https://github.com/rook/rook</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>fluentd</title>
      <link href="/2021/12/10/fluentd/fluentd/"/>
      <url>/2021/12/10/fluentd/fluentd/</url>
      
        <content type="html"><![CDATA[<h1 id="fluentd"><a href="#fluentd" class="headerlink" title="fluentd"></a>fluentd</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>splunk</title>
      <link href="/2021/12/10/log-plateform/splunk/splunk/"/>
      <url>/2021/12/10/log-plateform/splunk/splunk/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>puppet</title>
      <link href="/2021/12/10/puppet/puppet/"/>
      <url>/2021/12/10/puppet/puppet/</url>
      
        <content type="html"><![CDATA[<h1 id="puppet"><a href="#puppet" class="headerlink" title="puppet"></a>puppet</h1><blockquote><p>puppet 官网：<a href="https://puppet.com/">https://puppet.com/</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>lua</title>
      <link href="/2021/12/10/lua/lua/"/>
      <url>/2021/12/10/lua/lua/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>grafana</title>
      <link href="/2021/12/10/monitor/prometheus/grafana/grafana/"/>
      <url>/2021/12/10/monitor/prometheus/grafana/grafana/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>coredns</title>
      <link href="/2021/12/10/coredns/coredns/"/>
      <url>/2021/12/10/coredns/coredns/</url>
      
        <content type="html"><![CDATA[<h1 id="coredns"><a href="#coredns" class="headerlink" title="coredns"></a>coredns</h1><blockquote><p>官网：<a href="https://coredns.io/">https://coredns.io/</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>consul</title>
      <link href="/2021/12/10/consul/consul/"/>
      <url>/2021/12/10/consul/consul/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>chef</title>
      <link href="/2021/12/10/chef/chef/"/>
      <url>/2021/12/10/chef/chef/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>clickhouse</title>
      <link href="/2021/12/10/database/clickhouse/clickhouse/"/>
      <url>/2021/12/10/database/clickhouse/clickhouse/</url>
      
        <content type="html"><![CDATA[<h1 id="clickhouse"><a href="#clickhouse" class="headerlink" title="clickhouse"></a>clickhouse</h1><blockquote><p>官网： <a href="https://clickhouse.com/">https://clickhouse.com/</a></p><p>github地址：<a href="https://github.com/ClickHouse/ClickHouse">https://github.com/ClickHouse/ClickHouse</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>leveldb整体架构</title>
      <link href="/2021/12/10/database/leveldb/leveldb-zheng-ti-jia-gou/"/>
      <url>/2021/12/10/database/leveldb/leveldb-zheng-ti-jia-gou/</url>
      
        <content type="html"><![CDATA[<h1 id="leveldb整体架构"><a href="#leveldb整体架构" class="headerlink" title="leveldb整体架构"></a>leveldb整体架构</h1><h2 id="架构图"><a href="#架构图" class="headerlink" title="架构图"></a>架构图</h2><h2 id="重要组件"><a href="#重要组件" class="headerlink" title="重要组件"></a>重要组件</h2><h3 id="memtable"><a href="#memtable" class="headerlink" title="memtable"></a>memtable</h3><p>leveldb的一次写入操作并不是直接将数据刷新到磁盘文件，而是首先写入到内存中作为代替，memtable就是一个在内存中进行数据组织与维护的结构。memtable中，所有的数据按<strong>用户定义的排序方法</strong>排序之后按序存储，等到其存储内容的容量达到阈值时（默认为4MB），便将其转换成一个<strong>不可修改</strong>的memtable，与此同时创建一个新的memtable，供用户继续进行读写操作。memtable底层使用了一种跳表数据结构，这种数据结构效率可以比拟二叉查找树，绝大多数操作的时间复杂度为O(log n)。</p><h3 id="immutable-memtable"><a href="#immutable-memtable" class="headerlink" title="immutable memtable"></a>immutable memtable</h3><p>memtable的容量到达阈值时，便会转换成一个不可修改的memtable，也称为immutable memtable。这两者的结构定义完全一样，区别只是immutable memtable是只读的。当一个immutable memtable被创建时，leveldb的后台压缩进程便会将利用其中的内容，创建一个sstable，持久化到磁盘文件中。</p><h3 id="log"><a href="#log" class="headerlink" title="log"></a>log</h3><p>leveldb的写操作并不是直接写入磁盘的，而是首先写入到内存。假设写入到内存的数据还未来得及持久化，leveldb进程发生了异常，抑或是宿主机器发生了宕机，会造成用户的写入发生丢失。因此leveldb在写内存之前会首先将所有的写操作写到日志文件中，也就是log文件。当以下异常情况发生时，均可以通过日志文件进行恢复：</p><ol><li>写log期间进程异常；</li><li>写log完成，写内存未完成；</li><li>write动作完成（即log、内存写入都完成）后，进程异常；</li><li>Immutable memtable持久化过程中进程异常；</li><li>其他压缩异常（较为复杂，首先不在这里介绍）；</li></ol><p>当第一类情况发生时，数据库重启读取log时，发现异常日志数据，抛弃该条日志数据，即视作这次用户写入失败，保障了数据库的一致性；</p><p>当第二类，第三类，第四类情况发生了，均可以通过redo日志文件中记录的写入操作完成数据库的恢复。</p><p>每次日志的写操作都是一次顺序写，因此写效率高，整体写入性能较好。</p><p>此外，leveldb的<strong>用户写操作的原子性</strong>同样通过日志来实现。</p><h3 id="sstable"><a href="#sstable" class="headerlink" title="sstable"></a>sstable</h3><p>虽然leveldb采用了先写内存的方式来提高写入效率，但是内存中数据不可能无限增长，且日志中记录的写入操作过多，会导致异常发生时，恢复时间过长。因此内存中的数据达到一定容量，就需要将数据持久化到磁盘中。除了某些元数据文件，leveldb的数据主要都是通过sstable来进行存储。</p><p>虽然在内存中，所有的数据都是按序排列的，但是当多个memetable数据持久化到磁盘后，对应的不同的sstable之间是存在交集的，在读操作时，需要对所有的sstable文件进行遍历，严重影响了读取效率。因此leveldb后台会“定期“整合这些sstable文件，该过程也称为compaction。随着compaction的进行，sstable文件在逻辑上被分成若干层，由内存数据直接dump出来的文件称为level 0层文件，后期整合而成的文件为level i 层文件，这也是leveldb这个名字的由来。</p><blockquote><p>注意，所有的sstable文件本身的内容是不可修改的，这种设计哲学为leveldb带来了许多优势，简化了很多设计。</p></blockquote><h3 id="manifest"><a href="#manifest" class="headerlink" title="manifest"></a>manifest</h3><p>leveldb中有个版本的概念，一个版本中主要记录了每一层中所有文件的元数据，元数据包括（1）文件大小（2）最大key值（3）最小key值。该版本信息十分关键，除了在查找数据时，利用维护的每个文件的最大／小key值来加快查找，还在其中维护了一些进行compaction的统计值，来控制compaction的进行。</p><p>以goleveldb为例，一个文件的元数据主要包括了最大最小key，文件大小等信息；</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token comment">// tFile holds basic information about a table.</span><span class="token keyword">type</span> tFile <span class="token keyword">struct</span> <span class="token punctuation">{</span>    fd         storage<span class="token punctuation">.</span>FileDesc    seekLeft   <span class="token builtin">int32</span>    size       <span class="token builtin">int64</span>    imin<span class="token punctuation">,</span> imax internalKey<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>一个版本信息主要维护了每一层所有文件的元数据。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">type</span> version <span class="token keyword">struct</span> <span class="token punctuation">{</span>    s <span class="token operator">*</span>session <span class="token comment">// session - version</span>    levels <span class="token punctuation">[</span><span class="token punctuation">]</span>tFiles <span class="token comment">// file meta</span>    <span class="token comment">// Level that should be compacted next and its compaction score.</span>    <span class="token comment">// Score &lt; 1 means compaction is not strictly needed. These fields</span>    <span class="token comment">// are initialized by computeCompaction()</span>    cLevel <span class="token builtin">int</span> <span class="token comment">// next level</span>    cScore <span class="token builtin">float64</span> <span class="token comment">// current score</span>    cSeek unsafe<span class="token punctuation">.</span>Pointer    closing  <span class="token builtin">bool</span>    ref      <span class="token builtin">int</span>    released <span class="token builtin">bool</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当每次<strong>compaction完成</strong>（或者换一种更容易理解的说法，当每次sstable文件有新增或者减少），leveldb都会创建一个新的version，创建的规则是:</p><p>versionNew = versionOld + versionEdit</p><p>versionEdit指代的是基于旧版本的基础上，变化的内容（例如新增或删除了某些sstable文件）。</p><p><strong>manifest文件就是用来记录这些versionEdit信息的</strong>。一个versionEdit数据，会被编码成一条记录，写入manifest文件中。例如下图便是一个manifest文件的示意图，其中包含了3条versionEdit记录，每条记录包括（1）新增哪些sst文件（2）删除哪些sst文件（3）当前compaction的下标（4）日志文件编号（5）操作seqNumber等信息。通过这些信息，leveldb便可以在启动时，基于一个空的version，不断apply这些记录，最终得到一个上次运行结束时的版本信息。</p><h3 id="current"><a href="#current" class="headerlink" title="current"></a>current</h3><p>这个文件的内容只有一个信息，就是记载当前的manifest文件名。</p><p>因为每次leveldb启动时，都会创建一个新的Manifest文件。因此数据目录可能会存在多个Manifest文件。Current则用来指出哪个Manifest文件才是我们关心的那个Manifest文件。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>leveldb读写操作</title>
      <link href="/2021/12/10/database/leveldb/leveldb-du-xie-cao-zuo/"/>
      <url>/2021/12/10/database/leveldb/leveldb-du-xie-cao-zuo/</url>
      
        <content type="html"><![CDATA[<h1 id="leveldb读写操作"><a href="#leveldb读写操作" class="headerlink" title="leveldb读写操作"></a>leveldb读写操作</h1><h2 id="写操作"><a href="#写操作" class="headerlink" title="写操作"></a>写操作</h2><p>leveldb的一次写入分为两部分：</p><ol><li>将写操作写入日志；</li><li>将写操作应用到内存数据库中；</li></ol><blockquote><p>其实leveldb仍然存在写入丢失的隐患。在写设置为非同步的情况下，在写完日志文件以后，操作系统并不是直接将这些数据真正落到磁盘中，而是暂时留在操作系统缓存中，因此当用户写入操作完成，操作系统还未来得及落盘的情况下，发生系统宕机，就会造成写丢失；但是若只是进程异常退出，则不存在该问题。</p></blockquote><h3 id="写类型"><a href="#写类型" class="headerlink" title="写类型"></a>写类型</h3><p>leveldb对外提供的写入接口有：（1）Put（2）Delete两种。这两种本质对应同一种操作，Delete操作同样会被转换成一个value为空的Put操作。</p><p>除此以外，leveldb还提供了一个批量处理的工具Batch，用户可以依据Batch来完成批量的数据库更新操作，且这些操作是原子性的。</p><h3 id="batch结构"><a href="#batch结构" class="headerlink" title="batch结构"></a>batch结构</h3><p>无论是Put/Del操作，还是批量操作，底层都会为这些操作创建一个batch实例作为一个数据库操作的最小执行单元。因此首先介绍一下batch的组织结构。</p><p>在batch中，每一条数据项都按照上图格式进行编码。每条数据项编码后的第一位是这条数据项的类型（更新还是删除），之后是数据项key的长度，数据项key的内容；若该数据项不是删除操作，则再加上value的长度，value的内容。</p><p>batch中会维护一个size值，用于表示其中包含的数据量的大小。该size值为所有数据项key与value长度的累加，以及每条数据项额外的8个字节。这8个字节用于存储一条数据项额外的一些信息。</p><h3 id="key值编码"><a href="#key值编码" class="headerlink" title="key值编码"></a>key值编码</h3><p>当数据项从batch中写入到内存数据库中时，需要将一个key值的转换，即在leveldb内部，所有数据项的key是经过特殊编码的，这种格式称为internalKey。</p><p>internalkey在用户key的基础上，尾部追加了8个字节，用于存储（1）该操作对应的sequence number（2）该操作的类型。</p><p>其中，每一个操作都会被赋予一个sequence number。该计时器是在leveldb内部维护，每进行一次操作就做一个累加。由于在leveldb中，一次更新或者一次删除，采用的是append的方式，并非直接更新原数据。因此对应同样一个key，会有多个版本的数据记录，而最大的sequence number对应的数据记录就是最新的。</p><p>此外，leveldb的快照（snapshot）也是基于这个sequence number实现的，即每一个sequence number代表着数据库的一个版本。</p><h3 id="合并写"><a href="#合并写" class="headerlink" title="合并写"></a>合并写</h3><p>leveldb中，在面对并发写入时，做了一个处理的优化。在同一个时刻，只允许一个写入操作将内容写入到日志文件以及内存数据库中。为了在写入进程较多的情况下，减少日志文件的小写入，增加整体的写入性能，leveldb将一些“小写入”合并成一个“大写入”。</p><p>流程如下：</p><p><strong>第一个获取到写锁的写操作</strong></p><ul><li>第一个写入操作获取到写入锁；</li><li>在当前写操作的数据量未超过合并上限，且有其他写操作pending的情况下，将其他写操作的内容合并到自身；</li><li>若本次写操作的数据量超过上限，或者无其他pending的写操作了，将所有内容统一写入日志文件，并写入到内存数据库中；</li><li>通知每一个被合并的写操作最终的写入结果，释放或移交写锁；</li></ul><p><strong>其他写操作</strong>：</p><ul><li>等待获取写锁或者被合并；</li><li>若被合并，判断是否合并成功，若成功，则等待最终写入结果；反之，则表明获取锁的写操作已经oversize了，此时，该操作直接从上个占有锁的写操作中接过写锁进行写入；</li><li>若未被合并，则继续等待写锁或者等待被合并；</li></ul><h3 id="原子性"><a href="#原子性" class="headerlink" title="原子性"></a>原子性</h3><p>leveldb的任意一个写操作（无论包含了多少次写），其原子性都是由日志文件实现的。一个写操作中所有的内容会以一个日志中的一条记录，作为最小单位写入。</p><p>考虑以下两种异常情况：</p><ol><li>写日志未开始，或写日志完成一半，进程异常退出；</li><li>写日志完成，进程异常退出；</li></ol><p>前者中可能存储一个写操作的部分写已经被记载到日志文件中，仍然有部分写未被记录，这种情况下，当数据库重新启动恢复时，读到这条日志记录时，发现数据异常，直接丢弃或退出，实现了写入的原子性保障。</p><p>后者，写日志已经完成，写入日志的数据未真正持久化，数据库启动恢复时通过redo日志实现数据写入，仍然保障了原子性。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>influxdb</title>
      <link href="/2021/12/10/database/influxdb/influxdb/"/>
      <url>/2021/12/10/database/influxdb/influxdb/</url>
      
        <content type="html"><![CDATA[<h1 id="influxdb"><a href="#influxdb" class="headerlink" title="influxdb"></a>influxdb</h1><blockquote><p>&nbsp;官网：<a href="https://www.influxdata.com/">https://www.influxdata.com/</a></p><p>github 地址：<a href="https://github.com/influxdata/influxdb">https://github.com/influxdata/influxdb</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>leveldb环境配置</title>
      <link href="/2021/12/10/database/leveldb/leveldb-huan-jing-pei-zhi/"/>
      <url>/2021/12/10/database/leveldb/leveldb-huan-jing-pei-zhi/</url>
      
        <content type="html"><![CDATA[<h1 id="leveldb环境配置"><a href="#leveldb环境配置" class="headerlink" title="leveldb环境配置"></a>leveldb环境配置</h1><h2 id="leveldb源码安装"><a href="#leveldb源码安装" class="headerlink" title="leveldb源码安装"></a>leveldb源码安装</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">git</span> clone --recurse-submodules https://github.com/google/leveldb.git<span class="token builtin class-name">cd</span> leveldb <span class="token operator">&amp;&amp;</span> <span class="token function">mkdir</span> -p build <span class="token operator">&amp;&amp;</span> <span class="token builtin class-name">cd</span> buildcmake -DCMAKE_BUILD_TYPE<span class="token operator">=</span>Release <span class="token punctuation">..</span> <span class="token operator">&amp;&amp;</span> cmake --build <span class="token builtin class-name">.</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>leveldb</title>
      <link href="/2021/12/10/database/leveldb/leveldb/"/>
      <url>/2021/12/10/database/leveldb/leveldb/</url>
      
        <content type="html"><![CDATA[<h1 id="leveldb"><a href="#leveldb" class="headerlink" title="leveldb"></a>leveldb</h1><blockquote><p>leveled github 地址：<a href="https://github.com/google/leveldb">https://github.com/google/leveldb</a></p><p>中文文档：<a href="https://leveldb-handbook.readthedocs.io/zh/latest/basic.html">https://leveldb-handbook.readthedocs.io/zh/latest/basic.html</a></p></blockquote><h2 id="leveldb简介"><a href="#leveldb简介" class="headerlink" title="leveldb简介"></a>leveldb简介</h2><p>leveldb是一个写性能十分优秀的存储引擎，是典型的LSM树(Log Structured-Merge Tree)实现。LSM树的核心思想就是放弃部分读的性能，换取最大的写入能力。</p><p>LSM树写性能极高的原理，简单地来说就是尽量减少随机写的次数。对于每次写入操作，并不是直接将最新的数据驻留在磁盘中，而是将其拆分成（1）一次日志文件的顺序写（2）一次内存中的数据插入。leveldb正是实践了这种思想，将数据首先更新在内存中，当内存中的数据达到一定的阈值，将这部分数据真正刷新到磁盘文件中，因而获得了极高的写性能（顺序写60MB/s, 随机写45MB/s）。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>dockerfile指令</title>
      <link href="/2021/12/10/docker/dockerfile/dockerfile-zhi-ling/"/>
      <url>/2021/12/10/docker/dockerfile/dockerfile-zhi-ling/</url>
      
        <content type="html"><![CDATA[<h1 id="dockerfile指令"><a href="#dockerfile指令" class="headerlink" title="dockerfile指令"></a>dockerfile指令</h1><h2 id="FROM-指定基础镜像"><a href="#FROM-指定基础镜像" class="headerlink" title="FROM(指定基础镜像)"></a>FROM(指定基础镜像)</h2><p>基础镜像是必须指定的，而 <code>FROM</code> 就是指定基础镜像。</p><pre class="line-numbers language-none"><code class="language-none"><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>除了选择现有镜像为基础镜像外，Docker 还存在一个特殊的镜像，名为 <code>scratch</code>。这个镜像是虚拟的概念，并不实际存在，它表示一个空白的镜像。</p><p>不以任何系统为基础，直接将可执行文件复制进镜像的做法并不罕见，对于 Linux 下静态编译的程序来说，并不需要有操作系统提供运行时支持，所需的一切库都已经在可执行文件里了，因此直接 <code>FROM scratch</code> 会让镜像体积更加小巧。使用Go语言开发的应用很多会使用这种方式来制作镜像</p><h2 id="RUN-执行命令行命令"><a href="#RUN-执行命令行命令" class="headerlink" title="RUN(执行命令行命令)"></a>RUN(执行命令行命令)</h2><p><code>RUN</code> 指令是用来执行命令行命令的。其格式有两种：</p><ul><li><p><strong><em>shell</em> 格式</strong>：<code>RUN &lt;命令&gt;</code>，就像直接在命令行中输入的命令一样。刚才写的 Dockerfile 中的 <code>RUN</code> 指令就是这种格式。</p></li><li><p><strong><em>exec</em> 格式</strong>：<code>RUN ["可执行文件", "参数1", "参数2"]</code>，这更像是函数调用中的格式。</p></li></ul><p>每一个 <code>RUN</code> 都是启动一个容器、执行命令、然后提交存储层文件变更。</p><h2 id="COPY-复制文件"><a href="#COPY-复制文件" class="headerlink" title="COPY(复制文件)"></a>COPY(复制文件)</h2><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile"># 格式COPY [--chown=&lt;user&gt;:&lt;group&gt;] &lt;源路径&gt;... &lt;目标路径&gt;COPY [--chown=&lt;user&gt;:&lt;group&gt;] ["&lt;源路径1&gt;",... "&lt;目标路径&gt;"]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><code>COPY</code> 指令将从构建上下文目录中 <code>&lt;源路径&gt;</code> 的文件/目录复制到新的一层的镜像内的 <code>&lt;目标路径&gt;</code> 位置。</p><p><code>&lt;源路径&gt;</code> 可以是多个，甚至可以是通配符，其通配符规则要满足 Go 的 <a href="https://golang.org/pkg/path/filepath/#Match"><code>filepath.Match</code></a> 规则</p><p><code>&lt;目标路径&gt;</code> 可以是容器内的绝对路径，也可以是相对于工作目录的相对路径（工作目录可以用 <code>WORKDIR</code> 指令来指定）。目标路径不需要事先创建，如果目录不存在会在复制文件前先行创建缺失目录。</p><p>使用 <code>COPY</code> 指令，源文件的各种元数据都会保留。比如读、写、执行权限、文件变更时间等。这个特性对于镜像定制很有用。特别是构建相关文件都在使用 Git 进行管理的时候。</p><p>可以加上 <code>--chown=&lt;user&gt;:&lt;group&gt;</code> 选项来改变文件的所属用户及所属组。</p><p>如果源路径为文件夹，复制的时候不是直接复制该文件夹，而是将文件夹中的内容复制到目标路径</p><h2 id="ADD-高级的复制文件"><a href="#ADD-高级的复制文件" class="headerlink" title="ADD(高级的复制文件)"></a>ADD(高级的复制文件)</h2><p><code>ADD</code> 指令和 <code>COPY</code> 的格式和性质基本一致。但是在 <code>COPY</code> 基础上增加了一些功能。</p><p>比如 <code>&lt;源路径&gt;</code> 可以是一个 <code>URL</code>，这种情况下，Docker 引擎会试图去下载这个链接的文件放到 <code>&lt;目标路径&gt;</code> 去。下载后的文件权限自动设置为 <code>600</code>，如果这并不是想要的权限，那么还需要增加额外的一层 <code>RUN</code> 进行权限调整，另外，如果下载的是个压缩包，需要解压缩，也一样还需要额外的一层 <code>RUN</code> 指令进行解压缩。所以不如直接使用 <code>RUN</code> 指令，然后使用 <code>wget</code> 或者 <code>curl</code> 工具下载，处理权限、解压缩、然后清理无用文件更合理。因此，这个功能其实并不实用，而且不推荐使用。</p><p>如果 <code>&lt;源路径&gt;</code> 为一个 <code>tar</code> 压缩文件的话，压缩格式为 <code>gzip</code>, <code>bzip2</code> 以及 <code>xz</code> 的情况下，<code>ADD</code> 指令将会自动解压缩这个压缩文件到 <code>&lt;目标路径&gt;</code> 去。</p><p>复制个压缩文件进去，而不解压缩，这时就不可以使用 <code>ADD</code> 命令了。</p><p><code>ADD</code> 指令会令镜像构建缓存失效，从而可能会令镜像构建变得比较缓慢。</p><p>因此在 <code>COPY</code> 和 <code>ADD</code> 指令中选择的时候，可以遵循这样的原则，所有的文件复制均使用 <code>COPY</code> 指令，仅在需要自动解压缩的场合使用 <code>ADD</code>。</p><p>在使用该指令的时候还可以加上 <code>--chown=&lt;user&gt;:&lt;group&gt;</code> 选项来改变文件的所属用户及所属组。</p><h2 id="CMD-容器启动命令"><a href="#CMD-容器启动命令" class="headerlink" title="CMD(容器启动命令)"></a>CMD(容器启动命令)</h2><ul><li><strong>shell格式</strong>：<code>CMD &lt;命令&gt;</code></li><li><strong>exec格式</strong>：<code>CMD ["可执行文件", "参数1", "参数2"...]</code></li><li><strong>参数列表格式</strong>：<code>CMD ["参数1", "参数2"...]</code>。在指定了 <code>ENTRYPOINT</code> 指令后，用 <code>CMD</code> 指定具体的参数。</li></ul><p>Docker 不是虚拟机，容器就是进程。既然是进程，那么在启动容器的时候，需要指定所运行的程序及参数。<code>CMD</code> 指令就是用于指定默认的容器主进程的启动命令的。</p><p>在运行时可以指定新的命令来替代镜像设置中的这个默认命令</p><p>在指令格式上，一般推荐使用 <code>exec</code> 格式，这类格式在解析时会被解析为 JSON 数组，因此一定要使用双引号 <code>"</code>，而不要使用单引号。</p><p>如果使用 <code>shell</code> 格式的话，实际的命令会被包装为 <code>sh -c</code> 的参数的形式进行执行。比如：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">CMD <span class="token builtin class-name">echo</span> <span class="token environment constant">$HOME</span><span class="token comment"># 在实际执行中，会将其变更为</span>CMD <span class="token punctuation">[</span> <span class="token string">"sh"</span>, <span class="token string">"-c"</span>, <span class="token string">"echo <span class="token environment constant">$HOME</span>"</span> <span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>这就是为什么我们可以使用环境变量的原因，因为这些环境变量会被 shell 进行解析处理。</p><p>提到 <code>CMD</code> 就不得不提容器中应用在前台执行和后台执行的问题。</p><p>Docker 不是虚拟机，容器中的应用都应该以前台执行，而不是像虚拟机、物理机里面那样，用 <code>systemd</code> 去启动后台服务，容器内没有后台服务的概念。</p><p>对于容器而言，其启动程序就是容器应用进程，容器就是为了主进程而存在的，主进程退出，容器就失去了存在的意义，从而退出，其它辅助进程不是它需要关心的东西。</p><p>而使用 <code>service nginx start</code> 命令，则是希望 upstart 来以后台守护进程形式启动 <code>nginx</code> 服务。而刚才说了 <code>CMD service nginx start</code> 会被理解为 <code>CMD [ "sh", "-c", "service nginx start"]</code>，因此主进程实际上是 <code>sh</code>。那么当 <code>service nginx start</code> 命令结束后，<code>sh</code> 也就结束了，<code>sh</code> 作为主进程退出了，自然就会令容器退出。</p><p>正确的做法是直接执行 <code>nginx</code> 可执行文件，并且要求以前台形式运行。比如：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">CMD <span class="token punctuation">[</span><span class="token string">"nginx"</span>, <span class="token string">"-g"</span>, <span class="token string">"daemon off;"</span><span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="ENTRYPOINT-入口点"><a href="#ENTRYPOINT-入口点" class="headerlink" title="ENTRYPOINT(入口点)"></a>ENTRYPOINT(入口点)</h2><p><code>ENTRYPOINT</code> 在运行时也可以替代，需要通过 <code>docker run</code> 的参数 <code>--entrypoint</code> 来指定。当指定了 <code>ENTRYPOINT</code> 后，<code>CMD</code> 的含义就发生了改变，不再是直接的运行其命令，而是将 <code>CMD</code> 的内容作为参数传给 <code>ENTRYPOINT</code> 指令，换句话说实际执行时，将变为：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token operator">&lt;</span>ENTRYPOINT<span class="token operator">&gt;</span> <span class="token string">"&lt;CMD&gt;"</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="场景一：让镜像变成像命令一样使用"><a href="#场景一：让镜像变成像命令一样使用" class="headerlink" title="场景一：让镜像变成像命令一样使用"></a>场景一：让镜像变成像命令一样使用</h3><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile">FROM ubuntu:18.04RUN apt-get update \    &amp;&amp; apt-get install -y curl \    &amp;&amp; rm -rf /var/lib/apt/lists/*ENTRYPOINT [ "curl", "-s", "http://myip.ipip.net" ]# docker run myip# 当存在 `ENTRYPOINT` 后，`CMD` 的内容将会作为参数传给 `ENTRYPOINT`，而这里 `-i` 就是新的 `CMD`# docker run myip -i<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h1 id="场景二：应用运行前的准备工作"><a href="#场景二：应用运行前的准备工作" class="headerlink" title="场景二：应用运行前的准备工作"></a>场景二：应用运行前的准备工作</h1><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile">FROM alpine:3.4...RUN addgroup -S redis &amp;&amp; adduser -S -G redis redis...ENTRYPOINT ["docker-entrypoint.sh"]EXPOSE 6379CMD [ "redis-server" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>可以看到其中为了 redis 服务创建了 redis 用户，并在最后指定了 <code>ENTRYPOINT</code> 为 <code>docker-entrypoint.sh</code> 脚本。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token shebang important">#!/bin/sh</span><span class="token punctuation">..</span>.<span class="token comment"># allow the container to be started with `--user`</span><span class="token keyword">if</span> <span class="token punctuation">[</span> <span class="token string">"<span class="token variable">$1</span>"</span> <span class="token operator">=</span> <span class="token string">'redis-server'</span> -a <span class="token string">"<span class="token variable"><span class="token variable">$(</span><span class="token function">id</span> -u<span class="token variable">)</span></span>"</span> <span class="token operator">=</span> <span class="token string">'0'</span> <span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>    <span class="token function">find</span> <span class="token builtin class-name">.</span> <span class="token punctuation">\</span><span class="token operator">!</span> -user redis -exec <span class="token function">chown</span> redis <span class="token string">'{}'</span> +    <span class="token builtin class-name">exec</span> gosu redis <span class="token string">"<span class="token variable">$0</span>"</span> <span class="token string">"<span class="token variable">$@</span>"</span><span class="token keyword">fi</span><span class="token builtin class-name">exec</span> <span class="token string">"<span class="token variable">$@</span>"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>该脚本的内容就是根据 <code>CMD</code> 的内容来判断，如果是 <code>redis-server</code> 的话，则切换到 <code>redis</code> 用户身份启动服务器，否则依旧使用 <code>root</code> 身份执行。比如：</p><pre class="line-numbers language-none"><code class="language-none">$ docker run -it redis iduid=0(root) gid=0(root) groups=0(root)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h2 id="ENV-设置环境变量"><a href="#ENV-设置环境变量" class="headerlink" title="ENV(设置环境变量)"></a>ENV(设置环境变量)</h2><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile"># 格式ENV &lt;key&gt; &lt;value&gt;ENV &lt;key1&gt;=&lt;value1&gt; &lt;key2&gt;=&lt;value2&gt;...# sample# 这个示例包含如何换行，如何包含空格的值，如何在Dockerfile使用环境变量ENV VERSION=1.0 DEBUG=on \    NAME="Happy Feet"RUN echo $VERSION<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>下列指令可以支持环境变量展开： <code>ADD</code>、<code>COPY</code>、<code>ENV</code>、<code>EXPOSE</code>、<code>FROM</code>、<code>LABEL</code>、<code>USER</code>、<code>WORKDIR</code>、<code>VOLUME</code>、<code>STOPSIGNAL</code>、<code>ONBUILD</code>、<code>RUN</code>。</p><h2 id="ARG-构建参数"><a href="#ARG-构建参数" class="headerlink" title="ARG(构建参数)"></a>ARG(构建参数)</h2><p>格式：<code>ARG &lt;参数名&gt;[=&lt;默认值&gt;]</code></p><p>构建参数和 <code>ENV</code> 的效果一样，都是设置环境变量。所不同的是，<code>ARG</code> 所设置的构建环境的环境变量，在将来容器运行时是不会存在这些环境变量的。但是不要因此就使用 <code>ARG</code> 保存密码之类的信息，因为 <code>docker history</code> 还是可以看到所有值的。</p><p><code>Dockerfile</code> 中的 <code>ARG</code> 指令是定义参数名称，以及定义其默认值。该默认值可以在构建命令 <code>docker build</code> 中用 <code>--build-arg &lt;参数名&gt;=&lt;值&gt;</code> 来覆盖。</p><p>ARG 指令有生效范围，如果在 <code>FROM</code> 指令之前指定，那么只能用于 <code>FROM</code> 指令中。</p><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile"># ARG指令有效范围## 如果在 `FROM` 指令之前指定，那么只能用于 `FROM` 指令中ARG DOCKER_USERNAME=libraryFROM ${DOCKER_USERNAME}/alpine## 这里检测不到ARG，需要在这里重新指定RUN set -x ; echo ${DOCKER_USERNAME}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile"># 这个变量在每个 FROM 中都生效ARG DOCKER_USERNAME=libraryFROM ${DOCKER_USERNAME}/alpineRUN set -x ; echo 1FROM ${DOCKER_USERNAME}/alpineRUN set -x ; echo 2# 对于在各个阶段中使用的变量都必须在每个阶段分别指定ARG DOCKER_USERNAME=libraryFROM ${DOCKER_USERNAME}/alpine# 在FROM 之后使用变量，必须在每个阶段分别指定ARG DOCKER_USERNAME=libraryRUN set -x ; echo ${DOCKER_USERNAME}FROM ${DOCKER_USERNAME}/alpine# 在FROM 之后使用变量，必须在每个阶段分别指定ARG DOCKER_USERNAME=libraryRUN set -x ; echo ${DOCKER_USERNAME}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="VOLUME-定义匿名卷"><a href="#VOLUME-定义匿名卷" class="headerlink" title="VOLUME(定义匿名卷)"></a>VOLUME(定义匿名卷)</h2><p>格式为：</p><ul><li><code>VOLUME ["&lt;路径1&gt;", "&lt;路径2&gt;"...]</code></li><li><code>VOLUME &lt;路径&gt;</code></li></ul><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile"># `/data` 目录就会在容器运行时自动挂载为匿名卷，任何向 `/data` 中写入的信息都不会记录进容器存储层，从而保证了容器存储层的无状态化。VOLUME /data$ docker run -d -v mydata:/data xxxx# 在这行命令中，就使用了 `mydata` 这个命名卷挂载到了 `/data` 这个位置，替代了 `Dockerfile` 中定义的匿名卷的挂载配置。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h2 id="EXPOSE-暴露端口"><a href="#EXPOSE-暴露端口" class="headerlink" title="EXPOSE(暴露端口)"></a>EXPOSE(暴露端口)</h2><p>格式为 <code>EXPOSE &lt;端口1&gt; [&lt;端口2&gt;...]</code>。</p><p><code>EXPOSE</code> 指令是声明容器运行时提供服务的端口，这只是一个声明，在容器运行时并不会因为这个声明应用就会开启这个端口的服务。</p><p>在 Dockerfile 中写入这样的声明有两个好处，一个是帮助镜像使用者理解这个镜像服务的守护端口，以方便配置映射；另一个用处则是在运行时使用随机端口映射时，也就是 <code>docker run -P</code> 时，会自动随机映射 <code>EXPOSE</code> 的端口。</p><p>要将 <code>EXPOSE</code> 和在运行时使用 <code>-p &lt;宿主端口&gt;:&lt;容器端口&gt;</code> 区分开来。<code>-p</code>，是映射宿主端口和容器端口，换句话说，就是将容器的对应端口服务公开给外界访问，而 <code>EXPOSE</code> 仅仅是声明容器打算使用什么端口而已，并不会自动在宿主进行端口映射。</p><h2 id="WORKDIR-指定工作目录"><a href="#WORKDIR-指定工作目录" class="headerlink" title="WORKDIR(指定工作目录)"></a>WORKDIR(指定工作目录)</h2><p>格式为 <code>WORKDIR &lt;工作目录路径&gt;</code>。</p><p>使用 <code>WORKDIR</code> 指令可以来指定工作目录（或者称为当前目录），以后各层的当前目录就被改为指定的目录，如该目录不存在，<code>WORKDIR</code> 会帮你建立目录。</p><pre class="line-numbers language-none"><code class="language-none"># 错误做法RUN cd /appRUN echo "hello" &gt; world.txt# 如果需要改变以后各层的工作目录的位置WORKDIR /appRUN echo "hello" &gt; world.txt<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>如果将这个 <code>Dockerfile</code> 进行构建镜像运行后，会发现找不到 <code>/app/world.txt</code> 文件，或者其内容不是 <code>hello</code>。原因其实很简单，在 Shell 中，连续两行是同一个进程执行环境，因此前一个命令修改的内存状态，会直接影响后一个命令；而在 <code>Dockerfile</code> 中，这两行 <code>RUN</code> 命令的执行环境根本不同，是两个完全不同的容器。</p><p>之前说过每一个 <code>RUN</code> 都是启动一个容器、执行命令、然后提交存储层文件变更。第一层 <code>RUN cd /app</code> 的执行仅仅是当前进程的工作目录变更，一个内存上的变化而已，其结果不会造成任何文件变更。而到第二层的时候，启动的是一个全新的容器，跟第一层的容器更完全没关系，自然不可能继承前一层构建过程中的内存变化。</p><pre class="line-numbers language-none"><code class="language-none"># 如果你的 `WORKDIR` 指令使用的相对路径，那么所切换的路径与之前的 `WORKDIR` 有关WORKDIR /aWORKDIR bWORKDIR cRUN pwd# `RUN pwd` 的工作目录为 `/a/b/c`<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="USER-指定当前用户"><a href="#USER-指定当前用户" class="headerlink" title="USER(指定当前用户)"></a>USER(指定当前用户)</h2><p>格式：<code>USER &lt;用户名&gt;[:&lt;用户组&gt;]</code></p><p><code>USER</code> 指令和 <code>WORKDIR</code> 相似，都是改变环境状态并影响以后的层。<code>WORKDIR</code> 是改变工作目录，<code>USER</code> 则是改变之后层的执行 <code>RUN</code>, <code>CMD</code> 以及 <code>ENTRYPOINT</code> 这类命令的身份。</p><blockquote><p><strong>注意</strong><code>USER</code> 只是帮助你切换到指定用户而已，这个用户必须是事先建立好的，否则无法切换。</p></blockquote><pre class="line-numbers language-none"><code class="language-none">RUN groupadd -r redis &amp;&amp; useradd -r -g redis redisUSER redisRUN [ "redis-server" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>如果以 <code>root</code> 执行的脚本，在执行期间希望改变身份，比如希望以某个已经建立好的用户来运行某个服务进程，不要使用 <code>su</code> 或者 <code>sudo</code>，这些都需要比较麻烦的配置，而且在 TTY 缺失的环境下经常出错。建议使用 <a href="https://github.com/tianon/gosu"><code>gosu</code></a>。</p><pre class="line-numbers language-none"><code class="language-none"># 建立 redis 用户，并使用 gosu 换另一个用户执行命令RUN groupadd -r redis &amp;&amp; useradd -r -g redis redis# 下载 gosuRUN wget -O /usr/local/bin/gosu "https://github.com/tianon/gosu/releases/download/1.12/gosu-amd64" \    &amp;&amp; chmod +x /usr/local/bin/gosu \    &amp;&amp; gosu nobody true# 设置 CMD，并以另外的用户执行CMD [ "exec", "gosu", "redis", "redis-server" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="HEALTHCHECK-健康检查"><a href="#HEALTHCHECK-健康检查" class="headerlink" title="HEALTHCHECK(健康检查)"></a>HEALTHCHECK(健康检查)</h2><p>格式：</p><ul><li><code>HEALTHCHECK [选项] CMD &lt;命令&gt;</code>：设置检查容器健康状况的命令</li><li><code>HEALTHCHECK NONE</code>：如果基础镜像有健康检查指令，使用这行可以屏蔽掉其健康检查指令</li></ul><p><code>HEALTHCHECK</code> 指令是告诉 Docker 应该如何进行判断容器的状态是否正常，这是 Docker 1.12 引入的新指令。</p><p>在没有 <code>HEALTHCHECK</code> 指令前，Docker 引擎只可以通过容器内主进程是否退出来判断容器是否状态异常。很多情况下这没问题，但是如果程序进入死锁状态，或者死循环状态，应用进程并不退出，但是该容器已经无法提供服务了。在 1.12 以前，Docker 不会检测到容器的这种状态，从而不会重新调度，导致可能会有部分容器已经无法提供服务了却还在接受用户请求。</p><p>而自 1.12 之后，Docker 提供了 <code>HEALTHCHECK</code> 指令，通过该指令指定一行命令，用这行命令来判断容器主进程的服务状态是否还正常，从而比较真实的反应容器实际状态。</p><p>当在一个镜像指定了 <code>HEALTHCHECK</code> 指令后，用其启动容器，初始状态会为 <code>starting</code>，在 <code>HEALTHCHECK</code> 指令检查成功后变为 <code>healthy</code>，如果连续一定次数失败，则会变为 <code>unhealthy</code>。</p><p><code>HEALTHCHECK</code> 支持下列选项：</p><ul><li><code>--interval=&lt;间隔&gt;</code>：两次健康检查的间隔，默认为 30 秒；</li><li><code>--timeout=&lt;时长&gt;</code>：健康检查命令运行超时时间，如果超过这个时间，本次健康检查就被视为失败，默认 30 秒；</li><li><code>--retries=&lt;次数&gt;</code>：当连续失败指定次数后，则将容器状态视为 <code>unhealthy</code>，默认 3 次。</li></ul><p>和 <code>CMD</code>, <code>ENTRYPOINT</code> 一样，<code>HEALTHCHECK</code> 只可以出现一次，如果写了多个，只有最后一个生效。</p><p>在 <code>HEALTHCHECK [选项] CMD</code> 后面的命令，格式和 <code>ENTRYPOINT</code> 一样，分为 <code>shell</code> 格式，和 <code>exec</code> 格式。命令的返回值决定了该次健康检查的成功与否：<code>0</code>：成功；<code>1</code>：失败；<code>2</code>：保留，不要使用这个值。</p><p>假设我们有个镜像是个最简单的 Web 服务，我们希望增加健康检查来判断其 Web 服务是否在正常工作，我们可以用 <code>curl</code> 来帮助判断，其 <code>Dockerfile</code> 的 <code>HEALTHCHECK</code> 可以这么写：</p><pre class="line-numbers language-none"><code class="language-none">FROM nginxRUN apt-get update &amp;&amp; apt-get install -y curl &amp;&amp; rm -rf /var/lib/apt/lists/*HEALTHCHECK --interval=5s --timeout=3s \  CMD curl -fs http://localhost/ || exit 1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>这里我们设置了每 5 秒检查一次（这里为了试验所以间隔非常短，实际应该相对较长），如果健康检查命令超过 3 秒没响应就视为失败，并且使用 <code>curl -fs http://localhost/ || exit 1</code> 作为健康检查命令。</p><p>使用 <code>docker build</code> 来构建这个镜像：</p><pre class="line-numbers language-none"><code class="language-none">$ docker build -t myweb:v1 .<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>构建好了后，我们启动一个容器：</p><pre class="line-numbers language-none"><code class="language-none">$ docker run -d --name web -p 80:80 myweb:v1<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>当运行该镜像后，可以通过 <code>docker container ls</code> 看到最初的状态为 <code>(health: starting)</code>：</p><pre class="line-numbers language-none"><code class="language-none">$ docker container lsCONTAINER ID        IMAGE               COMMAND                  CREATED             STATUS                            PORTS               NAMES03e28eb00bd0        myweb:v1            "nginx -g 'daemon off"   3 seconds ago       Up 2 seconds (health: starting)   80/tcp, 443/tcp     web<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>在等待几秒钟后，再次 <code>docker container ls</code>，就会看到健康状态变化为了 <code>(healthy)</code>：</p><pre class="line-numbers language-none"><code class="language-none">$ docker container lsCONTAINER ID        IMAGE               COMMAND                  CREATED             STATUS                    PORTS               NAMES03e28eb00bd0        myweb:v1            "nginx -g 'daemon off"   18 seconds ago      Up 16 seconds (healthy)   80/tcp, 443/tcp     web<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>如果健康检查连续失败超过了重试次数，状态就会变为 <code>(unhealthy)</code>。</p><p>为了帮助排障，健康检查命令的输出（包括 <code>stdout</code> 以及 <code>stderr</code>）都会被存储于健康状态里，可以用 <code>docker inspect</code> 来查看。</p><pre class="line-numbers language-none"><code class="language-none">$ docker inspect --format '{{json .State.Health}}' web | python -m json.tool{    "FailingStreak": 0,    "Log": [        {            "End": "2016-11-25T14:35:37.940957051Z",            "ExitCode": 0,            "Output": "&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;\n&lt;title&gt;Welcome to nginx!&lt;/title&gt;\n&lt;style&gt;\n    body {\n        width: 35em;\n        margin: 0 auto;\n        font-family: Tahoma, Verdana, Arial, sans-serif;\n    }\n&lt;/style&gt;\n&lt;/head&gt;\n&lt;body&gt;\n&lt;h1&gt;Welcome to nginx!&lt;/h1&gt;\n&lt;p&gt;If you see this page, the nginx web server is successfully installed and\nworking. Further configuration is required.&lt;/p&gt;\n\n&lt;p&gt;For online documentation and support please refer to\n&lt;a href=\"http://nginx.org/\"&gt;nginx.org&lt;/a&gt;.&lt;br/&gt;\nCommercial support is available at\n&lt;a href=\"http://nginx.com/\"&gt;nginx.com&lt;/a&gt;.&lt;/p&gt;\n\n&lt;p&gt;&lt;em&gt;Thank you for using nginx.&lt;/em&gt;&lt;/p&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n",            "Start": "2016-11-25T14:35:37.780192565Z"        }    ],    "Status": "healthy"}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="ONBUILD"><a href="#ONBUILD" class="headerlink" title="ONBUILD"></a>ONBUILD</h2><p>格式：<code>ONBUILD &lt;其它指令&gt;</code>。</p><p><code>ONBUILD</code> 是一个特殊的指令，它后面跟的是其它指令，比如 <code>RUN</code>, <code>COPY</code> 等，而这些指令，在当前镜像构建时并不会被执行。只有当以当前镜像为基础镜像，去构建下一级镜像的时候才会被执行。</p><p><code>Dockerfile</code> 中的其它指令都是为了定制当前镜像而准备的，唯有 <code>ONBUILD</code> 是为了帮助别人定制自己而准备的。</p><p>假设我们要制作 Node.js 所写的应用的镜像。我们都知道 Node.js 使用 <code>npm</code> 进行包管理，所有依赖、配置、启动信息等会放到 <code>package.json</code> 文件里。在拿到程序代码后，需要先进行 <code>npm install</code> 才可以获得所有需要的依赖。然后就可以通过 <code>npm start</code> 来启动应用。因此，一般来说会这样写 <code>Dockerfile</code>：</p><pre class="line-numbers language-none"><code class="language-none">FROM node:slimRUN mkdir /appWORKDIR /appCOPY ./package.json /appRUN [ "npm", "install" ]COPY . /app/CMD [ "npm", "start" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>把这个 <code>Dockerfile</code> 放到 Node.js 项目的根目录，构建好镜像后，就可以直接拿来启动容器运行。但是如果我们还有第二个 Node.js 项目也差不多呢？好吧，那就再把这个 <code>Dockerfile</code> 复制到第二个项目里。那如果有第三个项目呢？再复制么？文件的副本越多，版本控制就越困难，让我们继续看这样的场景维护的问题。</p><p>如果第一个 Node.js 项目在开发过程中，发现这个 <code>Dockerfile</code> 里存在问题，比如敲错字了、或者需要安装额外的包，然后开发人员修复了这个 <code>Dockerfile</code>，再次构建，问题解决。第一个项目没问题了，但是第二个项目呢？虽然最初 <code>Dockerfile</code> 是复制、粘贴自第一个项目的，但是并不会因为第一个项目修复了他们的 <code>Dockerfile</code>，而第二个项目的 <code>Dockerfile</code> 就会被自动修复。</p><p>那么我们可不可以做一个基础镜像，然后各个项目使用这个基础镜像呢？这样基础镜像更新，各个项目不用同步 <code>Dockerfile</code> 的变化，重新构建后就继承了基础镜像的更新？好吧，可以，让我们看看这样的结果。那么上面的这个 <code>Dockerfile</code> 就会变为：</p><pre class="line-numbers language-none"><code class="language-none">FROM node:slimRUN mkdir /appWORKDIR /appCMD [ "npm", "start" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>这里我们把项目相关的构建指令拿出来，放到子项目里去。假设这个基础镜像的名字为 <code>my-node</code> 的话，各个项目内的自己的 <code>Dockerfile</code> 就变为：</p><pre class="line-numbers language-none"><code class="language-none">FROM my-nodeCOPY ./package.json /appRUN [ "npm", "install" ]COPY . /app/<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>基础镜像变化后，各个项目都用这个 <code>Dockerfile</code> 重新构建镜像，会继承基础镜像的更新。</p><p>那么，问题解决了么？没有。准确说，只解决了一半。如果这个 <code>Dockerfile</code> 里面有些东西需要调整呢？比如 <code>npm install</code> 都需要加一些参数，那怎么办？这一行 <code>RUN</code> 是不可能放入基础镜像的，因为涉及到了当前项目的 <code>./package.json</code>，难道又要一个个修改么？所以说，这样制作基础镜像，只解决了原来的 <code>Dockerfile</code> 的前4条指令的变化问题，而后面三条指令的变化则完全没办法处理。</p><p><code>ONBUILD</code> 可以解决这个问题。让我们用 <code>ONBUILD</code> 重新写一下基础镜像的 <code>Dockerfile</code>:</p><pre class="line-numbers language-none"><code class="language-none">FROM node:slimRUN mkdir /appWORKDIR /appONBUILD COPY ./package.json /appONBUILD RUN [ "npm", "install" ]ONBUILD COPY . /app/CMD [ "npm", "start" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这次我们回到原始的 <code>Dockerfile</code>，但是这次将项目相关的指令加上 <code>ONBUILD</code>，这样在构建基础镜像的时候，这三行并不会被执行。然后各个项目的 <code>Dockerfile</code> 就变成了简单地：</p><pre class="line-numbers language-none"><code class="language-none">FROM my-node<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>是的，只有这么一行。当在各个项目目录中，用这个只有一行的 <code>Dockerfile</code> 构建镜像时，之前基础镜像的那三行 <code>ONBUILD</code> 就会开始执行，成功的将当前项目的代码复制进镜像、并且针对本项目执行 <code>npm install</code>，生成应用镜像。</p><h2 id="LABEL-为镜像添加元数据"><a href="#LABEL-为镜像添加元数据" class="headerlink" title="LABEL(为镜像添加元数据)"></a>LABEL(为镜像添加元数据)</h2><p><code>LABEL</code> 指令用来给镜像以键值对的形式添加一些元数据（metadata）。</p><pre class="line-numbers language-none"><code class="language-none">LABEL &lt;key&gt;=&lt;value&gt; &lt;key&gt;=&lt;value&gt; &lt;key&gt;=&lt;value&gt; ...<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>我们还可以用一些标签来申明镜像的作者、文档地址等：</p><pre class="line-numbers language-none"><code class="language-none">LABEL org.opencontainers.image.authors="yeasy"LABEL org.opencontainers.image.documentation="https://yeasy.gitbooks.io"<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h2 id="SHELL"><a href="#SHELL" class="headerlink" title="SHELL"></a>SHELL</h2><p>格式：<code>SHELL ["executable", "parameters"]</code></p><p><code>SHELL</code> 指令可以指定 <code>RUN</code> <code>ENTRYPOINT</code> <code>CMD</code> 指令的 shell，Linux 中默认为 `[“/bin/sh”, “-c”]</p><pre class="line-numbers language-none"><code class="language-none">SHELL ["/bin/sh", "-c"]RUN lll ; lsSHELL ["/bin/sh", "-cex"]RUN lll ; ls<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>两个 <code>RUN</code> 运行同一命令，第二个 <code>RUN</code> 运行的命令会打印出每条命令并当遇到错误时退出。</p><p>当 <code>ENTRYPOINT</code> <code>CMD</code> 以 shell 格式指定时，<code>SHELL</code> 指令所指定的 shell 也会成为这两个指令的 shell</p><pre class="line-numbers language-none"><code class="language-none">SHELL ["/bin/sh", "-cex"]# /bin/sh -cex "nginx"ENTRYPOINT nginx<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-none"><code class="language-none">SHELL ["/bin/sh", "-cex"]# /bin/sh -cex "nginx"CMD nginx<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>free命令详解</title>
      <link href="/2021/12/09/linux/command/free-ming-ling-xiang-jie/"/>
      <url>/2021/12/09/linux/command/free-ming-ling-xiang-jie/</url>
      
        <content type="html"><![CDATA[<h1 id="free命令详解"><a href="#free命令详解" class="headerlink" title="free命令详解"></a>free命令详解</h1><h2 id="free简介"><a href="#free简介" class="headerlink" title="free简介"></a>free简介</h2><p>free命令显示系统内存的使用情况，包括物理内存，交换内存（swap）和内核缓冲区内存</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># free -h -s 3</span>              total        used        <span class="token function">free</span>      shared  buff/cache   availableMem:           187G        162G        574M         13M         24G         23GSwap:          <span class="token number">8</span>.0G        <span class="token number">5</span>.8G        <span class="token number">2</span>.2G<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><ul><li><strong>Mem</strong>： 内存使用情况</li><li><strong>Swap</strong>： 交换分区的使用情况</li><li><strong>total</strong>：显示系统总的可用物理内存和交换空间大小</li><li><strong>used</strong>: 显示已经被使用的物理内存和交换空间</li><li><strong>free</strong>: 显示还有多少物理内存和交换空间可使用</li><li><strong>shared</strong>: 显示被共享使用的物理内存大小</li><li><strong>buff/cache</strong>: buff/cache 使用的物理内存大小</li><li><strong>available</strong>: 显示还可以被应用程序使用的物理内存大小</li></ul><h3 id="buffer"><a href="#buffer" class="headerlink" title="buffer"></a>buffer</h3><p>buffer在操作系统中指buffer cache ，中文一般翻译为“缓冲区”，要理解缓冲区，必须明确另外两个概念：“扇区”和“块”，扇区是设备的最小寻址单位，也叫“硬扇区”或“设备块”。块是操作系统中文件系统的最小寻址单位，也叫“文件块”或I/O块。</p><ul><li>每一个块包含一个或多个扇区,但是大小不能超过一个页面，所以一个页可以容纳一个或多个内存中的块，当一个块调入内存时，他要存储在一个缓冲区中，每个缓冲区与一个块对应，他相当于磁盘块在内存中的显示。</li><li>buffer cache 只用块的概念没有文件的概念，他只是把磁盘上的块直接搬到内存中而不关心块究竟存放的是什么格式的文件</li></ul><h3 id="cache"><a href="#cache" class="headerlink" title="cache"></a>cache</h3><ul><li><p>cache 在操作系统中指<code>page cahce</code> (<code>页高速缓存</code>)，页高速缓存是内核实现的磁盘缓存，它只用用来减少对磁盘I/O 操作，具体来讲，是通过把磁盘的数据缓存到物理内存中，把对磁盘的访问变成对物理内存的访问，页高速缓存缓存的是内存页面。<strong>缓存中的页来自普通文件，块设备文件（这个指的是buffer cache ） 和内存文件的读写</strong></p></li><li><p>页高速缓存对普通文件的缓存我们可以理解：当内核要读一个文件时，他要先检查这个文件的数据是不是已经在页高速缓存中了，如果在，就放弃访问磁盘，直接从内存中读取。这个行为称为缓存命中，如果数据不再缓存中，就是未命中缓存，此时内核就要调度块I/O操作从磁盘中读取数据，然而内核将读来的数据放在页高速缓存中，这种缓存的目标是文件系统可以识别的文件</p></li><li><p>页高速缓存对块设备文件的缓存就是buffer cache，因为独立的磁盘块通过缓冲区页被存入页高速缓存（缓冲区最终是有页高速缓存来承载的）</p></li><li><p>无论是缓存区还是页高速缓存，它们的实现方式都是一样的，缓冲区只不过是一种概念上比较特殊的页高速缓存罢了。</p></li><li><p>free 命令不直接称为 cache 而非要写成 buff/cache？ 这是因为缓冲区和页高速缓存的实现并非天生就是统一的。在 linux 内核 2.4 中才将它们统一。更早的内核中有两个独立的磁盘缓存：页高速缓存和缓冲区高速缓存。前者缓存页面，后者缓存缓冲区。</p></li></ul><h3 id="free-available"><a href="#free-available" class="headerlink" title="free / available"></a>free / available</h3><ul><li><strong>free</strong> ： 真正尚未被使用的物理内存数量。</li><li><strong>available</strong> ： 从应用程序的角度看到的可用的内存数量。</li><li>linux内核为了提高磁盘操作性能，会消耗一部分内存去缓存磁盘数据，就是buffer 和cache ，所以对内核来说，buffer 和cache 都属于被使用的内存，当应用程序需要内存时，如果没有足够的free内存可以用，内核就会从buffer 和cache 中回收内存来满足应用程序的请求。所以应用程序的角度来说<code>available  = free + buffer + cache</code></li></ul><h3 id="swap"><a href="#swap" class="headerlink" title="swap"></a>swap</h3><p>swap space 是磁盘的一块区域，可以是一个分区，也可以使一个文件。所以具体的实现可以是swap分区也可以说swap文件。</p><p>当系统物理内存吃紧时，linux会将内存中不常访问的数据保存到swap上，这样系统就有更多的物理内存为各个进程服务，而当系统需要访问swap上的存储的内容时，再将swap 上的数据加载到内存中，就是常说的换入和换出，交换分区可以在一定程度上缓解内存不足的情况，但是它读写磁盘数据，所以性能不高。</p><h2 id="free命令"><a href="#free命令" class="headerlink" title="free命令"></a>free命令</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage: <span class="token function">free</span> <span class="token punctuation">[</span>options<span class="token punctuation">]</span>Options: -b, --bytes         以字节显示输出 -k, --kilo          以千字节显示输出 -m, --mega          以兆字节显示输出 -g, --giga          以千兆字节显示输出     --tera          show output <span class="token keyword">in</span> terabytes     --peta          show output <span class="token keyword">in</span> petabytes -h, --human         显示人类可读的输出     --si            use powers of <span class="token number">1000</span> not <span class="token number">1024</span> -l, --lohi          显示高低内存的利用率 -t, --total         显示linux的全部内存 -s N, --seconds N   每n秒重复打印 -c N, --count N     重复打印n次，然后退出 -w, --wide          wide output     --help     display this <span class="token builtin class-name">help</span> and <span class="token builtin class-name">exit</span> -V, --version  output version information and <span class="token builtin class-name">exit</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><p><code>free -b</code>:以字节显示输出</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># free -b</span>              total        used        <span class="token function">free</span>      shared  buff/cache   availableMem:    <span class="token number">201005993984</span> <span class="token number">175089881088</span>   <span class="token number">558923776</span>    <span class="token number">13971456</span> <span class="token number">25357189120</span> <span class="token number">25132953600</span>Swap:    <span class="token number">8589930496</span>  <span class="token number">6252322816</span>  <span class="token number">2337607680</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>free -k</code>:以千字节显示输出</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># free -k</span>              total        used        <span class="token function">free</span>      shared  buff/cache   availableMem:      <span class="token number">196294916</span>   <span class="token number">170987016</span>      <span class="token number">545700</span>       <span class="token number">13644</span>    <span class="token number">24762200</span>    <span class="token number">24543096</span>Swap:       <span class="token number">8388604</span>     <span class="token number">6105784</span>     <span class="token number">2282820</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>free -m</code>:以兆字节显示输出</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># free -m</span>              total        used        <span class="token function">free</span>      shared  buff/cache   availableMem:         <span class="token number">191694</span>      <span class="token number">166979</span>         <span class="token number">534</span>          <span class="token number">13</span>       <span class="token number">24180</span>       <span class="token number">23967</span>Swap:          <span class="token number">8191</span>        <span class="token number">5962</span>        <span class="token number">2229</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>free -g</code>: 以千兆字节显示输出</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># free -g</span>              total        used        <span class="token function">free</span>      shared  buff/cache   availableMem:            <span class="token number">187</span>         <span class="token number">163</span>           <span class="token number">0</span>           <span class="token number">0</span>          <span class="token number">23</span>          <span class="token number">23</span>Swap:             <span class="token number">7</span>           <span class="token number">5</span>           <span class="token number">2</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p><strong>注意：</strong>以上格式化的方式都会丢失精度，会将不满整的数据丢弃。最好是以M为单位。</p></blockquote></li><li><p><code>free -h</code>:显示人类可读的输出</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># free -h</span>              total        used        <span class="token function">free</span>      shared  buff/cache   availableMem:           187G        163G        529M         13M         23G         23GSwap:          <span class="token number">8</span>.0G        <span class="token number">5</span>.8G        <span class="token number">2</span>.2G<span class="token comment"># free 会根据内存的大小自动生成可读的大小（M，G...）</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p><strong>注意</strong>：在shell脚本中避免使用，因为单位的变化会造成意料之外的报错</p></blockquote></li><li><p><code>free -l</code>:显示高低内存的利用率</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># free -l</span>              total        used        <span class="token function">free</span>      shared  buff/cache   availableMem:      <span class="token number">196294916</span>   <span class="token number">170999048</span>      <span class="token number">547700</span>       <span class="token number">13644</span>    <span class="token number">24748168</span>    <span class="token number">24530996</span>Low:      <span class="token number">196294916</span>   <span class="token number">195747216</span>      <span class="token number">547700</span>High:             <span class="token number">0</span>           <span class="token number">0</span>           <span class="token number">0</span>Swap:       <span class="token number">8388604</span>     <span class="token number">6109624</span>     <span class="token number">2278980</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>free -t</code>:显示linux的全部内存</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># free -t</span>              total        used        <span class="token function">free</span>      shared  buff/cache   availableMem:      <span class="token number">196294916</span>   <span class="token number">171017312</span>      <span class="token number">535488</span>       <span class="token number">13644</span>    <span class="token number">24742116</span>    <span class="token number">24512824</span>Swap:       <span class="token number">8388604</span>     <span class="token number">6112696</span>     <span class="token number">2275908</span>Total:    <span class="token number">204683520</span>   <span class="token number">177130008</span>     <span class="token number">2811396</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h3 id="组合命令"><a href="#组合命令" class="headerlink" title="组合命令"></a>组合命令</h3><ul><li><p><code>free -s 2 -c 3 -h</code>:以人可读的方式每个两秒打印一次，共3次</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># free -s 2 -c 3 -h</span>              total        used        <span class="token function">free</span>      shared  buff/cache   availableMem:           187G        163G        546M         13M         23G         23GSwap:          <span class="token number">8</span>.0G        <span class="token number">5</span>.8G        <span class="token number">2</span>.2G<span class="token punctuation">..</span><span class="token punctuation">..</span><span class="token punctuation">..</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h2 id="shell中常用方式"><a href="#shell中常用方式" class="headerlink" title="shell中常用方式"></a>shell中常用方式</h2><blockquote><p>默认都是使用<code>free -m</code>,以兆字节为单位。</p></blockquote><h3 id="shell脚本监控系统内存"><a href="#shell脚本监控系统内存" class="headerlink" title="shell脚本监控系统内存"></a>shell脚本监控系统内存</h3><ul><li>常用相关数据的截取命令</li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># 物理内存free值</span><span class="token function">free</span> -m <span class="token operator">|</span> <span class="token function">grep</span> Mem <span class="token operator">|</span> <span class="token function">awk</span> <span class="token string">'{print $4}'</span><span class="token comment"># 缓冲区的free值</span><span class="token comment"># Swap分区free值</span><span class="token function">free</span> -m <span class="token operator">|</span> <span class="token function">grep</span> Swap <span class="token operator">|</span> <span class="token function">awk</span> <span class="token string">'{print $4}'</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>qemu-img</title>
      <link href="/2021/12/09/virtualization/kvm/qemu-img/"/>
      <url>/2021/12/09/virtualization/kvm/qemu-img/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>virsh</title>
      <link href="/2021/12/09/virtualization/kvm/virsh/"/>
      <url>/2021/12/09/virtualization/kvm/virsh/</url>
      
        <content type="html"><![CDATA[<h1 id="virsh"><a href="#virsh" class="headerlink" title="virsh"></a>virsh</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>virt-install</title>
      <link href="/2021/12/09/virtualization/kvm/virt-install/"/>
      <url>/2021/12/09/virtualization/kvm/virt-install/</url>
      
        <content type="html"><![CDATA[<h1 id="virt-install"><a href="#virt-install" class="headerlink" title="virt-install"></a>virt-install</h1><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">usage: virt-install --name NAME --memory MB STORAGE INSTALL <span class="token punctuation">[</span>options<span class="token punctuation">]</span>Create a new virtual machine from specified <span class="token function">install</span> media.optional arguments:  -h, --help            show this <span class="token builtin class-name">help</span> message and <span class="token builtin class-name">exit</span>  --version             show program<span class="token string">'s version number and exit  --connect URI         Connect to hypervisor with libvirt URIGeneral Options:  -n NAME, --name NAME  虚拟机实例的名称（唯一）  --memory MEMORY       配置虚拟机内存分配. Ex:                        --memory 1024 (in MiB)                        --memory 512,maxmemory=1024                        --memory 512,maxmemory=1024,hotplugmemorymax=2048,hotplugmemoryslots=2  --vcpus VCPUS         配置虚拟机虚拟CPU核心数量. Ex:                        --vcpus 5                        --vcpus 5,maxvcpus=10,cpuset=1-4,6,8                        --vcpus sockets=2,cores=4,threads=2  --cpu CPU             CPU model and features. Ex:                        --cpu coreduo,+x2apic                        --cpu host-passthrough                        --cpu host  --metadata METADATA   配置虚拟机元数据. Ex:                        --metadata name=foo,title="My pretty title",uuid=...                        --metadata description="My nice long description"Installation Method Options:  --cdrom CDROM         CD-ROM安装媒介  -l LOCATION, --location LOCATION                        Installation source (eg, nfs:host:/path,                        http://host/path, ftp://host/path)  --pxe                 Boot from the network using the PXE protocol  --import              Build guest around an existing disk image  --livecd              Treat the CD-ROM media as a Live CD  -x EXTRA_ARGS, --extra-args EXTRA_ARGS 传递到从--location启动的安装内核的其他参数  --initrd-inject INITRD_INJECT 从--location中添加给initrd的启动文件    --os-variant DISTRO_VARIANT 正在安装的OS变体 e.g.                        '</span>fedora18<span class="token string">', '</span>rhel6<span class="token string">', '</span>winxp<span class="token string">', etc.  --boot BOOT           Configure guest boot settings. Ex:                        --boot hd,cdrom,menu=on                        --boot init=/sbin/init (for containers)  --idmap IDMAP         Enable user namespace for LXC container. Ex:                        --idmap uid_start=0,uid_target=1000,uid_count=10Device Options:  --disk DISK           使用各种选项指定存储. Ex.                        --disk size=10 (new 10GiB image in default location)                        --disk /my/existing/disk,cache=none                        --disk device=cdrom,bus=scsi                        --disk=?  -w NETWORK, --network NETWORK                        Configure a guest network interface. Ex:                        --network bridge=mybr0                        --network network=my_libvirt_virtual_net                        --network network=mynet,model=virtio,mac=00:11...                        --network none                        --network help  --graphics GRAPHICS   Configure guest display settings. Ex:                        --graphics vnc                        --graphics spice,port=5901,tlsport=5902                        --graphics none                        --graphics vnc,password=foobar,port=5910,keymap=ja  --controller CONTROLLER                        Configure a guest controller device. Ex:                        --controller type=usb,model=ich9-ehci1  --input INPUT         Configure a guest input device. Ex:                        --input tablet                        --input keyboard,bus=usb  --serial SERIAL       Configure a guest serial device  --parallel PARALLEL   Configure a guest parallel device  --channel CHANNEL     Configure a guest communication channel  --console CONSOLE     Configure a text console connection between the guest                        and host  --hostdev HOSTDEV     Configure physical USB/PCI/etc host devices to be                        shared with the guest  --filesystem FILESYSTEM                        Pass host directory to the guest. Ex:                         --filesystem /my/source/dir,/dir/in/guest                        --filesystem template_name,/,type=template  --sound [SOUND]       Configure guest sound device emulation  --watchdog WATCHDOG   Configure a guest watchdog device  --video VIDEO         Configure guest video hardware.  --smartcard SMARTCARD                        Configure a guest smartcard device. Ex:                        --smartcard mode=passthrough  --redirdev REDIRDEV   Configure a guest redirection device. Ex:                        --redirdev usb,type=tcp,server=192.168.1.1:4000  --memballoon MEMBALLOON                        Configure a guest memballoon device. Ex:                        --memballoon model=virtio  --tpm TPM             Configure a guest TPM device. Ex:                        --tpm /dev/tpm  --rng RNG             Configure a guest RNG device. Ex:                        --rng /dev/urandom  --panic PANIC         Configure a guest panic device. Ex:                        --panic default  --memdev MEMDEV       Configure a guest memory device. Ex:                        --memdev dimm,target_size=1024Guest Configuration Options:  --security SECURITY   Set domain security driver configuration.  --cputune CPUTUNE     Tune CPU parameters for the domain process.  --numatune NUMATUNE   Tune NUMA policy for the domain process.  --memtune MEMTUNE     Tune memory policy for the domain process.  --blkiotune BLKIOTUNE                        Tune blkio policy for the domain process.  --memorybacking MEMORYBACKING                        Set memory backing policy for the domain process. Ex:                        --memorybacking hugepages=on  --features FEATURES   Set domain &lt;features&gt; XML. Ex:                        --features acpi=off                        --features apic=on,eoi=on  --clock CLOCK         Set domain &lt;clock&gt; XML. Ex:                        --clock offset=localtime,rtc_tickpolicy=catchup  --pm PM               Configure VM power management features  --events EVENTS       Configure VM lifecycle management policy  --resource RESOURCE   Configure VM resource partitioning (cgroups)  --sysinfo SYSINFO     Configure SMBIOS System Information. Ex:                        --sysinfo emulate                        --sysinfo host                        --sysinfo bios_vendor=Vendor_Inc.,bios_version=1.2.3-abc,...                        --sysinfo system_manufacturer=System_Corp.,system_product=Computer,...                        --sysinfo baseBoard_manufacturer=Baseboard_Corp.,baseBoard_product=Motherboard,...  --qemu-commandline QEMU_COMMANDLINE                        Pass arguments directly to the qemu emulator. Ex:                        --qemu-commandline='</span>-display gtk,gl<span class="token operator">=</span>on<span class="token string">'                        --qemu-commandline env=DISPLAY=:0.1Virtualization Platform Options:  -v, --hvm             This guest should be a fully virtualized guest  -p, --paravirt        This guest should be a paravirtualized guest  --container           This guest should be a container guest  --virt-type HV_TYPE   Hypervisor name to use (kvm, qemu, xen, ...)  --arch ARCH           The CPU architecture to simulate  --machine MACHINE     The machine type to emulateMiscellaneous Options:  --autostart           Have domain autostart on host boot up.  --transient           Create a transient domain.  --wait WAIT           Minutes to wait for install to complete.  --noautoconsole       Don'</span>t automatically try to connect to the guest                        console  --noreboot            Don't boot guest after completing install.  --print-xml <span class="token punctuation">[</span>XMLONLY<span class="token punctuation">]</span>                        Print the generated domain XML rather than create the                        guest.  --dry-run             Run through <span class="token function">install</span> process, but <span class="token keyword">do</span> not create devices                        or define the guest.  --check CHECK         Enable or disable validation checks. Example:                        --check <span class="token assign-left variable">path_in_use</span><span class="token operator">=</span>off                        --check <span class="token assign-left variable">all</span><span class="token operator">=</span>off  -q, --quiet           Suppress non-error output  -d, --debug           Print debugging information<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>kvm环境配置</title>
      <link href="/2021/12/09/virtualization/kvm/kvm-huan-jing-pei-zhi/"/>
      <url>/2021/12/09/virtualization/kvm/kvm-huan-jing-pei-zhi/</url>
      
        <content type="html"><![CDATA[<h1 id="kvm环境配置"><a href="#kvm环境配置" class="headerlink" title="kvm环境配置"></a>kvm环境配置</h1><h2 id="centos"><a href="#centos" class="headerlink" title="centos"></a>centos</h2><ul><li><p>KVM 是基于 x86 虚拟化扩展(Intel VT 或者 AMD-V) 技术的虚拟机软件，所以查看 CPU 是否支持 VT 技术，就可以判断是否支持KVM。有返回结果，如果结果中有vmx（Intel）或svm(AMD)字样，就说明CPU的支持的。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">egrep</span> <span class="token string">'(vmx|svm)'</span> --color<span class="token operator">=</span>always /proc/cpuinfo<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>关闭SELinux,防火墙</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">systemctl stop firewall <span class="token operator">&amp;&amp;</span> systemctl disbale firewalldsystemctl stop NetworkManager <span class="token operator">&amp;&amp;</span> systemctl disbale NetworkManagersetenforce <span class="token number">0</span> <span class="token operator">&amp;&amp;</span> <span class="token function">sed</span> -i <span class="token string">'s/SELINUX=.*/SELINUX=permissive/g'</span> /etc/selinux/config<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p>安装kvm</p><p>kvm相关安装包及其作用:</p><ul><li><code>qemu-kvm</code> 主要的KVM程序包</li><li><code>python-virtinst</code> 创建虚拟机所需要的命令行工具和程序库</li><li><code>virt-manager</code> GUI虚拟机管理工具</li><li><code>virt-top</code> 虚拟机统计命令</li><li><code>virt-viewer</code> GUI连接程序，连接到已配置好的虚拟机</li><li><code>libvirt</code> C语言工具包，提供libvirt服务</li><li><code>libvirt-client</code> 为虚拟客户机提供的C语言工具包</li><li><code>virt-install</code> 基于libvirt服务的虚拟机创建命令</li><li><code>bridge-utils</code> 创建和管理桥接设备的工具</li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">yum -y <span class="token function">install</span> qemu-kvm libvirt virt-install bridge-utils<span class="token comment"># 安装完成后重启机器以便加载kvm模块</span><span class="token function">reboot</span><span class="token comment"># 查看KVM模块是否被正确加载</span>lsmod <span class="token operator">|</span> <span class="token function">grep</span> kvmkvm_intel             <span class="token number">183621</span>  <span class="token number">90</span> kvm                   <span class="token number">586948</span>  <span class="token number">1</span> kvm_intel<span class="token comment"># 启动kvm服务</span>systemctl start libvirtdsystemctl <span class="token builtin class-name">enable</span> libvirtd<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h2 id="ubuntu"><a href="#ubuntu" class="headerlink" title="ubuntu"></a>ubuntu</h2><h2 id="安装脚本"><a href="#安装脚本" class="headerlink" title="安装脚本"></a>安装脚本</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>virtualbox</title>
      <link href="/2021/12/09/virtualization/virtualbox/virtualbox/"/>
      <url>/2021/12/09/virtualization/virtualbox/virtualbox/</url>
      
        <content type="html"><![CDATA[<h1 id="virtualbox"><a href="#virtualbox" class="headerlink" title="virtualbox"></a>virtualbox</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>ecs</title>
      <link href="/2021/12/09/alicloud/ecs/"/>
      <url>/2021/12/09/alicloud/ecs/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>emr</title>
      <link href="/2021/12/09/aws/emr/"/>
      <url>/2021/12/09/aws/emr/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>local-kms</title>
      <link href="/2021/12/09/security/local-kms/local-kms/"/>
      <url>/2021/12/09/security/local-kms/local-kms/</url>
      
        <content type="html"><![CDATA[<h1 id="local-kms"><a href="#local-kms" class="headerlink" title="local-kms"></a>local-kms</h1><blockquote><p>github 地址：<a href="https://github.com/nsmithuk/local-kms">https://github.com/nsmithuk/local-kms</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>kali</title>
      <link href="/2021/12/09/security/kali/kali/"/>
      <url>/2021/12/09/security/kali/kali/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【大数据】Flink</title>
      <link href="/2021/12/09/bigdata/flink/da-shu-ju-flink/"/>
      <url>/2021/12/09/bigdata/flink/da-shu-ju-flink/</url>
      
        <content type="html"><![CDATA[<h1 id="【大数据】Flink"><a href="#【大数据】Flink" class="headerlink" title="【大数据】Flink"></a>【大数据】Flink</h1><h2 id="flink简介"><a href="#flink简介" class="headerlink" title="flink简介"></a>flink简介</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【交互式笔记本】jupyter</title>
      <link href="/2021/12/09/bigdata/jupyter/jiao-hu-shi-bi-ji-ben-jupyter/"/>
      <url>/2021/12/09/bigdata/jupyter/jiao-hu-shi-bi-ji-ben-jupyter/</url>
      
        <content type="html"><![CDATA[<h1 id="【交互式笔记本】jupyter"><a href="#【交互式笔记本】jupyter" class="headerlink" title="【交互式笔记本】jupyter"></a>【交互式笔记本】jupyter</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【go每日一库】docker-client</title>
      <link href="/2021/12/06/golang/third-party-library/go-mei-ri-yi-ku-docker-client/"/>
      <url>/2021/12/06/golang/third-party-library/go-mei-ri-yi-ku-docker-client/</url>
      
        <content type="html"><![CDATA[<h1 id="【go每日一库】docker-client"><a href="#【go每日一库】docker-client" class="headerlink" title="【go每日一库】docker-client"></a>【go每日一库】docker-client</h1><blockquote><p>docker client 文档： <a href="https://pkg.go.dev/github.com/docker/docker/client">https://pkg.go.dev/github.com/docker/docker/client</a></p></blockquote><h2 id="docker-client-简介"><a href="#docker-client-简介" class="headerlink" title="docker-client 简介"></a>docker-client 简介</h2><h2 id="docker-client环境配置"><a href="#docker-client环境配置" class="headerlink" title="docker-client环境配置"></a>docker-client环境配置</h2><pre class="line-numbers language-none"><code class="language-none">go get github.com/docker/docker/client<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="docker-client使用"><a href="#docker-client使用" class="headerlink" title="docker-client使用"></a>docker-client使用</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Docker】私有仓库</title>
      <link href="/2021/12/06/docker/registry/docker-si-you-cang-ku/"/>
      <url>/2021/12/06/docker/registry/docker-si-you-cang-ku/</url>
      
        <content type="html"><![CDATA[<h1 id="【Docker】私有仓库"><a href="#【Docker】私有仓库" class="headerlink" title="【Docker】私有仓库"></a>【Docker】私有仓库</h1><blockquote><p>docker registry文档：<a href="https://docs.docker.com/registry/">https://docs.docker.com/registry/</a></p><p>docker registry api: <a href="https://docs.docker.com/registry/spec/api/">https://docs.docker.com/registry/spec/api/</a></p></blockquote><h2 id="registry简介"><a href="#registry简介" class="headerlink" title="registry简介"></a>registry简介</h2><p>Docker提供了开放的中央仓库dockerhub，同时也允许我们使用registry搭建本地私有仓库。</p><ul><li>节省网络带宽，提升Docker部署速度，不用每个镜像从DockerHub上去下载，只需从私有仓库下载就可</li><li>私有镜像，包含公司敏感信息，不方便公开对外，只在公司内部使用</li></ul><h2 id="registry环境搭建"><a href="#registry环境搭建" class="headerlink" title="registry环境搭建"></a>registry环境搭建</h2><ul><li><p>docker安装</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"># registry 服务端安装docker pull registrydocker run -d -v /home/dockerRegistry:/var/lib/registry -p 5000:5000 \--restart=always --privileged=true --name ev-registry registry:latest# registry web界面安装docker pull hyper/docker-registry-webdocker run -it -p 8080:8080 --restart=always --name ev-registry-web --link ev-registry -e REGISTRY_URL=http://ev-registry:5000/v2 -e REGISTRY_NAME=localhost:5000 hyper/docker-registry-web<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>docker-compose安装(docker-compose.yml)</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">version</span><span class="token punctuation">:</span> <span class="token string">"3.1"</span><span class="token key atrule">services</span><span class="token punctuation">:</span>  <span class="token key atrule">registry</span><span class="token punctuation">:</span>    <span class="token key atrule">image</span><span class="token punctuation">:</span> registry    <span class="token key atrule">restart</span><span class="token punctuation">:</span> always    <span class="token key atrule">container_name</span><span class="token punctuation">:</span> ev<span class="token punctuation">-</span>registry    <span class="token key atrule">ports</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> <span class="token string">"5000:5000"</span>    <span class="token key atrule">volumes</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> ./data<span class="token punctuation">:</span>/var/lib/registry  <span class="token key atrule">registry-web</span><span class="token punctuation">:</span>    <span class="token key atrule">image</span><span class="token punctuation">:</span> konradkleine/docker<span class="token punctuation">-</span>registry<span class="token punctuation">-</span>frontend    <span class="token key atrule">ports</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> <span class="token string">"8080:80"</span><span class="token comment">#    volumes: # 挂载相应证书文件</span><span class="token comment">#      - ./certs/registry.crt:/etc/apache2/server.crt:ro</span><span class="token comment">#      - ./certs/registry.key:/etc/apache2/server.key:ro</span>    <span class="token key atrule">environment</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> ENV_DOCKER_REGISTRY_HOST=localhost      <span class="token punctuation">-</span> ENV_DOCKER_REGISTRY_PORT=5000<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h2 id="registry使用"><a href="#registry使用" class="headerlink" title="registry使用"></a>registry使用</h2><h3 id="上传镜像到私有仓库"><a href="#上传镜像到私有仓库" class="headerlink" title="上传镜像到私有仓库"></a>上传镜像到私有仓库</h3><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">docker images | grep busybox# 修改镜像tagdocker tag busybox:latest 10.1.3.219:5000/busybox:latestdocker tag busybox:latest 10.1.3.219:5000/test/busybox:latest# 上传镜像docker push 10.1.3.219:5000/busybox:latestdocker push 10.1.3.219:5000/test/busybox:latest<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>注意：Docker在1.3.x之后默认docker registry使用的是https，为了解决这个问题，修改本地主机的docker启动配置文件.</p><p><code>/etc/docker/daemon.json</code></p><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token property">"insecure-registries"</span><span class="token operator">:</span> <span class="token punctuation">[</span>    <span class="token string">"10.1.3.219:5000"</span>  <span class="token punctuation">]</span><span class="token punctuation">,</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></blockquote><h3 id="拉取私有仓库镜像"><a href="#拉取私有仓库镜像" class="headerlink" title="拉取私有仓库镜像"></a>拉取私有仓库镜像</h3><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">docker pull 10.1.3.219:5000/busybox:latest<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="registry的api"><a href="#registry的api" class="headerlink" title="registry的api"></a>registry的api</h3><h4 id="名词解释"><a href="#名词解释" class="headerlink" title="名词解释"></a>名词解释</h4><ul><li><p>repository name(存储库名称)</p><p>存储库指在库中存储的镜像。(<code>/test/busybox:latest</code>)</p><p>语法：</p><ul><li><p>经典存储库名称由2级路径构成，每级路径小于30个字符，V2的api不强制要求这样的格式。</p></li><li><p>每级路径名至少有一个小写字母或者数字，使用句号，破折号和下划线分割。更严格来说，它必须符合正则表达式：<code>[a-z0-9]+[._-][a-z0-9]+)</code></p></li><li><p>多级路径用/分隔</p></li><li><p>存储库名称总长度（包括/）不能超过256个字符</p></li></ul></li><li><p>digest(摘要)</p><p>摘要是镜像每个层的唯一标示。虽然算法允许使用任意算法，但是为了兼容性应该使用sha256。</p></li></ul><h4 id="API清单"><a href="#API清单" class="headerlink" title="API清单"></a>API清单</h4><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">GET /v2/ 检查registry api 是不是 V2版本---GET /v2/&lt;image&gt;/tags/list 获取通过名称标示的所有的tag列表---GET /v2/&lt;image&gt;/manifests/&lt;referevce&gt;---PUT /v2/&lt;image&gt;/manifests/&lt;referevce&gt;---DELETE /v2/&lt;image&gt;/manifests/&lt;reference&gt;---GET /v2/&lt;image&gt;/blobs/&lt;digest&gt;---DELETE /v2/&lt;image&gt;/blobs/&lt;digest&gt;---POST /v2/&lt;image&gt;/blobs/uploads/---GET /v2/&lt;image&gt;/blobs/uploads/&lt;uuid&gt;---PATCH /v2/&lt;image&gt;/blobs/uploads/&lt;uuid&gt;---PUT /v2/&lt;image&gt;/blobs/uploads/&lt;uuid&gt;---DELETE /v2/&lt;image&gt;/blobs/uploads/&lt;uuid&gt;---GET /v2/_catalog  <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="镜像拉取过程"><a href="#镜像拉取过程" class="headerlink" title="镜像拉取过程"></a>镜像拉取过程</h4><p>镜像由一个json清单和层叠文件组成，pull镜像的过程就是检索这两个组件的过程。</p><p>第一步就是获取清单，清单组成: <code>registry:5000/v2/busybox/manifests/latest</code></p><blockquote><p>manifests字段详情：</p><ul><li>name（镜像名称）</li><li>tag（镜像当前版本的tag）</li><li>fsLayers（层描述列表(包括摘要)）</li><li>signature（一个JWS签名，用来验证清单内容）</li></ul></blockquote><pre class="line-numbers language-ruby" data-language="ruby"><code class="language-ruby">当获取清单之后，客户端需要验证前面<span class="token punctuation">(</span>signature<span class="token punctuation">)</span>，以确保名称和fsLayers层是有效的。确认后，客户端可以使用digest去下载各个fs层。在<span class="token constant">V2api</span>中，层存储在blobs中已digest作为键值<span class="token punctuation">.</span><span class="token number">1.</span> 首先拉取镜像清单<span class="token punctuation">(</span>pulling an <span class="token constant">Image</span> <span class="token constant">Manifest</span><span class="token punctuation">)</span>    $ <span class="token constant">HEAD</span> <span class="token operator">/</span>v2<span class="token operator">/</span><span class="token operator">&lt;</span>image<span class="token operator">/</span>manifests<span class="token operator">/</span><span class="token operator">&lt;</span>reference<span class="token operator">&gt;</span><span class="token comment">#检查镜像清单是否存在</span>  $ <span class="token constant">GET</span> <span class="token operator">/</span>v2<span class="token operator">/</span><span class="token operator">&lt;</span>image<span class="token operator">&gt;</span><span class="token operator">/</span>manifests<span class="token operator">/</span><span class="token operator">&lt;</span>reference<span class="token operator">&gt;</span><span class="token comment">#拉取镜像清单</span>  提示：reference可是是tag或者是digest  <span class="token number">2.</span> 开始拉取每个层<span class="token punctuation">(</span>pulling a <span class="token constant">Layer</span><span class="token punctuation">)</span>   $ <span class="token constant">GET</span> <span class="token operator">/</span>v2<span class="token operator">/</span><span class="token operator">&lt;</span>image<span class="token operator">&gt;</span><span class="token operator">/</span>blobs<span class="token operator">/</span><span class="token operator">&lt;</span>digest<span class="token operator">&gt;</span>   提示：digest是镜像每个fsLayer层的唯一标识。存在于清单的fsLayers里面。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="Push镜像过程"><a href="#Push镜像过程" class="headerlink" title="Push镜像过程"></a>Push镜像过程</h4><p>推送镜像和拉取镜像过程相反,先推各个层到registry仓库，然后上传清单.</p><ul><li>Pushing a Layer(上传层)</li></ul><p>第一步使用post请求在registry仓库启动上传服务,返回一个url，这个url用来上传数据和检查状态。</p><ul><li><p>首先Existing Layers(检查层是否存在)</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">HEAD /v2/image/blobs/&lt;digest&gt;# 若返回200 OK 则表示存在，不用上传<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p>开始上传服务(Starting An Upload)</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">POST /v2/image/blobs/uploads/# 如果post请求返回202 accepted，一个url会在location字段返回. 202 Accepted Location: /v2/\&lt;image&gt;/blobs/uploads/\&lt;uuid&gt; Range: bytes=0-&lt;offset&gt; Content-Length: 0 Docker-Upload-UUID: &lt;uuid&gt; # 可以用来查看上传状态和实现断点续传<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>开始上传层(Uploging the Layer)</p><ul><li><p>上传进度(Upload Progress)</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">GET /v2/&lt;image&gt;/blobs/uploads/&lt;uuid&gt;# 204 No ContentLocation: /v2/&lt;name&gt;/blobs/uploads/&lt;uuid&gt;Range: bytes=0-&lt;offset&gt;Docker-Upload-UUID: &lt;uuid&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>整块上传(Monolithic Upload)</p></li></ul><pre class="line-numbers language-xml" data-language="xml"><code class="language-xml">PUT /v2/<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>name</span><span class="token punctuation">&gt;</span></span>/blobs/uploads/<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>uuid</span><span class="token punctuation">&gt;</span></span>?digest=\<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>digest</span><span class="token punctuation">&gt;</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><p>分块上传(Chunked Upload)</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">PATCH /v2/\&lt;name&gt;/blobs/uploads/\&lt;uuid&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ul></li><li><p>上传完成(Completed Upload)</p><p>分块上传在最后一块上传完毕后，需要提交一个上传完成的请求</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">PUT /v2/&lt;name&gt;/blob/uploads/&lt;uuid&gt;?digest=&lt;digest&gt;201 CreatedLocation: /v2/&lt;name&gt;/blobs/&lt;digest&gt;Content-Length: 0Docker-Content-Digest: &lt;digest&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>取消上传(Canceling an Upload)</p><p>这个请求执行后UUID将失效，当上传超时或者没有完成，客户端都应该发送这个请求。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">DELETE /v2/image/blobs/uploads/&lt;uuid&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>交叉上传(Cross Repository Blob Mount)</p><p>可以把客户端有访问权限的已有存储库中的层挂载到当前存储库中</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">POST /v2/&lt;name&gt;/blobs/uploads/?mount=&lt;digest&gt;&amp;from=&lt;repository name&gt;201 CreatedLocation: /v2/&lt;name&gt;/blobs/&lt;digest&gt;Content-Length: 0Docker-Content-Digest: &lt;digest&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>删除层(Deleting a Layer)</p><pre class="line-numbers language-none"><code class="language-none">DELETE /v2/&lt;image&gt;/blobs/&lt;digest&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>上传镜像清单(Pushing an Image Manifest)</p><p>我们上传完镜像层之后，就开始上传镜像清单</p><pre class="line-numbers language-none"><code class="language-none">PUT /v2/&lt;name&gt;/manifests/&lt;reference&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ul><h3 id="检索功能"><a href="#检索功能" class="headerlink" title="检索功能"></a>检索功能</h3><ul><li><p>列出所有存储库(Listing Repositories)</p><pre class="line-numbers language-none"><code class="language-none">GET /v2/_catalog<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>列出部分存储库(Pagination)</p><pre class="line-numbers language-none"><code class="language-none">GET /v2/_catalog?n=&lt;integer&gt; # integer表示要列出库的个数<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>列出镜像所有tags(Listing Image Tags)</p><pre class="line-numbers language-none"><code class="language-none">GET /v2/image/tags/list<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>列出镜像部分tags(Pagination)</p><pre class="line-numbers language-none"><code class="language-none">GET /v2/image/tags/list?n=&lt;integer&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>删除镜像(Deleting an Image)</p><pre class="line-numbers language-none"><code class="language-none">DELETE /v2/image/manifests/&lt;reference&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ul><p> 注意：默认情况下，registry不允许删除镜像操作，需要在启动registry时指定环境变量REGISTRY_STORAGE_DELETE_ENABLED=true,或者修改其配置文件即可。reference必须是digest,否则删除将失败。在registry2.3或更高版本删除清单时，必须在HEAD或GET获取清单以获取要删除的正确digest携带以下头：</p><pre class="line-numbers language-none"><code class="language-none">Accept: application/vnd.docker.distribution.manifest.v2+json<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Linux】firewalld</title>
      <link href="/2021/12/03/linux/command/linux-firewalld/"/>
      <url>/2021/12/03/linux/command/linux-firewalld/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Linux】systemd</title>
      <link href="/2021/12/03/linux/command/linux-systemd/"/>
      <url>/2021/12/03/linux/command/linux-systemd/</url>
      
        <content type="html"><![CDATA[<h1 id="【Linux】systemd"><a href="#【Linux】systemd" class="headerlink" title="【Linux】systemd"></a>【Linux】systemd</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【内网穿透】frp</title>
      <link href="/2021/12/03/networks/nei-wang-chuan-tou-frp/"/>
      <url>/2021/12/03/networks/nei-wang-chuan-tou-frp/</url>
      
        <content type="html"><![CDATA[<h1 id="【内网穿透】frp"><a href="#【内网穿透】frp" class="headerlink" title="【内网穿透】frp"></a>【内网穿透】frp</h1><blockquote><p>github 地址：<a href="https://github.com/fatedier/frp">https://github.com/fatedier/frp</a></p><p>官方文档：<a href="https://gofrp.org/">https://gofrp.org/</a></p></blockquote><h2 id="frp简介"><a href="#frp简介" class="headerlink" title="frp简介"></a>frp简介</h2><p>frp 是一个专注于内网穿透的高性能的反向代理应用，支持 TCP、UDP、HTTP、HTTPS 等多种协议。可以将内网服务以安全、便捷的方式通过具有公网 IP 节点的中转暴露到公网。</p><p>通过在具有公网 IP 的节点上部署 frp 服务端，可以轻松地将内网服务穿透到公网，同时提供诸多专业的功能特性，这包括：</p><ul><li>客户端服务端通信支持 TCP、KCP 以及 Websocket 等多种协议。</li><li>采用 TCP 连接流式复用，在单个连接间承载更多请求，节省连接建立时间。</li><li>代理组间的负载均衡。</li><li>端口复用，多个服务通过同一个服务端端口暴露。</li><li>多个原生支持的客户端插件（静态文件查看，HTTP、SOCK5 代理等），便于独立使用 frp 客户端完成某些工作。</li><li>高度扩展性的服务端插件系统，方便结合自身需求进行功能扩展。</li><li>服务端和客户端 UI 页面。</li></ul><h3 id="frp原理"><a href="#frp原理" class="headerlink" title="frp原理"></a>frp原理</h3><p>frp 主要由 <strong>客户端(frpc)</strong> 和 <strong>服务端(frps)</strong> 组成，服务端通常部署在具有公网 IP 的机器上，客户端通常部署在需要穿透的内网服务所在的机器上。</p><p>内网服务由于没有公网 IP，不能被非局域网内的其他用户访问。</p><p>用户通过访问服务端的 frps，由 frp 负责根据请求的端口或其他信息将请求路由到对应的内网机器，从而实现通信。</p><p>在 frp 中一个代理对应一个需要暴露的内网服务。一个客户端支持同时配置多个代理。</p><h2 id="frp环境配置"><a href="#frp环境配置" class="headerlink" title="frp环境配置"></a>frp环境配置</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">https://github.com/fatedier/frp/releases# 下载到最新版本的客户端和服务端二进制文件，所有文件被打包在一个压缩包中。tar xf frp_0.38.0_linux_amd64.tar.gz## 服务端mv frps /usr/bin/mkdir /etc/frp &amp;&amp; mv frps.ini /etc/frp/ ## 客户端mv frpc /usr/binmkdir /etc/frp &amp;&amp; frpc.ini /etc/frp<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="frp配置成系统服务"><a href="#frp配置成系统服务" class="headerlink" title="frp配置成系统服务"></a>frp配置成系统服务</h3><p>如果服务器是 Linux 系统，在下载<code>frp</code>解压目录的 systemd 目录下面已经有提供将 <code>frps </code>配置成系统服务的文件。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"># 服务端mv systemd/frps.service /etc/systemd/system/systemctl start frps# 客户端mv systemd/frpc.service /etc/systemd/system/systemctl start frpc<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="frp配置使用"><a href="#frp配置使用" class="headerlink" title="frp配置使用"></a>frp配置使用</h2><p>frps.ini配置</p><pre class="line-numbers language-ini" data-language="ini"><code class="language-ini"><span class="token header"><span class="token punctuation">[</span><span class="token section-name selector">common</span><span class="token punctuation">]</span></span><span class="token comment"># 设置地址及通信端口</span><span class="token key attr-name">bind_addr</span> <span class="token punctuation">=</span> <span class="token value attr-value">0.0.0.0</span><span class="token key attr-name">bind_port</span> <span class="token punctuation">=</span> <span class="token value attr-value">7000</span><span class="token comment"># 设置默认的UDP端口</span><span class="token key attr-name">bind_udp_port</span> <span class="token punctuation">=</span> <span class="token value attr-value">7001</span><span class="token comment"># 监听7080与7443端口，进行 HTTP 与 HTTPS 代理，HTTP 与 HTTPS 端口可以成设置一样</span><span class="token key attr-name">vhost_http_port</span> <span class="token punctuation">=</span> <span class="token value attr-value">7080</span><span class="token key attr-name">vhost_https_port</span> <span class="token punctuation">=</span> <span class="token value attr-value">7443</span><span class="token comment"># 设置查看仪表板服务地址及端口，dashboard_addr 不设置，默认与 bind_addr 一样，如果不设置 dashboard_port 端口不会开启这个服务</span><span class="token key attr-name">dashboard_addr</span> <span class="token punctuation">=</span> <span class="token value attr-value">0.0.0.0</span><span class="token key attr-name">dashboard_port</span> <span class="token punctuation">=</span> <span class="token value attr-value">7500</span><span class="token comment"># 设置仪表盘服务的登录的账号与密码，如果不设置就默认都是 admin</span><span class="token key attr-name">dashboard_user</span> <span class="token punctuation">=</span> <span class="token value attr-value">admin</span><span class="token key attr-name">dashboard_pwd</span> <span class="token punctuation">=</span> <span class="token value attr-value">admin</span><span class="token comment"># 设置云服务器的域名，方便简单的配置代理出去的服务可以通过子域名的方式访问</span><span class="token key attr-name">subdomain_host</span> <span class="token punctuation">=</span> <span class="token value attr-value">example.com</span><span class="token comment"># 设置Token, 尽量配置复杂些，配置 frpc 会用到</span><span class="token key attr-name">token</span> <span class="token punctuation">=</span> <span class="token value attr-value">oQ0@sC0+iU5)kL6^fY6:kO3"tG0</span><span class="token comment"># 设置日志文件记录路径</span><span class="token key attr-name">log_file</span> <span class="token punctuation">=</span> <span class="token value attr-value">./logs/frps.log</span><span class="token comment"># 设置日志记录级别，分别有trace, debug, info, warn, error</span><span class="token key attr-name">log_level</span> <span class="token punctuation">=</span> <span class="token value attr-value">info</span><span class="token comment"># 设置日志记录最大天数</span><span class="token key attr-name">log_max_days</span> <span class="token punctuation">=</span> <span class="token value attr-value">1</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>frpc.ini配置</p><pre class="line-numbers language-ini" data-language="ini"><code class="language-ini"><span class="token header"><span class="token punctuation">[</span><span class="token section-name selector">common</span><span class="token punctuation">]</span></span><span class="token comment"># 云服务器的IP地址及 frps 里面设置的通信端口</span><span class="token key attr-name">server_addr</span> <span class="token punctuation">=</span> <span class="token value attr-value">x.x.x.x</span><span class="token key attr-name">server_port</span> <span class="token punctuation">=</span> <span class="token value attr-value">7000</span><span class="token comment"># 授权 token 与 frps 配置的一样</span><span class="token key attr-name">token</span> <span class="token punctuation">=</span> <span class="token value attr-value">123456789</span><span class="token comment"># 设置日志文件记录路径</span><span class="token key attr-name">log_file</span> <span class="token punctuation">=</span> <span class="token value attr-value">./logs/frps.log</span><span class="token comment"># 设置日志记录级别，分别有trace, debug, info, warn, error</span><span class="token key attr-name">log_level</span> <span class="token punctuation">=</span> <span class="token value attr-value">info</span><span class="token comment"># 设置日志记录最大天数</span><span class="token key attr-name">log_max_days</span> <span class="token punctuation">=</span> <span class="token value attr-value">1</span><span class="token comment"># 设置可在浏览器里面配置热更新的服务</span><span class="token key attr-name">admin_addr</span> <span class="token punctuation">=</span> <span class="token value attr-value">127.0.0.1</span><span class="token key attr-name">admin_port</span> <span class="token punctuation">=</span> <span class="token value attr-value">7400</span><span class="token key attr-name">admin_user</span> <span class="token punctuation">=</span> <span class="token value attr-value">admin</span><span class="token key attr-name">admin_pwd</span> <span class="token punctuation">=</span> <span class="token value attr-value">admin</span><span class="token comment"># tcp 范围 7001-7010</span><span class="token comment"># RDP，即Remote Desktop 远程桌面，Windows的RDP默认端口是3389，协议为TCP</span><span class="token header"><span class="token punctuation">[</span><span class="token section-name selector">rdp</span><span class="token punctuation">]</span></span><span class="token key attr-name">type</span> <span class="token punctuation">=</span> <span class="token value attr-value">tcp</span><span class="token key attr-name">local_ip</span> <span class="token punctuation">=</span> <span class="token value attr-value">127.0.0.1</span>           <span class="token key attr-name">local_port</span> <span class="token punctuation">=</span> <span class="token value attr-value">3389</span><span class="token key attr-name">remote_port</span> <span class="token punctuation">=</span> <span class="token value attr-value">7001</span><span class="token key attr-name">subdomain</span> <span class="token punctuation">=</span> <span class="token value attr-value">rdp</span><span class="token comment"># SMB，即Windows文件共享所使用的协议，默认端口号445，协议TCP，本条规则可实现远程文件访问。</span><span class="token comment">#[smb]</span><span class="token comment">#type = tcp</span><span class="token comment">#local_ip = 127.0.0.1</span><span class="token comment">#local_port = 445</span><span class="token comment">#remote_port = 7002</span><span class="token comment"># 代理本机 SSH</span><span class="token comment">#[ssh]</span><span class="token comment">#type = tcp</span><span class="token comment">#local_ip = 127.0.0.1</span><span class="token comment">#local_port = 22</span><span class="token comment">#remote_port = 7003</span><span class="token comment"># 设置本地端口80 HTTP 服务的代理</span><span class="token comment"># 可通过 frps 里面配置的 subdomain_host 域名</span><span class="token comment"># 访问 test.example.com 加 HTTP 代理端口，即 http://test.example.com:7080</span><span class="token header"><span class="token punctuation">[</span><span class="token section-name selector">web_test</span><span class="token punctuation">]</span></span><span class="token key attr-name">type</span> <span class="token punctuation">=</span> <span class="token value attr-value">http</span><span class="token key attr-name">local_port</span> <span class="token punctuation">=</span> <span class="token value attr-value">80</span><span class="token key attr-name">subdomain</span> <span class="token punctuation">=</span> <span class="token value attr-value">test</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> 内网穿透 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> frp </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【数据交换格式】protocol buffer</title>
      <link href="/2021/12/03/protobuff/shu-ju-jiao-huan-ge-shi-protocol-buffer/"/>
      <url>/2021/12/03/protobuff/shu-ju-jiao-huan-ge-shi-protocol-buffer/</url>
      
        <content type="html"><![CDATA[<h1 id="【数据交换格式】protocol-buffer"><a href="#【数据交换格式】protocol-buffer" class="headerlink" title="【数据交换格式】protocol buffer"></a>【数据交换格式】protocol buffer</h1><h2 id="protocol-buffer简介"><a href="#protocol-buffer简介" class="headerlink" title="protocol buffer简介"></a>protocol buffer简介</h2><p>Protocol Buffers 是一种与语言无关，平台无关的可扩展机制，用于序列化结构化数据。使用Protocol Buffers 可以一次定义结构化的数据，然后可以使用特殊生成的源代码轻松地在各种数据流中使用各种语言编写和读取结构化数据。</p><p>Protocol Buffers 目前有2和3两个版本号。</p><p><strong>优点</strong></p><ul><li>性能较xml,json, thirft等好，效率高</li><li>代码生成机制，数据解析类自动生成</li><li>支持向后兼容和向前兼容</li><li>支持多种编程语言</li></ul><p><strong>缺点</strong></p><ul><li>二进制格式导致可读性差</li><li>缺乏自描述</li></ul><h2 id="protocol-buffers语法"><a href="#protocol-buffers语法" class="headerlink" title="protocol buffers语法"></a>protocol buffers语法</h2><h3 id="文档结构"><a href="#文档结构" class="headerlink" title="文档结构"></a>文档结构</h3><p>Protocol Buffers 使用 .proto 文件来保存文档。</p><ul><li><p>Protocol Buffers文档的第一个非注释行，为版本申明，不填写的话默认为版本2</p><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf"><span class="token keyword">syntax</span> <span class="token operator">=</span> <span class="token string">"proto3"</span><span class="token punctuation">;</span><span class="token comment">// 或者</span><span class="token keyword">syntax</span> <span class="token operator">=</span> <span class="token string">"proto2"</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p>Protocol Buffers可以声明package，来防止命名冲突。 不同的是 Packages是可选的</p><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf"><span class="token keyword">package</span> foo<span class="token punctuation">.</span>bar<span class="token punctuation">;</span><span class="token keyword">message</span> <span class="token class-name">Open</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">;</span><span class="token comment">// 使用的时候需要加上命名空间</span><span class="token keyword">message</span> <span class="token class-name">Foo</span> <span class="token punctuation">{</span>foo<span class="token punctuation">.</span>bar<span class="token punctuation">.</span>Open <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token comment">// 不同语言的申明方式不同</span><span class="token comment">// java</span><span class="token comment">// 如果在.proto中显示使用 option java_package定义包名，则使用option java_package 定义。</span><span class="token comment">// go</span><span class="token comment">// 如果在.proto中显示使用 an option go_package定义包名，则使用option go_package.</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>Protocol Buffers 中可以导入其它文件消息</p><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf"><span class="token keyword">import</span> <span class="token string">"myproject/other_protos.proto"</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>消息类似结构体、类等，服务是用来定义RPC通信的方法。message 关键字来定义消息, service 关键字来定义服务。</p></li></ul><h3 id="添加注释"><a href="#添加注释" class="headerlink" title="添加注释"></a>添加注释</h3><p>Protocol Buffers 使用C / C ++ - 样式//和/* … */语法为文件添加注释。</p><h3 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h3><p>Protocol Buffers 中数据类型分为<strong>标量类型和复合类型</strong>。</p><h4 id="标量类型"><a href="#标量类型" class="headerlink" title="标量类型"></a>标量类型</h4><p>该表显示.proto文件中指定的类型，以及自动生成的类中的相应类型</p><ul><li><p><strong>double</strong></p><pre class="line-numbers language-none"><code class="language-none"># java -&gt; double # python -&gt; float# go -&gt; double<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p><strong>float</strong></p><pre class="line-numbers language-none"><code class="language-none"># java -&gt; float# python -&gt; float<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><strong>int32</strong></p><p>使用变长编码，对负数编码效率低， 如果你的变量可能是负数，可以使用sint32</p></li><li><p><strong>int64</strong></p><p>使用变长编码，对负数编码效率低，如果你的变量可能是负数，可以使用sint64</p></li><li><p><strong>uint32</strong></p></li><li><p><strong>uint64</strong></p></li><li><p><strong>sint32</strong></p><p>使用变长编码，带符号的int类型，对负数编码比int32高效</p></li><li><p><strong>sint64</strong></p><p>使用变长编码，带符号的int类型，对负数编码比int64高效</p></li><li><p><strong>fixed32</strong></p><p>4字节编码， 如果变量经常大于2^{28} 的话，会比uint32高效</p></li><li><p><strong>fixed64</strong></p><p>8字节编码， 如果变量经常大于2^{56} 的话，会比uint64高效</p></li><li><p><strong>sfixed32</strong></p></li><li><p><strong>sfixed64</strong></p></li><li><p><strong>bool</strong></p></li><li><p><strong>string</strong></p><p>必须包含utf-8编码或者7-bit ASCII text</p></li><li><p><strong>bytes</strong></p><p>任意的字节序列</p></li></ul><h4 id="复合类型"><a href="#复合类型" class="headerlink" title="复合类型"></a>复合类型</h4><ul><li><p><strong>枚举</strong></p><p>在 Proto Buffers 中，我们可以定义枚举和枚举类型</p><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf"><span class="token keyword">enum</span> <span class="token class-name">Corpus</span> <span class="token punctuation">{</span>UNIVERSAL <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>  WEB <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>  IMAGES <span class="token operator">=</span> <span class="token number">2</span><span class="token punctuation">;</span>  LOCAL <span class="token operator">=</span> <span class="token number">3</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token positional-class-name class-name">Corpus</span> corpus <span class="token operator">=</span> <span class="token number">4</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>枚举定义在一个消息内部或消息外部都是可以的，如果枚举是 定义在 message 内部，而其他 message 又想使用，那么可以通过 MessageType.EnumType 的方式引用。</p><p><strong>定义枚举的时候，我们要保证第一个枚举值必须是0，枚举值不能重复，除非使用 option allow_alias = true 选项来开启别名。</strong></p><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf"><span class="token keyword">enum</span> <span class="token class-name">EnumAllowingAlias</span> <span class="token punctuation">{</span>    <span class="token keyword">option</span> allow_alias <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>    UNKNOWN <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>    STARTED <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>    RUNNING <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>枚举值的范围是32-bit integer，但因为枚举值使用变长编码，所以不推荐使用负数作为枚举值，因为这会带来效率问题</p></li></ul><h3 id="定义消息"><a href="#定义消息" class="headerlink" title="定义消息"></a>定义消息</h3><p>Proto Buffers 使用message定义消息</p><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf"><span class="token keyword">syntax</span> <span class="token operator">=</span> <span class="token string">"proto3"</span><span class="token punctuation">;</span> <span class="token comment">//  定义proto的语法版本，这必须是文件的第一个非空的非注释行。</span><span class="token keyword">message</span> <span class="token class-name">SearchRequest</span> <span class="token punctuation">{</span> <span class="token comment">// 每个字段都有一个名称和类型和编号。</span>  <span class="token builtin">string</span> query <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>  <span class="token builtin">int32</span> page_number <span class="token operator">=</span> <span class="token number">2</span><span class="token punctuation">;</span>  <span class="token builtin">int32</span> result_per_page <span class="token operator">=</span> <span class="token number">3</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="字段编号"><a href="#字段编号" class="headerlink" title="字段编号"></a>字段编号</h4><p>消息定义中的每个字段都有唯一的编号。</p><p>这些字段编号用于以消息二进制格式标识字段，并且在使用消息类型后不应更改。请注意，1到15范围内的字段编号需要一个字节进行编码，包括字段编号和字段类型<strong>。</strong>16到2047范围内的字段编号占用两个字节。因此，您应该为非常频繁出现的消息元素保留数字1到15。请记住为将来可能添加的常用元素留出一些空间</p><p>您可以指定的最小字段数为1，最大字段数为 229 - 1或536,870,911。您也不能使用数字19000到19999，因为它们是为Protocol Buffers实现保留的 - 如果您使用其中一个保留号码，Protocol Buffers编译器会报错。同样，您不能使用任何以前保留的字段编号。</p><h4 id="字段规则"><a href="#字段规则" class="headerlink" title="字段规则"></a>字段规则</h4><ul><li><p>singular：格式良好的消息可以包含该字段中的零个或一个（但不超过一个）。</p></li><li><p>repeated：在格式良好的消息中重复任意次数（包括零）。将保留重复值的顺序。</p></li></ul><p>在proto3中，repeated标量数字类型的字段为默认使用编码。</p><h4 id="添加更多消息类型"><a href="#添加更多消息类型" class="headerlink" title="添加更多消息类型"></a>添加更多消息类型</h4><p>可以在单个.proto文件中定义多种消息类型。如果要定义多个相关消息，这很有用。</p><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf"><span class="token keyword">message</span> <span class="token class-name">SearchRequest</span> <span class="token punctuation">{</span>  <span class="token builtin">string</span> query <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>  <span class="token builtin">int32</span> page_number <span class="token operator">=</span> <span class="token number">2</span><span class="token punctuation">;</span>  <span class="token builtin">int32</span> result_per_page <span class="token operator">=</span> <span class="token number">3</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token keyword">message</span> <span class="token class-name">SearchResponse</span> <span class="token punctuation">{</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="保留字段"><a href="#保留字段" class="headerlink" title="保留字段"></a>保留字段</h4><p>保留变量不被使用</p><p>如果通过完全删除字段或将其注释来更新消息类型，则未来用户可以在对类型进行自己的更新时重用字段编号。如果以后加载相同的旧版本，这可能会导致严重问题，包括数据损坏，隐私错误等。确保不会发生这种情况的一种方法是<strong>指定已删除字段的字段编号（和/或名称，这也可能导致JSON序列化问题）reserved</strong>。如果将来的任何用户尝试使用这些字段标识符，协议缓冲编译器将会报错。</p><pre class="line-numbers language-none"><code class="language-none">message Foo {  reserved 2, 15, 9 to 11;  reserved "foo", "bar";}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h4 id="默认值"><a href="#默认值" class="headerlink" title="默认值"></a>默认值</h4><p>解析消息时，如果编码消息不包含特定的单数元素，则解析对象中的相应字段将设置为该字段的默认值。</p><p><strong>这些默认值是特定于类型</strong>：</p><ul><li><p>对于字符串，默认值为空字符串。</p></li><li><p>对于字节，默认值为空字节。</p></li><li><p>对于bools，默认值为false。</p></li><li><p>对于数字类型，默认值为零。</p></li><li><p>对于枚举，默认值是第一个定义的枚举值，该值必须为0。</p></li><li><p>对于消息字段，未设置该字段。它的确切值取决于语言。</p></li><li><p>重复字段的默认值为空（通常是相应语言的空列表）。</p></li></ul><h4 id="导入定义"><a href="#导入定义" class="headerlink" title="导入定义"></a>导入定义</h4><p>默认情况下，您只能使用直接导入.proto文件中的定义。但是，有时您可能需要将.proto文件移动到新位置。.proto现在，您可以.proto在旧位置放置一个虚拟文件，以使用该import public概念将所有导入转发到新位置，而不是直接移动文件并在一次更改中更新所有调用站点。import public任何导入包含该import public语句的proto的人都可以传递依赖关系。</p><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf"><span class="token comment">// new.proto</span><span class="token comment">//所有定义都移到这里</span><span class="token comment">// old.proto</span><span class="token comment">//这是所有客户端都要导入的原型。</span><span class="token keyword">import</span> <span class="token keyword">public</span> <span class="token string">"new.proto"</span><span class="token punctuation">;</span><span class="token keyword">import</span> <span class="token string">"other.proto”"</span>"<span class="token comment">// client.proto</span><span class="token keyword">import</span> <span class="token string">"old.proto"</span><span class="token punctuation">;</span><span class="token comment">//您使用old.proto和new.proto中的定义，但不使用other.proto</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="嵌套类型"><a href="#嵌套类型" class="headerlink" title="嵌套类型"></a>嵌套类型</h4><p>你可以在其他消息类型中定义、使用消息类型</p><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf"><span class="token keyword">message</span> <span class="token class-name">SearchResponse</span> <span class="token punctuation">{</span>  <span class="token keyword">message</span> <span class="token class-name">Result</span> <span class="token punctuation">{</span>    <span class="token builtin">string</span> url <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>    <span class="token builtin">string</span> title <span class="token operator">=</span> <span class="token number">2</span><span class="token punctuation">;</span>    <span class="token keyword">repeated</span> <span class="token builtin">string</span> snippets <span class="token operator">=</span> <span class="token number">3</span><span class="token punctuation">;</span>  <span class="token punctuation">}</span>  <span class="token keyword">repeated</span> <span class="token positional-class-name class-name">Result</span> results <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="map"><a href="#map" class="headerlink" title="map"></a>map</h4><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf">map <span class="token operator">&lt;</span> key_type ，value_type <span class="token operator">&gt;</span> map_field <span class="token operator">=</span> N <span class="token punctuation">;</span><span class="token comment">// key_type可以是任何整数或字符串类型</span><span class="token comment">// value_type可以是任何类型(除了map)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>如果要创建项目映射，其中每条Project消息都与字符串键相关联</p><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf">map <span class="token operator">&lt;</span> <span class="token builtin">string</span> ，Project <span class="token operator">&gt;</span> projects <span class="token operator">=</span> <span class="token number">3</span> <span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="定义服务"><a href="#定义服务" class="headerlink" title="定义服务"></a>定义服务</h3><p>Proto Buffers 可以使用在RPC（远程过程调用）环境中。 要定义RPC方法，需要使用service来定义服务，并在里面定义方法。</p><p>Proto Buffers 编译器会根据选择的语言生成服务接口代码和存根（Stub）</p><pre class="line-numbers language-protobuf" data-language="protobuf"><code class="language-protobuf"><span class="token keyword">service</span> <span class="token class-name">SearchService</span> <span class="token punctuation">{</span>  <span class="token keyword">rpc</span> <span class="token function">Search</span> <span class="token punctuation">(</span><span class="token class-name">SearchRequest</span><span class="token punctuation">)</span> <span class="token keyword">returns</span> <span class="token punctuation">(</span><span class="token class-name">SearchResponse</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h2 id="多语言使用"><a href="#多语言使用" class="headerlink" title="多语言使用"></a>多语言使用</h2><h3 id="Golang"><a href="#Golang" class="headerlink" title="Golang"></a>Golang</h3>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【go每日一库】gRPC</title>
      <link href="/2021/12/03/grpc/rpc-grpc/"/>
      <url>/2021/12/03/grpc/rpc-grpc/</url>
      
        <content type="html"><![CDATA[<h1 id="【go每日一库】gRPC"><a href="#【go每日一库】gRPC" class="headerlink" title="【go每日一库】gRPC"></a>【go每日一库】gRPC</h1><blockquote><p>grpc 官网： <a href="https://grpc.io/">https://grpc.io/</a></p><p>github address: <a href="https://github.com/grpc">https://github.com/grpc</a></p><p>grpc doc: <a href="http://doc.oschina.net/grpc">http://doc.oschina.net/grpc</a></p></blockquote><h2 id="gRPC-简介"><a href="#gRPC-简介" class="headerlink" title="gRPC&nbsp;简介"></a>gRPC&nbsp;简介</h2><p>在 gRPC 里<em>客户端</em>应用可以像调用本地对象一样直接调用另一台不同的机器上<em>服务端</em>应用的方法，使得您能够更容易地创建分布式应用和服务。与许多 RPC 系统类似，gRPC 也是基于以下理念：定义一个<em>服务</em>，指定其能够被远程调用的方法（包含参数和返回类型）。在服务端实现这个接口，并运行一个 gRPC 服务器来处理客户端调用。在客户端拥有一个<em>存根</em>能够像服务端一样的方法。</p><p>gRPC 客户端和服务端可以在多种环境中运行和交互 - 从 google 内部的服务器到你自己的笔记本，并且可以用任何 gRPC支持的语言来编写。此外，Google 最新 API 将有 gRPC 版本的接口，使你很容易地将 Google 的功能集成到你的应用里。</p><h2 id="gRPC概念"><a href="#gRPC概念" class="headerlink" title="gRPC概念"></a>gRPC概念</h2><h3 id="服务定义"><a href="#服务定义" class="headerlink" title="服务定义"></a>服务定义</h3><p>gRPC 基于如下思想：定义一个服务， 指定其可以被远程调用的方法及其参数和返回类型。gRPC 默认使用protocol buffers作为接口定义语言，来描述服务接口和有效载荷消息结构。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【go每日一库】Beego</title>
      <link href="/2021/12/03/golang/beego/go-mei-ri-yi-ku-beego/"/>
      <url>/2021/12/03/golang/beego/go-mei-ri-yi-ku-beego/</url>
      
        <content type="html"><![CDATA[<h1 id="【go每日一库】Beego"><a href="#【go每日一库】Beego" class="headerlink" title="【go每日一库】Beego"></a>【go每日一库】Beego</h1><blockquote><p>beego官网： <a href="https://beego.me/">https://beego.me/</a></p><p>beego github 地址: <a href="https://github.com/beego/beego/">https://github.com/beego/beego/</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【监控】Loki</title>
      <link href="/2021/12/02/log-plateform/loki/jian-kong-loki/"/>
      <url>/2021/12/02/log-plateform/loki/jian-kong-loki/</url>
      
        <content type="html"><![CDATA[<h1 id="【监控】Loki"><a href="#【监控】Loki" class="headerlink" title="【监控】Loki"></a>【监控】Loki</h1><blockquote><p>Loki github地址：<a href="https://github.com/grafana/loki">https://github.com/grafana/loki</a></p></blockquote><h2 id="Loki简介"><a href="#Loki简介" class="headerlink" title="Loki简介"></a>Loki简介</h2><p>Loki是受Prometheus启发由Grafana Labs团队开源的水平可扩展，高度可用的多租户日志聚合系统。 它的设计具有很高的成本效益，并且易于操作。使用标签来作为索引，而不是对全文进行检索，也就是说，你通过这些标签既可以查询日志的内容也可以查询到监控的数据签，极大地降低了日志索引的存储。</p><h3 id="系统架构"><a href="#系统架构" class="headerlink" title="系统架构"></a>系统架构</h3><ul><li>Loki 是主服务器，负责存储日志和处理查询 。</li><li>promtail 是代理，负责收集日志并将其发送给 loki（只收集日志，不在本地进行运算） 。</li><li>Grafana 用于 UI 展示。</li></ul><p>只要在被监控服务器上安装promtail来收集日志然后发送给Loki存储，就可以在Grafana UI界面通过添加Loki为数据源进行日志查询（如果Loki服务器性能不够，可以部署多个Loki进行存储及查询）。作为一个日志系统不关只有查询分析日志的能力，还能对日志进行监控和报警</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Prometheus】PromQL</title>
      <link href="/2021/12/02/monitor/prometheus/promql/prometheus-promql/"/>
      <url>/2021/12/02/monitor/prometheus/promql/prometheus-promql/</url>
      
        <content type="html"><![CDATA[<h1 id="PromQL"><a href="#PromQL" class="headerlink" title="PromQL"></a>PromQL</h1><blockquote></blockquote><h2 id="PromQL简介"><a href="#PromQL简介" class="headerlink" title="PromQL简介"></a>PromQL简介</h2><p>PromQL (Prometheus Query Language) 是 Prometheus 自己开发的数据查询 DSL 语言，语言表现力非常丰富，内置函数很多，在日常数据可视化以及rule 告警中都会使用到它</p><h2 id="PromQL使用"><a href="#PromQL使用" class="headerlink" title="PromQL使用"></a>PromQL使用</h2><p><strong>字符串</strong>: 在查询语句中，字符串往往作为查询条件 labels 的值，和 Golang 字符串语法一致，可以使用 “”, ‘’, 或者 `` , 格式如：</p><pre class="line-numbers language-none"><code class="language-none">- "this is a string"- 'these are unescaped: \n \\ \t'- `these are not unescaped: \n ' " \t`<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h3 id="查询结果类型"><a href="#查询结果类型" class="headerlink" title="查询结果类型"></a>查询结果类型</h3><ul><li><strong>瞬时数据</strong> (Instant vector): 包含一组时序，每个时序只有一个<code>http_requests_total</code></li><li><strong>区间数据</strong> (Range vector): 包含一组时序,每个时序有多个点<code>http_requests_total[5m]</code></li><li><strong>纯量数据</strong> (Scalar): 纯量只有一个数字，没有时序<code>count(http_requests_total)</code></li></ul><h3 id="查询条件"><a href="#查询条件" class="headerlink" title="查询条件"></a>查询条件</h3><blockquote><p>Prometheus 存储的是时序数据，而它的时序是由名字和一组标签构成的，其实名字也可以写出标签的形式，例如 <strong>http_requests_total</strong> 等价于 {name=”http_requests_total”}。</p></blockquote><ul><li><p>一个简单的查询相当于是对各种标签的筛选</p><pre class="line-numbers language-none"><code class="language-none">http_requests_total{code="200"}# 表示查询名字为 http_requests_total，code 为 "200" 的数据<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p>查询条件支持正则匹配</p><pre class="line-numbers language-none"><code class="language-none">http_requests_total{code!="200"}     # 表示查询 code 不为 "200" 的数据http_requests_total{code=～"2.."}    # 表示查询 code 为 "2xx" 的数据http_requests_total{code!～"2.."}    # 表示查询 code 不为 "2xx" 的数据<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li></ul><h3 id="操作符"><a href="#操作符" class="headerlink" title="操作符"></a>操作符</h3><ul><li><p>算术运算符</p><pre class="line-numbers language-none"><code class="language-none">支持的算术运算符有 +，-，*，/，%，^, 例如 http_requests_total * 2 表示将 http_requests_total 所有数据 double 一倍。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p>比较运算符</p><pre class="line-numbers language-none"><code class="language-none">支持的比较运算符有 ==，!=，&gt;，&lt;，&gt;=，&lt;=例如 http_requests_total &gt; 100 表示 http_requests_total 结果中大于 100 的数据。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p>逻辑运算符</p><pre class="line-numbers language-none"><code class="language-none">支持的逻辑运算符有 and，or，unless例如 http_requests_total == 5 or http_requests_total == 2 表示 http_requests_total 结果中等于 5 或者 2 的数据。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p>聚合运算符</p><pre class="line-numbers language-none"><code class="language-none">支持的聚合运算符有 sum，min，max，avg，stddev，stdvar，count，count_values，bottomk，topk，quantile例如 max(http_requests_total) 表示 http_requests_total 结果中最大的数据。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li></ul><h3 id="内置函数"><a href="#内置函数" class="headerlink" title="内置函数"></a>内置函数</h3><h3 id="promQL进阶语法"><a href="#promQL进阶语法" class="headerlink" title="promQL进阶语法"></a>promQL进阶语法</h3><h4 id="即时矢量选择器"><a href="#即时矢量选择器" class="headerlink" title="即时矢量选择器"></a>即时矢量选择器</h4><ul><li>=：匹配与标签相等的内容</li><li>!=：不匹配与标签相等的内容</li><li>=~: 根据正则表达式匹配与标签符合的内容</li><li>!~：根据正则表达式不匹配与标签符合的内容</li><li>向量选择器必须指定一个名称或至少一个与空字符串不匹配的标签匹配器</li></ul><h4 id="范围矢量选择器"><a href="#范围矢量选择器" class="headerlink" title="范围矢量选择器"></a>范围矢量选择器</h4><ul><li>持续时间仅限于数字</li></ul><h4 id="偏移量修改器"><a href="#偏移量修改器" class="headerlink" title="偏移量修改器"></a>偏移量修改器</h4><ul><li>偏移修改器允许更改查询中各个即时和范围向量的时间偏移</li></ul><h3 id="二元运算符"><a href="#二元运算符" class="headerlink" title="二元运算符"></a>二元运算符</h3>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Go标准库】flag</title>
      <link href="/2021/12/02/golang/standard-library/go-biao-zhun-ku-flag/"/>
      <url>/2021/12/02/golang/standard-library/go-biao-zhun-ku-flag/</url>
      
        <content type="html"><![CDATA[<h1 id="【Go标准库】flag"><a href="#【Go标准库】flag" class="headerlink" title="【Go标准库】flag"></a>【Go标准库】flag</h1><blockquote><p>Flag 文档：<a href="https://pkg.go.dev/flag@go1.17.3">https://pkg.go.dev/flag@go1.17.3</a></p></blockquote><h2 id="flag简介"><a href="#flag简介" class="headerlink" title="flag简介"></a>flag简介</h2><p><code>flag</code>用于解析命令行选项</p><h2 id=""><a href="#" class="headerlink" title=""></a></h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【微信】微信公众号</title>
      <link href="/2021/12/02/wechat/wei-xin-wei-xin-gong-zhong-hao/"/>
      <url>/2021/12/02/wechat/wei-xin-wei-xin-gong-zhong-hao/</url>
      
        <content type="html"><![CDATA[<h1 id="【微信】微信公众号"><a href="#【微信】微信公众号" class="headerlink" title="【微信】微信公众号"></a>【微信】微信公众号</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【微信】微信小游戏</title>
      <link href="/2021/12/02/wechat/wei-xin-wei-xin-xiao-you-xi/"/>
      <url>/2021/12/02/wechat/wei-xin-wei-xin-xiao-you-xi/</url>
      
        <content type="html"><![CDATA[<h1 id="【微信】微信小游戏"><a href="#【微信】微信小游戏" class="headerlink" title="【微信】微信小游戏"></a>【微信】微信小游戏</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【微信】微信小程序</title>
      <link href="/2021/12/02/wechat/wei-xin-wei-xin-xiao-cheng-xu/"/>
      <url>/2021/12/02/wechat/wei-xin-wei-xin-xiao-cheng-xu/</url>
      
        <content type="html"><![CDATA[<h1 id="【微信】微信小程序"><a href="#【微信】微信小程序" class="headerlink" title="【微信】微信小程序"></a>【微信】微信小程序</h1><blockquote><p>微信小程序：<a href="https://developers.weixin.qq.com/miniprogram/dev/framework/">https://developers.weixin.qq.com/miniprogram/dev/framework/</a></p></blockquote><h2 id="小程序简介"><a href="#小程序简介" class="headerlink" title="小程序简介"></a>小程序简介</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【前端】微信</title>
      <link href="/2021/12/02/wechat/qian-duan-wei-xin/"/>
      <url>/2021/12/02/wechat/qian-duan-wei-xin/</url>
      
        <content type="html"><![CDATA[<h1 id="【前端】微信"><a href="#【前端】微信" class="headerlink" title="【前端】微信"></a>【前端】微信</h1><blockquote><p>微信API文档：<a href="https://developers.weixin.qq.com/doc/">https://developers.weixin.qq.com/doc/</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【每日一库】go-echarts</title>
      <link href="/2021/12/01/golang/third-party-library/go-mei-ri-yi-ku-go-echarts/"/>
      <url>/2021/12/01/golang/third-party-library/go-mei-ri-yi-ku-go-echarts/</url>
      
        <content type="html"><![CDATA[<h1 id="【每日一库】go-echarts"><a href="#【每日一库】go-echarts" class="headerlink" title="【每日一库】go-echarts"></a>【每日一库】go-echarts</h1><blockquote><p>github 地址：<a href="https://github.com/go-echarts/go-echarts">https://github.com/go-echarts/go-echarts</a></p></blockquote><h2 id="go-echarts简介"><a href="#go-echarts简介" class="headerlink" title="go echarts简介"></a>go echarts简介</h2><h2 id="go-echarts环境配置"><a href="#go-echarts环境配置" class="headerlink" title="go echarts环境配置"></a>go echarts环境配置</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># GOPATH</span>go get -u github.com/go-echarts/go-echarts/<span class="token punctuation">..</span>.<span class="token comment"># 因为 gomod 的特殊的版本管理方式，使用 go get 方式并不能直接使用 v2 go-echarts 🐶</span><span class="token comment"># 不过可以通过以下方法使用新版本...</span><span class="token builtin class-name">cd</span> <span class="token variable">$go</span>-echarts-project<span class="token function">mkdir</span> v2 <span class="token operator">&amp;&amp;</span> <span class="token function">mv</span> charts components datasets opts render templates types v2<span class="token comment"># GOMOD</span>github.com/go-echarts/go-echarts/v2 v2.2.5-0.20211021024243-33ae1aa415d6<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【可视化图表】Echarts</title>
      <link href="/2021/12/01/frontend/echarts/ke-shi-hua-tu-biao-echarts/"/>
      <url>/2021/12/01/frontend/echarts/ke-shi-hua-tu-biao-echarts/</url>
      
        <content type="html"><![CDATA[<h1 id="可视化图表】Echarts"><a href="#可视化图表】Echarts" class="headerlink" title="可视化图表】Echarts"></a>可视化图表】Echarts</h1><blockquote><p>echarts 官网：<a href="https://echarts.apache.org/zh/index.html">https://echarts.apache.org/zh/index.html</a></p><p>echarts github地址：<a href="https://github.com/apache/echarts">https://github.com/apache/echarts</a></p></blockquote><h2 id="Echarts简介"><a href="#Echarts简介" class="headerlink" title="Echarts简介"></a>Echarts简介</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Mysql数据归档】percona-toolkit</title>
      <link href="/2021/12/01/database/mysql/mysql-shu-ju-gui-dang-pt-achiver/"/>
      <url>/2021/12/01/database/mysql/mysql-shu-ju-gui-dang-pt-achiver/</url>
      
        <content type="html"><![CDATA[<h1 id="【Mysql数据归档】percona-toolkit"><a href="#【Mysql数据归档】percona-toolkit" class="headerlink" title="【Mysql数据归档】percona-toolkit"></a>【Mysql数据归档】percona-toolkit</h1><blockquote><p>官网：<a href="https://www.percona.com/doc/percona-toolkit/3.0/pt-archiver.html">https://www.percona.com/doc/percona-toolkit/3.0/pt-archiver.html</a></p><p>github地址：<a href="https://github.com/percona/percona-toolkit">https://github.com/percona/percona-toolkit</a></p></blockquote><h2 id="pt-achiver简介"><a href="#pt-achiver简介" class="headerlink" title="pt-achiver简介"></a>pt-achiver简介</h2><p>工作上需要删除或则归档一张大表，其不仅可以归档数据，还有删除、导出到文件等功能。并且在主从架构当中，可以兼顾从库（一个或则多个）进行归档，避免归档、删除数据时候压力太大，造成从库的延迟。该工具的目标是一个低影响，从表中剔除旧数据，而不会影响OLTP查询。也可以将数据插入到另一个表中，该表不必位于同一服务器上。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Mysql数据归档】pt-achiver</title>
      <link href="/2021/12/01/database/mysql/mysql-shu-ju-gui-dang-percona-toolkit/"/>
      <url>/2021/12/01/database/mysql/mysql-shu-ju-gui-dang-percona-toolkit/</url>
      
        <content type="html"><![CDATA[<h1 id="【Mysql数据归档】pt-achiver"><a href="#【Mysql数据归档】pt-achiver" class="headerlink" title="【Mysql数据归档】pt-achiver"></a>【Mysql数据归档】pt-achiver</h1><blockquote><p>官网：<a href="https://www.percona.com/doc/percona-toolkit/3.0/pt-archiver.html">https://www.percona.com/doc/percona-toolkit/3.0/pt-archiver.html</a></p><p>github地址：<a href="https://github.com/percona/percona-toolkit">https://github.com/percona/percona-toolkit</a></p></blockquote><h2 id="pt-achiver简介"><a href="#pt-achiver简介" class="headerlink" title="pt-achiver简介"></a>pt-achiver简介</h2><p>工作上需要删除或则归档一张大表，其不仅可以归档数据，还有删除、导出到文件等功能。并且在主从架构当中，可以兼顾从库（一个或则多个）进行归档，避免归档、删除数据时候压力太大，造成从库的延迟。该工具的目标是一个低影响，从表中剔除旧数据，而不会影响OLTP查询。也可以将数据插入到另一个表中，该表不必位于同一服务器上。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【列式数据库】HBase</title>
      <link href="/2021/11/30/database/hbase/lie-shi-shu-ju-ku-hbase/"/>
      <url>/2021/11/30/database/hbase/lie-shi-shu-ju-ku-hbase/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【大数据】Hive</title>
      <link href="/2021/11/30/bigdata/hive/da-shu-ju-hive/"/>
      <url>/2021/11/30/bigdata/hive/da-shu-ju-hive/</url>
      
        <content type="html"><![CDATA[<h1 id="【大数据】Hive"><a href="#【大数据】Hive" class="headerlink" title="【大数据】Hive"></a>【大数据】Hive</h1><h2 id="hive简介"><a href="#hive简介" class="headerlink" title="hive简介"></a>hive简介</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【大数据】Yarn</title>
      <link href="/2021/11/30/bigdata/yarn/da-shu-ju-yarn/"/>
      <url>/2021/11/30/bigdata/yarn/da-shu-ju-yarn/</url>
      
        <content type="html"><![CDATA[<h1 id="【大数据】Yarn"><a href="#【大数据】Yarn" class="headerlink" title="【大数据】Yarn"></a>【大数据】Yarn</h1><h2 id="yarn简介"><a href="#yarn简介" class="headerlink" title="yarn简介"></a>yarn简介</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【文件系统】Ceph</title>
      <link href="/2021/11/30/storage/ceph/wen-jian-xi-tong-ceph/"/>
      <url>/2021/11/30/storage/ceph/wen-jian-xi-tong-ceph/</url>
      
        <content type="html"><![CDATA[<h1 id="【文件系统】Ceph"><a href="#【文件系统】Ceph" class="headerlink" title="【文件系统】Ceph"></a>【文件系统】Ceph</h1><blockquote><p>ceph官网：<a href="https://ceph.com/en/">https://ceph.com/en/</a></p><p>ceph中文文档：<a href="http://docs.ceph.org.cn/">http://docs.ceph.org.cn/</a></p></blockquote><h2 id="ceph简介"><a href="#ceph简介" class="headerlink" title="ceph简介"></a>ceph简介</h2><p>Ceph 独一无二地在一个统一的系统中同时提供了<strong>对象、块、和文件存储功能</strong>。</p><ul><li><strong>Ceph OSDs</strong>: Ceph OSD 守护进程（ Ceph OSD ）的功能是存储数据，处理数据的复制、恢复、回填、再均衡，并通过检查其他OSD 守护进程的心跳来向 Ceph Monitors 提供一些监控信息。当 Ceph 存储集群设定为有2个副本时，至少需要2个 OSD 守护进程，集群才能达到 <code>active+clean</code> 状态（ Ceph 默认有3个副本，但你可以调整副本数）。</li><li><strong>Monitors</strong>: Ceph Monitor维护着展示集群状态的各种图表，包括监视器图、 OSD 图、归置组（ PG ）图、和 CRUSH 图。 Ceph 保存着发生在Monitors 、 OSD 和 PG上的每一次状态变更的历史信息（称为 epoch ）。</li><li><strong>MDSs</strong>: Ceph 元数据服务器（ MDS ）为Ceph 文件系统存储元数据（Ceph 块设备和 Ceph 对象存储不使用MDS ）。元数据服务器使得 POSIX 文件系统的用户们，可以在不对 Ceph 存储集群造成负担的前提下，执行诸如 <code>ls</code>、<code>find</code> 等基本命令。</li></ul><p>Ceph 把客户端数据保存为存储池内的对象。通过使用 CRUSH 算法， Ceph 可以计算出哪个归置组（PG）应该持有指定的对象(Object)，然后进一步计算出哪个 OSD 守护进程持有该归置组。 CRUSH 算法使得 Ceph 存储集群能够动态地伸缩、再均衡和修复</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【文件系统】HDFS</title>
      <link href="/2021/11/30/storage/hdfs/wen-jian-xi-tong-hdfs/"/>
      <url>/2021/11/30/storage/hdfs/wen-jian-xi-tong-hdfs/</url>
      
        <content type="html"><![CDATA[<h1 id="【文件系统】HDFS"><a href="#【文件系统】HDFS" class="headerlink" title="【文件系统】HDFS"></a>【文件系统】HDFS</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【大数据】Hadoop</title>
      <link href="/2021/11/30/bigdata/hadoop/da-shu-ju-hadoop/"/>
      <url>/2021/11/30/bigdata/hadoop/da-shu-ju-hadoop/</url>
      
        <content type="html"><![CDATA[<h1 id="【大数据】Hadoop"><a href="#【大数据】Hadoop" class="headerlink" title="【大数据】Hadoop"></a>【大数据】Hadoop</h1><blockquote><p>hadoop官网： <a href="https://hadoop.apache.org/">https://hadoop.apache.org/</a></p></blockquote><h2 id="Hadoop简介"><a href="#Hadoop简介" class="headerlink" title="Hadoop简介"></a>Hadoop简介</h2><p>Hadoop 3.2.1</p><p>Zeppelin 0.9.0</p><p>Livy 0.7.1</p><p>JupyterHub 1.4.1</p><p>Tez 0.9.2</p><p>Flink 1.13.1</p><p>Ganglia 3.7.2</p><p>HBase 2.4.4</p><p>Pig 0.17.0</p><p>Hive 3.1.2</p><p>Presto 0.254.1</p><p>ZooKeeper 3.5.7</p><p>JupyterEnterpriseGateway 2.1.0</p><p>MXNet 1.8.0</p><p>Sqoop 1.4.7</p><p>Hue 4.9.0</p><p>Phoenix 5.1.2</p><p>Trino 359</p><p>Oozie 5.2.1</p><p>Spark 3.1.2</p><p>HCatalog 3.1.2</p><p>TensorFlow 2.4.1</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【对象存储】Minio</title>
      <link href="/2021/11/30/storage/minio/dui-xiang-cun-chu-minio/"/>
      <url>/2021/11/30/storage/minio/dui-xiang-cun-chu-minio/</url>
      
        <content type="html"><![CDATA[<h1 id="【对象存储】Minio"><a href="#【对象存储】Minio" class="headerlink" title="【对象存储】Minio"></a>【对象存储】Minio</h1><blockquote><p>中文官网：<a href="http://www.minio.org.cn/">http://www.minio.org.cn/</a></p><p>英文官网：<a href="https://min.io/">https://min.io/</a></p><p>GitHub 地址：<a href="https://github.com/minio/">https://github.com/minio/</a></p></blockquote><h2 id="minio简介"><a href="#minio简介" class="headerlink" title="minio简介"></a>minio简介</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Go标准库】context</title>
      <link href="/2021/11/29/golang/standard-library/go-biao-zhun-ku-context/"/>
      <url>/2021/11/29/golang/standard-library/go-biao-zhun-ku-context/</url>
      
        <content type="html"><![CDATA[<h1 id="【Go标准库】context"><a href="#【Go标准库】context" class="headerlink" title="【Go标准库】context"></a>【Go标准库】context</h1><blockquote><p><a href="https://pkg.go.dev/context@go1.17.3">https://pkg.go.dev/context@go1.17.3</a></p></blockquote><pre class="line-numbers language-none"><code class="language-none">import "context"<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><code>func WithCancel(parent Context) (ctx Context, cancel CancelFunc)</code></li><li><code>func WithDeadline(parent Context, d time.Time) (Context, CancelFunc)</code></li><li><code>func WithTimeout(parent Context, timeout time.Duration) (Context, CancelFunc)</code></li><li>``</li></ul><pre class="line-numbers language-none"><code class="language-none">type CancelFunctype Contextfunc Background() Contextfunc TODO() Contextfunc WithValue(parent Context, key, val interface{}) Context<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Go标准库】builtin</title>
      <link href="/2021/11/29/golang/standard-library/go-biao-zhun-ku-builtin/"/>
      <url>/2021/11/29/golang/standard-library/go-biao-zhun-ku-builtin/</url>
      
        <content type="html"><![CDATA[<h1 id="【Go标准库】builtin"><a href="#【Go标准库】builtin" class="headerlink" title="【Go标准库】builtin"></a>【Go标准库】builtin</h1><blockquote><p><a href="https://pkg.go.dev/builtin@go1.17.3">https://pkg.go.dev/builtin@go1.17.3</a></p></blockquote><ul><li><p><code>func append(slice []Type, elems ...Type) []Type</code></p><p>添加元素到切片的末尾，如果切片容量不足，将会分配新的底层数组并添加元素返回新的切片。因此，有必要将附加结果存储在持有切片本身的变量中</p><p><strong>代码使用示例</strong></p><pre class="line-numbers language-go" data-language="go"><code class="language-go">  <span class="token string">``</span><span class="token string">`  - `</span><span class="token keyword">func</span> <span class="token function">cap</span><span class="token punctuation">(</span>v Type<span class="token punctuation">)</span> <span class="token builtin">int</span><span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">close</span><span class="token punctuation">(</span>c <span class="token keyword">chan</span><span class="token operator">&lt;-</span> Type<span class="token punctuation">)</span><span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">complex</span><span class="token punctuation">(</span>r<span class="token punctuation">,</span> i FloatType<span class="token punctuation">)</span> ComplexType<span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">copy</span><span class="token punctuation">(</span>dst<span class="token punctuation">,</span> src <span class="token punctuation">[</span><span class="token punctuation">]</span>Type<span class="token punctuation">)</span> <span class="token builtin">int</span><span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">delete</span><span class="token punctuation">(</span>m <span class="token keyword">map</span><span class="token punctuation">[</span>Type<span class="token punctuation">]</span>Type1<span class="token punctuation">,</span> key Type<span class="token punctuation">)</span><span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">imag</span><span class="token punctuation">(</span>c ComplexType<span class="token punctuation">)</span> FloatType<span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">len</span><span class="token punctuation">(</span>v Type<span class="token punctuation">)</span> <span class="token builtin">int</span><span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">make</span><span class="token punctuation">(</span>t Type<span class="token punctuation">,</span> size <span class="token operator">...</span>IntegerType<span class="token punctuation">)</span> Type<span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">new</span><span class="token punctuation">(</span>Type<span class="token punctuation">)</span> <span class="token operator">*</span>Type<span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">panic</span><span class="token punctuation">(</span>v <span class="token keyword">interface</span><span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">print</span><span class="token punctuation">(</span>args <span class="token operator">...</span>Type<span class="token punctuation">)</span><span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">println</span><span class="token punctuation">(</span>args <span class="token operator">...</span>Type<span class="token punctuation">)</span><span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">real</span><span class="token punctuation">(</span>c ComplexType<span class="token punctuation">)</span> FloatType<span class="token string">`- `</span><span class="token keyword">func</span> <span class="token function">recover</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">interface</span><span class="token punctuation">{</span><span class="token punctuation">}</span>`<span class="token operator">-</span> <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>type ComplexType<br>type FloatType<br>type IntegerType<br>type Type<br>type Type1<br>type bool<br>type byte<br>type complex128<br>type complex64<br>type error<br>type float32<br>type float64<br>type int<br>type int16<br>type int32<br>type int64<br>type int8<br>type rune<br>type string<br>type uint<br>type uint16<br>type uint32<br>type uint64<br>type uint8<br>type uintptr<br>```</p></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Go标准库】time</title>
      <link href="/2021/11/29/golang/standard-library/go-biao-zhun-ku-time/"/>
      <url>/2021/11/29/golang/standard-library/go-biao-zhun-ku-time/</url>
      
        <content type="html"><![CDATA[<h1 id="Go标准库】time"><a href="#Go标准库】time" class="headerlink" title="Go标准库】time"></a>Go标准库】time</h1><blockquote><p><a href="https://pkg.go.dev/time@go1.17.3">https://pkg.go.dev/time@go1.17.3</a></p></blockquote><p>提供测量和显示时间的功能</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">import</span> <span class="token string">"time"</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><code>func After(d Duration) &lt;-chan Time</code></li><li><code>func Sleep(d Duration)</code></li><li></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Go标准库】os</title>
      <link href="/2021/11/29/golang/standard-library/go-biao-zhun-ku-os/"/>
      <url>/2021/11/29/golang/standard-library/go-biao-zhun-ku-os/</url>
      
        <content type="html"><![CDATA[<h1 id="【Go标准库】os"><a href="#【Go标准库】os" class="headerlink" title="【Go标准库】os"></a>【Go标准库】os</h1><blockquote><p><a href="https://pkg.go.dev/os@go1.17.3">https://pkg.go.dev/os@go1.17.3</a></p></blockquote><p>提供了对操作系统功能的非平台相关访问接口。接口为Unix风格。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">import</span> <span class="token string">"os"</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><p><code>os.Hostname()</code>：<strong>返回内核报告的主机名</strong></p><p><strong>代码使用示例</strong></p><pre class="line-numbers language-go" data-language="go"><code class="language-go">  hostname<span class="token punctuation">,</span> err <span class="token operator">:=</span> os<span class="token punctuation">.</span><span class="token function">Hostname</span><span class="token punctuation">(</span><span class="token punctuation">)</span>  <span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>    <span class="token keyword">return</span>  <span class="token punctuation">}</span>  fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span>hostname<span class="token punctuation">)</span><span class="token comment">// AndrewWandeMacBook-Pro.local</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>  <strong>源码解读</strong></p>  <pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token string">``</span><span class="token string">`- `</span>os<span class="token punctuation">.</span><span class="token function">Setenv</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token string">`: **设置环境变量**  **代码使用示例**  `</span><span class="token string">``</span><span class="token keyword">go</span>  <span class="token comment">// 同时设置多个环境变量，只能用循环设置</span>  err <span class="token operator">:=</span> os<span class="token punctuation">.</span><span class="token function">Setenv</span><span class="token punctuation">(</span><span class="token string">"hello"</span><span class="token punctuation">,</span> <span class="token string">"world"</span><span class="token punctuation">)</span> <span class="token comment">// key不能为空，invalid argument</span>  <span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>  <span class="token function">panic</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span>  <span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>源码解读</strong></p><pre class="line-numbers language-none"><code class="language-none">  <span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><code>os.Getenv()</code>:<strong>获取环境变量</strong></p><p><strong>代码使用示例</strong></p><pre class="line-numbers language-go" data-language="go"><code class="language-go">env <span class="token operator">:=</span> os<span class="token punctuation">.</span><span class="token function">Getenv</span><span class="token punctuation">(</span><span class="token string">"hello"</span><span class="token punctuation">)</span>fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span>env<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><code>os.Unsetenv()</code>:<strong>删除指定的环境变量</strong></p><p><strong>代码使用示例</strong></p><pre class="line-numbers language-go" data-language="go"><code class="language-go">err <span class="token operator">=</span> os<span class="token punctuation">.</span><span class="token function">Unsetenv</span><span class="token punctuation">(</span><span class="token string">"hello"</span><span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span><span class="token function">panic</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p><strong>源码解读</strong></p><pre class="line-numbers language-none"><code class="language-none">  ```  - `os.Environ()`:**获取所有环境变量**  **代码使用示例**  ```go  envs := os.Environ()  fmt.Println(envs)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>源码解读</strong></p><pre class="line-numbers language-none"><code class="language-none">  ```  - `os.Clearenv()`:**清除所有环境变量**  **代码使用示例**  ```go  os.Clearenv()<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>源码解读</strong></p><pre class="line-numbers language-none"><code class="language-none">  ```&gt; 注意：对于环境相关操作来说，只能获取到系统环境变量和当前shell中添加的。其他shell中添加的环境变量无法获取。- `os.Getuid()`:**返回调用者的ID**  **代码使用示例**  ```go  // 谁调用程序，返回谁的ID  uid := os.Getuid()  fmt.Println(uid)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>源码解读</strong></p><pre class="line-numbers language-none"><code class="language-none">  ```  - `os.Geteuid()`:**返回调用者的有效用户ID**  **代码使用示例**  ```go  euid := os.Geteuid()  fmt.Println(euid)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>os.Getgid()</code>:<strong>返回调用者的组ID</strong></p><p><strong>代码使用示例</strong></p><pre class="line-numbers language-go" data-language="go"><code class="language-go">gid <span class="token operator">:=</span> os<span class="token punctuation">.</span><span class="token function">Getgid</span><span class="token punctuation">(</span><span class="token punctuation">)</span>fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span>gid<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><code>os.Getegid()</code>:<strong>返回调用者的有效组ID</strong></p><p><strong>代码使用示例</strong></p><pre class="line-numbers language-go" data-language="go"><code class="language-go">egid <span class="token operator">:=</span> os<span class="token punctuation">.</span><span class="token function">Getegid</span><span class="token punctuation">(</span><span class="token punctuation">)</span>fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span>egid<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><code>os.Getgroups()</code>:<strong>返回调用者所属的组的数字ID列表</strong></p><p><strong>代码使用示例</strong></p><pre class="line-numbers language-go" data-language="go"><code class="language-go">getgroups<span class="token punctuation">,</span> err <span class="token operator">:=</span> os<span class="token punctuation">.</span><span class="token function">Getgroups</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span><span class="token function">panic</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span><span class="token punctuation">}</span>fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span>getgroups<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>os.Exit()</code>:<strong>退出程序，并返回给的状态码</strong></p><p><strong>代码使用示例</strong></p><pre class="line-numbers language-go" data-language="go"><code class="language-go">os<span class="token punctuation">.</span><span class="token function">Exit</span><span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">)</span><span class="token comment">// Process finished with the exit code 2</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><code>os.Getpid()</code>:<strong>返回调用者的进程ID</strong></p><p><strong>代码使用示例</strong></p><pre class="line-numbers language-go" data-language="go"><code class="language-go">pid <span class="token operator">:=</span> os<span class="token punctuation">.</span><span class="token function">Getpid</span><span class="token punctuation">(</span><span class="token punctuation">)</span>fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span>pid<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><code>os.Getppid()</code>:<strong>返回调用者的父进程ID</strong></p><p><strong>代码使用示例</strong></p><pre class="line-numbers language-go" data-language="go"><code class="language-go">ppid <span class="token operator">:=</span> os<span class="token punctuation">.</span><span class="token function">Getppid</span><span class="token punctuation">(</span><span class="token punctuation">)</span>fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span>ppid<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><code>os.Getwd()</code>:</p></li><li><p><code>func Chdir(dir string) error</code>:</p></li><li><p><code>os.Chmod()</code></p></li></ul><p>​    </p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【标准库】JavaScript标准库</title>
      <link href="/2021/11/29/frontend/javascript/biao-zhun-ku-javascript-biao-zhun-ku/"/>
      <url>/2021/11/29/frontend/javascript/biao-zhun-ku-javascript-biao-zhun-ku/</url>
      
        <content type="html"><![CDATA[<h1 id="【标准库】JavaScript标准库"><a href="#【标准库】JavaScript标准库" class="headerlink" title="【标准库】JavaScript标准库"></a>【标准库】JavaScript标准库</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【标准库】Java标准库</title>
      <link href="/2021/11/29/java/standard-library/biao-zhun-ku-java-biao-zhun-ku/"/>
      <url>/2021/11/29/java/standard-library/biao-zhun-ku-java-biao-zhun-ku/</url>
      
        <content type="html"><![CDATA[<h1 id="【标准库】Java标准库"><a href="#【标准库】Java标准库" class="headerlink" title="【标准库】Java标准库"></a>【标准库】Java标准库</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【标准库】Python标准库</title>
      <link href="/2021/11/29/python/standard-library/biao-zhun-ku-python-biao-zhun-ku/"/>
      <url>/2021/11/29/python/standard-library/biao-zhun-ku-python-biao-zhun-ku/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【标准库】Go标准库</title>
      <link href="/2021/11/29/golang/standard-library/biao-zhun-ku-go-biao-zhun-ku/"/>
      <url>/2021/11/29/golang/standard-library/biao-zhun-ku-go-biao-zhun-ku/</url>
      
        <content type="html"><![CDATA[<h1 id="【标准库】Go标准库"><a href="#【标准库】Go标准库" class="headerlink" title="【标准库】Go标准库"></a>【标准库】Go标准库</h1><blockquote><p><a href="https://pkg.go.dev/std">https://pkg.go.dev/std</a></p></blockquote><ul><li><p><strong>achive</strong></p></li><li><p><strong>bufio</strong></p><p>在io的基础上提供了缓存功能。</p></li><li><p><strong>sync</strong></p><p>提供了基本的同步原语。在多个goroutine访问共享资源的时候，需要使用sync中提供的锁机制。</p></li><li><p><strong>os</strong></p><p>提供了对操作系统功能的非平台相关访问接口。接口为Unix风格。提供的功能包括文件操作、进程管理、信号和用户账号等。</p></li><li><p><strong>time</strong></p><p>时间相关的处理</p></li><li><p><strong>fmt</strong></p><p>实现格式化的输入输出操作。</p></li><li><p><strong>io</strong></p><p>实现了一系列非平台相关的IO相关接口和实现</p></li><li><p><strong>strconv</strong></p><p>提供字符串与基本数据类型互转的能力</p></li><li><p><strong>flag</strong></p><p>提供命令行参数的规则定义和传入参数解析的功能。</p></li><li><p><strong>http</strong></p><p>提供web服务</p></li><li><p><strong>string</strong></p><p>处理字符串的一些函数集合，包括合并、查找、分割、比较、后缀检查、索引、大小写处理等等。</p></li><li><p><strong>bytes</strong></p><p>bytes 包提供了对字节切片进行读写操作的一系列函数。</p></li><li><p><strong>reflect</strong></p><p>reflect 包实现了运行时反射，允许程序通过抽象类型操作对象。</p></li><li><p><strong>context</strong></p></li><li><p><strong>path</strong></p></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【每日一库】goquery</title>
      <link href="/2021/11/28/golang/third-party-library/go-mei-ri-yi-ku-goquery/"/>
      <url>/2021/11/28/golang/third-party-library/go-mei-ri-yi-ku-goquery/</url>
      
        <content type="html"><![CDATA[<h1 id="【每日一库】goquery"><a href="#【每日一库】goquery" class="headerlink" title="【每日一库】goquery"></a>【每日一库】goquery</h1><blockquote><p>goquery github地址：<a href="https://github.com/PuerkitoBio/goquery">https://github.com/PuerkitoBio/goquery</a></p></blockquote><h2 id="goquery简介"><a href="#goquery简介" class="headerlink" title="goquery简介"></a>goquery简介</h2><p>goquery是用 Go 语言编写的一个类似于 jQuery 的库。它基于 HTML 解析库net/html和 CSS 库cascadia，提供与 jQuery 相近的接口</p><h2 id="goquery环境配置"><a href="#goquery环境配置" class="headerlink" title="goquery环境配置"></a>goquery环境配置</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">go get -u github.com/PuerkitoBio/goquery<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="goquery使用"><a href="#goquery使用" class="headerlink" title="goquery使用"></a>goquery使用</h2><h3 id="选择器"><a href="#选择器" class="headerlink" title="选择器"></a>选择器</h3><ul><li><p>HTML Element 元素的选择器</p><p>基于a,p等这些HTML的基本元素进行选择。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">"p"</span><span class="token punctuation">)</span> <span class="token comment">// 匹配文档中所有的p标签</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>ID 选择器</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">"#id"</span><span class="token punctuation">)</span>dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">"#title"</span><span class="token punctuation">)</span> <span class="token comment">// 匹配文档中所有的 id=title的内容</span>dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">"p#title"</span><span class="token punctuation">)</span> <span class="token comment">// 多个标签的ID都是title，可以指定某一个标签</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p>Class选择器</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">".class"</span><span class="token punctuation">)</span>dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">".title"</span><span class="token punctuation">)</span> <span class="token comment">// 匹配文档中所有的class=title的元素</span>dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">"p.title"</span><span class="token punctuation">)</span> <span class="token comment">// 多个标签的class都是title，可以指定某一个标签</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p>属性选择器</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">"p[class=content1"</span><span class="token punctuation">]</span> <span class="token comment">// 匹配文档中所有的 p标签的class属性是content1的元素。</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><table><thead><tr><th align="left">选择器</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left">Find(“div[my]”)</td><td align="left">筛选含有my属性的div元素</td></tr><tr><td align="left">Find(“div[my=zh]”)</td><td align="left">筛选my属性为zh的div元素</td></tr><tr><td align="left">Find(“div[my!=zh]”)</td><td align="left">筛选my属性不等于zh的div元素</td></tr><tr><td align="left">Find(“div[my¦=zh]”)</td><td align="left">筛选my属性为zh或者zh-开头的div元素</td></tr><tr><td align="left">Find(“div[my*=zh]”)</td><td align="left">筛选my属性包含zh这个字符串的div元素</td></tr><tr><td align="left">Find(“div[my~=zh]”)</td><td align="left">筛选my属性包含zh这个单词的div元素，单词以空格分开的</td></tr><tr><td align="left">Find(“div[my$=zh]”)</td><td align="left">筛选my属性以zh结尾的div元素，区分大小写</td></tr><tr><td align="left">Find(“div[my^=zh]”)</td><td align="left">筛选my属性以zh开头的div元素，区分大小写</td></tr></tbody></table></li><li><p>parent &gt; child选择器</p><p>​    筛选出某个元素下的子元素</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">"div&gt;p"</span><span class="token punctuation">)</span> <span class="token comment">// 筛选div标签下的p标签</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>element + next 相邻选择器</p><p>如果要筛选的元素没有规律，但是该元素的上一个元素有规律，我们就可以使用这种下一个相邻选择器来进行选择。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token operator">&lt;</span>div<span class="token operator">&gt;</span><span class="token operator">&lt;</span>p my<span class="token operator">=</span><span class="token string">"a"</span><span class="token operator">&gt;</span>a<span class="token operator">&lt;</span><span class="token operator">/</span>p<span class="token operator">&gt;</span><span class="token operator">&lt;</span>p<span class="token operator">&gt;</span>b<span class="token operator">&lt;</span><span class="token operator">/</span>p<span class="token operator">&gt;</span><span class="token operator">&lt;</span>p<span class="token operator">&gt;</span>c<span class="token operator">&lt;</span><span class="token operator">/</span>p<span class="token operator">&gt;</span><span class="token operator">&lt;</span>div<span class="token operator">&gt;</span>dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">"p[my=a]+p"</span><span class="token punctuation">)</span> <span class="token comment">//筛选出p标签属性my的值为a的相邻p标签</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>element~next 兄弟选择器</p><p>需要筛选同一父元素下，不相邻的标签，可以使用兄弟选择器</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">"p[my=a]~p"</span><span class="token punctuation">)</span> <span class="token comment">// 筛选出p标签属性my的值为a的兄弟p标签。</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ul><h3 id="过滤器"><a href="#过滤器" class="headerlink" title="过滤器"></a>过滤器</h3><ul><li><p>:contains过滤器</p><p>筛选出的元素要包含指定的文本</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">dom<span class="token punctuation">.</span><span class="token function">Find</span><span class="token punctuation">(</span><span class="token string">"p:contains(a)"</span><span class="token punctuation">)</span> <span class="token comment">// 筛选出内容包含a的p标签</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>:first-child过滤器 和:first-of-type过滤器</p><p>筛选出的元素要是他们的父元素的第一个子元素，如果不是，则不会被筛选出来。</p><p>:first-child选择器限制的比较死，必须得是第一个子元素，如果该元素前有其他在前面，就不能用:first-child了，这时候:first-of-type就派上用场了，它要求只要是这个类型的第一个就可以。</p></li><li><p>:last-child 和:last-of-type过滤器</p></li><li><p>:nth-child(n) 过滤器</p></li><li><p>:nth-of-type(n) 过滤器</p></li><li><p>:nth-last-child(n) 和:nth-last-of-type(n) 过滤器</p></li><li><p>:only-child 过滤器 和 :only-of-type 过滤器</p><p>筛选出父元素中，只有它自己的一个的元素。</p></li></ul><h3 id="常用方法"><a href="#常用方法" class="headerlink" title="常用方法"></a>常用方法</h3><ul><li><p>类似函数的位置操作</p><ul><li>Find(selection) *Selection //根据选择器查找节点集</li><li>Eq(index int) *Selection //根据索引获取某个节点集</li><li>First() *Selection //获取第一个子节点集</li><li>Last() *Selection //获取最后一个子节点集</li><li>Next() *Selection //获取下一个兄弟节点集</li><li>NextAll() *Selection //获取后面所有兄弟节点集</li><li>Prev() *Selection //前一个兄弟节点集</li><li>Get(index int) *html.Node //根据索引获取一个节点</li><li>Index() int //返回选择对象中第一个元素的位置</li><li>Slice(start, end int) *Selection //根据起始位置获取子节点集</li></ul></li><li><p>循环遍历选择的节点</p><ul><li><p>Each(f func(int, *Selection)) *Selection //遍历</p></li><li><p>EachWithBreak(f func(int, *Selection) bool) *Selection //可中断遍历</p></li><li><p>Map(f func(int, *Selection) string) (result []string) //返回字符串数组</p></li></ul></li><li><p>检测或获取节点属性值</p><ul><li><p>Attr(), RemoveAttr(), SetAttr() //获取，移除，设置属性的值</p></li><li><p>AddClass(), HasClass(), RemoveClass(), ToggleClass()</p></li><li><p>Html() //获取该节点的html</p></li><li><p>Length() //返回该Selection的元素个数</p></li><li><p>Text() //获取该节点的文本值</p></li></ul></li><li><p>在文档树之间来回跳转（常用的查找节点方法）</p><ul><li><p>Children() //返回selection中各个节点下的孩子节点</p></li><li><p>Contents() //获取当前节点下的所有节点</p></li><li><p>Find() //查找获取当前匹配的元素</p></li><li><p>Next() //下一个元素</p></li><li><p>Prev() //上一个元素</p></li></ul></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【爬虫】Crawlab</title>
      <link href="/2021/11/28/golang/web-crawler/pa-chong-crawlab/"/>
      <url>/2021/11/28/golang/web-crawler/pa-chong-crawlab/</url>
      
        <content type="html"><![CDATA[<h1 id="【爬虫】Crawlab"><a href="#【爬虫】Crawlab" class="headerlink" title="【爬虫】Crawlab"></a>【爬虫】Crawlab</h1><blockquote><p>github 地址：<a href="https://github.com/crawlab-team/crawlab">https://github.com/crawlab-team/crawlab</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【爬虫】Colly</title>
      <link href="/2021/11/28/golang/web-crawler/pa-chong-colly/"/>
      <url>/2021/11/28/golang/web-crawler/pa-chong-colly/</url>
      
        <content type="html"><![CDATA[<h1 id="【爬虫】Colly"><a href="#【爬虫】Colly" class="headerlink" title="【爬虫】Colly"></a>【爬虫】Colly</h1><blockquote><p>github 地址：<a href="https://github.com/gocolly/colly">https://github.com/gocolly/colly</a></p><p>官网：<a href="http://go-colly.org/">http://go-colly.org/</a></p></blockquote><h2 id="colly简介"><a href="#colly简介" class="headerlink" title="colly简介"></a>colly简介</h2><p>colly 是 Go 实现的比较有名的一款爬虫框架，而且 Go 在高并发和分布式场景的优势也正是爬虫技术所需要的。它的主要特点是轻量、快速，设计非常优雅，并且分布式的支持也非常简单，易于扩展。</p><h3 id="colly架构"><a href="#colly架构" class="headerlink" title="colly架构"></a>colly架构</h3><ul><li><p><strong>架构特点</strong></p><p>colly的逻辑更像是面向过程编程的， colly的逻辑就是按上面生命周期的顺序管道处理， 只是在不同阶段，加上回调函数进行过滤的时候进行处理。</p></li><li><p><strong>架构图</strong></p></li></ul><h2 id="colly环境配置"><a href="#colly环境配置" class="headerlink" title="colly环境配置"></a>colly环境配置</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">go mod init github.com/ev-opss/ur-spider-evopssgo get -u github.com/gocolly/colly/v2<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h2 id="colly使用"><a href="#colly使用" class="headerlink" title="colly使用"></a>colly使用</h2><h3 id="收集器"><a href="#收集器" class="headerlink" title="收集器"></a>收集器</h3><p>Colly的主要实体是收集器对象。收集器管理网络通信并负责在收集器作业运行时执行附加的回调。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">collector <span class="token operator">:=</span> colly<span class="token punctuation">.</span><span class="token function">NewCollector</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token comment">// 创建默认的收集器</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><p><strong>收集器配置</strong></p><p>通过覆盖收集器的属性，可以在爬虫作业的任何点更改配置。最好的办法是设置一个用户代理切换器，在每个请求上更改用户代理</p></li></ul><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">const</span> letterBytes <span class="token operator">=</span> <span class="token string">"abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ"</span><span class="token keyword">func</span> <span class="token function">RandomString</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token builtin">string</span> <span class="token punctuation">{</span>b <span class="token operator">:=</span> <span class="token function">make</span><span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token builtin">byte</span><span class="token punctuation">,</span> rand<span class="token punctuation">.</span><span class="token function">Intn</span><span class="token punctuation">(</span><span class="token number">10</span><span class="token punctuation">)</span><span class="token operator">+</span><span class="token number">10</span><span class="token punctuation">)</span><span class="token keyword">for</span> i <span class="token operator">:=</span> <span class="token keyword">range</span> b <span class="token punctuation">{</span>b<span class="token punctuation">[</span>i<span class="token punctuation">]</span> <span class="token operator">=</span> letterBytes<span class="token punctuation">[</span>rand<span class="token punctuation">.</span><span class="token function">Intn</span><span class="token punctuation">(</span><span class="token function">len</span><span class="token punctuation">(</span>letterBytes<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">}</span><span class="token keyword">return</span> <span class="token function">string</span><span class="token punctuation">(</span>b<span class="token punctuation">)</span><span class="token punctuation">}</span>c <span class="token operator">:=</span> colly<span class="token punctuation">.</span><span class="token function">NewCollector</span><span class="token punctuation">(</span><span class="token punctuation">)</span>c<span class="token punctuation">.</span><span class="token function">OnRequest</span><span class="token punctuation">(</span><span class="token keyword">func</span><span class="token punctuation">(</span>r <span class="token operator">*</span>colly<span class="token punctuation">.</span>Request<span class="token punctuation">)</span> <span class="token punctuation">{</span>r<span class="token punctuation">.</span>Headers<span class="token punctuation">.</span><span class="token function">Set</span><span class="token punctuation">(</span><span class="token string">"User-Agent"</span><span class="token punctuation">,</span> <span class="token function">RandomString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><p><strong>环境变量配置</strong></p><p>也可以通过环境变量来改变配置，环境变量配置是在 collector 初始化时生效，正式启动后，配置是可以被覆盖的。这样就不需要重新编译了。</p><p>支持的配置项：</p></li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">COLLY_ALLOWED_DOMAINS <span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token punctuation">]</span>string<span class="token punctuation">)</span> 允许的域名，<span class="token punctuation">[</span><span class="token punctuation">]</span>string<span class="token punctuation">{</span><span class="token string">"baidu.com"</span>, <span class="token string">"zhihu.com"</span><span class="token punctuation">}</span>COLLY_CACHE_DIR <span class="token punctuation">(</span>string<span class="token punctuation">)</span> 缓存目录COLLY_DETECT_CHARSET <span class="token punctuation">(</span>y/n<span class="token punctuation">)</span> 是否检测响应编码COLLY_DISABLE_COOKIES <span class="token punctuation">(</span>y/n<span class="token punctuation">)</span> 禁止cookiesCOLLY_DISALLOWED_DOMAINS <span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token punctuation">]</span>string<span class="token punctuation">)</span>禁止的域名COLLY_IGNORE_ROBOTSTXT <span class="token punctuation">(</span>y/n<span class="token punctuation">)</span>是否忽略ROBOTS协议COLLY_FOLLOW_REDIRECTS <span class="token punctuation">(</span>y/n<span class="token punctuation">)</span> 是否允许跳转COLLY_MAX_BODY_SIZE <span class="token punctuation">(</span>int<span class="token punctuation">)</span> 响应体最大字节数COLLY_MAX_DEPTH <span class="token punctuation">(</span>int - <span class="token number">0</span> means infinite<span class="token punctuation">)</span> 访问深度COLLY_PARSE_HTTP_ERROR_RESPONSE <span class="token punctuation">(</span>y/n<span class="token punctuation">)</span> 解析HTTP响应错误COLLY_USER_AGENT <span class="token punctuation">(</span>string<span class="token punctuation">)</span> 用户代理<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><p><strong>HTTP配置</strong></p><pre class="line-numbers language-go" data-language="go"><code class="language-go">c <span class="token operator">:=</span> colly<span class="token punctuation">.</span><span class="token function">NewCollector</span><span class="token punctuation">(</span><span class="token punctuation">)</span>c<span class="token punctuation">.</span><span class="token function">WithTransport</span><span class="token punctuation">(</span><span class="token operator">&amp;</span>http<span class="token punctuation">.</span>Transport<span class="token punctuation">{</span>Proxy<span class="token punctuation">:</span> http<span class="token punctuation">.</span>ProxyFromEnvironment<span class="token punctuation">,</span>DialContext<span class="token punctuation">:</span> <span class="token punctuation">(</span><span class="token operator">&amp;</span>net<span class="token punctuation">.</span>Dialer<span class="token punctuation">{</span>Timeout<span class="token punctuation">:</span>   <span class="token number">30</span> <span class="token operator">*</span> time<span class="token punctuation">.</span>Second<span class="token punctuation">,</span>KeepAlive<span class="token punctuation">:</span> <span class="token number">30</span> <span class="token operator">*</span> time<span class="token punctuation">.</span>Second<span class="token punctuation">,</span>DualStack<span class="token punctuation">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">.</span>DialContext<span class="token punctuation">,</span>MaxIdleConns<span class="token punctuation">:</span>          <span class="token number">100</span><span class="token punctuation">,</span>IdleConnTimeout<span class="token punctuation">:</span>       <span class="token number">90</span> <span class="token operator">*</span> time<span class="token punctuation">.</span>Second<span class="token punctuation">,</span>TLSHandshakeTimeout<span class="token punctuation">:</span>   <span class="token number">10</span> <span class="token operator">*</span> time<span class="token punctuation">.</span>Second<span class="token punctuation">,</span>ExpectContinueTimeout<span class="token punctuation">:</span> <span class="token number">1</span> <span class="token operator">*</span> time<span class="token punctuation">.</span>Second<span class="token punctuation">,</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h3 id="回调"><a href="#回调" class="headerlink" title="回调"></a>回调</h3><p>您可以将不同类型的回调函数连接到收集器以控制收集作业或检索信息</p><ul><li><p><strong>OnRequest</strong> 在请求之前调用</p><pre class="line-numbers language-none"><code class="language-none">  <span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>OnError</strong>  如果请求期间发生错误时调用</p><pre class="line-numbers language-none"><code class="language-none">  <span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>OnResponseHeaders</strong>  收到响应头后调用</p><pre class="line-numbers language-none"><code class="language-none">  <span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>OnResponse</strong>  收到的响应后调用</p><pre class="line-numbers language-none"><code class="language-none">  <span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>OnHTML</strong>  如果收到的内容是HTML，则在OnResponse之后调用</p><pre class="line-numbers language-none"><code class="language-none">  <span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>OnXML</strong>  如果收到的内容为HTML或XML，则在OnHTML之后调用</p><pre class="line-numbers language-none"><code class="language-none">  <span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><strong>OnScraped</strong>  收集器抓取完后最后执行的回调</p><pre class="line-numbers language-none"><code class="language-none">  <span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ul><h3 id="调试"><a href="#调试" class="headerlink" title="调试"></a>调试</h3><p>debugger 是一个接口，我们只要实现它其中的两个方法，就可完成运行时信息的收集。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">type</span> Debugger <span class="token keyword">interface</span> <span class="token punctuation">{</span>    <span class="token comment">// Init initializes the backend</span>    <span class="token function">Init</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token builtin">error</span>    <span class="token comment">// Event receives a new collector event.</span>    <span class="token function">Event</span><span class="token punctuation">(</span>e <span class="token operator">*</span>Event<span class="token punctuation">)</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">package</span> main<span class="token keyword">import</span> <span class="token punctuation">(</span><span class="token string">"log"</span><span class="token string">"os"</span><span class="token string">"github.com/gocolly/colly"</span><span class="token string">"github.com/gocolly/colly/debug"</span><span class="token punctuation">)</span><span class="token keyword">func</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>writer<span class="token punctuation">,</span> err <span class="token operator">:=</span> os<span class="token punctuation">.</span><span class="token function">OpenFile</span><span class="token punctuation">(</span><span class="token string">"collector.log"</span><span class="token punctuation">,</span> os<span class="token punctuation">.</span>O_RDWR<span class="token operator">|</span>os<span class="token punctuation">.</span>O_CREATE<span class="token punctuation">,</span> <span class="token number">0666</span><span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span><span class="token function">panic</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span><span class="token punctuation">}</span>c <span class="token operator">:=</span> colly<span class="token punctuation">.</span><span class="token function">NewCollector</span><span class="token punctuation">(</span>colly<span class="token punctuation">.</span><span class="token function">Debugger</span><span class="token punctuation">(</span><span class="token operator">&amp;</span>debug<span class="token punctuation">.</span>LogDebugger<span class="token punctuation">{</span>Output<span class="token punctuation">:</span> writer<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">,</span> colly<span class="token punctuation">.</span><span class="token function">MaxDepth</span><span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">)</span>c<span class="token punctuation">.</span><span class="token function">OnHTML</span><span class="token punctuation">(</span><span class="token string">"a[href]"</span><span class="token punctuation">,</span> <span class="token keyword">func</span><span class="token punctuation">(</span>e <span class="token operator">*</span>colly<span class="token punctuation">.</span>HTMLElement<span class="token punctuation">)</span> <span class="token punctuation">{</span><span class="token keyword">if</span> err <span class="token operator">:=</span> e<span class="token punctuation">.</span>Request<span class="token punctuation">.</span><span class="token function">Visit</span><span class="token punctuation">(</span>e<span class="token punctuation">.</span><span class="token function">Attr</span><span class="token punctuation">(</span><span class="token string">"href"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>log<span class="token punctuation">.</span><span class="token function">Printf</span><span class="token punctuation">(</span><span class="token string">"visit err: %v"</span><span class="token punctuation">,</span> err<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">:=</span> c<span class="token punctuation">.</span><span class="token function">Visit</span><span class="token punctuation">(</span><span class="token string">"http://go-colly.org/"</span><span class="token punctuation">)</span><span class="token punctuation">;</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span><span class="token function">panic</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="分布式"><a href="#分布式" class="headerlink" title="分布式"></a>分布式</h3><p>分布式爬虫，可以从几个层面考虑，分别是代理层面、执行层面和存储层面。</p><h4 id="代理层面"><a href="#代理层面" class="headerlink" title="代理层面"></a>代理层面</h4><p>通过设置代理池，我们可以将下载任务分配给不同节点执行，有助于提供爬虫的网页下载速度。同时，这样还能有效降低因爬取速度太快而导致IP 被禁的可能性。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">package</span> main<span class="token keyword">import</span> <span class="token punctuation">(</span><span class="token string">"github.com/gocolly/colly"</span><span class="token string">"github.com/gocolly/colly/proxy"</span><span class="token punctuation">)</span><span class="token keyword">func</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>c <span class="token operator">:=</span> colly<span class="token punctuation">.</span><span class="token function">NewCollector</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">if</span> p<span class="token punctuation">,</span> err <span class="token operator">:=</span> proxy<span class="token punctuation">.</span><span class="token function">RoundRobinProxySwitcher</span><span class="token punctuation">(</span> <span class="token comment">//colly 内置的通过轮询方式实现代理切换的函数</span><span class="token string">"socks5://127.0.0.1:1337"</span><span class="token punctuation">,</span><span class="token string">"socks5://127.0.0.1:1338"</span><span class="token punctuation">,</span><span class="token string">"http://127.0.0.1:8080"</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">;</span> err <span class="token operator">==</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>c<span class="token punctuation">.</span><span class="token function">SetProxyFunc</span><span class="token punctuation">(</span>p<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="执行层面"><a href="#执行层面" class="headerlink" title="执行层面"></a>执行层面</h4><p>将任务分配给不同的节点执行，实现真正意义的分布式。</p><h4 id="存储层面"><a href="#存储层面" class="headerlink" title="存储层面"></a>存储层面</h4><p>将任务分配到不同节点执行实现了分布式。但部分数据，在节点之间需要共享。</p><h3 id="存储"><a href="#存储" class="headerlink" title="存储"></a>存储</h3><h3 id="多收集器"><a href="#多收集器" class="headerlink" title="多收集器"></a>多收集器</h3><h3 id="配置优化"><a href="#配置优化" class="headerlink" title="配置优化"></a>配置优化</h3><p>colly 的默认配置针对是少量站点的优化配置。如果你是针对大量站点的抓取，还需要一些改进。</p><h4 id="持久化存储"><a href="#持久化存储" class="headerlink" title="持久化存储"></a>持久化存储</h4><p>默认情况下，colly 中的 cookies 和 url 是保存在内存中，我们要换成可持久化的存储。</p><h4 id="启用异步加快任务执行"><a href="#启用异步加快任务执行" class="headerlink" title="启用异步加快任务执行"></a>启用异步加快任务执行</h4><p>colly 默认会阻塞等待请求执行完成，这将会导致等待执行任务数越来越大。我们可以通过设置 collector 的 Async 选项为 true 实现异步处理，从而避免这个问题。如果采用这种方式，记住增加 c.Wait()，否则程序会立刻退出。</p><h4 id="禁止或者限制KeepAlive连接"><a href="#禁止或者限制KeepAlive连接" class="headerlink" title="禁止或者限制KeepAlive连接"></a>禁止或者限制KeepAlive连接</h4><p>colly 默认开启 KeepAlive 增加爬虫的抓取速度。但是，这对打开的文件描述符有要求，对于长时间运行的任务，进程非常容易就能达到最大描述符的限制。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">c <span class="token operator">:=</span> colly<span class="token punctuation">.</span><span class="token function">NewCollector</span><span class="token punctuation">(</span><span class="token punctuation">)</span>c<span class="token punctuation">.</span><span class="token function">WithTransport</span><span class="token punctuation">(</span><span class="token operator">&amp;</span>http<span class="token punctuation">.</span>Transport<span class="token punctuation">{</span>    DisableKeepAlives<span class="token punctuation">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h3 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h3>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【网络抓包】WireShark</title>
      <link href="/2021/11/26/networks/wang-luo-zhua-bao-wireshark/"/>
      <url>/2021/11/26/networks/wang-luo-zhua-bao-wireshark/</url>
      
        <content type="html"><![CDATA[<h1 id="【网络抓包】WireShark"><a href="#【网络抓包】WireShark" class="headerlink" title="【网络抓包】WireShark"></a>【网络抓包】WireShark</h1><blockquote><p>wireshark官网：<a href="https://www.wireshark.org/">https://www.wireshark.org/</a></p></blockquote><h2 id="wireshark简介"><a href="#wireshark简介" class="headerlink" title="wireshark简介"></a>wireshark简介</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Kubernetes包管理】Kustomize</title>
      <link href="/2021/11/25/kustomize/kubernetes-bao-guan-li-kustomize/"/>
      <url>/2021/11/25/kustomize/kubernetes-bao-guan-li-kustomize/</url>
      
        <content type="html"><![CDATA[<h1 id="Kustomize"><a href="#Kustomize" class="headerlink" title="Kustomize"></a>Kustomize</h1><blockquote><p>Kustomize 官网： <a href="https://kustomize.io/">https://kustomize.io/</a></p><p>github地址：<a href="https://github.com/kubernetes-sigs/kustomize/">https://github.com/kubernetes-sigs/kustomize/</a></p></blockquote><h2 id="Kustomize简介"><a href="#Kustomize简介" class="headerlink" title="Kustomize简介"></a>Kustomize简介</h2><p>kustomize 是 kubernetes 原生的配置管理，以无模板方式来定制应用的配置。kustomize 使用 k8s 原生概念帮助创建并复用资源配置(YAML)，允许用户以一个应用描述文件 （YAML 文件）为基础（Base YAML），然后通过 Overlay 的方式生成最终部署应用所需的描述文件。</p><h3 id="kustomize-术语"><a href="#kustomize-术语" class="headerlink" title="kustomize 术语"></a>kustomize 术语</h3><ul><li><p><strong>kustomization</strong></p><p>kustomization指的是 kustomization.yaml 文件，或者指的是包含 kustomization.yaml 文件的目录以及它里面引用的所有相关文件路径</p></li><li><p><strong>base</strong></p><p>base 指的是一个 kustomization , 任何的 kustomization 包括 overlay，都可以作为另一个 kustomization 的 base 。base 中描述了共享的内容。</p></li><li><p><strong>overlay</strong></p><p>overlay 是一个 kustomization, 它修改(并因此依赖于)另外一个 kustomization. overlay 中的 kustomization指的是一些其它的 kustomization, 称为其 base. 没有 base, overlay 无法使用，并且一个 overlay 可以用作 另一个 overlay 的 base(基础)。简而言之，overlay 声明了与 base 之间的差异。通过 overlay 来维护基于 base 的不同 variants(变体)</p></li><li><p><strong>variant</strong></p><p>variant 是在集群中将 overlay 应用于 base 的结果。简而言之，variant 是含有同一组 base 的不同 kustomization</p></li><li><p><strong>resource</strong></p><p>在 kustomize 的上下文中，resource 是描述 k8s API 对象的 YAML 或 JSON 文件的相对路径。即是指向一个声明了 kubernetes API 对象的 YAML 文件</p></li><li><p><strong>patch</strong></p><p>修改文件的一般说明。文件路径，指向一个声明了 kubernetes API patch 的 YAML 文件</p></li></ul><h3 id="工作原理"><a href="#工作原理" class="headerlink" title="工作原理"></a>工作原理</h3><p>kustomize 将对 K8s 应用定义为由 Yaml 表示的资源描述，对 K8s 应用的变更可以映射到对 Yaml 的变更上。</p><pre class="line-numbers language-none"><code class="language-none">~/someApp├── base # 基础的yaml成为Base│   ├── deployment.yaml # deployment资源文件│   ├── kustomization.yaml # 用来描述元数据│   └── service.yaml # service资源文件└── overlays # 补充 Base 的 Yaml 被称作 Overlay    ├── development    │   ├── cpu_count.yaml    │   ├── kustomization.yaml    │   └── replica_count.yaml    └── production        ├── cpu_count.yaml        ├── kustomization.yaml        └── replica_count.yaml<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>kustomize 通过描述文件的叠加，来生成完整的部署用 Yaml，可以直接使用 <code>build</code> 命令可以生成并部署：</p><pre class="line-numbers language-none"><code class="language-none">kubectl apply -k ~/someApp/overlays/development<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="Kustomize环境配置"><a href="#Kustomize环境配置" class="headerlink" title="Kustomize环境配置"></a>Kustomize环境配置</h2><ul><li>二进制安装[release](curl -s “<a href="https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh&quot;">https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh"</a>  | bash)</li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"># 脚本检测到您的操作系统，并将相应的kustomize二进制文件下载到当前工作目录。curl -s "https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh"  | bash<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><ul><li>docker安装</li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">docker pull k8s.gcr.io/kustomize/kustomize:v3.8.7 &lt;version&gt;# 根据需要指定不同的版本<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>macos安装</li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">brew install kustomize<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【包管理】apk</title>
      <link href="/2021/11/23/linux/package/linux-bao-guan-li-apk/"/>
      <url>/2021/11/23/linux/package/linux-bao-guan-li-apk/</url>
      
        <content type="html"><![CDATA[<h1 id="【Linux包管理】apk"><a href="#【Linux包管理】apk" class="headerlink" title="【Linux包管理】apk"></a>【Linux包管理】apk</h1><blockquote><p>alpine 包平台：<a href="https://pkgs.alpinelinux.org/packages">https://pkgs.alpinelinux.org/packages</a></p></blockquote><h1 id="apk简介"><a href="#apk简介" class="headerlink" title="apk简介"></a>apk简介</h1><p>apk是alpine的包管理工具</p><h2 id="apk命令行"><a href="#apk命令行" class="headerlink" title="apk命令行"></a>apk命令行</h2><pre class="line-numbers language-none"><code class="language-none">usage: apk [&lt;OPTIONS&gt;...] COMMAND [&lt;ARGUMENTS&gt;...]Package installation and removal:  add        添加指定包并确认所有依赖满足要求  del        删除指定包System maintenance:  fix        在不改动主要的依赖的情况下进行包的修复或者升级  update     从远程仓库获取信息更新本地仓库索引  upgrade    对已安装了的包进行更新  cache      对缓存进行操作，比如对缺失的包进行缓存或者对于不需要的包进行缓存删除Querying package information:  info       对于指定的包进行包或者仓库的详细信息进行显示  list       按照指定条件进行包的列表信息显示  dot        生成依赖之间的关联关系图  policy     显示包的仓库策略信息  search     查询相关的包的详细信息Repository maintenance:  index      使用文件生成仓库索引文件  fetch      从全局仓库下载包到本地目录  manifest   显示package各组成部分的checksum  verify     验证包的完整性和签名信息Miscellaneous:  audit      Audit system for changes  stats      Show statistics about repositories and installations  version    Compare package versions or perform tests on version strings<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="apk使用"><a href="#apk使用" class="headerlink" title="apk使用"></a>apk使用</h2><ul><li><p><strong>更新升级</strong></p><p>update命令会从各个镜像源列表下载APKINDEX.tar.gz并存储到本地缓存，一般在/var/cache/apk/(Alpine在该目录下)、 /var/lib/apk/ 、/etc/apk/cache/下。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">apk update  <span class="token comment"># 更新最新本地镜像源</span>apk upgrade <span class="token comment"># 升级软件</span>apk <span class="token function">add</span> --upgrade busybox <span class="token comment"># 指定升级部分软件包</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p><strong>下载包</strong></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">apk <span class="token function">add</span> openssh <span class="token function">vim</span> <span class="token comment"># 安装最新软件包，并自动安装必须的依赖包</span>apk <span class="token function">add</span> <span class="token assign-left variable">asterisk</span><span class="token operator">=</span><span class="token number">1.6</span>.0.21-r0 <span class="token comment"># 指定版本安装</span>apk <span class="token function">add</span> --no-cache mysql-clientapk <span class="token function">add</span> --allow-untrusted file.apk <span class="token comment"># 本地安装</span>apk <span class="token function">add</span> --allow-untrusted file.apk dep.apk<span class="token comment"># 如果有相关依赖可以一一列出</span>apk <span class="token function">add</span> docker --update-cache --repository http://mirrors.ustc.edu.cn/alpine/v3.4/main/ --allow-untrusted<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><strong>卸载包</strong></p><pre class="line-numbers language-none"><code class="language-none">apk del openssh # 删除包及其依赖apk del openssh openntp vim<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><strong>搜索</strong></p><ul><li><strong>-v</strong> 输出描述内容，支出通配符</li><li><strong>-d</strong> 通过软件包描述查询</li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">apk search <span class="token comment">#查找所以可用软件包</span>apk search -v <span class="token comment">#查找所以可用软件包及其描述内容</span>apk search -v <span class="token string">'acf*'</span> <span class="token comment">#通过软件包名称查找软件包</span>apk search -v -d <span class="token string">'docker'</span> <span class="token comment">#通过描述文件查找特定的软件包</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><strong>更新软件镜像源</strong></p><blockquote><p>阿里云镜像源：mirrors.aliyun.com</p><p>中科大镜像源：mirrors.ustc.edu.cn</p><p>清华镜像源：mirrors.tuna.tsinghua.edu.cn</p></blockquote><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># 默认alpine的地址是：https://dl-cdn.alpinelinux.org</span><span class="token comment"># 修改成aliyun镜像源</span><span class="token function">sed</span> -i <span class="token string">'s/dl-cdn.alpinelinux.org/mirrors.aliyun.com/g'</span> /etc/apk/repositories<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p><strong>查看包信息</strong></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">apk info <span class="token comment"># 列出所有已安装的软件包</span>apk info -a zlib <span class="token comment"># 显示完整的软件包信息</span>apk info --who-owns /sbin/lbu <span class="token comment"># 显示指定文件属于的包</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【包管理】apt</title>
      <link href="/2021/11/23/linux/package/linux-bao-guan-li-apt/"/>
      <url>/2021/11/23/linux/package/linux-bao-guan-li-apt/</url>
      
        <content type="html"><![CDATA[<h1 id="【Linux包管理】apt"><a href="#【Linux包管理】apt" class="headerlink" title="【Linux包管理】apt"></a>【Linux包管理】apt</h1><blockquote></blockquote><h2 id="apt简介"><a href="#apt简介" class="headerlink" title="apt简介"></a>apt简介</h2><p>APT（Advanced Package Tool）是linux的Debian发行版已经一些以Debian为基础的发行版（Ubuntu等）下，做软件包管理的一个工具，其中包括deselect和apt-get两个工具，他们都可以提供下载、安装、更新、删除软件包的功能。</p><h3 id="apt和apt-get区别"><a href="#apt和apt-get区别" class="headerlink" title="apt和apt-get区别"></a>apt和apt-get区别</h3><ul><li><p><strong>apt-get</strong></p><p>Advanced Package Tool，又名apt-get，是一款适用于Unix和Linux系统的应用程序管理器。最初于1998年发布，用于检索应用程序并将其加载到Debian Linux系统。主要用于自动从互联网的软件仓库中搜索、安装、升级、卸载软件或操作系统。</p><p>apt-get命令一般需要<strong>root权限</strong>执行，所以一般跟着sudo命令。</p></li><li><p><strong>apt</strong></p><p>apt 命令行实用程序于2014年推出第一个稳定版本，用于 Debian 发行版 .deb 软件包安装。它最初在不稳定的Debian版本中使用，然后在Debian 8中成为标准。</p></li></ul><h2 id="apt命令使用"><a href="#apt命令使用" class="headerlink" title="apt命令使用"></a>apt命令使用</h2><h3 id="apt-cache"><a href="#apt-cache" class="headerlink" title="apt-cache"></a>apt-cache</h3><pre class="line-numbers language-none"><code class="language-none">Usage: apt-cache [options] command       apt-cache [options] showpkg pkg1 [pkg2 ...]       apt-cache [options] showsrc pkg1 [pkg2 ...]apt-cache is a low-level tool used to query informationfrom APT's binary cache filesCommands:   gencaches - Build both the package and source cache   showpkg - Show some general information for a single package   showsrc - Show source records   stats - Show some basic statistics   dump - Show the entire file in a terse form   dumpavail - Print an available file to stdout   unmet - Show unmet dependencies   search - Search the package list for a regex pattern   show - Show a readable record for the package   depends - Show raw dependency information for a package   rdepends - Show reverse dependency information for a package   pkgnames - List the names of all packages in the system   dotty - Generate package graphs for GraphViz   xvcg - Generate package graphs for xvcg   policy - Show policy settings<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="apt-get"><a href="#apt-get" class="headerlink" title="apt-get"></a>apt-get</h3><pre class="line-numbers language-none"><code class="language-none">Usage: apt-get [options] command       apt-get [options] install|remove pkg1 [pkg2 ...]       apt-get [options] source pkg1 [pkg2 ...]apt-get is a simple command line interface for downloading andinstalling packages. The most frequently used commands are updateand install.Commands:   update - Retrieve new lists of packages   upgrade - Perform an upgrade   install - Install new packages (pkg is libc6 not libc6.deb)   remove - Remove packages   autoremove - Remove automatically all unused packages   purge - Remove packages and config files   source - Download source archives   build-dep - Configure build-dependencies for source packages   dist-upgrade - Distribution upgrade, see apt-get(8)   dselect-upgrade - Follow dselect selections   clean - Erase downloaded archive files   autoclean - Erase old downloaded archive files   check - Verify that there are no broken dependencies   changelog - Download and display the changelog for the given package   download - Download the binary package into the current directory<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="apt"><a href="#apt" class="headerlink" title="apt"></a>apt</h3><pre class="line-numbers language-none"><code class="language-none">Usage: apt [options] commandCLI for apt.Basic commands:  list - list packages based on package names search - search in package descriptions show - show package details update - update list of available packages install - install packages remove  - remove packages upgrade - upgrade the system by installing/upgrading packages full-upgrade - upgrade the system by removing/installing/upgrading packages edit-sources - edit the source information file<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【包管理】yum</title>
      <link href="/2021/11/23/linux/package/linux-bao-guan-li-yum/"/>
      <url>/2021/11/23/linux/package/linux-bao-guan-li-yum/</url>
      
        <content type="html"><![CDATA[<h1 id="【Linux包管理】yum"><a href="#【Linux包管理】yum" class="headerlink" title="【Linux包管理】yum"></a>【Linux包管理】yum</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【虚拟机】KVM</title>
      <link href="/2021/11/21/virtualization/kvm/xu-ni-ji-kvm/"/>
      <url>/2021/11/21/virtualization/kvm/xu-ni-ji-kvm/</url>
      
        <content type="html"><![CDATA[<h1 id="【虚拟机】KVM"><a href="#【虚拟机】KVM" class="headerlink" title="【虚拟机】KVM"></a>【虚拟机】KVM</h1><blockquote><p> kvm官网： <a href="http://www.linux-kvm.org/">http://www.linux-kvm.org/</a> </p></blockquote><h2 id="kvm简介"><a href="#kvm简介" class="headerlink" title="kvm简介"></a>kvm简介</h2><h3 id="kvm虚拟化架构"><a href="#kvm虚拟化架构" class="headerlink" title="kvm虚拟化架构"></a>kvm虚拟化架构</h3><p><strong>kvm虚拟化相关的程序包</strong></p><table><thead><tr><th align="center">程序包</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">qemu-kvm</td><td align="center">主要的KVM程序包</td></tr><tr><td align="center">libvirt</td><td align="center">用于管理超级监视程序的libvirtd服务</td></tr><tr><td align="center">libvirt-client</td><td align="center">用于管理虚拟机的virsh命令和客户端API</td></tr><tr><td align="center">virt-install</td><td align="center">创建虚拟机需要的命令行工具</td></tr><tr><td align="center">virt-manager</td><td align="center">GUI虚拟机管理工具（图形界面）</td></tr><tr><td align="center">virt-top</td><td align="center">虚拟机统计命令</td></tr><tr><td align="center">virt-viewer</td><td align="center">用于连接到虚拟机的图形控制台</td></tr></tbody></table><h2 id="kvm环境配置"><a href="#kvm环境配置" class="headerlink" title="kvm环境配置"></a>kvm环境配置</h2><ul><li><p>centos</p><ul><li><p>KVM 是基于 x86 虚拟化扩展(Intel VT 或者 AMD-V) 技术的虚拟机软件，所以查看 CPU 是否支持 VT 技术，就可以判断是否支持KVM。有返回结果，如果结果中有vmx（Intel）或svm(AMD)字样，就说明CPU的支持的。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">egrep</span> <span class="token string">'(vmx|svm)'</span> --color<span class="token operator">=</span>always /proc/cpuinfo<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>关闭SELinux,防火墙</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">systemctl stop firewall <span class="token operator">&amp;&amp;</span> systemctl disbale firewalldsystemctl stop NetworkManager <span class="token operator">&amp;&amp;</span> systemctl disbale NetworkManagersetenforce <span class="token number">0</span> <span class="token operator">&amp;&amp;</span> <span class="token function">sed</span> -i <span class="token string">'s/SELINUX=.*/SELINUX=permissive/g'</span> /etc/selinux/config<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p>安装kvm</p><p>kvm相关安装包及其作用:</p><ul><li><code>qemu-kvm</code> 主要的KVM程序包</li><li><code>python-virtinst</code> 创建虚拟机所需要的命令行工具和程序库</li><li><code>virt-manager</code> GUI虚拟机管理工具</li><li><code>virt-top</code> 虚拟机统计命令</li><li><code>virt-viewer</code> GUI连接程序，连接到已配置好的虚拟机</li><li><code>libvirt</code> C语言工具包，提供libvirt服务</li><li><code>libvirt-client</code> 为虚拟客户机提供的C语言工具包</li><li><code>virt-install</code> 基于libvirt服务的虚拟机创建命令</li><li><code>bridge-utils</code> 创建和管理桥接设备的工具</li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">yum -y <span class="token function">install</span> qemu-kvm libvirt virt-install bridge-utils<span class="token comment"># 安装完成后重启机器以便加载kvm模块</span><span class="token function">reboot</span><span class="token comment"># 查看KVM模块是否被正确加载</span>lsmod <span class="token operator">|</span> <span class="token function">grep</span> kvmkvm_intel             <span class="token number">183621</span>  <span class="token number">90</span> kvm                   <span class="token number">586948</span>  <span class="token number">1</span> kvm_intel<span class="token comment"># 启动kvm服务</span>systemctl start libvirtdsystemctl <span class="token builtin class-name">enable</span> libvirtd<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul></li><li><p>ubuntu</p><pre class="line-numbers language-none"><code class="language-none">  ```## kvm命令行### virt-install```shellusage: virt-install --name NAME --memory MB STORAGE INSTALL [options]Create a new virtual machine from specified install media.optional arguments:  -h, --help            show this help message and exit  --version             show program's version number and exit  --connect URI         Connect to hypervisor with libvirt URIGeneral Options:  -n NAME, --name NAME  虚拟机实例的名称（唯一）  --memory MEMORY       配置虚拟机内存分配. Ex:                        --memory 1024 (in MiB)                        --memory 512,maxmemory=1024                        --memory 512,maxmemory=1024,hotplugmemorymax=2048,hotplugmemoryslots=2  --vcpus VCPUS         配置虚拟机虚拟CPU核心数量. Ex:                        --vcpus 5                        --vcpus 5,maxvcpus=10,cpuset=1-4,6,8                        --vcpus sockets=2,cores=4,threads=2  --cpu CPU             CPU model and features. Ex:                        --cpu coreduo,+x2apic                        --cpu host-passthrough                        --cpu host  --metadata METADATA   配置虚拟机元数据. Ex:                        --metadata name=foo,title="My pretty title",uuid=...                        --metadata description="My nice long description"Installation Method Options:  --cdrom CDROM         CD-ROM安装媒介  -l LOCATION, --location LOCATION                        Installation source (eg, nfs:host:/path,                        http://host/path, ftp://host/path)  --pxe                 Boot from the network using the PXE protocol  --import              Build guest around an existing disk image  --livecd              Treat the CD-ROM media as a Live CD  -x EXTRA_ARGS, --extra-args EXTRA_ARGS 传递到从--location启动的安装内核的其他参数  --initrd-inject INITRD_INJECT 从--location中添加给initrd的启动文件    --os-variant DISTRO_VARIANT 正在安装的OS变体 e.g.                        'fedora18', 'rhel6', 'winxp', etc.  --boot BOOT           Configure guest boot settings. Ex:                        --boot hd,cdrom,menu=on                        --boot init=/sbin/init (for containers)  --idmap IDMAP         Enable user namespace for LXC container. Ex:                        --idmap uid_start=0,uid_target=1000,uid_count=10Device Options:  --disk DISK           使用各种选项指定存储. Ex.                        --disk size=10 (new 10GiB image in default location)                        --disk /my/existing/disk,cache=none                        --disk device=cdrom,bus=scsi                        --disk=?  -w NETWORK, --network NETWORK                        Configure a guest network interface. Ex:                        --network bridge=mybr0                        --network network=my_libvirt_virtual_net                        --network network=mynet,model=virtio,mac=00:11...                        --network none                        --network help  --graphics GRAPHICS   Configure guest display settings. Ex:                        --graphics vnc                        --graphics spice,port=5901,tlsport=5902                        --graphics none                        --graphics vnc,password=foobar,port=5910,keymap=ja  --controller CONTROLLER                        Configure a guest controller device. Ex:                        --controller type=usb,model=ich9-ehci1  --input INPUT         Configure a guest input device. Ex:                        --input tablet                        --input keyboard,bus=usb  --serial SERIAL       Configure a guest serial device  --parallel PARALLEL   Configure a guest parallel device  --channel CHANNEL     Configure a guest communication channel  --console CONSOLE     Configure a text console connection between the guest                        and host  --hostdev HOSTDEV     Configure physical USB/PCI/etc host devices to be                        shared with the guest  --filesystem FILESYSTEM                        Pass host directory to the guest. Ex:                         --filesystem /my/source/dir,/dir/in/guest                        --filesystem template_name,/,type=template  --sound [SOUND]       Configure guest sound device emulation  --watchdog WATCHDOG   Configure a guest watchdog device  --video VIDEO         Configure guest video hardware.  --smartcard SMARTCARD                        Configure a guest smartcard device. Ex:                        --smartcard mode=passthrough  --redirdev REDIRDEV   Configure a guest redirection device. Ex:                        --redirdev usb,type=tcp,server=192.168.1.1:4000  --memballoon MEMBALLOON                        Configure a guest memballoon device. Ex:                        --memballoon model=virtio  --tpm TPM             Configure a guest TPM device. Ex:                        --tpm /dev/tpm  --rng RNG             Configure a guest RNG device. Ex:                        --rng /dev/urandom  --panic PANIC         Configure a guest panic device. Ex:                        --panic default  --memdev MEMDEV       Configure a guest memory device. Ex:                        --memdev dimm,target_size=1024Guest Configuration Options:  --security SECURITY   Set domain security driver configuration.  --cputune CPUTUNE     Tune CPU parameters for the domain process.  --numatune NUMATUNE   Tune NUMA policy for the domain process.  --memtune MEMTUNE     Tune memory policy for the domain process.  --blkiotune BLKIOTUNE                        Tune blkio policy for the domain process.  --memorybacking MEMORYBACKING                        Set memory backing policy for the domain process. Ex:                        --memorybacking hugepages=on  --features FEATURES   Set domain &lt;features&gt; XML. Ex:                        --features acpi=off                        --features apic=on,eoi=on  --clock CLOCK         Set domain &lt;clock&gt; XML. Ex:                        --clock offset=localtime,rtc_tickpolicy=catchup  --pm PM               Configure VM power management features  --events EVENTS       Configure VM lifecycle management policy  --resource RESOURCE   Configure VM resource partitioning (cgroups)  --sysinfo SYSINFO     Configure SMBIOS System Information. Ex:                        --sysinfo emulate                        --sysinfo host                        --sysinfo bios_vendor=Vendor_Inc.,bios_version=1.2.3-abc,...                        --sysinfo system_manufacturer=System_Corp.,system_product=Computer,...                        --sysinfo baseBoard_manufacturer=Baseboard_Corp.,baseBoard_product=Motherboard,...  --qemu-commandline QEMU_COMMANDLINE                        Pass arguments directly to the qemu emulator. Ex:                        --qemu-commandline='-display gtk,gl=on'                        --qemu-commandline env=DISPLAY=:0.1Virtualization Platform Options:  -v, --hvm             This guest should be a fully virtualized guest  -p, --paravirt        This guest should be a paravirtualized guest  --container           This guest should be a container guest  --virt-type HV_TYPE   Hypervisor name to use (kvm, qemu, xen, ...)  --arch ARCH           The CPU architecture to simulate  --machine MACHINE     The machine type to emulateMiscellaneous Options:  --autostart           Have domain autostart on host boot up.  --transient           Create a transient domain.  --wait WAIT           Minutes to wait for install to complete.  --noautoconsole       Don't automatically try to connect to the guest                        console  --noreboot            Don't boot guest after completing install.  --print-xml [XMLONLY]                        Print the generated domain XML rather than create the                        guest.  --dry-run             Run through install process, but do not create devices                        or define the guest.  --check CHECK         Enable or disable validation checks. Example:                        --check path_in_use=off                        --check all=off  -q, --quiet           Suppress non-error output  -d, --debug           Print debugging information<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h3 id="virtsh"><a href="#virtsh" class="headerlink" title="virtsh"></a>virtsh</h3><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">virsh</span> <span class="token punctuation">[</span>options<span class="token punctuation">]</span><span class="token punctuation">..</span>. <span class="token punctuation">[</span><span class="token operator">&lt;</span>command_string<span class="token operator">&gt;</span><span class="token punctuation">]</span><span class="token function">virsh</span> <span class="token punctuation">[</span>options<span class="token punctuation">]</span><span class="token punctuation">..</span>. <span class="token operator">&lt;</span>command<span class="token operator">&gt;</span> <span class="token punctuation">[</span>args<span class="token punctuation">..</span>.<span class="token punctuation">]</span>  options:    -c <span class="token operator">|</span> --connect<span class="token operator">=</span>URI      hypervisor connection URI    -d <span class="token operator">|</span> --debug<span class="token operator">=</span>NUM        debug level <span class="token punctuation">[</span><span class="token number">0</span>-4<span class="token punctuation">]</span>    -e <span class="token operator">|</span> --escape <span class="token operator">&lt;</span>char<span class="token operator">&gt;</span>    <span class="token builtin class-name">set</span> escape sequence <span class="token keyword">for</span> console    -h <span class="token operator">|</span> --help             this <span class="token builtin class-name">help</span>    -k <span class="token operator">|</span> --keepalive-interval<span class="token operator">=</span>NUM                            keepalive interval <span class="token keyword">in</span> seconds, <span class="token number">0</span> <span class="token keyword">for</span> disable    -K <span class="token operator">|</span> --keepalive-count<span class="token operator">=</span>NUM                            number of possible missed keepalive messages    -l <span class="token operator">|</span> --log<span class="token operator">=</span>FILE         output logging to <span class="token function">file</span>    -q <span class="token operator">|</span> --quiet            quiet mode    -r <span class="token operator">|</span> --readonly         connect <span class="token builtin class-name">readonly</span>    -t <span class="token operator">|</span> --timing           print timing information    -v                      short version    -V                      long version         --version<span class="token punctuation">[</span><span class="token operator">=</span>TYPE<span class="token punctuation">]</span>   version, TYPE is short or long <span class="token punctuation">(</span>default short<span class="token punctuation">)</span>  commands <span class="token punctuation">(</span>non interactive mode<span class="token punctuation">)</span>: Domain Management <span class="token punctuation">(</span>help keyword <span class="token string">'domain'</span><span class="token punctuation">)</span>    attach-device                  attach device from an XML <span class="token function">file</span>    attach-disk                    attach disk device    attach-interface               attach network interface    autostart                      autostart a domain    blkdeviotune                   Set or query a block device I/O tuning parameters.    blkiotune                      Get or <span class="token builtin class-name">set</span> blkio parameters    blockcommit                    Start a block commit operation.    blockcopy                      Start a block copy operation.    blockjob                       Manage active block operations    blockpull                      Populate a disk from its backing image.    blockresize                    Resize block device of domain.    change-media                   Change media of CD or floppy drive    console                        connect to the guest console    cpu-stats                      show domain cpu statistics    create                         create a domain from an XML <span class="token function">file</span>    define                         define <span class="token punctuation">(</span>but don<span class="token string">'t start) a domain from an XML file    desc                           show or set domain'</span>s description or title    destroy                        destroy <span class="token punctuation">(</span>stop<span class="token punctuation">)</span> a domain    detach-device                  detach device from an XML <span class="token function">file</span>    detach-device-alias            detach device from an <span class="token builtin class-name">alias</span>    detach-disk                    detach disk device    detach-interface               detach network interface    domdisplay                     domain display connection URI    domfsfreeze                    Freeze domain<span class="token string">'s mounted filesystems.    domfsthaw                      Thaw domain'</span>s mounted filesystems.    domfsinfo                      Get information of domain<span class="token string">'s mounted filesystems.    domfstrim                      Invoke fstrim on domain'</span>s mounted filesystems.    domhostname                    print the domain<span class="token string">'s hostname    domid                          convert a domain name or UUID to domain id    domif-setlink                  set link state of a virtual interface    domiftune                      get/set parameters of a virtual interface    domjobabort                    abort active domain job    domjobinfo                     domain job information    domname                        convert a domain id or UUID to domain name    domrename                      rename a domain    dompmsuspend                   suspend a domain gracefully using power management functions    dompmwakeup                    wakeup a domain from pmsuspended state    domuuid                        convert a domain name or id to domain UUID    domxml-from-native             Convert native config to domain XML    domxml-to-native               Convert domain XML to native config    dump                           dump the core of a domain to a file for analysis    dumpxml                        domain information in XML    edit                           edit XML configuration for a domain    event                          Domain Events    inject-nmi                     Inject NMI to the guest    iothreadinfo                   view domain IOThreads    iothreadpin                    control domain IOThread affinity    iothreadadd                    add an IOThread to the guest domain    iothreaddel                    delete an IOThread from the guest domain    send-key                       Send keycodes to the guest    send-process-signal            Send signals to processes    lxc-enter-namespace            LXC Guest Enter Namespace    managedsave                    managed save of a domain state    managedsave-remove             Remove managed save of a domain    managedsave-edit               edit XML for a domain'</span>s managed save state <span class="token function">file</span>    managedsave-dumpxml            Domain information of managed save state <span class="token function">file</span> <span class="token keyword">in</span> XML    managedsave-define             redefine the XML <span class="token keyword">for</span> a domain<span class="token string">'s managed save state file    memtune                        Get or set memory parameters    perf                           Get or set perf event    metadata                       show or set domain'</span>s custom XML metadata    migrate                        migrate domain to another <span class="token function">host</span>    migrate-setmaxdowntime         <span class="token builtin class-name">set</span> maximum tolerable downtime    migrate-getmaxdowntime         get maximum tolerable downtime    migrate-compcache              get/set compression cache size    migrate-setspeed               Set the maximum migration bandwidth    migrate-getspeed               Get the maximum migration bandwidth    migrate-postcopy               Switch running migration from pre-copy to post-copy    numatune                       Get or <span class="token builtin class-name">set</span> numa parameters    qemu-attach                    QEMU Attach    qemu-monitor-command           QEMU Monitor Command    qemu-monitor-event             QEMU Monitor Events    qemu-agent-command             QEMU Guest Agent Command    <span class="token function">reboot</span>                         <span class="token function">reboot</span> a domain    reset                          reset a domain    restore                        restore a domain from a saved state <span class="token keyword">in</span> a <span class="token function">file</span>    resume                         resume a domain    save                           save a domain state to a <span class="token function">file</span>    save-image-define              redefine the XML <span class="token keyword">for</span> a domain<span class="token string">'s saved state file    save-image-dumpxml             saved state domain information in XML    save-image-edit                edit XML for a domain'</span>s saved state <span class="token function">file</span>    schedinfo                      show/set scheduler parameters    screenshot                     take a screenshot of a current domain console and store it into a <span class="token function">file</span>    set-lifecycle-action           change lifecycle actions    set-user-password              <span class="token builtin class-name">set</span> the user password inside the domain    setmaxmem                      change maximum memory limit    setmem                         change memory allocation    setvcpus                       change number of virtual CPUs    <span class="token function">shutdown</span>                       gracefully <span class="token function">shutdown</span> a domain    start                          start a <span class="token punctuation">(</span>previously defined<span class="token punctuation">)</span> inactive domain    <span class="token function">suspend</span>                        <span class="token function">suspend</span> a domain    ttyconsole                     <span class="token function">tty</span> console    undefine                       undefine a domain    update-device                  update device from an XML <span class="token function">file</span>    vcpucount                      domain vcpu counts    vcpuinfo                       detailed domain vcpu information    vcpupin                        control or query domain vcpu affinity    emulatorpin                    control or query domain emulator affinity    vncdisplay                     vnc display    guestvcpus                     query or modify state of vcpu <span class="token keyword">in</span> the guest <span class="token punctuation">(</span>via agent<span class="token punctuation">)</span>    setvcpu                        attach/detach vcpu or <span class="token function">groups</span> of threads    domblkthreshold                <span class="token builtin class-name">set</span> the threshold <span class="token keyword">for</span> block-threshold event <span class="token keyword">for</span> a given block device or it<span class="token string">'s backing chain element Domain Monitoring (help keyword '</span>monitor<span class="token string">')    domblkerror                    Show errors on block devices    domblkinfo                     domain block device size information    domblklist                     list all domain blocks    domblkstat                     get device block stats for a domain    domcontrol                     domain control interface state    domif-getlink                  get link state of a virtual interface    domifaddr                      Get network interfaces'</span> addresses <span class="token keyword">for</span> a running domain    domiflist                      list all domain virtual interfaces    domifstat                      get network interface stats <span class="token keyword">for</span> a domain    dominfo                        domain information    dommemstat                     get memory statistics <span class="token keyword">for</span> a domain    domstate                       domain state    domstats                       get statistics about one or multiple domains    domtime                        domain <span class="token function">time</span>    list                           list domains Host and Hypervisor <span class="token punctuation">(</span>help keyword <span class="token string">'host'</span><span class="token punctuation">)</span>    allocpages                     Manipulate pages pool size    capabilities                   capabilities    cpu-baseline                   compute baseline CPU    cpu-compare                    compare <span class="token function">host</span> CPU with a CPU described by an XML <span class="token function">file</span>    cpu-models                     CPU models    domcapabilities                domain capabilities    freecell                       NUMA <span class="token function">free</span> memory    freepages                      NUMA <span class="token function">free</span> pages    <span class="token function">hostname</span>                       print the hypervisor <span class="token function">hostname</span>    hypervisor-cpu-baseline        compute baseline CPU usable by a specific hypervisor    hypervisor-cpu-compare         compare a CPU with the CPU created by a hypervisor on the <span class="token function">host</span>    maxvcpus                       connection vcpu maximum    node-memory-tune               Get or <span class="token builtin class-name">set</span> node memory parameters    nodecpumap                     node cpu map    nodecpustats                   Prints cpu stats of the node.    nodeinfo                       node information    nodememstats                   Prints memory stats of the node.    nodesuspend                    <span class="token function">suspend</span> the <span class="token function">host</span> node <span class="token keyword">for</span> a given <span class="token function">time</span> duration    sysinfo                        print the hypervisor sysinfo    uri                            print the hypervisor canonical URI    version                        show version Interface <span class="token punctuation">(</span>help keyword <span class="token string">'interface'</span><span class="token punctuation">)</span>    iface-begin                    create a snapshot of current interfaces settings, <span class="token function">which</span> can be later committed <span class="token punctuation">(</span>iface-commit<span class="token punctuation">)</span> or restored <span class="token punctuation">(</span>iface-rollback<span class="token punctuation">)</span>    iface-bridge                   create a bridge device and attach an existing network device to it    iface-commit                   commit changes made since iface-begin and <span class="token function">free</span> restore point    iface-define                   define an inactive persistent physical <span class="token function">host</span> interface or modify an existing persistent one from an XML <span class="token function">file</span>    iface-destroy                  destroy a physical <span class="token function">host</span> interface <span class="token punctuation">(</span>disable it / <span class="token string">"if-down"</span><span class="token punctuation">)</span>    iface-dumpxml                  interface information <span class="token keyword">in</span> XML    iface-edit                     edit XML configuration <span class="token keyword">for</span> a physical <span class="token function">host</span> interface    iface-list                     list physical <span class="token function">host</span> interfaces    iface-mac                      convert an interface name to interface MAC address    iface-name                     convert an interface MAC address to interface name    iface-rollback                 rollback to previous saved configuration created via iface-begin    iface-start                    start a physical <span class="token function">host</span> interface <span class="token punctuation">(</span>enable it / <span class="token string">"if-up"</span><span class="token punctuation">)</span>    iface-unbridge                 undefine a bridge device after detaching its slave device    iface-undefine                 undefine a physical <span class="token function">host</span> interface <span class="token punctuation">(</span>remove it from configuration<span class="token punctuation">)</span> Network Filter <span class="token punctuation">(</span>help keyword <span class="token string">'filter'</span><span class="token punctuation">)</span>    nwfilter-define                define or update a network filter from an XML <span class="token function">file</span>    nwfilter-dumpxml               network filter information <span class="token keyword">in</span> XML    nwfilter-edit                  edit XML configuration <span class="token keyword">for</span> a network filter    nwfilter-list                  list network filters    nwfilter-undefine              undefine a network filter    nwfilter-binding-create        create a network filter binding from an XML <span class="token function">file</span>    nwfilter-binding-delete        delete a network filter binding    nwfilter-binding-dumpxml       network filter information <span class="token keyword">in</span> XML    nwfilter-binding-list          list network filter bindings Networking <span class="token punctuation">(</span>help keyword <span class="token string">'network'</span><span class="token punctuation">)</span>    net-autostart                  autostart a network    net-create                     create a network from an XML <span class="token function">file</span>    net-define                     define an inactive persistent virtual network or modify an existing persistent one from an XML <span class="token function">file</span>    net-destroy                    destroy <span class="token punctuation">(</span>stop<span class="token punctuation">)</span> a network    net-dhcp-leases                print lease info <span class="token keyword">for</span> a given network    net-dumpxml                    network information <span class="token keyword">in</span> XML    net-edit                       edit XML configuration <span class="token keyword">for</span> a network    net-event                      Network Events    net-info                       network information    net-list                       list networks    net-name                       convert a network UUID to network name    net-start                      start a <span class="token punctuation">(</span>previously defined<span class="token punctuation">)</span> inactive network    net-undefine                   undefine a persistent network    net-update                     update parts of an existing network<span class="token string">'s configuration    net-uuid                       convert a network name to network UUID Node Device (help keyword '</span>nodedev<span class="token string">')    nodedev-create                 create a device defined by an XML file on the node    nodedev-destroy                destroy (stop) a device on the node    nodedev-detach                 detach node device from its device driver    nodedev-dumpxml                node device details in XML    nodedev-list                   enumerate devices on this host    nodedev-reattach               reattach node device to its device driver    nodedev-reset                  reset node device    nodedev-event                  Node Device Events Secret (help keyword '</span>secret<span class="token string">')    secret-define                  define or modify a secret from an XML file    secret-dumpxml                 secret attributes in XML    secret-event                   Secret Events    secret-get-value               Output a secret value    secret-list                    list secrets    secret-set-value               set a secret value    secret-undefine                undefine a secret Snapshot (help keyword '</span>snapshot<span class="token string">')    snapshot-create                Create a snapshot from XML    snapshot-create-as             Create a snapshot from a set of args    snapshot-current               Get or set the current snapshot    snapshot-delete                Delete a domain snapshot    snapshot-dumpxml               Dump XML for a domain snapshot    snapshot-edit                  edit XML for a snapshot    snapshot-info                  snapshot information    snapshot-list                  List snapshots for a domain    snapshot-parent                Get the name of the parent of a snapshot    snapshot-revert                Revert a domain to a snapshot Storage Pool (help keyword '</span>pool<span class="token string">')    find-storage-pool-sources-as   find potential storage pool sources    find-storage-pool-sources      discover potential storage pool sources    pool-autostart                 autostart a pool    pool-build                     build a pool    pool-create-as                 create a pool from a set of args    pool-create                    create a pool from an XML file    pool-define-as                 define a pool from a set of args    pool-define                    define an inactive persistent storage pool or modify an existing persistent one from an XML file    pool-delete                    delete a pool    pool-destroy                   destroy (stop) a pool    pool-dumpxml                   pool information in XML    pool-edit                      edit XML configuration for a storage pool    pool-info                      storage pool information    pool-list                      list pools    pool-name                      convert a pool UUID to pool name    pool-refresh                   refresh a pool    pool-start                     start a (previously defined) inactive pool    pool-undefine                  undefine an inactive pool    pool-uuid                      convert a pool name to pool UUID    pool-event                     Storage Pool Events Storage Volume (help keyword '</span>volume<span class="token string">')    vol-clone                      clone a volume.    vol-create-as                  create a volume from a set of args    vol-create                     create a vol from an XML file    vol-create-from                create a vol, using another volume as input    vol-delete                     delete a vol    vol-download                   download volume contents to a file    vol-dumpxml                    vol information in XML    vol-info                       storage vol information    vol-key                        returns the volume key for a given volume name or path    vol-list                       list vols    vol-name                       returns the volume name for a given volume key or path    vol-path                       returns the volume path for a given volume name or key    vol-pool                       returns the storage pool for a given volume key or path    vol-resize                     resize a vol    vol-upload                     upload file contents to a volume    vol-wipe                       wipe a vol Virsh itself (help keyword '</span>virsh'<span class="token punctuation">)</span>    <span class="token builtin class-name">cd</span>                             change the current directory    <span class="token builtin class-name">echo</span>                           <span class="token builtin class-name">echo</span> arguments    <span class="token builtin class-name">exit</span>                           quit this interactive terminal    <span class="token builtin class-name">help</span>                           print <span class="token builtin class-name">help</span>    <span class="token builtin class-name">pwd</span>                            print the current directory    quit                           quit this interactive terminal    connect                        <span class="token punctuation">(</span>re<span class="token punctuation">)</span>connect to hypervisor<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="qemu-img"><a href="#qemu-img" class="headerlink" title="qemu-img"></a>qemu-img</h3><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">qemu-img version <span class="token number">1.5</span>.3, Copyright <span class="token punctuation">(</span>c<span class="token punctuation">)</span> <span class="token number">2004</span>-2008 Fabrice Bellardusage: qemu-img <span class="token builtin class-name">command</span> <span class="token punctuation">[</span>command options<span class="token punctuation">]</span>QEMU disk image utilityCommand syntax:  check <span class="token punctuation">[</span>-q<span class="token punctuation">]</span> <span class="token punctuation">[</span>-f fmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>--output<span class="token operator">=</span>ofmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>-r <span class="token punctuation">[</span>leaks <span class="token operator">|</span> all<span class="token punctuation">]</span><span class="token punctuation">]</span> <span class="token punctuation">[</span>-T src_cache<span class="token punctuation">]</span> filename  create <span class="token punctuation">[</span>-q<span class="token punctuation">]</span> <span class="token punctuation">[</span>-f fmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>-o options<span class="token punctuation">]</span> filename <span class="token punctuation">[</span>size<span class="token punctuation">]</span>  commit <span class="token punctuation">[</span>-q<span class="token punctuation">]</span> <span class="token punctuation">[</span>-f fmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>-t cache<span class="token punctuation">]</span> filename  compare <span class="token punctuation">[</span>-f fmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>-F fmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>-T src_cache<span class="token punctuation">]</span> <span class="token punctuation">[</span>-p<span class="token punctuation">]</span> <span class="token punctuation">[</span>-q<span class="token punctuation">]</span> <span class="token punctuation">[</span>-s<span class="token punctuation">]</span> filename1 filename2  convert <span class="token punctuation">[</span>-c<span class="token punctuation">]</span> <span class="token punctuation">[</span>-p<span class="token punctuation">]</span> <span class="token punctuation">[</span>-q<span class="token punctuation">]</span> <span class="token punctuation">[</span>-n<span class="token punctuation">]</span> <span class="token punctuation">[</span>-f fmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>-t cache<span class="token punctuation">]</span> <span class="token punctuation">[</span>-T src_cache<span class="token punctuation">]</span> <span class="token punctuation">[</span>-O output_fmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>-o options<span class="token punctuation">]</span> <span class="token punctuation">[</span>-s snapshot_name<span class="token punctuation">]</span> <span class="token punctuation">[</span>-S sparse_size<span class="token punctuation">]</span> filename <span class="token punctuation">[</span>filename2 <span class="token punctuation">[</span><span class="token punctuation">..</span>.<span class="token punctuation">]</span><span class="token punctuation">]</span> output_filename  info <span class="token punctuation">[</span>-f fmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>--output<span class="token operator">=</span>ofmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>--backing-chain<span class="token punctuation">]</span> filename  map <span class="token punctuation">[</span>-f fmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>--output<span class="token operator">=</span>ofmt<span class="token punctuation">]</span> filename  snapshot <span class="token punctuation">[</span>-q<span class="token punctuation">]</span> <span class="token punctuation">[</span>-l <span class="token operator">|</span> -a snapshot <span class="token operator">|</span> -c snapshot <span class="token operator">|</span> -d snapshot<span class="token punctuation">]</span> filename  rebase <span class="token punctuation">[</span>-q<span class="token punctuation">]</span> <span class="token punctuation">[</span>-f fmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>-t cache<span class="token punctuation">]</span> <span class="token punctuation">[</span>-T src_cache<span class="token punctuation">]</span> <span class="token punctuation">[</span>-p<span class="token punctuation">]</span> <span class="token punctuation">[</span>-u<span class="token punctuation">]</span> -b backing_file <span class="token punctuation">[</span>-F backing_fmt<span class="token punctuation">]</span> filename  resize <span class="token punctuation">[</span>-q<span class="token punctuation">]</span> filename <span class="token punctuation">[</span>+ <span class="token operator">|</span> -<span class="token punctuation">]</span>size  amend <span class="token punctuation">[</span>-q<span class="token punctuation">]</span> <span class="token punctuation">[</span>-f fmt<span class="token punctuation">]</span> <span class="token punctuation">[</span>-t cache<span class="token punctuation">]</span> -o options filenameCommand parameters:  <span class="token string">'filename'</span> is a disk image filename  <span class="token string">'fmt'</span> is the disk image format. It is guessed automatically <span class="token keyword">in</span> <span class="token function">most</span> cases  <span class="token string">'cache'</span> is the cache mode used to <span class="token function">write</span> the output disk image, the valid    options are: <span class="token string">'none'</span>, <span class="token string">'writeback'</span> <span class="token punctuation">(</span>default, except <span class="token keyword">for</span> convert<span class="token punctuation">)</span>, <span class="token string">'writethrough'</span>,    <span class="token string">'directsync'</span> and <span class="token string">'unsafe'</span> <span class="token punctuation">(</span>default <span class="token keyword">for</span> convert<span class="token punctuation">)</span>  <span class="token string">'src_cache'</span> is the cache mode used to <span class="token builtin class-name">read</span> input disk images, the valid    options are the same as <span class="token keyword">for</span> the <span class="token string">'cache'</span> option  <span class="token string">'size'</span> is the disk image size <span class="token keyword">in</span> bytes. Optional suffixes    <span class="token string">'k'</span> or <span class="token string">'K'</span> <span class="token punctuation">(</span>kilobyte, <span class="token number">1024</span><span class="token punctuation">)</span>, <span class="token string">'M'</span> <span class="token punctuation">(</span>megabyte, 1024k<span class="token punctuation">)</span>, <span class="token string">'G'</span> <span class="token punctuation">(</span>gigabyte, 1024M<span class="token punctuation">)</span>,    <span class="token string">'T'</span> <span class="token punctuation">(</span>terabyte, 1024G<span class="token punctuation">)</span>, <span class="token string">'P'</span> <span class="token punctuation">(</span>petabyte, 1024T<span class="token punctuation">)</span> and <span class="token string">'E'</span> <span class="token punctuation">(</span>exabyte, 1024P<span class="token punctuation">)</span>  are    supported. <span class="token string">'b'</span> is ignored.  <span class="token string">'output_filename'</span> is the destination disk image filename  <span class="token string">'output_fmt'</span> is the destination <span class="token function">format</span>  <span class="token string">'options'</span> is a comma separated list of <span class="token function">format</span> specific options <span class="token keyword">in</span> a    <span class="token assign-left variable">name</span><span class="token operator">=</span>value format. Use -o ? <span class="token keyword">for</span> an overview of the options supported by the    used <span class="token function">format</span>  <span class="token string">'-c'</span> indicates that target image must be compressed <span class="token punctuation">(</span>qcow <span class="token function">format</span> only<span class="token punctuation">)</span>  <span class="token string">'-u'</span> enables unsafe rebasing. It is assumed that old and new backing <span class="token function">file</span>       match exactly. The image doesn<span class="token string">'t need a working backing file before       rebasing in this case (useful for renaming the backing file)  '</span>-h<span class="token string">' with or without a command shows this help and lists the supported formats  '</span>-p<span class="token string">' show progress of command (only certain commands)  '</span>-q<span class="token string">' use Quiet mode - do not print any output (except errors)  '</span>-S<span class="token string">' indicates the consecutive number of bytes (defaults to 4k) that must       contain only zeros for qemu-img to create a sparse image during       conversion. If the number of bytes is 0, the source will not be scanned for       unallocated or zero sectors, and the destination image will always be       fully allocated  '</span>--output<span class="token string">' takes the format in which the output must be done (human or json)  '</span>-n<span class="token string">' skips the target volume creation (useful if the volume is created       prior to running qemu-img)Parameters to check subcommand:  '</span>-r<span class="token string">' tries to repair any inconsistencies that are found during the check.       '</span>-r leaks<span class="token string">' repairs only cluster leaks, whereas '</span>-r all<span class="token string">' fixes all       kinds of errors, with a higher risk of choosing the wrong fix or       hiding corruption that has already occurred.Parameters to snapshot subcommand:  '</span>snapshot<span class="token string">' is the name of the snapshot to create, apply or delete  '</span>-a<span class="token string">' applies a snapshot (revert disk to saved state)  '</span>-c<span class="token string">' creates a snapshot  '</span>-d<span class="token string">' deletes a snapshot  '</span>-l<span class="token string">' lists all snapshots in the given imageParameters to compare subcommand:  '</span>-f<span class="token string">' first image format  '</span>-F<span class="token string">' second image format  '</span>-s' run <span class="token keyword">in</span> Strict mode - fail on different image size or sector allocationSupported formats: vvfat vpc vmdk vhdx vdi <span class="token function">ssh</span> sheepdog rbd raw host_cdrom host_floppy host_device <span class="token function">file</span> qed qcow2 qcow parallels nbd iscsi gluster dmg tftp ftps <span class="token function">ftp</span> https http cloop bochs blkverify blkdebug<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="kvm自动化脚本"><a href="#kvm自动化脚本" class="headerlink" title="kvm自动化脚本"></a>kvm自动化脚本</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">virt-install  --name<span class="token operator">=</span>EVOPSS --ram <span class="token number">8192</span> --vcpus<span class="token operator">=</span><span class="token number">2</span> /--disk <span class="token assign-left variable">path</span><span class="token operator">=</span>/var/lib/libvirt/images/tomcat_01.img,size<span class="token operator">=</span><span class="token number">20</span>,format<span class="token operator">=</span>raw,bus<span class="token operator">=</span>virtio /--cdrom /var/iso/CentOS-6.7-x86_64-minimal.iso --network <span class="token assign-left variable">bridge</span><span class="token operator">=</span>br0,model<span class="token operator">=</span>virtio /--vnc --accelerate --force  --autostart<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Kubernetes管理】KubeSphere</title>
      <link href="/2021/11/21/kubernetes/kubesphere/kubernetes-guan-li-kubesphere/"/>
      <url>/2021/11/21/kubernetes/kubesphere/kubernetes-guan-li-kubesphere/</url>
      
        <content type="html"><![CDATA[<h1 id="【Kubernetes管理】KubeSphere"><a href="#【Kubernetes管理】KubeSphere" class="headerlink" title="【Kubernetes管理】KubeSphere"></a>【Kubernetes管理】KubeSphere</h1><blockquote><p>kubesphere 官网地址：<a href="https://kubesphere.com.cn/">https://kubesphere.com.cn/</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Kubernetes管理】Rancher</title>
      <link href="/2021/11/21/kubernetes/rancher/kubernetes-guan-li-rancher/"/>
      <url>/2021/11/21/kubernetes/rancher/kubernetes-guan-li-rancher/</url>
      
        <content type="html"><![CDATA[<h1 id="【Kubernetes管理】Rancher"><a href="#【Kubernetes管理】Rancher" class="headerlink" title="【Kubernetes管理】Rancher"></a>【Kubernetes管理】Rancher</h1><blockquote><p>rancher官方文档： <a href="https://rancher.com/">https://rancher.com</a></p><p>ranchergithub地址：<a href="https://github.com/rancher/rancher">https://github.com/rancher/rancher</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【镜像构建】s2i</title>
      <link href="/2021/11/19/s2i/jing-xiang-gou-jian-s2i/"/>
      <url>/2021/11/19/s2i/jing-xiang-gou-jian-s2i/</url>
      
        <content type="html"><![CDATA[<h1 id="【镜像构建】s2i"><a href="#【镜像构建】s2i" class="headerlink" title="【镜像构建】s2i"></a>【镜像构建】s2i</h1><blockquote><p>s2i gitHub 地址：<a href="https://github.com/openshift/source-to-image">https://github.com/openshift/source-to-image</a> </p></blockquote><h2 id="s2i简介"><a href="#s2i简介" class="headerlink" title="s2i简介"></a>s2i简介</h2><p><strong>Source-to-image (S2I)</strong> 是一个直接将源代码构建成镜像的自动化构建工具，它是通过将源代码放入一个负责编译源代码的 构建器镜像(Builder image) 中，自动将编译后的代码打包成 Docker 镜像。</p><p><strong>s2i优点：</strong></p><ul><li><p><strong>速度</strong>，S2I 可以实现很复杂的操作逻辑，而不会创建新的镜像层，所以运行起来很快。</p></li><li><p><strong>打补丁</strong>，如果所依赖的镜像需要安全补丁，S2I 允许你一次性重新构建所有镜像。</p></li><li><p><strong>高效</strong>, 在构建过程中，S2I不允许运行任意的 yum install 命令，以防止降低开发迭代速度</p></li><li><p><strong>生态</strong>，S2I 鼓励一个共享镜像生态。从而你的应用可以实现最佳实践。</p></li></ul><h3 id="s2i构建流程"><a href="#s2i构建流程" class="headerlink" title="s2i构建流程"></a>s2i构建流程</h3><ul><li><p>根据构建器镜像 (Builder Image) 运行容器，程序源代码注入到容器中指定的目录。</p></li><li><p>执行构建器镜像 (Builder Image) 中的 <code>assemble</code>脚本，会将源代码构建成可直接运行的程序。比如拉取各种依赖，配置程序到工作目录下等工作</p></li><li><p>设置运行构建器镜像 (Builder Image) 中提供的 <code>run</code>脚本作为容器的启动命令，然后提交新的镜像，此镜像就是用户所需要的应用程序镜像。</p></li></ul><h3 id="s2i配置文件信息"><a href="#s2i配置文件信息" class="headerlink" title="s2i配置文件信息"></a>s2i配置文件信息</h3><ul><li><strong>assemble</strong>： 负责构建应用程序的脚本</li><li><strong>run</strong>：负责运行应用程序的脚本</li><li><strong>save-artifacts</strong>：在增量构建时负责依赖管理的脚本 （可选)</li><li><strong>usage</strong>： 使用说明脚本 （可选）</li><li><strong>test</strong>：测试脚本 （可选)</li><li><strong>S2Itemplate</strong>： -描述构建程序所使用的基础环境</li></ul><p><strong>推荐将这些文件放到源码中<code>.s2i/bin</code>下</strong></p><h2 id="s2i环境配置"><a href="#s2i环境配置" class="headerlink" title="s2i环境配置"></a>s2i环境配置</h2><ul><li><p>linux安装</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"># 下载的对应的s2i的二进制包，解压chmod +x s2icp /path/to/s2i /usr/local/bin<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p>mac安装</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">brew install source-to-image<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>使用go get</p><pre class="line-numbers language-none"><code class="language-none">go get github.com/openshift/source-to-image/cmd/s2i<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【数据备份】redis-cli</title>
      <link href="/2021/11/19/database/redis/shu-ju-bei-fen-redis-cli/"/>
      <url>/2021/11/19/database/redis/shu-ju-bei-fen-redis-cli/</url>
      
        <content type="html"><![CDATA[<h1 id="【数据备份】redis-cli"><a href="#【数据备份】redis-cli" class="headerlink" title="【数据备份】redis-cli"></a>【数据备份】redis-cli</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【数据备份】mongodump</title>
      <link href="/2021/11/19/database/mongodb/shu-ju-bei-fen-mongodump/"/>
      <url>/2021/11/19/database/mongodb/shu-ju-bei-fen-mongodump/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【数据备份】pg_dump</title>
      <link href="/2021/11/19/database/postgresql/shu-ju-bei-fen-pg-dump/"/>
      <url>/2021/11/19/database/postgresql/shu-ju-bei-fen-pg-dump/</url>
      
        <content type="html"><![CDATA[<h1 id="【数据备份】pg-dump"><a href="#【数据备份】pg-dump" class="headerlink" title="【数据备份】pg_dump"></a>【数据备份】pg_dump</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【仓库】ChartMuseum</title>
      <link href="/2021/11/18/helm/registry/cang-ku-chartmuseum/"/>
      <url>/2021/11/18/helm/registry/cang-ku-chartmuseum/</url>
      
        <content type="html"><![CDATA[<h1 id="【仓库】ChartMuseum"><a href="#【仓库】ChartMuseum" class="headerlink" title="【仓库】ChartMuseum"></a>【仓库】ChartMuseum</h1><blockquote><p>Chartmuseum 官网： <a href="https://chartmuseum.com/">https://chartmuseum.com/</a></p><p>chartmuseum github地址：<a href="https://github.com/helm/chartmuseum">https://github.com/helm/chartmuseum</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【每日一库】go-cli</title>
      <link href="/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-go-cli/"/>
      <url>/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-go-cli/</url>
      
        <content type="html"><![CDATA[<h1 id="【每日一库】go-cli"><a href="#【每日一库】go-cli" class="headerlink" title="【每日一库】go-cli"></a>【每日一库】go-cli</h1><blockquote><p>cli github地址： <a href="https://github.com/urfave/cli">https://github.com/urfave/cli</a></p></blockquote><h2 id="go-cli简介"><a href="#go-cli简介" class="headerlink" title="go-cli简介"></a>go-cli简介</h2><h2 id="go-cli环境配置"><a href="#go-cli环境配置" class="headerlink" title="go-cli环境配置"></a>go-cli环境配置</h2><ul><li><p>cli v1版本</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token assign-left variable">GO111MODULE</span><span class="token operator">=</span>on go get github.com/urfave/cli<span class="token function">import</span> <span class="token punctuation">(</span>  <span class="token string">"github.com/urfave/cli"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>cli v2版本</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token assign-left variable">GO111MODULE</span><span class="token operator">=</span>on go get github.com/urfave/cli/v2<span class="token function">import</span> <span class="token punctuation">(</span>  <span class="token string">"github.com/urfave/cli/v2"</span> // imports as package <span class="token string">"cli"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【镜像构建】BuildKit</title>
      <link href="/2021/11/18/buildkit/jing-xiang-gou-jian-buildkit/"/>
      <url>/2021/11/18/buildkit/jing-xiang-gou-jian-buildkit/</url>
      
        <content type="html"><![CDATA[<h1 id="【镜像构建】BuildKit"><a href="#【镜像构建】BuildKit" class="headerlink" title="【镜像构建】BuildKit"></a>【镜像构建】BuildKit</h1><blockquote><p>github 地址：<a href="https://github.com/moby/buildkit">https://github.com/moby/buildkit</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【镜像构建】kaniko</title>
      <link href="/2021/11/18/kaniko/jing-xiang-gou-jian-kaniko/"/>
      <url>/2021/11/18/kaniko/jing-xiang-gou-jian-kaniko/</url>
      
        <content type="html"><![CDATA[<h1 id="【镜像构建】Kaniko"><a href="#【镜像构建】Kaniko" class="headerlink" title="【镜像构建】Kaniko"></a>【镜像构建】Kaniko</h1><blockquote><p>GitHub 地址：<a href="https://github.com/GoogleContainerTools/kaniko">https://github.com/GoogleContainerTools/kaniko</a></p></blockquote><h2 id="kaniko简介"><a href="#kaniko简介" class="headerlink" title="kaniko简介"></a>kaniko简介</h2><p>kaniko是一个在容器或者Kubernetes集群中用于从dockerfile中构建容器镜像的工具。kaniko不依赖于docker的守护进程，可以在用户空间中正确执行dockerfile中的每个构建命令。这使您可以在<code>没有特权模式</code>或没有运行<code>Docker daemon</code>的环境中构建容器镜像。</p><h3 id="kaniko工作原理"><a href="#kaniko工作原理" class="headerlink" title="kaniko工作原理"></a>kaniko工作原理</h3><p>Kaniko使用自己的<code>executor</code>执行构建步骤，该构建步骤在容器中运行。当然，需要使构建上下文对容器或Pod可用，使得它能在构建中被使用。因为构建步骤是在容器内执行的，所以仍然最终依赖Docker守护程序或其他容器运行时，但重要的区别是构建步骤本身是由执行程序代码执行的。</p><p><code>executor</code>遍历Dockerfile中定义的每个构建步骤。它拉取为构建阶段准备的源镜像，并解压缩其rootfs。然后，它按顺序执行每个Dockerfile指令，并随其添加或更改rootfs的内容。如果对rootfs进行了更改，则<code>executor</code>对文件系统做快照化，更改“diff”层，并在必要时更新镜像元数据。通过将文件系统的先前状态与Dockerfile指令执行后的状态进行比较，可以实现快照。除了操作完全在用户空间中执行之外，这与overlayfs之类的copy-on-write文件系统的行为并无不同。构建步骤完成后，将diff层依次添加到基础镜像中以形成新镜像。</p><h2 id="kaniko环境配置"><a href="#kaniko环境配置" class="headerlink" title="kaniko环境配置"></a>kaniko环境配置</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【每日一库】rpc</title>
      <link href="/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-rpc/"/>
      <url>/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-rpc/</url>
      
        <content type="html"><![CDATA[<h1 id="【每日一库】rpc"><a href="#【每日一库】rpc" class="headerlink" title="【每日一库】rpc"></a>【每日一库】rpc</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【每日一库】go-ini</title>
      <link href="/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-go-ini/"/>
      <url>/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-go-ini/</url>
      
        <content type="html"><![CDATA[<h1 id="【每日一库】go-ini"><a href="#【每日一库】go-ini" class="headerlink" title="【每日一库】go-ini"></a>【每日一库】go-ini</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【每日一库】gin</title>
      <link href="/2021/11/18/golang/gin/go-mei-ri-yi-ku-gin/"/>
      <url>/2021/11/18/golang/gin/go-mei-ri-yi-ku-gin/</url>
      
        <content type="html"><![CDATA[<h1 id="【每日一库】gin"><a href="#【每日一库】gin" class="headerlink" title="【每日一库】gin"></a>【每日一库】gin</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【每日一库】email</title>
      <link href="/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-email/"/>
      <url>/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-email/</url>
      
        <content type="html"><![CDATA[<h1 id="【每日一库】email"><a href="#【每日一库】email" class="headerlink" title="【每日一库】email"></a>【每日一库】email</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【每日一库】Logrus</title>
      <link href="/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-logrus/"/>
      <url>/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-logrus/</url>
      
        <content type="html"><![CDATA[<h1 id="【每日一库】Logrus"><a href="#【每日一库】Logrus" class="headerlink" title="【每日一库】Logrus"></a>【每日一库】Logrus</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【go每日一库】Cobra</title>
      <link href="/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-cobra/"/>
      <url>/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-cobra/</url>
      
        <content type="html"><![CDATA[<h1 id="【go每日一库】Cobra"><a href="#【go每日一库】Cobra" class="headerlink" title="【go每日一库】Cobra"></a>【go每日一库】Cobra</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【每日一库】Gorm</title>
      <link href="/2021/11/18/golang/gorm/go-mei-ri-yi-ku-gorm/"/>
      <url>/2021/11/18/golang/gorm/go-mei-ri-yi-ku-gorm/</url>
      
        <content type="html"><![CDATA[<h1 id="【每日一库】Gorm"><a href="#【每日一库】Gorm" class="headerlink" title="【每日一库】Gorm"></a>【每日一库】Gorm</h1><blockquote><p>gorm 官网：<a href="https://gorm.io/">https://gorm.io/</a></p><p>gorm github地址：<a href="https://github.com/go-gorm/gorm">https://github.com/go-gorm/gorm</a></p></blockquote><h2 id="gorm简介"><a href="#gorm简介" class="headerlink" title="gorm简介"></a>gorm简介</h2><p><strong>特性：</strong></p><ul><li>全功能 ORM</li><li>关联 (<code>Has One</code>，<code>Has Many</code>，Belongs To，<code>Many To Many</code>，多态，单表继承)</li><li>Create，Save，Update，Delete，Find 中钩子方法</li><li>支持 <code>Preload</code>、<code>Joins</code> 的预加载</li><li>事务，嵌套事务，Save Point，Rollback To Saved Point</li><li>Context、预编译模式、DryRun 模式</li><li>批量插入，FindInBatches，Find/Create with Map，使用 SQL 表达式、Context Valuer 进行 CRUD</li><li>SQL 构建器，Upsert，数据库锁，Optimizer/Index/Comment Hint，命名参数，子查询</li><li>复合主键，索引，约束</li><li>Auto Migration</li><li>自定义 Logger</li><li>灵活的可扩展插件 API：Database Resolver（多数据库，读写分离）、Prometheus…</li></ul><h2 id="gorm环境配置"><a href="#gorm环境配置" class="headerlink" title="gorm环境配置"></a>gorm环境配置</h2><p>GORM 官方支持的数据库类型有： MySQL, PostgreSQL, SQlite, SQL Server</p><ul><li><strong>gorm安装</strong></li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">go get -u gorm.io/gormgo get -u gorm.io/driver/mysql # mysql引擎go get -u gorm.io/driver/postgres # postgresql引擎go get -u gorm.io/driver/sqlite # sqlite引擎go get -u gorm.io/driver/sqlserver # sqlserver引擎go get -u gorm.io/driver/clickhouse # clickhouse引擎<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><strong>gorm连接mysql</strong></li></ul><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">import</span> <span class="token punctuation">(</span><span class="token string">"gorm.io/driver/mysql"</span><span class="token string">"gorm.io/gorm"</span><span class="token punctuation">)</span><span class="token keyword">func</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>dsn <span class="token operator">:=</span> <span class="token string">"user:pass@tcp(127.0.0.1:3306)/dbname?charset=utf8mb4&amp;parseTime=True&amp;loc=Local"</span>gorm<span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span>mysql<span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span>dsn<span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>gorm<span class="token punctuation">.</span>Config<span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token comment">// 添加mysql的高级配置</span>db<span class="token punctuation">,</span> err <span class="token operator">:=</span> gorm<span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span>mysql<span class="token punctuation">.</span><span class="token function">New</span><span class="token punctuation">(</span>mysql<span class="token punctuation">.</span>Config<span class="token punctuation">{</span>  DSN<span class="token punctuation">:</span> <span class="token string">"gorm:gorm@tcp(127.0.0.1:3306)/gorm?charset=utf8&amp;parseTime=True&amp;loc=Local"</span><span class="token punctuation">,</span> <span class="token comment">// DSN data source name</span>  DefaultStringSize<span class="token punctuation">:</span> <span class="token number">256</span><span class="token punctuation">,</span> <span class="token comment">// string 类型字段的默认长度</span>  DisableDatetimePrecision<span class="token punctuation">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span> <span class="token comment">// 禁用 datetime 精度，MySQL 5.6 之前的数据库不支持</span>  DontSupportRenameIndex<span class="token punctuation">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span> <span class="token comment">// 重命名索引时采用删除并新建的方式，MySQL 5.7 之前的数据库和 MariaDB 不支持重命名索引</span>  DontSupportRenameColumn<span class="token punctuation">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span> <span class="token comment">// 用 `change` 重命名列，MySQL 8 之前的数据库和 MariaDB 不支持重命名列</span>  SkipInitializeWithVersion<span class="token punctuation">:</span> <span class="token boolean">false</span><span class="token punctuation">,</span> <span class="token comment">// 根据当前 MySQL 版本自动配置</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>gorm<span class="token punctuation">.</span>Config<span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p><strong>注意：</strong>想要正确的处理 <code>time.Time</code> ，您需要带上 <code>parseTime</code> 参数， 要支持完整的 UTF-8 编码，您需要将 <code>charset=utf8</code> 更改为 <code>charset=utf8mb4</code></p></blockquote><ul><li><strong>gorm连接postgresql</strong></li></ul><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">import</span> <span class="token punctuation">(</span>  <span class="token string">"gorm.io/driver/postgres"</span>  <span class="token string">"gorm.io/gorm"</span><span class="token punctuation">)</span>dsn <span class="token operator">:=</span> <span class="token string">"host=localhost user=gorm password=gorm dbname=gorm port=9920 sslmode=disable TimeZone=Asia/Shanghai"</span>db<span class="token punctuation">,</span> err <span class="token operator">:=</span> gorm<span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span>postgres<span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span>dsn<span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>gorm<span class="token punctuation">.</span>Config<span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token comment">//  pgx 作为 postgres 的 database/sql 驱动，默认情况下，它会启用 prepared statement 缓存</span>db<span class="token punctuation">,</span> err <span class="token operator">:=</span> gorm<span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span>postgres<span class="token punctuation">.</span><span class="token function">New</span><span class="token punctuation">(</span>postgres<span class="token punctuation">.</span>Config<span class="token punctuation">{</span>  DSN<span class="token punctuation">:</span> <span class="token string">"user=gorm password=gorm dbname=gorm port=9920 sslmode=disable TimeZone=Asia/Shanghai"</span><span class="token punctuation">,</span>  PreferSimpleProtocol<span class="token punctuation">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span> <span class="token comment">// disables implicit prepared statement usage</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>gorm<span class="token punctuation">.</span>Config<span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><strong>gorm连接SQLite</strong></li></ul><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">import</span> <span class="token punctuation">(</span>  <span class="token string">"gorm.io/driver/sqlite"</span>  <span class="token string">"gorm.io/gorm"</span><span class="token punctuation">)</span><span class="token comment">// github.com/mattn/go-sqlite3</span>db<span class="token punctuation">,</span> err <span class="token operator">:=</span> gorm<span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span>sqlite<span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span><span class="token string">"gorm.db"</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>gorm<span class="token punctuation">.</span>Config<span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>注意：使用 <code>file::memory:?cache=shared</code> 替代文件路径。 这会告诉 SQLite 在系统内存中使用一个临时数据库</p></blockquote><ul><li><strong>gorm连接SQL Server</strong></li></ul><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">import</span> <span class="token punctuation">(</span>  <span class="token string">"gorm.io/driver/sqlserver"</span>  <span class="token string">"gorm.io/gorm"</span><span class="token punctuation">)</span><span class="token comment">// github.com/denisenkom/go-mssqldb</span>dsn <span class="token operator">:=</span> <span class="token string">"sqlserver://gorm:LoremIpsum86@localhost:9930?database=gorm"</span>db<span class="token punctuation">,</span> err <span class="token operator">:=</span> gorm<span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span>sqlserver<span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span>dsn<span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>gorm<span class="token punctuation">.</span>Config<span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="模型定义"><a href="#模型定义" class="headerlink" title="模型定义"></a>模型定义</h2><p>模型是标准的 struct，由 Go 的基本数据类型、实现了<code>Scanner</code>和<code>Valuer</code>接口的自定义类型及其指针或别名组成。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">type</span> User <span class="token keyword">struct</span> <span class="token punctuation">{</span>  ID           <span class="token builtin">uint</span>  Name         <span class="token builtin">string</span>  Email        <span class="token operator">*</span><span class="token builtin">string</span>  Age          <span class="token builtin">uint8</span>  Birthday     <span class="token operator">*</span>time<span class="token punctuation">.</span>Time  MemberNumber sql<span class="token punctuation">.</span>NullString  ActivatedAt  sql<span class="token punctuation">.</span>NullTime  CreatedAt    time<span class="token punctuation">.</span>Time  UpdatedAt    time<span class="token punctuation">.</span>Time<span class="token punctuation">}</span><span class="token comment">// gorm.Model</span><span class="token comment">// GORM 定义一个 gorm.Model 结构体，其包括字段 ID、CreatedAt、UpdatedAt、DeletedAt</span><span class="token keyword">type</span> Model <span class="token keyword">struct</span> <span class="token punctuation">{</span>  ID        <span class="token builtin">uint</span>           <span class="token string">`gorm:"primaryKey"`</span>  CreatedAt time<span class="token punctuation">.</span>Time  UpdatedAt time<span class="token punctuation">.</span>Time  DeletedAt gorm<span class="token punctuation">.</span>DeletedAt <span class="token string">`gorm:"index"`</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="约定"><a href="#约定" class="headerlink" title="约定"></a>约定</h3><p>GORM 倾向于约定，而不是配置。默认情况下，GORM 使用 <code>ID</code> 作为主键，使用结构体名的 <code>蛇形复数</code> 作为表名，字段名的 <code>蛇形</code> 作为列名，并使用 <code>CreatedAt</code>、<code>UpdatedAt</code> 字段追踪创建、更新时间</p><h3 id="高级选项"><a href="#高级选项" class="headerlink" title="高级选项"></a>高级选项</h3><h4 id="字段级权限控制"><a href="#字段级权限控制" class="headerlink" title="字段级权限控制"></a>字段级权限控制</h4><p>可导出的字段在使用 GORM 进行 CRUD 时拥有全部的权限，此外，GORM 允许您用标签控制字段级别的权限。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token comment">// 使用 GORM Migrator 创建表时，不会创建被忽略的字段</span><span class="token keyword">type</span> User <span class="token keyword">struct</span> <span class="token punctuation">{</span>  Name <span class="token builtin">string</span> <span class="token string">`gorm:"&lt;-:create"`</span> <span class="token comment">// 允许读和创建</span>  Name <span class="token builtin">string</span> <span class="token string">`gorm:"&lt;-:update"`</span> <span class="token comment">// 允许读和更新</span>  Name <span class="token builtin">string</span> <span class="token string">`gorm:"&lt;-"`</span>        <span class="token comment">// 允许读和写（创建和更新）</span>  Name <span class="token builtin">string</span> <span class="token string">`gorm:"&lt;-:false"`</span>  <span class="token comment">// 允许读，禁止写</span>  Name <span class="token builtin">string</span> <span class="token string">`gorm:"-&gt;"`</span>        <span class="token comment">// 只读（除非有自定义配置，否则禁止写）</span>  Name <span class="token builtin">string</span> <span class="token string">`gorm:"-&gt;;&lt;-:create"`</span> <span class="token comment">// 允许读和写</span>  Name <span class="token builtin">string</span> <span class="token string">`gorm:"-&gt;:false;&lt;-:create"`</span> <span class="token comment">// 仅创建（禁止从 db 读）</span>  Name <span class="token builtin">string</span> <span class="token string">`gorm:"-"`</span>  <span class="token comment">// 通过 struct 读写会忽略该字段,</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="创建-更新时间追踪（纳秒、毫秒、秒、Time）"><a href="#创建-更新时间追踪（纳秒、毫秒、秒、Time）" class="headerlink" title="创建/更新时间追踪（纳秒、毫秒、秒、Time）"></a>创建/更新时间追踪（纳秒、毫秒、秒、Time）</h4><p>如果您想要保存 UNIX（毫/纳）秒时间戳，而不是 time，您只需简单地将 <code>time.Time</code> 修改为 <code>int</code> 即可</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">type</span> User <span class="token keyword">struct</span> <span class="token punctuation">{</span>  CreatedAt time<span class="token punctuation">.</span>Time <span class="token comment">// Set to current time if it is zero on creating</span>  UpdatedAt <span class="token builtin">int</span>       <span class="token comment">// Set to current unix seconds on updating or if it is zero on creating</span>  Updated   <span class="token builtin">int64</span> <span class="token string">`gorm:"autoUpdateTime:nano"`</span> <span class="token comment">// Use unix nano seconds as updating time</span>  Updated   <span class="token builtin">int64</span> <span class="token string">`gorm:"autoUpdateTime:milli"`</span><span class="token comment">// Use unix milli seconds as updating time</span>  Created   <span class="token builtin">int64</span> <span class="token string">`gorm:"autoCreateTime"`</span>      <span class="token comment">// Use unix seconds as creating time</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="嵌入结构体"><a href="#嵌入结构体" class="headerlink" title="嵌入结构体"></a>嵌入结构体</h4><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">type</span> Author <span class="token keyword">struct</span> <span class="token punctuation">{</span>    Name  <span class="token builtin">string</span>    Email <span class="token builtin">string</span><span class="token punctuation">}</span><span class="token comment">// 对于正常的结构体字段，你也可以通过标签 embedded 将其嵌入</span><span class="token keyword">type</span> Blog <span class="token keyword">struct</span> <span class="token punctuation">{</span>  ID      <span class="token builtin">int</span>  Author  Author <span class="token string">`gorm:"embedded"`</span>  Upvotes <span class="token builtin">int32</span><span class="token punctuation">}</span><span class="token comment">// 可以使用标签 embeddedPrefix 来为 db 中的字段名添加前缀</span><span class="token keyword">type</span> Blog <span class="token keyword">struct</span> <span class="token punctuation">{</span>  ID      <span class="token builtin">int</span>  Author  Author <span class="token string">`gorm:"embedded;embeddedPrefix:author_"`</span> <span class="token comment">// == AuthorName</span>  Upvotes <span class="token builtin">int32</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="字段标签"><a href="#字段标签" class="headerlink" title="字段标签"></a>字段标签</h4><p>声明 model 时，tag 是可选的，GORM 支持以下 tag： tag 名大小写不敏感，但建议使用 <code>camelCase</code> 风格</p><table><thead><tr><th align="left">标签名</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left">column</td><td align="left">指定 db 列名</td></tr><tr><td align="left">type</td><td align="left">列数据类型，推荐使用兼容性好的通用类型，例如：所有数据库都支持 bool、int、uint、float、string、time、bytes 并且可以和其他标签一起使用，例如：<code>not null</code>、<code>size</code>, <code>autoIncrement</code>… 像 <code>varbinary(8)</code> 这样指定数据库数据类型也是支持的。在使用指定数据库数据类型时，它需要是完整的数据库数据类型，如：<code>MEDIUMINT UNSIGNED not NULL AUTO_INCREMENT</code></td></tr><tr><td align="left">size</td><td align="left">指定列大小，例如：<code>size:256</code></td></tr><tr><td align="left">primaryKey</td><td align="left">指定列为主键</td></tr><tr><td align="left">unique</td><td align="left">指定列为唯一</td></tr><tr><td align="left">default</td><td align="left">指定列的默认值</td></tr><tr><td align="left">precision</td><td align="left">指定列的精度</td></tr><tr><td align="left">scale</td><td align="left">指定列大小</td></tr><tr><td align="left">not null</td><td align="left">指定列为 NOT NULL</td></tr><tr><td align="left">autoIncrement</td><td align="left">指定列为自动增长</td></tr><tr><td align="left">autoIncrementIncrement</td><td align="left">自动步长，控制连续记录之间的间隔</td></tr><tr><td align="left">embedded</td><td align="left">嵌套字段</td></tr><tr><td align="left">embeddedPrefix</td><td align="left">嵌入字段的列名前缀</td></tr><tr><td align="left">autoCreateTime</td><td align="left">创建时追踪当前时间，对于 <code>int</code> 字段，它会追踪秒级时间戳，您可以使用 <code>nano</code>/<code>milli</code> 来追踪纳秒、毫秒时间戳，例如：<code>autoCreateTime:nano</code></td></tr><tr><td align="left">autoUpdateTime</td><td align="left">创建/更新时追踪当前时间，对于 <code>int</code> 字段，它会追踪秒级时间戳，您可以使用 <code>nano</code>/<code>milli</code> 来追踪纳秒、毫秒时间戳，例如：<code>autoUpdateTime:milli</code></td></tr><tr><td align="left">index</td><td align="left">根据参数创建索引，多个字段使用相同的名称则创建复合索引</td></tr><tr><td align="left">uniqueIndex</td><td align="left">与 <code>index</code> 相同，但创建的是唯一索引</td></tr><tr><td align="left">check</td><td align="left">创建检查约束，例如 <code>check:age &gt; 13</code></td></tr><tr><td align="left">&lt;-</td><td align="left">设置字段写入的权限， <code>&lt;-:create</code> 只创建、<code>&lt;-:update</code> 只更新、<code>&lt;-:false</code> 无写入权限、<code>&lt;-</code> 创建和更新权限</td></tr><tr><td align="left">-&gt;</td><td align="left">设置字段读的权限，<code>-&gt;:false</code> 无读权限</td></tr><tr><td align="left">-</td><td align="left">忽略该字段，<code>-</code> 无读写权限</td></tr><tr><td align="left">comment</td><td align="left">迁移时为字段添加注释</td></tr></tbody></table><h2 id="CRUD接口"><a href="#CRUD接口" class="headerlink" title="CRUD接口"></a>CRUD接口</h2><h3 id="创建"><a href="#创建" class="headerlink" title="创建"></a>创建</h3><h3 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h3><h3 id="高级查询"><a href="#高级查询" class="headerlink" title="高级查询"></a>高级查询</h3><h3 id="更新"><a href="#更新" class="headerlink" title="更新"></a>更新</h3><h3 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h3>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【每日一库】Viper</title>
      <link href="/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-viper/"/>
      <url>/2021/11/18/golang/third-party-library/go-mei-ri-yi-ku-viper/</url>
      
        <content type="html"><![CDATA[<h1 id="【每日一库】Viper"><a href="#【每日一库】Viper" class="headerlink" title="【每日一库】Viper"></a>【每日一库】Viper</h1><blockquote><p>GitHub 地址：<a href="https://github.com/spf13/viper">https://github.com/spf13/viper</a></p></blockquote><h2 id="viper简介"><a href="#viper简介" class="headerlink" title="viper简介"></a>viper简介</h2><p>Viper是适用于Go应用程序的完整配置解决方案。它被设计用于在应用程序中工作，并且可以处理所有类型的配置需求和格式。</p><p><strong>它支持以下特性：</strong></p><ul><li>设置默认值</li><li>从<code>JSON</code>、<code>TOML</code>、<code>YAML</code>、<code>HCL</code>、<code>envfile</code>和<code>Java properties</code>格式的配置文件读取配置信息</li><li>实时监控和重新读取配置文件（可选）</li><li>从环境变量中读取</li><li>从远程配置系统（etcd或Consul）读取并监控配置变化</li><li>从命令行参数读取配置</li><li>从buffer读取配置</li><li>显式配置值</li></ul><p><strong>Viper能够为你执行下列操作</strong>：</p><ul><li>查找、加载和反序列化<code>JSON</code>、<code>TOML</code>、<code>YAML</code>、<code>HCL</code>、<code>INI</code>、<code>envfile</code>和<code>Java properties</code>格式的配置文件。</li><li>提供一种机制为你的不同配置选项设置默认值。</li><li>提供一种机制来通过命令行参数覆盖指定选项的值。</li><li>提供别名系统，以便在不破坏现有代码的情况下轻松重命名参数。</li><li>当用户提供了与默认值相同的命令行或配置文件时，可以很容易地分辨出它们之间的区别。</li></ul><p><strong>Viper的优先级</strong>（由高到低）。:</p><ul><li>显示调用<code>Set</code>设置值</li><li>命令行参数（flag）</li><li>环境变量</li><li>配置文件</li><li>key/value存储</li><li>默认值</li></ul><blockquote><p>注意：目前Viper配置的键（Key）是大小写不敏感的。</p></blockquote><h2 id="viper环境配置"><a href="#viper环境配置" class="headerlink" title="viper环境配置"></a>viper环境配置</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">go get github.com/spf13/viper<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="Viper使用"><a href="#Viper使用" class="headerlink" title="Viper使用"></a>Viper使用</h2><h3 id="值存入Viper"><a href="#值存入Viper" class="headerlink" title="值存入Viper"></a>值存入Viper</h3><h4 id="创建默认值"><a href="#创建默认值" class="headerlink" title="创建默认值"></a>创建默认值</h4><p>一个好的配置系统应该支持默认值。键不需要默认值，但如果没有通过配置文件、环境变量、远程配置或命令行标志（flag）设置键，则默认值非常有用。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">viper<span class="token punctuation">.</span><span class="token function">SetDefault</span><span class="token punctuation">(</span><span class="token string">"ContentDir"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">SetDefault</span><span class="token punctuation">(</span><span class="token string">"Taxonomies"</span><span class="token punctuation">,</span> <span class="token keyword">map</span><span class="token punctuation">[</span><span class="token builtin">string</span><span class="token punctuation">]</span><span class="token builtin">string</span><span class="token punctuation">{</span><span class="token string">"tag"</span><span class="token punctuation">:</span> <span class="token string">"tags"</span><span class="token punctuation">,</span> <span class="token string">"category"</span><span class="token punctuation">:</span> <span class="token string">"categories"</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h4 id="读取配置文件"><a href="#读取配置文件" class="headerlink" title="读取配置文件"></a>读取配置文件</h4><p>Viper支持<code>JSON</code>、<code>TOML</code>、<code>YAML</code>、<code>HCL</code>、<code>envfile</code>和<code>Java properties</code>格式的配置文件。Viper可以搜索多个路径，但目前单个Viper实例只支持单个配置文件。Viper不默认任何配置搜索路径，将默认决策留给应用程序。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">viper<span class="token punctuation">.</span><span class="token function">SetConfigFile</span><span class="token punctuation">(</span><span class="token string">"./config.yaml"</span><span class="token punctuation">)</span> <span class="token comment">// 指定配置文件路径</span>viper<span class="token punctuation">.</span><span class="token function">SetConfigName</span><span class="token punctuation">(</span><span class="token string">"config"</span><span class="token punctuation">)</span> <span class="token comment">// 配置文件名称(无扩展名)</span>viper<span class="token punctuation">.</span><span class="token function">SetConfigType</span><span class="token punctuation">(</span><span class="token string">"yaml"</span><span class="token punctuation">)</span> <span class="token comment">// 如果配置文件的名称中没有扩展名，则需要配置此项</span>viper<span class="token punctuation">.</span><span class="token function">AddConfigPath</span><span class="token punctuation">(</span><span class="token string">"/etc/appname/"</span><span class="token punctuation">)</span>   <span class="token comment">// 查找配置文件所在的路径</span>viper<span class="token punctuation">.</span><span class="token function">AddConfigPath</span><span class="token punctuation">(</span><span class="token string">"$HOME/.appname"</span><span class="token punctuation">)</span>  <span class="token comment">// 多次调用以添加多个搜索路径</span>viper<span class="token punctuation">.</span><span class="token function">AddConfigPath</span><span class="token punctuation">(</span><span class="token string">"."</span><span class="token punctuation">)</span>               <span class="token comment">// 还可以在工作目录中查找配置</span><span class="token keyword">if</span> err <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">ReadInConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>    <span class="token keyword">if</span> <span class="token boolean">_</span><span class="token punctuation">,</span> ok <span class="token operator">:=</span> err<span class="token punctuation">.</span><span class="token punctuation">(</span>viper<span class="token punctuation">.</span>ConfigFileNotFoundError<span class="token punctuation">)</span><span class="token punctuation">;</span> ok <span class="token punctuation">{</span>        <span class="token comment">// 配置文件未找到错误；如果需要可以忽略</span>    <span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>        <span class="token comment">// 配置文件被找到，但产生了另外的错误</span>    <span class="token punctuation">}</span><span class="token punctuation">}</span><span class="token comment">// 配置文件找到并成功解析</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="写入配置文件"><a href="#写入配置文件" class="headerlink" title="写入配置文件"></a>写入配置文件</h4><p>从配置文件中读取配置文件是有用的，但是有时你想要存储在运行时所做的所有修改。为此，可以使用下面一组命令，每个命令都有自己的用途:</p><ul><li><code>WriteConfig</code> : 将当前的<code>viper</code>配置写入预定义的路径并覆盖（如果存在的话）。如果没有预定义的路径，则报错。</li><li><code>SafeWriteConfig</code> : 将当前的<code>viper</code>配置写入预定义的路径。如果没有预定义的路径，则报错。如果存在，将不会覆盖当前的配置文件。</li><li><code>WriteConfigAs</code> : 将当前的<code>viper</code>配置写入给定的文件路径。将覆盖给定的文件(如果它存在的话)。</li><li><code>SafeWriteConfigAs</code> : 将当前的<code>viper</code>配置写入给定的文件路径。不会覆盖给定的文件(如果它存在的话)。</li></ul><p>根据经验，标记为<code>safe</code>的所有方法都不会覆盖任何文件，而是直接创建（如果不存在），而默认行为是创建或截断。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">viper<span class="token punctuation">.</span><span class="token function">WriteConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token comment">// 将当前配置写入“viper.AddConfigPath()”和“viper.SetConfigName”设置的预定义路径</span>viper<span class="token punctuation">.</span><span class="token function">SafeWriteConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">WriteConfigAs</span><span class="token punctuation">(</span><span class="token string">"/path/to/my/.config"</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">SafeWriteConfigAs</span><span class="token punctuation">(</span><span class="token string">"/path/to/my/.config"</span><span class="token punctuation">)</span> <span class="token comment">// 因为该配置文件写入过，所以会报错</span>viper<span class="token punctuation">.</span><span class="token function">SafeWriteConfigAs</span><span class="token punctuation">(</span><span class="token string">"/path/to/my/.other_config"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="监控并重新读取配置文件"><a href="#监控并重新读取配置文件" class="headerlink" title="监控并重新读取配置文件"></a>监控并重新读取配置文件</h4><p>Viper支持在运行时实时读取配置文件的功能。viper驱动的应用程序可以在运行时读取配置文件的更新,你可以为Viper提供一个回调函数，以便在每次发生更改时运行。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token comment">// 确保在调用WatchConfig()之前添加了所有的配置路径。</span>viper<span class="token punctuation">.</span><span class="token function">WatchConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">OnConfigChange</span><span class="token punctuation">(</span><span class="token keyword">func</span><span class="token punctuation">(</span>e fsnotify<span class="token punctuation">.</span>Event<span class="token punctuation">)</span> <span class="token punctuation">{</span>  <span class="token comment">// 配置文件发生变更之后会调用的回调函数</span>fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span><span class="token string">"Config file changed:"</span><span class="token punctuation">,</span> e<span class="token punctuation">.</span>Name<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="从io-Reader读取配置"><a href="#从io-Reader读取配置" class="headerlink" title="从io.Reader读取配置"></a>从io.Reader读取配置</h4><p>Viper预先定义了许多配置源，如文件、环境变量、标志和远程K/V存储，但你不受其约束。你还可以实现自己所需的配置源并将其提供给viper。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">viper<span class="token punctuation">.</span><span class="token function">SetConfigType</span><span class="token punctuation">(</span><span class="token string">"yaml"</span><span class="token punctuation">)</span> <span class="token comment">// 或者 viper.SetConfigType("YAML")</span><span class="token comment">// 任何需要将此配置添加到程序中的方法。</span><span class="token keyword">var</span> yamlExample <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token function">byte</span><span class="token punctuation">(</span><span class="token string">`Hacker: truename: stevehobbies:- skateboarding- snowboarding- goclothing:  jacket: leather  trousers: denimage: 35eyes : brownbeard: true`</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">ReadConfig</span><span class="token punctuation">(</span>bytes<span class="token punctuation">.</span><span class="token function">NewBuffer</span><span class="token punctuation">(</span>yamlExample<span class="token punctuation">)</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">Get</span><span class="token punctuation">(</span><span class="token string">"name"</span><span class="token punctuation">)</span> <span class="token comment">// 这里会得到 "steve"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="覆盖设置"><a href="#覆盖设置" class="headerlink" title="覆盖设置"></a>覆盖设置</h4><p>这些可能来自命令行标志，也可能来自你自己的应用程序逻辑。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">viper<span class="token punctuation">.</span><span class="token function">Set</span><span class="token punctuation">(</span><span class="token string">"Verbose"</span><span class="token punctuation">,</span> <span class="token boolean">true</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">Set</span><span class="token punctuation">(</span><span class="token string">"LogFile"</span><span class="token punctuation">,</span> LogFile<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h4 id="注册和使用别名"><a href="#注册和使用别名" class="headerlink" title="注册和使用别名"></a>注册和使用别名</h4><p>别名允许多个键引用单个值</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">viper<span class="token punctuation">.</span><span class="token function">RegisterAlias</span><span class="token punctuation">(</span><span class="token string">"loud"</span><span class="token punctuation">,</span> <span class="token string">"Verbose"</span><span class="token punctuation">)</span>  <span class="token comment">// 注册别名（此处loud和Verbose建立了别名）</span>viper<span class="token punctuation">.</span><span class="token function">Set</span><span class="token punctuation">(</span><span class="token string">"verbose"</span><span class="token punctuation">,</span> <span class="token boolean">true</span><span class="token punctuation">)</span> <span class="token comment">// 结果与下一行相同</span>viper<span class="token punctuation">.</span><span class="token function">Set</span><span class="token punctuation">(</span><span class="token string">"loud"</span><span class="token punctuation">,</span> <span class="token boolean">true</span><span class="token punctuation">)</span>   <span class="token comment">// 结果与前一行相同</span>viper<span class="token punctuation">.</span><span class="token function">GetBool</span><span class="token punctuation">(</span><span class="token string">"loud"</span><span class="token punctuation">)</span> <span class="token comment">// true</span>viper<span class="token punctuation">.</span><span class="token function">GetBool</span><span class="token punctuation">(</span><span class="token string">"verbose"</span><span class="token punctuation">)</span> <span class="token comment">// true</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="使用环境变量"><a href="#使用环境变量" class="headerlink" title="使用环境变量"></a>使用环境变量</h4><p>Viper完全支持环境变量。</p><blockquote><p>使用ENV变量时，务必要意识到Viper将ENV变量视为区分大小写。</p></blockquote><p>Viper提供了一种机制来确保ENV变量是惟一的。通过使用<code>SetEnvPrefix</code>，你可以告诉Viper在读取环境变量时使用前缀。<code>BindEnv</code>和<code>AutomaticEnv</code>都将使用这个前缀。</p><p><code>BindEnv</code>使用一个或两个参数。第一个参数是键名称，第二个是环境变量的名称。环境变量的名称区分大小写。如果没有提供ENV变量名，那么Viper将自动假设ENV变量与以下格式匹配：前缀+ “_” +键名全部大写。当你显式提供ENV变量名（第二个参数）时，它 <strong>不会</strong> 自动添加前缀。</p><p>在使用ENV变量时，需要注意的一件重要事情是，每次访问该值时都将读取它。Viper在调用<code>BindEnv</code>时不固定该值。</p><p><code>AutomaticEnv</code>是一个强大的助手，尤其是与<code>SetEnvPrefix</code>结合使用时。调用时，Viper会在发出<code>viper.Get</code>请求时随时检查环境变量。它将应用以下规则。它将检查环境变量的名称是否与键匹配（如果设置了<code>EnvPrefix</code>）。</p><p><code>SetEnvKeyReplacer</code>允许你使用<code>strings.Replacer</code>对象在一定程度上重写 Env 键。如果你希望在<code>Get()</code>调用中使用<code>-</code>或者其他什么符号，但是环境变量里使用<code>_</code>分隔符，那么这个功能是非常有用的。可以在<code>viper_test.go</code>中找到它的使用示例。</p><p>或者，你可以使用带有<code>NewWithOptions</code>工厂函数的<code>EnvKeyReplacer</code>。与<code>SetEnvKeyReplacer</code>不同，它接受<code>StringReplacer</code>接口，允许你编写自定义字符串替换逻辑。</p><p>默认情况下，空环境变量被认为是未设置的，并将返回到下一个配置源。若要将空环境变量视为已设置，请使用<code>AllowEmptyEnv</code>方法。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token function">SetEnvPrefix</span><span class="token punctuation">(</span><span class="token string">"spf"</span><span class="token punctuation">)</span> <span class="token comment">// 将自动转为大写</span><span class="token function">BindEnv</span><span class="token punctuation">(</span><span class="token string">"id"</span><span class="token punctuation">)</span>os<span class="token punctuation">.</span><span class="token function">Setenv</span><span class="token punctuation">(</span><span class="token string">"SPF_ID"</span><span class="token punctuation">,</span> <span class="token string">"13"</span><span class="token punctuation">)</span> <span class="token comment">// 通常是在应用程序之外完成的</span>id <span class="token operator">:=</span> <span class="token function">Get</span><span class="token punctuation">(</span><span class="token string">"id"</span><span class="token punctuation">)</span> <span class="token comment">// 13</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="使用Flags"><a href="#使用Flags" class="headerlink" title="使用Flags"></a>使用Flags</h4><p>Viper 具有绑定到标志的能力。具体来说，Viper支持<a href="https://github.com/spf13/cobra">Cobra</a>库中使用的<code>Pflag</code>。</p><p>与<code>BindEnv</code>类似，该值不是在调用绑定方法时设置的，而是在访问该方法时设置的。这意味着你可以根据需要尽早进行绑定，即使在<code>init()</code>函数中也是如此。</p><p>对于单个标志，<code>BindPFlag()</code>方法提供此功能。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">serverCmd<span class="token punctuation">.</span><span class="token function">Flags</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">Int</span><span class="token punctuation">(</span><span class="token string">"port"</span><span class="token punctuation">,</span> <span class="token number">1138</span><span class="token punctuation">,</span> <span class="token string">"Port to run Application server on"</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">BindPFlag</span><span class="token punctuation">(</span><span class="token string">"port"</span><span class="token punctuation">,</span> serverCmd<span class="token punctuation">.</span><span class="token function">Flags</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">Lookup</span><span class="token punctuation">(</span><span class="token string">"port"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>你还可以绑定一组现有的pflags （pflag.FlagSet）：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">pflag<span class="token punctuation">.</span><span class="token function">Int</span><span class="token punctuation">(</span><span class="token string">"flagname"</span><span class="token punctuation">,</span> <span class="token number">1234</span><span class="token punctuation">,</span> <span class="token string">"help message for flagname"</span><span class="token punctuation">)</span>pflag<span class="token punctuation">.</span><span class="token function">Parse</span><span class="token punctuation">(</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">BindPFlags</span><span class="token punctuation">(</span>pflag<span class="token punctuation">.</span>CommandLine<span class="token punctuation">)</span>i <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">GetInt</span><span class="token punctuation">(</span><span class="token string">"flagname"</span><span class="token punctuation">)</span> <span class="token comment">// 从viper而不是从pflag检索值</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在 Viper 中使用 pflag 并不阻碍其他包中使用标准库中的 flag 包。pflag 包可以通过导入这些 flags 来处理flag包定义的flags。这是通过调用pflag包提供的便利函数<code>AddGoFlagSet()</code>来实现的。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">package</span> main<span class="token keyword">import</span> <span class="token punctuation">(</span><span class="token string">"flag"</span><span class="token string">"github.com/spf13/pflag"</span><span class="token punctuation">)</span><span class="token keyword">func</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span><span class="token comment">// 使用标准库 "flag" 包</span>flag<span class="token punctuation">.</span><span class="token function">Int</span><span class="token punctuation">(</span><span class="token string">"flagname"</span><span class="token punctuation">,</span> <span class="token number">1234</span><span class="token punctuation">,</span> <span class="token string">"help message for flagname"</span><span class="token punctuation">)</span>pflag<span class="token punctuation">.</span>CommandLine<span class="token punctuation">.</span><span class="token function">AddGoFlagSet</span><span class="token punctuation">(</span>flag<span class="token punctuation">.</span>CommandLine<span class="token punctuation">)</span>pflag<span class="token punctuation">.</span><span class="token function">Parse</span><span class="token punctuation">(</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">BindPFlags</span><span class="token punctuation">(</span>pflag<span class="token punctuation">.</span>CommandLine<span class="token punctuation">)</span>i <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">GetInt</span><span class="token punctuation">(</span><span class="token string">"flagname"</span><span class="token punctuation">)</span> <span class="token comment">// 从 viper 检索值</span><span class="token operator">...</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h5 id="flag接口"><a href="#flag接口" class="headerlink" title="flag接口"></a>flag接口</h5><p>如果你不使用<code>Pflag</code>，Viper 提供了两个Go接口来绑定其他 flag 系统。</p><p><code>FlagValue</code>表示单个flag。这是一个关于如何实现这个接口的非常简单的例子：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">type</span> myFlag <span class="token keyword">struct</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token punctuation">(</span>f myFlag<span class="token punctuation">)</span> <span class="token function">HasChanged</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token builtin">bool</span> <span class="token punctuation">{</span> <span class="token keyword">return</span> <span class="token boolean">false</span> <span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token punctuation">(</span>f myFlag<span class="token punctuation">)</span> <span class="token function">Name</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token builtin">string</span> <span class="token punctuation">{</span> <span class="token keyword">return</span> <span class="token string">"my-flag-name"</span> <span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token punctuation">(</span>f myFlag<span class="token punctuation">)</span> <span class="token function">ValueString</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token builtin">string</span> <span class="token punctuation">{</span> <span class="token keyword">return</span> <span class="token string">"my-flag-value"</span> <span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token punctuation">(</span>f myFlag<span class="token punctuation">)</span> <span class="token function">ValueType</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token builtin">string</span> <span class="token punctuation">{</span> <span class="token keyword">return</span> <span class="token string">"string"</span> <span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>一旦你的 flag 实现了这个接口，你可以很方便地告诉Viper绑定它：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">viper<span class="token punctuation">.</span><span class="token function">BindFlagValue</span><span class="token punctuation">(</span><span class="token string">"my-flag-name"</span><span class="token punctuation">,</span> myFlag<span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><code>FlagValueSet</code>代表一组 flags 。这是一个关于如何实现这个接口的非常简单的例子:</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">type</span> myFlagSet <span class="token keyword">struct</span> <span class="token punctuation">{</span>flags <span class="token punctuation">[</span><span class="token punctuation">]</span>myFlag<span class="token punctuation">}</span><span class="token keyword">func</span> <span class="token punctuation">(</span>f myFlagSet<span class="token punctuation">)</span> <span class="token function">VisitAll</span><span class="token punctuation">(</span>fn <span class="token keyword">func</span><span class="token punctuation">(</span>FlagValue<span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span><span class="token keyword">for</span> <span class="token boolean">_</span><span class="token punctuation">,</span> flag <span class="token operator">:=</span> <span class="token keyword">range</span> flags <span class="token punctuation">{</span><span class="token function">fn</span><span class="token punctuation">(</span>flag<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>一旦你的flag set实现了这个接口，你就可以很方便地告诉Viper绑定它：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">fSet <span class="token operator">:=</span> myFlagSet<span class="token punctuation">{</span>flags<span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>myFlag<span class="token punctuation">{</span>myFlag<span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">,</span> myFlag<span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span class="token punctuation">,</span><span class="token punctuation">}</span>viper<span class="token punctuation">.</span><span class="token function">BindFlagValues</span><span class="token punctuation">(</span><span class="token string">"my-flags"</span><span class="token punctuation">,</span> fSet<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h4 id="远程Key-Value存储支持"><a href="#远程Key-Value存储支持" class="headerlink" title="远程Key/Value存储支持"></a>远程Key/Value存储支持</h4><p>在Viper中启用远程支持，需要在代码中匿名导入<code>viper/remote</code>这个包。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">import</span> <span class="token boolean">_</span> <span class="token string">"github.com/spf13/viper/remote"</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>Viper将读取从Key/Value存储（例如etcd或Consul）中的路径检索到的配置字符串（如<code>JSON</code>、<code>TOML</code>、<code>YAML</code>、<code>HCL</code>、<code>envfile</code>和<code>Java properties</code>格式）。这些值的优先级高于默认值，但是会被从磁盘、flag或环境变量检索到的配置值覆盖。（译注：也就是说Viper加载配置值的优先级为：磁盘上的配置文件&gt;命令行标志位&gt;环境变量&gt;远程Key/Value存储&gt;默认值。）</p><p>Viper使用<a href="https://github.com/bketelsen/crypt">crypt</a>从K/V存储中检索配置，这意味着如果你有正确的gpg密匙，你可以将配置值加密存储并自动解密。加密是可选的。</p><p>你可以将远程配置与本地配置结合使用，也可以独立使用。</p><p><code>crypt</code>有一个命令行助手，你可以使用它将配置放入K/V存储中。<code>crypt</code>默认使用在<a href="http://127.0.0.1:4001/">http://127.0.0.1:4001</a>的etcd。</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">$ go get github.com/bketelsen/crypt/bin/crypt$ crypt <span class="token builtin class-name">set</span> -plaintext /config/hugo.json /Users/hugo/settings/config.json<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>确认值已经设置：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">$ crypt get -plaintext /config/hugo.json<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>有关如何设置加密值或如何使用Consul的示例，请参见<code>crypt</code>文档。</p><h4 id="远程Key-Value存储-未加密"><a href="#远程Key-Value存储-未加密" class="headerlink" title="远程Key/Value存储-未加密"></a>远程Key/Value存储-未加密</h4><h5 id="etcd"><a href="#etcd" class="headerlink" title="etcd"></a>etcd</h5><pre class="line-numbers language-go" data-language="go"><code class="language-go">viper<span class="token punctuation">.</span><span class="token function">AddRemoteProvider</span><span class="token punctuation">(</span><span class="token string">"etcd"</span><span class="token punctuation">,</span> <span class="token string">"http://127.0.0.1:4001"</span><span class="token punctuation">,</span><span class="token string">"/config/hugo.json"</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">SetConfigType</span><span class="token punctuation">(</span><span class="token string">"json"</span><span class="token punctuation">)</span> <span class="token comment">// 因为在字节流中没有文件扩展名，所以这里需要设置下类型。支持的扩展名有 "json", "toml", "yaml", "yml", "properties", "props", "prop", "env", "dotenv"</span>err <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">ReadRemoteConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h5 id="Consul"><a href="#Consul" class="headerlink" title="Consul"></a>Consul</h5><p>你需要 Consul Key/Value存储中设置一个Key保存包含所需配置的JSON值。例如，创建一个key<code>MY_CONSUL_KEY</code>将下面的值存入Consul key/value 存储：</p><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>    <span class="token property">"port"</span><span class="token operator">:</span> <span class="token number">8080</span><span class="token punctuation">,</span>    <span class="token property">"hostname"</span><span class="token operator">:</span> <span class="token string">"liwenzhou.com"</span><span class="token punctuation">}</span>viper.AddRemoteProvider(<span class="token string">"consul"</span><span class="token punctuation">,</span> <span class="token string">"localhost:8500"</span><span class="token punctuation">,</span> <span class="token string">"MY_CONSUL_KEY"</span>)viper.SetConfigType(<span class="token string">"json"</span>) <span class="token comment">// 需要显示设置成json</span>err <span class="token operator">:</span>= viper.ReadRemoteConfig()fmt.Println(viper.Get(<span class="token string">"port"</span>)) <span class="token comment">// 8080</span>fmt.Println(viper.Get(<span class="token string">"hostname"</span>)) <span class="token comment">// liwenzhou.com</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h5 id="Firestore"><a href="#Firestore" class="headerlink" title="Firestore"></a>Firestore</h5><pre class="line-numbers language-go" data-language="go"><code class="language-go">viper<span class="token punctuation">.</span><span class="token function">AddRemoteProvider</span><span class="token punctuation">(</span><span class="token string">"firestore"</span><span class="token punctuation">,</span> <span class="token string">"google-cloud-project-id"</span><span class="token punctuation">,</span> <span class="token string">"collection/document"</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">SetConfigType</span><span class="token punctuation">(</span><span class="token string">"json"</span><span class="token punctuation">)</span> <span class="token comment">// 配置的格式: "json", "toml", "yaml", "yml"</span>err <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">ReadRemoteConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>当然，你也可以使用<code>SecureRemoteProvider</code>。</p><h4 id="远程Key-Value存储示例-加密"><a href="#远程Key-Value存储示例-加密" class="headerlink" title="远程Key/Value存储示例-加密"></a>远程Key/Value存储示例-加密</h4><pre class="line-numbers language-go" data-language="go"><code class="language-go">viper<span class="token punctuation">.</span><span class="token function">AddSecureRemoteProvider</span><span class="token punctuation">(</span><span class="token string">"etcd"</span><span class="token punctuation">,</span><span class="token string">"http://127.0.0.1:4001"</span><span class="token punctuation">,</span><span class="token string">"/config/hugo.json"</span><span class="token punctuation">,</span><span class="token string">"/etc/secrets/mykeyring.gpg"</span><span class="token punctuation">)</span>viper<span class="token punctuation">.</span><span class="token function">SetConfigType</span><span class="token punctuation">(</span><span class="token string">"json"</span><span class="token punctuation">)</span> <span class="token comment">// 因为在字节流中没有文件扩展名，所以这里需要设置下类型。支持的扩展名有 "json", "toml", "yaml", "yml", "properties", "props", "prop", "env", "dotenv"</span>err <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">ReadRemoteConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h4 id="监控etcd中的更改-未加密"><a href="#监控etcd中的更改-未加密" class="headerlink" title="监控etcd中的更改-未加密"></a>监控etcd中的更改-未加密</h4><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token comment">// 或者你可以创建一个新的viper实例</span><span class="token keyword">var</span> runtime_viper <span class="token operator">=</span> viper<span class="token punctuation">.</span><span class="token function">New</span><span class="token punctuation">(</span><span class="token punctuation">)</span>runtime_viper<span class="token punctuation">.</span><span class="token function">AddRemoteProvider</span><span class="token punctuation">(</span><span class="token string">"etcd"</span><span class="token punctuation">,</span> <span class="token string">"http://127.0.0.1:4001"</span><span class="token punctuation">,</span> <span class="token string">"/config/hugo.yml"</span><span class="token punctuation">)</span>runtime_viper<span class="token punctuation">.</span><span class="token function">SetConfigType</span><span class="token punctuation">(</span><span class="token string">"yaml"</span><span class="token punctuation">)</span> <span class="token comment">// 因为在字节流中没有文件扩展名，所以这里需要设置下类型。支持的扩展名有 "json", "toml", "yaml", "yml", "properties", "props", "prop", "env", "dotenv"</span><span class="token comment">// 第一次从远程读取配置</span>err <span class="token operator">:=</span> runtime_viper<span class="token punctuation">.</span><span class="token function">ReadRemoteConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token comment">// 反序列化</span>runtime_viper<span class="token punctuation">.</span><span class="token function">Unmarshal</span><span class="token punctuation">(</span><span class="token operator">&amp;</span>runtime_conf<span class="token punctuation">)</span><span class="token comment">// 开启一个单独的goroutine一直监控远端的变更</span><span class="token keyword">go</span> <span class="token keyword">func</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span><span class="token keyword">for</span> <span class="token punctuation">{</span>    time<span class="token punctuation">.</span><span class="token function">Sleep</span><span class="token punctuation">(</span>time<span class="token punctuation">.</span>Second <span class="token operator">*</span> <span class="token number">5</span><span class="token punctuation">)</span> <span class="token comment">// 每次请求后延迟一下</span>    <span class="token comment">// 目前只测试了etcd支持</span>    err <span class="token operator">:=</span> runtime_viper<span class="token punctuation">.</span><span class="token function">WatchRemoteConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    <span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>        log<span class="token punctuation">.</span><span class="token function">Errorf</span><span class="token punctuation">(</span><span class="token string">"unable to read remote config: %v"</span><span class="token punctuation">,</span> err<span class="token punctuation">)</span>        <span class="token keyword">continue</span>    <span class="token punctuation">}</span>    <span class="token comment">// 将新配置反序列化到我们运行时的配置结构体中。你还可以借助channel实现一个通知系统更改的信号</span>    runtime_viper<span class="token punctuation">.</span><span class="token function">Unmarshal</span><span class="token punctuation">(</span><span class="token operator">&amp;</span>runtime_conf<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="从Viper获取值"><a href="#从Viper获取值" class="headerlink" title="从Viper获取值"></a>从Viper获取值</h3><p>在Viper中，有几种方法可以根据值的类型获取值。存在以下功能和方法:</p><ul><li><code>Get(key string) : interface{}</code></li><li><code>GetBool(key string) : bool</code></li><li><code>GetFloat64(key string) : float64</code></li><li><code>GetInt(key string) : int</code></li><li><code>GetIntSlice(key string) : []int</code></li><li><code>GetString(key string) : string</code></li><li><code>GetStringMap(key string) : map[string]interface{}</code></li><li><code>GetStringMapString(key string) : map[string]string</code></li><li><code>GetStringSlice(key string) : []string</code></li><li><code>GetTime(key string) : time.Time</code></li><li><code>GetDuration(key string) : time.Duration</code></li><li><code>IsSet(key string) : bool</code></li><li><code>AllSettings() : map[string]interface{}</code></li></ul><p>需要认识到的一件重要事情是，每一个Get方法在找不到值的时候都会返回零值。为了检查给定的键是否存在，提供了<code>IsSet()</code>方法。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">viper<span class="token punctuation">.</span><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token string">"logfile"</span><span class="token punctuation">)</span> <span class="token comment">// 不区分大小写的设置和获取</span><span class="token keyword">if</span> viper<span class="token punctuation">.</span><span class="token function">GetBool</span><span class="token punctuation">(</span><span class="token string">"verbose"</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>    fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span><span class="token string">"verbose enabled"</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h4 id="访问嵌套的键"><a href="#访问嵌套的键" class="headerlink" title="访问嵌套的键"></a>访问嵌套的键</h4><p>访问器方法也接受深度嵌套键的格式化路径。例如，如果加载下面的JSON文件：</p><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>    <span class="token property">"host"</span><span class="token operator">:</span> <span class="token punctuation">{</span>        <span class="token property">"address"</span><span class="token operator">:</span> <span class="token string">"localhost"</span><span class="token punctuation">,</span>        <span class="token property">"port"</span><span class="token operator">:</span> <span class="token number">5799</span>    <span class="token punctuation">}</span><span class="token punctuation">,</span>    <span class="token property">"datastore"</span><span class="token operator">:</span> <span class="token punctuation">{</span>        <span class="token property">"metric"</span><span class="token operator">:</span> <span class="token punctuation">{</span>            <span class="token property">"host"</span><span class="token operator">:</span> <span class="token string">"127.0.0.1"</span><span class="token punctuation">,</span>            <span class="token property">"port"</span><span class="token operator">:</span> <span class="token number">3099</span>        <span class="token punctuation">}</span><span class="token punctuation">,</span>        <span class="token property">"warehouse"</span><span class="token operator">:</span> <span class="token punctuation">{</span>            <span class="token property">"host"</span><span class="token operator">:</span> <span class="token string">"198.0.0.1"</span><span class="token punctuation">,</span>            <span class="token property">"port"</span><span class="token operator">:</span> <span class="token number">2112</span>        <span class="token punctuation">}</span>    <span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>Viper可以通过传入<code>.</code>分隔的路径来访问嵌套字段：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token string">"datastore.metric.host"</span><span class="token punctuation">)</span> <span class="token comment">// (返回 "127.0.0.1")</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这遵守上面建立的优先规则；搜索路径将遍历其余配置注册表，直到找到为止。(译注：因为Viper支持从多种配置来源，例如磁盘上的配置文件&gt;命令行标志位&gt;环境变量&gt;远程Key/Value存储&gt;默认值，我们在查找一个配置的时候如果在当前配置源中没找到，就会继续从后续的配置源查找，直到找到为止。)</p><p>例如，在给定此配置文件的情况下，<code>datastore.metric.host</code>和<code>datastore.metric.port</code>均已定义（并且可以被覆盖）。如果另外在默认值中定义了<code>datastore.metric.protocol</code>，Viper也会找到它。</p><p>然而，如果<code>datastore.metric</code>被直接赋值覆盖（被flag，环境变量，<code>set()</code>方法等等…），那么<code>datastore.metric</code>的所有子键都将变为未定义状态，它们被高优先级配置级别“遮蔽”（shadowed）了。</p><p>最后，如果存在与分隔的键路径匹配的键，则返回其值。例如：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token punctuation">{</span>    <span class="token string">"datastore.metric.host"</span><span class="token punctuation">:</span> <span class="token string">"0.0.0.0"</span><span class="token punctuation">,</span>    <span class="token string">"host"</span><span class="token punctuation">:</span> <span class="token punctuation">{</span>        <span class="token string">"address"</span><span class="token punctuation">:</span> <span class="token string">"localhost"</span><span class="token punctuation">,</span>        <span class="token string">"port"</span><span class="token punctuation">:</span> <span class="token number">5799</span>    <span class="token punctuation">}</span><span class="token punctuation">,</span>    <span class="token string">"datastore"</span><span class="token punctuation">:</span> <span class="token punctuation">{</span>        <span class="token string">"metric"</span><span class="token punctuation">:</span> <span class="token punctuation">{</span>            <span class="token string">"host"</span><span class="token punctuation">:</span> <span class="token string">"127.0.0.1"</span><span class="token punctuation">,</span>            <span class="token string">"port"</span><span class="token punctuation">:</span> <span class="token number">3099</span>        <span class="token punctuation">}</span><span class="token punctuation">,</span>        <span class="token string">"warehouse"</span><span class="token punctuation">:</span> <span class="token punctuation">{</span>            <span class="token string">"host"</span><span class="token punctuation">:</span> <span class="token string">"198.0.0.1"</span><span class="token punctuation">,</span>            <span class="token string">"port"</span><span class="token punctuation">:</span> <span class="token number">2112</span>        <span class="token punctuation">}</span>    <span class="token punctuation">}</span><span class="token punctuation">}</span><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token string">"datastore.metric.host"</span><span class="token punctuation">)</span> <span class="token comment">// 返回 "0.0.0.0"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="提取子树"><a href="#提取子树" class="headerlink" title="提取子树"></a>提取子树</h4><p>从Viper中提取子树。</p><p>例如，<code>viper</code>实例现在代表了以下配置：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">app</span><span class="token punctuation">:</span>  <span class="token key atrule">cache1</span><span class="token punctuation">:</span>    <span class="token key atrule">max-items</span><span class="token punctuation">:</span> <span class="token number">100</span>    <span class="token key atrule">item-size</span><span class="token punctuation">:</span> <span class="token number">64</span>  <span class="token key atrule">cache2</span><span class="token punctuation">:</span>    <span class="token key atrule">max-items</span><span class="token punctuation">:</span> <span class="token number">200</span>    <span class="token key atrule">item-size</span><span class="token punctuation">:</span> <span class="token number">80</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>执行后：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">subv <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">Sub</span><span class="token punctuation">(</span><span class="token string">"app.cache1"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><code>subv</code>现在就代表：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">max-items</span><span class="token punctuation">:</span> <span class="token number">100</span><span class="token key atrule">item-size</span><span class="token punctuation">:</span> <span class="token number">64</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>假设我们现在有这么一个函数：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">func</span> <span class="token function">NewCache</span><span class="token punctuation">(</span>cfg <span class="token operator">*</span>Viper<span class="token punctuation">)</span> <span class="token operator">*</span>Cache <span class="token punctuation">{</span><span class="token operator">...</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>它基于<code>subv</code>格式的配置信息创建缓存。现在，可以轻松地分别创建这两个缓存，如下所示：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">cfg1 <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">Sub</span><span class="token punctuation">(</span><span class="token string">"app.cache1"</span><span class="token punctuation">)</span>cache1 <span class="token operator">:=</span> <span class="token function">NewCache</span><span class="token punctuation">(</span>cfg1<span class="token punctuation">)</span>cfg2 <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">Sub</span><span class="token punctuation">(</span><span class="token string">"app.cache2"</span><span class="token punctuation">)</span>cache2 <span class="token operator">:=</span> <span class="token function">NewCache</span><span class="token punctuation">(</span>cfg2<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="反序列化"><a href="#反序列化" class="headerlink" title="反序列化"></a>反序列化</h4><p>你还可以选择将所有或特定的值解析到结构体、map等。</p><p>有两种方法可以做到这一点：</p><ul><li><code>Unmarshal(rawVal interface{}) : error</code></li><li><code>UnmarshalKey(key string, rawVal interface{}) : error</code></li></ul><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">type</span> config <span class="token keyword">struct</span> <span class="token punctuation">{</span>Port <span class="token builtin">int</span>Name <span class="token builtin">string</span>PathMap <span class="token builtin">string</span> <span class="token string">`mapstructure:"path_map"`</span><span class="token punctuation">}</span><span class="token keyword">var</span> C configerr <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">Unmarshal</span><span class="token punctuation">(</span><span class="token operator">&amp;</span>C<span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>t<span class="token punctuation">.</span><span class="token function">Fatalf</span><span class="token punctuation">(</span><span class="token string">"unable to decode into struct, %v"</span><span class="token punctuation">,</span> err<span class="token punctuation">)</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>如果你想要解析那些键本身就包含<code>.</code>(默认的键分隔符）的配置，你需要修改分隔符：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">v <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">NewWithOptions</span><span class="token punctuation">(</span>viper<span class="token punctuation">.</span><span class="token function">KeyDelimiter</span><span class="token punctuation">(</span><span class="token string">"::"</span><span class="token punctuation">)</span><span class="token punctuation">)</span>v<span class="token punctuation">.</span><span class="token function">SetDefault</span><span class="token punctuation">(</span><span class="token string">"chart::values"</span><span class="token punctuation">,</span> <span class="token keyword">map</span><span class="token punctuation">[</span><span class="token builtin">string</span><span class="token punctuation">]</span><span class="token keyword">interface</span><span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">{</span>    <span class="token string">"ingress"</span><span class="token punctuation">:</span> <span class="token keyword">map</span><span class="token punctuation">[</span><span class="token builtin">string</span><span class="token punctuation">]</span><span class="token keyword">interface</span><span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">{</span>        <span class="token string">"annotations"</span><span class="token punctuation">:</span> <span class="token keyword">map</span><span class="token punctuation">[</span><span class="token builtin">string</span><span class="token punctuation">]</span><span class="token keyword">interface</span><span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">{</span>            <span class="token string">"traefik.frontend.rule.type"</span><span class="token punctuation">:</span>                 <span class="token string">"PathPrefix"</span><span class="token punctuation">,</span>            <span class="token string">"traefik.ingress.kubernetes.io/ssl-redirect"</span><span class="token punctuation">:</span> <span class="token string">"true"</span><span class="token punctuation">,</span>        <span class="token punctuation">}</span><span class="token punctuation">,</span>    <span class="token punctuation">}</span><span class="token punctuation">,</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token keyword">type</span> config <span class="token keyword">struct</span> <span class="token punctuation">{</span>Chart <span class="token keyword">struct</span><span class="token punctuation">{</span>        Values <span class="token keyword">map</span><span class="token punctuation">[</span><span class="token builtin">string</span><span class="token punctuation">]</span><span class="token keyword">interface</span><span class="token punctuation">{</span><span class="token punctuation">}</span>    <span class="token punctuation">}</span><span class="token punctuation">}</span><span class="token keyword">var</span> C configv<span class="token punctuation">.</span><span class="token function">Unmarshal</span><span class="token punctuation">(</span><span class="token operator">&amp;</span>C<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>Viper还支持解析到嵌入的结构体：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">type</span> config <span class="token keyword">struct</span> <span class="token punctuation">{</span>Module <span class="token keyword">struct</span> <span class="token punctuation">{</span>Enabled <span class="token builtin">bool</span>moduleConfig <span class="token string">`mapstructure:",squash"`</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span class="token comment">// moduleConfig could be in a module specific package</span><span class="token keyword">type</span> moduleConfig <span class="token keyword">struct</span> <span class="token punctuation">{</span>Token <span class="token builtin">string</span><span class="token punctuation">}</span><span class="token keyword">var</span> C configerr <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">Unmarshal</span><span class="token punctuation">(</span><span class="token operator">&amp;</span>C<span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>t<span class="token punctuation">.</span><span class="token function">Fatalf</span><span class="token punctuation">(</span><span class="token string">"unable to decode into struct, %v"</span><span class="token punctuation">,</span> err<span class="token punctuation">)</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>Viper在后台使用<a href="https://github.com/mitchellh/mapstructure">github.com/mitchellh/mapstructure</a>来解析值，其默认情况下使用<code>mapstructure</code>tag。</p><blockquote><p> <strong>注意</strong> 当我们需要将viper读取的配置反序列到我们定义的结构体变量中时，一定要使用<code>mapstructure</code>tag！</p></blockquote><h4 id="序列化成字符串"><a href="#序列化成字符串" class="headerlink" title="序列化成字符串"></a>序列化成字符串</h4><p>你可能需要将viper中保存的所有设置序列化到一个字符串中，而不是将它们写入到一个文件中。你可以将自己喜欢的格式的序列化器与<code>AllSettings()</code>返回的配置一起使用。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">import</span> <span class="token punctuation">(</span>    yaml <span class="token string">"gopkg.in/yaml.v2"</span>    <span class="token comment">// ...</span><span class="token punctuation">)</span><span class="token keyword">func</span> <span class="token function">yamlStringSettings</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token builtin">string</span> <span class="token punctuation">{</span>    c <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">AllSettings</span><span class="token punctuation">(</span><span class="token punctuation">)</span>    bs<span class="token punctuation">,</span> err <span class="token operator">:=</span> yaml<span class="token punctuation">.</span><span class="token function">Marshal</span><span class="token punctuation">(</span>c<span class="token punctuation">)</span>    <span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>        log<span class="token punctuation">.</span><span class="token function">Fatalf</span><span class="token punctuation">(</span><span class="token string">"unable to marshal config to YAML: %v"</span><span class="token punctuation">,</span> err<span class="token punctuation">)</span>    <span class="token punctuation">}</span>    <span class="token keyword">return</span> <span class="token function">string</span><span class="token punctuation">(</span>bs<span class="token punctuation">)</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="使用单个还是多个Viper实例"><a href="#使用单个还是多个Viper实例" class="headerlink" title="使用单个还是多个Viper实例"></a>使用单个还是多个Viper实例</h3><p>Viper是开箱即用的。你不需要配置或初始化即可开始使用Viper。由于大多数应用程序都希望使用单个中央存储库管理它们的配置信息，所以viper包提供了这个功能。它类似于单例模式。</p><h4 id="使用多个viper实例"><a href="#使用多个viper实例" class="headerlink" title="使用多个viper实例"></a>使用多个viper实例</h4><p>你还可以在应用程序中创建许多不同的viper实例。每个都有自己独特的一组配置和值。每个人都可以从不同的配置文件，key value存储区等读取数据。每个都可以从不同的配置文件、键值存储等中读取。viper包支持的所有功能都被镜像为viper实例的方法。</p><p>例如：</p><pre class="line-numbers language-go" data-language="go"><code class="language-go">x <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">New</span><span class="token punctuation">(</span><span class="token punctuation">)</span>y <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">New</span><span class="token punctuation">(</span><span class="token punctuation">)</span>x<span class="token punctuation">.</span><span class="token function">SetDefault</span><span class="token punctuation">(</span><span class="token string">"ContentDir"</span><span class="token punctuation">,</span> <span class="token string">"content"</span><span class="token punctuation">)</span>y<span class="token punctuation">.</span><span class="token function">SetDefault</span><span class="token punctuation">(</span><span class="token string">"ContentDir"</span><span class="token punctuation">,</span> <span class="token string">"foobar"</span><span class="token punctuation">)</span><span class="token comment">//...</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当使用多个viper实例时，由用户来管理不同的viper实例。</p><h4 id="使用Viper示例"><a href="#使用Viper示例" class="headerlink" title="使用Viper示例"></a>使用Viper示例</h4><p>假设我们的项目现在有一个<code>./conf/config.yaml</code>配置文件，内容如下：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">port</span><span class="token punctuation">:</span> <span class="token number">8123</span><span class="token key atrule">version</span><span class="token punctuation">:</span> <span class="token string">"v1.2.3"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>接下来通过示例代码演示两种在项目中使用<code>viper</code>管理项目配置信息的方式。</p><h5 id="直接使用viper管理配置"><a href="#直接使用viper管理配置" class="headerlink" title="直接使用viper管理配置"></a>直接使用viper管理配置</h5><p>这里用一个demo演示如何在gin框架搭建的web项目中使用<code>viper</code>，使用viper加载配置文件中的信息，并在代码中直接使用<code>viper.GetXXX()</code>方法获取对应的配置值。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">package</span> main<span class="token keyword">import</span> <span class="token punctuation">(</span><span class="token string">"fmt"</span><span class="token string">"net/http"</span><span class="token string">"github.com/gin-gonic/gin"</span><span class="token string">"github.com/spf13/viper"</span><span class="token punctuation">)</span><span class="token keyword">func</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>viper<span class="token punctuation">.</span><span class="token function">SetConfigFile</span><span class="token punctuation">(</span><span class="token string">"config.yaml"</span><span class="token punctuation">)</span> <span class="token comment">// 指定配置文件</span>viper<span class="token punctuation">.</span><span class="token function">AddConfigPath</span><span class="token punctuation">(</span><span class="token string">"./conf/"</span><span class="token punctuation">)</span>     <span class="token comment">// 指定查找配置文件的路径</span>err <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">ReadInConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span>        <span class="token comment">// 读取配置信息</span><span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>                    <span class="token comment">// 读取配置信息失败</span><span class="token function">panic</span><span class="token punctuation">(</span>fmt<span class="token punctuation">.</span><span class="token function">Errorf</span><span class="token punctuation">(</span><span class="token string">"Fatal error config file: %s \n"</span><span class="token punctuation">,</span> err<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token comment">// 监控配置文件变化</span>viper<span class="token punctuation">.</span><span class="token function">WatchConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span>r <span class="token operator">:=</span> gin<span class="token punctuation">.</span><span class="token function">Default</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token comment">// 访问/version的返回值会随配置文件的变化而变化</span>r<span class="token punctuation">.</span><span class="token function">GET</span><span class="token punctuation">(</span><span class="token string">"/version"</span><span class="token punctuation">,</span> <span class="token keyword">func</span><span class="token punctuation">(</span>c <span class="token operator">*</span>gin<span class="token punctuation">.</span>Context<span class="token punctuation">)</span> <span class="token punctuation">{</span>c<span class="token punctuation">.</span><span class="token function">String</span><span class="token punctuation">(</span>http<span class="token punctuation">.</span>StatusOK<span class="token punctuation">,</span> viper<span class="token punctuation">.</span><span class="token function">GetString</span><span class="token punctuation">(</span><span class="token string">"version"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">:=</span> r<span class="token punctuation">.</span><span class="token function">Run</span><span class="token punctuation">(</span>fmt<span class="token punctuation">.</span><span class="token function">Sprintf</span><span class="token punctuation">(</span><span class="token string">":%d"</span><span class="token punctuation">,</span> viper<span class="token punctuation">.</span><span class="token function">GetInt</span><span class="token punctuation">(</span><span class="token string">"port"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span><span class="token function">panic</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="使用结构体变量保存配置信息"><a href="#使用结构体变量保存配置信息" class="headerlink" title="使用结构体变量保存配置信息"></a>使用结构体变量保存配置信息</h4><p>除了上面的用法外，我们还可以在项目中定义与配置文件对应的结构体，<code>viper</code>加载完配置信息后使用结构体变量保存配置信息。</p><pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">package</span> main<span class="token keyword">import</span> <span class="token punctuation">(</span><span class="token string">"fmt"</span><span class="token string">"net/http"</span><span class="token string">"github.com/fsnotify/fsnotify"</span><span class="token string">"github.com/gin-gonic/gin"</span><span class="token string">"github.com/spf13/viper"</span><span class="token punctuation">)</span><span class="token keyword">type</span> Config <span class="token keyword">struct</span> <span class="token punctuation">{</span>Port    <span class="token builtin">int</span>    <span class="token string">`mapstructure:"port"`</span>Version <span class="token builtin">string</span> <span class="token string">`mapstructure:"version"`</span><span class="token punctuation">}</span><span class="token keyword">var</span> Conf <span class="token operator">=</span> <span class="token function">new</span><span class="token punctuation">(</span>Config<span class="token punctuation">)</span><span class="token keyword">func</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>viper<span class="token punctuation">.</span><span class="token function">SetConfigFile</span><span class="token punctuation">(</span><span class="token string">"./conf/config.yaml"</span><span class="token punctuation">)</span> <span class="token comment">// 指定配置文件路径</span>err <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">ReadInConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span>               <span class="token comment">// 读取配置信息</span><span class="token keyword">if</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span>                           <span class="token comment">// 读取配置信息失败</span><span class="token function">panic</span><span class="token punctuation">(</span>fmt<span class="token punctuation">.</span><span class="token function">Errorf</span><span class="token punctuation">(</span><span class="token string">"Fatal error config file: %s \n"</span><span class="token punctuation">,</span> err<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token comment">// 将读取的配置信息保存至全局变量Conf</span><span class="token keyword">if</span> err <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">Unmarshal</span><span class="token punctuation">(</span>Conf<span class="token punctuation">)</span><span class="token punctuation">;</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span><span class="token function">panic</span><span class="token punctuation">(</span>fmt<span class="token punctuation">.</span><span class="token function">Errorf</span><span class="token punctuation">(</span><span class="token string">"unmarshal conf failed, err:%s \n"</span><span class="token punctuation">,</span> err<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token comment">// 监控配置文件变化</span>viper<span class="token punctuation">.</span><span class="token function">WatchConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token comment">// 注意！！！配置文件发生变化后要同步到全局变量Conf</span>viper<span class="token punctuation">.</span><span class="token function">OnConfigChange</span><span class="token punctuation">(</span><span class="token keyword">func</span><span class="token punctuation">(</span>in fsnotify<span class="token punctuation">.</span>Event<span class="token punctuation">)</span> <span class="token punctuation">{</span>fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span><span class="token string">"夭寿啦~配置文件被人修改啦..."</span><span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">:=</span> viper<span class="token punctuation">.</span><span class="token function">Unmarshal</span><span class="token punctuation">(</span>Conf<span class="token punctuation">)</span><span class="token punctuation">;</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span><span class="token function">panic</span><span class="token punctuation">(</span>fmt<span class="token punctuation">.</span><span class="token function">Errorf</span><span class="token punctuation">(</span><span class="token string">"unmarshal conf failed, err:%s \n"</span><span class="token punctuation">,</span> err<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span class="token punctuation">)</span>r <span class="token operator">:=</span> gin<span class="token punctuation">.</span><span class="token function">Default</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token comment">// 访问/version的返回值会随配置文件的变化而变化</span>r<span class="token punctuation">.</span><span class="token function">GET</span><span class="token punctuation">(</span><span class="token string">"/version"</span><span class="token punctuation">,</span> <span class="token keyword">func</span><span class="token punctuation">(</span>c <span class="token operator">*</span>gin<span class="token punctuation">.</span>Context<span class="token punctuation">)</span> <span class="token punctuation">{</span>c<span class="token punctuation">.</span><span class="token function">String</span><span class="token punctuation">(</span>http<span class="token punctuation">.</span>StatusOK<span class="token punctuation">,</span> Conf<span class="token punctuation">.</span>Version<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token keyword">if</span> err <span class="token operator">:=</span> r<span class="token punctuation">.</span><span class="token function">Run</span><span class="token punctuation">(</span>fmt<span class="token punctuation">.</span><span class="token function">Sprintf</span><span class="token punctuation">(</span><span class="token string">":%d"</span><span class="token punctuation">,</span> Conf<span class="token punctuation">.</span>Port<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> err <span class="token operator">!=</span> <span class="token boolean">nil</span> <span class="token punctuation">{</span><span class="token function">panic</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>goleveldb</title>
      <link href="/2021/11/18/golang/third-party-library/goleveldb/"/>
      <url>/2021/11/18/golang/third-party-library/goleveldb/</url>
      
        <content type="html"><![CDATA[<h1 id="goleveldb"><a href="#goleveldb" class="headerlink" title="goleveldb"></a>goleveldb</h1><blockquote><p>github 地址：<a href="https://github.com/syndtr/goleveldb">https://github.com/syndtr/goleveldb</a></p><p>文档：<a href="https://pkg.go.dev/github.com/syndtr/goleveldb/leveldb">https://pkg.go.dev/github.com/syndtr/goleveldb/leveldb</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>CMake</title>
      <link href="/2021/11/18/c-c/tool/cmake/cmake/"/>
      <url>/2021/11/18/c-c/tool/cmake/cmake/</url>
      
        <content type="html"><![CDATA[<h1 id="CMake"><a href="#CMake" class="headerlink" title="CMake"></a>CMake</h1><blockquote><p>cmake 官网：<a href="https://cmake.org/">https://cmake.org/</a></p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【编译管理】Makefile</title>
      <link href="/2021/11/18/c-c/tool/makefile/bian-yi-guan-li-makefile/"/>
      <url>/2021/11/18/c-c/tool/makefile/bian-yi-guan-li-makefile/</url>
      
        <content type="html"><![CDATA[<h1 id="【编译管理】Makefile"><a href="#【编译管理】Makefile" class="headerlink" title="【编译管理】Makefile"></a>【编译管理】Makefile</h1><blockquote><p><a href="https://seisman.github.io/how-to-write-makefile/overview.html">https://seisman.github.io/how-to-write-makefile/overview.html</a></p></blockquote><h2 id="Makefile简介"><a href="#Makefile简介" class="headerlink" title="Makefile简介"></a>Makefile简介</h2><p>什么是makefile？或许很多Windows的程序员都不知道这个东西，因为那些Windows的集成开发环境（integrated development environment，IDE）都为你做了这个工作，但我觉得要作一个好的和专业的程序员，makefile还是要懂。这就好像现在有这么多的HTML编辑器，但如果你想成为一个专业人士，你还是要了解HTML的标签的含义。特别在Unix下的软件编译，你就不能不自己写makefile了，会不会写makefile，从一个侧面说明了一个人是否具备完成大型工程的能力。</p><p>因为，makefile关系到了整个工程的编译规则。一个工程中的源文件不计其数，并且按类型、功能、模块分别放在若干个目录中，makefile定义了一系列的规则来指定，哪些文件需要先编译，哪些文件需要后编译，哪些文件需要重新编译，甚至于进行更复杂的功能操作，因为makefile就像一个Shell脚本一样，其中也可以执行操作系统的命令。</p><p>makefile带来的好处就是——“自动化编译”，一旦写好，只需要一个make命令，整个工程完全自动编译，极大的提高了软件开发的效率。 make是一个命令工具，是一个解释makefile中指令的命令工具，一般来说，大多数的IDE都有这个命令，比如：Delphi的make，Visual C++的nmake，Linux下GNU的make。可见，makefile都成为了一种在工程方面的编译方法。</p><p>现在讲述如何写makefile的文章比较少，这是我想写这篇文章的原因。当然，不同产商的make各不相同，也有不同的语法，但其本质都是在 “文件依赖性”上做文章，这里，我仅对GNU的make进行讲述，我的环境是RedHat Linux 8.0，make的版本是3.80。毕竟，这个make是应用最为广泛的，也是用得最多的。而且其还是最遵循于IEEE 1003.2-1992标准的（POSIX.2）。</p><p>在这篇文档中，将以C/C++的源码作为基础，所以必然涉及一些关于C/C++的编译的知识。关于这方面的内容，还请各位查看相关的编译器的文档。这里所默认的编译器是UNIX下的GCC和CC。</p><h3 id="关于程序的编译和链接"><a href="#关于程序的编译和链接" class="headerlink" title="关于程序的编译和链接"></a>关于程序的编译和链接</h3><p>在此，我想多说关于程序编译的一些规范和方法。一般来说，无论是C还是C++，首先要把源文件编译成中间代码文件，在Windows下也就是 <code>.obj</code> 文件，UNIX下是 <code>.o</code> 文件，即Object File，这个动作叫做编译（compile）。然后再把大量的Object File合成执行文件，这个动作叫作链接（link）。</p><p>编译时，编译器需要的是语法的正确，函数与变量的声明的正确。对于后者，通常是你需要告诉编译器头文件的所在位置（头文件中应该只是声明，而定义应该放在C/C++文件中），只要所有的语法正确，编译器就可以编译出中间目标文件。一般来说，每个源文件都应该对应于一个中间目标文件（ <code>.o</code> 文件或 <code>.obj</code> 文件）。</p><p>链接时，主要是链接函数和全局变量。所以，我们可以使用这些中间目标文件（ <code>.o</code> 文件或 <code>.obj</code> 文件）来链接我们的应用程序。链接器并不管函数所在的源文件，只管函数的中间目标文件（Object File），在大多数时候，由于源文件太多，编译生成的中间目标文件太多，而在链接时需要明显地指出中间目标文件名，这对于编译很不方便。所以，我们要给中间目标文件打个包，在Windows下这种包叫“库文件”（Library File），也就是 <code>.lib</code> 文件，在UNIX下，是Archive File，也就是 <code>.a</code> 文件。</p><p>总结一下，源文件首先会生成中间目标文件，再由中间目标文件生成执行文件。在编译时，编译器只检测程序语法和函数、变量是否被声明。如果函数未被声明，编译器会给出一个警告，但可以生成Object File。而在链接程序时，链接器会在所有的Object File中找寻函数的实现，如果找不到，那到就会报链接错误码（Linker Error），在VC下，这种错误一般是： <code>Link 2001错误</code> ，意思说是说，链接器未能找到函数的实现。你需要指定函数的Object File。</p><h2 id="makefile环境配置"><a href="#makefile环境配置" class="headerlink" title="makefile环境配置"></a>makefile环境配置</h2><p>Ubuntu/Debian 环境，执行以下命令：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">sudo</span> <span class="token function">apt-get</span> <span class="token function">install</span> <span class="token function">make</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>Fedora/RHEL/CentOS，执行以下命令：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">sudo</span> yum <span class="token function">install</span> <span class="token function">make</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Kubernetes】PV-PVC</title>
      <link href="/2021/11/15/kubernetes/kubernetes-pv-pvc/"/>
      <url>/2021/11/15/kubernetes/kubernetes-pv-pvc/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Kubernetes】ConfigMap</title>
      <link href="/2021/11/15/kubernetes/kubernetes-configmap/"/>
      <url>/2021/11/15/kubernetes/kubernetes-configmap/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Kubernetes】CronJob</title>
      <link href="/2021/11/15/kubernetes/kubernetes-cronjob/"/>
      <url>/2021/11/15/kubernetes/kubernetes-cronjob/</url>
      
        <content type="html"><![CDATA[<h1 id="【kubernetes】CronJob"><a href="#【kubernetes】CronJob" class="headerlink" title="【kubernetes】CronJob"></a>【kubernetes】CronJob</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【数据备份】mysqldump</title>
      <link href="/2021/11/09/database/mysql/shu-ju-bei-fen-mysqldump/"/>
      <url>/2021/11/09/database/mysql/shu-ju-bei-fen-mysqldump/</url>
      
        <content type="html"><![CDATA[<h1 id="【数据备份】mysqldump"><a href="#【数据备份】mysqldump" class="headerlink" title="【数据备份】mysqldump"></a>【数据备份】mysqldump</h1><h2 id="mysqldump简介"><a href="#mysqldump简介" class="headerlink" title="mysqldump简介"></a>mysqldump简介</h2><p><strong>mysqldump命令</strong> 是mysql数据库中备份工具，用于将MySQL服务器中的数据库以标准的sql语言的方式导出，并保存到文件中。</p><h2 id="mysqldump环境配置"><a href="#mysqldump环境配置" class="headerlink" title="mysqldump环境配置"></a>mysqldump环境配置</h2><ul><li><p>linux</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment">## redhat</span>yum -y <span class="token function">install</span> mysql-client<span class="token comment">## debian</span><span class="token function">apt-get</span> <span class="token function">install</span> mysql-client<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h2 id="mysqldump命令行"><a href="#mysqldump命令行" class="headerlink" title="mysqldump命令行"></a>mysqldump命令行</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Dumping structure and contents of MySQL databases and tables.Usage: mysqldump <span class="token punctuation">[</span>OPTIONS<span class="token punctuation">]</span> database <span class="token punctuation">[</span>tables<span class="token punctuation">]</span>OR     mysqldump <span class="token punctuation">[</span>OPTIONS<span class="token punctuation">]</span> --databases <span class="token punctuation">[</span>OPTIONS<span class="token punctuation">]</span> DB1 <span class="token punctuation">[</span>DB2 DB3<span class="token punctuation">..</span>.<span class="token punctuation">]</span>OR     mysqldump <span class="token punctuation">[</span>OPTIONS<span class="token punctuation">]</span> --all-databases <span class="token punctuation">[</span>OPTIONS<span class="token punctuation">]</span>Default options are <span class="token builtin class-name">read</span> from the following files <span class="token keyword">in</span> the given order:/etc/my.cnf /etc/mysql/my.cnf /opt/bitnami/mysql/conf/my.cnf ~/.my.cnf The following <span class="token function">groups</span> are read: mysqldump clientThe following options may be given as the first argument:--print-defaults        Print the program argument list and exit.--no-defaults           Don<span class="token string">'t read default options from any option file,                        except for login file.--defaults-file=#       Only read default options from the given file #.--defaults-extra-file=# Read this file after the global files are read.--defaults-group-suffix=#                        Also read groups with concat(group, suffix)--login-path=#          Read this path from the login file.  -A, --all-databases 导出全部数据库. This will be same as --databases                      with all databases selected.  -Y, --all-tablespaces 导出所有表空间.                        -y, --no-tablespaces 不导出任何表空间信息.    --add-drop-database 每个数据库创建之前添加drop数据库语句。    --add-drop-table    每个数据表创建之前添加drop数据表语句.                      (Defaults to on; use --skip-add-drop-table to disable.)                        --add-drop-trigger  Add a DROP TRIGGER before each create.  --add-locks         在每个表导出之前增加LOCK TABLES并且之后UNLOCK TABLE。                      (Defaults to on; use --skip-add-locks to disable.)                        --allow-keywords    允许创建是关键词的列名字。  --apply-slave-statements 在'</span>CHANGE MASTER<span class="token string">'前添加'</span>STOP SLAVE<span class="token string">'，并且在导出的最后添加'</span>START SLAVE<span class="token string">'。                        --bind-address=name IP address to bind to.  --character-sets-dir=name 字符集文件的目录                        -i, --comments      附加注释信息.                      (Defaults to on; use --skip-comments to disable.)                        --compatible=name    导出的数据将和其它数据库或旧版本的MySQL 相兼容。    值可以为ansi、mysql323、mysql40、postgresql、oracle、mssql、db2、maxdb、no_key_options、no_tables_options、no_field_options等，要使用几个值，用逗号将它们隔开。它并不保证能完全兼容，而是尽量兼容。  --compact           导出更少的输出信息(用于调试)。去掉注释和头尾等结构.  Enables                      options --skip-add-drop-table --skip-add-locks                      --skip-comments --skip-disable-keys --skip-set-charset.  -c, --complete-insert                       Use complete insert statements.  -C, --compress      Use compression in server/client protocol.  -a, --create-options                       Include all MySQL specific create options.                      (Defaults to on; use --skip-create-options to disable.)  -B, --databases     Dump several databases. Note the difference in usage; in                      this case no tables are given. All name arguments are                      regarded as database names. '</span>USE db_name<span class="token punctuation">;</span><span class="token string">' will be                      included in the output.  -#, --debug[=#]     This is a non-debug version. Catch this and exit.  --debug-check       This is a non-debug version. Catch this and exit.  --debug-info        This is a non-debug version. Catch this and exit.  --default-character-set=name                       Set the default character set.  --delete-master-logs                       Delete logs on master after backup. This automatically                      enables --master-data.  -K, --disable-keys  '</span>/*<span class="token operator">!</span><span class="token number">40000</span> ALTER TABLE tb_name DISABLE KEYS */<span class="token punctuation">;</span> and                      <span class="token string">'/*!40000 ALTER TABLE tb_name ENABLE KEYS */; will be put                      in the output.                      (Defaults to on; use --skip-disable-keys to disable.)  --dump-slave[=#]    This causes the binary log position and filename of the                      master to be appended to the dumped data output. Setting                      the value to 1, will printit as a CHANGE MASTER command                      in the dumped data output; if equal to 2, that command                      will be prefixed with a comment symbol. This option will                      turn --lock-all-tables on, unless --single-transaction is                      specified too (in which case a global read lock is only                      taken a short time at the beginning of the dump - don'</span>t                      forget to <span class="token builtin class-name">read</span> about --single-transaction below<span class="token punctuation">)</span>. In all                      cases any action on logs will happen at the exact moment                      of the dump.Option automatically turns --lock-tables off.  -E, --events        Dump events.  -e, --extended-insert                       Use multiple-row INSERT syntax that include several                      VALUES lists.                      <span class="token punctuation">(</span>Defaults to on<span class="token punctuation">;</span> use --skip-extended-insert to disable.<span class="token punctuation">)</span>  --fields-terminated-by<span class="token operator">=</span>name                       Fields <span class="token keyword">in</span> the output <span class="token function">file</span> are terminated by the given                      string.  --fields-enclosed-by<span class="token operator">=</span>name                       Fields <span class="token keyword">in</span> the output <span class="token function">file</span> are enclosed by the given                      character.  --fields-optionally-enclosed-by<span class="token operator">=</span>name                       Fields <span class="token keyword">in</span> the output <span class="token function">file</span> are optionally enclosed by the                      given character.  --fields-escaped-by<span class="token operator">=</span>name                       Fields <span class="token keyword">in</span> the output <span class="token function">file</span> are escaped by the given                      character.  -F, --flush-logs    Flush logs <span class="token function">file</span> <span class="token keyword">in</span> server before starting dump. Note that                      <span class="token keyword">if</span> you dump many databases at once <span class="token punctuation">(</span>using the option                      --databases<span class="token operator">=</span> or --all-databases<span class="token punctuation">)</span>, the logs will be                      flushed <span class="token keyword">for</span> each database dumped. The exception is when                      using --lock-all-tables or --master-data: <span class="token keyword">in</span> this <span class="token keyword">case</span>                      the logs will be flushed only once, corresponding to the                      moment all tables are locked. So <span class="token keyword">if</span> you want your dump                      and the log flush to happen at the same exact moment you                      should use --lock-all-tables or --master-data with                      --flush-logs.  --flush-privileges  Emit a FLUSH PRIVILEGES statement after dumping the mysql                      database.  This option should be used any <span class="token function">time</span> the dump                      contains the mysql database and any other database that                      depends on the data <span class="token keyword">in</span> the mysql database <span class="token keyword">for</span> proper                      restore.   -f, --force         Continue even <span class="token keyword">if</span> we get an SQL error.  -?, --help          Display this <span class="token builtin class-name">help</span> message and exit.  --hex-blob          Dump binary strings <span class="token punctuation">(</span>BINARY, VARBINARY, BLOB<span class="token punctuation">)</span> <span class="token keyword">in</span>                      hexadecimal format.  -h, --host<span class="token operator">=</span>name     Connect to host.  --ignore-error<span class="token operator">=</span>name A comma-separated list of error numbers to be ignored <span class="token keyword">if</span>                      encountered during dump.  --ignore-table<span class="token operator">=</span>name Do not dump the specified table. To specify <span class="token function">more</span> than one                      table to ignore, use the directive multiple times, once                      <span class="token keyword">for</span> each table.  Each table must be specified with both                      database and table names, e.g.,                      --ignore-table<span class="token operator">=</span>database.table.  --include-master-host-port                       Adds <span class="token string">'MASTER_HOST=&lt;host&gt;, MASTER_PORT=&lt;port&gt;'</span> to <span class="token string">'CHANGE                      MASTER TO..'</span> <span class="token keyword">in</span> dump produced with --dump-slave.  --insert-ignore     Insert rows with INSERT IGNORE.  --lines-terminated-by<span class="token operator">=</span>name                       Lines <span class="token keyword">in</span> the output <span class="token function">file</span> are terminated by the given                      string.  -x, --lock-all-tables                       Locks all tables across all databases. This is achieved                      by taking a global <span class="token builtin class-name">read</span> lock <span class="token keyword">for</span> the duration of the                      whole dump. Automatically turns --single-transaction and                      --lock-tables off.  -l, --lock-tables   Lock all tables <span class="token keyword">for</span> read.                      <span class="token punctuation">(</span>Defaults to on<span class="token punctuation">;</span> use --skip-lock-tables to disable.<span class="token punctuation">)</span>  --log-error<span class="token operator">=</span>name    Append warnings and errors to given file.  --master-data<span class="token punctuation">[</span><span class="token operator">=</span><span class="token comment">#]   This causes the binary log position and filename to be</span>                      appended to the output. If equal to <span class="token number">1</span>, will print it as a                      CHANGE MASTER <span class="token builtin class-name">command</span><span class="token punctuation">;</span> <span class="token keyword">if</span> equal to <span class="token number">2</span>, that <span class="token builtin class-name">command</span> will                      be prefixed with a comment symbol. This option will turn                      --lock-all-tables on, unless --single-transaction is                      specified too <span class="token punctuation">(</span>in <span class="token function">which</span> <span class="token keyword">case</span> a global <span class="token builtin class-name">read</span> lock is only                      taken a short <span class="token function">time</span> at the beginning of the dump<span class="token punctuation">;</span> don<span class="token string">'t                      forget to read about --single-transaction below). In all                      cases, any action on logs will happen at the exact moment                      of the dump. Option automatically turns --lock-tables                      off.  --max-allowed-packet=#                       The maximum packet length to send to or receive from                      server.  --net-buffer-length=#                       The buffer size for TCP/IP and socket communication.  --no-autocommit     Wrap tables with autocommit/commit statements.  -n, --no-create-db  Suppress the CREATE DATABASE ... IF EXISTS statement that                      normally is output for each dumped database if                      --all-databases or --databases is given.  -t, --no-create-info                       Don'</span>t <span class="token function">write</span> table creation info.  -d, --no-data       No row information.  -N, --no-set-names  Same as --skip-set-charset.  --opt               Same as --add-drop-table, --add-locks, --create-options,                      --quick, --extended-insert, --lock-tables, --set-charset,                      and --disable-keys. Enabled by default, disable with                      --skip-opt.  --order-by-primary  Sorts each table<span class="token string">'s rows by primary key, or first unique                      key, if such a key exists.  Useful when dumping a MyISAM                      table to be loaded into an InnoDB table, but will make                      the dump itself take considerably longer.  -p, --password[=name]                       Password to use when connecting to server. If password is                      not given it'</span>s solicited on the tty.  -P, --port<span class="token operator">=</span><span class="token comment">#        Port number to use for connection.</span>  --protocol<span class="token operator">=</span>name     The protocol to use <span class="token keyword">for</span> connection <span class="token punctuation">(</span>tcp, socket, pipe,                      memory<span class="token punctuation">)</span>.  -q, --quick         Don<span class="token string">'t buffer query, dump directly to stdout.                      (Defaults to on; use --skip-quick to disable.)  -Q, --quote-names   Quote table and column names with backticks (`).                      (Defaults to on; use --skip-quote-names to disable.)  --replace           Use REPLACE INTO instead of INSERT INTO.  -r, --result-file=name                       Direct output to a given file. This option should be used                      in systems (e.g., DOS, Windows) that use carriage-return                      linefeed pairs (\r\n) to separate text lines. This option                      ensures that only a single newline is used.  -R, --routines      Dump stored routines (functions and procedures).  --set-charset       Add '</span>SET NAMES default_character_set<span class="token string">' to the output.                      (Defaults to on; use --skip-set-charset to disable.)  --set-gtid-purged[=name]                       Add '</span>SET @@GLOBAL.GTID_PURGED<span class="token string">' to the output. Possible                      values for this option are ON, OFF and AUTO. If ON is                      used and GTIDs are not enabled on the server, an error is                      generated. If OFF is used, this option does nothing. If                      AUTO is used and GTIDs are enabled on the server, '</span>SET                      @@GLOBAL.GTID_PURGED<span class="token string">' is added to the output. If GTIDs                      are disabled, AUTO does nothing. If no value is supplied                      then the default (AUTO) value will be considered.  --single-transaction                       Creates a consistent snapshot by dumping all tables in a                      single transaction. Works ONLY for tables stored in                      storage engines which support multiversioning (currently                      only InnoDB does); the dump is NOT guaranteed to be                      consistent for other storage engines. While a                      --single-transaction dump is in process, to ensure a                      valid dump file (correct table contents and binary log                      position), no other connection should use the following                      statements: ALTER TABLE, DROP TABLE, RENAME TABLE,                      TRUNCATE TABLE, as consistent snapshot is not isolated                      from them. Option automatically turns off --lock-tables.  --dump-date         Put a dump date to the end of the output.                      (Defaults to on; use --skip-dump-date to disable.)  --skip-opt          Disable --opt. Disables --add-drop-table, --add-locks,                      --create-options, --quick, --extended-insert,                      --lock-tables, --set-charset, and --disable-keys.  -S, --socket=name   The socket file to use for connection.  --secure-auth       Refuse client connecting to server if it uses old                      (pre-4.1.1) protocol. Deprecated. Always TRUE  --ssl-mode=name     SSL connection mode.  --ssl               Deprecated. Use --ssl-mode instead.                      (Defaults to on; use --skip-ssl to disable.)  --ssl-verify-server-cert                       Deprecated. Use --ssl-mode=VERIFY_IDENTITY instead.  --ssl-ca=name       CA file in PEM format.  --ssl-capath=name   CA directory.  --ssl-cert=name     X509 cert in PEM format.  --ssl-cipher=name   SSL cipher to use.  --ssl-key=name      X509 key in PEM format.  --ssl-crl=name      Certificate revocation list.  --ssl-crlpath=name  Certificate revocation list path.  --tls-version=name  TLS version to use, permitted values are: TLSv1, TLSv1.1,                      TLSv1.2  --server-public-key-path=name                       File path to the server public RSA key in PEM format.  --get-server-public-key                       Get server public key  -T, --tab=name      Create tab-separated textfile for each table to given                      path. (Create .sql and .txt files.) NOTE: This only works                      if mysqldump is run on the same machine as the mysqld                      server.  --tables            Overrides option --databases (-B).  --triggers          Dump triggers for each dumped table.                      (Defaults to on; use --skip-triggers to disable.)  --tz-utc            SET TIME_ZONE='</span>+00:00' at <span class="token function">top</span> of dump to allow dumping of                      TIMESTAMP data when a server has data <span class="token keyword">in</span> different <span class="token function">time</span>                      zones or data is being moved between servers with                      different <span class="token function">time</span> zones.                      <span class="token punctuation">(</span>Defaults to on<span class="token punctuation">;</span> use --skip-tz-utc to disable.<span class="token punctuation">)</span>  -u, --user<span class="token operator">=</span>name     User <span class="token keyword">for</span> login <span class="token keyword">if</span> not current user.  -v, --verbose       Print info about the various stages.  -V, --version       Output version information and exit.  -w, --where<span class="token operator">=</span>name    Dump only selected records. Quotes are mandatory.  -X, --xml           Dump a database as well formed XML.  --plugin-dir<span class="token operator">=</span>name   Directory <span class="token keyword">for</span> client-side plugins.  --default-auth<span class="token operator">=</span>name Default authentication client-side plugin to use.  --enable-cleartext-plugin                       Enable/disable the <span class="token function">clear</span> text authentication plugin.Variables <span class="token punctuation">(</span>--variable-name<span class="token operator">=</span>value<span class="token punctuation">)</span>and boolean options <span class="token punctuation">{</span>FALSE<span class="token operator">|</span>TRUE<span class="token punctuation">}</span>  Value <span class="token punctuation">(</span>after reading options<span class="token punctuation">)</span>--------------------------------- ----------------------------------------all-databases                     FALSEall-tablespaces                   FALSEno-tablespaces                    FALSEadd-drop-database                 FALSEadd-drop-table                    TRUEadd-drop-trigger                  FALSEadd-locks                         TRUEallow-keywords                    FALSEapply-slave-statements            FALSEbind-address                      <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>character-sets-dir                <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>comments                          TRUEcompatible                        <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>compact                           FALSEcomplete-insert                   FALSEcompress                          FALSEcreate-options                    TRUEdatabases                         FALSEdefault-character-set             UTF8delete-master-logs                FALSEdisable-keys                      TRUEdump-slave                        <span class="token number">0</span>events                            FALSEextended-insert                   TRUEfields-terminated-by              <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>fields-enclosed-by                <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>fields-optionally-enclosed-by     <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>fields-escaped-by                 <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>flush-logs                        FALSEflush-privileges                  FALSEforce                             FALSEhex-blob                          FALSE<span class="token function">host</span>                              <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>ignore-error                      <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>include-master-host-port          FALSEinsert-ignore                     FALSElines-terminated-by               <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>lock-all-tables                   FALSElock-tables                       TRUElog-error                         <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>master-data                       <span class="token number">0</span>max-allowed-packet                <span class="token number">25165824</span>net-buffer-length                 <span class="token number">1046528</span>no-autocommit                     FALSEno-create-db                      FALSEno-create-info                    FALSEno-data                           FALSEorder-by-primary                  FALSEport                              <span class="token number">3306</span>quick                             TRUEquote-names                       TRUEreplace                           FALSEroutines                          FALSEset-charset                       TRUEsingle-transaction                FALSEdump-date                         TRUEsocket                            /opt/bitnami/mysql/tmp/mysql.socksecure-auth                       TRUEssl                               TRUEssl-verify-server-cert            FALSEssl-ca                            <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>ssl-capath                        <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>ssl-cert                          <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>ssl-cipher                        <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>ssl-key                           <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>ssl-crl                           <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>ssl-crlpath                       <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>tls-version                       <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>server-public-key-path            <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>get-server-public-key             FALSEtab                               <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>triggers                          TRUEtz-utc                            TRUEuser                              <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>verbose                           FALSEwhere                             <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>plugin-dir                        /opt/bitnami/mysql/plugindefault-auth                      <span class="token punctuation">(</span>No default value<span class="token punctuation">)</span>enable-cleartext-plugin           FALSE<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> mysqldump </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【Git】GitFlow</title>
      <link href="/2021/11/04/git/git-gitflow/"/>
      <url>/2021/11/04/git/git-gitflow/</url>
      
        <content type="html"><![CDATA[<h1 id="【Git】GitFlow"><a href="#【Git】GitFlow" class="headerlink" title="【Git】GitFlow"></a>【Git】GitFlow</h1><blockquote><p>gitflow github地址：<a href="https://github.com/petervanderdoes/gitflow-avh">https://github.com/petervanderdoes/gitflow-avh</a></p></blockquote><h2 id="gitflow简介"><a href="#gitflow简介" class="headerlink" title="gitflow简介"></a>gitflow简介</h2><p>Git Flow是一套基于git的工作流程，这个工作流程围绕着project的发布(release)定义了一个严格的如何建立分支的模型。</p><ul><li><p><strong>master分支</strong></p><p>master分支只存放历史发布(release)版本的源代码。各个版本通过tag来标记</p></li><li><p><strong>develop分支</strong></p><p>develop分支则用来整合各个feature分支。开发中的版本的源代码存放在这里。</p></li><li><p><strong>feature分支</strong></p><p>每一个特性都必须在自己的分支里开发，feature分支派生自develop分支。当feature开发完毕后，要合并回develop分支。feature分支永远不会和master分支打交道。</p></li><li><p><strong>release分支</strong></p><p>release分支不是一个放正式发布产品的分支，你可以将它理解为“待发布”分支。这个分支里面<strong>绝对不会添加新的特性</strong>。当和发布相关的工作都完成后，release分支合并回develop和master分支。</p><p>此分支只和发布相关：</p><ul><li>把这个分支打包给测试人员测试</li><li>在这个分支里修复bug</li><li>编写发布文档</li></ul></li></ul><p>​    单独搞一个release分支的好处是，当一个团队在做发布相关的工作时，另一个团队则可    以接着开发下一版本的东西。</p><ul><li><p><strong>hotfix分支</strong></p><p><strong>项目发布后</strong>或多或少肯定会有一些bug存在，而bug的修复工作并不适合在develop上做，这是因为：</p><ul><li>develop分支上包含还未验证过的feature</li><li>用户未必需要develop上的feature</li><li>develop还不能马上发布，而客户急需这个bug的修复。</li></ul><p>这时就需要新建hotfix分支，hotfix分支派生自master分支，仅仅用于修复bug，当bug修复完毕后，马上回归到master分支，然后发布一个新版本。同时hotfix也要合并回develop分支，这样develop分支就能享受到bug修复的好处了。</p></li></ul><h2 id="gitflow环境配置"><a href="#gitflow环境配置" class="headerlink" title="gitflow环境配置"></a>gitflow环境配置</h2><ul><li><p>macos</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">brew install git-flow-avh<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>ubuntu</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">apt-get install git-flow<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>windows</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"># 安装 git-flow, 需要 wget 和 util-linuxwget -q -O - --no-check-certificate https://raw.github.com/petervanderdoes/gitflow-avh/develop/contrib/gitflow-installer.sh install stable | bash<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li></ul><h2 id="gitflow使用"><a href="#gitflow使用" class="headerlink" title="gitflow使用"></a>gitflow使用</h2><h3 id="initial"><a href="#initial" class="headerlink" title="initial"></a>initial</h3><ul><li><strong>gitflow init 命令</strong></li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">➜  gitflow git:(develop) git flow init -h    USAGE: git flow init [flags] argsflags:  -h  show this help (default: false)  -f  强制设置gitflow分支，即使已经配置 (default: false)  -d  使用默认分支命名约定(不会提示你输入) (default: false)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><strong>gitflow init 实践</strong></li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">➜  gitflow git:(master) git flow initNo branches exist yet. Base branches must be created now.Branch name for production releases: [master] Branch name for "next release" development: [develop] How to name your supporting branch prefixes? # 可以自定义各个分支的前缀Feature branches? [feature/] Release branches? [release/] Hotfix branches? [hotfix/] Support branches? [support/] Version tag prefix? [] # 一般初始化之后，会为你新建一个develop分支.<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="feature"><a href="#feature" class="headerlink" title="feature"></a>feature</h3><ul><li><strong>gitflow feature命令</strong></li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"># 列出所有的featuregit flow feature# 创建一个release# &lt;base&gt;默认是develop分支，也可以指定其他分支git flow feature start &lt;name&gt; &lt;base&gt;# 合并featuregit flow feature finish &lt;name&gt;# 删除featuregit flow feature delete &lt;name&gt;# 推送feature分支到远程仓库git flow feature publish &lt;name&gt;# 拉去远程仓库的feature分支git flow feature track &lt;name&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><strong>gitflow feature实践</strong></li></ul><pre class="line-numbers language-none"><code class="language-none">```### release- **gitflow release命令**```shell# 列出所有的releasegit flow release# 创建一个release# &lt;base&gt;默认是develop分支，也可以指定其他分支git flow release start &lt;name&gt; &lt;base&gt;# 合并releasegit flow release finish &lt;name&gt;# 删除releasegit flow release delete &lt;name&gt;# 推送release分支到远程仓库git flow release publish &lt;name&gt;# 拉去远程仓库的release分支git flow release track &lt;name&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><strong>gitflow release实践</strong></li></ul><pre class="line-numbers language-none"><code class="language-none">```### hotfix- **gitflow hotfix命令**```shell# 列出所有的hotfixgit flow hotfix# 创建一个hotfix# &lt;base&gt;默认是production分支，也可以指定其他分支git flow hotfix start &lt;name&gt; &lt;base&gt;# 合并hotfixgit flow hotfix finish &lt;name&gt;# 删除hotfixgit flow hotfix delete &lt;name&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【Git】GitOps</title>
      <link href="/2021/11/04/gitops/git-gitops/"/>
      <url>/2021/11/04/gitops/git-gitops/</url>
      
        <content type="html"><![CDATA[<h1 id="【Git】GitOps"><a href="#【Git】GitOps" class="headerlink" title="【Git】GitOps"></a>【Git】GitOps</h1><h2 id="gitops简介"><a href="#gitops简介" class="headerlink" title="gitops简介"></a>gitops简介</h2><p>GitOps是一种持续交付的方式。它的核心思想是将应用系统的声明性基础架构和应用程序存放在Git版本库中。</p><p>GitOps 是一种快速、安全的方法，可供开发或运维人员维护和更新运行在 Kubernetes 或其他声明式编排框架中的复杂应用。</p><h2 id="gitops基本原则"><a href="#gitops基本原则" class="headerlink" title="gitops基本原则"></a>gitops基本原则</h2><ul><li><p><strong>任何能够被描述的内容都必须存储在Git库中</strong></p><p>通过使用Git作为存储声明性基础架构和应用程序代码的存储仓库，可以方便地监控集群，以及检查比较实际环境的状态与代码库上的状态是否一致。所以，<strong>我们的目标是描述系统相关的所有内容：策略，代码，配置，甚至监控事件和版本控制等</strong>，并且将这些内容全部存储在版本库中，在通过版本库中的内容构建系统的基础架构或者应用程序的时候，如果没有成功，则可以迅速的回滚，并且重新来过。</p></li><li><p><strong>不应直接使用Kubectl</strong></p><p>不提倡在命令行中直接使用kubectl命令操作执行部署基础架构或应用程序到集群中。还有一些开发者使用CI工具驱动应用程序的部署，但如果这样做，可能会给生产环境带来潜在不可预测的风险。</p></li><li><p><strong>调用Kubernetes 的API的接口或者控制器应该遵循 Operator 模式</strong></p><p>调用Kubernetes 的API的接口或者控制器应该遵循 Operator 模式，集群的状态和Git库中的配置文件等要保持一致，并且查看分析它们之间的状态差异。</p></li></ul><h2 id="gitops流水线"><a href="#gitops流水线" class="headerlink" title="gitops流水线"></a>gitops流水线</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2021/11/03/docker/dockerfile/docker-dockerfile/"/>
      <url>/2021/11/03/docker/dockerfile/docker-dockerfile/</url>
      
        <content type="html"><![CDATA[<h1 id="Dockerfile"><a href="#Dockerfile" class="headerlink" title="Dockerfile"></a>Dockerfile</h1><h2 id="Dockerfile指令"><a href="#Dockerfile指令" class="headerlink" title="Dockerfile指令"></a>Dockerfile指令</h2><h3 id="FROM-指定基础镜像"><a href="#FROM-指定基础镜像" class="headerlink" title="FROM(指定基础镜像)"></a>FROM(指定基础镜像)</h3><p>基础镜像是必须指定的，而 <code>FROM</code> 就是指定基础镜像。</p><p>除了选择现有镜像为基础镜像外，Docker 还存在一个特殊的镜像，名为 <code>scratch</code>。这个镜像是虚拟的概念，并不实际存在，它表示一个空白的镜像。</p><p>不以任何系统为基础，直接将可执行文件复制进镜像的做法并不罕见，对于 Linux 下静态编译的程序来说，并不需要有操作系统提供运行时支持，所需的一切库都已经在可执行文件里了，因此直接 <code>FROM scratch</code> 会让镜像体积更加小巧。使用 <a href="https://golang.google.cn/">Go 语言</a> 开发的应用很多会使用这种方式来制作镜像</p><h3 id="RUN-执行命令行命令"><a href="#RUN-执行命令行命令" class="headerlink" title="RUN(执行命令行命令)"></a>RUN(执行命令行命令)</h3><p><code>RUN</code> 指令是用来执行命令行命令的。其格式有两种：</p><ul><li><p><strong><em>shell</em> 格式</strong>：<code>RUN &lt;命令&gt;</code>，就像直接在命令行中输入的命令一样。刚才写的 Dockerfile 中的 <code>RUN</code> 指令就是这种格式。</p></li><li><p><strong><em>exec</em> 格式</strong>：<code>RUN ["可执行文件", "参数1", "参数2"]</code>，这更像是函数调用中的格式。</p></li></ul><p>每一个 <code>RUN</code> 都是启动一个容器、执行命令、然后提交存储层文件变更。</p><h2 id="COPY-复制文件"><a href="#COPY-复制文件" class="headerlink" title="COPY(复制文件)"></a>COPY(复制文件)</h2><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile"># 格式COPY [--chown=&lt;user&gt;:&lt;group&gt;] &lt;源路径&gt;... &lt;目标路径&gt;COPY [--chown=&lt;user&gt;:&lt;group&gt;] ["&lt;源路径1&gt;",... "&lt;目标路径&gt;"]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><code>COPY</code> 指令将从构建上下文目录中 <code>&lt;源路径&gt;</code> 的文件/目录复制到新的一层的镜像内的 <code>&lt;目标路径&gt;</code> 位置。</p><p><code>&lt;源路径&gt;</code> 可以是多个，甚至可以是通配符，其通配符规则要满足 Go 的 <a href="https://golang.org/pkg/path/filepath/#Match"><code>filepath.Match</code></a> 规则</p><p><code>&lt;目标路径&gt;</code> 可以是容器内的绝对路径，也可以是相对于工作目录的相对路径（工作目录可以用 <code>WORKDIR</code> 指令来指定）。目标路径不需要事先创建，如果目录不存在会在复制文件前先行创建缺失目录。</p><p>使用 <code>COPY</code> 指令，源文件的各种元数据都会保留。比如读、写、执行权限、文件变更时间等。这个特性对于镜像定制很有用。特别是构建相关文件都在使用 Git 进行管理的时候。</p><p>可以加上 <code>--chown=&lt;user&gt;:&lt;group&gt;</code> 选项来改变文件的所属用户及所属组。</p><p>如果源路径为文件夹，复制的时候不是直接复制该文件夹，而是将文件夹中的内容复制到目标路径</p><h2 id="ADD-高级的复制文件"><a href="#ADD-高级的复制文件" class="headerlink" title="ADD(高级的复制文件)"></a>ADD(高级的复制文件)</h2><p><code>ADD</code> 指令和 <code>COPY</code> 的格式和性质基本一致。但是在 <code>COPY</code> 基础上增加了一些功能。</p><p>比如 <code>&lt;源路径&gt;</code> 可以是一个 <code>URL</code>，这种情况下，Docker 引擎会试图去下载这个链接的文件放到 <code>&lt;目标路径&gt;</code> 去。下载后的文件权限自动设置为 <code>600</code>，如果这并不是想要的权限，那么还需要增加额外的一层 <code>RUN</code> 进行权限调整，另外，如果下载的是个压缩包，需要解压缩，也一样还需要额外的一层 <code>RUN</code> 指令进行解压缩。所以不如直接使用 <code>RUN</code> 指令，然后使用 <code>wget</code> 或者 <code>curl</code> 工具下载，处理权限、解压缩、然后清理无用文件更合理。因此，这个功能其实并不实用，而且不推荐使用。</p><p>如果 <code>&lt;源路径&gt;</code> 为一个 <code>tar</code> 压缩文件的话，压缩格式为 <code>gzip</code>, <code>bzip2</code> 以及 <code>xz</code> 的情况下，<code>ADD</code> 指令将会自动解压缩这个压缩文件到 <code>&lt;目标路径&gt;</code> 去。</p><p>复制个压缩文件进去，而不解压缩，这时就不可以使用 <code>ADD</code> 命令了。</p><p><code>ADD</code> 指令会令镜像构建缓存失效，从而可能会令镜像构建变得比较缓慢。</p><p>因此在 <code>COPY</code> 和 <code>ADD</code> 指令中选择的时候，可以遵循这样的原则，所有的文件复制均使用 <code>COPY</code> 指令，仅在需要自动解压缩的场合使用 <code>ADD</code>。</p><p>在使用该指令的时候还可以加上 <code>--chown=&lt;user&gt;:&lt;group&gt;</code> 选项来改变文件的所属用户及所属组。</p><h2 id="CMD-容器启动命令"><a href="#CMD-容器启动命令" class="headerlink" title="CMD(容器启动命令)"></a>CMD(容器启动命令)</h2><ul><li><strong>shell格式</strong>：<code>CMD &lt;命令&gt;</code></li><li><strong>exec格式</strong>：<code>CMD ["可执行文件", "参数1", "参数2"...]</code></li><li><strong>参数列表格式</strong>：<code>CMD ["参数1", "参数2"...]</code>。在指定了 <code>ENTRYPOINT</code> 指令后，用 <code>CMD</code> 指定具体的参数。</li></ul><p>Docker 不是虚拟机，容器就是进程。既然是进程，那么在启动容器的时候，需要指定所运行的程序及参数。<code>CMD</code> 指令就是用于指定默认的容器主进程的启动命令的。</p><p>在运行时可以指定新的命令来替代镜像设置中的这个默认命令</p><p>在指令格式上，一般推荐使用 <code>exec</code> 格式，这类格式在解析时会被解析为 JSON 数组，因此一定要使用双引号 <code>"</code>，而不要使用单引号。</p><p>如果使用 <code>shell</code> 格式的话，实际的命令会被包装为 <code>sh -c</code> 的参数的形式进行执行。比如：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">CMD <span class="token builtin class-name">echo</span> <span class="token environment constant">$HOME</span><span class="token comment"># 在实际执行中，会将其变更为</span>CMD <span class="token punctuation">[</span> <span class="token string">"sh"</span>, <span class="token string">"-c"</span>, <span class="token string">"echo <span class="token environment constant">$HOME</span>"</span> <span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>这就是为什么我们可以使用环境变量的原因，因为这些环境变量会被 shell 进行解析处理。</p><p>提到 <code>CMD</code> 就不得不提容器中应用在前台执行和后台执行的问题。</p><p>Docker 不是虚拟机，容器中的应用都应该以前台执行，而不是像虚拟机、物理机里面那样，用 <code>systemd</code> 去启动后台服务，容器内没有后台服务的概念。</p><p>对于容器而言，其启动程序就是容器应用进程，容器就是为了主进程而存在的，主进程退出，容器就失去了存在的意义，从而退出，其它辅助进程不是它需要关心的东西。</p><p>而使用 <code>service nginx start</code> 命令，则是希望 upstart 来以后台守护进程形式启动 <code>nginx</code> 服务。而刚才说了 <code>CMD service nginx start</code> 会被理解为 <code>CMD [ "sh", "-c", "service nginx start"]</code>，因此主进程实际上是 <code>sh</code>。那么当 <code>service nginx start</code> 命令结束后，<code>sh</code> 也就结束了，<code>sh</code> 作为主进程退出了，自然就会令容器退出。</p><p>正确的做法是直接执行 <code>nginx</code> 可执行文件，并且要求以前台形式运行。比如：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">CMD <span class="token punctuation">[</span><span class="token string">"nginx"</span>, <span class="token string">"-g"</span>, <span class="token string">"daemon off;"</span><span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="ENTRYPOINT-入口点"><a href="#ENTRYPOINT-入口点" class="headerlink" title="ENTRYPOINT(入口点)"></a>ENTRYPOINT(入口点)</h2><p><code>ENTRYPOINT</code> 在运行时也可以替代，需要通过 <code>docker run</code> 的参数 <code>--entrypoint</code> 来指定。当指定了 <code>ENTRYPOINT</code> 后，<code>CMD</code> 的含义就发生了改变，不再是直接的运行其命令，而是将 <code>CMD</code> 的内容作为参数传给 <code>ENTRYPOINT</code> 指令，换句话说实际执行时，将变为：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token operator">&lt;</span>ENTRYPOINT<span class="token operator">&gt;</span> <span class="token string">"&lt;CMD&gt;"</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="场景一：让镜像变成像命令一样使用"><a href="#场景一：让镜像变成像命令一样使用" class="headerlink" title="场景一：让镜像变成像命令一样使用"></a>场景一：让镜像变成像命令一样使用</h3><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile">FROM ubuntu:18.04RUN apt-get update \    &amp;&amp; apt-get install -y curl \    &amp;&amp; rm -rf /var/lib/apt/lists/*ENTRYPOINT [ "curl", "-s", "http://myip.ipip.net" ]# docker run myip# 当存在 `ENTRYPOINT` 后，`CMD` 的内容将会作为参数传给 `ENTRYPOINT`，而这里 `-i` 就是新的 `CMD`# docker run myip -i<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h1 id="场景二：应用运行前的准备工作"><a href="#场景二：应用运行前的准备工作" class="headerlink" title="场景二：应用运行前的准备工作"></a>场景二：应用运行前的准备工作</h1><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile">FROM alpine:3.4...RUN addgroup -S redis &amp;&amp; adduser -S -G redis redis...ENTRYPOINT ["docker-entrypoint.sh"]EXPOSE 6379CMD [ "redis-server" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>可以看到其中为了 redis 服务创建了 redis 用户，并在最后指定了 <code>ENTRYPOINT</code> 为 <code>docker-entrypoint.sh</code> 脚本。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token shebang important">#!/bin/sh</span><span class="token punctuation">..</span>.<span class="token comment"># allow the container to be started with `--user`</span><span class="token keyword">if</span> <span class="token punctuation">[</span> <span class="token string">"<span class="token variable">$1</span>"</span> <span class="token operator">=</span> <span class="token string">'redis-server'</span> -a <span class="token string">"<span class="token variable"><span class="token variable">$(</span><span class="token function">id</span> -u<span class="token variable">)</span></span>"</span> <span class="token operator">=</span> <span class="token string">'0'</span> <span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>    <span class="token function">find</span> <span class="token builtin class-name">.</span> <span class="token punctuation">\</span><span class="token operator">!</span> -user redis -exec <span class="token function">chown</span> redis <span class="token string">'{}'</span> +    <span class="token builtin class-name">exec</span> gosu redis <span class="token string">"<span class="token variable">$0</span>"</span> <span class="token string">"<span class="token variable">$@</span>"</span><span class="token keyword">fi</span><span class="token builtin class-name">exec</span> <span class="token string">"<span class="token variable">$@</span>"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>该脚本的内容就是根据 <code>CMD</code> 的内容来判断，如果是 <code>redis-server</code> 的话，则切换到 <code>redis</code> 用户身份启动服务器，否则依旧使用 <code>root</code> 身份执行。比如：</p><pre class="line-numbers language-none"><code class="language-none">$ docker run -it redis iduid=0(root) gid=0(root) groups=0(root)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h2 id="ENV-设置环境变量"><a href="#ENV-设置环境变量" class="headerlink" title="ENV(设置环境变量)"></a>ENV(设置环境变量)</h2><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile"># 格式ENV &lt;key&gt; &lt;value&gt;ENV &lt;key1&gt;=&lt;value1&gt; &lt;key2&gt;=&lt;value2&gt;...# sample# 这个示例包含如何换行，如何包含空格的值，如何在Dockerfile使用环境变量ENV VERSION=1.0 DEBUG=on \    NAME="Happy Feet"RUN echo $VERSION<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>下列指令可以支持环境变量展开： <code>ADD</code>、<code>COPY</code>、<code>ENV</code>、<code>EXPOSE</code>、<code>FROM</code>、<code>LABEL</code>、<code>USER</code>、<code>WORKDIR</code>、<code>VOLUME</code>、<code>STOPSIGNAL</code>、<code>ONBUILD</code>、<code>RUN</code>。</p><h2 id="ARG-构建参数"><a href="#ARG-构建参数" class="headerlink" title="ARG(构建参数)"></a>ARG(构建参数)</h2><p>格式：<code>ARG &lt;参数名&gt;[=&lt;默认值&gt;]</code></p><p>构建参数和 <code>ENV</code> 的效果一样，都是设置环境变量。所不同的是，<code>ARG</code> 所设置的构建环境的环境变量，在将来容器运行时是不会存在这些环境变量的。但是不要因此就使用 <code>ARG</code> 保存密码之类的信息，因为 <code>docker history</code> 还是可以看到所有值的。</p><p><code>Dockerfile</code> 中的 <code>ARG</code> 指令是定义参数名称，以及定义其默认值。该默认值可以在构建命令 <code>docker build</code> 中用 <code>--build-arg &lt;参数名&gt;=&lt;值&gt;</code> 来覆盖。</p><p>ARG 指令有生效范围，如果在 <code>FROM</code> 指令之前指定，那么只能用于 <code>FROM</code> 指令中。</p><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile"># ARG指令有效范围## 如果在 `FROM` 指令之前指定，那么只能用于 `FROM` 指令中ARG DOCKER_USERNAME=libraryFROM ${DOCKER_USERNAME}/alpine## 这里检测不到ARG，需要在这里重新指定RUN set -x ; echo ${DOCKER_USERNAME}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile"># 这个变量在每个 FROM 中都生效ARG DOCKER_USERNAME=libraryFROM ${DOCKER_USERNAME}/alpineRUN set -x ; echo 1FROM ${DOCKER_USERNAME}/alpineRUN set -x ; echo 2# 对于在各个阶段中使用的变量都必须在每个阶段分别指定ARG DOCKER_USERNAME=libraryFROM ${DOCKER_USERNAME}/alpine# 在FROM 之后使用变量，必须在每个阶段分别指定ARG DOCKER_USERNAME=libraryRUN set -x ; echo ${DOCKER_USERNAME}FROM ${DOCKER_USERNAME}/alpine# 在FROM 之后使用变量，必须在每个阶段分别指定ARG DOCKER_USERNAME=libraryRUN set -x ; echo ${DOCKER_USERNAME}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="VOLUME-定义匿名卷"><a href="#VOLUME-定义匿名卷" class="headerlink" title="VOLUME(定义匿名卷)"></a>VOLUME(定义匿名卷)</h2><p>格式为：</p><ul><li><code>VOLUME ["&lt;路径1&gt;", "&lt;路径2&gt;"...]</code></li><li><code>VOLUME &lt;路径&gt;</code></li></ul><pre class="line-numbers language-dockerfile" data-language="dockerfile"><code class="language-dockerfile"># `/data` 目录就会在容器运行时自动挂载为匿名卷，任何向 `/data` 中写入的信息都不会记录进容器存储层，从而保证了容器存储层的无状态化。VOLUME /data$ docker run -d -v mydata:/data xxxx# 在这行命令中，就使用了 `mydata` 这个命名卷挂载到了 `/data` 这个位置，替代了 `Dockerfile` 中定义的匿名卷的挂载配置。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h2 id="EXPOSE-暴露端口"><a href="#EXPOSE-暴露端口" class="headerlink" title="EXPOSE(暴露端口)"></a>EXPOSE(暴露端口)</h2><p>格式为 <code>EXPOSE &lt;端口1&gt; [&lt;端口2&gt;...]</code>。</p><p><code>EXPOSE</code> 指令是声明容器运行时提供服务的端口，这只是一个声明，在容器运行时并不会因为这个声明应用就会开启这个端口的服务。</p><p>在 Dockerfile 中写入这样的声明有两个好处，一个是帮助镜像使用者理解这个镜像服务的守护端口，以方便配置映射；另一个用处则是在运行时使用随机端口映射时，也就是 <code>docker run -P</code> 时，会自动随机映射 <code>EXPOSE</code> 的端口。</p><p>要将 <code>EXPOSE</code> 和在运行时使用 <code>-p &lt;宿主端口&gt;:&lt;容器端口&gt;</code> 区分开来。<code>-p</code>，是映射宿主端口和容器端口，换句话说，就是将容器的对应端口服务公开给外界访问，而 <code>EXPOSE</code> 仅仅是声明容器打算使用什么端口而已，并不会自动在宿主进行端口映射。</p><h2 id="WORKDIR-指定工作目录"><a href="#WORKDIR-指定工作目录" class="headerlink" title="WORKDIR(指定工作目录)"></a>WORKDIR(指定工作目录)</h2><p>格式为 <code>WORKDIR &lt;工作目录路径&gt;</code>。</p><p>使用 <code>WORKDIR</code> 指令可以来指定工作目录（或者称为当前目录），以后各层的当前目录就被改为指定的目录，如该目录不存在，<code>WORKDIR</code> 会帮你建立目录。</p><pre class="line-numbers language-none"><code class="language-none"># 错误做法RUN cd /appRUN echo "hello" &gt; world.txt# 如果需要改变以后各层的工作目录的位置WORKDIR /appRUN echo "hello" &gt; world.txt<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>如果将这个 <code>Dockerfile</code> 进行构建镜像运行后，会发现找不到 <code>/app/world.txt</code> 文件，或者其内容不是 <code>hello</code>。原因其实很简单，在 Shell 中，连续两行是同一个进程执行环境，因此前一个命令修改的内存状态，会直接影响后一个命令；而在 <code>Dockerfile</code> 中，这两行 <code>RUN</code> 命令的执行环境根本不同，是两个完全不同的容器。</p><p>之前说过每一个 <code>RUN</code> 都是启动一个容器、执行命令、然后提交存储层文件变更。第一层 <code>RUN cd /app</code> 的执行仅仅是当前进程的工作目录变更，一个内存上的变化而已，其结果不会造成任何文件变更。而到第二层的时候，启动的是一个全新的容器，跟第一层的容器更完全没关系，自然不可能继承前一层构建过程中的内存变化。</p><pre class="line-numbers language-none"><code class="language-none"># 如果你的 `WORKDIR` 指令使用的相对路径，那么所切换的路径与之前的 `WORKDIR` 有关WORKDIR /aWORKDIR bWORKDIR cRUN pwd# `RUN pwd` 的工作目录为 `/a/b/c`<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="USER-指定当前用户"><a href="#USER-指定当前用户" class="headerlink" title="USER(指定当前用户)"></a>USER(指定当前用户)</h2><p>格式：<code>USER &lt;用户名&gt;[:&lt;用户组&gt;]</code></p><p><code>USER</code> 指令和 <code>WORKDIR</code> 相似，都是改变环境状态并影响以后的层。<code>WORKDIR</code> 是改变工作目录，<code>USER</code> 则是改变之后层的执行 <code>RUN</code>, <code>CMD</code> 以及 <code>ENTRYPOINT</code> 这类命令的身份。</p><blockquote><p><strong>注意</strong><code>USER</code> 只是帮助你切换到指定用户而已，这个用户必须是事先建立好的，否则无法切换。</p></blockquote><pre class="line-numbers language-none"><code class="language-none">RUN groupadd -r redis &amp;&amp; useradd -r -g redis redisUSER redisRUN [ "redis-server" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>如果以 <code>root</code> 执行的脚本，在执行期间希望改变身份，比如希望以某个已经建立好的用户来运行某个服务进程，不要使用 <code>su</code> 或者 <code>sudo</code>，这些都需要比较麻烦的配置，而且在 TTY 缺失的环境下经常出错。建议使用 <a href="https://github.com/tianon/gosu"><code>gosu</code></a>。</p><pre class="line-numbers language-none"><code class="language-none"># 建立 redis 用户，并使用 gosu 换另一个用户执行命令RUN groupadd -r redis &amp;&amp; useradd -r -g redis redis# 下载 gosuRUN wget -O /usr/local/bin/gosu "https://github.com/tianon/gosu/releases/download/1.12/gosu-amd64" \    &amp;&amp; chmod +x /usr/local/bin/gosu \    &amp;&amp; gosu nobody true# 设置 CMD，并以另外的用户执行CMD [ "exec", "gosu", "redis", "redis-server" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="HEALTHCHECK-健康检查"><a href="#HEALTHCHECK-健康检查" class="headerlink" title="HEALTHCHECK(健康检查)"></a>HEALTHCHECK(健康检查)</h2><p>格式：</p><ul><li><code>HEALTHCHECK [选项] CMD &lt;命令&gt;</code>：设置检查容器健康状况的命令</li><li><code>HEALTHCHECK NONE</code>：如果基础镜像有健康检查指令，使用这行可以屏蔽掉其健康检查指令</li></ul><p><code>HEALTHCHECK</code> 指令是告诉 Docker 应该如何进行判断容器的状态是否正常，这是 Docker 1.12 引入的新指令。</p><p>在没有 <code>HEALTHCHECK</code> 指令前，Docker 引擎只可以通过容器内主进程是否退出来判断容器是否状态异常。很多情况下这没问题，但是如果程序进入死锁状态，或者死循环状态，应用进程并不退出，但是该容器已经无法提供服务了。在 1.12 以前，Docker 不会检测到容器的这种状态，从而不会重新调度，导致可能会有部分容器已经无法提供服务了却还在接受用户请求。</p><p>而自 1.12 之后，Docker 提供了 <code>HEALTHCHECK</code> 指令，通过该指令指定一行命令，用这行命令来判断容器主进程的服务状态是否还正常，从而比较真实的反应容器实际状态。</p><p>当在一个镜像指定了 <code>HEALTHCHECK</code> 指令后，用其启动容器，初始状态会为 <code>starting</code>，在 <code>HEALTHCHECK</code> 指令检查成功后变为 <code>healthy</code>，如果连续一定次数失败，则会变为 <code>unhealthy</code>。</p><p><code>HEALTHCHECK</code> 支持下列选项：</p><ul><li><code>--interval=&lt;间隔&gt;</code>：两次健康检查的间隔，默认为 30 秒；</li><li><code>--timeout=&lt;时长&gt;</code>：健康检查命令运行超时时间，如果超过这个时间，本次健康检查就被视为失败，默认 30 秒；</li><li><code>--retries=&lt;次数&gt;</code>：当连续失败指定次数后，则将容器状态视为 <code>unhealthy</code>，默认 3 次。</li></ul><p>和 <code>CMD</code>, <code>ENTRYPOINT</code> 一样，<code>HEALTHCHECK</code> 只可以出现一次，如果写了多个，只有最后一个生效。</p><p>在 <code>HEALTHCHECK [选项] CMD</code> 后面的命令，格式和 <code>ENTRYPOINT</code> 一样，分为 <code>shell</code> 格式，和 <code>exec</code> 格式。命令的返回值决定了该次健康检查的成功与否：<code>0</code>：成功；<code>1</code>：失败；<code>2</code>：保留，不要使用这个值。</p><p>假设我们有个镜像是个最简单的 Web 服务，我们希望增加健康检查来判断其 Web 服务是否在正常工作，我们可以用 <code>curl</code> 来帮助判断，其 <code>Dockerfile</code> 的 <code>HEALTHCHECK</code> 可以这么写：</p><pre class="line-numbers language-none"><code class="language-none">FROM nginxRUN apt-get update &amp;&amp; apt-get install -y curl &amp;&amp; rm -rf /var/lib/apt/lists/*HEALTHCHECK --interval=5s --timeout=3s \  CMD curl -fs http://localhost/ || exit 1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>这里我们设置了每 5 秒检查一次（这里为了试验所以间隔非常短，实际应该相对较长），如果健康检查命令超过 3 秒没响应就视为失败，并且使用 <code>curl -fs http://localhost/ || exit 1</code> 作为健康检查命令。</p><p>使用 <code>docker build</code> 来构建这个镜像：</p><pre class="line-numbers language-none"><code class="language-none">$ docker build -t myweb:v1 .<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>构建好了后，我们启动一个容器：</p><pre class="line-numbers language-none"><code class="language-none">$ docker run -d --name web -p 80:80 myweb:v1<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>当运行该镜像后，可以通过 <code>docker container ls</code> 看到最初的状态为 <code>(health: starting)</code>：</p><pre class="line-numbers language-none"><code class="language-none">$ docker container lsCONTAINER ID        IMAGE               COMMAND                  CREATED             STATUS                            PORTS               NAMES03e28eb00bd0        myweb:v1            "nginx -g 'daemon off"   3 seconds ago       Up 2 seconds (health: starting)   80/tcp, 443/tcp     web<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>在等待几秒钟后，再次 <code>docker container ls</code>，就会看到健康状态变化为了 <code>(healthy)</code>：</p><pre class="line-numbers language-none"><code class="language-none">$ docker container lsCONTAINER ID        IMAGE               COMMAND                  CREATED             STATUS                    PORTS               NAMES03e28eb00bd0        myweb:v1            "nginx -g 'daemon off"   18 seconds ago      Up 16 seconds (healthy)   80/tcp, 443/tcp     web<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>如果健康检查连续失败超过了重试次数，状态就会变为 <code>(unhealthy)</code>。</p><p>为了帮助排障，健康检查命令的输出（包括 <code>stdout</code> 以及 <code>stderr</code>）都会被存储于健康状态里，可以用 <code>docker inspect</code> 来查看。</p><pre class="line-numbers language-none"><code class="language-none">$ docker inspect --format '{{json .State.Health}}' web | python -m json.tool{    "FailingStreak": 0,    "Log": [        {            "End": "2016-11-25T14:35:37.940957051Z",            "ExitCode": 0,            "Output": "&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;\n&lt;title&gt;Welcome to nginx!&lt;/title&gt;\n&lt;style&gt;\n    body {\n        width: 35em;\n        margin: 0 auto;\n        font-family: Tahoma, Verdana, Arial, sans-serif;\n    }\n&lt;/style&gt;\n&lt;/head&gt;\n&lt;body&gt;\n&lt;h1&gt;Welcome to nginx!&lt;/h1&gt;\n&lt;p&gt;If you see this page, the nginx web server is successfully installed and\nworking. Further configuration is required.&lt;/p&gt;\n\n&lt;p&gt;For online documentation and support please refer to\n&lt;a href=\"http://nginx.org/\"&gt;nginx.org&lt;/a&gt;.&lt;br/&gt;\nCommercial support is available at\n&lt;a href=\"http://nginx.com/\"&gt;nginx.com&lt;/a&gt;.&lt;/p&gt;\n\n&lt;p&gt;&lt;em&gt;Thank you for using nginx.&lt;/em&gt;&lt;/p&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n",            "Start": "2016-11-25T14:35:37.780192565Z"        }    ],    "Status": "healthy"}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="ONBUILD"><a href="#ONBUILD" class="headerlink" title="ONBUILD"></a>ONBUILD</h2><p>格式：<code>ONBUILD &lt;其它指令&gt;</code>。</p><p><code>ONBUILD</code> 是一个特殊的指令，它后面跟的是其它指令，比如 <code>RUN</code>, <code>COPY</code> 等，而这些指令，在当前镜像构建时并不会被执行。只有当以当前镜像为基础镜像，去构建下一级镜像的时候才会被执行。</p><p><code>Dockerfile</code> 中的其它指令都是为了定制当前镜像而准备的，唯有 <code>ONBUILD</code> 是为了帮助别人定制自己而准备的。</p><p>假设我们要制作 Node.js 所写的应用的镜像。我们都知道 Node.js 使用 <code>npm</code> 进行包管理，所有依赖、配置、启动信息等会放到 <code>package.json</code> 文件里。在拿到程序代码后，需要先进行 <code>npm install</code> 才可以获得所有需要的依赖。然后就可以通过 <code>npm start</code> 来启动应用。因此，一般来说会这样写 <code>Dockerfile</code>：</p><pre class="line-numbers language-none"><code class="language-none">FROM node:slimRUN mkdir /appWORKDIR /appCOPY ./package.json /appRUN [ "npm", "install" ]COPY . /app/CMD [ "npm", "start" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>把这个 <code>Dockerfile</code> 放到 Node.js 项目的根目录，构建好镜像后，就可以直接拿来启动容器运行。但是如果我们还有第二个 Node.js 项目也差不多呢？好吧，那就再把这个 <code>Dockerfile</code> 复制到第二个项目里。那如果有第三个项目呢？再复制么？文件的副本越多，版本控制就越困难，让我们继续看这样的场景维护的问题。</p><p>如果第一个 Node.js 项目在开发过程中，发现这个 <code>Dockerfile</code> 里存在问题，比如敲错字了、或者需要安装额外的包，然后开发人员修复了这个 <code>Dockerfile</code>，再次构建，问题解决。第一个项目没问题了，但是第二个项目呢？虽然最初 <code>Dockerfile</code> 是复制、粘贴自第一个项目的，但是并不会因为第一个项目修复了他们的 <code>Dockerfile</code>，而第二个项目的 <code>Dockerfile</code> 就会被自动修复。</p><p>那么我们可不可以做一个基础镜像，然后各个项目使用这个基础镜像呢？这样基础镜像更新，各个项目不用同步 <code>Dockerfile</code> 的变化，重新构建后就继承了基础镜像的更新？好吧，可以，让我们看看这样的结果。那么上面的这个 <code>Dockerfile</code> 就会变为：</p><pre class="line-numbers language-none"><code class="language-none">FROM node:slimRUN mkdir /appWORKDIR /appCMD [ "npm", "start" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>这里我们把项目相关的构建指令拿出来，放到子项目里去。假设这个基础镜像的名字为 <code>my-node</code> 的话，各个项目内的自己的 <code>Dockerfile</code> 就变为：</p><pre class="line-numbers language-none"><code class="language-none">FROM my-nodeCOPY ./package.json /appRUN [ "npm", "install" ]COPY . /app/<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>基础镜像变化后，各个项目都用这个 <code>Dockerfile</code> 重新构建镜像，会继承基础镜像的更新。</p><p>那么，问题解决了么？没有。准确说，只解决了一半。如果这个 <code>Dockerfile</code> 里面有些东西需要调整呢？比如 <code>npm install</code> 都需要加一些参数，那怎么办？这一行 <code>RUN</code> 是不可能放入基础镜像的，因为涉及到了当前项目的 <code>./package.json</code>，难道又要一个个修改么？所以说，这样制作基础镜像，只解决了原来的 <code>Dockerfile</code> 的前4条指令的变化问题，而后面三条指令的变化则完全没办法处理。</p><p><code>ONBUILD</code> 可以解决这个问题。让我们用 <code>ONBUILD</code> 重新写一下基础镜像的 <code>Dockerfile</code>:</p><pre class="line-numbers language-none"><code class="language-none">FROM node:slimRUN mkdir /appWORKDIR /appONBUILD COPY ./package.json /appONBUILD RUN [ "npm", "install" ]ONBUILD COPY . /app/CMD [ "npm", "start" ]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这次我们回到原始的 <code>Dockerfile</code>，但是这次将项目相关的指令加上 <code>ONBUILD</code>，这样在构建基础镜像的时候，这三行并不会被执行。然后各个项目的 <code>Dockerfile</code> 就变成了简单地：</p><pre class="line-numbers language-none"><code class="language-none">FROM my-node<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>是的，只有这么一行。当在各个项目目录中，用这个只有一行的 <code>Dockerfile</code> 构建镜像时，之前基础镜像的那三行 <code>ONBUILD</code> 就会开始执行，成功的将当前项目的代码复制进镜像、并且针对本项目执行 <code>npm install</code>，生成应用镜像。</p><h2 id="LABEL-为镜像添加元数据"><a href="#LABEL-为镜像添加元数据" class="headerlink" title="LABEL(为镜像添加元数据)"></a>LABEL(为镜像添加元数据)</h2><p><code>LABEL</code> 指令用来给镜像以键值对的形式添加一些元数据（metadata）。</p><pre class="line-numbers language-none"><code class="language-none">LABEL &lt;key&gt;=&lt;value&gt; &lt;key&gt;=&lt;value&gt; &lt;key&gt;=&lt;value&gt; ...<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>我们还可以用一些标签来申明镜像的作者、文档地址等：</p><pre class="line-numbers language-none"><code class="language-none">LABEL org.opencontainers.image.authors="yeasy"LABEL org.opencontainers.image.documentation="https://yeasy.gitbooks.io"<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h2 id="SHELL"><a href="#SHELL" class="headerlink" title="SHELL"></a>SHELL</h2><p>格式：<code>SHELL ["executable", "parameters"]</code></p><p><code>SHELL</code> 指令可以指定 <code>RUN</code> <code>ENTRYPOINT</code> <code>CMD</code> 指令的 shell，Linux 中默认为 `[“/bin/sh”, “-c”]</p><pre class="line-numbers language-none"><code class="language-none">SHELL ["/bin/sh", "-c"]RUN lll ; lsSHELL ["/bin/sh", "-cex"]RUN lll ; ls<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>两个 <code>RUN</code> 运行同一命令，第二个 <code>RUN</code> 运行的命令会打印出每条命令并当遇到错误时退出。</p><p>当 <code>ENTRYPOINT</code> <code>CMD</code> 以 shell 格式指定时，<code>SHELL</code> 指令所指定的 shell 也会成为这两个指令的 shell</p><pre class="line-numbers language-none"><code class="language-none">SHELL ["/bin/sh", "-cex"]# /bin/sh -cex "nginx"ENTRYPOINT nginx<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-none"><code class="language-none">SHELL ["/bin/sh", "-cex"]# /bin/sh -cex "nginx"CMD nginx<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> Docker </category>
          
      </categories>
      
      
        <tags>
            
            <tag> dockerfile </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【Gitlab】Gitlab CI</title>
      <link href="/2021/10/29/gitlab/gitlab-cicd/gitlab-gitlab-cicd/"/>
      <url>/2021/10/29/gitlab/gitlab-cicd/gitlab-gitlab-cicd/</url>
      
        <content type="html"><![CDATA[<h1 id="【Gitlab】Gitlab-CI-CD"><a href="#【Gitlab】Gitlab-CI-CD" class="headerlink" title="【Gitlab】Gitlab CI/CD"></a>【Gitlab】Gitlab CI/CD</h1><p>[TOC]</p><blockquote><p>gitlab cicd文档：<a href="https://docs.gitlab.com/ee/ci/">https://docs.gitlab.com/ee/ci/</a></p></blockquote><h2 id="gitlab-cicd简介"><a href="#gitlab-cicd简介" class="headerlink" title="gitlab cicd简介"></a>gitlab cicd简介</h2><p>Gitlab CI/CD 是一款用于持续集成（CI），持续交付（CD）的工具。</p><h2 id="gitlab-cicd环境搭建"><a href="#gitlab-cicd环境搭建" class="headerlink" title="gitlab cicd环境搭建"></a>gitlab cicd环境搭建</h2><h3 id="gitlab-ci简介"><a href="#gitlab-ci简介" class="headerlink" title="gitlab ci简介"></a>gitlab ci简介</h3><ul><li>GitLab提交持续集成服务，当你在项目根目录中添加 <code>.gitlab-ci.yml</code> 文件，并配置项目的运行器( <code>GitLab Runner</code> )，那么后续的每次提交都会触发CI流水线( <code>pipeline</code> )的执行。</li><li>Job作业是 <code>.gitlab-ci.yml</code> 文件的基本元素，每个作业至少有 <code>script</code> 子句，在流水线中可以定义任意多个作业。</li><li>每个作业必须具有唯一的名称，但有一些保留的关键字不能用作作业名称</li><li>在流水线脚本中可以使用预定义的全局变量</li></ul><p>​    #### gitlab </p><h3 id="参数详解"><a href="#参数详解" class="headerlink" title="参数详解"></a>参数详解</h3><h4 id="script"><a href="#script" class="headerlink" title="script"></a><strong>script</strong></h4><p><code>script</code> 是作业中唯一必须的关键字参数，是运行器需要执行的脚本</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">build</span><span class="token punctuation">:</span><span class="token key atrule">script</span><span class="token punctuation">:</span><span class="token punctuation">-</span> echo "hello world"<span class="token punctuation">-</span> uname <span class="token punctuation">-</span>a<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>注意：使用冒号时应使用引号包裹起来，使用特殊字符时需要特别注意！</p><p>注意如果要输出冒号字符，冒号后面不能紧接空格！</p></blockquote><h4 id="image"><a href="#image" class="headerlink" title="image"></a><strong>image</strong></h4><p><code>image</code> 指定使用Docker镜像。</p><h4 id="services"><a href="#services" class="headerlink" title="services"></a><strong>services</strong></h4><p><code>services</code> 指定使用Docker镜像服务</p><h4 id="before-script"><a href="#before-script" class="headerlink" title="before_script"></a><strong>before_script</strong></h4><p><code>before_script</code> 用于定义在所有作业之前需要执行的命令</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">before_script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> echo "Before script section"<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h4 id="after-script"><a href="#after-script" class="headerlink" title="after_script"></a><strong>after_script</strong></h4><p><code>after_script</code> 用于定义在所有作业(即使失败)之后需要执行的命令</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">after_script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> echo "After script section"<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p><strong>特点：</strong></p><ul><li>before_script和script在一个上下文中是串行执行的，after_script是独立执行的，即after_script与before_script/script的上下文环境不同。</li><li>after_script会将当前工作目录设置为默认值。</li><li>在after_script中无法看到在before_script和script中所做的修改：<ul><li>在before_script和script中的命名别名、导出变量，对after_script不可见</li><li>before_script和script在工作树之外安装的软件，对after_script不可见</li></ul></li><li>你可以在作业中定义before_script，after_script，也可以将其定义为顶级元素，定义为顶级元素将为每一个任务都执行相应阶段的脚本或命令。<strong>作业级会覆盖全局级的定义。</strong></li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">before_script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> echo "Before script section"<span class="token key atrule">after_script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> echo "After script section"<span class="token key atrule">build1</span><span class="token punctuation">:</span><span class="token key atrule">stage</span><span class="token punctuation">:</span> build<span class="token key atrule">before_script</span><span class="token punctuation">:</span> <span class="token punctuation">-</span> yum install cloc <span class="token punctuation">-</span>y<span class="token key atrule">after_script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> cloc <span class="token punctuation">-</span><span class="token punctuation">-</span>version<span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> cloc <span class="token punctuation">-</span><span class="token punctuation">-</span>version<span class="token key atrule">tags</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> bluelog<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="stages"><a href="#stages" class="headerlink" title="stages"></a><strong>stages</strong></h4><p><code>stages</code> 定义流水线全局可使用的阶段，阶段允许有灵活的多级管道，<strong>阶段元素的排序定义了作业执行的顺序</strong></p><ul><li>相同 <code>stage</code> 阶段的作业并行运行</li><li>默认情况下，上一阶段的作业全部运行成功后才执行下一阶段的作业</li><li>默认有三个阶段， <code>build</code> 、<code>test</code> 、<code>deploy</code> 三个阶段</li><li>如果一个作业未定义 <code>stage</code> 阶段，则作业使用 <code>test</code> 测试阶段</li><li>默认情况下，任何一个前置的作业失败了，commit提交会标记为failed并且下一个stages的作业都不会执行</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">stages</span><span class="token punctuation">:</span><span class="token punctuation">-</span> build<span class="token punctuation">-</span> test<span class="token punctuation">-</span> build_image<span class="token punctuation">-</span> deploy<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="stage"><a href="#stage" class="headerlink" title="stage"></a><strong>stage</strong></h4><p><code>stage</code> 定义流水线中每个作业所处的阶段，处于相同阶段的作业并行执行</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">Build Image</span><span class="token punctuation">:</span><span class="token key atrule">stage</span><span class="token punctuation">:</span> build_image<span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>默认情况下，GitLab Runner运行器每次只执行一个作业，只有当满足以下条件之一时，才会真正的并行执行:</p><ul><li>作业运行在不同的运行器上；</li><li>你修改了运行器的 <code>concurrent</code> 设置，默认情况下 <code>concurrent = 1</code> 。</li></ul></blockquote><h4 id="only-except"><a href="#only-except" class="headerlink" title="only/except"></a><strong>only/except</strong></h4><p><code>only</code> 和 <code>except</code> 用于在创建作业时对作业的限制策略。</p><ul><li><code>only</code> 定义了哪些分支或标签(branches and tags)的作业会运行,其默认为 <code>only: ['branches', 'tags']</code></li><li><code>except</code> 定义了哪些分支或标签(branches and tags)的作业不会运行,默认 <code>except</code> 规则为空</li></ul><p><strong>策略规则：</strong></p><ul><li><code>only</code> 和 <code>except</code> 可同时使用，如果在一个作业中同时定义了 <code>only</code> 和 <code>except</code> ，则同时 <code>only</code> <code>except</code> 进行过滤(注意，不是忽略 <code>except</code> 条件) 。</li><li><code>only</code> 和 <code>except</code> 可以使用正则表达式。</li><li><code>only</code> 和 <code>except</code> 允许指定用于过滤forks作业的存储库路径。</li><li><code>only</code> 和 <code>except</code> 中可以使用特殊的关键字</li></ul><p><strong><code>only</code> 和 <code>except</code> 中可以使用特殊的关键字</strong>：</p><table><thead><tr><th>关键字</th><th>描述释义</th></tr></thead><tbody><tr><td>branches</td><td>当一个分支被push上来</td></tr><tr><td>tags</td><td>当一个打了tag标记的Release被提交时</td></tr><tr><td>api</td><td>当一个pipline被第二个piplines api所触发调起(不是触发器API)</td></tr><tr><td>external</td><td>当使用了GitLab以外的外部CI服务，如Jenkins</td></tr><tr><td>pipelines</td><td>针对多项目触发器而言，当使用CI_JOB_TOKEN， 并使用gitlab所提供的api创建多个pipelines的时候</td></tr><tr><td>pushes</td><td>当pipeline被用户的git push操作所触发的时候</td></tr><tr><td>schedules</td><td>针对预定好的pipline计划而言（每日构建一类）</td></tr><tr><td>triggers</td><td>用触发器token创建piplines的时候</td></tr><tr><td>web</td><td>在GitLab WEB页面上Pipelines标签页下，按下run pipline的时候</td></tr><tr><td>merge_requests</td><td>当合并请求创建或更新的时候</td></tr><tr><td>chats</td><td>当使用GitLab ChatOps 创建作业的时候</td></tr></tbody></table><p><strong>使用示例：</strong></p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># job将只会运行以 issue- 开始的refs(分支)，然而except中指定分支不能执行，</span><span class="token comment"># 所以这个job将不会执行</span><span class="token key atrule">job</span><span class="token punctuation">:</span><span class="token key atrule">only</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> /^issue<span class="token punctuation">-</span>.<span class="token important">*$/</span><span class="token key atrule">except</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> branches<span class="token comment"># 匹配模式默认是大小写敏感的(case-sensitive)，使用 i 标志，</span><span class="token comment"># 如 /pattern/i 可以使匹配模式大小写不敏感</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> /^issue<span class="token punctuation">-</span>.<span class="token important">*$/i</span>  <span class="token key atrule">except</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> branches<span class="token comment"># 仅当指定标记的tags的refs引用，或者通过API触发器的构建、或者流水线计划调度的构建才会运行</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> tags    <span class="token punctuation">-</span> triggers    <span class="token punctuation">-</span> schedules<span class="token comment"># 仓库的路径(repository path)只能用于父级仓库执行作业，不能用于forks</span><span class="token comment"># 将会在所有分支执行，但 不会在 master主干以及以release/开头的分支上执行</span><span class="token key atrule">job</span><span class="token punctuation">:</span><span class="token key atrule">only</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> branches@gitlab<span class="token punctuation">-</span>org/gitlab<span class="token punctuation">-</span>ce<span class="token key atrule">except</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> master@gitlab<span class="token punctuation">-</span>org/gitlab<span class="token punctuation">-</span>ce  <span class="token punctuation">-</span> /^release/.<span class="token important">*$/@gitlab-org/gitlab-ce</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>正则表达式使用的说明:</strong></p><ul><li>因为 <code>@</code> 用于表示ref的存储库路径的开头，所以在正则表达式中匹配包含 <code>@</code> 字符的ref名称需要使用十六进制字符代码 <code>\x40</code> 。</li><li>仅标签和分支名称才能使用正则表达式匹配，仓库路径按字面意义匹配</li><li>如果使用正则表达式匹配标签或分支名称，则匹配模式的整个引用部分都是正则表达式</li><li>正则表达式必须以 <code>/</code> 开头和结尾，即 <code>/regular expressions/</code> ，因此， <code>issue-/.*/</code> 不会匹配以 <code>issue-</code> 开头的标签或分支。</li><li>可以在正则表达式中使用锚点 <code>^$</code> ，用来匹配开头或结尾，如 <code>/^issue-.*$/</code> 与 <code>/^issue-/</code> 等价， 但 <code>/issue/</code> 却可以匹配名称为 <code>severe-issues</code> 的分支，所以正则表达式的使用要谨慎！</li></ul><p><strong><code>only</code> 和 <code>except</code> 高级用法</strong></p><ul><li><p><code>only</code> 和 <code>except</code> 支持高级策略，<code>refs</code> 、 <code>variables</code> 、 <code>changes</code> 、 <code>kubernetes</code> 四个关键字可以使用。</p></li><li><p>如果同时使用多个关键字，中间的逻辑是 <code>逻辑与AND</code> 。</p></li><li><p>refs 策略可以使用 only 和 except 基本用法中的关键字</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># only:refs/except:refs</span><span class="token key atrule">deploy</span><span class="token punctuation">:</span> <span class="token comment"># deploy作业仅当流水线是计划作业或者在master主干运行</span>  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">refs</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> master      <span class="token punctuation">-</span> schedules<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>kubernetes</code> 策略仅支持 <code>active</code> 关键字</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># only:kubernetes/except:kubernetes</span><span class="token key atrule">deploy</span><span class="token punctuation">:</span> <span class="token comment"># deploy作业仅当kubernetes服务启动后才会运行</span>  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">kubernetes</span><span class="token punctuation">:</span> active<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>variables</code> 关键字用来定义变量表达式，你可以使用预定义变量、项目、组、环境变量来评估一个作业是否需要创建或运行。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># pnly:varaibles/except:varaibles</span><span class="token key atrule">deploy</span><span class="token punctuation">:</span> <span class="token comment"># 使用了变量表达式</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> cap staging deploy  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">refs</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> branches    <span class="token key atrule">variables</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> $RELEASE == "staging"      <span class="token punctuation">-</span> $STAGING<span class="token key atrule">end-to-end</span><span class="token punctuation">:</span> <span class="token comment"># 根据提交日志信息来排除某些作业</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> rake test<span class="token punctuation">:</span>end<span class="token punctuation">-</span>to<span class="token punctuation">-</span>end  <span class="token key atrule">except</span><span class="token punctuation">:</span>    <span class="token key atrule">variables</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> $CI_COMMIT_MESSAGE =~ /skip<span class="token punctuation">-</span>end<span class="token punctuation">-</span>to<span class="token punctuation">-</span>end<span class="token punctuation">-</span>tests/<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>changes</code> 策略表明一个作业只有在使用 <code>git push</code> 事件使文件发生变化时执行。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># only:changes/except:changes</span><span class="token key atrule">docker build</span><span class="token punctuation">:</span> <span class="token comment"># deploy作业仅当流水线是计划作业或者在master主干运行</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> docker build <span class="token punctuation">-</span>t my<span class="token punctuation">-</span>image<span class="token punctuation">:</span>$CI_COMMIT_REF_SLUG .  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">changes</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> Dockerfile      <span class="token punctuation">-</span> docker/scripts/*      <span class="token punctuation">-</span> dockerfiles/<span class="token important">**/*</span>      <span class="token punctuation">-</span> more_scripts/<span class="token important">*.</span><span class="token punctuation">{</span>rb<span class="token punctuation">,</span>py<span class="token punctuation">,</span>sh<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>使用 <code>glob模式匹配</code> 来匹配根目录下的文件，或者任何目录下的文件。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">test</span><span class="token punctuation">:</span>   <span class="token key atrule">script</span><span class="token punctuation">:</span> npm run test  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">changes</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> <span class="token string">"*.json"</span>      <span class="token punctuation">-</span> <span class="token string">"**/*.sql"</span><span class="token comment"># 当md文件发生变化时，会忽略CI作业</span><span class="token key atrule">build</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> npm run build  <span class="token key atrule">except</span><span class="token punctuation">:</span>    <span class="token key atrule">changes</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> <span class="token string">"*.md"</span><span class="token comment"># 合并请求中修改了 Dockerfile 文件或者修改了 service 目录下的文件，都会触发Docker构建。      </span><span class="token key atrule">docker build service one</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> docker build <span class="token punctuation">-</span>t my<span class="token punctuation">-</span>service<span class="token punctuation">-</span>one<span class="token punctuation">-</span>image<span class="token punctuation">:</span>$CI_COMMIT_REF_SLUG .  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token key atrule">refs</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> merge_requests    <span class="token key atrule">changes</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> Dockerfile      <span class="token punctuation">-</span> service<span class="token punctuation">-</span>one/<span class="token important">**/*</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p><code>glob模式匹配</code> 的字符串需要使用双引号包裹起来，否则会导致 <code>.gitlab-ci.yml</code> 解析错误。</p></blockquote></li></ul><h4 id="tags"><a href="#tags" class="headerlink" title="tags"></a><strong>tags</strong></h4><p><code>tags</code> 关键字用于指定 <code>GitLab Runner</code> 运行器使用哪一个运行器来执行作业。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 只有运行器注册时定义了 ruby 和 postgres 两个标签的运行器才能执行作业</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">tags</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> ruby    <span class="token punctuation">-</span> postgres<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="allow-failure"><a href="#allow-failure" class="headerlink" title="allow_failure"></a><strong>allow_failure</strong></h4><ul><li><code>allow_failure</code> 可以用于当你想设置一个作业失败的之后并不影响后续的CI组件的时候。失败的作业不会影响到commit提交状态。</li><li>如果允许失败的作业失败了，则相应的作业会显示一个黄色的警告，但对流水线成功与否不产生影响。</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">job1</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> execute_script_that_will_fail  <span class="token key atrule">allow_failure</span><span class="token punctuation">:</span> <span class="token boolean important">true</span><span class="token key atrule">job2</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> execute_script_that_will_succeed<span class="token key atrule">job3</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> deploy_to_staging<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="when"><a href="#when" class="headerlink" title="when"></a><strong>when</strong></h4><p><code>when</code> 关键字用于实现在作业失败时或发生故障时运行的作业</p><p><strong><code>when</code>可以设置的值：</strong></p><ul><li><code>on_success</code> ：只有前面的阶段的所有作业都成功时才执行，这是默认值。</li><li><code>on_failure</code> ：当前面阶段的作业至少有一个失败时才执行。</li><li><code>always</code> : 无论前面的作业是否成功，一直执行本作业。</li><li><code>manual</code> ：手动执行作业，作业不会自动执行，需要人工手动点击启动作业。</li><li><code>delayed</code> : 延迟执行作业，配合 <code>start_in</code> 关键字一起作用， <code>start_in</code> 设置的值必须小于或等于1小时，<code>start_in</code> 设置的值示例： <code>10 seconds</code> 、 <code>30 minutes</code> 、 <code>1 hour</code> ，前面的作业结束时计时器马上开始计时。</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 延时处理</span><span class="token key atrule">timed rollout 10%</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span> echo 'Rolling out 10% <span class="token punctuation">...</span>'  <span class="token key atrule">when</span><span class="token punctuation">:</span> delayed  <span class="token key atrule">start_in</span><span class="token punctuation">:</span> 30 minutes<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="environment"><a href="#environment" class="headerlink" title="environment"></a><strong>environment</strong></h4><p><code>environment</code> 用于定义作业部署到特殊的环境中。如果指定了 <code>environment</code> ，并且在 <code>运维</code> –&gt; <code>环境</code> 界面的环境列表中没有该名称下的环境，则会自动创建新环境。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">deploy to production</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span> git push production HEAD<span class="token punctuation">:</span>master  <span class="token key atrule">environment</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> production<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><p><code>environment:name</code></p><ul><li>推荐使用 <code>name</code> 关键字来定义环境的名称</li><li><code>name</code> 关键字的参数可以使用任何定义的CI变量，包括预定义的变量、安全变量、以及 <code>.gitlab-ci.yml</code> 配置文件中定义的变量，但不能使用 <code>script</code> 中定义的变量(因为这里面的变量是局部变量)。</li><li><code>environment</code> 环境的名称可以包含：英文字母(letters)、数字(digits)、空格(space)、_、/、$、{、}等。</li></ul></li><li><p><code>environment:url</code></p><ul><li><code>environment:url</code> 是可选的，用于设置环境的URL地址的按钮，通过点击按钮可以访问环境相应的URL地址。</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 如果作业都成功完成，那么会在 评审请求 和 环境部署 页面创建一个Button按钮，</span><span class="token comment"># 你点击 打开运行中的环境 按钮就可以访问环境对应的URL地址 https://prod.example.com 。</span><span class="token key atrule">deploy to production</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span> git push production HEAD<span class="token punctuation">:</span>master  <span class="token key atrule">environment</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> production    <span class="token key atrule">url</span><span class="token punctuation">:</span> https<span class="token punctuation">:</span>//prod.example.com<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>environment:on_stop和environment:action</code></p><ul><li><code>environment:on_stop</code> 与 <code>environment:action</code> 配合使用。</li><li>可以通过 <code>environment:on_stop</code> 关键字定义一个关闭(停止)环境的作业。</li><li><code>action</code> 关键字在关闭环境的作业中定义。</li></ul></li><li><p>动态环境</p><p>在环境的名称中使用变量，在 <code>environment:name</code> 和 <code>environment:url</code> 中使用变量，则可以达到动态环境的目的，动态环境需要底层应用的支持。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">deploy as review app</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span> make deploy  <span class="token key atrule">environment</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> review/$<span class="token punctuation">{</span>CI_COMMIT_REF_NAME<span class="token punctuation">}</span>    <span class="token key atrule">url</span><span class="token punctuation">:</span> https<span class="token punctuation">:</span>//$<span class="token punctuation">{</span>CI_ENVIRONMENT_SLUG<span class="token punctuation">}</span>.example.com/<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h4 id="cache"><a href="#cache" class="headerlink" title="cache"></a><strong>cache</strong></h4><ul><li>可以在全局设置或者每个作业中设置。</li><li>可以在不同的的流水线或作业之间共享数据。</li><li>在 <code>artifacts</code> 工件之前恢复缓存。</li><li><code>cache</code> 缓存机制用于指定一系列的文件或文件夹在不同的流水线或作业之间共享数据，仅能使用项目工作空间( <code>project workspace</code> )中的路径作为缓存的路径。</li><li>如果 <code>cache</code> 配置的路径是作业工作空间外部，则说明配置是全局的缓存，所有作业共享。</li></ul><h4 id="artifacts"><a href="#artifacts" class="headerlink" title="artifacts"></a><strong>artifacts</strong></h4><ul><li><p><code>artifacts</code> 用于指定在作业成功、失败、或者一直等状态下时，一系列的文件或文件夹附加到作业中。</p></li><li><p>作业完成后，工件被发送到GitLab，可以在GitLab Web界面下载。</p></li><li><p>默认情况下，只有成功的作业才会生成工件。</p></li><li><p>并不是所有的 <code>executor</code> 执行器都支持工件。</p></li><li><p><code>artifacts:paths</code> 用于指定哪些文件或文件夹会被打包成工件，仅仅项目工作空间( <code>project workspace</code> )的路径可以使用。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 将目录 binaries/ 和文件 .config 打包成工件</span><span class="token key atrule">artifacts</span><span class="token punctuation">:</span>  <span class="token key atrule">paths</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> binaries/    <span class="token punctuation">-</span> .config<span class="token comment"># 要禁用工件传递，请使用空依赖关系定义作业</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> build  <span class="token key atrule">script</span><span class="token punctuation">:</span> make build  <span class="token key atrule">dependencies</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token comment"># 仅为打标记的release发布版本创建工作，这样可以避免临时构建产生大量的存储需求</span><span class="token key atrule">default-job</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> mvn test <span class="token punctuation">-</span>U  <span class="token key atrule">except</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> tags<span class="token key atrule">release-job</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> mvn package <span class="token punctuation">-</span>U  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> target/<span class="token important">*.war</span>  <span class="token key atrule">only</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> tags<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>工件的默认名称是 <code>artifacts</code> ，当下载时名称是 <code>artifacts.zip</code></p></li><li><p>通过 <code>artifacts:name</code> 关键字可以自定义工件的归档名称，这样你可以为每个工件设置独一无二的名称，归档名称可以使用预定义的变量。</p></li><li><p>如果分支名称中包含斜杠(比如 <code>feature/my-feature</code> )，推荐使用 <code>$CI_COMMIT_REF_SLUG</code> 代替 <code>$CI_COMMIT_REF_NAME</code> 作为工件名称。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 使用作业名称使用工件名称</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> <span class="token string">"$CI_JOB_NAME"</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span class="token comment"># 使用当前分支或tag版本标签名作为工件名称</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> <span class="token string">"$CI_COMMIT_REF_NAME"</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span class="token comment"># 使用当前作业名称以及当前分支或tag版本标签名作为工件名称</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> <span class="token string">"$CI_JOB_NAME-$CI_COMMIT_REF_NAME"</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span class="token comment"># 同时使用当前作业阶段名称以及当前分支名称作为工件名称</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">name</span><span class="token punctuation">:</span> <span class="token string">"$CI_JOB_STAGE-$CI_COMMIT_REF_NAME"</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>artifacts:untracked</code> 用于将git未加入版本库的文件作为工件文件。</p></li><li><p><code>artifacts:untracked</code> 将会忽略配置文件 <code>.gitignore</code>。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 将所有的未跟踪文件打包成工件</span><span class="token key atrule">artifacts</span><span class="token punctuation">:</span>  <span class="token key atrule">untracked</span><span class="token punctuation">:</span> <span class="token boolean important">true</span><span class="token comment"># 将所有的未跟踪文件以及目录 binaries 中文件打包成工件</span><span class="token key atrule">artifacts</span><span class="token punctuation">:</span>  <span class="token key atrule">untracked</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token key atrule">paths</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> binaries/<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>artifacts:when</code> 用于在作业失败时或者忽略失败时上传工件。</p><p><strong>可用值：</strong></p><ul><li><code>on_success</code> ，默认值，当作业成功上传工件。</li><li><code>on_failure</code> ，当作业失败上传工件。</li><li><code>always</code> ，无论作业是否成功一直上传工件。</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">when</span><span class="token punctuation">:</span> on_failure<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p><code>artifacts:expire_in</code> 用于设置工件的过期时间。</p></li><li><p>你可以点击界面上的 <code>Keep</code> 保持按钮，永久保存工件。</p></li><li><p>工件到期后，默认情况下每小时删除一次工件(通过cron作业)，并且后续不能再访问该工件。</p></li><li><p>工件默认有效期是30天，可以通过 <code>Admin area</code> –&gt; <code>Settings</code> –&gt; <code>Continuous Integration and Deployment</code> 设置默认的有效性时间。</p></li><li><p>如果你不提供时间单位的话，工作有效性的时间是以秒为单位的时间</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 时间示例</span><span class="token comment"># ‘42’</span><span class="token comment"># ‘3 mins 4 sec’</span><span class="token comment"># ‘2 hrs 20 min’</span><span class="token comment"># ‘2h20min’</span><span class="token comment"># ‘6 mos 1 day’</span><span class="token comment"># ‘47 yrs 6 mos and 4d’</span><span class="token comment"># ‘3 weeks and 2 days’</span><span class="token key atrule">job</span><span class="token punctuation">:</span>  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">expire_in</span><span class="token punctuation">:</span> 1 week<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>artifacts:reports</code> 用于收集测试报告(report)，并在GitLab UI界面中显示出来。</p></li><li><p>无论作业是否成功，都会收集测试报告。</p></li><li><p>可以通过设置工件的打包路径 <code>artifacts:paths</code> 添加测试的报告输出文件。</p></li><li><p><code>artifacts:reports:junit</code> 可以用来收集单元测试的报告</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">rspec</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> bundle install  <span class="token punctuation">-</span> rspec <span class="token punctuation">-</span><span class="token punctuation">-</span>format RspecJunitFormatter <span class="token punctuation">-</span><span class="token punctuation">-</span>out rspec.xml  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">reports</span><span class="token punctuation">:</span>      <span class="token key atrule">junit</span><span class="token punctuation">:</span> rspec.xml<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>如果你的测试报告是多个XML文件，你可以在一个作业中指定多个单元测试报告，GitLab会自动将他们转换成一个文件，可以像下面这样表示报告的路径:</p><ul><li>文件匹配模式: <code>junit: rspec-*.xml</code></li><li>文件列表: <code>junit: [rspec-1.xml, rspec-2.xml, rspec-3.xml]</code></li><li>混合模式：<code>junit: [rspec.xml, test-results/TEST-*.xml]</code></li></ul></blockquote></li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">golang</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> go get <span class="token punctuation">-</span>u github.com/jstemmer/go<span class="token punctuation">-</span>junit<span class="token punctuation">-</span>report  <span class="token punctuation">-</span> go test <span class="token punctuation">-</span>v 2<span class="token punctuation">&gt;</span><span class="token important">&amp;1</span> <span class="token punctuation">|</span> go<span class="token punctuation">-</span>junit<span class="token punctuation">-</span>report <span class="token punctuation">&gt;</span> report.xml  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">reports</span><span class="token punctuation">:</span>      <span class="token key atrule">junit</span><span class="token punctuation">:</span> report.xml<span class="token comment"># 如果GoogleTest需要运行在多个平台(如 x86 、 x64 、arm )，需要为每种平台设置唯一的报告名称，最后将结果汇总起来。</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="dependencies"><a href="#dependencies" class="headerlink" title="dependencies"></a><strong>dependencies</strong></h4><ul><li><p><code>dependencies</code> 依赖关键字应该与 <code>artifacts</code> 工件关键字联合使用，允许你在不同作业间传递工件。</p></li><li><p>默认情况下，会传递所有本作业之前阶段的所有工件。</p></li><li><p>需要在作业上下文中定义 <code>dependencies</code> 依赖关键字，并指出所有需要使用的前序工件的作业名称列表。 <strong>作业列表中不能使用该作业后的作业名称</strong> 。</p></li><li><p>定义空的依赖项，将下不会下载任何工件。</p></li><li><p>使用依赖项不会考虑前面作业的运行状态。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">build:osx</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> build  <span class="token key atrule">script</span><span class="token punctuation">:</span> make build<span class="token punctuation">:</span>osx  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span class="token key atrule">build:linux</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> build  <span class="token key atrule">script</span><span class="token punctuation">:</span> make build<span class="token punctuation">:</span>linux  <span class="token key atrule">artifacts</span><span class="token punctuation">:</span>    <span class="token key atrule">paths</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> binaries/<span class="token key atrule">test:osx</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span> make test<span class="token punctuation">:</span>osx  <span class="token key atrule">dependencies</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> build<span class="token punctuation">:</span>osx<span class="token key atrule">test:linux</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span> make test<span class="token punctuation">:</span>linux  <span class="token key atrule">dependencies</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> build<span class="token punctuation">:</span>linux<span class="token key atrule">deploy</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> deploy  <span class="token key atrule">script</span><span class="token punctuation">:</span> make deploy<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h4 id="coverage"><a href="#coverage" class="headerlink" title="coverage"></a><strong>coverage</strong></h4><ul><li><p><code>coverage</code> 可以从作业的输出log中提取代码覆盖率。</p></li><li><p>仅支持正则表达式方式获取覆盖率。</p></li><li><p>字符串的前后必须使用/包含来表明一个正确的正则表达式规则。特殊字符串需要转义。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">job1</span><span class="token punctuation">:</span>  <span class="token key atrule">coverage</span><span class="token punctuation">:</span> <span class="token string">'/Code coverage:\d+\.\d+%/'</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li></ul><h4 id="retry"><a href="#retry" class="headerlink" title="retry"></a><strong>retry</strong></h4><ul><li><p><code>retry</code> 重试关键字用于配置当作业失败时可以重新执行的次数。</p></li><li><p>当作业失败时，如果配置了 <code>retry</code> ，那么该作业就会重试，直到允许的最大次数。</p></li><li><p>如果 <code>retry</code> 设置值为2，如果第一次重试运行成功了，那么就不会进行第二次重试。</p></li><li><p><code>retry</code> 设置值只能是0、1、2三个整数。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">test</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> rspec  <span class="token key atrule">retry</span><span class="token punctuation">:</span> <span class="token number">2</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p>为了更好的控制重试次数，<code>retry</code> 可以设置以下两个关键字：</p><ul><li><code>max</code> : 最大重试次数</li><li><code>when</code> : 何时重试</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 只有当运行器系统出现故障时才能最多重试两次</span><span class="token key atrule">test</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> rspec  <span class="token key atrule">retry</span><span class="token punctuation">:</span>    <span class="token key atrule">max</span><span class="token punctuation">:</span> <span class="token number">2</span>    <span class="token key atrule">when</span><span class="token punctuation">:</span> runner_system_failure<span class="token comment"># 为了针对多种重试情形，我们可以使用矩阵形式罗列出错误情形</span><span class="token key atrule">test</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span> rspec  <span class="token key atrule">retry</span><span class="token punctuation">:</span>    <span class="token key atrule">max</span><span class="token punctuation">:</span> <span class="token number">2</span>    <span class="token key atrule">when</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> runner_system_failure      <span class="token punctuation">-</span> stuck_or_timeout_failure<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>when的值：</strong></p><ul><li><code>always</code> : 一直重试，默认值。</li><li><code>unknown_failure</code> ：当错误未知时重试。</li><li><code>script_failure</code> ： 脚本错误时重试。</li><li><code>api_failure</code> ： API调用错误时重试。</li><li><code>stuck_or_timeout_failure</code> ： 作业卡信或超时错误时重试。</li><li><code>runner_system_failure</code> ： 运行器系统错误(如设置工作失败)时重试。</li><li><code>missing_dependency_failure</code> ： 依赖工件丢失错误时重试。</li><li><code>runner_unsupported</code> ： 运行器不支持错误时重试。</li></ul></li></ul><h4 id="parallel"><a href="#parallel" class="headerlink" title="parallel"></a><strong>parallel</strong></h4><h4 id="include"><a href="#include" class="headerlink" title="include"></a>include</h4><p><code>include</code> 包含关键字可以将其他yaml文件载入到当前的 <code>.gitlab-ci.yml</code> 配置文件中</p><h4 id="trigger"><a href="#trigger" class="headerlink" title="trigger"></a><strong>trigger</strong></h4><p><code>trigger</code> 关键字用于多项目流水线时，定义下游的流水线工程，由于社区版本不支持此功能，不详细介绍。</p><h4 id="extends"><a href="#extends" class="headerlink" title="extends"></a><strong>extends</strong></h4><ul><li><code>extends</code> 扩展用于定义当前作业从哪里继承。</li><li>它是使用YAML锚点的替代方案，更加灵活、可读性强。</li></ul><h4 id="pages"><a href="#pages" class="headerlink" title="pages"></a>pages</h4><p><code>pages</code> 是一项特殊工作，用于将静态内容上传到GitLab，可用于为您的网站提供服务。</p><h4 id="variables"><a href="#variables" class="headerlink" title="variables"></a><strong>variables</strong></h4><ul><li><p>在 <code>.gitlab-ci.yml</code> 配置文件中可以通过 <code>variables</code> 关键字配置全局变量或者作业级的局部变量。</p></li><li><p>当 <code>variables</code> 关键字使用在作业层级时，它会覆盖全局变量或预定义变量。</p></li><li><p>可以在 <code>variables</code> 关键字中定义非敏感性配置。</p></li><li><p>全局变量可以在各个作业中作业，而作业级别的局部变量只能在该作业中使用。</p></li><li><p>可以在GitLab WEB界面定义一些敏感性配置变量，或者可能变动的变量。</p></li><li><p>在 <code>script</code> 中使用 <code>export</code> 可以导出当前可用的变量信息。</p></li><li><p>作业内部修改全局变量只对当前作用生效，不会影响其他作业。</p></li><li><p>可以使用赋值语句对全局变量或局部变量进行重新赋值。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">variables</span><span class="token punctuation">:</span>  <span class="token key atrule">DATABASE_URL</span><span class="token punctuation">:</span> <span class="token string">"postgres://postgres@postgres/my_database"</span><span class="token key atrule">test1</span><span class="token punctuation">:</span>  <span class="token key atrule">stage</span><span class="token punctuation">:</span> test  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> echo <span class="token punctuation">-</span>e "$<span class="token punctuation">{</span>DATABASE_URL<span class="token punctuation">}</span>"<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><blockquote><p>变量的优先顺序是（从最高到最低）：</p><p>  触发变量或预定的流水线变量。<br>  项目级别变量或受保护变量。<br>  组级别变量或受保护变量。<br>  YAML定义的作业级变量。<br>  YAML定义的全局变量。<br>  部署环境变量。<br>  预定义的环境变量。</p><ul><li>不要将敏感信息，如用户密码、Token等信息放在 <code>.gitlab-ci.yml</code> 配置文件中定义变量。</li><li>敏感信息可以WEB配置界面添加变量，并将变量设置为 <code>Protected受保护</code> 或者 <code>Masked</code> ，设置为 <code>Masked</code> 的变量不会直接显示在作业日志信息中。</li></ul></blockquote><p><strong>关闭全局层级定义的变量</strong></p><p>如果你要在作业层级关闭全局层级定义的变量，可以给 <code>variables</code> 关键字定义一个空的 <code>hash</code></p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">job_name</span><span class="token punctuation">:</span>  <span class="token key atrule">variables</span><span class="token punctuation">:</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p><strong>变量定义时使用其他变量</strong></p><p>可以在变量定义时，使用其他的变量，需要使用 <code>$$</code> 进行转义。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">variables</span><span class="token punctuation">:</span>  <span class="token key atrule">LS_CMD</span><span class="token punctuation">:</span> <span class="token string">'ls $FLAGS $$TMP_DIR'</span>  <span class="token key atrule">FLAGS</span><span class="token punctuation">:</span> <span class="token string">'-al'</span><span class="token key atrule">script</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span class="token string">'eval $LS_CMD'</span>  <span class="token comment"># will execute 'ls -al $TMP_DIR'</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>克隆策略</strong></p><ul><li>你可以通过设置 <code>GIT_STRATEGY</code> 变量来指定GitLab Runner运行作业时使用什么样的方式来获取最新的代码，可以在全局级或作业级进行设置。</li><li><code>GIT_STRATEGY</code> 变量可以设置为 <code>fetch</code> 、<code>clone</code> 、<code>none</code> 。</li><li><code>clone</code> 是最慢的方式，这种方式会从头开始克隆仓库。</li><li><code>fetch</code> 相对来说更快一点，如果本地项目空间中存在远程仓库的克隆，只用从远程获取最新到本地，而不用从头开始克隆整个仓库(如果本地项目空间不存在远程仓库的克隆的话，则此时等同于clone，从头开始克隆远程仓库)。</li><li><code>none</code> 也是利用本地项目空间中的文件，但不会从远程获取到最新的修改数据，本地数据不是最新的。</li></ul><p><strong>子模块策略</strong></p><ul><li><code>GIT_SUBMODULE_STRATEGY</code> 类似于 <code>GIT_STRATEGY</code> ，当你的项目需要包含别的项目代码时，可以将别的项目作为你的项目的子模块，这个时候就可以使用 <code>GIT_SUBMODULE_STRATEGY</code> 。</li><li><code>GIT_SUBMODULE_STRATEGY</code> 默认取值 <code>none</code> ，即拉取代码时，子模块不会被引入。</li><li><code>GIT_SUBMODULE_STRATEGY</code> 可取值 <code>normal</code> ，意味着在只有顶级子模块会被引入。</li><li><code>GIT_SUBMODULE_STRATEGY</code> 可取值 <code>recursive</code> ，递归的意思，意味着所有级子模块会被引入。</li></ul><p><strong>检出分支策略</strong></p><ul><li><p>当 <code>GIT_STRATEGY</code> 设置为 <code>fetch</code> 或者 <code>clone</code> 时，可以通过 <code>GIT_CHECKOUT</code> 变量设置是否需要做 <code>git checkout</code> 操作，如果未指定该参数，默认值为 <code>true</code> ，即需要做 <code>git checkout</code> 操作。</p></li><li><p>可以在全局级或作业级进行设置。</p></li><li><p>如果 <code>GIT_CHECKOUT</code> 变量设置为 <code>true</code> ，GitLab Runner都会将本地工作副本检出并切换到当前流水线相关的修订版本分支上。</p></li><li><p>如果 <code>GIT_CHECKOUT</code> 变量设置为 <code>false</code> ，那么运行器操作如下：</p><ul><li><p><code>fetch</code> 操作时，更新仓库并在当前版本上保留工作副本。</p></li><li><p><code>clone</code> 操作时，克隆仓库并在默认分支中保留工作副本。</p></li></ul></li></ul><p><strong>清理工作</strong></p><ul><li>可以使用 <code>GIT_CLEAN_FLAGS</code> 变量来控制在检出源码后 <code>git clean</code> 的默认行为，可以在全局级或作业级进行设置。</li><li><code>GIT_CLEAN_FLAGS</code> 变量接受 <code>git clean</code> 命令的所有参数。</li><li>如果指定了 <code>GIT_CHECKOUT: "false"</code> ，那么 <code>git clean</code> 将不可用。</li><li><code>git-clean</code> : Remove untracked files from the working tree，即删除未跟踪的文件。</li><li><code>git clean</code> 命令用来从你的工作目录中删除所有没有tracked过的文件， <code>git reset --hard</code> 用于删除跟踪文件的修改记录。</li><li><code>git clean -n</code> 命令列出将被删除的文件。</li><li><code>git clean -f</code> 命令删除当前目录下所有没有跟踪过的文件。</li><li><code>git clean -d</code> 命令删除当前目录下所有没有跟踪过的目录。</li><li><code>git clean -e &lt;pattern&gt;</code> 命令排除( <code>--exclude=&lt;pattern&gt;</code> ) 某文件或目录，即不删除模式匹配的文件。</li><li><code>git clean -ffxd</code> 命令删除当前目录(包括由其他git仓库管理的子目录)下所有没有跟踪过的目录和文件。</li><li><code>GIT_CLEAN_FLAGS</code> 变量未指定时， <code>git clean</code> 命令的参数是 <code>-ffxd</code> 。</li><li><code>GIT_CLEAN_FLAGS</code> 变量指定为 <code>none</code> 时， <code>git clean</code> 命令不会执行。</li></ul><p><strong>作业重试次数</strong></p><ul><li>您可以设置正在运行的作业尝试执行以下每个阶段的尝试次数。可以在全局级或作业级进行设置。</li><li>涉及三个变量 <code>GET_SOURCES_ATTEMPTS</code> 、 <code>ARTIFACT_DOWNLOAD_ATTEMPTS</code> 、 <code>RESTORE_CACHE_ATTEMPTS</code> 。</li><li><code>GET_SOURCES_ATTEMPTS</code> 变量设置获取源码的尝试次数。</li><li><code>ARTIFACT_DOWNLOAD_ATTEMPTS</code> 变量设置下载归档文件的尝试次数。</li><li><code>RESTORE_CACHE_ATTEMPTS</code> 变量设置重建缓存的尝试次数。</li><li>默认是1次尝试。</li></ul><p><strong>处理并发</strong></p><ul><li><p>当执行器配置并发数 <code>concurrent</code> 大于1时，有可能导致作业运行失败，因为有可能多个作业都运行在相同的目录上，GitLab Runner运行器并不会去阻止这种情形，管理员和开发人员必须遵守Runner配置的要求。</p></li><li><p>要避免这种情况，您可以在 <code>$CI_BUILDS_DIR</code> 中使用唯一路径，因为Runner公开了另外两个提供唯一并发ID的变量：</p><ul><li><code>$CI_CONCURRENT_ID</code> ：给定执行程序中运行的所有作业的唯一ID。</li><li><code>$CI_CONCURRENT_PROJECT_ID</code> ：在给定执行程序和项目中运行的所有作业的唯一ID。</li></ul></li><li><p>在任何场景和任何执行器中都应该运行良好的最稳定的配置是在 <code>GIT_CLONE_PATH</code> 中使用 <code>$CI_CONCURRENT_ID</code> 。</p></li></ul><p><strong>特殊yaml功能</strong></p><p>可以使用特殊的YAML功能，如锚点( <code>＆</code> )，别名( <code>*</code> )和合并( <code>&lt;&lt;</code> )，这将使您大大降低 <code>.gitlab-ci.yml</code> 的复杂性。</p><ul><li><p>隐藏关键字和作业</p><p>如果我们想暂时禁用某个作业，我们可以将该作业的所有行都注释掉，更好的方法是，我们在作业名称前面增加一个点号( <code>.</code> )， 这样GitLab CI流水线就会自动处理忽略掉 <code>.hidden_job</code> 作业</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">hidden_job</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> run test<span class="token key atrule">.hidden_job</span><span class="token punctuation">:</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> run test<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>锚点</p><p>锚点可以让你容易复制文档内容，锚点可以用来复制或继承某些属性，锚点与隐藏作业一起使用可提供作业模板。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">.job_template</span><span class="token punctuation">:</span> <span class="token important">&amp;job_definition</span>  <span class="token comment"># Hidden key that defines an anchor named 'job_definition'</span>  <span class="token key atrule">image</span><span class="token punctuation">:</span> ruby<span class="token punctuation">:</span><span class="token number">2.1</span>  <span class="token key atrule">services</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> postgres    <span class="token punctuation">-</span> redis<span class="token key atrule">test1</span><span class="token punctuation">:</span>  <span class="token key atrule">&lt;&lt;</span><span class="token punctuation">:</span> <span class="token important">*job_definition</span>           <span class="token comment"># Merge the contents of the 'job_definition' alias</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> test1 project<span class="token key atrule">test2</span><span class="token punctuation">:</span>  <span class="token key atrule">&lt;&lt;</span><span class="token punctuation">:</span> <span class="token important">*job_definition</span>           <span class="token comment"># Merge the contents of the 'job_definition' alias</span>  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> test2 project<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><code>&amp;</code> 用于设置锚点名称为 <code>job_definition</code> ，也就是给隐藏作业设置一个锚点 <code>job_definition</code> 。</li><li><code>&lt;&lt;</code> 合并，将锚点定义的模板内容复制到当前作业的当前位置来。</li><li><code>*</code> 包含锚点的名称 <code>job_definition</code>。</li></ul></li></ul><p>​    </p>]]></content>
      
      
      <categories>
          
          <category> gitlab </category>
          
      </categories>
      
      
        <tags>
            
            <tag> gitlab ci </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【SHELL】shell代码规范</title>
      <link href="/2021/10/29/linux/shell/shell-shell-dai-ma-gui-fan/"/>
      <url>/2021/10/29/linux/shell/shell-shell-dai-ma-gui-fan/</url>
      
        <content type="html"><![CDATA[<h1 id="【SHELL】shell代码规范"><a href="#【SHELL】shell代码规范" class="headerlink" title="【SHELL】shell代码规范"></a>【SHELL】shell代码规范</h1><h3 id="使用哪一种Shell"><a href="#使用哪一种Shell" class="headerlink" title="使用哪一种Shell"></a>使用哪一种Shell</h3><blockquote><p>Bash是唯一被允许执行的shell脚本语言。</p></blockquote><p>可执行文件必须以<code>#!/bin/bash</code>和最小数量的标志开始。请使用 set 来设置shell的选项，使得用<code>bash &lt;script_name&gt;</code>调用你的脚本时不会破坏其功能。限制所有的可执行shell脚本为bash使得我们安装在所有计算机中的shell语言保持一致性。</p><h3 id="什么时候使用Shell"><a href="#什么时候使用Shell" class="headerlink" title="什么时候使用Shell"></a>什么时候使用Shell</h3><blockquote><p>Shell应该仅仅被用于小功能或者简单的包装脚本。</p></blockquote><p>尽管Shell脚本不是一种开发语言，但在整个谷歌它被用于编写多种实用工具的脚本。这个风格指南更多的是认同它的使用，而不是一个建议，即它可被用于广泛部署。</p><p>以下是一些准则：</p><ul><li>如果你主要是在调用其他的工具并且做一些相对很小数据量的操作，那么使用shell来完成任务是一种可接受的选择。</li><li>如果你在乎性能，那么请选择其他工具，而不是使用shell。</li><li>如果你发现你需要使用数据而不是变量赋值（如 ${PHPESTATUS} ），那么你应该使用Python脚本。</li><li>如果你将要编写的脚本会超过100行，那么你可能应该使用Python来编写，而不是Shell。请记住，当脚本行数增加，尽早使用另外一种语言重写你的脚本，以避免之后花更多的时间来重写。</li></ul><h2 id="Shell文件和解释器调用"><a href="#Shell文件和解释器调用" class="headerlink" title="Shell文件和解释器调用"></a>Shell文件和解释器调用</h2><h3 id="文件扩展名"><a href="#文件扩展名" class="headerlink" title="文件扩展名"></a>文件扩展名</h3><blockquote><p>可执行文件应该没有扩展名（强烈建议）或者使用.sh扩展名。库文件必须使用.sh作为扩展名，而且应该是不可执行的。</p></blockquote><p>当执行一个程序时，并不需要知道它是用什么语言编写的。而且shell脚本也不要求有扩展名。所以我们更喜欢可执行文件没有扩展名。</p><p>然而，对于库文件，知道其用什么语言编写的是很重要的，有时候会需要使用不同语言编写的相似的库文件。使用.sh这样特定语言后缀作为扩展名，就使得用不同语言编写的具有相同功能的库文件可以采用一样的名称。</p><h3 id="SUID-SGID"><a href="#SUID-SGID" class="headerlink" title="SUID / SGID"></a>SUID / SGID</h3><blockquote><p>SUID(Set User ID)和SGID(Set Group ID)在shell脚本中是被禁止的。</p></blockquote><p>shell存在太多的安全问题，以致于如果允许SUID/SGID会使得shell几乎不可能足够安全。虽然bash使得运行SUID非常困难，但在某些平台上仍然有可能运行，这就是为什么我们明确提出要禁止它。</p><p>如果你需要较高权限的访问请使用<code>sudo</code>。</p><h2 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h2><h3 id="STDOUT-vs-STDERR"><a href="#STDOUT-vs-STDERR" class="headerlink" title="STDOUT vs STDERR"></a>STDOUT vs STDERR</h3><blockquote><p>所有的错误信息都应该被导向STDERR。</p></blockquote><p>这使得从实际问题中分离出正常状态变得更容易。</p><p>推荐使用类似如下函数，将错误信息和其他状态信息一起打印出来。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function-name function">err</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>    <span class="token builtin class-name">echo</span> <span class="token string">"[<span class="token variable"><span class="token variable">$(</span><span class="token function">date</span> +<span class="token string">'%Y-%m-%dT%H:%M:%S%z'</span><span class="token variable">)</span></span>]: <span class="token variable">$@</span>"</span> <span class="token operator">&gt;</span><span class="token file-descriptor important">&amp;2</span><span class="token punctuation">}</span><span class="token keyword">if</span> <span class="token operator">!</span> do_something<span class="token punctuation">;</span> <span class="token keyword">then</span>    err <span class="token string">"Unable to do_something"</span>    <span class="token builtin class-name">exit</span> <span class="token string">"<span class="token variable">${E_DID_NOTHING}</span>"</span><span class="token keyword">fi</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h2><h3 id="文件头"><a href="#文件头" class="headerlink" title="文件头"></a>文件头</h3><blockquote><p>每个文件的开头是其文件内容的描述。</p></blockquote><p>每个文件必须包含一个顶层注释，对其内容进行简要概述。版权声明和作者信息是可选的。</p><p>例如：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token shebang important">#!/bin/bash</span><span class="token comment">#</span><span class="token comment"># Perform hot backups of Oracle databases.</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h3 id="功能注释"><a href="#功能注释" class="headerlink" title="功能注释"></a>功能注释</h3><blockquote><p>任何不是既明显又短的函数都必须被注释。任何库函数无论其长短和复杂性都必须被注释。</p></blockquote><p>其他人通过阅读注释（和帮助信息，如果有的话）就能够学会如何使用你的程序或库函数，而不需要阅读代码。</p><p>所有的函数注释应该包含：</p><ul><li>函数的描述</li><li>全局变量的使用和修改</li><li>使用的参数说明</li><li>返回值，而不是上一条命令运行后默认的退出状态<br>例如：</li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token shebang important">#!/bin/bash</span><span class="token comment">#</span><span class="token comment"># Perform hot backups of Oracle databases.</span><span class="token builtin class-name">export</span> <span class="token assign-left variable"><span class="token environment constant">PATH</span></span><span class="token operator">=</span><span class="token string">'/usr/xpg4/bin:/usr/bin:/opt/csw/bin:/opt/goog/bin'</span><span class="token comment">#######################################</span><span class="token comment"># Cleanup files from the backup dir</span><span class="token comment"># Globals:</span><span class="token comment">#   BACKUP_DIR</span><span class="token comment">#   ORACLE_SID</span><span class="token comment"># Arguments:</span><span class="token comment">#   None</span><span class="token comment"># Returns:</span><span class="token comment">#   None</span><span class="token comment">#######################################</span><span class="token function-name function">cleanup</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>  <span class="token punctuation">..</span>.<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="实现部分的注释"><a href="#实现部分的注释" class="headerlink" title="实现部分的注释"></a>实现部分的注释</h3><blockquote><p>注释你代码中含有技巧、不明显、有趣的或者重要的部分。</p></blockquote><p>这部分遵循谷歌代码注释的通用做法。不要注释所有代码。如果有一个复杂的算法或者你正在做一些与众不同的，放一个简单的注释。</p><h3 id="TODO注释"><a href="#TODO注释" class="headerlink" title="TODO注释"></a>TODO注释</h3><blockquote><p>使用TODO注释临时的、短期解决方案的、或者足够好但不够完美的代码。</p></blockquote><p>这与C++指南中的约定相一致。</p><p>TODOs应该包含全部大写的字符串TODO，接着是括号中你的用户名。冒号是可选的。最好在TODO条目之后加上 bug或者ticket 的序号。</p><p>例如：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># TODO(mrmonkey): Handle the unlikely edge cases (bug ####)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="格式"><a href="#格式" class="headerlink" title="格式"></a>格式</h2><h3 id="缩进"><a href="#缩进" class="headerlink" title="缩进"></a>缩进</h3><blockquote><p>缩进两个空格，没有制表符。</p></blockquote><p>在代码块之间请使用空行以提升可读性。缩进为两个空格。无论你做什么，请不要使用制表符。对于已有文件，保持已有的缩进格式。</p><h3 id="行的长度和长字符串"><a href="#行的长度和长字符串" class="headerlink" title="行的长度和长字符串"></a>行的长度和长字符串</h3><blockquote><p>行的最大长度为80个字符。</p></blockquote><p>如果你必须写长度超过80个字符的字符串，如果可能的话，尽量使用here document或者嵌入的换行符。长度超过80个字符的文字串且不能被合理地分割，这是正常的。但强烈建议找到一个方法使其变短。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># DO use 'here document's</span><span class="token function">cat</span> <span class="token operator">&lt;&lt;</span>END<span class="token punctuation">;</span>I am an exceptionally longstring.END<span class="token comment"># Embedded newlines are ok too</span><span class="token assign-left variable">long_string</span><span class="token operator">=</span><span class="token string">"I am an exceptionally  long string."</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="管道"><a href="#管道" class="headerlink" title="管道"></a>管道</h3><blockquote><p>如果一行容不下整个管道操作，那么请将整个管道操作分割成每行一个管段。</p></blockquote><p>如果一行容得下整个管道操作，那么请将整个管道操作写在同一行。</p><p>否则，应该将整个管道操作分割成每行一个管段，管道操作的下一部分应该将管道符放在新行并且缩进2个空格。这适用于使用管道符’|’的合并命令链以及使用’||’和’&amp;&amp;’的逻辑运算链。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># All fits on one line</span>command1 <span class="token operator">|</span> command2<span class="token comment"># Long commands</span>command1 <span class="token punctuation">\</span>  <span class="token operator">|</span> command2 <span class="token punctuation">\</span>  <span class="token operator">|</span> command3 <span class="token punctuation">\</span>  <span class="token operator">|</span> command4<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="循环"><a href="#循环" class="headerlink" title="循环"></a>循环</h3><blockquote><p>请将<code>; do</code>,<code>; then</code>和<code>while</code>,<code>for</code>,<code>if</code>放在同一行。</p></blockquote><p>shell中的循环略有不同，但是我们遵循跟声明函数时的大括号相同的原则。也就是说， ; do , ; then 应该和 if/for/while 放在同一行。 else 应该单独一行，结束语句应该单独一行并且跟开始语句垂直对齐。</p><p>例如：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token keyword">for</span> <span class="token for-or-select variable">dir</span> <span class="token keyword">in</span> <span class="token variable">${dirs_to_cleanup}</span><span class="token punctuation">;</span> <span class="token keyword">do</span>  <span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> -d <span class="token string">"<span class="token variable">${dir}</span>/<span class="token variable">${ORACLE_SID}</span>"</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>    log_date <span class="token string">"Cleaning up old files in <span class="token variable">${dir}</span>/<span class="token variable">${ORACLE_SID}</span>"</span>    <span class="token function">rm</span> <span class="token string">"<span class="token variable">${dir}</span>/<span class="token variable">${ORACLE_SID}</span>/"</span>*    <span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"<span class="token variable">$?</span>"</span> -ne <span class="token number">0</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>      error_message    <span class="token keyword">fi</span>  <span class="token keyword">else</span>    <span class="token function">mkdir</span> -p <span class="token string">"<span class="token variable">${dir}</span>/<span class="token variable">${ORACLE_SID}</span>"</span>    <span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"<span class="token variable">$?</span>"</span> -ne <span class="token number">0</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>      error_message    <span class="token keyword">fi</span>  <span class="token keyword">fi</span><span class="token keyword">done</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="case语句"><a href="#case语句" class="headerlink" title="case语句"></a>case语句</h3><blockquote><ul><li>通过2个空格缩进可选项。</li><li>在同一行可选项的模式右圆括号之后和结束符 ;; 之前各需要一个空格。</li><li>长可选项或者多命令可选项应该被拆分成多行，模式、操作和结束符 ;; 在不同的行。</li></ul></blockquote><p>匹配表达式比 case 和 esac 缩进一级。多行操作要再缩进一级。一般情况下，不需要引用匹配表达式。模式表达式前面不应该出现左括号。避免使用 ;&amp; 和 ;;&amp; 符号。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token keyword">case</span> <span class="token string">"<span class="token variable">${expression}</span>"</span> <span class="token keyword">in</span>  a<span class="token punctuation">)</span>    <span class="token assign-left variable">variable</span><span class="token operator">=</span><span class="token string">"..."</span>    some_command <span class="token string">"<span class="token variable">${variable}</span>"</span> <span class="token string">"<span class="token variable">${other_expr}</span>"</span> <span class="token punctuation">..</span>.    <span class="token punctuation">;</span><span class="token punctuation">;</span>  absolute<span class="token punctuation">)</span>    <span class="token assign-left variable">actions</span><span class="token operator">=</span><span class="token string">"relative"</span>    another_command <span class="token string">"<span class="token variable">${actions}</span>"</span> <span class="token string">"<span class="token variable">${other_expr}</span>"</span> <span class="token punctuation">..</span>.    <span class="token punctuation">;</span><span class="token punctuation">;</span>  *<span class="token punctuation">)</span>    error <span class="token string">"Unexpected expression '<span class="token variable">${expression}</span>'"</span>    <span class="token punctuation">;</span><span class="token punctuation">;</span><span class="token keyword">esac</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>只要整个表达式可读，简单的命令可以跟模式和 ;; 写在同一行。这通常适用于单字母选项的处理。当单行容不下操作时，请将模式单独放一行，然后是操作，最后结束符 ;; 也单独一行。当操作在同一行时，模式的右括号之后和结束符 ;; 之前请使用一个空格分隔。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token assign-left variable">verbose</span><span class="token operator">=</span><span class="token string">'false'</span><span class="token assign-left variable">aflag</span><span class="token operator">=</span><span class="token string">''</span><span class="token assign-left variable">bflag</span><span class="token operator">=</span><span class="token string">''</span><span class="token assign-left variable">files</span><span class="token operator">=</span><span class="token string">''</span><span class="token keyword">while</span> <span class="token builtin class-name">getopts</span> <span class="token string">'abf:v'</span> flag<span class="token punctuation">;</span> <span class="token keyword">do</span>  <span class="token keyword">case</span> <span class="token string">"<span class="token variable">${flag}</span>"</span> <span class="token keyword">in</span>    a<span class="token punctuation">)</span> <span class="token assign-left variable">aflag</span><span class="token operator">=</span><span class="token string">'true'</span> <span class="token punctuation">;</span><span class="token punctuation">;</span>    b<span class="token punctuation">)</span> <span class="token assign-left variable">bflag</span><span class="token operator">=</span><span class="token string">'true'</span> <span class="token punctuation">;</span><span class="token punctuation">;</span>    f<span class="token punctuation">)</span> <span class="token assign-left variable">files</span><span class="token operator">=</span><span class="token string">"<span class="token variable">${OPTARG}</span>"</span> <span class="token punctuation">;</span><span class="token punctuation">;</span>    <span class="token function">v</span><span class="token punctuation">)</span> <span class="token assign-left variable">verbose</span><span class="token operator">=</span><span class="token string">'true'</span> <span class="token punctuation">;</span><span class="token punctuation">;</span>    *<span class="token punctuation">)</span> error <span class="token string">"Unexpected option <span class="token variable">${flag}</span>"</span> <span class="token punctuation">;</span><span class="token punctuation">;</span>  <span class="token keyword">esac</span><span class="token keyword">done</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="变量扩展"><a href="#变量扩展" class="headerlink" title="变量扩展"></a>变量扩展</h3><blockquote><p>按优先级顺序：保持跟你所发现的一致；引用你的变量；推荐用 ${var} 而不是 $var ，详细解释如下。</p></blockquote><p>这些仅仅是指南，因为作为强制规定似乎饱受争议。</p><p>以下按照优先顺序列出。</p><ol><li>与现存代码中你所发现的保持一致。</li><li>引用变量参阅下面一节，引用。</li><li>除非绝对必要或者为了避免深深的困惑，否则不要用大括号将单个字符的shell特殊变量或定位变量括起来。推荐将其他所有变量用大括号括起来。<pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># Section of recommended cases.</span><span class="token comment"># Preferred style for 'special' variables:</span><span class="token builtin class-name">echo</span> <span class="token string">"Positional: <span class="token variable">$1</span>"</span> <span class="token string">"<span class="token variable">$5</span>"</span> <span class="token string">"<span class="token variable">$3</span>"</span><span class="token builtin class-name">echo</span> <span class="token string">"Specials: !=<span class="token variable">$!</span>, -=$-, _=<span class="token variable">$_</span>. ?=<span class="token variable">$?</span>, #=<span class="token variable">$#</span> *=<span class="token variable">$*</span> @=<span class="token variable">$@</span> \$=<span class="token variable">$$</span> ..."</span><span class="token comment"># Braces necessary:</span><span class="token builtin class-name">echo</span> <span class="token string">"many parameters: <span class="token variable">${10}</span>"</span><span class="token comment"># Braces avoiding confusion:</span><span class="token comment"># Output is "a0b0c0"</span><span class="token builtin class-name">set</span> -- a b c<span class="token builtin class-name">echo</span> <span class="token string">"<span class="token variable">${1}</span>0<span class="token variable">${2}</span>0<span class="token variable">${3}</span>0"</span><span class="token comment"># Preferred style for other variables:</span><span class="token builtin class-name">echo</span> <span class="token string">"PATH=<span class="token variable">${<span class="token environment constant">PATH</span>}</span>, PWD=<span class="token variable">${<span class="token environment constant">PWD</span>}</span>, mine=<span class="token variable">${some_var}</span>"</span><span class="token keyword">while</span> <span class="token builtin class-name">read</span> f<span class="token punctuation">;</span> <span class="token keyword">do</span>  <span class="token builtin class-name">echo</span> <span class="token string">"file=<span class="token variable">${f}</span>"</span><span class="token keyword">done</span> <span class="token operator">&lt;</span> <span class="token operator">&lt;</span><span class="token punctuation">(</span><span class="token function">ls</span> -l /tmp<span class="token punctuation">)</span><span class="token comment"># Section of discouraged cases</span><span class="token comment"># Unquoted vars, unbraced vars, brace-quoted single letter</span><span class="token comment"># shell specials.</span><span class="token builtin class-name">echo</span> <span class="token assign-left variable">a</span><span class="token operator">=</span><span class="token variable">$avar</span> <span class="token string">"b=<span class="token variable">$bvar</span>"</span> <span class="token string">"PID=<span class="token variable">${$}</span>"</span> <span class="token string">"<span class="token variable">${1}</span>"</span><span class="token comment"># Confusing use: this is expanded as "${1}0${2}0${3}0",</span><span class="token comment"># not "${10}${20}${30}</span><span class="token builtin class-name">set</span> -- a b c<span class="token builtin class-name">echo</span> <span class="token string">"<span class="token variable">$10</span><span class="token variable">$20</span><span class="token variable">$30</span>"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h3></li></ol><blockquote><ol><li>除非需要小心不带引用的扩展，否则总是引用包含变量、命令替换符、空格或shell元字符的字符串。</li><li>推荐引用是单词的字符串（而不是命令选项或者路径名）。</li><li>千万不要引用整数。</li><li>注意 [[ 中模式匹配的引用规则。</li><li>请使用 $@ 除非你有特殊原因需要使用 $* 。</li></ol></blockquote><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># 'Single' quotes indicate that no substitution is desired.</span><span class="token comment"># "Double" quotes indicate that substitution is required/tolerated.</span><span class="token comment"># Simple examples</span><span class="token comment"># "quote command substitutions"</span><span class="token assign-left variable">flag</span><span class="token operator">=</span><span class="token string">"<span class="token variable"><span class="token variable">$(</span>some_command and its args <span class="token string">"<span class="token variable">$@</span>"</span> <span class="token string">'quoted separately'</span><span class="token variable">)</span></span>"</span><span class="token comment"># "quote variables"</span><span class="token builtin class-name">echo</span> <span class="token string">"<span class="token variable">${flag}</span>"</span><span class="token comment"># "never quote literal integers"</span><span class="token assign-left variable">value</span><span class="token operator">=</span><span class="token number">32</span><span class="token comment"># "quote command substitutions", even when you expect integers</span><span class="token assign-left variable">number</span><span class="token operator">=</span><span class="token string">"<span class="token variable"><span class="token variable">$(</span>generate_number<span class="token variable">)</span></span>"</span><span class="token comment"># "prefer quoting words", not compulsory</span><span class="token builtin class-name">readonly</span> <span class="token assign-left variable">USE_INTEGER</span><span class="token operator">=</span><span class="token string">'true'</span><span class="token comment"># "quote shell meta characters"</span><span class="token builtin class-name">echo</span> <span class="token string">'Hello stranger, and well met. Earn lots of $$$'</span><span class="token builtin class-name">echo</span> <span class="token string">"Process <span class="token variable">$$</span>: Done making \$\$\$."</span><span class="token comment"># "command options or path names"</span><span class="token comment"># ($1 is assumed to contain a value here)</span><span class="token function">grep</span> -li Hugo /dev/null <span class="token string">"<span class="token variable">$1</span>"</span><span class="token comment"># Less simple examples</span><span class="token comment"># "quote variables, unless proven false": ccs might be empty</span><span class="token function">git</span> send-email --to <span class="token string">"<span class="token variable">${reviewers}</span>"</span> <span class="token variable">${ccs<span class="token operator">:+</span>"--cc" "${ccs}</span>"<span class="token punctuation">}</span><span class="token comment"># Positional parameter precautions: $1 might be unset</span><span class="token comment"># Single quotes leave regex as-is.</span><span class="token function">grep</span> -cP <span class="token string">'([Ss]pecial|\|?characters*)$'</span> <span class="token variable">${1<span class="token operator">:+</span>"$1"}</span><span class="token comment"># For passing on arguments,</span><span class="token comment"># "$@" is right almost everytime, and</span><span class="token comment"># $* is wrong almost everytime:</span><span class="token comment">#</span><span class="token comment"># * $* and $@ will split on spaces, clobbering up arguments</span><span class="token comment">#   that contain spaces and dropping empty strings;</span><span class="token comment"># * "$@" will retain arguments as-is, so no args</span><span class="token comment">#   provided will result in no args being passed on;</span><span class="token comment">#   This is in most cases what you want to use for passing</span><span class="token comment">#   on arguments.</span><span class="token comment"># * "$*" expands to one argument, with all args joined</span><span class="token comment">#   by (usually) spaces,</span><span class="token comment">#   so no args provided will result in one empty string</span><span class="token comment">#   being passed on.</span><span class="token comment"># (Consult 'man bash' for the nit-grits ;-)</span><span class="token builtin class-name">set</span> -- <span class="token number">1</span> <span class="token string">"2 two"</span> <span class="token string">"3 three tres"</span><span class="token punctuation">;</span> <span class="token builtin class-name">echo</span> <span class="token variable">$#</span> <span class="token punctuation">;</span> <span class="token builtin class-name">set</span> -- <span class="token string">"<span class="token variable">$*</span>"</span><span class="token punctuation">;</span> <span class="token builtin class-name">echo</span> <span class="token string">"<span class="token variable">$#</span>, <span class="token variable">$@</span>"</span><span class="token punctuation">)</span><span class="token builtin class-name">set</span> -- <span class="token number">1</span> <span class="token string">"2 two"</span> <span class="token string">"3 three tres"</span><span class="token punctuation">;</span> <span class="token builtin class-name">echo</span> <span class="token variable">$#</span> <span class="token punctuation">;</span> <span class="token builtin class-name">set</span> -- <span class="token string">"<span class="token variable">$@</span>"</span><span class="token punctuation">;</span> <span class="token builtin class-name">echo</span> <span class="token string">"<span class="token variable">$#</span>, <span class="token variable">$@</span>"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="特性及错误"><a href="#特性及错误" class="headerlink" title="特性及错误"></a>特性及错误</h2><h3 id="命令替换"><a href="#命令替换" class="headerlink" title="命令替换"></a>命令替换</h3><blockquote><p>使用 $(command) 而不是反引号。</p></blockquote><p>嵌套的反引号要求用反斜杠转义内部的反引号。而 $(command) 形式嵌套时不需要改变，而且更易于阅读。</p><p>例如：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># This is preferred:</span><span class="token assign-left variable">var</span><span class="token operator">=</span><span class="token string">"$(command "<span class="token variable"><span class="token variable">$(</span>command1<span class="token variable">)</span></span>"</span><span class="token punctuation">)</span><span class="token string">"# This is not:var="</span><span class="token variable"><span class="token variable">`</span><span class="token builtin class-name">command</span> <span class="token punctuation">\</span><span class="token variable">`</span></span>command1<span class="token punctuation">\</span>``"<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h5 id="test，-和"><a href="#test，-和" class="headerlink" title="test，[和[["></a>test，[和[[</h5><blockquote><p>推荐使用 [[ … ]] ，而不是 [ , test , 和 /usr/bin/ [ 。</p></blockquote><p>因为在 [[ 和 ]] 之间不会有路径名称扩展或单词分割发生，所以使用 [[ … ]] 能够减少错误。而且 [[ … ]] 允许正则表达式匹配，而 [ … ] 不允许。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># This ensures the string on the left is made up of characters in the</span><span class="token comment"># alnum character class followed by the string name.</span><span class="token comment"># Note that the RHS should not be quoted here.</span><span class="token comment"># For the gory details, see</span><span class="token comment"># E14 at http://tiswww.case.edu/php/chet/bash/FAQ</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"filename"</span> <span class="token operator">=~</span> ^<span class="token punctuation">[</span><span class="token punctuation">[</span>:alnum:<span class="token punctuation">]</span><span class="token punctuation">]</span>+name <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  <span class="token builtin class-name">echo</span> <span class="token string">"Match"</span><span class="token keyword">fi</span><span class="token comment"># This matches the exact pattern "f*" (Does not match in this case)</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"filename"</span> <span class="token operator">==</span> <span class="token string">"f*"</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  <span class="token builtin class-name">echo</span> <span class="token string">"Match"</span><span class="token keyword">fi</span><span class="token comment"># This gives a "too many arguments" error as f* is expanded to the</span><span class="token comment"># contents of the current directory</span><span class="token keyword">if</span> <span class="token punctuation">[</span> <span class="token string">"filename"</span> <span class="token operator">==</span> f* <span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  <span class="token builtin class-name">echo</span> <span class="token string">"Match"</span><span class="token keyword">fi</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="测试字符串"><a href="#测试字符串" class="headerlink" title="测试字符串"></a>测试字符串</h3><blockquote><p>尽可能使用引用，而不是过滤字符串。</p></blockquote><p>Bash足以在测试中处理空字符串。所以，请使用空（非空）字符串测试，而不是过滤字符，使得代码更易于阅读。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># Do this:</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"<span class="token variable">${my_var}</span>"</span> <span class="token operator">=</span> <span class="token string">"some_string"</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  do_something<span class="token keyword">fi</span><span class="token comment"># -z (string length is zero) and -n (string length is not zero) are</span><span class="token comment"># preferred over testing for an empty string</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> -z <span class="token string">"<span class="token variable">${my_var}</span>"</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  do_something<span class="token keyword">fi</span><span class="token comment"># This is OK (ensure quotes on the empty side), but not preferred:</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"<span class="token variable">${my_var}</span>"</span> <span class="token operator">=</span> <span class="token string">""</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  do_something<span class="token keyword">fi</span><span class="token comment"># Not this:</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"<span class="token variable">${my_var}</span>X"</span> <span class="token operator">=</span> <span class="token string">"some_stringX"</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  do_something<span class="token keyword">fi</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>为了避免对你测试的目的产生困惑，请明确使用<code>-z</code>或者<code>-n</code></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># Use this</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> -n <span class="token string">"<span class="token variable">${my_var}</span>"</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  do_something<span class="token keyword">fi</span><span class="token comment"># Instead of this as errors can occur if ${my_var} expands to a test</span><span class="token comment"># flag</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"<span class="token variable">${my_var}</span>"</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  do_something<span class="token keyword">fi</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="文件名的通配符扩展"><a href="#文件名的通配符扩展" class="headerlink" title="文件名的通配符扩展"></a>文件名的通配符扩展</h3><blockquote><p>当进行文件名的通配符扩展时，请使用明确的路径。</p></blockquote><p>因为文件名可能以 - 开头，所以使用扩展通配符 ./* 比 * 来得安全得多。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># Here's the contents of the directory:</span><span class="token comment"># -f  -r  somedir  somefile</span><span class="token comment"># This deletes almost everything in the directory by force</span>psa@bilby$ <span class="token function">rm</span> -v *removed directory: <span class="token variable"><span class="token variable">`</span>somedir'removed <span class="token variable">`</span></span>somefile<span class="token string">'# As opposed to:psa@bilby$ rm -v ./*removed `./-f'</span>removed <span class="token variable"><span class="token variable">`</span>./-r'rm: cannot remove <span class="token variable">`</span></span>./somedir<span class="token string">': Is a directoryremoved `./somefile'</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="Eval"><a href="#Eval" class="headerlink" title="Eval"></a>Eval</h3><blockquote><p>应该避免使用eval。</p></blockquote><p>当用于给变量赋值时，Eval解析输入，并且能够设置变量，但无法检查这些变量是什么。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># What does this set?</span><span class="token comment"># Did it succeed? In part or whole?</span><span class="token builtin class-name">eval</span> <span class="token variable"><span class="token variable">$(</span>set_my_variables<span class="token variable">)</span></span><span class="token comment"># What happens if one of the returned values has a space in it?</span><span class="token assign-left variable">variable</span><span class="token operator">=</span><span class="token string">"<span class="token variable"><span class="token variable">$(</span><span class="token builtin class-name">eval</span> some_function<span class="token variable">)</span></span>"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="管道导向while循环"><a href="#管道导向while循环" class="headerlink" title="管道导向while循环"></a>管道导向while循环</h3><blockquote><p>请使用过程替换或者for循环，而不是管道导向while循环。在while循环中被修改的变量是不能传递给父shell的，因为循环命令是在一个子shell中运行的。</p></blockquote><p>管道导向while循环中的隐式子shell使得追踪bug变得很困难。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token assign-left variable">last_line</span><span class="token operator">=</span><span class="token string">'NULL'</span>your_command <span class="token operator">|</span> <span class="token keyword">while</span> <span class="token builtin class-name">read</span> line<span class="token punctuation">;</span> <span class="token keyword">do</span>  <span class="token assign-left variable">last_line</span><span class="token operator">=</span><span class="token string">"<span class="token variable">${line}</span>"</span><span class="token keyword">done</span><span class="token comment"># This will output 'NULL'</span><span class="token builtin class-name">echo</span> <span class="token string">"<span class="token variable">${last_line}</span>"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>如果你确定输入中不包含空格或者特殊符号（通常意味着不是用户输入的），那么可以使用一个for循环。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token assign-left variable">total</span><span class="token operator">=</span><span class="token number">0</span><span class="token comment"># Only do this if there are no spaces in return values.</span><span class="token keyword">for</span> <span class="token for-or-select variable">value</span> <span class="token keyword">in</span> <span class="token variable"><span class="token variable">$(</span><span class="token builtin class-name">command</span><span class="token variable">)</span></span><span class="token punctuation">;</span> <span class="token keyword">do</span>  <span class="token assign-left variable">total</span><span class="token operator">+=</span><span class="token string">"<span class="token variable">${value}</span>"</span><span class="token keyword">done</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>使用过程替换允许重定向输出，但是请将命令放入一个显式的子shell中，而不是bash为while循环创建的隐式子shell。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token assign-left variable">total</span><span class="token operator">=</span><span class="token number">0</span><span class="token assign-left variable">last_file</span><span class="token operator">=</span><span class="token keyword">while</span> <span class="token builtin class-name">read</span> count filename<span class="token punctuation">;</span> <span class="token keyword">do</span>  <span class="token assign-left variable">total</span><span class="token operator">+=</span><span class="token string">"<span class="token variable">${count}</span>"</span>  <span class="token assign-left variable">last_file</span><span class="token operator">=</span><span class="token string">"<span class="token variable">${filename}</span>"</span><span class="token keyword">done</span> <span class="token operator">&lt;</span> <span class="token operator">&lt;</span><span class="token punctuation">(</span>your_command <span class="token operator">|</span> <span class="token function">uniq</span> -c<span class="token punctuation">)</span><span class="token comment"># This will output the second field of the last line of output from</span><span class="token comment"># the command.</span><span class="token builtin class-name">echo</span> <span class="token string">"Total = <span class="token variable">${total}</span>"</span><span class="token builtin class-name">echo</span> <span class="token string">"Last one = <span class="token variable">${last_file}</span>"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当不需要传递复杂的结果给父shell时可以使用while循环。这通常需要一些更复杂的“解析”。请注意简单的例子使用如awk这类工具可能更容易完成。当你特别不希望改变父shell的范围变量时这可能也是有用的。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># Trivial implementation of awk expression:</span><span class="token comment">#   awk '$3 == "nfs" { print $2 " maps to " $1 }' /proc/mounts</span><span class="token function">cat</span> /proc/mounts <span class="token operator">|</span> <span class="token keyword">while</span> <span class="token builtin class-name">read</span> src dest <span class="token builtin class-name">type</span> opts rest<span class="token punctuation">;</span> <span class="token keyword">do</span>  <span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token variable">${type}</span> <span class="token operator">==</span> <span class="token string">"nfs"</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>    <span class="token builtin class-name">echo</span> <span class="token string">"NFS <span class="token variable">${dest}</span> maps to <span class="token variable">${src}</span>"</span>  <span class="token keyword">fi</span><span class="token keyword">done</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="命名约定"><a href="#命名约定" class="headerlink" title="命名约定"></a>命名约定</h2><h3 id="函数名"><a href="#函数名" class="headerlink" title="函数名"></a>函数名</h3><blockquote><p>使用小写字母，并用下划线分隔单词。使用双冒号 :: 分隔库。函数名之后必须有圆括号。关键词 function 是可选的，但必须在一个项目中保持一致。</p></blockquote><p>如果你正在写单个函数，请用小写字母来命名，并用下划线分隔单词。如果你正在写一个包，使用双冒号 :: 来分隔包名。大括号必须和函数名位于同一行（就像在Google的其他语言一样），并且函数名和圆括号之间没有空格。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># Single function</span><span class="token function-name function">my_func</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>  <span class="token punctuation">..</span>.<span class="token punctuation">}</span><span class="token comment"># Part of a package</span>mypackage::<span class="token function-name function">my_func</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>  <span class="token punctuation">..</span>.<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当函数名后存在 () 时，关键词 function 是多余的。但是其促进了函数的快速辨识。</p><h3 id="变量名"><a href="#变量名" class="headerlink" title="变量名"></a>变量名</h3><blockquote><p>如函数名。</p></blockquote><p>循环的变量名应该和循环的任何变量同样命名。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token keyword">for</span> <span class="token for-or-select variable">zone</span> <span class="token keyword">in</span> <span class="token variable">${zones}</span><span class="token punctuation">;</span> <span class="token keyword">do</span>  something_with <span class="token string">"<span class="token variable">${zone}</span>"</span><span class="token keyword">done</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h3 id="常量和环境变量名"><a href="#常量和环境变量名" class="headerlink" title="常量和环境变量名"></a>常量和环境变量名</h3><blockquote><p>全部大写，用下划线分隔，声明在文件的顶部。</p></blockquote><p>常量和任何导出到环境中的都应该大写。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># Constant</span><span class="token builtin class-name">readonly</span> <span class="token assign-left variable">PATH_TO_FILES</span><span class="token operator">=</span><span class="token string">'/some/path'</span><span class="token comment"># Both constant and environment</span><span class="token builtin class-name">declare</span> -xr <span class="token assign-left variable">ORACLE_SID</span><span class="token operator">=</span><span class="token string">'PROD'</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>第一次设置时有一些就变成了常量（例如，通过getopts）。因此，可以在getopts中或基于条件来设定常量，但之后应该立即设置其为只读。值得注意的是，在函数中 declare 不会对全局变量进行操作。所以推荐使用 readonly 和 export 来代替。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token assign-left variable">VERBOSE</span><span class="token operator">=</span><span class="token string">'false'</span><span class="token keyword">while</span> <span class="token builtin class-name">getopts</span> <span class="token string">'v'</span> flag<span class="token punctuation">;</span> <span class="token keyword">do</span>  <span class="token keyword">case</span> <span class="token string">"<span class="token variable">${flag}</span>"</span> <span class="token keyword">in</span>    <span class="token function">v</span><span class="token punctuation">)</span> <span class="token assign-left variable">VERBOSE</span><span class="token operator">=</span><span class="token string">'true'</span> <span class="token punctuation">;</span><span class="token punctuation">;</span>  <span class="token keyword">esac</span><span class="token keyword">done</span><span class="token builtin class-name">readonly</span> VERBOSE<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="源文件名"><a href="#源文件名" class="headerlink" title="源文件名"></a>源文件名</h3><blockquote><p>小写，如果需要的话使用下划线分隔单词。</p></blockquote><p>这是为了和在Google中的其他代码风格保持一致： maketemplate 或者 make_template ，而不是 make-template 。</p><h3 id="只读变量"><a href="#只读变量" class="headerlink" title="只读变量"></a>只读变量</h3><blockquote><p>使用 readonly 或者 declare -r 来确保变量只读。</p></blockquote><p>因为全局变量在shell中广泛使用，所以在使用它们的过程中捕获错误是很重要的。当你声明了一个变量，希望其只读，那么请明确指出。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token assign-left variable">zip_version</span><span class="token operator">=</span><span class="token string">"<span class="token variable"><span class="token variable">$(</span>dpkg --status <span class="token function">zip</span> <span class="token operator">|</span> <span class="token function">grep</span> Version: <span class="token operator">|</span> <span class="token function">cut</span> -d <span class="token string">' '</span> -f <span class="token number">2</span><span class="token variable">)</span></span>"</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> -z <span class="token string">"<span class="token variable">${zip_version}</span>"</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  error_message<span class="token keyword">else</span>  <span class="token builtin class-name">readonly</span> zip_version<span class="token keyword">fi</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="使用本地变量"><a href="#使用本地变量" class="headerlink" title="使用本地变量"></a>使用本地变量</h3><blockquote><p>使用 local 声明特定功能的变量。声明和赋值应该在不同行。</p></blockquote><p>使用 local 来声明局部变量以确保其只在函数内部和子函数中可见。这避免了污染全局命名空间和不经意间设置可能具有函数之外重要性的变量。</p><p>当赋值的值由命令替换提供时，声明和赋值必须分开。因为内建的 local 不会从命令替换中传递退出码。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function-name function">my_func2</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>  <span class="token builtin class-name">local</span> <span class="token assign-left variable">name</span><span class="token operator">=</span><span class="token string">"<span class="token variable">$1</span>"</span>  <span class="token comment"># Separate lines for declaration and assignment:</span>  <span class="token builtin class-name">local</span> my_var  <span class="token assign-left variable">my_var</span><span class="token operator">=</span><span class="token string">"<span class="token variable"><span class="token variable">$(</span>my_func<span class="token variable">)</span></span>"</span> <span class="token operator">||</span> <span class="token builtin class-name">return</span>  <span class="token comment"># DO NOT do this: $? contains the exit code of 'local', not my_func</span>  <span class="token builtin class-name">local</span> <span class="token assign-left variable">my_var</span><span class="token operator">=</span><span class="token string">"<span class="token variable"><span class="token variable">$(</span>my_func<span class="token variable">)</span></span>"</span>  <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token variable">$?</span> -eq <span class="token number">0</span> <span class="token punctuation">]</span><span class="token punctuation">]</span> <span class="token operator">||</span> <span class="token builtin class-name">return</span>  <span class="token punctuation">..</span>.<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="函数位置"><a href="#函数位置" class="headerlink" title="函数位置"></a>函数位置</h3><blockquote><p>将文件中所有的函数一起放在常量下面。不要在函数之间隐藏可执行代码。</p></blockquote><p>如果你有函数，请将他们一起放在文件头部。只有includes， set 声明和常量设置可能在函数声明之前完成。不要在函数之间隐藏可执行代码。如果那样做，会使得代码在调试时难以跟踪并出现意想不到的讨厌结果。</p><h3 id="主函数main"><a href="#主函数main" class="headerlink" title="主函数main"></a>主函数main</h3><blockquote><p>对于包含至少一个其他函数的足够长的脚本，需要称为 main 的函数。</p></blockquote><p>为了方便查找程序的开始，将主程序放入一个称为 main 的函数，作为最下面的函数。这使其和代码库的其余部分保持一致性，同时允许你定义更多变量为局部变量（如果主代码不是一个函数就不能这么做）。文件中最后的非注释行应该是对 main 函数的调用。</p><pre class="line-numbers language-none"><code class="language-none">main "$@"<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>显然，对于仅仅是线性流的短脚本， main 是矫枉过正，因此是不需要的。</p><h2 id="调用命令"><a href="#调用命令" class="headerlink" title="调用命令"></a>调用命令</h2><h3 id="检查返回值"><a href="#检查返回值" class="headerlink" title="检查返回值"></a>检查返回值</h3><blockquote><p>总是检查返回值，并给出信息返回值。</p></blockquote><p>对于非管道命令，使用 $? 或直接通过一个 if 语句来检查以保持其简洁。</p><p>例如：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token keyword">if</span> <span class="token operator">!</span> <span class="token function">mv</span> <span class="token string">"<span class="token variable">${file_list}</span>"</span> <span class="token string">"<span class="token variable">${dest_dir}</span>/"</span> <span class="token punctuation">;</span> <span class="token keyword">then</span>  <span class="token builtin class-name">echo</span> <span class="token string">"Unable to move <span class="token variable">${file_list}</span> to <span class="token variable">${dest_dir}</span>"</span> <span class="token operator">&gt;</span><span class="token file-descriptor important">&amp;2</span>  <span class="token builtin class-name">exit</span> <span class="token string">"<span class="token variable">${E_BAD_MOVE}</span>"</span><span class="token keyword">fi</span><span class="token comment"># Or</span><span class="token function">mv</span> <span class="token string">"<span class="token variable">${file_list}</span>"</span> <span class="token string">"<span class="token variable">${dest_dir}</span>/"</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"<span class="token variable">$?</span>"</span> -ne <span class="token number">0</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  <span class="token builtin class-name">echo</span> <span class="token string">"Unable to move <span class="token variable">${file_list}</span> to <span class="token variable">${dest_dir}</span>"</span> <span class="token operator">&gt;</span><span class="token file-descriptor important">&amp;2</span>  <span class="token builtin class-name">exit</span> <span class="token string">"<span class="token variable">${E_BAD_MOVE}</span>"</span><span class="token keyword">fi</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>Bash也有 PIPESTATUS 变量，允许检查从管道所有部分返回的代码。如果仅仅需要检查整个管道是成功还是失败，以下的方法是可以接受的：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">tar</span> -cf - ./* <span class="token operator">|</span> <span class="token punctuation">(</span> <span class="token builtin class-name">cd</span> <span class="token string">"<span class="token variable">${dir}</span>"</span> <span class="token operator">&amp;&amp;</span> <span class="token function">tar</span> -xf - <span class="token punctuation">)</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"<span class="token variable">${<span class="token environment constant">PIPESTATUS</span><span class="token punctuation">[</span>0<span class="token punctuation">]</span>}</span>"</span> -ne <span class="token number">0</span> <span class="token operator">||</span> <span class="token string">"<span class="token variable">${<span class="token environment constant">PIPESTATUS</span><span class="token punctuation">[</span>1<span class="token punctuation">]</span>}</span>"</span> -ne <span class="token number">0</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  <span class="token builtin class-name">echo</span> <span class="token string">"Unable to tar files to <span class="token variable">${dir}</span>"</span> <span class="token operator">&gt;</span><span class="token file-descriptor important">&amp;2</span><span class="token keyword">fi</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>可是，只要你运行任何其他命令， PIPESTATUS 将会被覆盖。如果你需要基于管道中发生的错误执行不同的操作，那么你需要在运行命令后立即将 PIPESTATUS 赋值给另一个变量（别忘了 [ 是一个会将 PIPESTATUS 擦除的命令）。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">tar</span> -cf - ./* <span class="token operator">|</span> <span class="token punctuation">(</span> <span class="token builtin class-name">cd</span> <span class="token string">"<span class="token variable">${DIR}</span>"</span> <span class="token operator">&amp;&amp;</span> <span class="token function">tar</span> -xf - <span class="token punctuation">)</span><span class="token assign-left variable">return_codes</span><span class="token operator">=</span><span class="token punctuation">(</span><span class="token variable">${<span class="token environment constant">PIPESTATUS</span><span class="token punctuation">[</span>*<span class="token punctuation">]</span>}</span><span class="token punctuation">)</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"<span class="token variable">${return_codes<span class="token punctuation">[</span>0<span class="token punctuation">]</span>}</span>"</span> -ne <span class="token number">0</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  do_something<span class="token keyword">fi</span><span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token string">"<span class="token variable">${return_codes<span class="token punctuation">[</span>1<span class="token punctuation">]</span>}</span>"</span> -ne <span class="token number">0</span> <span class="token punctuation">]</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  do_something_else<span class="token keyword">fi</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="内建命令和外部命令"><a href="#内建命令和外部命令" class="headerlink" title="内建命令和外部命令"></a>内建命令和外部命令</h3><blockquote><p>可以在调用shell内建命令和调用另外的程序之间选择，请选择内建命令。</p></blockquote><p>我们更喜欢使用内建命令，如在 bash(1) 中参数扩展函数。因为它更强健和便携（尤其是跟像 sed 这样的命令比较）</p><p>例如：</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># Prefer this:</span><span class="token assign-left variable">addition</span><span class="token operator">=</span><span class="token variable"><span class="token variable">$((</span>${X} <span class="token operator">+</span> ${Y}<span class="token variable">))</span></span><span class="token assign-left variable">substitution</span><span class="token operator">=</span><span class="token string">"<span class="token variable">${string<span class="token operator">/</span><span class="token operator">#</span>foo<span class="token operator">/</span>bar}</span>"</span><span class="token comment"># Instead of this:</span><span class="token assign-left variable">addition</span><span class="token operator">=</span><span class="token string">"<span class="token variable"><span class="token variable">$(</span><span class="token function">expr</span> $<span class="token punctuation">{</span>X<span class="token punctuation">}</span> + $<span class="token punctuation">{</span>Y<span class="token punctuation">}</span><span class="token variable">)</span></span>"</span><span class="token assign-left variable">substitution</span><span class="token operator">=</span><span class="token string">"<span class="token variable"><span class="token variable">$(</span><span class="token builtin class-name">echo</span> <span class="token string">"<span class="token variable">${string}</span>"</span> <span class="token operator">|</span> <span class="token function">sed</span> -e <span class="token string">'s/^foo/bar/'</span><span class="token variable">)</span></span>"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> shell </category>
          
      </categories>
      
      
        <tags>
            
            <tag> shell规范 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【GitHub】GitHub Action</title>
      <link href="/2021/10/27/github/github-action/github-github-action/"/>
      <url>/2021/10/27/github/github-action/github-github-action/</url>
      
        <content type="html"><![CDATA[<h1 id="【仓库】GitHub-Action"><a href="#【仓库】GitHub-Action" class="headerlink" title="【仓库】GitHub Action"></a>【仓库】GitHub Action</h1><blockquote><p>github action 文档：<a href="https://docs.github.com/cn/actions">https://docs.github.com/cn/actions</a></p><p><a href="https://github.com/actions/starter-workflows">https://github.com/actions/starter-workflows</a></p><p><a href="https://github.com/marketplace">https://github.com/marketplace</a></p></blockquote><h2 id="Github-Action简介"><a href="#Github-Action简介" class="headerlink" title="Github Action简介"></a>Github Action简介</h2><p>在 GitHub Actions 的仓库中自动化、自定义和执行软件开发工作流程。</p><h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p><strong>Workflows</strong></p><p>工作流，可以添加到存储库中的自动化过程。工作流由一个或多个作业组成，可以由事件调度或触发。</p><p><strong>Event</strong></p><p>事件，触发工作流的特定动作。例如，向存储库提交 pr 或 pull 请求。</p><p><strong>Jobs</strong></p><p>作业，在同一跑步器上执行的一组步骤。默认情况下，具有多个作业的工作流将并行运行这些作业。</p><p><strong>Steps</strong></p><p>步骤，可以在作业中运行命令的单个任务。步骤可以是操作，也可以是 shell 命令。作业中的每个步骤都在同一个运行程序上执行，从而允许该作业中的操作彼此共享数据。</p><p><strong>Actions</strong></p><p>操作是独立的命令，它们被组合成创建作业的步骤。操作是工作流中最小的可移植构建块。你可以创建自己的动作，或者使用 GitHub 社区创建的动作。</p><p><strong>Runners</strong></p><p>运行器，安装了 GitHub Actions 运行器应用程序的服务器。。Github 托管的运行器基于 Ubuntu Linux、Microsoft Windows 和 macOS，工作流中的每个作业都在一个新的虚拟环境中运行。</p><h2 id="workflow语法"><a href="#workflow语法" class="headerlink" title="workflow语法"></a>workflow语法</h2><blockquote><p>workflow文件必须存储在仓库的.github/workflows的目录中，扩展名为.yml, .yaml</p></blockquote><p><strong>name</strong></p><p>workflow的名称，Github在仓库操作页面上显示workflow的名称。</p><p><strong>on</strong></p><p>触发workflow的Github事件的名称。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 单个事件</span><span class="token key atrule">on</span><span class="token punctuation">:</span> push<span class="token comment"># 多个事件</span><span class="token key atrule">on</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>push<span class="token punctuation">,</span> pull_request<span class="token punctuation">]</span><span class="token comment"># 指定分支</span><span class="token key atrule">on</span><span class="token punctuation">:</span>  <span class="token key atrule">push</span><span class="token punctuation">:</span>    <span class="token key atrule">branches</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> main <span class="token comment"># 定时调度</span> <span class="token key atrule">on</span><span class="token punctuation">:</span>   <span class="token key atrule">schedule</span><span class="token punctuation">:</span>     <span class="token punctuation">-</span> <span class="token key atrule">cron</span><span class="token punctuation">:</span> <span class="token string">''</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>jobs</strong></p><p>作业，workflow 主要执行的核心任务。</p><ul><li><p><strong>jobs.<job_id></job_id></strong></p><p>每个job必须关联一个ID</p></li><li><p><strong>jobs.<job_id>.name</job_id></strong></p><p>job显示在github上的名称</p></li><li><p><strong>jobs.<job_id>.needs</job_id></strong></p><p>识别在此作业运行之前必须成功完成的其他作业。</p></li><li><p><strong>jobs.<job_id>.runs-on</job_id></strong></p><p>要运行作业的机器类型。 机器可以是 GitHub 托管的运行器或自托管的运行器。</p><p><strong>strategy策略：</strong></p><ul><li>matrix： 构建矩阵</li><li>fail-fast：默认为true，一旦某个矩阵任务失败立即取消所有还在进行中的任务。</li><li>max-paraller：可同时执行的最大并发数，默认情况下 <code>GitHub</code> 会动态调整。</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">runs-on</span><span class="token punctuation">:</span> $<span class="token punctuation">{</span><span class="token punctuation">{</span> matrix.os <span class="token punctuation">}</span><span class="token punctuation">}</span><span class="token key atrule">strategy</span><span class="token punctuation">:</span> <span class="token comment"># strategy策略</span>  <span class="token key atrule">matrix</span><span class="token punctuation">:</span> <span class="token comment"># 构建矩阵</span>    <span class="token key atrule">os</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>macos<span class="token punctuation">-</span>latest<span class="token punctuation">,</span> windows<span class="token punctuation">-</span>latest<span class="token punctuation">,</span> ubuntu<span class="token punctuation">-</span><span class="token number">18.04</span><span class="token punctuation">]</span>    <span class="token key atrule">node</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token number">4</span><span class="token punctuation">,</span> <span class="token number">6</span><span class="token punctuation">,</span> <span class="token number">8</span><span class="token punctuation">,</span> <span class="token number">10</span><span class="token punctuation">]</span>    <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><strong>jobs.<job_id>.steps</job_id></strong></p><p>步骤，每个 Job 包含一个或多个步骤。步骤可以是运行命令、运行设置任务，或者运行仓库中的操作和 Dcoker 镜像发布等。</p></li><li><p>job.<job_id>.steps.name</job_id></p></li></ul>]]></content>
      
      
      <categories>
          
          <category> GitHub </category>
          
      </categories>
      
      
        <tags>
            
            <tag> github action </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【持续集成交付 Argo】Argo Rollouts</title>
      <link href="/2021/10/26/argo/chi-xu-ji-cheng-jiao-fu-argo-argo-rollouts/"/>
      <url>/2021/10/26/argo/chi-xu-ji-cheng-jiao-fu-argo-argo-rollouts/</url>
      
        <content type="html"><![CDATA[<h1 id="【持续集成交付-Argo】Argo-Rollouts"><a href="#【持续集成交付-Argo】Argo-Rollouts" class="headerlink" title="【持续集成交付 Argo】Argo Rollouts"></a>【持续集成交付 Argo】Argo Rollouts</h1>]]></content>
      
      
      <categories>
          
          <category> 持续集成交付 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> argo </tag>
            
            <tag> argo-rollouts </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【持续集成交付 Argo】Argo Events</title>
      <link href="/2021/10/26/argo/chi-xu-ji-cheng-jiao-fu-argo-argo-events/"/>
      <url>/2021/10/26/argo/chi-xu-ji-cheng-jiao-fu-argo-argo-events/</url>
      
        <content type="html"><![CDATA[<h1 id="【持续集成交付-Argo】Argo-Events"><a href="#【持续集成交付-Argo】Argo-Events" class="headerlink" title="【持续集成交付 Argo】Argo Events"></a>【持续集成交付 Argo】Argo Events</h1>]]></content>
      
      
      <categories>
          
          <category> 持续集成交付 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> argo </tag>
            
            <tag> argo-events </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【持续集成交付 Argo】Argo Workflows</title>
      <link href="/2021/10/26/argo/chi-xu-ji-cheng-jiao-fu-argo-argo-workflows/"/>
      <url>/2021/10/26/argo/chi-xu-ji-cheng-jiao-fu-argo-argo-workflows/</url>
      
        <content type="html"><![CDATA[<h1 id="持续集成交付-Argo】Argo-Workflows"><a href="#持续集成交付-Argo】Argo-Workflows" class="headerlink" title="持续集成交付 Argo】Argo Workflows"></a>持续集成交付 Argo】Argo Workflows</h1>]]></content>
      
      
      <categories>
          
          <category> 持续集成交付 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> argo </tag>
            
            <tag> argo-workflows </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【持续集成交付 Argo】Argo CD</title>
      <link href="/2021/10/26/argo/chi-xu-ji-cheng-jiao-fu-argo-argo-cd/"/>
      <url>/2021/10/26/argo/chi-xu-ji-cheng-jiao-fu-argo-argo-cd/</url>
      
        <content type="html"><![CDATA[<h1 id="【持续集成交付-Argo】Argo-CD"><a href="#【持续集成交付-Argo】Argo-CD" class="headerlink" title="【持续集成交付 Argo】Argo CD"></a>【持续集成交付 Argo】Argo CD</h1><blockquote><p>argo-cd 文档地址：<a href="https://argo-cd.readthedocs.io/en/stable/">https://argo-cd.readthedocs.io/en/stable/</a></p><p>argo-cd github地址： <a href="https://argo-cd.readthedocs.io/en/stable/">https://argo-cd.readthedocs.io/en/stable/</a></p></blockquote><h2 id="Argo-CD简介"><a href="#Argo-CD简介" class="headerlink" title="Argo CD简介"></a>Argo CD简介</h2><p>Argo CD是用于Kubernetes的声明性GitOps持续交付工具。</p><p>应用的定义/配置和环境应该被声明和版本管理，应用程序部署和生命周期管理应该是自动化的，可审核的且易于理解的。</p><h3 id="argo-cd架构"><a href="#argo-cd架构" class="headerlink" title="argo cd架构"></a>argo cd架构</h3><p>Argo CD被实现为kubernetes控制器，该控制器连续监视正在运行的应用程序，并将当前的活动状态与所需的目标状态（在Git存储库中指定）进行比较。处于活动状态偏离目标状态的已部署应用程序被视为OutOfSync。Argo CD报告并可视化差异，同时提供了自动或手动将实时状态同步回所需目标状态的功能。在Git存储库中对所需目标状态所做的任何修改都可以自动应用并反映在指定的目标环境中。</p><h3 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h3><ul><li><p><strong>Application</strong>：清单定义的一组Kubernetes资源。这是一个自定义资源定义（CRD）。</p></li><li><p><strong>Application source type</strong>：使用哪个工具来构建应用程序</p></li><li><p><strong>Target state</strong>：应用程序的期望状态，由Git存储库中的文件表示。</p></li><li><p><strong>Live state</strong>：该应用程序的实时状态。部署了哪些Pod等。</p></li><li><p><strong>Sync status</strong>：实时状态是否与目标状态匹配。部署的应用程序是否与Git所说的相同</p></li><li><p><strong>Sync</strong>：使应用程序移至其目标状态的过程。例如。通过将更改应用于Kubernetes集群。</p></li><li><p><strong>Refresh</strong>：将Git中的最新代码与实时状态进行比较。找出有什么不同。</p></li><li><p><strong>Health</strong>:应用程序的运行状况是否正常运行？它可以满足请求吗？</p></li><li><p><strong>Tool</strong>:从文件目录创建清单的工具。例如。Kustomize或Ksonnet。</p></li></ul><h2 id="Argo-CD环境配置"><a href="#Argo-CD环境配置" class="headerlink" title="Argo CD环境配置"></a>Argo CD环境配置</h2><ul><li><p><strong>kubernetes安装</strong></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">kubectl create namespace argocdkubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml## 内核功能argo cdkubectl create namespace argocdkubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/core-install.yaml<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><strong>argo cli安装</strong></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"># 从github上下载最新版本argo clihttps://github.com/argoproj/argo-cd/releases/latest# mac上安装brew install argocd<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><strong>helm安装</strong></p><blockquote><p>helm 官方地址： <a href="https://github.com/argoproj/argo-helm/tree/master/charts/argo-cd">https://github.com/argoproj/argo-helm/tree/master/charts/argo-cd</a></p><p>helm自定义地址：</p></blockquote></li></ul><h2 id="Argo-CD命令行"><a href="#Argo-CD命令行" class="headerlink" title="Argo CD命令行"></a>Argo CD命令行</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage:  argocd [flags]  argocd [command]Available Commands:  account     Manage account settings  admin       Contains a set of commands useful for Argo CD administrators and requires direct Kubernetes access  app         Manage applications  cert        Manage repository certificates and SSH known hosts entries  cluster     Manage cluster credentials  completion  output shell completion code for the specified shell (bash or zsh)  context     Switch between contexts  gpg         Manage GPG keys used for signature verification  help        Help about any command  login       Log in to Argo CD  logout      Log out from Argo CD  proj        Manage projects  relogin     Refresh an expired authenticate token  repo        Manage repository connection parameters  repocreds   Manage repository connection parameters  version     Print version information<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><p>登陆argo</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"># 通过用户名密码登陆argocd login --name admin --password xxxx# 通过SSO登陆argocd login --sso# 使用Kubernetes API Server配置直接访问argocd login --core<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>创建应用</p></li><li><p>添加集群</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">argocd cluster add context --kubeconfig config --name cluster-name<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li></li></ul>]]></content>
      
      
      <categories>
          
          <category> 持续集成交付 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> argo </tag>
            
            <tag> argo-cd </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Harbor</title>
      <link href="/2021/10/20/harbor/cang-ku-harbor/"/>
      <url>/2021/10/20/harbor/cang-ku-harbor/</url>
      
        <content type="html"><![CDATA[<h1 id="【仓库】Harbor"><a href="#【仓库】Harbor" class="headerlink" title="【仓库】Harbor"></a>【仓库】Harbor</h1><blockquote><p>harbor 官网: <a href="https://goharbor.io/">https://goharbor.io/</a></p><p>harbor github地址: <a href="https://github.com/goharbor/harbor">https://github.com/goharbor/harbor</a></p></blockquote><h2 id="Harbor简介"><a href="#Harbor简介" class="headerlink" title="Harbor简介"></a>Harbor简介</h2>]]></content>
      
      
      <categories>
          
          <category> 仓库 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> harbor </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【仓库】GitLab</title>
      <link href="/2021/10/20/gitlab/cang-ku-gitlab/"/>
      <url>/2021/10/20/gitlab/cang-ku-gitlab/</url>
      
        <content type="html"><![CDATA[<h1 id="【仓库】Gitlab"><a href="#【仓库】Gitlab" class="headerlink" title="【仓库】Gitlab"></a>【仓库】Gitlab</h1><blockquote><p>gitlab 官网： <a href="https://gitlab.com/">https://gitlab.com</a></p><p>gitlab 文档: <a href="https://docs.gitlab.com/">https://docs.gitlab.com/</a></p></blockquote><h2 id="gitlab简介"><a href="#gitlab简介" class="headerlink" title="gitlab简介"></a>gitlab简介</h2><h2 id="gitlab-环境搭建"><a href="#gitlab-环境搭建" class="headerlink" title="gitlab 环境搭建"></a>gitlab 环境搭建</h2>]]></content>
      
      
      <categories>
          
          <category> 仓库 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> gitlab </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>GitHub</title>
      <link href="/2021/10/20/github/cang-ku-github/"/>
      <url>/2021/10/20/github/cang-ku-github/</url>
      
        <content type="html"><![CDATA[<h1 id="GitHub"><a href="#GitHub" class="headerlink" title="GitHub"></a>GitHub</h1><blockquote><p>github 官网: <a href="https://github.com/">https://github.com/</a></p><p>github 文档: <a href="https://docs.github.com/en">https://docs.github.com/en</a></p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 仓库 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> github </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>LVS</title>
      <link href="/2021/10/19/lvs/fu-zai-jun-heng-lvs/"/>
      <url>/2021/10/19/lvs/fu-zai-jun-heng-lvs/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【代理】Nginx</title>
      <link href="/2021/10/19/nginx/fu-zai-jun-heng-nginx/"/>
      <url>/2021/10/19/nginx/fu-zai-jun-heng-nginx/</url>
      
        <content type="html"><![CDATA[<h1 id="【代理】Nginx"><a href="#【代理】Nginx" class="headerlink" title="【代理】Nginx"></a>【代理】Nginx</h1>]]></content>
      
      
      <categories>
          
          <category> 代理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> nginx </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【监控】Zabbix</title>
      <link href="/2021/10/19/monitor/zabbix/jian-kong-zabbix/"/>
      <url>/2021/10/19/monitor/zabbix/jian-kong-zabbix/</url>
      
        <content type="html"><![CDATA[<h1 id="【监控】Zabbix"><a href="#【监控】Zabbix" class="headerlink" title="【监控】Zabbix"></a>【监控】Zabbix</h1>]]></content>
      
      
      <categories>
          
          <category> 监控 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> zabbix </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>LDAP</title>
      <link href="/2021/10/19/ldap/tong-yi-ren-zheng-ldap/"/>
      <url>/2021/10/19/ldap/tong-yi-ren-zheng-ldap/</url>
      
        <content type="html"><![CDATA[<h1 id="【统一认证】LDAP"><a href="#【统一认证】LDAP" class="headerlink" title="【统一认证】LDAP"></a>【统一认证】LDAP</h1>]]></content>
      
      
      <categories>
          
          <category> 统一认证 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ldap </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Algorithms</title>
      <link href="/2021/10/19/algorithms/suan-fa-algorithms/"/>
      <url>/2021/10/19/algorithms/suan-fa-algorithms/</url>
      
        <content type="html"><![CDATA[<h1 id="【算法】Algorithms"><a href="#【算法】Algorithms" class="headerlink" title="【算法】Algorithms"></a>【算法】Algorithms</h1>]]></content>
      
      
      <categories>
          
          <category> 算法 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> algorithms </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Data Structure</title>
      <link href="/2021/10/19/algorithms/data-structure/shu-ju-jie-gou-data-structure/"/>
      <url>/2021/10/19/algorithms/data-structure/shu-ju-jie-gou-data-structure/</url>
      
        <content type="html"><![CDATA[<h1 id="【数据结构】data-structure"><a href="#【数据结构】data-structure" class="headerlink" title="【数据结构】data structure"></a>【数据结构】data structure</h1>]]></content>
      
      
      <categories>
          
          <category> 数据结构 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> data structure </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>LeetCode</title>
      <link href="/2021/10/19/algorithms/leetcode/suan-fa-ti-leetcode/"/>
      <url>/2021/10/19/algorithms/leetcode/suan-fa-ti-leetcode/</url>
      
        <content type="html"><![CDATA[<h1 id="【算法题】LeetCode"><a href="#【算法题】LeetCode" class="headerlink" title="【算法题】LeetCode"></a>【算法题】LeetCode</h1>]]></content>
      
      
      <categories>
          
          <category> 算法题 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> algorithms </tag>
            
            <tag> data structure </tag>
            
            <tag> leetcode </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>MongoDB</title>
      <link href="/2021/10/19/database/mongodb/wen-dang-xing-shu-ju-ku-mongodb/"/>
      <url>/2021/10/19/database/mongodb/wen-dang-xing-shu-ju-ku-mongodb/</url>
      
        <content type="html"><![CDATA[<h1 id="【文档型数据库】MongoDB"><a href="#【文档型数据库】MongoDB" class="headerlink" title="【文档型数据库】MongoDB"></a>【文档型数据库】MongoDB</h1><blockquote><p>Mongoldb 官网：<a href="https://www.mongodb.com/">https://www.mongodb.com/</a></p><p>github 地址：<a href="https://github.com/mongodb">https://github.com/mongodb</a></p></blockquote><h2 id="mongo简介"><a href="#mongo简介" class="headerlink" title="mongo简介"></a>mongo简介</h2><h3 id="mongo特点"><a href="#mongo特点" class="headerlink" title="mongo特点"></a>mongo特点</h3><ul><li>  模式自由：可以将不同格式的文档存在同一个数据库中</li><li>  面向集合的储存： 适合储存JSON风格文件的形式</li><li>  完整的索引支持：对任何书写可索引</li><li>  复制和高可用性： 支持服务器之间的数据复制，支持主-从模式及服务器之间的相互复制</li><li>  自动分片： 支持云级别的伸缩性：自动分片功能支持水平的数据库集群，可动态添加额外的机器</li><li>  丰富的查询： 支持丰富的查询表达方式</li><li>  快速就地更新：查询优化器会分析查询表达式，并生成一个高效的查询计划</li><li>  高效的传统储存方法，支持二进制数据及大型对象，比如图片</li></ul><h3 id="mongo术语"><a href="#mongo术语" class="headerlink" title="mongo术语"></a>mongo术语</h3>]]></content>
      
      
      <categories>
          
          <category> 文档型数据库 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> mongodb </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【搜索引擎】ElasticSearch</title>
      <link href="/2021/10/19/database/elasticsearch/sou-suo-yin-qing-elasticsearch/"/>
      <url>/2021/10/19/database/elasticsearch/sou-suo-yin-qing-elasticsearch/</url>
      
        <content type="html"><![CDATA[<h1 id="【搜索引擎】ElasticSearch"><a href="#【搜索引擎】ElasticSearch" class="headerlink" title="【搜索引擎】ElasticSearch"></a>【搜索引擎】ElasticSearch</h1>]]></content>
      
      
      <categories>
          
          <category> 搜索数据库 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> elasticsearch </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>RocketMQ</title>
      <link href="/2021/10/19/message-middleware/rocketmq/xiao-xi-zhong-jian-jian-rocketmq/"/>
      <url>/2021/10/19/message-middleware/rocketmq/xiao-xi-zhong-jian-jian-rocketmq/</url>
      
        <content type="html"><![CDATA[<h1 id="【消息中间件】RocketMQ"><a href="#【消息中间件】RocketMQ" class="headerlink" title="【消息中间件】RocketMQ"></a>【消息中间件】RocketMQ</h1>]]></content>
      
      
      <categories>
          
          <category> 消息中间件 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> rocketmq </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>RabbitMQ</title>
      <link href="/2021/10/19/message-middleware/rabbitmq/xiao-xi-zhong-jian-jian-rabbitmq/"/>
      <url>/2021/10/19/message-middleware/rabbitmq/xiao-xi-zhong-jian-jian-rabbitmq/</url>
      
        <content type="html"><![CDATA[<h1 id="【消息中间件】RabbitMQ"><a href="#【消息中间件】RabbitMQ" class="headerlink" title="【消息中间件】RabbitMQ"></a>【消息中间件】RabbitMQ</h1>]]></content>
      
      
      <categories>
          
          <category> 消息中间件 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> rabbitmq </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Kafka</title>
      <link href="/2021/10/19/message-middleware/kafka/xiao-xi-zhong-jian-jian-kafka/"/>
      <url>/2021/10/19/message-middleware/kafka/xiao-xi-zhong-jian-jian-kafka/</url>
      
        <content type="html"><![CDATA[<h1 id="【分布式事件流平台】Kafka"><a href="#【分布式事件流平台】Kafka" class="headerlink" title="【分布式事件流平台】Kafka"></a>【分布式事件流平台】Kafka</h1>]]></content>
      
      
      <categories>
          
          <category> 分布式事件流平台 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> kafka </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Redis</title>
      <link href="/2021/10/19/database/redis/jian-zhi-shu-ju-ku-redis/"/>
      <url>/2021/10/19/database/redis/jian-zhi-shu-ju-ku-redis/</url>
      
        <content type="html"><![CDATA[<h1 id="【键值数据库】Redis"><a href="#【键值数据库】Redis" class="headerlink" title="【键值数据库】Redis"></a>【键值数据库】Redis</h1><blockquote><p>Redis 官网：<a href="https://redis.io/">https://redis.io/</a></p></blockquote><h2 id="redis简介"><a href="#redis简介" class="headerlink" title="redis简介"></a>redis简介</h2><p>Redis是一款内存高速缓存数据库。Redis全称为：<strong>Remote Dictionary Server</strong>（远程数据服务），使用C语言编写，Redis是一个key-value存储系统（键值存储系统），支持丰富的数据类型。</p><p>Redis是一种支持key-value等多种数据结构的存储系统。可用于缓存，事件发布或订阅，高速队列等场景。支持网络，提供字符串，哈希，列表，队列，集合结构直接存取，基于内存，可持久化。</p><h2 id="redis环境配置"><a href="#redis环境配置" class="headerlink" title="redis环境配置"></a>redis环境配置</h2><ul><li><p>yum安装redis</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># 添加EPEL仓库，然后更新yum源</span><span class="token function">sudo</span> yum <span class="token function">install</span> epel-release<span class="token function">sudo</span> yum update<span class="token comment"># 安装Redis数据库</span><span class="token function">sudo</span> yum -y <span class="token function">install</span> redis<span class="token comment"># 启动redis服务</span><span class="token function">sudo</span> systemctl start redis<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>apt安装redis</p></li><li><p>brew 安装redis</p></li><li><p>docker安装redis</p></li></ul>]]></content>
      
      
      <categories>
          
          <category> 键值数据库 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> redis </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>PostgreSQL</title>
      <link href="/2021/10/19/database/postgresql/guan-xi-xing-shu-ju-ku-postgresql/"/>
      <url>/2021/10/19/database/postgresql/guan-xi-xing-shu-ju-ku-postgresql/</url>
      
        <content type="html"><![CDATA[<h1 id="PostgreSQL"><a href="#PostgreSQL" class="headerlink" title="PostgreSQL"></a>PostgreSQL</h1>]]></content>
      
      
      <categories>
          
          <category> 关系型数据库 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> postgresql </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ETCD</title>
      <link href="/2021/10/19/storage/etcd/cun-chu-etcd/"/>
      <url>/2021/10/19/storage/etcd/cun-chu-etcd/</url>
      
        <content type="html"><![CDATA[<h1 id="【分布式键值存储】ETCD"><a href="#【分布式键值存储】ETCD" class="headerlink" title="【分布式键值存储】ETCD"></a>【分布式键值存储】ETCD</h1><blockquote><p>etcd 官网: <a href="https://etcd.io/">https://etcd.io/</a></p><p>etcd github地址: <a href="https://github.com/etcd-io/etcd">https://github.com/etcd-io/etcd</a></p></blockquote><h2 id="etcd简介"><a href="#etcd简介" class="headerlink" title="etcd简介"></a>etcd简介</h2><h2 id="etcd环境配置"><a href="#etcd环境配置" class="headerlink" title="etcd环境配置"></a>etcd环境配置</h2>]]></content>
      
      
      <categories>
          
          <category> 存储 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> etcd </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【关系型数据库】Mysql</title>
      <link href="/2021/10/19/database/mysql/guan-xi-xing-shu-ju-ku-mysql/"/>
      <url>/2021/10/19/database/mysql/guan-xi-xing-shu-ju-ku-mysql/</url>
      
        <content type="html"><![CDATA[<h1 id="【关系型数据库】Mysql"><a href="#【关系型数据库】Mysql" class="headerlink" title="【关系型数据库】Mysql"></a>【关系型数据库】Mysql</h1>]]></content>
      
      
      <categories>
          
          <category> 关系型数据库 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> mysql </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【任务调度】Airflow</title>
      <link href="/2021/10/19/ariflow/ren-wu-diao-du-airflow/"/>
      <url>/2021/10/19/ariflow/ren-wu-diao-du-airflow/</url>
      
        <content type="html"><![CDATA[<h1 id="【任务调度】Airflow"><a href="#【任务调度】Airflow" class="headerlink" title="【任务调度】Airflow"></a>【任务调度】Airflow</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Calico</title>
      <link href="/2021/10/19/calico/wang-luo-calico/"/>
      <url>/2021/10/19/calico/wang-luo-calico/</url>
      
        <content type="html"><![CDATA[<h1 id="【网络】Calico"><a href="#【网络】Calico" class="headerlink" title="【网络】Calico"></a>【网络】Calico</h1><blockquote><p>calico 官网： <a href="https://www.tigera.io/">https://www.tigera.io/</a></p><p>calico github 地址： <a href="https://github.com/projectcalico/calico">https://github.com/projectcalico/calico</a></p></blockquote><h2 id="Calico简介"><a href="#Calico简介" class="headerlink" title="Calico简介"></a>Calico简介</h2>]]></content>
      
      
      <categories>
          
          <category> 网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> calico </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Flannel</title>
      <link href="/2021/10/19/flannel/wang-luo-flannel/"/>
      <url>/2021/10/19/flannel/wang-luo-flannel/</url>
      
        <content type="html"><![CDATA[<h1 id="【网络】Flannel"><a href="#【网络】Flannel" class="headerlink" title="【网络】Flannel"></a>【网络】Flannel</h1>]]></content>
      
      
      <categories>
          
          <category> 网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> flannel </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Envoy</title>
      <link href="/2021/10/19/envoy/fu-zai-jun-heng-envoy/"/>
      <url>/2021/10/19/envoy/fu-zai-jun-heng-envoy/</url>
      
        <content type="html"><![CDATA[<h1 id="【负载均衡】Envoy"><a href="#【负载均衡】Envoy" class="headerlink" title="【负载均衡】Envoy"></a>【负载均衡】Envoy</h1><blockquote><p>envoy 官网：<a href="https://www.envoyproxy.io/">https://www.envoyproxy.io/</a></p><p>envoy github地址：<a href="https://github.com/envoyproxy/envoy">https://github.com/envoyproxy/envoy</a></p></blockquote><h2 id="envoy简介"><a href="#envoy简介" class="headerlink" title="envoy简介"></a>envoy简介</h2><h2 id="envoy环境配置"><a href="#envoy环境配置" class="headerlink" title="envoy环境配置"></a>envoy环境配置</h2>]]></content>
      
      
      <categories>
          
          <category> 代理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> envoy </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>kiali</title>
      <link href="/2021/10/19/service-mesh/kiali/kiali/"/>
      <url>/2021/10/19/service-mesh/kiali/kiali/</url>
      
        <content type="html"><![CDATA[<h1 id="kiali"><a href="#kiali" class="headerlink" title="kiali"></a>kiali</h1><blockquote><p>官网：<a href="https://kiali.io/">https://kiali.io/</a></p><p>github地址：<a href="https://github.com/kiali">https://github.com/kiali</a></p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 服务网格 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> istio </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>cert-manager</title>
      <link href="/2021/10/19/service-mesh/cert-manager/cert-manager/"/>
      <url>/2021/10/19/service-mesh/cert-manager/cert-manager/</url>
      
        <content type="html"><![CDATA[<h1 id="cert-manager"><a href="#cert-manager" class="headerlink" title="cert-manager"></a>cert-manager</h1><blockquote><p>官网：<a href="https://cert-manager.io/">https://cert-manager.io/</a></p><p>github地址：<a href="https://github.com/jetstack/cert-manager">https://github.com/jetstack/cert-manager</a></p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 服务网格 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> istio </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Istio</title>
      <link href="/2021/10/19/service-mesh/istio/fu-wu-wang-ge-istio/"/>
      <url>/2021/10/19/service-mesh/istio/fu-wu-wang-ge-istio/</url>
      
        <content type="html"><![CDATA[<h1 id="【服务网格】Istio"><a href="#【服务网格】Istio" class="headerlink" title="【服务网格】Istio"></a>【服务网格】Istio</h1><blockquote><p>istio官网： <a href="https://istio.io/">https://istio.io/</a></p><p>istio github地址：<a href="https://github.com/istio/">https://github.com/istio/</a></p></blockquote><h2 id="istio简介"><a href="#istio简介" class="headerlink" title="istio简介"></a>istio简介</h2><h2 id="istio环境配置"><a href="#istio环境配置" class="headerlink" title="istio环境配置"></a>istio环境配置</h2>]]></content>
      
      
      <categories>
          
          <category> 服务网格 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> istio </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>jaeger</title>
      <link href="/2021/10/19/service-mesh/jaeger/jaeger/"/>
      <url>/2021/10/19/service-mesh/jaeger/jaeger/</url>
      
        <content type="html"><![CDATA[<h1 id="jaeger"><a href="#jaeger" class="headerlink" title="jaeger"></a>jaeger</h1><blockquote><p>官网： <a href="https://www.jaegertracing.io/">https://www.jaegertracing.io/</a></p><p>github地址：<a href="https://github.com/jaegertracing/jaeger">https://github.com/jaegertracing/jaeger</a></p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 服务网格 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> istio </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>zipkin</title>
      <link href="/2021/10/19/service-mesh/zipkin/zipkin/"/>
      <url>/2021/10/19/service-mesh/zipkin/zipkin/</url>
      
        <content type="html"><![CDATA[<h1 id="zipkin"><a href="#zipkin" class="headerlink" title="zipkin"></a>zipkin</h1><blockquote><p>官网：<a href="https://zipkin.io/">https://zipkin.io/</a></p><p>github 地址：<a href="https://github.com/openzipkin/zipkin">https://github.com/openzipkin/zipkin</a></p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 服务网格 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> istio </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>linkerd</title>
      <link href="/2021/10/19/service-mesh/linkerd/linkerd/"/>
      <url>/2021/10/19/service-mesh/linkerd/linkerd/</url>
      
        <content type="html"><![CDATA[<h1 id="linkerd"><a href="#linkerd" class="headerlink" title="linkerd"></a>linkerd</h1><blockquote><p>官网：<a href="https://linkerd.io/%EF%BD%9C">https://linkerd.io/｜</a> <a href="https://doczhcn.gitbook.io/linkerd/">https://doczhcn.gitbook.io/linkerd/</a></p><p>github地址：<a href="https://github.com/linkerd/linkerd2">https://github.com/linkerd/linkerd2</a></p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 服务网格 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> istio </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【编程语言】Assembly</title>
      <link href="/2021/10/19/assembly/bian-cheng-yu-yan-assembly/"/>
      <url>/2021/10/19/assembly/bian-cheng-yu-yan-assembly/</url>
      
        <content type="html"><![CDATA[<h1 id="【编程语言】Assembly"><a href="#【编程语言】Assembly" class="headerlink" title="【编程语言】Assembly"></a>【编程语言】Assembly</h1>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> assembly </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Sass</title>
      <link href="/2021/10/19/frontend/css/cass/css-yu-bian-yi-sass/"/>
      <url>/2021/10/19/frontend/css/cass/css-yu-bian-yi-sass/</url>
      
        <content type="html"><![CDATA[<h1 id="Sass"><a href="#Sass" class="headerlink" title="Sass"></a>Sass</h1>]]></content>
      
      
      <categories>
          
          <category> Css预编译 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> sass </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【容器编排】Docker Compose</title>
      <link href="/2021/10/19/docker-compose/rong-qi-bian-pai-docker-compose/"/>
      <url>/2021/10/19/docker-compose/rong-qi-bian-pai-docker-compose/</url>
      
        <content type="html"><![CDATA[<h1 id="【容器编排】Docker-Compose"><a href="#【容器编排】Docker-Compose" class="headerlink" title="【容器编排】Docker Compose"></a>【容器编排】Docker Compose</h1><blockquote><p>compose github地址： <a href="https://github.com/docker/compose">https://github.com/docker/compose</a></p></blockquote><h2 id="compose简介"><a href="#compose简介" class="headerlink" title="compose简介"></a>compose简介</h2><p>Compose 是用于定义和运行多容器 Docker 应用程序的工具。</p><h2 id="compose环境配置"><a href="#compose环境配置" class="headerlink" title="compose环境配置"></a>compose环境配置</h2><ul><li><p>linux安装</p><blockquote><p>compose 各版本: <a href="https://github.com/docker/compose/releases">https://github.com/docker/compose/releases</a> </p></blockquote></li></ul><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">sudo curl -L "https://github.com/docker/compose/releases/download/latest/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-composesudo chmod +x /usr/local/bin/docker-compose<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h3 id="compose命令"><a href="#compose命令" class="headerlink" title="compose命令"></a>compose命令</h3><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage:  docker compose [OPTIONS] COMMANDDocker ComposeOptions:      --ansi string                Control when to print ANSI control characters ("never"|"always"|"auto") (default "auto")      --env-file string            Specify an alternate environment file.  -f, --file stringArray           Compose configuration files      --profile stringArray        Specify a profile to enable      --project-directory string   Specify an alternate working directory                                   (default: the path of the Compose file)  -p, --project-name string        Project nameCommands:  build       构建或重新构建服务  convert     Converts the compose file to platform's canonical format  cp          Copy files/folders between a service container and the local filesystem  create      Creates containers for a service.  down        Stop and remove containers, networks  events      Receive real time events from containers.  exec        Execute a command in a running container.  images      列出所有创建的容器所使用的镜像  kill        强制停止服务容器  logs        查看容器日志输出  ls          列出所有运行的compose项目  pause       pause services  port        打印绑定的公共端口  ps          列出所有容器。  pull        拉取服务镜像。  push        推送所有服务的镜像  restart     重启容器  rm          删除停止的服务容器。  run         在一个服务上执行一个命令。  start       启动一个已经存在的服务容器。  stop        停止一个已经运行的容器，但不删除它。  top         Display the running processes  unpause     unpause services  up          构建，（重新）创建，启动，链接一个服务相关的容器。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="compose语法"><a href="#compose语法" class="headerlink" title="compose语法"></a>compose语法</h3><blockquote><p>默认的模板文件是 <code>docker-compose.yml</code>，其中定义的每个服务都必须通过 <code>image</code> 指令指定镜像或 <code>build</code> 指令（需要 Dockerfile）来自动构建。</p></blockquote><p><strong>服务（service）</strong>：一个应用容器，实际上可以运行多个相同镜像的实例。</p><p>**项目(project)**：由一组关联的应用容器组成的一个完整业务单元。</p><h4 id="version"><a href="#version" class="headerlink" title="version"></a>version</h4><p>指定本 yml 依从的 compose 哪个版本制定的。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">version</span><span class="token punctuation">:</span> <span class="token string">"3"</span><span class="token key atrule">version</span><span class="token punctuation">:</span> <span class="token string">"2"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h4 id="image"><a href="#image" class="headerlink" title="image"></a>image</h4><p>指定为镜像名称或镜像 ID。如果镜像在本地不存在，<code>Compose</code> 将会尝试拉去这个镜像。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">image</span><span class="token punctuation">:</span> ubuntu<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h4 id="build"><a href="#build" class="headerlink" title="build"></a>build</h4><p>指定 <code>Dockerfile</code> 所在文件夹的路径。 <code>Compose</code> 将会利用它自动构建这个镜像，然后使用这个镜像。</p><ul><li><strong>context</strong>: 上下文路径</li><li><strong>dockerfile</strong>：指定构建镜像的 Dockerfile 文件名。</li><li><strong>args</strong>：添加构建参数，这是只能在构建过程中访问的环境变量。</li><li><strong>labels</strong>：设置构建镜像的标签。</li><li><strong>target</strong>：多层构建，可以指定构建哪一层。</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">build</span><span class="token punctuation">:</span> /path/to/build/dir<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h4 id="cap-add，cap-drop"><a href="#cap-add，cap-drop" class="headerlink" title="cap_add，cap_drop"></a>cap_add，cap_drop</h4><p>添加或删除容器拥有的宿主机的内核功能。</p><pre class="line-numbers language-none"><code class="language-none">cap_add:  - ALL # 开启全部权限cap_drop:  - SYS_PTRACE # 关闭 ptrace权限<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="cgroup-parent"><a href="#cgroup-parent" class="headerlink" title="cgroup_parent"></a>cgroup_parent</h4><p>为容器指定父 cgroup 组，意味着将继承该组的资源限制。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">cgroup_parent</span><span class="token punctuation">:</span> m<span class="token punctuation">-</span>executor<span class="token punctuation">-</span>abcd<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h4 id="command"><a href="#command" class="headerlink" title="command"></a>command</h4><p>覆盖容器启动的默认命令。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">command</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"bundle"</span><span class="token punctuation">,</span> <span class="token string">"exec"</span><span class="token punctuation">,</span> <span class="token string">"thin"</span><span class="token punctuation">,</span> <span class="token string">"-p"</span><span class="token punctuation">,</span> <span class="token string">"3000"</span><span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h4 id="container-name"><a href="#container-name" class="headerlink" title="container_name"></a>container_name</h4><p>指定自定义容器名称，而不是生成的默认名称。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">container_name</span><span class="token punctuation">:</span> container<span class="token punctuation">-</span>name<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h4 id="depends-on"><a href="#depends-on" class="headerlink" title="depends_on"></a>depends_on</h4><p>设置依赖关系。</p><ul><li>docker-compose up ：以依赖性顺序启动服务。</li><li>docker-compose up SERVICE ：自动包含 SERVICE 的依赖项。</li><li>docker-compose stop ：按依赖关系顺序停止服务。</li></ul><h4 id="deploy"><a href="#deploy" class="headerlink" title="deploy"></a>deploy</h4><p>指定与服务的部署和运行有关的配置。只在 swarm 模式下才会有用。</p><p>可以选参数：</p><ul><li><strong>endpoint_mode</strong>：访问集群服务的方式。</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">endpoint_mode</span><span class="token punctuation">:</span> vip <span class="token comment"># Docker 集群服务一个对外的虚拟 ip。所有的请求都会通过这个虚拟 ip 到达集群服务内部的机器。</span><span class="token key atrule">endpoint_mode</span><span class="token punctuation">:</span> dnsrr<span class="token comment"># DNS 轮询（DNSRR）。所有的请求会自动轮询获取到集群 ip 列表中的一个 ip 地址。</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><ul><li><p><strong>labels</strong>：在服务上设置标签。可以用容器上的 labels（跟 deploy 同级的配置） 覆盖 deploy 下的 labels。</p></li><li><p><strong>mode</strong>：指定服务提供的模式。</p><ul><li><strong>replicated</strong>：复制服务，复制指定服务到集群的机器上。</li><li><strong>global</strong>：全局服务，服务将部署至集群的每个节点。</li></ul></li></ul><ul><li><p><strong>replicas：mode</strong> 为 replicated 时，需要使用此参数配置具体运行的节点数量。</p></li><li><p><strong>resources</strong>：配置服务器资源使用的限制，例如上例子，配置 redis 集群运行需要的 cpu 的百分比 和 内存的占用。避免占用资源过高出现异常。</p></li><li><p><strong>restart_policy</strong>：配置如何在退出容器时重新启动容器。</p><ul><li>condition：可选 none，on-failure 或者 any（默认值：any）。</li><li>delay：设置多久之后重启（默认值：0）。</li><li>max_attempts：尝试重新启动容器的次数，超出次数，则不再尝试（默认值：一直重试）。</li><li>window：设置容器重启超时时间（默认值：0）。</li></ul></li><li><p><strong>rollback_config</strong>：配置在更新失败的情况下应如何回滚服务。</p><ul><li>parallelism：一次要回滚的容器数。如果设置为0，则所有容器将同时回滚。</li><li>delay：每个容器组回滚之间等待的时间（默认为0s）。</li><li>failure_action：如果回滚失败，该怎么办。其中一个 continue 或者 pause（默认pause）。</li><li>monitor：每个容器更新后，持续观察是否失败了的时间 (ns|us|ms|s|m|h)（默认为0s）。</li><li>max_failure_ratio：在回滚期间可以容忍的故障率（默认为0）。</li><li>order：回滚期间的操作顺序。其中一个 stop-first（串行回滚），或者 start-first（并行回滚）（默认 stop-first ）。</li></ul></li><li><p><strong>update_config</strong>：配置应如何更新服务，对于配置滚动更新很有用。</p><ul><li>parallelism：一次更新的容器数。</li><li>delay：在更新一组容器之间等待的时间。</li><li>failure_action：如果更新失败，该怎么办。其中一个 continue，rollback 或者pause （默认：pause）。</li><li>monitor：每个容器更新后，持续观察是否失败了的时间 (ns|us|ms|s|m|h)（默认为0s）。</li><li>max_failure_ratio：在更新过程中可以容忍的故障率。</li><li>order：回滚期间的操作顺序。其中一个 stop-first（串行回滚），或者 start-first（并行回滚）（默认stop-first）。</li></ul></li></ul><p><strong>注</strong>：仅支持 V3.4 及更高版本。</p><h4 id="devices"><a href="#devices" class="headerlink" title="devices"></a>devices</h4><p>指定设备映射列表。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">devices</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span class="token string">"/dev/ttyUSB0:/dev/ttyUSB0"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h4 id="dns"><a href="#dns" class="headerlink" title="dns"></a>dns</h4><p>自定义 DNS 服务器，可以是单个值或列表的多个值。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">dns</span><span class="token punctuation">:</span> 8.8.8.8<span class="token key atrule">dns</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> 8.8.8.8  <span class="token punctuation">-</span> 9.9.9.9<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="dns-search"><a href="#dns-search" class="headerlink" title="dns_search"></a>dns_search</h4><p>自定义 DNS 搜索域。可以是单个值或列表。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">dns_search</span><span class="token punctuation">:</span> example.com<span class="token key atrule">dns_search</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> dc1.example.com  <span class="token punctuation">-</span> dc2.example.com<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="entrypoint"><a href="#entrypoint" class="headerlink" title="entrypoint"></a>entrypoint</h4><p>覆盖容器默认的 entrypoint。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">entrypoint</span><span class="token punctuation">:</span> /code/entrypoint.sh<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>也可以是以下格式：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">entrypoint</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> php    <span class="token punctuation">-</span> <span class="token punctuation">-</span>d    <span class="token punctuation">-</span> zend_extension=/usr/local/lib/php/extensions/no<span class="token punctuation">-</span>debug<span class="token punctuation">-</span>non<span class="token punctuation">-</span>zts<span class="token punctuation">-</span>20100525/xdebug.so    <span class="token punctuation">-</span> <span class="token punctuation">-</span>d    <span class="token punctuation">-</span> memory_limit=<span class="token punctuation">-</span><span class="token number">1</span>    <span class="token punctuation">-</span> vendor/bin/phpunit<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="env-file"><a href="#env-file" class="headerlink" title="env_file"></a>env_file</h4><p>从文件添加环境变量。可以是单个值或列表的多个值。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">env_file</span><span class="token punctuation">:</span> .env<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>也可以是列表格式：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">env_file</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> ./common.env  <span class="token punctuation">-</span> ./apps/web.env  <span class="token punctuation">-</span> /opt/secrets.env<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h4 id="environment"><a href="#environment" class="headerlink" title="environment"></a>environment</h4><p>添加环境变量。您可以使用数组或字典、任何布尔值，布尔值需要用引号引起来，以确保 YML 解析器不会将其转换为 True 或 False。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">environment</span><span class="token punctuation">:</span>  <span class="token key atrule">RACK_ENV</span><span class="token punctuation">:</span> development  <span class="token key atrule">SHOW</span><span class="token punctuation">:</span> <span class="token string">'true'</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h4 id="expose"><a href="#expose" class="headerlink" title="expose"></a>expose</h4><p>暴露端口，但不映射到宿主机，只被连接的服务访问。</p><p>仅可以指定内部端口为参数：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">expose</span><span class="token punctuation">:</span> <span class="token punctuation">-</span> <span class="token string">"3000"</span> <span class="token punctuation">-</span> <span class="token string">"8000"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h4 id="extra-hosts"><a href="#extra-hosts" class="headerlink" title="extra_hosts"></a>extra_hosts</h4><p>添加主机名映射。类似 docker client –add-host。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">extra_hosts</span><span class="token punctuation">:</span> <span class="token punctuation">-</span> <span class="token string">"somehost:162.242.195.82"</span> <span class="token punctuation">-</span> <span class="token string">"otherhost:50.31.209.229"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>以上会在此服务的内部容器中 /etc/hosts 创建一个具有 ip 地址和主机名的映射关系：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">162.242.195.82  somehost50.31.209.229   otherhost<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h4 id="healthcheck"><a href="#healthcheck" class="headerlink" title="healthcheck"></a>healthcheck</h4><p>用于检测 docker 服务是否健康运行。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">healthcheck</span><span class="token punctuation">:</span>  <span class="token key atrule">test</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"CMD"</span><span class="token punctuation">,</span> <span class="token string">"curl"</span><span class="token punctuation">,</span> <span class="token string">"-f"</span><span class="token punctuation">,</span> <span class="token string">"http://localhost"</span><span class="token punctuation">]</span> <span class="token comment"># 设置检测程序</span>  <span class="token key atrule">interval</span><span class="token punctuation">:</span> 1m30s <span class="token comment"># 设置检测间隔</span>  <span class="token key atrule">timeout</span><span class="token punctuation">:</span> 10s <span class="token comment"># 设置检测超时时间</span>  <span class="token key atrule">retries</span><span class="token punctuation">:</span> <span class="token number">3</span> <span class="token comment"># 设置重试次数</span>  <span class="token key atrule">start_period</span><span class="token punctuation">:</span> 40s <span class="token comment"># 启动后，多少秒开始启动检测程序</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="image-1"><a href="#image-1" class="headerlink" title="image"></a>image</h4><p>指定容器运行的镜像。以下格式都可以：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">image</span><span class="token punctuation">:</span> redis<span class="token key atrule">image</span><span class="token punctuation">:</span> ubuntu<span class="token punctuation">:</span><span class="token number">14.04</span><span class="token key atrule">image</span><span class="token punctuation">:</span> tutum/influxdb<span class="token key atrule">image</span><span class="token punctuation">:</span> example<span class="token punctuation">-</span>registry.com<span class="token punctuation">:</span>4000/postgresql<span class="token key atrule">image</span><span class="token punctuation">:</span> a4bc65fd <span class="token comment"># 镜像id</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="logging"><a href="#logging" class="headerlink" title="logging"></a>logging</h4><p>服务的日志记录配置。</p><p>driver：指定服务容器的日志记录驱动程序，默认值为json-file。有以下三个选项</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">driver</span><span class="token punctuation">:</span> <span class="token string">"json-file"</span><span class="token key atrule">driver</span><span class="token punctuation">:</span> <span class="token string">"syslog"</span><span class="token key atrule">driver</span><span class="token punctuation">:</span> <span class="token string">"none"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>仅在 json-file 驱动程序下，可以使用以下参数，限制日志得数量和大小。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">logging</span><span class="token punctuation">:</span>  <span class="token key atrule">driver</span><span class="token punctuation">:</span> json<span class="token punctuation">-</span>file  <span class="token key atrule">options</span><span class="token punctuation">:</span>    <span class="token key atrule">max-size</span><span class="token punctuation">:</span> <span class="token string">"200k"</span> <span class="token comment"># 单个文件大小为200k</span>    <span class="token key atrule">max-file</span><span class="token punctuation">:</span> <span class="token string">"10"</span> <span class="token comment"># 最多10个文件</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当达到文件限制上限，会自动删除旧得文件。</p><p>syslog 驱动程序下，可以使用 syslog-address 指定日志接收地址。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">logging</span><span class="token punctuation">:</span>  <span class="token key atrule">driver</span><span class="token punctuation">:</span> syslog  <span class="token key atrule">options</span><span class="token punctuation">:</span>    <span class="token key atrule">syslog-address</span><span class="token punctuation">:</span> <span class="token string">"tcp://192.168.0.42:123"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h4 id="network-mode"><a href="#network-mode" class="headerlink" title="network_mode"></a>network_mode</h4><p>设置网络模式。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">network_mode</span><span class="token punctuation">:</span> <span class="token string">"bridge"</span><span class="token key atrule">network_mode</span><span class="token punctuation">:</span> <span class="token string">"host"</span><span class="token key atrule">network_mode</span><span class="token punctuation">:</span> <span class="token string">"none"</span><span class="token key atrule">network_mode</span><span class="token punctuation">:</span> <span class="token string">"service:[service name]"</span><span class="token key atrule">network_mode</span><span class="token punctuation">:</span> <span class="token string">"container:[container name/id]"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="networks"><a href="#networks" class="headerlink" title="networks"></a>networks</h4><p>配置容器连接的网络，引用顶级 networks 下的条目 。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">services</span><span class="token punctuation">:</span>  <span class="token key atrule">some-service</span><span class="token punctuation">:</span>    <span class="token key atrule">networks</span><span class="token punctuation">:</span>      <span class="token key atrule">some-network</span><span class="token punctuation">:</span>        <span class="token key atrule">aliases</span><span class="token punctuation">:</span>         <span class="token punctuation">-</span> alias1      <span class="token key atrule">other-network</span><span class="token punctuation">:</span>        <span class="token key atrule">aliases</span><span class="token punctuation">:</span>         <span class="token punctuation">-</span> alias2<span class="token key atrule">networks</span><span class="token punctuation">:</span>  <span class="token key atrule">some-network</span><span class="token punctuation">:</span>    <span class="token comment"># Use a custom driver</span>    <span class="token key atrule">driver</span><span class="token punctuation">:</span> custom<span class="token punctuation">-</span>driver<span class="token punctuation">-</span><span class="token number">1</span>  <span class="token key atrule">other-network</span><span class="token punctuation">:</span>    <span class="token comment"># Use a custom driver which takes special options</span>    <span class="token key atrule">driver</span><span class="token punctuation">:</span> custom<span class="token punctuation">-</span>driver<span class="token punctuation">-</span><span class="token number">2</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>aliases</strong> ：同一网络上的其他容器可以使用服务名称或此别名来连接到对应容器的服务。</p><h4 id="restart"><a href="#restart" class="headerlink" title="restart"></a>restart</h4><ul><li>no：是默认的重启策略，在任何情况下都不会重启容器。</li><li>always：容器总是重新启动。</li><li>on-failure：在容器非正常退出时（退出状态非0），才会重启容器。</li><li>unless-stopped：在容器退出时总是重启容器，但是不考虑在Docker守护进程启动时就已经停止了的容器</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">restart</span><span class="token punctuation">:</span> <span class="token string">"no"</span><span class="token key atrule">restart</span><span class="token punctuation">:</span> always<span class="token key atrule">restart</span><span class="token punctuation">:</span> on<span class="token punctuation">-</span>failure<span class="token key atrule">restart</span><span class="token punctuation">:</span> unless<span class="token punctuation">-</span>stopped<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>注：swarm 集群模式，请改用 restart_policy。</p><h4 id="secrets"><a href="#secrets" class="headerlink" title="secrets"></a>secrets</h4><p>存储敏感数据，例如密码：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">version</span><span class="token punctuation">:</span> <span class="token string">"3.1"</span><span class="token key atrule">services</span><span class="token punctuation">:</span><span class="token key atrule">mysql</span><span class="token punctuation">:</span>  <span class="token key atrule">image</span><span class="token punctuation">:</span> mysql  <span class="token key atrule">environment</span><span class="token punctuation">:</span>    <span class="token key atrule">MYSQL_ROOT_PASSWORD_FILE</span><span class="token punctuation">:</span> /run/secrets/my_secret  <span class="token key atrule">secrets</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> my_secret<span class="token key atrule">secrets</span><span class="token punctuation">:</span>  <span class="token key atrule">my_secret</span><span class="token punctuation">:</span>    <span class="token key atrule">file</span><span class="token punctuation">:</span> ./my_secret.txt<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="security-opt"><a href="#security-opt" class="headerlink" title="security_opt"></a>security_opt</h4><p>修改容器默认的 schema 标签。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">security<span class="token punctuation">-</span>opt：  <span class="token punctuation">-</span> label<span class="token punctuation">:</span>user<span class="token punctuation">:</span>USER   <span class="token comment"># 设置容器的用户标签</span>  <span class="token punctuation">-</span> label<span class="token punctuation">:</span>role<span class="token punctuation">:</span>ROLE   <span class="token comment"># 设置容器的角色标签</span>  <span class="token punctuation">-</span> label<span class="token punctuation">:</span>type<span class="token punctuation">:</span>TYPE   <span class="token comment"># 设置容器的安全策略标签</span>  <span class="token punctuation">-</span> label<span class="token punctuation">:</span>level<span class="token punctuation">:</span>LEVEL  <span class="token comment"># 设置容器的安全等级标签</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="stop-grace-period"><a href="#stop-grace-period" class="headerlink" title="stop_grace_period"></a>stop_grace_period</h4><p>指定在容器无法处理 SIGTERM (或者任何 stop_signal 的信号)，等待多久后发送 SIGKILL 信号关闭容器。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">stop_grace_period</span><span class="token punctuation">:</span> 1s <span class="token comment"># 等待 1 秒</span><span class="token key atrule">stop_grace_period</span><span class="token punctuation">:</span> 1m30s <span class="token comment"># 等待 1 分 30 秒 </span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>默认的等待时间是 10 秒。</p><h4 id="stop-signal"><a href="#stop-signal" class="headerlink" title="stop_signal"></a>stop_signal</h4><p>设置停止容器的替代信号。默认情况下使用 SIGTERM 。</p><p>以下示例，使用 SIGUSR1 替代信号 SIGTERM 来停止容器。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">stop_signal</span><span class="token punctuation">:</span> SIGUSR1<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h4 id="sysctls"><a href="#sysctls" class="headerlink" title="sysctls"></a>sysctls</h4><p>设置容器中的内核参数，可以使用数组或字典格式。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">sysctls</span><span class="token punctuation">:</span>  <span class="token key atrule">net.core.somaxconn</span><span class="token punctuation">:</span> <span class="token number">1024</span>  <span class="token key atrule">net.ipv4.tcp_syncookies</span><span class="token punctuation">:</span> <span class="token number">0</span><span class="token key atrule">sysctls</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> net.core.somaxconn=1024  <span class="token punctuation">-</span> net.ipv4.tcp_syncookies=0<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="tmpfs"><a href="#tmpfs" class="headerlink" title="tmpfs"></a>tmpfs</h4><p>在容器内安装一个临时文件系统。可以是单个值或列表的多个值。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">tmpfs</span><span class="token punctuation">:</span> /run<span class="token key atrule">tmpfs</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> /run  <span class="token punctuation">-</span> /tmp<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="ulimits"><a href="#ulimits" class="headerlink" title="ulimits"></a>ulimits</h4><p>覆盖容器默认的 ulimit。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">ulimits</span><span class="token punctuation">:</span>  <span class="token key atrule">nproc</span><span class="token punctuation">:</span> <span class="token number">65535</span>  <span class="token key atrule">nofile</span><span class="token punctuation">:</span>    <span class="token key atrule">soft</span><span class="token punctuation">:</span> <span class="token number">20000</span>    <span class="token key atrule">hard</span><span class="token punctuation">:</span> <span class="token number">40000</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="volumes"><a href="#volumes" class="headerlink" title="volumes"></a>volumes</h4><p>将主机的数据卷或着文件挂载到容器里。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">version</span><span class="token punctuation">:</span> <span class="token string">"3.7"</span><span class="token key atrule">services</span><span class="token punctuation">:</span>  <span class="token key atrule">db</span><span class="token punctuation">:</span>    <span class="token key atrule">image</span><span class="token punctuation">:</span> postgres<span class="token punctuation">:</span>latest    <span class="token key atrule">volumes</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> <span class="token string">"/localhost/postgres.sock:/var/run/postgres/postgres.sock"</span>      <span class="token punctuation">-</span> <span class="token string">"/localhost/data:/var/lib/postgresql/data"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> 容器编排 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> docker compose </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Flask</title>
      <link href="/2021/10/19/python/flask/web-kuang-jia-flask/"/>
      <url>/2021/10/19/python/flask/web-kuang-jia-flask/</url>
      
        <content type="html"><![CDATA[<h1 id="【web框架】Flask"><a href="#【web框架】Flask" class="headerlink" title="【web框架】Flask"></a>【web框架】Flask</h1>]]></content>
      
      
      <categories>
          
          <category> web框架 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> flask </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Django</title>
      <link href="/2021/10/19/python/django/web-kuang-jia-django/"/>
      <url>/2021/10/19/python/django/web-kuang-jia-django/</url>
      
        <content type="html"><![CDATA[<h1 id="【web框架】Django"><a href="#【web框架】Django" class="headerlink" title="【web框架】Django"></a>【web框架】Django</h1>]]></content>
      
      
      <categories>
          
          <category> web框架 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> Django </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【Helm管理】Helmfile</title>
      <link href="/2021/10/19/helm/helmfile/helm-guan-li-helmfile/"/>
      <url>/2021/10/19/helm/helmfile/helm-guan-li-helmfile/</url>
      
        <content type="html"><![CDATA[<h1 id="Helmfile"><a href="#Helmfile" class="headerlink" title="Helmfile"></a>Helmfile</h1><blockquote><p>helmfile github 地址： <a href="https://github.com/roboll/helmfile">https://github.com/roboll/helmfile</a></p></blockquote><h2 id="Helmfile简介"><a href="#Helmfile简介" class="headerlink" title="Helmfile简介"></a>Helmfile简介</h2><p>helmfile通过<code>helmfile.yaml</code>文件帮助管理和维护众多的helm chart。</p><p><strong>helmfile作用：</strong></p><ul><li><p>集成在CICD中，提高部署的可观测性和可重复性，区分环境，同时避免过多使用–set造成的混淆</p></li><li><p>方便对helm chart进行版本控制（指定版本范围，锁定版本）</p></li><li><p>定期同步，避免环境中出现不符合预期的配置</p></li></ul><h2 id="helmfile环境配置"><a href="#helmfile环境配置" class="headerlink" title="helmfile环境配置"></a>helmfile环境配置</h2><h3 id="helmfile安装"><a href="#helmfile安装" class="headerlink" title="helmfile安装"></a>helmfile安装</h3><ul><li><p>archlinux系统</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">pacman -S helmfile<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>openSUSE系统</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">zypper</span> <span class="token keyword">in</span> helmfile<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>MacOS系统</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">brew <span class="token function">install</span> helmfile<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>Windows系统</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">scoop <span class="token function">install</span> helmfile<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>基于镜像安装</p></li></ul><h3 id="依赖安装"><a href="#依赖安装" class="headerlink" title="依赖安装"></a>依赖安装</h3><p>除了安装 helmfile 以外，还需要安装 helm、kubectl 以及 helm 插件 helm-diff</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">helm plugin <span class="token function">install</span> https://github.com/databus23/helm-diff<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="helmfile语法"><a href="#helmfile语法" class="headerlink" title="helmfile语法"></a>helmfile语法</h2><p>helmfile.yaml 是helmfile的核心文件，所有的配置都是基于此文件。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># Chart repositories used from within this state file</span><span class="token comment">#</span><span class="token comment"># Use `helm-s3` and `helm-git` and whatever Helm Downloader plugins</span><span class="token comment"># to use repositories other than the official repository or one backend by chartmuseum.</span><span class="token key atrule">repositories</span><span class="token punctuation">:</span><span class="token comment"># To use official "stable" charts a.k.a https://github.com/helm/charts/tree/master/stable</span><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> stable  <span class="token key atrule">url</span><span class="token punctuation">:</span> https<span class="token punctuation">:</span>//charts.helm.sh/stable<span class="token comment"># To use official "incubator" charts a.k.a https://github.com/helm/charts/tree/master/incubator</span><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> incubator  <span class="token key atrule">url</span><span class="token punctuation">:</span> https<span class="token punctuation">:</span>//charts.helm.sh/incubator<span class="token comment"># helm-git powered repository: You can treat any Git repository as a charts repository</span><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> polaris  <span class="token key atrule">url</span><span class="token punctuation">:</span> git+https<span class="token punctuation">:</span>//github.com/reactiveops/polaris@deploy/helm<span class="token punctuation">?</span>ref=master<span class="token comment"># Advanced configuration: You can setup basic or tls auth and optionally enable helm OCI integration</span><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> roboll  <span class="token key atrule">url</span><span class="token punctuation">:</span> http<span class="token punctuation">:</span>//roboll.io/charts  <span class="token key atrule">certFile</span><span class="token punctuation">:</span> optional_client_cert  <span class="token key atrule">keyFile</span><span class="token punctuation">:</span> optional_client_key  <span class="token key atrule">username</span><span class="token punctuation">:</span> optional_username  <span class="token key atrule">password</span><span class="token punctuation">:</span> optional_password  <span class="token key atrule">oci</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token key atrule">passCredentials</span><span class="token punctuation">:</span> <span class="token boolean important">true</span><span class="token comment"># Advanced configuration: You can use a ca bundle to use an https repo</span><span class="token comment"># with a self-signed certificate</span><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> insecure   <span class="token key atrule">url</span><span class="token punctuation">:</span> https<span class="token punctuation">:</span>//charts.my<span class="token punctuation">-</span>insecure<span class="token punctuation">-</span>domain.com   <span class="token key atrule">caFile</span><span class="token punctuation">:</span> optional_ca_crt<span class="token comment"># context: kube-context # this directive is deprecated, please consider using helmDefaults.kubeContext</span><span class="token comment"># 替代helm二进制文件的路径 (--helm-binary)</span><span class="token key atrule">helmBinary</span><span class="token punctuation">:</span> path/to/helm3<span class="token comment"># Default values to set for args along with dedicated keys that can be set by contributors, cli args take precedence over these.</span><span class="token comment"># In other words, unset values results in no flags passed to helm.</span><span class="token comment"># See the helm usage (helm SUBCOMMAND -h) for more info on default values when those flags aren't provided.</span><span class="token key atrule">helmDefaults</span><span class="token punctuation">:</span>  <span class="token key atrule">tillerNamespace</span><span class="token punctuation">:</span> tiller<span class="token punctuation">-</span>namespace  <span class="token comment">#dedicated default key for tiller-namespace</span>  <span class="token key atrule">tillerless</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>                  <span class="token comment">#dedicated default key for tillerless</span>  <span class="token key atrule">kubeContext</span><span class="token punctuation">:</span> kube<span class="token punctuation">-</span>context          <span class="token comment">#dedicated default key for kube-context (--kube-context)</span>  <span class="token key atrule">cleanupOnFail</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>               <span class="token comment">#dedicated default key for helm flag --cleanup-on-fail</span>  <span class="token comment"># additional and global args passed to helm (default "")</span>  <span class="token key atrule">args</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> <span class="token string">"--set k=v"</span>  <span class="token comment"># verify the chart before upgrading (only works with packaged charts not directories) (default false)</span>  <span class="token key atrule">verify</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token comment"># wait for k8s resources via --wait. (default false)</span>  <span class="token key atrule">wait</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token comment"># if set and --wait enabled, will wait until all Jobs have been completed before marking the release as successful. It will wait for as long as --timeout (default false, Implemented in Helm3.5)</span>  <span class="token key atrule">waitForJobs</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token comment"># time in seconds to wait for any individual Kubernetes operation (like Jobs for hooks, and waits on pod/pvc/svc/deployment readiness) (default 300)</span>  <span class="token key atrule">timeout</span><span class="token punctuation">:</span> <span class="token number">600</span>  <span class="token comment"># performs pods restart for the resource if applicable (default false)</span>  <span class="token key atrule">recreatePods</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token comment"># forces resource update through delete/recreate if needed (default false)</span>  <span class="token key atrule">force</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>  <span class="token comment"># enable TLS for request to Tiller (default false)</span>  <span class="token key atrule">tls</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token comment"># path to TLS CA certificate file (default "$HELM_HOME/ca.pem")</span>  <span class="token key atrule">tlsCACert</span><span class="token punctuation">:</span> <span class="token string">"path/to/ca.pem"</span>  <span class="token comment"># path to TLS certificate file (default "$HELM_HOME/cert.pem")</span>  <span class="token key atrule">tlsCert</span><span class="token punctuation">:</span> <span class="token string">"path/to/cert.pem"</span>  <span class="token comment"># path to TLS key file (default "$HELM_HOME/key.pem")</span>  <span class="token key atrule">tlsKey</span><span class="token punctuation">:</span> <span class="token string">"path/to/key.pem"</span>  <span class="token comment"># limit the maximum number of revisions saved per release. Use 0 for no limit. (default 10)</span>  <span class="token key atrule">historyMax</span><span class="token punctuation">:</span> <span class="token number">10</span>  <span class="token comment"># when using helm 3.2+, automatically create release namespaces if they do not exist (default true)</span>  <span class="token key atrule">createNamespace</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token comment"># if used with charts museum allows to pull unstable charts for deployment, for example: if 1.2.3 and 1.2.4-dev versions exist and set to true, 1.2.4-dev will be pulled (default false)</span>  <span class="token key atrule">devel</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token comment"># When set to `true`, skips running `helm dep up` and `helm dep build` on this release's chart.</span>  <span class="token comment"># Useful when the chart is broken, like seen in https://github.com/roboll/helmfile/issues/1547</span>  <span class="token key atrule">skipDeps</span><span class="token punctuation">:</span> <span class="token boolean important">false</span><span class="token comment"># these labels will be applied to all releases in a Helmfile. Useful in templating if you have a helmfile per environment or customer and don't want to copy the same label to each release</span><span class="token key atrule">commonLabels</span><span class="token punctuation">:</span>  <span class="token key atrule">hello</span><span class="token punctuation">:</span> world<span class="token comment"># The desired states of Helm releases.</span><span class="token comment">#</span><span class="token comment"># Helmfile runs various helm commands to converge the current state in the live cluster to the desired state defined here.</span><span class="token key atrule">releases</span><span class="token punctuation">:</span>  <span class="token comment"># Published chart example</span>  <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> vault                            <span class="token comment"># name of this release</span>    <span class="token key atrule">namespace</span><span class="token punctuation">:</span> vault                       <span class="token comment"># target namespace</span>    <span class="token key atrule">createNamespace</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>                  <span class="token comment"># helm 3.2+ automatically create release namespace (default true)</span>    <span class="token key atrule">labels</span><span class="token punctuation">:</span>                                <span class="token comment"># Arbitrary key value pairs for filtering releases</span>      <span class="token key atrule">foo</span><span class="token punctuation">:</span> bar    <span class="token key atrule">chart</span><span class="token punctuation">:</span> roboll/vault<span class="token punctuation">-</span>secret<span class="token punctuation">-</span>manager     <span class="token comment"># the chart being installed to create this release, referenced by `repository/chart` syntax</span>    <span class="token key atrule">version</span><span class="token punctuation">:</span> ~1.24.1                       <span class="token comment"># the semver of the chart. range constraint is supported</span>    <span class="token key atrule">condition</span><span class="token punctuation">:</span> vault.enabled               <span class="token comment"># The values lookup key for filtering releases. Corresponds to the boolean value of `vault.enabled`, where `vault` is an arbitrary value</span>    <span class="token key atrule">missingFileHandler</span><span class="token punctuation">:</span> Warn <span class="token comment"># set to either "Error" or "Warn". "Error" instructs helmfile to fail when unable to find a values or secrets file. When "Warn", it prints the file and continues.</span>    <span class="token comment"># Values files used for rendering the chart</span>    <span class="token key atrule">values</span><span class="token punctuation">:</span>      <span class="token comment"># Value files passed via --values</span>      <span class="token punctuation">-</span> vault.yaml      <span class="token comment"># Inline values, passed via a temporary values file and --values, so that it doesn't suffer from type issues like --set</span>      <span class="token punctuation">-</span> <span class="token key atrule">address</span><span class="token punctuation">:</span> https<span class="token punctuation">:</span>//vault.example.com      <span class="token comment"># Go template available in inline values and values files.</span>      <span class="token punctuation">-</span> <span class="token key atrule">image</span><span class="token punctuation">:</span>          <span class="token comment"># The end result is more or less YAML. So do `quote` to prevent number-like strings from accidentally parsed into numbers!</span>          <span class="token comment"># See https://github.com/roboll/helmfile/issues/608</span>          <span class="token key atrule">tag</span><span class="token punctuation">:</span> <span class="token punctuation">{</span><span class="token punctuation">{</span> requiredEnv "IMAGE_TAG" <span class="token punctuation">|</span> quote <span class="token punctuation">}</span><span class="token punctuation">}</span>          <span class="token comment"># Otherwise:</span>          <span class="token comment">#   tag: "{{ requiredEnv "IMAGE_TAG" }}"</span>          <span class="token comment">#   tag: !!string {{ requiredEnv "IMAGE_TAG" }}</span>        <span class="token key atrule">db</span><span class="token punctuation">:</span>          <span class="token key atrule">username</span><span class="token punctuation">:</span> <span class="token punctuation">{</span><span class="token punctuation">{</span> requiredEnv "DB_USERNAME" <span class="token punctuation">}</span><span class="token punctuation">}</span>          <span class="token comment"># value taken from environment variable. Quotes are necessary. Will throw an error if the environment variable is not set. $DB_PASSWORD needs to be set in the calling environment ex: export DB_PASSWORD='password1'</span>          <span class="token key atrule">password</span><span class="token punctuation">:</span> <span class="token punctuation">{</span><span class="token punctuation">{</span> requiredEnv "DB_PASSWORD" <span class="token punctuation">}</span><span class="token punctuation">}</span>        <span class="token key atrule">proxy</span><span class="token punctuation">:</span>          <span class="token comment"># Interpolate environment variable with a fixed string</span>          <span class="token key atrule">domain</span><span class="token punctuation">:</span> <span class="token punctuation">{</span><span class="token punctuation">{</span> requiredEnv "PLATFORM_ID" <span class="token punctuation">}</span><span class="token punctuation">}</span>.my<span class="token punctuation">-</span>domain.com          <span class="token key atrule">scheme</span><span class="token punctuation">:</span> <span class="token punctuation">{</span><span class="token punctuation">{</span> env "SCHEME" <span class="token punctuation">|</span> default "https" <span class="token punctuation">}</span><span class="token punctuation">}</span>    <span class="token comment"># Use `values` whenever possible!</span>    <span class="token comment"># `set` translates to helm's `--set key=val`, that is known to suffer from type issues like https://github.com/roboll/helmfile/issues/608</span>    <span class="token key atrule">set</span><span class="token punctuation">:</span>    <span class="token comment"># single value loaded from a local file, translates to --set-file foo.config=path/to/file</span>    <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> foo.config      <span class="token key atrule">file</span><span class="token punctuation">:</span> path/to/file    <span class="token comment"># set a single array value in an array, translates to --set bar[0]={1,2}</span>    <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> bar<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span>      <span class="token key atrule">values</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> <span class="token number">1</span>      <span class="token punctuation">-</span> <span class="token number">2</span>    <span class="token comment"># set a templated value</span>    <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> namespace      <span class="token key atrule">value</span><span class="token punctuation">:</span> <span class="token punctuation">{</span><span class="token punctuation">{</span> .Namespace <span class="token punctuation">}</span><span class="token punctuation">}</span>    <span class="token comment"># will attempt to decrypt it using helm-secrets plugin</span>    <span class="token key atrule">secrets</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> vault_secret.yaml    <span class="token comment"># Override helmDefaults options for verify, wait, waitForJobs, timeout, recreatePods and force.</span>    <span class="token key atrule">verify</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>    <span class="token key atrule">wait</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>    <span class="token key atrule">waitForJobs</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>    <span class="token key atrule">timeout</span><span class="token punctuation">:</span> <span class="token number">60</span>    <span class="token key atrule">recreatePods</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>    <span class="token key atrule">force</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>    <span class="token comment"># set `false` to uninstall this release on sync.  (default true)</span>    <span class="token key atrule">installed</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>    <span class="token comment"># restores previous state in case of failed release (default false)</span>    <span class="token key atrule">atomic</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>    <span class="token comment"># when true, cleans up any new resources created during a failed release (default false)</span>    <span class="token key atrule">cleanupOnFail</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>    <span class="token comment"># name of the tiller namespace (default "")</span>    <span class="token key atrule">tillerNamespace</span><span class="token punctuation">:</span> vault    <span class="token comment"># if true, will use the helm-tiller plugin (default false)</span>    <span class="token key atrule">tillerless</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>    <span class="token comment"># enable TLS for request to Tiller (default false)</span>    <span class="token key atrule">tls</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>    <span class="token comment"># path to TLS CA certificate file (default "$HELM_HOME/ca.pem")</span>    <span class="token key atrule">tlsCACert</span><span class="token punctuation">:</span> <span class="token string">"path/to/ca.pem"</span>    <span class="token comment"># path to TLS certificate file (default "$HELM_HOME/cert.pem")</span>    <span class="token key atrule">tlsCert</span><span class="token punctuation">:</span> <span class="token string">"path/to/cert.pem"</span>    <span class="token comment"># path to TLS key file (default "$HELM_HOME/key.pem")</span>    <span class="token key atrule">tlsKey</span><span class="token punctuation">:</span> <span class="token string">"path/to/key.pem"</span>    <span class="token comment"># --kube-context to be passed to helm commands</span>    <span class="token comment"># CAUTION: this doesn't work as expected for `tilerless: true`.</span>    <span class="token comment"># See https://github.com/roboll/helmfile/issues/642</span>    <span class="token comment"># (default "", which means the standard kubeconfig, either ~/kubeconfig or the file pointed by $KUBECONFIG environment variable)</span>    <span class="token key atrule">kubeContext</span><span class="token punctuation">:</span> kube<span class="token punctuation">-</span>context    <span class="token comment"># passes --disable-validation to helm 3 diff plugin, this requires diff plugin &gt;= 3.1.2</span>    <span class="token comment"># It may be helpful to deploy charts with helm api v1 CRDS</span>    <span class="token comment"># https://github.com/roboll/helmfile/pull/1373</span>    <span class="token key atrule">disableValidation</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>    <span class="token comment"># passes --disable-validation to helm 3 diff plugin, this requires diff plugin &gt;= 3.1.2</span>    <span class="token comment"># It is useful when any release contains custom resources for CRDs that is not yet installed onto the cluster.</span>    <span class="token comment"># https://github.com/roboll/helmfile/pull/1618</span>    <span class="token key atrule">disableValidationOnInstall</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>    <span class="token comment"># passes --disable-openapi-validation to helm 3 diff plugin, this requires diff plugin &gt;= 3.1.2</span>    <span class="token comment"># It may be helpful to deploy charts with helm api v1 CRDS</span>    <span class="token comment"># https://github.com/roboll/helmfile/pull/1373</span>    <span class="token key atrule">disableOpenApiValidation</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>    <span class="token comment"># limit the maximum number of revisions saved per release. Use 0 for no limit (default 10)</span>    <span class="token key atrule">historyMax</span><span class="token punctuation">:</span> <span class="token number">10</span>    <span class="token comment"># When set to `true`, skips running `helm dep up` and `helm dep build` on this release's chart.</span>    <span class="token comment"># Useful when the chart is broken, like seen in https://github.com/roboll/helmfile/issues/1547</span>    <span class="token key atrule">skipDeps</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>  <span class="token comment"># Local chart example</span>  <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> grafana                            <span class="token comment"># name of this release</span>    <span class="token key atrule">namespace</span><span class="token punctuation">:</span> another                       <span class="token comment"># target namespace</span>    <span class="token key atrule">chart</span><span class="token punctuation">:</span> ../my<span class="token punctuation">-</span>charts/grafana              <span class="token comment"># the chart being installed to create this release, referenced by relative path to local helmfile</span>    <span class="token key atrule">values</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> <span class="token string">"../../my-values/grafana/values.yaml"</span>             <span class="token comment"># Values file (relative path to manifest)</span>    <span class="token punctuation">-</span> ./values/<span class="token punctuation">{</span><span class="token punctuation">{</span> requiredEnv "PLATFORM_ENV" <span class="token punctuation">}</span><span class="token punctuation">}</span>/config.yaml <span class="token comment"># Values file taken from path with environment variable. $PLATFORM_ENV must be set in the calling environment.</span>    <span class="token key atrule">wait</span><span class="token punctuation">:</span> <span class="token boolean important">true</span><span class="token comment">#</span><span class="token comment"># Advanced Configuration: Nested States</span><span class="token comment">#</span><span class="token key atrule">helmfiles</span><span class="token punctuation">:</span><span class="token punctuation">-</span> <span class="token comment"># Path to the helmfile state file being processed BEFORE releases in this state file</span>  <span class="token key atrule">path</span><span class="token punctuation">:</span> path/to/subhelmfile.yaml  <span class="token comment"># Label selector used for filtering releases in the nested state.</span>  <span class="token comment"># For example, `name=prometheus` in this context is equivalent to processing the nested state like</span>  <span class="token comment">#   helmfile -f path/to/subhelmfile.yaml -l name=prometheus sync</span>  <span class="token key atrule">selectors</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> name=prometheus  <span class="token comment"># Override state values</span>  <span class="token key atrule">values</span><span class="token punctuation">:</span>  <span class="token comment"># Values files merged into the nested state's values</span>  <span class="token punctuation">-</span> additional.values.yaml  <span class="token comment"># One important aspect of using values here is that they first need to be defined in the values section</span>  <span class="token comment"># of the origin helmfile, so in this example key1 needs to be in the values or environments.NAME.values of path/to/subhelmfile.yaml</span>  <span class="token comment"># Inline state values merged into the nested state's values</span>  <span class="token punctuation">-</span> <span class="token key atrule">key1</span><span class="token punctuation">:</span> val1<span class="token punctuation">-</span> <span class="token comment"># All the nested state files under `helmfiles:` is processed in the order of definition.</span>  <span class="token comment"># So it can be used for preparation for your main `releases`. An example would be creating CRDs required by `releases` in the parent state file.</span>  <span class="token key atrule">path</span><span class="token punctuation">:</span> path/to/mycrd.helmfile.yaml<span class="token punctuation">-</span> <span class="token comment"># Terraform-module-like URL for importing a remote directory and use a file in it as a nested-state file</span>  <span class="token comment"># The nested-state file is locally checked-out along with the remote directory containing it.</span>  <span class="token comment"># Therefore all the local paths in the file are resolved relative to the file</span>  <span class="token key atrule">path</span><span class="token punctuation">:</span> git<span class="token punctuation">:</span><span class="token punctuation">:</span>https<span class="token punctuation">:</span>//github.com/cloudposse/helmfiles.git@releases/kiam.yaml<span class="token punctuation">?</span>ref=0.40.0<span class="token comment"># If set to "Error", return an error when a subhelmfile points to a</span><span class="token comment"># non-existent path. The default behavior is to print a warning and continue.</span><span class="token key atrule">missingFileHandler</span><span class="token punctuation">:</span> Error<span class="token comment">#</span><span class="token comment"># Advanced Configuration: Environments</span><span class="token comment">#</span><span class="token comment"># The list of environments managed by helmfile.</span><span class="token comment">#</span><span class="token comment"># The default is `environments: {"default": {}}` which implies:</span><span class="token comment">#</span><span class="token comment"># - `{{ .Environment.Name }}` evaluates to "default"</span><span class="token comment"># - `{{ .Values }}` being empty</span><span class="token key atrule">environments</span><span class="token punctuation">:</span>  <span class="token comment"># The "default" environment is available and used when `helmfile` is run without `--environment NAME`.</span>  <span class="token key atrule">default</span><span class="token punctuation">:</span>    <span class="token comment"># Everything from the values.yaml is available via `{{ .Values.KEY }}`.</span>    <span class="token comment"># Suppose `{"foo": {"bar": 1}}` contained in the values.yaml below,</span>    <span class="token comment"># `{{ .Values.foo.bar }}` is evaluated to `1`.</span>    <span class="token key atrule">values</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> environments/default/values.yaml    <span class="token comment"># Each entry in values can be either a file path or inline values.</span>    <span class="token comment"># The below is an example of inline values, which is merged to the `.Values`</span>    <span class="token punctuation">-</span> <span class="token key atrule">myChartVer</span><span class="token punctuation">:</span> 1.0.0<span class="token punctuation">-</span>dev  <span class="token comment"># Any environment other than `default` is used only when `helmfile` is run with `--environment NAME`.</span>  <span class="token comment"># That is, the "production" env below is used when and only when it is run like `helmfile --environment production sync`.</span>  <span class="token key atrule">production</span><span class="token punctuation">:</span>    <span class="token key atrule">values</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> environments/production/values.yaml    <span class="token punctuation">-</span> <span class="token key atrule">myChartVer</span><span class="token punctuation">:</span> 1.0.0    <span class="token comment"># disable vault release processing</span>    <span class="token punctuation">-</span> <span class="token key atrule">vault</span><span class="token punctuation">:</span>        <span class="token key atrule">enabled</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>    <span class="token comment">## `secrets.yaml` is decrypted by `helm-secrets` and available via `{{ .Environment.Values.KEY }}`</span>    <span class="token key atrule">secrets</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> environments/production/secrets.yaml    <span class="token comment"># Instructs helmfile to fail when unable to find a environment values file listed under `environments.NAME.values`.</span>    <span class="token comment">#</span>    <span class="token comment"># Possible values are  "Error", "Warn", "Info", "Debug". The default is "Error".</span>    <span class="token comment">#</span>    <span class="token comment"># Use "Warn", "Info", or "Debug" if you want helmfile to not fail when a values file is missing, while just leaving</span>    <span class="token comment"># a message about the missing file at the log-level.</span>    <span class="token key atrule">missingFileHandler</span><span class="token punctuation">:</span> Error    <span class="token comment"># kubeContext to use for this environment</span>    <span class="token key atrule">kubeContext</span><span class="token punctuation">:</span> kube<span class="token punctuation">-</span>context<span class="token comment">#</span><span class="token comment"># Advanced Configuration: Layering</span><span class="token comment">#</span><span class="token comment"># Helmfile merges all the "base" state files and this state file before processing.</span><span class="token comment">#</span><span class="token comment"># Assuming this state file is named `helmfile.yaml`, all the files are merged in the order of:</span><span class="token comment">#   environments.yaml &lt;- defaults.yaml &lt;- templates.yaml &lt;- helmfile.yaml</span><span class="token key atrule">bases</span><span class="token punctuation">:</span><span class="token punctuation">-</span> environments.yaml<span class="token punctuation">-</span> defaults.yaml<span class="token punctuation">-</span> templates.yaml<span class="token comment">#</span><span class="token comment"># Advanced Configuration: API Capabilities</span><span class="token comment">#</span><span class="token comment"># 'helmfile template' renders releases locally without querying an actual cluster,</span><span class="token comment"># and in this case `.Capabilities.APIVersions` cannot be populated.</span><span class="token comment"># When a chart queries for a specific CRD, this can lead to unexpected results.</span><span class="token comment">#</span><span class="token comment"># Configure a fixed list of api versions to pass to 'helm template' via the --api-versions flag:</span><span class="token key atrule">apiVersions</span><span class="token punctuation">:</span><span class="token punctuation">-</span> example/v1<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="helmfile命令"><a href="#helmfile命令" class="headerlink" title="helmfile命令"></a>helmfile命令</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">NAME:   helmfileUSAGE:   helmfile <span class="token punctuation">[</span>global options<span class="token punctuation">]</span> <span class="token builtin class-name">command</span> <span class="token punctuation">[</span>command options<span class="token punctuation">]</span> <span class="token punctuation">[</span>arguments<span class="token punctuation">..</span>.<span class="token punctuation">]</span>VERSION:   v0.140.1COMMANDS:   deps          update charts based on their requirements   repos         <span class="token function">sync</span> repositories from state <span class="token function">file</span> <span class="token punctuation">(</span>helm repo <span class="token function">add</span> <span class="token operator">&amp;&amp;</span> helm repo update<span class="token punctuation">)</span>   charts        DEPRECATED: <span class="token function">sync</span> releases from state <span class="token function">file</span> <span class="token punctuation">(</span>helm upgrade --install<span class="token punctuation">)</span>   <span class="token function">diff</span>          <span class="token function">diff</span> releases from state <span class="token function">file</span> against <span class="token function">env</span> <span class="token punctuation">(</span>helm <span class="token function">diff</span><span class="token punctuation">)</span>   template      template releases from state <span class="token function">file</span> against <span class="token function">env</span> <span class="token punctuation">(</span>helm template<span class="token punctuation">)</span>   write-values  <span class="token function">write</span> values files <span class="token keyword">for</span> releases. Similar to <span class="token variable"><span class="token variable">`</span>helmfile template<span class="token variable">`</span></span>, <span class="token function">write</span> values files instead of manifests.   lint          lint charts from state <span class="token function">file</span> <span class="token punctuation">(</span>helm lint<span class="token punctuation">)</span>   fetch         fetch charts from state <span class="token function">file</span>   <span class="token function">sync</span>          <span class="token function">sync</span> all resources from state <span class="token function">file</span> <span class="token punctuation">(</span>repos, releases and chart deps<span class="token punctuation">)</span>   apply         apply all resources from state <span class="token function">file</span> only when there are changes   status        retrieve status of releases <span class="token keyword">in</span> state <span class="token function">file</span>   delete        DEPRECATED: delete releases from state <span class="token function">file</span> <span class="token punctuation">(</span>helm delete<span class="token punctuation">)</span>   destroy       deletes and <span class="token keyword">then</span> purges releases   <span class="token builtin class-name">test</span>          <span class="token builtin class-name">test</span> releases from state <span class="token function">file</span> <span class="token punctuation">(</span>helm <span class="token builtin class-name">test</span><span class="token punctuation">)</span>   build         output compiled helmfile state<span class="token punctuation">(</span>s<span class="token punctuation">)</span> as YAML   list          list releases defined <span class="token keyword">in</span> state <span class="token function">file</span>   version       Show the version <span class="token keyword">for</span> Helmfile.   help, h       Shows a list of commands or <span class="token builtin class-name">help</span> <span class="token keyword">for</span> one <span class="token builtin class-name">command</span>GLOBAL OPTIONS:   --helm-binary value, -b value           helm二进制文件路径 <span class="token punctuation">(</span>default: <span class="token string">"helm"</span><span class="token punctuation">)</span>   --file helmfile.yaml, -f helmfile.yaml  从文件或目录加载配置。默认为helmfile.yaml or <span class="token variable"><span class="token variable">`</span>helmfile.d<span class="token variable">`</span></span><span class="token punctuation">(</span>means <span class="token variable"><span class="token variable">`</span>helmfile.d/*.yaml<span class="token variable">`</span></span><span class="token punctuation">)</span> <span class="token keyword">in</span> this preference   --environment value, -e value           specify the environment name. defaults to <span class="token string">"default"</span>   --state-values-set value                <span class="token builtin class-name">set</span> state values on the <span class="token builtin class-name">command</span> line <span class="token punctuation">(</span>can specify multiple or separate values with commas: <span class="token assign-left variable">key1</span><span class="token operator">=</span>val1,key2<span class="token operator">=</span>val2<span class="token punctuation">)</span>   --state-values-file value               specify state values <span class="token keyword">in</span> a YAML <span class="token function">file</span>   --quiet, -q                             Silence output. Equivalent to log-level warn   --kube-context value                    Set kubectl context. Uses current context by default   --debug                                 Enable verbose output <span class="token keyword">for</span> Helm and <span class="token builtin class-name">set</span> log-level to debug, this disables --quiet/-q effect   --no-color                              Output without color   --log-level value                       Set log level, default info   --namespace value, -n value             Set namespace. Uses the namespace <span class="token builtin class-name">set</span> <span class="token keyword">in</span> the context by default, and is available <span class="token keyword">in</span> templates as <span class="token punctuation">{</span><span class="token punctuation">{</span> .Namespace <span class="token punctuation">}</span><span class="token punctuation">}</span>   --chart value, -c value                 Set chart. Uses the chart <span class="token builtin class-name">set</span> <span class="token keyword">in</span> release by default, and is available <span class="token keyword">in</span> template as <span class="token punctuation">{</span><span class="token punctuation">{</span> .Chart <span class="token punctuation">}</span><span class="token punctuation">}</span>   --selector value, -l value              Only run using the releases that match labels. Labels can take the form of <span class="token assign-left variable">foo</span><span class="token operator">=</span>bar or foo<span class="token operator">!=</span>bar.                                           A release must match all labels <span class="token keyword">in</span> a group <span class="token keyword">in</span> order to be used. Multiple <span class="token function">groups</span> can be specified at once.                                           --selector <span class="token assign-left variable">tier</span><span class="token operator">=</span>frontend,tier<span class="token operator">!=</span>proxy --selector <span class="token assign-left variable">tier</span><span class="token operator">=</span>backend. Will match all frontend, non-proxy releases AND all backend releases.                                           The name of a release can be used as a label. --selector <span class="token assign-left variable">name</span><span class="token operator">=</span>myrelease   --allow-no-matching-release             Do not <span class="token builtin class-name">exit</span> with an error code <span class="token keyword">if</span> the provided selector has no matching releases.   --interactive, -i                       Request confirmation before attempting to modify clusters   --help, -h                              show <span class="token builtin class-name">help</span>   --version, -v                           print the version<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> Helm管理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> helmfile </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Json</title>
      <link href="/2021/10/19/json/shu-ju-jiao-huan-ge-shi-json/"/>
      <url>/2021/10/19/json/shu-ju-jiao-huan-ge-shi-json/</url>
      
        <content type="html"><![CDATA[<h1 id="【数据交换格式】Json"><a href="#【数据交换格式】Json" class="headerlink" title="【数据交换格式】Json"></a>【数据交换格式】Json</h1>]]></content>
      
      
      <categories>
          
          <category> 数据交换格式 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> json </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Yaml</title>
      <link href="/2021/10/19/yaml/shu-ju-jiao-huan-ge-shi-yaml/"/>
      <url>/2021/10/19/yaml/shu-ju-jiao-huan-ge-shi-yaml/</url>
      
        <content type="html"><![CDATA[<h1 id="【数据交换格式】Yaml"><a href="#【数据交换格式】Yaml" class="headerlink" title="【数据交换格式】Yaml"></a>【数据交换格式】Yaml</h1><blockquote><p>yaml 官网: <a href="https://yaml.org/">https://yaml.org/</a></p></blockquote><h2 id="Yaml简介"><a href="#Yaml简介" class="headerlink" title="Yaml简介"></a>Yaml简介</h2><p>YAML是一种人性化、跨语言、基于 Unicode 的数据序列化语言，它围绕动态编程语言的常见本机数据类型而设计。它对于从配置文件到 Internet 消息传递到对象持久性再到数据审计和可视化的编程需求非常有用。</p><h2 id="Yaml基本语法"><a href="#Yaml基本语法" class="headerlink" title="Yaml基本语法"></a>Yaml基本语法</h2><ul><li>大小写敏感</li><li>使用缩进表示层级关系<ul><li>缩进时不允许使用Tab键，只允许使用空格</li><li>缩进的空格数量不重要，但是同一层级的元素左侧必须对齐</li></ul></li><li>使用#表示注释<ul><li>只支持单行注释</li></ul></li><li>一个文件中可以包含多个文件的内容<ul><li>用<code>---</code>表示内容的开始</li><li>用<code>...</code>表示内容的结束（可选）</li></ul></li></ul><h2 id="Yaml数据结构"><a href="#Yaml数据结构" class="headerlink" title="Yaml数据结构"></a>Yaml数据结构</h2><h3 id="对象（Mapping）"><a href="#对象（Mapping）" class="headerlink" title="对象（Mapping）"></a>对象（Mapping）</h3><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># key: value</span><span class="token key atrule">Name</span><span class="token punctuation">:</span> enderviggin<span class="token comment"># 多层嵌套(缩进表示层级关系), 同级中键不能同名</span><span class="token key atrule">Info</span><span class="token punctuation">:</span>  <span class="token key atrule">Name</span><span class="token punctuation">:</span> enderviggin  <span class="token key atrule">Job</span><span class="token punctuation">:</span> devops  <span class="token comment"># 流式风格</span><span class="token key atrule">Info</span><span class="token punctuation">:</span> <span class="token punctuation">{</span><span class="token key atrule">Name</span><span class="token punctuation">:</span> enderviggin<span class="token punctuation">,</span> <span class="token key atrule">Job</span><span class="token punctuation">:</span> devops<span class="token punctuation">}</span><span class="token comment"># ?声明一个复杂对象，允许使用多个词汇（数组）来组成键(不常用)</span><span class="token punctuation">?</span><span class="token punctuation">-</span> Name<span class="token punctuation">-</span> <span class="token key atrule">Job</span><span class="token punctuation">:</span><span class="token punctuation">-</span> enderviggin<span class="token punctuation">-</span> devops<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="数组（Sequence）"><a href="#数组（Sequence）" class="headerlink" title="数组（Sequence）"></a>数组（Sequence）</h3><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 区块格式</span><span class="token key atrule">Names</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> ender  <span class="token punctuation">-</span> viggin  <span class="token punctuation">-</span> andrew<span class="token comment"># 内联格式</span><span class="token key atrule">Names</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>ender<span class="token punctuation">,</span> viggin<span class="token punctuation">,</span> andrew<span class="token punctuation">]</span><span class="token comment"># 多维数组</span><span class="token key atrule">Names</span><span class="token punctuation">:</span>   <span class="token punctuation">-</span>     <span class="token punctuation">-</span> ender    <span class="token punctuation">-</span> viggin  <span class="token punctuation">-</span>     <span class="token punctuation">-</span> andrew    <span class="token punctuation">-</span> enderviggin<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="字符串（String）"><a href="#字符串（String）" class="headerlink" title="字符串（String）"></a>字符串（String）</h3><ul><li><p>字符串一般不需要使用引号包裹，如果字符串中使用了<code>\</code>&nbsp;开头的转义字符则需要使用引号</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">Strings</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> ender viggin <span class="token comment"># 不需要引号包裹</span>  <span class="token punctuation">-</span> ender  viggin <span class="token comment"># 拆成多行后会自动在中间添加空格</span>  <span class="token punctuation">-</span> <span class="token string">'ender viggin'</span> <span class="token comment"># 单引号</span>  <span class="token punctuation">-</span> <span class="token string">"ender viggin"</span> <span class="token comment"># 双引号</span>  <span class="token punctuation">-</span> <span class="token string">"enderviggin. \u263A"</span> <span class="token comment"># 使用双引号包裹时支持 Unicode 编码</span>  <span class="token punctuation">-</span> <span class="token string">"\x0d\x0a is \r\n"</span> <span class="token comment"># 使用双引号包裹时还支持 Hex 编码</span>  <span class="token punctuation">-</span> <span class="token string">'hello "world!"'</span> <span class="token comment"># 单双引号支持嵌套"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>多行字符串</p><ul><li><p><strong>保留换行</strong></p><blockquote><p>使用<code>|</code>来表示，每行的默认缩进和行尾空白都会被去掉，而额外的缩进会被保留</p></blockquote><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">Poetry</span><span class="token punctuation">:</span> <span class="token punctuation">|</span><span class="token scalar string">  人生若只如初见  何事秋风悲画扇     等闲变却故人心         却道故心人易变</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>折叠换行</p><blockquote><p>使用<code>&gt;</code>表示，只有<strong>空白行</strong>才会被识别为换行吗原来的换行符都会被转换为<strong>空格</strong>。</p></blockquote><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">Poetry</span><span class="token punctuation">:</span> <span class="token punctuation">&gt;</span><span class="token scalar string">  人生若只如初见  何事秋风悲画扇</span>       等闲变却故人心         却道故心人易变<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul></li></ul><h3 id="布尔值（Boolean）"><a href="#布尔值（Boolean）" class="headerlink" title="布尔值（Boolean）"></a>布尔值（Boolean）</h3><ul><li><p>真： <code>true</code>,<code>True</code>,<code>TRUE</code>,<code>yes</code>,<code>Yes</code>, <code>YES</code></p></li><li><p>假：<code>false</code>,<code>False</code>,<code>FALSE</code>,<code>no</code>,<code>No</code>,<code>NO</code></p></li></ul><h3 id="整数（Integer）"><a href="#整数（Integer）" class="headerlink" title="整数（Integer）"></a>整数（Integer）</h3><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">Integer</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span class="token number">10</span>  <span class="token punctuation">-</span> 0001_0000 <span class="token comment"># 支持二进制表示</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h3 id="浮点数（Floating）"><a href="#浮点数（Floating）" class="headerlink" title="浮点数（Floating）"></a>浮点数（Floating）</h3><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">Float</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span class="token number">3.14</span>  <span class="token punctuation">-</span> <span class="token number">6.8523015e+5</span> <span class="token comment"># 使用科学计数法</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h3 id="空（null）"><a href="#空（null）" class="headerlink" title="空（null）"></a>空（null）</h3><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># "null", "Null", "~"都是空，不指定默认值也是空</span><span class="token key atrule">Nulls</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span class="token null important">null</span>  <span class="token punctuation">-</span> <span class="token null important">Null</span>  <span class="token punctuation">-</span> <span class="token null important">~</span>  <span class="token punctuation">-</span> <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="时间戳（Timestamp）"><a href="#时间戳（Timestamp）" class="headerlink" title="时间戳（Timestamp）"></a>时间戳（Timestamp）</h3><pre class="line-numbers language-none"><code class="language-none"># yaml也支持ISO 8601格式的时间数据Date1: 2020-05-26date2: 2020-05-26T01:00:00+08:00dete3: 2020-05-26T02:00:00.10+08:00date4: 2020-05-26 03:00:00.10 +8<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h3><ul><li><p>yaml支持使用严格类型标签<code>!!</code>来强制转换类型</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># !! + 目标类型</span><span class="token key atrule">toFloat</span><span class="token punctuation">:</span> <span class="token tag">!!float</span> <span class="token string">'666'</span> <span class="token key atrule">toString</span><span class="token punctuation">:</span> <span class="token string">'666'</span> <span class="token comment"># 引号也算类型转换符</span><span class="token key atrule">Int2String</span><span class="token punctuation">:</span> <span class="token tag">!!str</span> <span class="token number">666</span> <span class="token comment"># 整数转成字符串</span><span class="token key atrule">Float2String</span><span class="token punctuation">:</span> <span class="token tag">!!str</span> <span class="token number">666.6</span> <span class="token comment"># 浮点数转字符串</span>Boolean2String <span class="token tag">!!str</span> true <span class="token comment"># 布尔值转字符串</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h2 id="数据重用与合并"><a href="#数据重用与合并" class="headerlink" title="数据重用与合并"></a>数据重用与合并</h2><ul><li>为了保持内容的简洁，避免过多重复的定义，YAML 提供了由<strong>锚点标签“&amp;”</strong>和<strong>引用标签“*”</strong>组成的语法，利用这套语法可以快速引用相同的一些数据</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">Name1</span><span class="token punctuation">:</span> <span class="token important">&amp;anchor</span>  <span class="token key atrule">ender</span><span class="token punctuation">:</span> ender  <span class="token key atrule">viggin</span><span class="token punctuation">:</span> viggin  <span class="token key atrule">andrew</span><span class="token punctuation">:</span> andrew<span class="token key atrule">Name2</span><span class="token punctuation">:</span> <span class="token important">*anchor</span>  <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>配合<strong>合并标签“&lt;&lt;”</strong>使用可以与任意数据进行合并，你可以把这套操作想象成面向对象语言中的继承</li></ul><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">Animal</span><span class="token punctuation">:</span> <span class="token important">&amp;base</span> <span class="token comment"># 添加名为base的锚点</span>  <span class="token key atrule">body</span><span class="token punctuation">:</span> <span class="token number">1</span>  <span class="token key atrule">hair</span><span class="token punctuation">:</span> <span class="token number">2</span><span class="token key atrule">Single</span><span class="token punctuation">:</span>  <span class="token key atrule">&lt;&lt;</span><span class="token punctuation">:</span> <span class="token important">*base</span> <span class="token comment"># 引用 base 锚点，实例化时会自动展开</span>  <span class="token key atrule">skill</span><span class="token punctuation">:</span> sing <span class="token comment"># 添加额外的属性</span><span class="token key atrule">Programer</span><span class="token punctuation">:</span>  <span class="token key atrule">&lt;&lt;</span><span class="token punctuation">:</span> <span class="token important">*base</span> <span class="token comment"># 引用 base 锚点，实例化时会自动展开</span>  <span class="token key atrule">hair</span><span class="token punctuation">:</span> <span class="token number">6</span> <span class="token comment"># 覆写 base 中的属性</span>  <span class="token key atrule">skill</span><span class="token punctuation">:</span> code <span class="token comment"># 添加额外的属性</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="Yaml使用"><a href="#Yaml使用" class="headerlink" title="Yaml使用"></a>Yaml使用</h2><h3 id="Go操作Yaml"><a href="#Go操作Yaml" class="headerlink" title="Go操作Yaml"></a>Go操作Yaml</h3><h3 id="Python操作Yaml"><a href="#Python操作Yaml" class="headerlink" title="Python操作Yaml"></a>Python操作Yaml</h3><h3 id="JavaScript操作Yaml"><a href="#JavaScript操作Yaml" class="headerlink" title="JavaScript操作Yaml"></a>JavaScript操作Yaml</h3><h3 id="Java操作Yaml"><a href="#Java操作Yaml" class="headerlink" title="Java操作Yaml"></a>Java操作Yaml</h3><h3 id="Groovy操作Yaml"><a href="#Groovy操作Yaml" class="headerlink" title="Groovy操作Yaml"></a>Groovy操作Yaml</h3><h3 id="Ruby操作Yaml"><a href="#Ruby操作Yaml" class="headerlink" title="Ruby操作Yaml"></a>Ruby操作Yaml</h3>]]></content>
      
      
      <categories>
          
          <category> 数据交换格式 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> yaml </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Gomod</title>
      <link href="/2021/10/19/golang/tool/go-bao-guan-li-gomod/"/>
      <url>/2021/10/19/golang/tool/go-bao-guan-li-gomod/</url>
      
        <content type="html"><![CDATA[<h1 id="【Go包管理】Gomod"><a href="#【Go包管理】Gomod" class="headerlink" title="【Go包管理】Gomod"></a>【Go包管理】Gomod</h1><h2 id="gomod简介"><a href="#gomod简介" class="headerlink" title="gomod简介"></a>gomod简介</h2><p>go modules是golang 1.11加入的新特性，如果需要使用go modules则需要将go的版本升级至1.11以上</p><blockquote><p>模块是相关Go包的集合。modules是源代码交换和版本控制的单元。<br>go命令直接支持使用modules，包括记录和解析对其他模块的依赖性。<br>modules替换旧的基于GOPATH的方法来指定在给定构建中使用哪些源文件。</p></blockquote><h3 id="GO111MODULE"><a href="#GO111MODULE" class="headerlink" title="GO111MODULE"></a>GO111MODULE</h3><p>GO111MODULE 有三个值：off, on和auto（默认值）。</p><ul><li>GO111MODULE=off，go命令行将不会支持module功能，寻找依赖包的方式将会沿用旧版本那种通过vendor目录或者GOPATH模式来查找。</li><li>GO111MODULE=on，go命令行会使用modules，而不会去GOPATH目录下查找。</li><li>GO111MODULE=auto，默认值，go命令行将会根据当前目录来决定是否启用module功能。这种情况下可以分为两种情形：<ul><li>当前目录在GOPATH/src之外且该目录包含go.mod文件</li><li>当前文件在包含go.mod文件的目录下面。</li></ul></li></ul><blockquote><p>当modules 功能启用时，依赖包的存放位置变更为$GOPATH/pkg，允许同一个package多个版本并存，且多个项目可以共享缓存的 module。</p></blockquote><h2 id="gomod命令"><a href="#gomod命令" class="headerlink" title="gomod命令"></a>gomod命令</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage:        go mod <span class="token operator">&lt;</span>command<span class="token operator">&gt;</span> <span class="token punctuation">[</span>arguments<span class="token punctuation">]</span>The commands are:        download    将模块下载到本地缓存        edit        从工具或脚本编辑go.mod        graph       打印模块依赖图        init        在当前目录中初始化mod        tidy        拉取缺少的模块，移除不用的模块        vendor      将依赖复制到vendor下        verify      验证依赖是否正确        why         解释为什么需要依赖<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="gomod语法"><a href="#gomod语法" class="headerlink" title="gomod语法"></a>gomod语法</h2><p>go.mod提供了module，require，replace和exclude命令</p><ul><li>module 语句指定包的名字（路径）</li><li>require 语句指定的依赖项模块</li><li>replace 语句可以替换依赖项模块</li><li>exclude 语句可以忽略依赖项模块</li></ul><h3 id="replace用法"><a href="#replace用法" class="headerlink" title="replace用法"></a>replace用法</h3><p>由于某些已知的原因，并不是所有的package都能成功下载， modules 可以通过在 go.mod 文件中使用 replace 指令替换成github上对应的库。</p><h2 id="goget命令"><a href="#goget命令" class="headerlink" title="goget命令"></a>goget命令</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">usage: go get <span class="token punctuation">[</span>-d<span class="token punctuation">]</span> <span class="token punctuation">[</span>-t<span class="token punctuation">]</span> <span class="token punctuation">[</span>-u<span class="token punctuation">]</span> <span class="token punctuation">[</span>-v<span class="token punctuation">]</span> <span class="token punctuation">[</span>build flags<span class="token punctuation">]</span> <span class="token punctuation">[</span>packages<span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><table><thead><tr><th>参数</th><th>描述</th></tr></thead><tbody><tr><td>-d</td><td>只下载，不安装</td></tr><tr><td>-t</td><td>同时下载命令行指定包的测试代码（测试包）</td></tr><tr><td>-u</td><td>在线下载更新指定的模块（包）及依赖包（默认不更新已安装模块），并创建、安装</td></tr><tr><td>-v</td><td>打印出所下载的包名</td></tr><tr><td>-insecure</td><td>允许命令在非安全的scheme（如HTTP）下执行get命令</td></tr><tr><td>-fix</td><td>在下载代码包后先执行修正动作，而后再进行编译和安装，根据当前GO版本对所下载的模块（包）代码做语法修正</td></tr><tr><td>-f</td><td>忽略掉对已下载代码包的导入路径的检查</td></tr><tr><td>-x</td><td>打印输出，get 执行过程中的具体命令</td></tr></tbody></table><h3 id="go-get升级"><a href="#go-get升级" class="headerlink" title="go get升级"></a>go get升级</h3><ul><li>运行 go get -u 将会升级到最新的次要版本或者修订版本(x.y.z, z是修订版本号， y是次要版本号)</li><li>运行 go get -u=patch 将会升级到最新的修订版本</li><li>运行 go get package@version 将会升级到指定的版本号version</li><li>运行go get如果有版本的更改，那么go.mod文件也会更改</li></ul>]]></content>
      
      
      <categories>
          
          <category> 包管理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> go </tag>
            
            <tag> gomod </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Yarn</title>
      <link href="/2021/10/19/frontend/nodejs/yarn/node-bao-guan-li-yarn/"/>
      <url>/2021/10/19/frontend/nodejs/yarn/node-bao-guan-li-yarn/</url>
      
        <content type="html"><![CDATA[<h1 id="【Node包管理】Yarn"><a href="#【Node包管理】Yarn" class="headerlink" title="【Node包管理】Yarn"></a>【Node包管理】Yarn</h1><blockquote><p>yarn官网：<a href="https://yarnpkg.com/">https://yarnpkg.com/</a></p></blockquote><h2 id="yarn简介"><a href="#yarn简介" class="headerlink" title="yarn简介"></a>yarn简介</h2><p>yarn作为项目的包管理工具，快速、安全、可靠。你下载的包将不再重新下载。而且确保在不同系统中可以正常工作。</p><blockquote><ul><li><p><strong>速度快</strong>: Yarn 缓存了每个下载过的包，再次使用时无需重复下载。同时它是并行的，因此安装速度更快</p></li><li><p><strong>安全</strong>: 在执行代码之前，Yarn 会通过算法校验每个安装包的完整性。</p></li><li><p><strong>可靠</strong>: 使用详细、简洁的锁文件格式和明确的安装算法，使 Yarn 能够在不同系统上保证无差异的工作。</p></li></ul></blockquote><h2 id="yarn环境配置"><a href="#yarn环境配置" class="headerlink" title="yarn环境配置"></a>yarn环境配置</h2><ul><li><p>ubuntu安装yarn</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># 配置yarn仓库</span><span class="token function">curl</span> -sS https://dl.yarnpkg.com/debian/pubkey.gpg <span class="token operator">|</span> <span class="token function">sudo</span> apt-key <span class="token function">add</span> -<span class="token builtin class-name">echo</span> <span class="token string">"deb https://dl.yarnpkg.com/debian/ stable main"</span> <span class="token operator">|</span> <span class="token function">sudo</span> <span class="token function">tee</span> /etc/apt/sources.list.d/yarn.list<span class="token comment"># 安装yarn</span><span class="token function">sudo</span> <span class="token function">apt</span> update <span class="token operator">&amp;&amp;</span> <span class="token function">sudo</span> <span class="token function">apt</span> <span class="token function">install</span> <span class="token function">yarn</span><span class="token comment"># 如何使用nvm，可以通过如下命令避免安装node</span><span class="token function">sudo</span> <span class="token function">apt</span> update <span class="token operator">&amp;&amp;</span> <span class="token function">sudo</span> <span class="token function">apt</span> <span class="token function">install</span> --no-install-recommends <span class="token function">yarn</span><span class="token comment"># 如果在环境变量中没有找到yarn</span><span class="token builtin class-name">export</span> <span class="token assign-left variable"><span class="token environment constant">PATH</span></span><span class="token operator">=</span><span class="token string">"<span class="token environment constant">$PATH</span>:<span class="token variable"><span class="token variable">`</span><span class="token function">yarn</span> global bin<span class="token variable">`</span></span>"</span><span class="token comment">## 测试yarn是否安装成功</span><span class="token function">yarn</span> --version<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul>]]></content>
      
      
      <categories>
          
          <category> 包管理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> yarn </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Npm</title>
      <link href="/2021/10/19/frontend/nodejs/npm/node-bao-guan-li-npm/"/>
      <url>/2021/10/19/frontend/nodejs/npm/node-bao-guan-li-npm/</url>
      
        <content type="html"><![CDATA[<h1 id="【node包管理】Npm"><a href="#【node包管理】Npm" class="headerlink" title="【node包管理】Npm"></a>【node包管理】Npm</h1><h2 id="npm简介"><a href="#npm简介" class="headerlink" title="npm简介"></a>npm简介</h2>]]></content>
      
      
      <categories>
          
          <category> 包管理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> npm </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Gradle</title>
      <link href="/2021/10/19/java/tool/java-bao-guan-li-gradle/"/>
      <url>/2021/10/19/java/tool/java-bao-guan-li-gradle/</url>
      
        <content type="html"><![CDATA[<h1 id="【Java包管理】Gradle"><a href="#【Java包管理】Gradle" class="headerlink" title="【Java包管理】Gradle"></a>【Java包管理】Gradle</h1><blockquote><p>gradle 官网：<a href="https://gradle.org/">https://gradle.org/</a></p></blockquote><h2 id="gradle简介"><a href="#gradle简介" class="headerlink" title="gradle简介"></a>gradle简介</h2><h2 id="gradle环境配置"><a href="#gradle环境配置" class="headerlink" title="gradle环境配置"></a>gradle环境配置</h2>]]></content>
      
      
      <categories>
          
          <category> 包管理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> gradle </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Pipenv</title>
      <link href="/2021/10/19/python/tool/python-bao-guan-li-pipenv/"/>
      <url>/2021/10/19/python/tool/python-bao-guan-li-pipenv/</url>
      
        <content type="html"><![CDATA[<h1 id="【Python包管理】Pipenv"><a href="#【Python包管理】Pipenv" class="headerlink" title="【Python包管理】Pipenv"></a>【Python包管理】Pipenv</h1><blockquote><p>pipenv github地址： <a href="https://github.com/pypa/pipenv">https://github.com/pypa/pipenv</a></p></blockquote><h2 id="pipenv简介"><a href="#pipenv简介" class="headerlink" title="pipenv简介"></a>pipenv简介</h2><p>pipenv是python虚拟化环境管理工具，是pip，virtualenv和pyenv的组合体，主要使用Pipfile来管理依赖记录（代替原有的requirements.txt文件），同时可以使用Pipfile.lock文件来锁定相关包的版本。</p><h2 id="pipenv环境配置"><a href="#pipenv环境配置" class="headerlink" title="pipenv环境配置"></a>pipenv环境配置</h2><h3 id="pipenv-安装"><a href="#pipenv-安装" class="headerlink" title="pipenv 安装"></a>pipenv 安装</h3><ul><li><p>debian系统</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">sudo</span> <span class="token function">apt</span> <span class="token function">install</span> pipenv<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>Fedora系统</p>  <pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">sudo</span> dnf <span class="token function">install</span> pipenv<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>FreeBSD系统</p>  <pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">pkg <span class="token function">install</span> py36-pipenv<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>Windows系统</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">pip <span class="token function">install</span> --user pipenv<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>mac系统</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">brew <span class="token function">install</span> pipenv<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>使用pip安装</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">pipx <span class="token function">install</span> pipenv<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>镜像安装pipenv</p></li></ul><blockquote><p>无法用pip管理的包，pipenv同样无法使用</p><p>pipenv依赖：psutil, virtualenv-clone, pew, certifi, urllib3, chardet, requests, mccabe, pyflakes, pycodestyle, flake8等第三方模块。</p></blockquote><h3 id="pipenv补全"><a href="#pipenv补全" class="headerlink" title="pipenv补全"></a>pipenv补全</h3><p>将下面的配置放置在.bashrc, .bash_profile或者.zshrc就可以实现自动补全</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token builtin class-name">eval</span> <span class="token string">"<span class="token variable"><span class="token variable">$(</span>pipenv --completion<span class="token variable">)</span></span>"</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="Python常用镜像源"><a href="#Python常用镜像源" class="headerlink" title="Python常用镜像源"></a>Python常用镜像源</h3><blockquote><p>清华：<a href="https://pypi.tuna.tsinghua.edu.cn/simple">https://pypi.tuna.tsinghua.edu.cn/simple</a></p><p>阿里云：<a href="http://mirrors.aliyun.com/pypi/simple/">http://mirrors.aliyun.com/pypi/simple/</a></p><p>中国科技大学 <a href="https://pypi.mirrors.ustc.edu.cn/simple/">https://pypi.mirrors.ustc.edu.cn/simple/</a></p><p>华中理工大学：<a href="http://pypi.hustunique.com/">http://pypi.hustunique.com/</a></p><p>山东理工大学：<a href="http://pypi.sdutlinux.org/">http://pypi.sdutlinux.org/</a></p><p>豆瓣：<a href="http://pypi.douban.com/simple/">http://pypi.douban.com/simple/</a></p></blockquote><h2 id="pipenv命令"><a href="#pipenv命令" class="headerlink" title="pipenv命令"></a>pipenv命令</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage: pipenv <span class="token punctuation">[</span>OPTIONS<span class="token punctuation">]</span> COMMAND <span class="token punctuation">[</span>ARGS<span class="token punctuation">]</span><span class="token punctuation">..</span>.Options:  --where                         输出项目主页信息.  --venv                          输出虚拟环境信息.  --py                            输出Python解释器信息.  --envs                          输出环境变量选项.  --rm                            删除虚拟环境.  --bare                          最小化输出.  --completion                    完整输出.  --man                           显示帮助页面.  --support                       Output diagnostic information <span class="token keyword">for</span> <span class="token for-or-select variable">use</span> <span class="token keyword">in</span>                                  GitHub issues.  --site-packages / --no-site-packages                                  附带安装原Python解释器中的第三方库.                                  <span class="token punctuation">[</span>env var: PIPENV_SITE_PACKAGES<span class="token punctuation">]</span>  --python TEXT                   指定某个Python版本作为虚拟环境的安装源.  --three / --two                 使用Python <span class="token number">3</span>/2创建虚拟环境.  --clear                         Clears caches <span class="token punctuation">(</span>pipenv, pip, and pip-tools<span class="token punctuation">)</span>.                                  <span class="token punctuation">[</span>env var: PIPENV_CLEAR<span class="token punctuation">]</span>  -v, --verbose                   Verbose mode.  --pypi-mirror TEXT              指定PYPI的镜像源.  --version                       Show the version and exit.  -h, --help                      Show this message and exit.Commands:  check      检测Pipefile中依赖的安全漏洞.  clean      卸载Pipfile.lock中未指定的所有包.  graph      显示当前安装的依赖关系图信息.  <span class="token function">install</span>    安装提供的包，并将它们添加到pipfile，如果没有指定，从pipfile安装所有包.  lock       生成Pipfile.lock.  <span class="token function">open</span>       在编辑器中查看给定模块.  run        在虚拟环境中运行命令.  scripts    列出当前环境配置中的脚本.  shell      进入虚拟环境.  <span class="token function">sync</span>       安装在pipfile.lock中所有包.  uninstall  卸载提供的包并从pipfile中删除它.  update     Runs lock, <span class="token keyword">then</span> sync.<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><p><strong>创建虚拟环境</strong></p><p>可以通过-two或者-three选择使用的python版本，如果没有加此参数则使用默认python版本来安装，也可以指定准确的版本信息</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">mkdir</span> project <span class="token operator">&amp;&amp;</span> <span class="token builtin class-name">cd</span> projectpipenv <span class="token function">install</span> pipenv <span class="token function">install</span> --three pipenv <span class="token function">install</span> --python <span class="token number">3.6</span> <span class="token comment"># 指定准确的python版本</span>pipenv <span class="token function">install</span> --pypi-mirror<span class="token operator">=</span>mirror_path <span class="token comment"># 指定镜像源，会生效在Pipefile中</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><strong>进入虚拟环境</strong></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">pipenv shell<span class="token comment"># 退出虚拟环境</span><span class="token builtin class-name">exit</span> / ctrl+d <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p><strong>在虚拟环境中运行命令</strong></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># 启动django服务</span>pipenv run python manage.py runserver<span class="token comment"># 运行简单脚本</span>pipenv run python script.py<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><strong>安装软件包</strong></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">pipenv <span class="token function">install</span> <span class="token comment"># 安装Pipfile中的依赖</span>pipenv <span class="token function">install</span> urllib3 <span class="token comment"># 安装最新版本包，并在Pipfile写入相关信息</span>pipenv <span class="token function">install</span> <span class="token assign-left variable">urllib3</span><span class="token operator">==</span><span class="token number">1.22</span> <span class="token comment"># 指定版本安装</span>pipenv <span class="token function">install</span> httpie --dev <span class="token comment"># 安装到dev-packages中</span><span class="token comment"># 从git仓库中下载软件包,需要通过#egg=来指定包名同时注意https和ssh的使用方式</span>pipenv <span class="token function">install</span> -e git+https://github.com/explosion/spaCy<span class="token comment">#egg=spacy</span>pipenv <span class="token function">install</span> -e git+ssh://git@github.com:explosion/spaCy.git<span class="token comment">#egg=spacy</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><strong>卸载软件包</strong></p><pre class="line-numbers language-none"><code class="language-none">pipenv uninstall urllib3 # 卸载指定的包pipenv uninstall --all # 卸载所有的包<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><strong>更新软件包</strong></p><pre class="line-numbers language-none"><code class="language-none">pipenv update urllib3# 这个命令会删除所有软件包然后重新安装最新的版本pipenv update <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre></li><li><p><strong>检查软件包完整性</strong></p><p>根据 Pipfile 里的 PEP 508 标记检查安全漏洞</p><pre class="line-numbers language-none"><code class="language-none">pipenv checkChecking PEP 508 requirements...Passed!Checking installed package safety...All good!<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><strong>环境变量管理</strong></p><p>开发调试时需要配一堆环境变量，可以写到 .env 文件中，在 <code>pipenv shell</code> 进入虚拟环境时，它会帮你把这些环境变量加载好</p><pre class="line-numbers language-none"><code class="language-none">echo "HELLO=WORLD" &gt; .envecho ${HELLO}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><strong>requirements依赖包pipenv管理</strong></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># 将pipfile中的依赖导入到requirements.txt,用于项目发布时没有pipenv</span>pipenv run pip freeze <span class="token operator">&gt;</span> requirements.txtpipenv lock -r --dev <span class="token operator">&gt;</span> requirements.txt<span class="token comment"># 将requirements.txt中的依赖导入pipfile中</span>pipenv <span class="token function">install</span> -r requirements.txt<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li></ul><h2 id="pycharm和Vscode-IDE中pipenv使用"><a href="#pycharm和Vscode-IDE中pipenv使用" class="headerlink" title="pycharm和Vscode IDE中pipenv使用"></a>pycharm和Vscode IDE中pipenv使用</h2>]]></content>
      
      
      <categories>
          
          <category> 包管理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> pip </tag>
            
            <tag> pipenv </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Maven</title>
      <link href="/2021/10/19/java/tool/java-bao-guan-li-maven/"/>
      <url>/2021/10/19/java/tool/java-bao-guan-li-maven/</url>
      
        <content type="html"><![CDATA[<h1 id="【Java包管理】Maven"><a href="#【Java包管理】Maven" class="headerlink" title="【Java包管理】Maven"></a>【Java包管理】Maven</h1><blockquote><p>maven 官网: <a href="https://maven.apache.org/">https://maven.apache.org/</a></p></blockquote><h2 id="maven简介"><a href="#maven简介" class="headerlink" title="maven简介"></a>maven简介</h2><h2 id="maven环境配置"><a href="#maven环境配置" class="headerlink" title="maven环境配置"></a>maven环境配置</h2>]]></content>
      
      
      <categories>
          
          <category> 包管理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> maven </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Git</title>
      <link href="/2021/10/19/git/ban-ben-kong-zhi-git/"/>
      <url>/2021/10/19/git/ban-ben-kong-zhi-git/</url>
      
        <content type="html"><![CDATA[<h1 id="【版本控制】Git"><a href="#【版本控制】Git" class="headerlink" title="【版本控制】Git"></a>【版本控制】Git</h1><blockquote><p>git 官网： <a href="https://git-scm.com/">https://git-scm.com/</a></p></blockquote><h2 id="git简介"><a href="#git简介" class="headerlink" title="git简介"></a>git简介</h2>]]></content>
      
      
      <categories>
          
          <category> 版本控制 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>rvm</title>
      <link href="/2021/10/19/ruby/tool/ruby-ban-ben-guan-li-rvm/"/>
      <url>/2021/10/19/ruby/tool/ruby-ban-ben-guan-li-rvm/</url>
      
        <content type="html"><![CDATA[<h1 id="【Ruby版本管理】Rvm"><a href="#【Ruby版本管理】Rvm" class="headerlink" title="【Ruby版本管理】Rvm"></a>【Ruby版本管理】Rvm</h1>]]></content>
      
      
      <categories>
          
          <category> 版本管理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ruby </tag>
            
            <tag> rvm </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>gvm</title>
      <link href="/2021/10/19/golang/tool/go-ban-ben-guan-li-gvm/"/>
      <url>/2021/10/19/golang/tool/go-ban-ben-guan-li-gvm/</url>
      
        <content type="html"><![CDATA[<h1 id="【Go版本管理】Gvm"><a href="#【Go版本管理】Gvm" class="headerlink" title="【Go版本管理】Gvm"></a>【Go版本管理】Gvm</h1><blockquote><p>gvm github地址：<a href="https://github.com/moovweb/gvm">https://github.com/moovweb/gvm</a></p></blockquote><h2 id="gvm配置"><a href="#gvm配置" class="headerlink" title="gvm配置"></a>gvm配置</h2><h3 id="gvm安装"><a href="#gvm安装" class="headerlink" title="gvm安装"></a>gvm安装</h3><p>如果使用的是zsh只需要将bash换成zsh</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">bash</span> <span class="token operator">&lt;</span> <span class="token operator">&lt;</span><span class="token punctuation">(</span><span class="token function">curl</span> -s -S -L https://raw.githubusercontent.com/moovweb/gvm/master/binscripts/gvm-installer<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="使用gvm管理go版本"><a href="#使用gvm管理go版本" class="headerlink" title="使用gvm管理go版本"></a>使用gvm管理go版本</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">➜  ~ gvm <span class="token builtin class-name">help</span>   Usage: gvm <span class="token punctuation">[</span>command<span class="token punctuation">]</span>Description:  GVM is the Go Version ManagerCommands:  version    - print the gvm version number  get        - gets the latest code <span class="token punctuation">(</span>for debugging<span class="token punctuation">)</span>  use        - <span class="token keyword">select</span> a go version to use <span class="token punctuation">(</span>--default to <span class="token builtin class-name">set</span> permanently<span class="token punctuation">)</span>  <span class="token function">diff</span>       - view changes to Go root  <span class="token builtin class-name">help</span>       - display this usage text  implode    - completely remove gvm  <span class="token function">install</span>    - <span class="token function">install</span> go versions  uninstall  - uninstall go versions  cross      - <span class="token function">install</span> go cross compilers  linkthis   - <span class="token function">link</span> this directory into GOPATH  list       - list installed go versions  listall    - list available versions  <span class="token builtin class-name">alias</span>      - manage go version aliases  pkgset     - manage go packages sets  pkgenv     - edit the environment <span class="token keyword">for</span> a package <span class="token builtin class-name">set</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><p> <code>gvm listall</code> 命令显示了可以下载和编译的可用的 Go 版本</p></li><li><p><code>gvm use</code>设置系统的go版本</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">gvm use go1.16gvm use go1.16 --default<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p><code>gvm diff</code></p></li><li><p><code>gvm install &lt;version&gt;</code>安装指定版本的go</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">gvm <span class="token function">install</span> go1.16<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p><code>gvm uninstall &lt;version&gt;</code>卸载指定版本的go</p></li><li><p><code>gvm list</code>列出当前环境中所有的go版本</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">➜  ~ gvm list                gvm gos <span class="token punctuation">(</span>installed<span class="token punctuation">)</span>   go1.15.7<span class="token operator">=</span><span class="token operator">&gt;</span> go1.16   go1.16.7   go1.17<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>gvm alias</code></p></li><li><p><code>gvm pkgset</code></p><p>GVM 通过使用pkgsets将项目的新目录附加到 Go 安装版本的默认 $GOPATH，很好地完成了项目之间包的管理和隔离。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># global pkgset将默认使用该版本</span>➜  ~ <span class="token builtin class-name">echo</span> <span class="token variable">$GOPATH</span>    /home/enderviggin/.gvm/pkgsets/go1.16/global➜  ~ gvm pkgset create opsmb➜  ~ gvm pkgset use opsmb           Now using version go1.16@opsmb➜  ~ gvm pkgset listgvm go package sets <span class="token punctuation">(</span>go1.16<span class="token punctuation">)</span>    global<span class="token operator">=</span><span class="token operator">&gt;</span>  opsmb➜  ~ <span class="token builtin class-name">echo</span> <span class="token variable">$GOPATH</span>/home/enderviggin/.gvm/pkgsets/go1.16/opsmb:/home/enderviggin/.gvm/pkgsets/go1.16/global<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p><code>gvm pkgenv</code></p></li><li><p><code>gvm implode</code>删除gvm和所有go版本和安装的包</p></li></ul><h2 id="Go环境变量"><a href="#Go环境变量" class="headerlink" title="Go环境变量"></a>Go环境变量</h2><blockquote><p>go env // 打印go的所有环境变量</p><p>go env GPPATH // 打印某个环境变量的值</p></blockquote><h3 id="常用环境变量"><a href="#常用环境变量" class="headerlink" title="常用环境变量"></a>常用环境变量</h3><h4 id="GOROOT"><a href="#GOROOT" class="headerlink" title="GOROOT"></a>GOROOT</h4><p>GOROOT表示go语言的安装目录，可以使用<code>go env GOROOT</code>查看。GOROOT/bin是go提供的工具链，如果需要全局使用，则需要将此配置在环境变量中。</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># go 全局工具链配置(~/.zshrc, ~/.bashrc)</span><span class="token builtin class-name">export</span> <span class="token assign-left variable">GOROOT</span><span class="token operator">=</span><span class="token variable"><span class="token variable">$(</span>go <span class="token function">env</span> GOROOT<span class="token variable">)</span></span> <span class="token comment"># 默认GOROOT属于全局变量，可以不单独设置</span><span class="token builtin class-name">export</span> <span class="token assign-left variable"><span class="token environment constant">PATH</span></span><span class="token operator">=</span><span class="token variable">${<span class="token environment constant">PATH</span>}</span><span class="token builtin class-name">:</span><span class="token variable">${GOROOT}</span>/bin<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h4 id="GOPATH"><a href="#GOPATH" class="headerlink" title="GOPATH"></a>GOPATH</h4><p>GOPATH表示开发工作区，是用来存放源代码，测试文件，库静态文件，可执行文件。可以使用<code>go env GOPATH</code>查看。</p><p>可以通过修改GOPATH来更换工作区，这里推荐使用<code>gvm pkgset</code>来创建设置工作区</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># GOPATH目录结构</span>➜  ~ tree <span class="token variable"><span class="token variable">$(</span>go <span class="token function">env</span> GOPATH<span class="token variable">)</span></span> -L <span class="token number">3</span> <span class="token comment"># 有缩减</span>├── bin            <span class="token comment"># 源代码编译后的可执行文件</span>│   ├── goctl ├── overlay│   ├── bin│   └── lib│       └── pkgconfig├── pkg             <span class="token comment"># 库静态文件</span>│   ├── mod│   │   ├── github.com│   │   ├── go.etcd.io│   └── sumdb│       └── sum.golang.org└── src             <span class="token comment"># 　开发的源代码文件</span>    └── github.com  <span class="token comment"># 包</span>        └── ender-viggin<span class="token comment"># 设置工作区，虽然可以指定多个工作区，但是在使用go get安装包时，默认会安装到第一个工作区</span><span class="token builtin class-name">export</span> <span class="token assign-left variable">GOPATH</span><span class="token operator">=</span>folder<span class="token builtin class-name">export</span> <span class="token assign-left variable">GOAPTH</span><span class="token operator">=</span>folder1<span class="token punctuation">;</span>folder2<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="GOBIN"><a href="#GOBIN" class="headerlink" title="GOBIN"></a>GOBIN</h4><p>GOBIN表示开发程序编译后的二进制命令的安装目录即GOPATH下面的bin目录。如果需要全局使用命令，需要配置在全局变量中</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token builtin class-name">export</span> <span class="token assign-left variable">GOBIN</span><span class="token operator">=</span><span class="token variable">${GOPATH}</span>/bin<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h4 id="GOPROXY"><a href="#GOPROXY" class="headerlink" title="GOPROXY"></a>GOPROXY</h4><p>设置go代理，提高下载速度</p><blockquote><p>国内常用代理地址</p><p>goproxy: <a href="https://goproxy.io/zh/">https://goproxy.io/zh/</a></p><p>七牛云： <a href="https://goproxy.cn/">https://goproxy.cn</a></p><p>阿里云： <a href="https://mirrors.aliyun.com/goproxy/">https://mirrors.aliyun.com/goproxy/</a></p></blockquote><p><strong>代理设置方式</strong></p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># Go 版本是 1.13 及以上</span>go <span class="token function">env</span> -w <span class="token assign-left variable">GO111MODULE</span><span class="token operator">=</span>ongo <span class="token function">env</span> -w <span class="token assign-left variable">GOPROXY</span><span class="token operator">=</span>https://goproxy.cn,direct<span class="token comment"># 其他版本</span><span class="token builtin class-name">export</span> <span class="token assign-left variable">GO111MODULE</span><span class="token operator">=</span>on<span class="token builtin class-name">export</span> <span class="token assign-left variable">GOPROXY</span><span class="token operator">=</span>https://goproxy.cn<span class="token comment"># 取消代理</span>go <span class="token function">env</span> -u GOPROXY<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="交叉编译"><a href="#交叉编译" class="headerlink" title="交叉编译"></a>交叉编译</h3><p>在一个平台上就能生成在其他平台上运行的代码。</p><h4 id="GOOS和GOARCH"><a href="#GOOS和GOARCH" class="headerlink" title="GOOS和GOARCH"></a>GOOS和GOARCH</h4><p>GOOS的默认值是我们当前的操作系统， GOARCH则表示CPU架构， 可以使用<code>go env GOOS GOARCH</code>查看当前系统的架构信息。</p><blockquote><p>GOOS和GOARCH是成对出现的，并且只能是下面对应的值</p></blockquote><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token variable">$GOOS</span>    <span class="token variable">$GOARCH</span>android    armdarwin    <span class="token number">386</span>darwin    amd64darwin    armdarwin    arm64dragonfly   amd64freebsd    <span class="token number">386</span>freebsd    amd64freebsd    armlinux      <span class="token number">386</span>linux      amd64linux      armlinux      arm64linux      ppc64linux      ppc64lelinux      mipslinux      mipslelinux      mips64linux      mips64lelinux      s390xnetbsd    <span class="token number">386</span>netbsd    amd64netbsd    armopenbsd    <span class="token number">386</span>openbsd    amd64openbsd    armplan9      <span class="token number">386</span>plan9      amd64solaris    amd64windows    <span class="token number">386</span>windows    amd64<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="全部的环境变量"><a href="#全部的环境变量" class="headerlink" title="全部的环境变量"></a>全部的环境变量</h3><pre class="line-numbers language-none"><code class="language-none">GO111MODULEGOARCHGOBINGOCACHEGOENVGOEXEGOEXPERIMENTGOFLAGSGOHOSTARCHGOHOSTOSGOINSECUREGOMODCACHEGONOPROXYGONOSUMDBGOOSGOPATHGOPRIVATEGOPROXYGOROOTGOSUMDBGOTMPDIRGOTOOLDIRGOVCSGOVERSIONGCCGOARCCCXXCGO_ENABLEDGOMODCGO_CFLAGSCGO_CPPFLAGSCGO_CXXFLAGSCGO_FFLAGSCGO_LDFLAGSPKG_CONFIGGOGCCFLAGS<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> 版本管理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> go </tag>
            
            <tag> gvm </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>nvm</title>
      <link href="/2021/10/19/frontend/nodejs/tool/node-ban-ben-guan-li-nvm/"/>
      <url>/2021/10/19/frontend/nodejs/tool/node-ban-ben-guan-li-nvm/</url>
      
        <content type="html"><![CDATA[<h1 id="【Node版本管理】nvm"><a href="#【Node版本管理】nvm" class="headerlink" title="【Node版本管理】nvm"></a>【Node版本管理】nvm</h1><blockquote><p>nvm github地址: <a href="https://github.com/nvm-sh/nvm">https://github.com/nvm-sh/nvm</a></p></blockquote><h2 id="nvm简介"><a href="#nvm简介" class="headerlink" title="nvm简介"></a>nvm简介</h2><p>nvm可以快速安装和使用不同的node版本</p><h2 id="nvm环境配置"><a href="#nvm环境配置" class="headerlink" title="nvm环境配置"></a>nvm环境配置</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">curl</span> -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh <span class="token operator">|</span> <span class="token function">bash</span><span class="token function">wget</span> -qO- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh <span class="token operator">|</span> <span class="token function">bash</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h2 id="nvm命令"><a href="#nvm命令" class="headerlink" title="nvm命令"></a>nvm命令</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Node Version Manager <span class="token punctuation">(</span>v0.38.0<span class="token punctuation">)</span>Usage:  nvm <span class="token function">install</span> <span class="token punctuation">[</span><span class="token operator">&lt;</span>version<span class="token operator">&gt;</span><span class="token punctuation">]</span>                     Download and <span class="token function">install</span> a <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span>. Uses .nvmrc <span class="token keyword">if</span> available and version is omitted.   The following optional arguments, <span class="token keyword">if</span> provided, must appear directly after <span class="token variable"><span class="token variable">`</span>nvm <span class="token function">install</span><span class="token variable">`</span></span><span class="token builtin class-name">:</span>    -s                                        Skip binary download, <span class="token function">install</span> from <span class="token builtin class-name">source</span> only.    -b                                        Skip <span class="token builtin class-name">source</span> download, <span class="token function">install</span> from binary only.    --reinstall-packages-from<span class="token operator">=</span><span class="token operator">&lt;</span>version<span class="token operator">&gt;</span>       When installing, reinstall packages installed <span class="token keyword">in</span> <span class="token operator">&lt;</span>node<span class="token operator">|</span>iojs<span class="token operator">|</span>node version number<span class="token operator">&gt;</span>    --lts                                     When installing, only <span class="token keyword">select</span> from LTS <span class="token punctuation">(</span>long-term support<span class="token punctuation">)</span> versions    --lts<span class="token operator">=</span><span class="token operator">&lt;</span>LTS name<span class="token operator">&gt;</span>                          When installing, only <span class="token keyword">select</span> from versions <span class="token keyword">for</span> a specific LTS line    --skip-default-packages                   When installing, skip the default-packages <span class="token function">file</span> <span class="token keyword">if</span> it exists    --latest-npm                              After installing, attempt to upgrade to the latest working <span class="token function">npm</span> on the given node version    --no-progress                             Disable the progress bar on any downloads    --alias<span class="token operator">=</span><span class="token operator">&lt;</span>name<span class="token operator">&gt;</span>                            After installing, <span class="token builtin class-name">set</span> the <span class="token builtin class-name">alias</span> specified to the version specified. <span class="token punctuation">(</span>same as: nvm <span class="token builtin class-name">alias</span> <span class="token operator">&lt;</span>name<span class="token operator">&gt;</span> <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span><span class="token punctuation">)</span>    --default                                 After installing, <span class="token builtin class-name">set</span> default <span class="token builtin class-name">alias</span> to the version specified. <span class="token punctuation">(</span>same as: nvm <span class="token builtin class-name">alias</span> default <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span><span class="token punctuation">)</span>  nvm uninstall <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span>                     Uninstall a version  nvm uninstall --lts                         Uninstall using automatic LTS <span class="token punctuation">(</span>long-term support<span class="token punctuation">)</span> <span class="token builtin class-name">alias</span> <span class="token variable"><span class="token variable">`</span>lts/*<span class="token variable">`</span></span>, <span class="token keyword">if</span> available.  nvm uninstall --lts<span class="token operator">=</span><span class="token operator">&lt;</span>LTS name<span class="token operator">&gt;</span>              Uninstall using automatic <span class="token builtin class-name">alias</span> <span class="token keyword">for</span> provided LTS line, <span class="token keyword">if</span> available.  nvm use <span class="token punctuation">[</span><span class="token operator">&lt;</span>version<span class="token operator">&gt;</span><span class="token punctuation">]</span>                         Modify <span class="token environment constant">PATH</span> to use <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span>. Uses .nvmrc <span class="token keyword">if</span> available and version is omitted.   The following optional arguments, <span class="token keyword">if</span> provided, must appear directly after <span class="token variable"><span class="token variable">`</span>nvm use<span class="token variable">`</span></span><span class="token builtin class-name">:</span>    --silent                                  Silences stdout/stderr output    --lts                                     Uses automatic LTS <span class="token punctuation">(</span>long-term support<span class="token punctuation">)</span> <span class="token builtin class-name">alias</span> <span class="token variable"><span class="token variable">`</span>lts/*<span class="token variable">`</span></span>, <span class="token keyword">if</span> available.    --lts<span class="token operator">=</span><span class="token operator">&lt;</span>LTS name<span class="token operator">&gt;</span>                          Uses automatic <span class="token builtin class-name">alias</span> <span class="token keyword">for</span> provided LTS line, <span class="token keyword">if</span> available.  nvm <span class="token builtin class-name">exec</span> <span class="token punctuation">[</span><span class="token operator">&lt;</span>version<span class="token operator">&gt;</span><span class="token punctuation">]</span> <span class="token punctuation">[</span><span class="token operator">&lt;</span>command<span class="token operator">&gt;</span><span class="token punctuation">]</span>            Run <span class="token operator">&lt;</span>command<span class="token operator">&gt;</span> on <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span>. Uses .nvmrc <span class="token keyword">if</span> available and version is omitted.   The following optional arguments, <span class="token keyword">if</span> provided, must appear directly after <span class="token variable"><span class="token variable">`</span>nvm <span class="token builtin class-name">exec</span><span class="token variable">`</span></span><span class="token builtin class-name">:</span>    --silent                                  Silences stdout/stderr output    --lts                                     Uses automatic LTS <span class="token punctuation">(</span>long-term support<span class="token punctuation">)</span> <span class="token builtin class-name">alias</span> <span class="token variable"><span class="token variable">`</span>lts/*<span class="token variable">`</span></span>, <span class="token keyword">if</span> available.    --lts<span class="token operator">=</span><span class="token operator">&lt;</span>LTS name<span class="token operator">&gt;</span>                          Uses automatic <span class="token builtin class-name">alias</span> <span class="token keyword">for</span> provided LTS line, <span class="token keyword">if</span> available.  nvm run <span class="token punctuation">[</span><span class="token operator">&lt;</span>version<span class="token operator">&gt;</span><span class="token punctuation">]</span> <span class="token punctuation">[</span><span class="token operator">&lt;</span>args<span class="token operator">&gt;</span><span class="token punctuation">]</span>                Run <span class="token variable"><span class="token variable">`</span>node<span class="token variable">`</span></span> on <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span> with <span class="token operator">&lt;</span>args<span class="token operator">&gt;</span> as arguments. Uses .nvmrc <span class="token keyword">if</span> available and version is omitted.   The following optional arguments, <span class="token keyword">if</span> provided, must appear directly after <span class="token variable"><span class="token variable">`</span>nvm run<span class="token variable">`</span></span><span class="token builtin class-name">:</span>    --silent                                  Silences stdout/stderr output    --lts                                     Uses automatic LTS <span class="token punctuation">(</span>long-term support<span class="token punctuation">)</span> <span class="token builtin class-name">alias</span> <span class="token variable"><span class="token variable">`</span>lts/*<span class="token variable">`</span></span>, <span class="token keyword">if</span> available.    --lts<span class="token operator">=</span><span class="token operator">&lt;</span>LTS name<span class="token operator">&gt;</span>                          Uses automatic <span class="token builtin class-name">alias</span> <span class="token keyword">for</span> provided LTS line, <span class="token keyword">if</span> available.  nvm current                                 Display currently activated version of Node  nvm <span class="token function">ls</span> <span class="token punctuation">[</span><span class="token operator">&lt;</span>version<span class="token operator">&gt;</span><span class="token punctuation">]</span>                          List installed versions, matching a given <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span> <span class="token keyword">if</span> provided    --no-colors                               Suppress colored output    --no-alias                                Suppress <span class="token variable"><span class="token variable">`</span>nvm <span class="token builtin class-name">alias</span><span class="token variable">`</span></span> output  nvm ls-remote <span class="token punctuation">[</span><span class="token operator">&lt;</span>version<span class="token operator">&gt;</span><span class="token punctuation">]</span>                   List remote versions available <span class="token keyword">for</span> install, matching a given <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span> <span class="token keyword">if</span> provided    --lts                                     When listing, only show LTS <span class="token punctuation">(</span>long-term support<span class="token punctuation">)</span> versions    --lts<span class="token operator">=</span><span class="token operator">&lt;</span>LTS name<span class="token operator">&gt;</span>                          When listing, only show versions <span class="token keyword">for</span> a specific LTS line    --no-colors                               Suppress colored output  nvm version <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span>                       Resolve the given description to a single <span class="token builtin class-name">local</span> version  nvm version-remote <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span>                Resolve the given description to a single remote version    --lts                                     When listing, only <span class="token keyword">select</span> from LTS <span class="token punctuation">(</span>long-term support<span class="token punctuation">)</span> versions    --lts<span class="token operator">=</span><span class="token operator">&lt;</span>LTS name<span class="token operator">&gt;</span>                          When listing, only <span class="token keyword">select</span> from versions <span class="token keyword">for</span> a specific LTS line  nvm deactivate                              Undo effects of <span class="token variable"><span class="token variable">`</span>nvm<span class="token variable">`</span></span> on current shell    --silent                                  Silences stdout/stderr output  nvm <span class="token builtin class-name">alias</span> <span class="token punctuation">[</span><span class="token operator">&lt;</span>pattern<span class="token operator">&gt;</span><span class="token punctuation">]</span>                       Show all aliases beginning with <span class="token operator">&lt;</span>pattern<span class="token operator">&gt;</span>    --no-colors                               Suppress colored output  nvm <span class="token builtin class-name">alias</span> <span class="token operator">&lt;</span>name<span class="token operator">&gt;</span> <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span>                  Set an <span class="token builtin class-name">alias</span> named <span class="token operator">&lt;</span>name<span class="token operator">&gt;</span> pointing to <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span>  nvm <span class="token builtin class-name">unalias</span> <span class="token operator">&lt;</span>name<span class="token operator">&gt;</span>                          Deletes the <span class="token builtin class-name">alias</span> named <span class="token operator">&lt;</span>name<span class="token operator">&gt;</span>  nvm install-latest-npm                      Attempt to upgrade to the latest working <span class="token variable"><span class="token variable">`</span><span class="token function">npm</span><span class="token variable">`</span></span> on the current node version  nvm reinstall-packages <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span>            Reinstall global <span class="token variable"><span class="token variable">`</span><span class="token function">npm</span><span class="token variable">`</span></span> packages contained <span class="token keyword">in</span> <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span> to current version  nvm unload                                  Unload <span class="token variable"><span class="token variable">`</span>nvm<span class="token variable">`</span></span> from shell  nvm <span class="token function">which</span> <span class="token punctuation">[</span>current <span class="token operator">|</span> <span class="token operator">&lt;</span>version<span class="token operator">&gt;</span><span class="token punctuation">]</span>             Display path to installed node version. Uses .nvmrc <span class="token keyword">if</span> available and version is omitted.    --silent                                  Silences stdout/stderr output when a version is omitted  nvm cache <span class="token function">dir</span>                               Display path to the cache directory <span class="token keyword">for</span> nvm  nvm cache <span class="token function">clear</span>                             Empty cache directory <span class="token keyword">for</span> nvmNote:  to remove, delete, or uninstall nvm - just remove the <span class="token variable"><span class="token variable">`</span>$NVM_DIR<span class="token variable">`</span></span> folder <span class="token punctuation">(</span>usually <span class="token variable"><span class="token variable">`</span>~/.nvm<span class="token variable">`</span></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> 版本管理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> node </tag>
            
            <tag> nvm </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Swagger</title>
      <link href="/2021/10/19/swagger/api-wen-dang-swagger/"/>
      <url>/2021/10/19/swagger/api-wen-dang-swagger/</url>
      
        <content type="html"><![CDATA[<h1 id="swagger"><a href="#swagger" class="headerlink" title="swagger"></a>swagger</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Less</title>
      <link href="/2021/10/19/frontend/css/less/css-yu-bian-yi-less/"/>
      <url>/2021/10/19/frontend/css/less/css-yu-bian-yi-less/</url>
      
        <content type="html"><![CDATA[<h1 id="【css预编译】Less"><a href="#【css预编译】Less" class="headerlink" title="【css预编译】Less"></a>【css预编译】Less</h1><blockquote><p>less github地址： <a href="https://github.com/less/less.js">https://github.com/less/less.js</a></p><p>less 官网： <a href="https://lesscss.org/">https://lesscss.org/</a><br>less中文网：<a href="https://less.bootcss.com/">https://less.bootcss.com/</a></p></blockquote><h2 id="Less简介"><a href="#Less简介" class="headerlink" title="Less简介"></a>Less简介</h2><p>Less （Leaner Style Sheets 的缩写） 是一门向后兼容的 CSS 扩展语言。</p><h2 id="Less配置"><a href="#Less配置" class="headerlink" title="Less配置"></a>Less配置</h2><h2 id="Less语法"><a href="#Less语法" class="headerlink" title="Less语法"></a>Less语法</h2><h3 id="变量-Variables"><a href="#变量-Variables" class="headerlink" title="变量(Variables)"></a>变量(Variables)</h3><pre class="line-numbers language-less" data-language="less"><code class="language-less"><span class="token variable">@width<span class="token punctuation">:</span></span> 10px<span class="token punctuation">;</span><span class="token variable">@height<span class="token punctuation">:</span></span> <span class="token variable">@width</span> <span class="token operator">+</span> 10px<span class="token punctuation">;</span><span class="token selector">#header</span> <span class="token punctuation">{</span>  <span class="token property">width</span><span class="token punctuation">:</span> <span class="token variable">@width</span><span class="token punctuation">;</span>  <span class="token property">height</span><span class="token punctuation">:</span> <span class="token variable">@height</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="混合-Mixins"><a href="#混合-Mixins" class="headerlink" title="混合(Mixins)"></a>混合(Mixins)</h3><p>混合（Mixin）是一种将一组属性从一个规则集包含（或混入）到另一个规则集的方法。</p><pre class="line-numbers language-less" data-language="less"><code class="language-less"><span class="token selector">.bordered</span> <span class="token punctuation">{</span>  <span class="token property">border-top</span><span class="token punctuation">:</span> dotted 1px black<span class="token punctuation">;</span>  <span class="token property">border-bottom</span><span class="token punctuation">:</span> solid 2px black<span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token selector">#menu a</span> <span class="token punctuation">{</span>  <span class="token property">color</span><span class="token punctuation">:</span> #111<span class="token punctuation">;</span>  <span class="token mixin-usage function">.bordered</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token selector">.post a</span> <span class="token punctuation">{</span>  <span class="token property">color</span><span class="token punctuation">:</span> red<span class="token punctuation">;</span>  <span class="token mixin-usage function">.bordered</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="嵌套-Nesting"><a href="#嵌套-Nesting" class="headerlink" title="嵌套(Nesting)"></a>嵌套(Nesting)</h3><p>Less 提供了使用嵌套（nesting）代替层叠或与层叠结合使用的能力。</p><pre class="line-numbers language-css" data-language="css"><code class="language-css"><span class="token selector">#header</span> <span class="token punctuation">{</span>  <span class="token property">color</span><span class="token punctuation">:</span> black<span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token selector"># header .navigation</span> <span class="token punctuation">{</span>  <span class="token property">font-size</span><span class="token punctuation">:</span> 12px<span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token selector"># header .logo</span> <span class="token punctuation">{</span>  <span class="token property">width</span><span class="token punctuation">:</span> 300px<span class="token punctuation">;</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>可以使用嵌套转换成less</p><pre class="line-numbers language-less" data-language="less"><code class="language-less"><span class="token selector">#header</span> <span class="token punctuation">{</span>  <span class="token property">color</span><span class="token punctuation">:</span> black<span class="token punctuation">;</span>  <span class="token selector">.navigation</span> <span class="token punctuation">{</span>    <span class="token property">font-size</span><span class="token punctuation">:</span> 12px<span class="token punctuation">;</span>  <span class="token punctuation">}</span>  <span class="token selector">.logo</span> <span class="token punctuation">{</span>    <span class="token property">width</span><span class="token punctuation">:</span> 300px<span class="token punctuation">;</span>  <span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="运算-Operations"><a href="#运算-Operations" class="headerlink" title="运算(Operations)"></a>运算(Operations)</h3><p>算术运算符 +、-、*、/ 可以对任何数字、颜色或变量进行运算。<br>如果可能的话，算术运算符在加、减或比较之前会进行单位换算。计算的结果以最左侧操作数的单位类型为准。如果单位换算无效或失去意义，则忽略单位。无效的单位换算例如：px 到 cm 或 rad 到 % 的转换。</p><pre class="line-numbers language-less" data-language="less"><code class="language-less"><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="转义-Escaping"><a href="#转义-Escaping" class="headerlink" title="转义(Escaping)"></a>转义(Escaping)</h3><h3 id="函数-Functions"><a href="#函数-Functions" class="headerlink" title="函数(Functions)"></a>函数(Functions)</h3><h3 id="命名空间和访问符"><a href="#命名空间和访问符" class="headerlink" title="命名空间和访问符"></a>命名空间和访问符</h3><h3 id="映射-Maps"><a href="#映射-Maps" class="headerlink" title="映射(Maps)"></a>映射(Maps)</h3><h3 id="作用域-Scope"><a href="#作用域-Scope" class="headerlink" title="作用域(Scope)"></a>作用域(Scope)</h3><h3 id="注释-Comments"><a href="#注释-Comments" class="headerlink" title="注释(Comments)"></a>注释(Comments)</h3><pre class="line-numbers language-less" data-language="less"><code class="language-less"># 块注释<span class="token comment">/* 一个块注释 * style comment! */</span><span class="token variable">@var<span class="token punctuation">:</span></span> red<span class="token punctuation">;</span><span class="token comment">// 行注释</span><span class="token variable">@var<span class="token punctuation">:</span></span> white<span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="导入-Importing"><a href="#导入-Importing" class="headerlink" title="导入(Importing)"></a>导入(Importing)</h3>]]></content>
      
      
      <categories>
          
          <category> Css预编译 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> less </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Webpack</title>
      <link href="/2021/10/19/frontend/webpack/bian-yi-gou-jian-webpack/"/>
      <url>/2021/10/19/frontend/webpack/bian-yi-gou-jian-webpack/</url>
      
        <content type="html"><![CDATA[<h1 id="【编译构建】webpack"><a href="#【编译构建】webpack" class="headerlink" title="【编译构建】webpack"></a>【编译构建】webpack</h1><blockquote><p>webpack 官网：<a href="https://webpack.js.org/">https://webpack.js.org/</a></p><p>github地址：<a href="https://github.com/webpack/webpack">https://github.com/webpack/webpack</a></p></blockquote><h2 id="webpack简介"><a href="#webpack简介" class="headerlink" title="webpack简介"></a>webpack简介</h2><p><strong>webpack</strong> 是一个用于现代 JavaScript 应用程序的 <em>静态模块打包工具</em>。当 webpack 处理应用程序时，它会在内部从一个或多个入口点构建一个依赖图(dependency graph)，然后将你项目中所需的每一个模块组合成一个或多个 <em>bundles</em>，它们均为静态资源，用于展示你的内容。</p><h2 id=""><a href="#" class="headerlink" title=""></a></h2>]]></content>
      
      
      <categories>
          
          <category> 编译构建 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> webpack </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Jmeter</title>
      <link href="/2021/10/19/test/jmeter/ya-li-ce-shi-jmeter/"/>
      <url>/2021/10/19/test/jmeter/ya-li-ce-shi-jmeter/</url>
      
        <content type="html"><![CDATA[<h1 id="Jmeter"><a href="#Jmeter" class="headerlink" title="Jmeter"></a>Jmeter</h1>]]></content>
      
      
      <categories>
          
          <category> 压力测试 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> jmeter </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【代码质量】SonarQube</title>
      <link href="/2021/10/19/test/sonarqube/dai-ma-zhi-liang-sonarqube/"/>
      <url>/2021/10/19/test/sonarqube/dai-ma-zhi-liang-sonarqube/</url>
      
        <content type="html"><![CDATA[<h1 id="【代码质量】SonarQube"><a href="#【代码质量】SonarQube" class="headerlink" title="【代码质量】SonarQube"></a>【代码质量】SonarQube</h1>]]></content>
      
      
      <categories>
          
          <category> 代码质量 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> sonarqube </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Selenium</title>
      <link href="/2021/10/19/test/selenium/zi-dong-hua-ce-shi-selenium/"/>
      <url>/2021/10/19/test/selenium/zi-dong-hua-ce-shi-selenium/</url>
      
        <content type="html"><![CDATA[<h1 id="【自动化测试】Selenium"><a href="#【自动化测试】Selenium" class="headerlink" title="【自动化测试】Selenium"></a>【自动化测试】Selenium</h1>]]></content>
      
      
      <categories>
          
          <category> 自动化测试 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> selenium </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【虚拟机管理】Vagrant</title>
      <link href="/2021/10/19/vagrant/xu-ni-ji-guan-li-vagrant/"/>
      <url>/2021/10/19/vagrant/xu-ni-ji-guan-li-vagrant/</url>
      
        <content type="html"><![CDATA[<h1 id="【虚拟机管理】Vagrant"><a href="#【虚拟机管理】Vagrant" class="headerlink" title="【虚拟机管理】Vagrant"></a>【虚拟机管理】Vagrant</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Vault</title>
      <link href="/2021/10/19/vault/vault/"/>
      <url>/2021/10/19/vault/vault/</url>
      
        <content type="html"><![CDATA[<h1 id="Vault"><a href="#Vault" class="headerlink" title="Vault"></a>Vault</h1>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>【容器】Containerd</title>
      <link href="/2021/10/19/containerd/rong-qi-containerd/"/>
      <url>/2021/10/19/containerd/rong-qi-containerd/</url>
      
        <content type="html"><![CDATA[<h1 id="【容器】Containerd"><a href="#【容器】Containerd" class="headerlink" title="【容器】Containerd"></a>【容器】Containerd</h1><blockquote><p>官网：<a href="https://containerd.io/">https://containerd.io/</a></p><p>github地址：<a href="https://github.com/containerd/containerd">https://github.com/containerd/containerd</a></p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 容器 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> containerd </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【容器编排】Kubernetes</title>
      <link href="/2021/10/19/kubernetes/rong-qi-bian-pai-kubernetes/"/>
      <url>/2021/10/19/kubernetes/rong-qi-bian-pai-kubernetes/</url>
      
        <content type="html"><![CDATA[<h1 id="【容器编排】Kubernetes"><a href="#【容器编排】Kubernetes" class="headerlink" title="【容器编排】Kubernetes"></a>【容器编排】Kubernetes</h1><blockquote><p>kubernetes官网: <a href="https://kubernetes.io/">https://kubernetes.io/</a></p><p>kubernetes github地址: <a href="https://github.com/kubernetes/kubernetes">https://github.com/kubernetes/kubernetes</a></p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 容器编排 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> kubernetes </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【容器】Docker</title>
      <link href="/2021/10/19/docker/rong-qi-docker/"/>
      <url>/2021/10/19/docker/rong-qi-docker/</url>
      
        <content type="html"><![CDATA[<h1 id="Docker"><a href="#Docker" class="headerlink" title="Docker"></a>Docker</h1><blockquote><p>docker 官网： <a href="https://www.docker.com/">https://www.docker.com/</a></p><p>docker hub 地址： <a href="https://hub.docker.com/">https://hub.docker.com/</a></p></blockquote><h2 id="Docker简介"><a href="#Docker简介" class="headerlink" title="Docker简介"></a>Docker简介</h2><h2 id="Docker环境配置"><a href="#Docker环境配置" class="headerlink" title="Docker环境配置"></a>Docker环境配置</h2><h2 id="Docker概念"><a href="#Docker概念" class="headerlink" title="Docker概念"></a>Docker概念</h2><h2 id="Docker命令行"><a href="#Docker命令行" class="headerlink" title="Docker命令行"></a>Docker命令行</h2><h2 id="Dockerfile"><a href="#Dockerfile" class="headerlink" title="Dockerfile"></a>Dockerfile</h2>]]></content>
      
      
      <categories>
          
          <category> 容器 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> docker </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Terraform</title>
      <link href="/2021/10/19/terraform/zi-dong-hua-pei-zhi-terraform/"/>
      <url>/2021/10/19/terraform/zi-dong-hua-pei-zhi-terraform/</url>
      
        <content type="html"><![CDATA[<h1 id="【自动化配置】Terraform"><a href="#【自动化配置】Terraform" class="headerlink" title="【自动化配置】Terraform"></a>【自动化配置】Terraform</h1><blockquote><p>terraform 官网: <a href="https://www.terraform.io/">https://www.terraform.io/</a></p><p>terraform github地址: <a href="https://github.com/hashicorp/terraform">https://github.com/hashicorp/terraform</a></p><p>alicloud provider: <a href="https://github.com/hashicorp/terraform-provider-alicloud">https://github.com/hashicorp/terraform-provider-alicloud</a></p><p>aws provider: <a href="https://github.com/hashicorp/terraform-provider-aws">https://github.com/hashicorp/terraform-provider-aws</a></p></blockquote><h2 id="terraform简介"><a href="#terraform简介" class="headerlink" title="terraform简介"></a>terraform简介</h2><p>Terraform是一种基础架构即代码（IAC）工具，允许您安全有效地构建，更改和版本化基础架构。这包括计算实例，存储和网络等低级组件，以及DNS条目和SAAS功能等高级组件。</p><h2 id="terraform环境配置"><a href="#terraform环境配置" class="headerlink" title="terraform环境配置"></a>terraform环境配置</h2><h2 id="terraform命令"><a href="#terraform命令" class="headerlink" title="terraform命令"></a>terraform命令</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell">Usage: terraform [global options] &lt;subcommand&gt; [args]Main commands:  init          为其他命令准备工作目录  validate      检查配置是否有效  plan          显示当前配置所需的更改  apply         创建或更新基础架构  destroy       销毁以前创建的基础设施All other commands:  console       Try Terraform expressions at an interactive command prompt  fmt           Reformat your configuration in the standard style  force-unlock  Release a stuck lock on the current workspace  get           安装或升级远程Terraform模块  graph         Generate a Graphviz graph of the steps in an operation  import        Associate existing infrastructure with a Terraform resource  login         Obtain and save credentials for a remote host  logout        Remove locally-stored credentials for a remote host  output        Show output values from your root module  providers     Show the providers required for this configuration  refresh       更新状态以匹配远程系统  show          Show the current state or a saved plan  state         高级状态管理  taint         Mark a resource instance as not fully functional  test          Experimental support for module integration testing  untaint       Remove the 'tainted' state from a resource instance  version       Show the current Terraform version  workspace     Workspace managementGlobal options (use these before the subcommand, if any):  -chdir=DIR    Switch to a different working directory before executing the                given subcommand.  -help         Show this help output, or the help for a specified subcommand.  -version      An alias for the "version" subcommand.<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> 自动化配置 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> terraform </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Ansible</title>
      <link href="/2021/10/19/ansible/zi-dong-hua-pei-zhi-ansible/"/>
      <url>/2021/10/19/ansible/zi-dong-hua-pei-zhi-ansible/</url>
      
        <content type="html"><![CDATA[<h1 id="【自动化配置】Ansible"><a href="#【自动化配置】Ansible" class="headerlink" title="【自动化配置】Ansible"></a>【自动化配置】Ansible</h1>]]></content>
      
      
      <categories>
          
          <category> 自动化配置 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ansible </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【持续集成交付】Teckton</title>
      <link href="/2021/10/19/teckton/chi-xu-ji-cheng-jiao-fu-teckton/"/>
      <url>/2021/10/19/teckton/chi-xu-ji-cheng-jiao-fu-teckton/</url>
      
        <content type="html"><![CDATA[<h1 id="Teckton"><a href="#Teckton" class="headerlink" title="Teckton"></a>Teckton</h1>]]></content>
      
      
      <categories>
          
          <category> 持续集成交付 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> teckton </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【前端框架】React</title>
      <link href="/2021/10/19/frontend/react/qian-duan-kuang-jia-react/"/>
      <url>/2021/10/19/frontend/react/qian-duan-kuang-jia-react/</url>
      
        <content type="html"><![CDATA[<h1 id="React"><a href="#React" class="headerlink" title="React"></a>React</h1><blockquote><p>官网：<a href="https://reactjs.org/">https://reactjs.org/</a><br>github地址：<a href="https://github.com/facebook/react/">https://github.com/facebook/react/</a></p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 前端框架 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> react </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Prometheus</title>
      <link href="/2021/10/19/monitor/prometheus/jian-kong-prometheus/"/>
      <url>/2021/10/19/monitor/prometheus/jian-kong-prometheus/</url>
      
        <content type="html"><![CDATA[<h1 id="【监控】Prometheus"><a href="#【监控】Prometheus" class="headerlink" title="【监控】Prometheus"></a>【监控】Prometheus</h1><blockquote><p>官网： <a href="https://prometheus.io/">https://prometheus.io/</a></p><p>中文文档： <a href="https://prometheus.fuckcloudnative.io/">https://prometheus.fuckcloudnative.io/</a></p><p>github地址： <a href="https://github.com/prometheus">https://github.com/prometheus</a></p></blockquote><h2 id="prometheus简介"><a href="#prometheus简介" class="headerlink" title="prometheus简介"></a>prometheus简介</h2><h3 id="prometheus优势"><a href="#prometheus优势" class="headerlink" title="prometheus优势"></a>prometheus优势</h3><ul><li><p>由指标名称和和键/值对标签标识的时间序列数据组成的多维数据模型。</p></li><li><p>强大的查询语言 PromQL。</p></li><li><p>不依赖分布式存储；单个服务节点具有自治能力。</p></li><li><p>时间序列数据是服务端通过 HTTP 协议主动拉取获得的。</p></li><li><p>可以通过中间网关来推送时间序列数据。</p></li><li><p>可以通过静态配置文件或服务发现来获取监控目标。</p></li><li><p>支持多种类型的图表和仪表盘。</p></li></ul><h3 id="prometheus组件"><a href="#prometheus组件" class="headerlink" title="prometheus组件"></a>prometheus组件</h3><p>Prometheus 生态系统由多个组件组成，其中有许多组件是可选的：</p><ul><li>Prometheus Server作为服务端，用来存储时间序列数据。</li><li>客户端库用来检测应用程序代码。</li><li>用于支持临时任务的推送网关。</li><li>Exporter用来监控 HAProxy，StatsD，Graphite 等特殊的监控目标，并向 Prometheus 提供标准格式的监控样本数据。</li><li>alartmanager 用来处理告警。</li><li>其他各种周边工具。</li></ul><h3 id="数据模型"><a href="#数据模型" class="headerlink" title="数据模型"></a>数据模型</h3><p>Prometheus 所有采集的监控数据均以指标（metric）的形式保存在内置的时间序列数据库当中（TSDB）：属于同一指标名称，同一标签集合的、有时间戳标记的数据流。除了存储的时间序列，Prometheus 还可以根据查询请求产生临时的、衍生的时间序列作为返回结果。</p><h4 id="指标名称和标签"><a href="#指标名称和标签" class="headerlink" title="指标名称和标签"></a>指标名称和标签</h4><p>每一条时间序列由指标名称（Metrics Name）以及一组标签（键值对）唯一标识。其中指标的名称（metric name）可以反映被监控样本的含义（例如，<code>http_requests_total</code> — 表示当前系统接收到的 HTTP 请求总量），指标名称只能由 ASCII 字符、数字、下划线以及冒号组成，同时必须匹配正则表达式 <code>[a-zA-Z_:][a-zA-Z0-9_:]*</code>。</p><blockquote><p>注意：冒号用来表示用户自定义的记录规则，不能在 exporter 中或监控对象直接暴露的指标中使用冒号来定义指标名称。</p></blockquote><p>通过使用标签，Prometheus 开启了强大的多维数据模型：对于相同的指标名称，通过不同标签列表的集合，会形成特定的度量维度实例（例如：所有包含度量名称为 <code>/api/tracks</code> 的 http 请求，打上 <code>method=POST</code> 的标签，就会形成具体的 http 请求）。该查询语言在这些指标和标签列表的基础上进行过滤和聚合。改变任何度量指标上的任何标签值（包括添加或删除指标），都会创建新的时间序列。</p><p>标签的名称只能由 ASCII 字符、数字以及下划线组成并满足正则表达式 <code>[a-zA-Z_][a-zA-Z0-9_]*</code>。其中以 <code>__</code> 作为前缀的标签，是系统保留的关键字，只能在系统内部使用。标签的值则可以包含任何 <code>Unicode</code> 编码的字符。</p><h4 id="样本"><a href="#样本" class="headerlink" title="样本"></a>样本</h4><p>在时间序列中的每一个点称为一个样本（sample），样本由以下三部分组成：</p><ul><li><strong>指标</strong>（metric）：指标名称和描述当前样本特征的 labelsets；</li><li><strong>时间戳</strong>（timestamp）：一个精确到毫秒的时间戳；</li><li><strong>样本值</strong>（value）： 一个 folat64 的浮点型数据表示当前样本的值。</li></ul><p><strong>表示方式</strong></p><p>通过如下表达方式表示指定指标名称和指定标签集合的时间序列：</p><pre class="line-numbers language-none"><code class="language-none">&lt;metric name&gt;{&lt;label name&gt;=&lt;label value&gt;, ...}<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>例如，指标名称为 <code>api_http_requests_total</code>，标签为 <code>method="POST"</code> 和 <code>handler="/messages"</code> 的时间序列可以表示为：</p><pre class="line-numbers language-none"><code class="language-none">api_http_requests_total{method="POST", handler="/messages"}<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="指标类型"><a href="#指标类型" class="headerlink" title="指标类型"></a>指标类型</h3><p>Prometheus 的客户端库中提供了四种核心的指标类型。但这些类型只是在客户端库（客户端可以根据不同的数据类型调用不同的 API 接口）和在线协议中，实际在 Prometheus server 中并不对指标类型进行区分，而是简单地把这些指标统一视为无类型的时间序列。</p><h4 id="Counter（计数器）"><a href="#Counter（计数器）" class="headerlink" title="Counter（计数器）"></a>Counter（计数器）</h4><p>Counter 类型代表一种样本数据单调递增的指标，即只增不减，除非监控系统发生了重置。例如，你可以使用 counter 类型的指标来表示服务的请求数、已完成的任务数、错误发生的次数等。counter 主要有两个方法：</p><pre class="line-numbers language-none"><code class="language-none">//将counter值加1.Inc()// 将指定值加到counter值上，如果指定值&lt;0 会panic.Add(float64)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>Counter 类型数据可以让用户方便的了解事件产生的速率的变化，在 PromQL 内置的相关操作函数可以提供相应的分析，比如以 HTTP 应用请求量来进行说明：</p><pre class="line-numbers language-none"><code class="language-none">//通过rate()函数获取HTTP请求量的增长率rate(http_requests_total[5m])//查询当前系统中，访问量前10的HTTP地址topk(10, http_requests_total)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h4 id="Gauge（仪表盘）"><a href="#Gauge（仪表盘）" class="headerlink" title="Gauge（仪表盘）"></a>Gauge（仪表盘）</h4><p>Gauge 类型代表一种样本数据可以任意变化的指标，即可增可减。Gauge 通常用于像温度或者内存使用率这种指标数据，也可以表示能随时增加或减少的“总数”，例如：当前并发请求的数量。</p><p>对于 Gauge 类型的监控指标，通过 PromQL 内置函数delta()可以获取样本在一段时间内的变化情况，例如，计算 CPU 温度在两小时内的差异：</p><pre class="line-numbers language-none"><code class="language-none">dalta(cpu_temp_celsius{host="zeus"}[2h])<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>你还可以通过PromQL 内置函数 <a href="https://www.yangcs.net/prometheus/3-prometheus/functions.html#predictlinear">predict_linear()</a> 基于简单线性回归的方式，对样本数据的变化趋势做出预测。例如，基于 2 小时的样本数据，来预测主机可用磁盘空间在 4 个小时之后的剩余情况：</p><pre class="line-numbers language-none"><code class="language-none">predict_linear(node_filesystem_free{job="node"}[2h], 4 * 3600) &lt; 0<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h4 id="Histogram（直方图）"><a href="#Histogram（直方图）" class="headerlink" title="Histogram（直方图）"></a>Histogram（直方图）</h4><p>在大多数情况下人们都倾向于使用某些量化指标的平均值，例如 CPU 的平均使用率、页面的平均响应时间。这种方式的问题很明显，以系统 API 调用的平均响应时间为例：如果大多数 API 请求都维持在 100ms 的响应时间范围内，而个别请求的响应时间需要 5s，那么就会导致某些 WEB 页面的响应时间落到中位数的情况，而这种现象被称为<strong>长尾问题</strong>。</p><p>为了区分是平均的慢还是长尾的慢，最简单的方式就是按照请求延迟的范围进行分组。例如，统计延迟在 0<del>10ms 之间的请求数有多少而 10</del>20ms 之间的请求数又有多少。通过这种方式可以快速分析系统慢的原因。Histogram 和 Summary 都是为了能够解决这样问题的存在，通过 Histogram 和 Summary 类型的监控指标，我们可以快速了解监控样本的分布情况。</p><p>Histogram 在一段时间范围内对数据进行采样（通常是请求持续时间或响应大小等），并将其计入可配置的存储桶（bucket）中，后续可通过指定区间筛选样本，也可以统计样本总数，最后一般将数据展示为直方图。</p><p>Histogram 类型的样本会提供三种指标（假设指标名称为 <code>&lt;basename&gt;</code>）：</p><ul><li><p>样本的值分布在 bucket 中的数量，命名为 <code>&lt;basename&gt;_bucket{le="&lt;上边界&gt;"}</code>。解释的更通俗易懂一点，这个值表示指标值小于等于上边界的所有样本数量。</p><pre class="line-numbers language-none"><code class="language-none">// 在总共2次请求当中。http 请求响应时间 &lt;=0.005 秒 的请求次数为0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="0.005",} 0.0// 在总共2次请求当中。http 请求响应时间 &lt;=0.01 秒 的请求次数为0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="0.01",} 0.0// 在总共2次请求当中。http 请求响应时间 &lt;=0.025 秒 的请求次数为0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="0.025",} 0.0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="0.05",} 0.0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="0.075",} 0.0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="0.1",} 0.0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="0.25",} 0.0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="0.5",} 0.0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="0.75",} 0.0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="1.0",} 0.0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="2.5",} 0.0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="5.0",} 0.0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="7.5",} 2.0// 在总共2次请求当中。http 请求响应时间 &lt;=10 秒 的请求次数为 2io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="10.0",} 2.0io_namespace_http_requests_latency_seconds_histogram_bucket{path="/",method="GET",code="200",le="+Inf",} 2.0<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>所有样本值的大小总和，命名为 <code>&lt;basename&gt;_sum</code>。</p></li><li><p>样本总数，命名为 <code>&lt;basename&gt;_count</code>。值和 <code>&lt;basename&gt;_bucket{le="+Inf"}</code> 相同。</p></li></ul><h4 id="Summary（摘要）"><a href="#Summary（摘要）" class="headerlink" title="Summary（摘要）"></a>Summary（摘要）</h4><p>与 Histogram 类型类似，用于表示一段时间内的数据采样结果（通常是请求持续时间或响应大小等），但它直接存储了分位数（通过客户端计算，然后展示出来），而不是通过区间来计算。</p><h2 id="prometheus-环境配置"><a href="#prometheus-环境配置" class="headerlink" title="prometheus 环境配置"></a>prometheus 环境配置</h2><h2 id="prometheus使用"><a href="#prometheus使用" class="headerlink" title="prometheus使用"></a>prometheus使用</h2><h3 id="PromQL"><a href="#PromQL" class="headerlink" title="PromQL"></a>PromQL</h3><h3 id="test"><a href="#test" class="headerlink" title="test"></a>test</h3>]]></content>
      
      
      <categories>
          
          <category> 监控 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> prometheus </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【kubernetes包管理工具】Helm</title>
      <link href="/2021/10/19/helm/kubernetes-bao-guan-li-helm/"/>
      <url>/2021/10/19/helm/kubernetes-bao-guan-li-helm/</url>
      
        <content type="html"><![CDATA[<h1 id="【kubernetes包管理工具】Helm"><a href="#【kubernetes包管理工具】Helm" class="headerlink" title="【kubernetes包管理工具】Helm"></a>【kubernetes包管理工具】Helm</h1><blockquote><p>Helm 官网: <a href="https://helm.sh/">https://helm.sh/</a></p><p>Helm github地址: <a href="https://github.com/helm/">https://github.com/helm/</a></p><p>helm go api: <a href="https://pkg.go.dev/helm.sh/helm/v3">https://pkg.go.dev/helm.sh/helm/v3</a></p></blockquote><h2 id="Helm简介"><a href="#Helm简介" class="headerlink" title="Helm简介"></a>Helm简介</h2><p>Kubernetes 是一个分布式的容器集群管理系统，它把集群中的管理资源抽象化成一个个 API 对象，并且推荐使用声明式的方式创建，修改，删除这些对象，每个 API 对象都通过一个 <code>yaml</code> 格式或者 <code>json</code> 格式的文本来声明。这带来的一个问题就是这些 API 对象声明文本的管理成本，每当我需要创建一个应用，都需要去编写一堆这样的声明文件。</p><p>Helm 就是用来管理这些 API 对象的工具。它类似于 CentOS 的 YUM 包管理，Ubuntu 的 APT 包管理，你可以把它理解成 <strong>Kubernetes 的包管理工具</strong>。它能够把创建一个应用所需的所有 Kubernetes API 对象声明文件组合并打包在一起。并提供了仓库的机制便于分发共享，还支持模版变量替换，，同时还有版本的概念，使之能够对一个应用进行版本的管理。</p><h2 id="Helm架构"><a href="#Helm架构" class="headerlink" title="Helm架构"></a>Helm架构</h2><p><strong>Helm 采用客户端/服务器架构，有如下组件（概念）组成：</strong></p><ul><li><strong>Chart:</strong> 就是 Helm 的一个包（package），包含一个应用所有的 Kubernetes manifest 模版，类似于 YUM 的 RPM 或者 APT 的 dpkg 文件。</li><li><strong>Helm CLI:</strong> Helm 的客户端组件，它通过 gRPC aAPI 向 tiller 发送请求。</li><li><strong>Tiller:</strong> Helm 的服务器端组件，在 Kubernetes 群集上运行，负载解析客户端端发送过来的 Chart，并根据 Chart 中的定义在 Kubernetes 中创建出相应的资源，tiller 把 release 相关的信息存入 Kubernetes 的 ConfigMap 中。</li><li><strong>Repository:</strong> 用于发布和存储 Chart 的仓库。</li><li><strong>Release:</strong> 可以理解成 Chart 部署的一个实例。通过 Chart 在 Kubernetes 中部署的应用都会产生一个唯一的 Release，即使是同一个 Chart，部署多次就会产生多个 Release。</li></ul><h2 id="Helm环境配置"><a href="#Helm环境配置" class="headerlink" title="Helm环境配置"></a>Helm环境配置</h2><h3 id="二进制安装"><a href="#二进制安装" class="headerlink" title="二进制安装"></a>二进制安装</h3><h3 id="脚本安装"><a href="#脚本安装" class="headerlink" title="脚本安装"></a>脚本安装</h3><h3 id="包管理器安装"><a href="#包管理器安装" class="headerlink" title="包管理器安装"></a>包管理器安装</h3><h2 id="Helm命令行"><a href="#Helm命令行" class="headerlink" title="Helm命令行"></a>Helm命令行</h2><h2 id="Helm模版"><a href="#Helm模版" class="headerlink" title="Helm模版"></a>Helm模版</h2>]]></content>
      
      
      <categories>
          
          <category> kubernetes包管理 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> helm </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【持续集成交付】Argo</title>
      <link href="/2021/10/19/argo/chi-xu-ji-cheng-jiao-fu-argo/"/>
      <url>/2021/10/19/argo/chi-xu-ji-cheng-jiao-fu-argo/</url>
      
        <content type="html"><![CDATA[<h1 id="Argo"><a href="#Argo" class="headerlink" title="Argo"></a>Argo</h1><blockquote><p>argo 官网: <a href="https://argoproj.github.io/">https://argoproj.github.io/</a></p><p>argo github地址: <a href="https://github.com/argoproj/argoproj">https://github.com/argoproj/argoproj</a></p></blockquote><h2 id="Argo-Workflows"><a href="#Argo-Workflows" class="headerlink" title="Argo Workflows"></a>Argo Workflows</h2><h3 id="Argo-Workflows简介"><a href="#Argo-Workflows简介" class="headerlink" title="Argo Workflows简介"></a>Argo Workflows简介</h3><p>Argo Workflows 是一个开源容器原生工作流引擎，用于在 Kubernetes 上编排并行作业。 Argo 工作流作为 Kubernetes CRD 实现。</p><p><strong>特点：</strong></p><ul><li>工作流的每一步都是一个容器</li><li>将多步骤工作流建模为一系列任务，或者使用有向无环图（DAG）描述任务之间的依赖关系</li><li>可以在短时间内轻松运行用于机器学习或数据处理的计算密集型作业</li><li>在Kubernetes上运行CI/CD Pipeline，无需复杂的软件配置</li></ul><h3 id="Argo-Workflows核心概念"><a href="#Argo-Workflows核心概念" class="headerlink" title="Argo Workflows核心概念"></a>Argo Workflows核心概念</h3><h4 id="Workflow"><a href="#Workflow" class="headerlink" title="Workflow"></a>Workflow</h4><p>workflow是argo中最重要的资源，并提供了两个重要的功能：</p><ul><li>定义了执行的工作流</li><li>存储工作流的状态</li></ul><p>由于这些双重职责，应该将workflow视为“live”对象。 它不仅是静态定义，而且是所述定义的“instance”。</p><h5 id="workflow-spec"><a href="#workflow-spec" class="headerlink" title="workflow spec"></a>workflow spec</h5><p>要执行的工作流程在Workflow.Spec字段中定义。 workflow spec 的核心结构是模板列表和<code>entrypoint</code>。</p><p>模板可以松散地被认为是“functions”：它们定义要执行的指令。 entryPoint字段定义“main”功能将是什么 - 即首先执行的模板。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">apiVersion</span><span class="token punctuation">:</span> argoproj.io/v1alpha1<span class="token key atrule">kind</span><span class="token punctuation">:</span> Workflow<span class="token key atrule">metadata</span><span class="token punctuation">:</span>  <span class="token key atrule">generateName</span><span class="token punctuation">:</span> argo<span class="token punctuation">-</span>test  <span class="token comment"># workflow name</span><span class="token key atrule">spec</span><span class="token punctuation">:</span>  <span class="token key atrule">entrypoint</span><span class="token punctuation">:</span> whalesay <span class="token comment"># 定义“whalesay”作为“main”模版</span>  <span class="token key atrule">templates</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> whalesay <span class="token comment"># 定义“whalesay”模版</span>      <span class="token key atrule">container</span><span class="token punctuation">:</span>        <span class="token key atrule">image</span><span class="token punctuation">:</span> docker/whalesay        <span class="token key atrule">command</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>cowsay<span class="token punctuation">]</span>        <span class="token key atrule">args</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token string">"hello world"</span><span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>container</strong><br>最常见的模板类型，它将调度一个容器。 模板的spec与k8s container spec相同。</p><p><strong>script</strong><br>这个spec和容器相同，但是提供了一个<code>source:</code>字段允许你去定义脚本，这个脚本会被保存在文件中并执行, 脚本的结果将自动导出到ARGO变量<code>{{tasks.<name>.outputs.result}}</name></code> or <code>{{steps.<name>.outputs.result}}</name></code>,取决于调用方式。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> gen<span class="token punctuation">-</span>random<span class="token punctuation">-</span>int  <span class="token key atrule">script</span><span class="token punctuation">:</span>    <span class="token key atrule">image</span><span class="token punctuation">:</span> python<span class="token punctuation">:</span>alpine3.6    <span class="token key atrule">command</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>python<span class="token punctuation">]</span>    <span class="token key atrule">source</span><span class="token punctuation">:</span> <span class="token punctuation">|</span><span class="token scalar string">      import random      i = random.randint(1, 100)      print(i)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>resource</strong><br>直接对群集资源执行操作。 他能够用于在集群中get，create，apply，delete，replace，patch资源.</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> k8s<span class="token punctuation">-</span>owner<span class="token punctuation">-</span>reference  <span class="token key atrule">resource</span><span class="token punctuation">:</span>    <span class="token key atrule">action</span><span class="token punctuation">:</span> create    <span class="token key atrule">manifest</span><span class="token punctuation">:</span> <span class="token punctuation">|</span><span class="token scalar string">      apiVersion: v1      kind: ConfigMap      metadata:        generateName: owned-eg-      data:        some: value</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>suspend</strong><br>suspend模板将暂停执行，无论是持续时间还是直到手动恢复。 可以从CLI（带ARGO RESUME），API端点或UI恢复暂停模板。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> delay  <span class="token key atrule">suspend</span><span class="token punctuation">:</span>    <span class="token key atrule">duration</span><span class="token punctuation">:</span> <span class="token string">"20s"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><strong>steps</strong><br>steps模板允许您在一系列步骤中定义任务。 模板的结构是“list of lists”。 外部列表将按顺序运行，内部列表将并行运行。 您可以设置各种选项来控制执行.</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># step1先执行，当step1执行完成，step2a和step2b将并行执行</span>  <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> hello<span class="token punctuation">-</span>hello<span class="token punctuation">-</span>hello    <span class="token key atrule">steps</span><span class="token punctuation">:</span>    <span class="token punctuation">-</span> <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> step1         <span class="token key atrule">template</span><span class="token punctuation">:</span> prepare<span class="token punctuation">-</span>data    <span class="token punctuation">-</span> <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> step2a        <span class="token key atrule">template</span><span class="token punctuation">:</span> run<span class="token punctuation">-</span>data<span class="token punctuation">-</span>first<span class="token punctuation">-</span>half      <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> step2b        <span class="token key atrule">template</span><span class="token punctuation">:</span> run<span class="token punctuation">-</span>data<span class="token punctuation">-</span>second<span class="token punctuation">-</span>half<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>dag</strong><br>dag模版允许你定义一个有向无环状图。在DAG中，您列出了所有任务和设置，在特定任务开始之前必须完成其他任务。 没有任何依赖项的任务将立即运行。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># A先执行，B，C会并行执行，D最后运行</span>  <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> diamond    <span class="token key atrule">dag</span><span class="token punctuation">:</span>      <span class="token key atrule">tasks</span><span class="token punctuation">:</span>      <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> A        <span class="token key atrule">template</span><span class="token punctuation">:</span> echo      <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> B        <span class="token key atrule">dependencies</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>A<span class="token punctuation">]</span>        <span class="token key atrule">template</span><span class="token punctuation">:</span> echo      <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> C        <span class="token key atrule">dependencies</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>A<span class="token punctuation">]</span>        <span class="token key atrule">template</span><span class="token punctuation">:</span> echo      <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> D        <span class="token key atrule">dependencies</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>B<span class="token punctuation">,</span> C<span class="token punctuation">]</span>        <span class="token key atrule">template</span><span class="token punctuation">:</span> echo<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="Argo命令行"><a href="#Argo命令行" class="headerlink" title="Argo命令行"></a>Argo命令行</h3><h3 id="workflow变量"><a href="#workflow变量" class="headerlink" title="workflow变量"></a>workflow变量</h3><h4 id="Template-Tag-Kinds"><a href="#Template-Tag-Kinds" class="headerlink" title="Template Tag Kinds"></a>Template Tag Kinds</h4><p>workflow提供了两种类型的tag</p><ul><li>simple： <code>{{workflow.name}}</code></li><li>express：<code>{{=workflow.name}}</code></li></ul><h5 id="表达式语法"><a href="#表达式语法" class="headerlink" title="表达式语法"></a>表达式语法</h5><h3 id="Argo-Workflows环境配置"><a href="#Argo-Workflows环境配置" class="headerlink" title="Argo Workflows环境配置"></a>Argo Workflows环境配置</h3><h2 id="Argo-CD"><a href="#Argo-CD" class="headerlink" title="Argo CD"></a>Argo CD</h2><blockquote><p><a href="https://github.com/Hacker-Linner/argocd-cn/blob/main/docs/index.md">https://github.com/Hacker-Linner/argocd-cn/blob/main/docs/index.md</a></p></blockquote><h3 id="Argo-CD简介"><a href="#Argo-CD简介" class="headerlink" title="Argo CD简介"></a>Argo CD简介</h3><p>Argo CD遵循GitOps模式，该模式使用Git存储库作为定义所需应用程序状态的真实来源。</p><p><strong>Kubernetes清单可以通过几种方式指定</strong>：</p><ul><li>kustomize applications</li><li>helm charts</li><li>ksonnet applications</li><li>jsonnet files</li><li>Plain directory of YAML/json manifests</li><li>Any custom config management tool configured as a config management plugin</li></ul><p>Argo CD可在指定的目标环境中自动部署所需的应用程序状态。应用程序部署可以在Git提交时跟踪对分支，标签的更新，或固定到清单的特定版本。</p><h3 id="Argo-CD-架构"><a href="#Argo-CD-架构" class="headerlink" title="Argo CD 架构"></a>Argo CD 架构</h3><p>Argo CD被实现为kubernetes控制器，该控制器连续监视正在运行的应用程序，并将当前的活动状态与所需的目标状态（在Git存储库中指定）进行比较。处于活动状态偏离目标状态的已部署应用程序被视为OutOfSync。Argo CD报告并可视化差异，同时提供了自动或手动将实时状态同步回所需目标状态的功能。在Git存储库中对所需目标状态所做的任何修改都可以自动应用并反映在指定的目标环境中。</p><h3 id="Argo-CD核心概念"><a href="#Argo-CD核心概念" class="headerlink" title="Argo CD核心概念"></a>Argo CD核心概念</h3><h3 id="Argo-CD环境配置"><a href="#Argo-CD环境配置" class="headerlink" title="Argo CD环境配置"></a>Argo CD环境配置</h3><h2 id="Argo-Rollouts"><a href="#Argo-Rollouts" class="headerlink" title="Argo Rollouts"></a>Argo Rollouts</h2><h2 id="Argo-Events"><a href="#Argo-Events" class="headerlink" title="Argo Events"></a>Argo Events</h2>]]></content>
      
      
      <categories>
          
          <category> 持续集成交付 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> argo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Jenkins</title>
      <link href="/2021/10/19/jenkins/chi-xu-ji-cheng-jiao-fu-jenkins/"/>
      <url>/2021/10/19/jenkins/chi-xu-ji-cheng-jiao-fu-jenkins/</url>
      
        <content type="html"><![CDATA[<h1 id="Jenkins"><a href="#Jenkins" class="headerlink" title="Jenkins"></a>Jenkins</h1><blockquote><p>jenkins 官网: <a href="https://www.jenkins.io/">https://www.jenkins.io/</a></p><p>jenkins github地址: <a href="https://github.com/jenkinsci/jenkins">https://github.com/jenkinsci/jenkins</a></p></blockquote><h2 id="Jenkins简介"><a href="#Jenkins简介" class="headerlink" title="Jenkins简介"></a>Jenkins简介</h2><h2 id="Jenkins环境配置"><a href="#Jenkins环境配置" class="headerlink" title="Jenkins环境配置"></a>Jenkins环境配置</h2><ul><li>kubernetes安装</li></ul>]]></content>
      
      
      <categories>
          
          <category> 持续集成交付 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> jenkins </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【编程语言】C++ Basic Grammar</title>
      <link href="/2021/10/19/c-c/grammar/bian-cheng-yu-yan-c-basic-grammar/"/>
      <url>/2021/10/19/c-c/grammar/bian-cheng-yu-yan-c-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="C-基础语法"><a href="#C-基础语法" class="headerlink" title="C++基础语法"></a>C++基础语法</h1>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 编译型 </tag>
            
            <tag> c++ </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【编程语言】C Basic Grammar</title>
      <link href="/2021/10/19/c-c/grammar/bian-cheng-yu-yan-c-basic-grammar/"/>
      <url>/2021/10/19/c-c/grammar/bian-cheng-yu-yan-c-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="C基础语法"><a href="#C基础语法" class="headerlink" title="C基础语法"></a>C基础语法</h1>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 编译型 </tag>
            
            <tag> c </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Ruby Basic Grammar</title>
      <link href="/2021/10/19/ruby/grammar/bian-cheng-yu-yan-ruby-basic-grammar/"/>
      <url>/2021/10/19/ruby/grammar/bian-cheng-yu-yan-ruby-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="Ruby基础语法"><a href="#Ruby基础语法" class="headerlink" title="Ruby基础语法"></a>Ruby基础语法</h1>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> ruby </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>HTML Basic Grammar</title>
      <link href="/2021/10/19/frontend/html/bian-cheng-yu-yan-html-basic-grammar/"/>
      <url>/2021/10/19/frontend/html/bian-cheng-yu-yan-html-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="Html基础语法"><a href="#Html基础语法" class="headerlink" title="Html基础语法"></a>Html基础语法</h1><h2 id="html简介"><a href="#html简介" class="headerlink" title="html简介"></a>html简介</h2><p><strong>HTML</strong>（超文本标记语言——HyperText Markup Language）它定义了网页内容的含义和结构。“超文本”（hypertext）是指连接单个网站内或多个网站间的网页的链接。</p><p> HTML是一种相当简单的、由不同元素组成的标记语言，它可以应用于文本片段，使文本在文档中具有不同的含义，将文档结构化为逻辑块，并且可以将图片，影像等内容嵌入到页面中。</p><h2 id="html基础概念"><a href="#html基础概念" class="headerlink" title="html基础概念"></a>html基础概念</h2><h3 id="html元素"><a href="#html元素" class="headerlink" title="html元素"></a>html元素</h3><pre class="line-numbers language-html" data-language="html"><code class="language-html"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>p</span><span class="token punctuation">&gt;</span></span>hello world<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>p</span><span class="token punctuation">&gt;</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>元素主要部分：</p><ul><li><strong>开始标签</strong>（Opening tag）：包含元素的名称，被左、右角括号所包围。表示元素从这里开始或者开始起作用</li><li><strong>结束标签</strong>（Closing tag）：与开始标签相似，只是其在元素名之前包含了一个斜杠。这表示着元素的结尾 </li><li><strong>内容</strong>（Content）：元素的内容</li><li><strong>元素</strong>（Element）：开始标签、结束标签与内容相结合，便是一个完整的元素</li></ul><h4 id="嵌套元素"><a href="#嵌套元素" class="headerlink" title="嵌套元素"></a>嵌套元素</h4><p>把元素放到其它元素之中——这被称作嵌套</p><pre class="line-numbers language-html" data-language="html"><code class="language-html"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>p</span><span class="token punctuation">&gt;</span></span>hello<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>strong</span><span class="token punctuation">&gt;</span></span>world<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>strong</span><span class="token punctuation">&gt;</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>p</span><span class="token punctuation">&gt;</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h4 id="块级元素和内联元素"><a href="#块级元素和内联元素" class="headerlink" title="块级元素和内联元素"></a>块级元素和内联元素</h4><ul><li>块级元素在页面中以块的形式展现 —— 相对于其前面的内容它会出现在新的一行，其后的内容也会被挤到下一行展现。块级元素通常用于展示页面上结构化的内容，例如段落、列表、导航菜单、页脚等等。一个以block形式展现的块级元素不会被嵌套进内联元素中，但可以嵌套在其它块级元素中。</li><li>内联元素通常出现在块级元素中并环绕文档内容的一小部分，而不是一整个段落或者一组内容。内联元素不会导致文本换行：它通常出现在一堆文字之间.</li></ul><h4 id="空元素"><a href="#空元素" class="headerlink" title="空元素"></a>空元素</h4><p>不是所有元素都拥有开始标签，内容，结束标签。一些元素只有一个标签，通常用来在此元素所在位置插入/嵌入一些东西。</p><pre class="line-numbers language-html" data-language="html"><code class="language-html"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>img</span> <span class="token attr-name">src</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span><span class="token punctuation">"</span></span><span class="token punctuation">&gt;</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="html属性"><a href="#html属性" class="headerlink" title="html属性"></a>html属性</h3><p>属性包含元素的额外信息，这些信息不会出现在实际的内容中。</p><p>一个属性必须包含如下内容：</p><ul><li><p>一个空格，在属性和元素名称之间。(如果已经有一个或多个属性，就与前一个属性之间有一个空格。)</p></li><li><p>属性名称，后面跟着一个等于号。</p></li><li><p>一个属性值，由一对引号“ ”引起来。(最好加上引号)</p></li></ul><h4 id="布尔属性"><a href="#布尔属性" class="headerlink" title="布尔属性"></a>布尔属性</h4><p>有时你会看到没有值的属性，它是合法的。这些属性被称为布尔属性，他们只能有跟它的属性名一样的属性值。</p><pre class="line-numbers language-html" data-language="html"><code class="language-html"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>input</span> <span class="token attr-name">type</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>text<span class="token punctuation">"</span></span> <span class="token attr-name">disabled</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>diabled<span class="token punctuation">"</span></span><span class="token punctuation">&gt;</span></span> <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>input</span> <span class="token attr-name">type</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>text<span class="token punctuation">"</span></span> <span class="token attr-name">disabled</span><span class="token punctuation">&gt;</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h3 id="html文档"><a href="#html文档" class="headerlink" title="html文档"></a>html文档</h3><pre class="line-numbers language-html" data-language="html"><code class="language-html"><span class="token doctype"><span class="token punctuation">&lt;!</span><span class="token doctype-tag">DOCTYPE</span> <span class="token name">html</span><span class="token punctuation">&gt;</span></span> <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>html</span> <span class="token attr-name">lang</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>en<span class="token punctuation">"</span></span><span class="token punctuation">&gt;</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>head</span><span class="token punctuation">&gt;</span></span>    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>meta</span> <span class="token attr-name">charset</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>UTF-8<span class="token punctuation">"</span></span><span class="token punctuation">&gt;</span></span>    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>title</span><span class="token punctuation">&gt;</span></span>Title<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>title</span><span class="token punctuation">&gt;</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>head</span><span class="token punctuation">&gt;</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>body</span><span class="token punctuation">&gt;</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>body</span><span class="token punctuation">&gt;</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>html</span><span class="token punctuation">&gt;</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><code>&lt;!DOCTYPE html&gt;</code>: 声明文档类型.</li><li><code>&lt;html&gt;&lt;/html&gt;</code>: <code>&lt;html&gt;</code>元素。这个元素包裹了整个完整的页面，是一个根元素。</li><li><code>&lt;head&gt;&lt;/head&gt;</code>: <code>&lt;head&gt;元素</code>. 这个元素是一个容器，它包含了所有你想包含在HTML页面中但不想在HTML页面中显示的内容。这些内容包括你想在搜索结果中出现的关键字和页面描述，CSS样式，字符集声明等等。</li><li><code>&lt;meta charset="UTF-8"&gt;</code>：这个元素设置文档使用utf-8字符集编码，utf-8字符集包含了人类大部分的文字。基本上他能识别你放上去的所有文本内容。毫无疑问要使用它，并且它能在以后避免很多其他问题。</li><li><code>&lt;title&gt;&lt;/title&gt;</code>: 设置页面标题，出现在浏览器标签上，当你标记/收藏页面时它可用来描述页面。</li><li><code>&lt;body&gt;&lt;/body&gt;</code>: <code>&lt;body&gt;</code>元素。 包含了你访问页面时所有显示在页面上的内容，文本，图片，音频，游戏等等。</li></ul><h4 id="html元数据-lt-meta元素-gt"><a href="#html元数据-lt-meta元素-gt" class="headerlink" title="html元数据<meta元素>"></a>html元数据&lt;meta元素&gt;</h4><p>元数据就是描述数据的数据，而HTML有一个“官方的”方式来为一个文档添加元数据</p><blockquote><p>facebook 元数据协议：<a href="https://ogp.me/">https://ogp.me/</a></p></blockquote><p><strong>站点自定义图标</strong></p><ul><li><p>将其保存在与网站的索引页面相同的目录中，以.ico格式保存。</p></li><li><p>将以下行添加到HTML 中以引用它：</p><pre class="line-numbers language-html" data-language="html"><code class="language-html"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>link</span> <span class="token attr-name">rel</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>shortcut icon<span class="token punctuation">"</span></span> <span class="token attr-name">href</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>favicon.ico<span class="token punctuation">"</span></span> <span class="token attr-name">type</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>image/x-icon<span class="token punctuation">"</span></span><span class="token punctuation">&gt;</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ul><h4 id="html空白"><a href="#html空白" class="headerlink" title="html空白"></a>html空白</h4><p>无论你在HTML元素的内容中使用多少空格(包括空白字符，包括换行)，当渲染这些代码的时候，HTML解释器会将连续出现的空白字符减少为一个单独的空格符。</p><pre class="line-numbers language-html" data-language="html"><code class="language-html"><span class="token comment">&lt;!--这两个代码片段是等价的--&gt;</span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>p</span><span class="token punctuation">&gt;</span></span>狗 狗 很 呆 萌。<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>p</span><span class="token punctuation">&gt;</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>p</span><span class="token punctuation">&gt;</span></span>狗 狗        很         呆 萌。<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>p</span><span class="token punctuation">&gt;</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h4 id="html特殊字符"><a href="#html特殊字符" class="headerlink" title="html特殊字符"></a>html特殊字符</h4><p>在HTML中，字符 <code>&lt;</code>, <code>&gt;</code>,<code>"</code>,<code>'</code> 和 <code>&amp;</code> 是特殊字符. 它们是HTML语法自身的一部分，如果想要使用这些特殊字符，必须将其转义。</p><table><thead><tr><th align="left">原义字符</th><th align="left">等价字符引用</th></tr></thead><tbody><tr><td align="left">&lt;</td><td align="left">&amp;lt;</td></tr><tr><td align="left">&gt;</td><td align="left">&amp;gt;</td></tr><tr><td align="left">“</td><td align="left">&amp;quot;</td></tr><tr><td align="left">‘</td><td align="left">&amp;apos;</td></tr><tr><td align="left">&amp;</td><td align="left">&amp;amp;</td></tr></tbody></table><h4 id="html常用标签"><a href="#html常用标签" class="headerlink" title="html常用标签"></a>html常用标签</h4><table><thead><tr><th align="left">标签</th><th align="left">描述</th></tr></thead><tbody><tr><td align="left"><strong>基础</strong></td><td align="left"></td></tr><tr><td align="left"><code>&lt;!DOCTYPE&gt;</code></td><td align="left">定义文档类型。</td></tr><tr><td align="left"></td><td align="left">定义一个 HTML 文档</td></tr><tr><td align="left"><code>&lt;title&gt;&lt;/title&gt;</code></td><td align="left">为文档定义一个标题</td></tr><tr><td align="left"><code>&lt;body&gt;&lt;/body&gt;</code></td><td align="left">定义文档的主体</td></tr><tr><td align="left"><code>&lt;h1&gt;&lt;/h1&gt; &lt;h6&gt;&lt;/h6&gt;</code></td><td align="left">定义 HTML 标题</td></tr><tr><td align="left"><code>&lt;p&gt;&lt;/p&gt;</code></td><td align="left">定义一个段落</td></tr><tr><td align="left"><code>&lt;br&gt;</code></td><td align="left">定义简单的折行。</td></tr><tr><td align="left"><code>&lt;hr&gt;</code></td><td align="left">定义水平线。</td></tr><tr><td align="left"><strong>格式</strong></td><td align="left"></td></tr><tr><td align="left"><code>&lt;abbr&gt;</code></td><td align="left">定义一个缩写。</td></tr><tr><td align="left"><code>&lt;address&gt;</code></td><td align="left">定义文档作者或拥有者的联系信息。</td></tr><tr><td align="left"><code>&lt;b&gt;&lt;/b&gt;</code></td><td align="left">定义粗体文本。</td></tr><tr><td align="left"></td><td align="left">允许您设置一段文本，使其脱离其父元素的文本方向设置。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-bdo.html"></a></td><td align="left">定义文本的方向。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-blockquote.html"></a></td><td align="left">定义块引用。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-cite.html"></a></td><td align="left">定义引用(citation)。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-code.html"></a></td><td align="left">定义计算机代码文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-del.html"></a></td><td align="left">定义被删除文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-dfn.html"></a></td><td align="left">定义定义项目。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-em.html"></a></td><td align="left">定义强调文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-i.html"></a></td><td align="left">定义斜体文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-ins.html"></a></td><td align="left">定义被插入文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-kbd.html"></a></td><td align="left">定义键盘文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-mark.html"></a><strong>New</strong></td><td align="left">定义带有记号的文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-meter.html"></a><strong>New</strong></td><td align="left">定义度量衡。仅用于已知最大和最小值的度量。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-pre.html"></a></td><td align="left">定义预格式文本</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-progress.html"></a><strong>New</strong></td><td align="left">定义运行中的任务进度（进程）。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-q.html"></a></td><td align="left">定义短的引用。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-rp.html"></a><strong>New</strong></td><td align="left">定义不支持 ruby 元素的浏览器所显示的内容。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-rt.html"></a><strong>New</strong></td><td align="left">定义字符（中文注音或字符）的解释或发音。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-ruby.html"></a><strong>New</strong></td><td align="left">定义 ruby 注释（中文注音或字符）。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-s.html"></a></td><td align="left">定义加删除线的文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-samp.html"></a></td><td align="left">定义计算机代码样本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-small.html"></a></td><td align="left">定义小号文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-strong.html"></a></td><td align="left">定义语气更为强烈的强调文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-sub.html"></a></td><td align="left">定义下标文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-sup.html"></a></td><td align="left">定义上标文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-time.html"></a><strong>New</strong></td><td align="left">定义一个日期/时间</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-u.html"></a></td><td align="left">定义下划线文本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-var.html"></a></td><td align="left">定义文本的变量部分。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-wbr.html"></a><strong>New</strong></td><td align="left">规定在文本中的何处适合添加换行符。</td></tr><tr><td align="left"><strong>表单</strong></td><td align="left"></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-form.html"></a></td><td align="left">定义一个 HTML 表单，用于用户输入。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-input.html"></a></td><td align="left">定义一个输入控件</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-textarea.html"></a></td><td align="left">定义多行的文本输入控件。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-button.html"></a></td><td align="left">定义按钮。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-select.html"></a></td><td align="left">定义选择列表（下拉列表）。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-optgroup.html"></a></td><td align="left">定义选择列表中相关选项的组合。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-option.html"></a></td><td align="left">定义选择列表中的选项。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-label.html"></a></td><td align="left">定义 input 元素的标注。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-fieldset.html"></a></td><td align="left">定义围绕表单中元素的边框。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-legend.html"></a></td><td align="left">定义 fieldset 元素的标题。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-datalist.html"></a><strong>New</strong></td><td align="left">规定了 input 元素可能的选项列表。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-keygen.html"></a><strong>New</strong></td><td align="left">规定用于表单的密钥对生成器字段。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-output.html"></a><strong>New</strong></td><td align="left">定义一个计算的结果</td></tr><tr><td align="left"><strong>框架</strong></td><td align="left"></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-iframe.html"></a></td><td align="left">定义内联框架。</td></tr><tr><td align="left"><strong>图像</strong></td><td align="left"></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-img.html"></a></td><td align="left">定义图像。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-map.html"></a></td><td align="left">定义图像映射。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-area.html"></a></td><td align="left">定义图像地图内部的区域。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-canvas.html"></a><strong>New</strong></td><td align="left">通过脚本（通常是 JavaScript）来绘制图形（比如图表和其他图像）。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-figcaption.html"></a><strong>New</strong></td><td align="left">定义一个 caption for a <figure> element</figure></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-figure.html"></a><strong>New</strong></td><td align="left">figure 标签用于对元素进行组合。</td></tr><tr><td align="left"><strong>Audio/Video</strong></td><td align="left"></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-audio.html"></a><strong>New</strong></td><td align="left">定义声音，比如音乐或其他音频流。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-source.html"></a><strong>New</strong></td><td align="left">定义media元素 (<video> 和 <audio>)的媒体资源。media</audio></video></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-track.html"></a><strong>New</strong></td><td align="left">为媒体(<video> 和 <audio>)元素定义外部文本轨道。</audio></video></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-video.html"></a><strong>New</strong></td><td align="left">定义一个音频或者视频</td></tr><tr><td align="left"><strong>链接</strong></td><td align="left"></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-a.html"></a></td><td align="left">定义一个链接</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-link.html"></a></td><td align="left">定义文档与外部资源的关系。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-main.html"></a></td><td align="left">定义文档的主体部分。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-nav.html"></a><strong>New</strong></td><td align="left">定义导航链接</td></tr><tr><td align="left"><strong>列表</strong></td><td align="left"></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-ul.html"></a></td><td align="left">定义一个无序列表</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-ol.html"></a></td><td align="left">定义一个有序列表</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-li.html"></a></td><td align="left">定义一个列表项</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-dl.html"></a></td><td align="left">定义一个定义列表</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-dt.html"></a></td><td align="left">定义一个定义定义列表中的项目。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-dd.html"></a></td><td align="left">定义定义列表中项目的描述。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-menu.html"></a></td><td align="left">定义菜单列表。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-command.html"></a><strong>New</strong></td><td align="left">定义用户可能调用的命令（比如单选按钮、复选框或按钮）。</td></tr><tr><td align="left"><strong>表格</strong></td><td align="left"></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-table.html"></a></td><td align="left">定义一个表格</td></tr><tr><td align="left">](<a href="https://www.runoob.com/tags/tag-caption.html">https://www.runoob.com/tags/tag-caption.html</a>)</td><td align="left">定义表格标题。</td></tr><tr><td align="left">](<a href="https://www.runoob.com/tags/tag-th.html">https://www.runoob.com/tags/tag-th.html</a>)</td><td align="left">定义表格中的表头单元格。</td></tr><tr><td align="left"></td><td align="left">定义表格中的行。</td></tr><tr><td align="left">](<a href="https://www.runoob.com/tags/tag-td.html">https://www.runoob.com/tags/tag-td.html</a>)</td><td align="left">定义表格中的单元。</td></tr><tr><td align="left"></td><td align="left">定义表格中的表头内容。</td></tr><tr><td align="left"></td><td align="left">定义表格中的主体内容。</td></tr><tr><td align="left"></td><td align="left">定义表格中的表注内容（脚注）。</td></tr><tr><td align="left"></td><td align="left">定义表格中一个或多个列的属性值。</td></tr><tr><td align="left"></td><td align="left">定义表格中供格式化的列组。</td></tr><tr><td align="left"><strong>样式/节</strong></td><td align="left"></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-style.html"></a></td><td align="left">定义文档的样式信息。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-div.html"></a></td><td align="left">定义文档中的节。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-span.html"></a></td><td align="left">定义文档中的节。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-header.html"></a><strong>New</strong></td><td align="left">定义一个文档头部部分</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-footer.html"></a><strong>New</strong></td><td align="left">定义一个文档底部</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-section.html"></a><strong>New</strong></td><td align="left">定义了文档的某个区域</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-article.html"></a><strong>New</strong></td><td align="left">定义一个文章内容</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-aside.html"></a><strong>New</strong></td><td align="left">定义其所处内容之外的内容。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-details.html"></a><strong>New</strong></td><td align="left">定义了用户可见的或者隐藏的需求的补充细节。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-dialog.html"></a><strong>New</strong></td><td align="left">定义一个对话框或者窗口</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-summary.html"></a><strong>New</strong></td><td align="left">定义一个可见的标题。 当用户点击标题时会显示出详细信息。</td></tr><tr><td align="left"><strong>元信息</strong></td><td align="left"></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-head.html"></a></td><td align="left">定义关于文档的信息</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-meta.html"></a></td><td align="left">定义关于 HTML 文档的元信息。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-base.html"></a></td><td align="left">定义页面中所有链接的默认地址或默认目标。</td></tr><tr><td align="left"><strong>程序</strong></td><td align="left"></td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-script.html"></a></td><td align="left">定义客户端脚本。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-noscript.html"></a></td><td align="left">定义针对不支持客户端脚本的用户的替代内容。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-embed.html"></a><strong>New</strong></td><td align="left">定义了一个容器，用来嵌入外部应用或者互动程序（插件）。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-object.html"></a></td><td align="left">定义嵌入的对象。</td></tr><tr><td align="left"><a href="https://www.runoob.com/tags/tag-param.html"></a></td><td align="left">定义对象的参数。</td></tr></tbody></table>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> html </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CSS Basic Grammar</title>
      <link href="/2021/10/19/frontend/css/bian-cheng-yu-yan-css-basic-grammar/"/>
      <url>/2021/10/19/frontend/css/bian-cheng-yu-yan-css-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="【编程语言】CSS基础语法"><a href="#【编程语言】CSS基础语法" class="headerlink" title="【编程语言】CSS基础语法"></a>【编程语言】CSS基础语法</h1>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> css </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【编程语言】Shell Basic Grammar</title>
      <link href="/2021/10/19/linux/shell/bian-cheng-yu-yan-shell-basic-grammar/"/>
      <url>/2021/10/19/linux/shell/bian-cheng-yu-yan-shell-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="【编程语言】Shell基础语法"><a href="#【编程语言】Shell基础语法" class="headerlink" title="【编程语言】Shell基础语法"></a>【编程语言】Shell基础语法</h1><h2 id="shell简介"><a href="#shell简介" class="headerlink" title="shell简介"></a>shell简介</h2><p>Shell是一个命令行解释器，它为用户提供了一个面向Linux内核发送请求以便运行程序的界面系统级程序，用户可以用Shell来启动、挂起、停止甚至是编写一些程序。Shell还是一个功能相当强大的编程语言，易编写，易调试，灵活性较强。Shell是解释执行的脚本语言，在Shell中可以直接调用Linux系统命令。</p><p><strong>shell执行方式</strong>：</p><ul><li><strong>交互式（Interactive）</strong>：用户输入一条命令，Shell就解释执行一条</li><li><strong>批处理（Batch）</strong>： 用户事先写一个Shell脚本（Script），其中有很多条命令，让Shell一次把这些命令执行完</li></ul><h4 id="shell分类"><a href="#shell分类" class="headerlink" title="shell分类"></a>shell分类</h4><p>由于历史原因，UNIX系统上有很多种Shell：</p><ul><li>sh（Bourne Shell）：由Steve Bourne开发，各种UNIX系统都配有sh。</li><li>csh（C Shell）：由Bill Joy开发，随BSD UNIX发布，它的流程控制语句很像C语言，支持很多Bourne Shell所不支持的功能：作业控制，命令历史，命令行编辑。</li><li>ksh（Korn Shell）：由David Korn开发，向后兼容sh的功能，并且添加了csh引入的新功能，是目前很多UNIX系统标准配置的Shell，在这些系统上/bin/sh往往是指向/bin/ksh的符号链接。</li><li>tcsh（TENEX C Shell）：是csh的增强版本，引入了命令补全等功能，在FreeBSD、MacOS X等系统上替代了csh。</li><li>bash（Bourne Again Shell）：由GNU开发的Shell，主要目标是与POSIX标准保持一致，同时兼顾对sh的兼容，bash从csh和ksh借鉴了很多功能，是各种Linux发行版标准配置的Shell，在Linux系统上/bin/sh往往是指向/bin/bash的符号链接。虽然如此，bash和sh还是有很多不同的，一方面，bash扩展了一些命令和参数，另一方面，bash并不完全和sh兼容，有些行为并不一致，所以bash需要模拟sh的行为：当我们通过sh这个程序名启动bash时，bash可以假装自己是sh，不认扩展的命令，并且行为与sh保持一致。</li></ul><p>bash示例：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token punctuation">[</span>root@localhost ~<span class="token punctuation">]</span><span class="token comment"># vim /etc/passwd # 其中最后一列显示了用户对应的shell类型 </span>root:x:0:0:root:/root:/bin/bash nobody:x:65534:65534:nobody:/nonexistent:/bin/sh syslog:x:101:103::/home/syslog:/bin/false itcast:x:1000:1000:itcast,,,:/home/itcast:/bin/bash ftp:x:115:125:ftp daemon,,,:/srv/ftp:/bin/false<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>用户在命令行输入命令后，一般情况下Shell会fork并exec该命令，但是Shell的内建命令例外，执行内建命令相当于调用Shell进程中的一个函数，并不创建新的进程。<br>以前学过的cd、alias、umask、exit等命令即是内建命令，凡是用which命令查不到程序文件所在位置的命令都是内建命令，内建命令没有单独的man手册，要在man手册中查看<em>内建命令</em>，应该执行<code>itcast$ man bash-builtins</code><br>如export、shift、if、eval、[、for、while等等。内建命令虽然不创建新的进程，但也会有Exit Status，通常也用0表示成功非零表示失败，虽然内建命令不创建新的进程，但执行结束后也会有一个状态码，也可以用特殊变量$?读出。</p><h2 id="shell变量"><a href="#shell变量" class="headerlink" title="shell变量"></a>shell变量</h2>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> shell </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【编程语言】TypeScript Basic Grammar</title>
      <link href="/2021/10/19/frontend/typescript/bian-cheng-yu-yan-typescript-basic-grammar/"/>
      <url>/2021/10/19/frontend/typescript/bian-cheng-yu-yan-typescript-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="【编程语言】TypeScript基础语法"><a href="#【编程语言】TypeScript基础语法" class="headerlink" title="【编程语言】TypeScript基础语法"></a>【编程语言】TypeScript基础语法</h1><blockquote><p>TypeScript 官方文档: <a href="https://www.typescriptlang.org/">https://www.typescriptlang.org/</a></p><p>TypeScript 中文手册： <a href="https://typescript.bootcss.com/">https://typescript.bootcss.com/</a></p><p>TypeScript github地址: <a href="https://github.com/microsoft/TypeScript">https://github.com/microsoft/TypeScript</a></p></blockquote><h2 id="TS简介"><a href="#TS简介" class="headerlink" title="TS简介"></a>TS简介</h2><p>TypeScript 是一种由微软开发的自由和开源的编程语言。它是 JavaScript 的一个超集，而且本质上向这个语言添加了可选的静态类型和基于类的面向对象编程。<br> TypeScript 扩展了 JavaScript 的句法，所以任何现有的 JavaScript 程序可以不加改变的在 TypeScript 下工作。TypeScript 是为大型应用之开发而设计，而编译时它产生 JavaScript 以确保兼容性。</p><h2 id="TS环境配置"><a href="#TS环境配置" class="headerlink" title="TS环境配置"></a>TS环境配置</h2><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token comment"># 全局安装typescript</span><span class="token function">npm</span> <span class="token function">install</span> -g typescripttsc --version <span class="token comment"># 查看typescript版本</span><span class="token comment"># 工作区安装typescript</span><span class="token function">npm</span> <span class="token function">install</span> typescript --save-dev<span class="token function">yarn</span> <span class="token function">add</span> typescript --dev<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="ts-node配置"><a href="#ts-node配置" class="headerlink" title="ts-node配置"></a>ts-node配置</h3><p>typescript自带的tsc命令并不能直接运行typescript代码。但值得注意的是 ts-node 并不等于 typescript 的 Node.js ，仅仅封装了 typescript 的编译过程，提供直接运行typescript代码的能力</p><pre class="line-numbers language-shell" data-language="shell"><code class="language-shell"><span class="token function">npm</span> <span class="token function">install</span> -g ts-node<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="TS基础类型"><a href="#TS基础类型" class="headerlink" title="TS基础类型"></a>TS基础类型</h2><ul><li><p>number(数字)</p><p>ts中所有的数字都是浮点数（与javascript相同），这些浮点数的类型是number。支持二进制，八进制，十进制，十六进制。</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> binaryLiteral<span class="token operator">:</span> <span class="token builtin">number</span> <span class="token operator">=</span> <span class="token number">0b1010</span><span class="token punctuation">;</span> <span class="token comment">// 二进制</span><span class="token keyword">let</span> octalLiteral<span class="token operator">:</span> <span class="token builtin">number</span> <span class="token operator">=</span> <span class="token number">0o744</span><span class="token punctuation">;</span>   <span class="token comment">// 八进制</span><span class="token keyword">let</span> decLiteral<span class="token operator">:</span> <span class="token builtin">number</span> <span class="token operator">=</span> <span class="token number">6</span><span class="token punctuation">;</span>         <span class="token comment">// 十进制</span><span class="token keyword">let</span> hexLiteral<span class="token operator">:</span> <span class="token builtin">number</span> <span class="token operator">=</span> <span class="token number">0xf00d</span><span class="token punctuation">;</span>    <span class="token comment">// 十六进制</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>boolean(布尔值)</p><p>boolean有两个值：true/false</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> isDone<span class="token operator">:</span> <span class="token builtin">boolean</span> <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>string（字符串）</p><p>string表示文本数据类型，可以使用双引号“或者单引号‘来表示字符串</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> name<span class="token operator">:</span> <span class="token builtin">string</span> <span class="token operator">=</span> <span class="token string">"enderviggin"</span><span class="token punctuation">;</span><span class="token keyword">let</span> name<span class="token operator">:</span> <span class="token builtin">string</span> <span class="token operator">=</span> <span class="token string">'enderviggin'</span><span class="token punctuation">;</span>name <span class="token operator">=</span> <span class="token string">"enderviggin"</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>还可以使用模版字符串，可以用来定义多行文本和内嵌表达式。这种字符串是被反引号包围，并且使用<code>${expr}</code>来嵌入表达式。</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> name<span class="token operator">:</span> <span class="token builtin">string</span> <span class="token operator">=</span> <span class="token string">"enderviggin"</span><span class="token punctuation">;</span><span class="token keyword">let</span> age<span class="token operator">:</span> <span class="token builtin">number</span> <span class="token operator">=</span> <span class="token number">18</span><span class="token punctuation">;</span><span class="token keyword">let</span> sentence<span class="token operator">:</span> <span class="token builtin">string</span> <span class="token operator">=</span> <span class="token template-string"><span class="token template-punctuation string">`</span><span class="token string">my name is </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span> name <span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">.I'll be </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span>age <span class="token operator">+</span> <span class="token number">1</span><span class="token interpolation-punctuation punctuation">}</span></span><span class="token string"> years old</span><span class="token template-punctuation string">`</span></span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>array（数组）</p><ul><li>在元素类型后面加上[],用此表示此类型元素组成的数组</li></ul><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> list<span class="token operator">:</span> <span class="token builtin">number</span><span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">]</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>使用数组泛型，Array&lt;元素类型&gt;</li></ul><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> list<span class="token operator">:</span> <span class="token builtin">Array</span><span class="token operator">&lt;</span><span class="token builtin">number</span><span class="token operator">&gt;</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">]</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li><li><p>tuple（元组）</p><p>元组类型允许表示一个已知<strong>元素数量</strong>和<strong>类型</strong>的数组，各元素的类型不必相同。</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> stringNumber<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token builtin">string</span><span class="token punctuation">,</span> <span class="token builtin">number</span><span class="token punctuation">]</span>stringNumber <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token string">"typescript"</span><span class="token punctuation">,</span> <span class="token number">10</span><span class="token punctuation">]</span> <span class="token comment">// 初始化元组时，元素的顺序必须和声明的顺序一致</span><span class="token comment">// 当访问一个已知索引的元素，会得到正确的类型</span><span class="token builtin">console</span><span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>stringNumber<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">.</span><span class="token function">substr</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>enum(枚举)</p><p>使用枚举类型可以为一组数值赋予友好的名字,默认情况下，从0开始为元素编号，也可以手动编号，或者是对所有的元素手动编号。</p><p>枚举类型可以通过枚举的值的到对应的名字。</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">enum</span> Color <span class="token punctuation">{</span>    Red<span class="token punctuation">,</span>    Green<span class="token punctuation">,</span>    Blue<span class="token punctuation">}</span><span class="token keyword">let</span> green<span class="token operator">:</span> Color <span class="token operator">=</span> Color<span class="token punctuation">.</span>Green<span class="token keyword">let</span> colorName<span class="token operator">:</span> <span class="token builtin">string</span> <span class="token operator">=</span> Color<span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>any(任意值)</p><p>any可以保存任意类型的值，直接跳过类型检查，一般情况下用于传入的变量类型不确定时。</p><p>any与object有类似的功能，但是<code>Object</code>类型的变量只是允许你给它赋任意值 - 但是却不能够在它上面调用任意的方法，即便它真的有这些方法。</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> notSure<span class="token operator">:</span> <span class="token builtin">any</span> <span class="token operator">=</span> <span class="token number">4</span><span class="token punctuation">;</span>notSure <span class="token operator">=</span> <span class="token string">"hello world"</span><span class="token punctuation">;</span>notSure <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">;</span><span class="token keyword">let</span> anyList<span class="token operator">:</span> <span class="token builtin">any</span><span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token boolean">true</span><span class="token punctuation">,</span> <span class="token string">"hello"</span><span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>void(空值)</p><p>某种程度上来说，<code>void</code>类型像是与<code>any</code>类型相反，它表示没有任何类型。如果是void类型的变量，只能赋值为undefined和null</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">function</span> <span class="token function">warnUser</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">:</span> <span class="token keyword">void</span> <span class="token punctuation">{</span><span class="token function">alert</span><span class="token punctuation">(</span><span class="token string">"warning message"</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token keyword">let</span> unusable<span class="token operator">:</span> <span class="token keyword">void</span> <span class="token operator">=</span> <span class="token keyword">undefined</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>null和undefied</p><p><code>undefined</code>和<code>null</code>两者各自有自己的类型分别叫做<code>undefined</code>和<code>null</code>。 和<code>void</code>相似，它们的本身的类型用处不是很大.</p><p>默认情况下null和undefined是所有类型的子类型，可以将其赋值给任意类型。当你指定了<code>--strictNullChecks</code>标记，<code>null</code>和<code>undefined</code>只能赋值给<code>void</code>和它们自己。</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> u<span class="token operator">:</span> <span class="token keyword">undefined</span> <span class="token operator">=</span> <span class="token keyword">undefined</span><span class="token punctuation">;</span><span class="token keyword">let</span> n<span class="token operator">:</span> <span class="token keyword">null</span> <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p>never</p><p><code>never</code>类型表示的是那些永不存在的值的类型。</p><p><code>never</code>类型是任何类型的子类型，也可以赋值给任何类型；然而，<em>没有</em>类型是<code>never</code>的子类型或可以赋值给<code>never</code>类型（除了<code>never</code>本身之外）。 即使<code>any</code>也不可以赋值给<code>never</code>。</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token comment">// 返回never的函数必须存在无法达到的终点</span><span class="token keyword">function</span> <span class="token function">error</span><span class="token punctuation">(</span>message<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">)</span><span class="token operator">:</span> <span class="token builtin">never</span> <span class="token punctuation">{</span>    <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">Error</span><span class="token punctuation">(</span>message<span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token comment">// 推断的返回值类型为never</span><span class="token keyword">function</span> <span class="token function">fail</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>    <span class="token keyword">return</span> <span class="token function">error</span><span class="token punctuation">(</span><span class="token string">"Something failed"</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token comment">// 返回never的函数必须存在无法达到的终点</span><span class="token keyword">function</span> <span class="token function">infiniteLoop</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">:</span> <span class="token builtin">never</span> <span class="token punctuation">{</span>    <span class="token keyword">while</span> <span class="token punctuation">(</span><span class="token boolean">true</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>    <span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li><li><p>类型断言</p><p>类型断言类似于类型转换，但是不进行特殊的数据检查和解构。 它没有运行时的影响，只是在编译阶段起作用。</p><ul><li><p>类型断言尖括号语法</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> someValue<span class="token operator">:</span> <span class="token builtin">any</span> <span class="token operator">=</span> <span class="token string">"this is a string"</span><span class="token punctuation">;</span><span class="token keyword">let</span> stringLength<span class="token operator">:</span> <span class="token builtin">number</span> <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token operator">&lt;</span><span class="token builtin">string</span><span class="token operator">&gt;</span>someValue<span class="token punctuation">)</span><span class="token punctuation">.</span>length<span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre></li><li><p>类型断言as语法</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> stringLength<span class="token operator">:</span> <span class="token builtin">number</span> <span class="token operator">=</span> <span class="token punctuation">(</span>someValue <span class="token keyword">as</span> <span class="token builtin">string</span><span class="token punctuation">)</span><span class="token punctuation">.</span>length<span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ul></li></ul><h2 id="变量声明"><a href="#变量声明" class="headerlink" title="变量声明"></a>变量声明</h2><h3 id="var声明"><a href="#var声明" class="headerlink" title="var声明"></a>var声明</h3><p><strong>var变量提升机制</strong>：在全局作用域中或还是在局部作用域中，使用<code>var</code>关键字声明的变量，都会被提升到该作用域的最顶部。也就是说<code>var</code>声明可以在包含它的函数，模块，命名空间或全局作用域内部任何位置被访问。</p><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">function</span> <span class="token function">f</span><span class="token punctuation">(</span>shouldInitialize<span class="token operator">:</span> <span class="token builtin">boolean</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>    <span class="token keyword">if</span> <span class="token punctuation">(</span>shouldInitialize<span class="token punctuation">)</span> <span class="token punctuation">{</span>        <span class="token keyword">var</span> x <span class="token operator">=</span> <span class="token number">10</span><span class="token punctuation">;</span>    <span class="token punctuation">}</span>    <span class="token keyword">return</span> x<span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token function">f</span><span class="token punctuation">(</span><span class="token boolean">true</span><span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// returns '10'</span><span class="token function">f</span><span class="token punctuation">(</span><span class="token boolean">false</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// returns 'undefined'</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="let声明"><a href="#let声明" class="headerlink" title="let声明"></a>let声明</h3><p>当用<code>let</code>声明一个变量，它使用的是<em>词法作用域</em>或<em>块作用域</em>。</p><p><strong>块作用域特点</strong>：</p><ul><li>块作用域变量在包含他们的块或者for循环之外是不能访问的，catch里面的变量也如此。</li><li>变量不能在被声明之前读或写。 虽然这些变量始终“存在”于它们的作用域里，但在直到声明它的代码之前的区域都属于<em>暂时性死区</em>。</li></ul><h3 id="const声明"><a href="#const声明" class="headerlink" title="const声明"></a>const声明</h3><p>const声明的变量被赋值后不能再改变。</p><h2 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h2><p>TypeScript的核心原则之一是对值所具有的<em>结构</em>进行类型检查。 它有时被称做“鸭式辨型法”或“结构性子类型化”。 在TypeScript里，接口的作用就是为这些类型命名和为你的代码或第三方代码定义契约。</p><h3 id="接口示例"><a href="#接口示例" class="headerlink" title="接口示例"></a>接口示例</h3><pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">interface</span> <span class="token class-name">LabelledValue</span> <span class="token punctuation">{</span>    label<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">;</span><span class="token punctuation">}</span><span class="token keyword">function</span> <span class="token function">printLabel</span><span class="token punctuation">(</span>labelledValue<span class="token operator">:</span> LabelledValue<span class="token punctuation">)</span> <span class="token punctuation">{</span>    <span class="token builtin">console</span><span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>labelledValue<span class="token punctuation">.</span>label<span class="token punctuation">)</span><span class="token punctuation">}</span><span class="token keyword">let</span> obj <span class="token operator">=</span> <span class="token punctuation">{</span>size<span class="token operator">:</span> <span class="token number">10</span><span class="token punctuation">,</span> label<span class="token operator">:</span> <span class="token string">"ts"</span><span class="token punctuation">}</span><span class="token function">printLabel</span><span class="token punctuation">(</span>obj<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="可选属性"><a href="#可选属性" class="headerlink" title="可选属性"></a>可选属性</h3>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> typescript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JavaScript Basic Grammar</title>
      <link href="/2021/10/19/frontend/javascript/bian-cheng-yu-yan-javascript-basic-grammar/"/>
      <url>/2021/10/19/frontend/javascript/bian-cheng-yu-yan-javascript-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="【编程语言】JavaScript基础语法"><a href="#【编程语言】JavaScript基础语法" class="headerlink" title="【编程语言】JavaScript基础语法"></a>【编程语言】JavaScript基础语法</h1><blockquote><p>javascript 官网： <a href="https://www.javascript.com/">https://www.javascript.com/</a></p></blockquote><h2 id="javascript简介"><a href="#javascript简介" class="headerlink" title="javascript简介"></a>javascript简介</h2><h2 id="javascript环境配置"><a href="#javascript环境配置" class="headerlink" title="javascript环境配置"></a>javascript环境配置</h2>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【编程语言】Java Basic Grammar</title>
      <link href="/2021/10/19/java/grammar/bian-cheng-yu-yan-java-basic-grammar/"/>
      <url>/2021/10/19/java/grammar/bian-cheng-yu-yan-java-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="【编程语言】Java基础语法"><a href="#【编程语言】Java基础语法" class="headerlink" title="【编程语言】Java基础语法"></a>【编程语言】Java基础语法</h1>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> java </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Python Basic Grammar</title>
      <link href="/2021/10/19/python/grammar/bian-cheng-yu-yan-python-basic-grammar/"/>
      <url>/2021/10/19/python/grammar/bian-cheng-yu-yan-python-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="Python基础语法"><a href="#Python基础语法" class="headerlink" title="Python基础语法"></a>Python基础语法</h1>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Groovy Basic Grammar</title>
      <link href="/2021/10/19/groovy/grammar/bian-cheng-yu-yan-groovy-basic-grammar/"/>
      <url>/2021/10/19/groovy/grammar/bian-cheng-yu-yan-groovy-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="Groovy基础语法"><a href="#Groovy基础语法" class="headerlink" title="Groovy基础语法"></a>Groovy基础语法</h1><blockquote><p>groovy官网: <a href="https://groovy-lang.org/index.html">https://groovy-lang.org/index.html</a></p><p>groovy github:  <a href="https://github.com/apache/groovy">https://github.com/apache/groovy</a></p></blockquote><h2 id="Groovy简介"><a href="#Groovy简介" class="headerlink" title="Groovy简介"></a>Groovy简介</h2><blockquote><p>Apache Groovy is a <strong>powerful</strong>, <strong>optionally typed</strong> and <strong>dynamic</strong> language, with <strong>static-typing and static compilation</strong> capabilities, for the Java platform aimed at improving developer productivity thanks to a concise, <strong>familiar and easy to learn syntax</strong>. It integrates smoothly with any Java program, and immediately delivers to your application powerful features, including scripting capabilities, <strong>Domain-Specific Language</strong> authoring, runtime and compile-time <strong>meta-programming</strong> and <strong>functional</strong> programming. </p></blockquote><p>Apache Groovy 是一种强大的、可选类型的动态语言，具有静态类型和静态编译功能，用于 Java 平台，旨在通过简洁、熟悉且易于学习的语法提高开发人员的生产力。 它可以与任何 Java 程序顺利集成，并立即为您的应用程序提供强大的功能，包括脚本功能、领域特定语言创作、运行时和编译时元编程以及函数式编程。</p><h2 id="Groovy环境配置"><a href="#Groovy环境配置" class="headerlink" title="Groovy环境配置"></a>Groovy环境配置</h2><h2 id=""><a href="#" class="headerlink" title=""></a></h2>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 解释型语言 </tag>
            
            <tag> groovy </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【编程语言】Golang Basic Grammar</title>
      <link href="/2021/10/18/golang/grammer/bian-cheng-yu-yan-golang-basic-grammar/"/>
      <url>/2021/10/18/golang/grammer/bian-cheng-yu-yan-golang-basic-grammar/</url>
      
        <content type="html"><![CDATA[<h1 id="Golang基础语法"><a href="#Golang基础语法" class="headerlink" title="Golang基础语法"></a>Golang基础语法</h1><blockquote><p>go 官网: <a href="https://golang.org/">https://golang.org/</a></p><p>go 包整理: <a href="https://go.dev/">https://go.dev/</a></p><p>go github地址:  <a href="mailto:git@github.com">git@github.com</a>:golang/go.git</p></blockquote><h2 id="golang简介"><a href="#golang简介" class="headerlink" title="golang简介"></a>golang简介</h2><p>go语言（或 Golang）是Google开发的开源编程语言，诞生于2006年1月2日，于2009年11月开源，2012年发布go稳定版。Go语言在多核并发上拥有原生的设计优势，Go语言从底层原生支持并发，无须第三方库、开发者的编程技巧和开发经验。</p><h2 id="golang环境配置"><a href="#golang环境配置" class="headerlink" title="golang环境配置"></a>golang环境配置</h2><blockquote><p>具体golang环境配置参考<a href="https://blog.evcoder.com/2021/10/19/gvm/">https://blog.evcoder.com/2021/10/19/gvm/</a></p></blockquote><h2 id="golang基础"><a href="#golang基础" class="headerlink" title="golang基础"></a>golang基础</h2>]]></content>
      
      
      <categories>
          
          <category> 编程语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 编译型 </tag>
            
            <tag> go </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
